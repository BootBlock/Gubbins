# Continuous scan & batch actions

When you've got a *pile* of things to process — a delivery to receive, a shelf to relocate, a kit
to sign out — scanning them one at a time is slow. **Continuous scan** keeps the camera running
and collects everything you scan, so you can act on the whole batch at once.

**Where to find it:** the continuous mode inside the [[scanner|Camera-Scanning]] (**Scan**).

## Scanning a batch

In continuous mode, Gubbins keeps decoding without stopping after each hit — scan one code after
another and each recognised item is added to a running list. You see what you've captured as you
go.

## Acting on the whole batch

Once you've scanned a batch, apply one action to **all** of them together, for example:

- **Move all** to a [[location|Locations-and-Stock]] — great for putting a delivery away.
- **Check out all** to a [[contact|Contacts]] — sign out a whole kit in one go.

> **💡 Tip**
> Continuous scan turns a tedious job into a quick sweep: scan every box off the van, then
> **move all** to *Receiving* in one action instead of editing each item.

## Knowing a scan landed, without looking

Each item that joins the list gets a confirmation beep and a buzz, so you can keep your eyes on
the shelf rather than the screen. (Both can be turned off under **Settings → Scanning & labels**
if you'd rather scan quietly.)

A code you scan twice isn't counted twice — the batch tracks distinct items. A repeat gets its own
**lower, shorter tone** and an *Already scanned* message instead of the usual confirmation, so you
can hear the difference between "that one's already on the list" and "that didn't read at all".
Silence means the code wasn't read: move a little closer, steady your hand, and try again.

> **ℹ️ Note**
> Leaving a label sitting in the viewfinder won't add it over and over. Gubbins settles on a code
> for a couple of seconds after reading it, so one sweep past a label counts once — and it isn't
> re-checked against your inventory in the meantime, which keeps a long batch easy on the battery.

## Related pages

- **[[Camera scanning|Camera-Scanning]]** — single-item scanning and how decoding works.
- **[[QR codes & label printing|QR-Codes-and-Label-Printing]]** — the labels that make batches
  fast.
- **[[Bulk edit & clone|Bulk-Edit-and-Clone]]** — the same idea for items you select on screen.
