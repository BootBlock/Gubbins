# Full codebase audit — phased plan and findings register

> **Status:** 🟢 ACTIVE — Phases 0–17 complete; Phase 18 (Runtime performance at scale) is next.

This is the single source of truth for a whole-repository audit of Gubbins. Its purpose is to find
every **genuine** mechanical, functional, performance and prompt defect in the codebase and log each
one as a GitHub issue — nothing more. **The audit fixes nothing.** Fixing is the job of the ordinary
issue workflow in `CLAUDE.md`, one issue at a time, after this audit has produced the backlog.

It is a **living document**. Each phase updates the phase map (§8), the findings register (§10), the
carry-over list (§11) and the change log (§13) in the same change that lands its work, so the next
session can pick up from the document alone. A phase is started by pasting its prompt (§9) into a
fresh session.

Sibling audits already on record, kept so their conclusions are not re-derived:
[`done/feature-gap-audit_2026-07-09.md`](done/feature-gap-audit_2026-07-09.md) (feature gaps
against comparable tools), [`done/ui-bodge-audit.md`](done/ui-bodge-audit.md) (design-system
bodges) and the 2026-06-29 security audit (no exploitable defect; CSP tightened to zero inline
scripts). This audit is broader than all three and does not repeat their scope questions; it looks
for defects in what exists.

---

## 1. Scope

**Everything tracked in the repository is in scope**, partitioned into the phases in §8 so that each
session has a bounded reading list. The partition is by directory, so nothing falls between two
phases; the phase map names every top-level path once. Where a finding's root cause lies outside the
phase that noticed it, the finding is still verified and filed — the register records which phase it
belongs to.

Out of scope, deliberately:

- **Fixing.** Not even a one-line fix. A phase that fixes something has stopped auditing.
- **Feature gaps.** "Gubbins does not do X" is a feature request, not a defect, unless the app, the
  wiki or a tool description *claims* it does X. Feature gaps were the subject of the earlier audit
  above and are filed as `enhancement` issues only when a claim is contradicted.
- **Style preferences.** A finding must name a consequence a user, an integrator, an agent or a
  future maintainer will actually suffer. "I would have written this differently" is not one.
- **The sibling programmes that are already tracked as issues** — see §4.2. A phase may file a
  *new, specific* defect inside one of those areas, but never a restatement of the programme.

## 2. What counts as a finding

Four classes. Every finding is tagged with exactly one primary class in the register; the issue's
labels come from the class and the area (§5.3).

### 2.1 Mechanical

The code does not do what the code says it does, or does it unsafely. Examples: an unhandled
rejection; a missing `await`; a race between an effect and its cleanup; an event listener, timer or
worker that is never released; a swallowed error; a cast (`as any`, `as unknown as`, a `!` assertion)
that hides a real `null`; a `switch` over a domain union without the exhaustive guard; SQL that
interpolates an identifier; a multi-statement write outside a transaction; a persisted-store shape
read back without reconciliation; a seam bypassed where `CLAUDE.md` or a memory note says it must
not be (§7 lists them with a grep for each); a build, lint, hook or CI step that cannot fail or does
not run what it says it runs; a test that cannot fail.

### 2.2 Functional

The code does what it says, but what it says is wrong for the user. Examples: behaviour that
contradicts the wiki, a tool description, a label, a tooltip or the issue that introduced it; an
off-by-one at a page, day or currency boundary; a timezone or DST shift; a validation gap that lets a
bad value in or a good value out; an unreachable state or control; a permission the ACL layer does
not enforce; two devices that do not converge after sync; an offline path that silently loses a
write; a keyboard, focus or screen-reader path that does not work; a copy string that misleads.

### 2.3 Performance

The code produces the right result at a cost the target hardware cannot pay. Gubbins must run from
a low-end phone to a desktop (issue #112), and visual effects are not to be removed to get there.
Examples: an unbounded query; a `SELECT *` or a blob column read into a list; a correlated subquery
per row on a paginated scan; an N+1 loop of round-trips through the worker RPC; an invalidation that
refetches everything on every write; a component that re-renders a whole list on one keystroke; a
list that is not virtualised; an image decoded at full size for a thumbnail; work on the main thread
that belongs in the worker; object churn in a hot loop or an animation frame; a chunk that loads
eagerly but is used rarely; a service-worker precache that has grown past what a first install can
afford. A performance finding needs a **measurement** (§3.3), not an opinion.

### 2.4 Prompt

Text that another program or a model reads and acts on, where the text is ambiguous, wrong, stale or
self-contradictory. Two surfaces:

- **Shipped to integrators and assistants:** the MCP tool names, schemas and descriptions
  (`bridge/src/mcp/tools.ts`), the spoken-answer shaper (`bridge/src/spoken.ts`), the Home Assistant
  intent sentences and strings (`homeassistant/custom_sentences/`, `custom_components/gubbins/`),
  the OpenAPI document (`bridge/openapi.yaml`), the OData metadata, and the wiki page an assistant
  user is pointed at. A tool whose description promises a filter the dispatcher ignores, a schema
  whose enum is narrower than the database's, an error string that leaves the model no way to
  recover, a sentence the intent parser cannot match — these are prompt defects.
- **Read by agents and contributors working on the repository:** `CLAUDE.md`, `AGENTS.md`,
  `.claude/skills/*/SKILL.md`, the issue and PR templates, `docs/dev/*`, the live plans in
  `docs/todo/`, `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `bridge/README.md`,
  `extension/README.md`. A command that no longer exists, a rule that contradicts another rule, a
  path that moved, a count that went stale, an instruction that a session cannot follow as written
  — these are prompt defects too, and they are cheap to verify: run the command, open the path.

## 3. The verification standard (mandatory)

**A candidate becomes a finding only when it has been demonstrated, not argued.** This is the
project's standing rule — check the real state, never infer it — applied to the audit. A confident
wrong issue costs more than a missed one, because it gets actioned.

### 3.1 Every finding needs all of these

1. **A location.** `path:line` at the pinned commit (§6.2). "Somewhere in sync" is not a location.
2. **A demonstration**, one of:
   - a scratch test that fails on the current code for the stated reason (§3.2);
   - a script, query or command whose output shows the defect (paste the output);
   - the app driven in a real browser with the `verify` skill, with the observed vs expected state
     written down (and a screenshot where a screenshot is the evidence);
   - for performance, a measurement (§3.3);
   - for prompt defects, the text quoted beside the behaviour or state that contradicts it, and — for
     a stale command or path — the command run and its failure shown.
3. **A consequence.** Who is affected and how (§5.2 severity). A defect with no consequence is
   rejected, and the rejection is recorded so the next phase does not re-find it.
4. **A dedupe check** against open *and* closed issues, this register, and the deliberate-decision
   sources (§4).
5. **A re-check against the tip of `main`** immediately before filing, because `main` advances while
   a phase runs and a finding fixed in the meantime is a duplicate, not a finding.

### 3.2 Scratch tests

A scratch test is the preferred demonstration for a mechanical or functional finding because it can
be re-run by whoever actions the issue. Rules:

- Name it `<subject>.audit-scratch.test.ts(x)` beside the code it exercises, inside the phase's
  worktree. The suffix makes it visible in `git status`; **none is ever committed** — the landing
  step (§6.6) greps for the suffix and refuses to commit if one is present.
- Run it from the worktree with `npx vitest run --config vitest.worktree.config.ts <file>`
  (never junction `node_modules` for Vitest — two instances). Bridge code uses
  `--config bridge/vitest.config.ts` from the primary checkout only; a bridge scratch test therefore
  lives in the scratchpad and is copied in for the run.
- The test must **fail for the stated reason**. Read the failure. A test that fails because of a
  fixture typo proves nothing, and a test that passes has disproved the candidate — record that.
- Paste the essential assertion and the failure output into the issue body, so the fixer can
  reproduce it without the scratch file.

### 3.3 Performance measurements

- **SQL:** build the real schema from
  `src/db/migrations/__fixtures__/schema-baseline.snapshot.json`, seed in chronological order, and
  measure with **no `ANALYZE` and no `PRAGMA optimize`** — the shipped planner has no statistics. Report
  `EXPLAIN QUERY PLAN` output and ratios between shapes, not absolute milliseconds. The memory note
  `Benchmarking index changes no stats` has the full recipe.
- **Rendering and interaction:** drive the app with Playwright against a seeded vault, with CDP CPU
  throttling (4×) to stand in for a low-end phone, and read the React Profiler or a performance
  trace. Count renders or long tasks; do not eyeball.
- **Bundle and precache:** `npm run build` and `npm run check:bundle`; compare the chunk map to what
  a cold first paint actually needs.
- **Memory:** a heap snapshot before and after a repeated action; growth that does not plateau is
  the finding.
- State the hardware and the seed size in the issue. A claim that cannot be re-measured is not filed.

### 3.4 Unverifiable candidates

A candidate the verifier cannot demonstrate here — it needs a device, a network, a second machine, a
Home Assistant instance — is **not** filed. It goes to §11 with a note of exactly what would settle
it. The close-out phase reviews §11 with the maintainer.

## 4. Dedupe and deliberate decisions

### 4.1 The dedupe check

Before filing, search **all** issues, not just open ones — 565 are closed and many closed ones
document a decision:

```bash
gh issue list --repo BootBlock/Gubbins --state all --limit 1000 --search "<two or three distinctive words>" --json number,title,state,labels
```

Search twice with different words (the symptom and the location). A closed issue that describes the
same defect means either it regressed (file a new issue that links the old one and says so) or the
fix was partial (file the residue, narrowly). A `wontfix` closure is a deliberate decision — do not
re-file; record the candidate as rejected with the issue number.

Also check: this register (§10) including other phases' rejections; §11; the "deliberate non-goals"
and "considered but not flagged" sections of the plan docs in `docs/todo/` and `docs/todo/done/`;
the memory notes the session-start hook lists (a note that explains *why* something is the way it
is — the OAuth implicit flow, the unconditional `ON CONFLICT DO UPDATE`, the number field that
reports and never rewrites — is a decision, not a defect); and the code's own comments, which in
this repository frequently record the decision beside the code.

### 4.2 Tracked programmes — do not restate

These are open, known and owned. A phase files a finding in one of these areas only when it is
**new and specific** — a concrete defect the programme's issues do not already name.

| Programme | Issues |
| --- | --- |
| i18n conversion of the remaining screens and strings | #60, #213, #224–#244, #682 |
| Accessibility sweep findings | #209–#223, #491, #515, #541–#553, #671 |
| Full performance pass | #112 (this audit's Phase 18 *is* that pass; its findings link to #112) |
| Repository metadata and settings audit | #454 (Phase 0 covers the file side; the settings side stays with #454) |
| Wiki coverage gaps | #53, #263–#271, #395, #580, #581, #585, #611 |
| Backlog triage order | #403 |
| Dependency upgrades blocked upstream | #186, #401 |

## 5. Issue format

Issues are world-readable and permanent. They describe **what** is wrong and **why it matters**, never
the plumbing that found it — no worktree, sub-agent, scratch-test, phase or register names, no
tool-call narration, no agent reasoning. The scratch test's *assertion* is evidence and belongs in
the issue; the fact that a scratch test was the method does not.

### 5.1 Title

One factual sentence, in the repository's house style: **what does what, so what happens** — e.g.
*"A loan created from a booking is due at the start of the booking's last day, so it is overdue for
the whole of it"*. No severity prefix, no `[area]` tag, no "Bug:" — labels carry those. The title
must be true on its own; a reader who sees only the title in a list should know what is broken.

### 5.2 Body

Write it to a file and pass `--body-file`; never inline a multi-line body.

```markdown
## What happens
<the observed behaviour, in user or integrator terms>

## Where
[path/to/file.ts:123](https://github.com/BootBlock/Gubbins/blob/<sha>/path/to/file.ts#L123)
<one line per relevant location; permalinks to the pinned commit, not to `main`>

## How to reproduce / how it was verified
<steps, or the assertion and its failure output, or the measurement with its setup>

## Expected
<what should happen, with the source of that expectation: the wiki page, the tool description,
the label, the issue that introduced the behaviour, or the invariant in CLAUDE.md>

## Impact
Severity: <data-loss | wrong-data | unusable | degraded | cosmetic>
<who is affected, under what conditions, how often>

## Suggested direction (optional)
<a sentence or two; never a patch, and never the only place the defect is explained>

---
This issue was opened by an agent on behalf of @BootBlock.
```

The five severities follow the ordering in #403: silent loss or corruption first, wrong-but-present
data second, unusable third, then degraded, then cosmetic. Related issues are linked with `#N` in the
body (a regression links the closed original; a performance finding links #112).

### 5.3 Labels

Use only the repository's existing labels. One nature label plus at least one area label:

| Class (§2) | Nature label |
| --- | --- |
| Mechanical, Functional | `bug` (or `enhancement` when the code is correct but a claim is unmet) |
| Performance | `performance` (+ `bug` when a limit is exceeded in normal use) |
| Prompt — shipped text | `bug` + `mcp` / `bridge` / `documentation` as fits |
| Prompt — agent guidance | `documentation` (+ `github` for `.github/` and repository plumbing) |

| Area | Label(s) |
| --- | --- |
| `src/db/**`, repositories, queries | `backend`; `schema` when the data model or a migration is implicated |
| `src/features/sync`, backup, archive, storage, danger-zone | `sync` (+ `backend`) |
| Foundry, screens, dialogs | `ui`; add `usability` when the defect is a hard-to-use control, `accessibility` for AT/keyboard, `theming` for tokens and themes, `mobile` for touch or narrow-screen |
| Inventory, items, custom fields | `ui` or `backend` by layer; `schema` for the item model; `categories`, `locations`, `measurements` as fits |
| Purchasing, suppliers, pricing | `pricing` |
| Projects, BOM, kits | `projects` |
| Reports, dashboard | `reports`, `dashboard` |
| Search, command palette | `search` |
| Scanner, OCR, barcode | `scanner` |
| Import, export, migration mappers | `import/export` |
| Bridge (all of `bridge/**`) | `bridge`; `mcp` for the MCP server; `deployment` for Docker and hosting |
| `custom_components/`, `homeassistant/` | `bridge` |
| `extension/` | `scanner` is wrong — use `import/export` for scraping and `security` if the fetch gate is implicated |
| i18n catalogs and text | `i18n` |
| `.github/`, hooks, scripts, CI | `github`; `github_actions` for workflows; `dependencies` for the lockfile and packages |
| `docs/**` | `documentation` |
| Any secret, privacy or trust-boundary finding | `security` (and stop and tell the maintainer before filing if it is exploitable — see `SECURITY.md`) |

If no existing label fits, note that in the register and file with the nearest; never invent one.

### 5.4 Filing

```bash
gh issue create --repo BootBlock/Gubbins --title "<title>" --body-file <file> --label bug --label backend
```

Record the returned number in the register the same session. One issue per defect: two symptoms of
one root cause are one issue that lists both; two defects that happen to share a file are two issues.

## 6. How a phase runs

### 6.1 Start

1. Read this document in full, then `CLAUDE.md` and `AGENTS.md`. Read every memory note the
   session-start hook lists whose title touches the phase's area — they hold the deliberate decisions
   that §4.1 says not to re-file.
2. Confirm the phase is the next one in the §8 map and that no earlier phase is marked in progress.
   If one is, stop and tell the maintainer — two sessions on one register conflict.
3. Mark the phase **in progress** in §8 (the very first edit in the worktree, committed on its own so a
   concurrent session sees it).

### 6.2 Worktree and pinned commit

Every phase edits this document, so every phase runs in a worktree — the `CLAUDE.md` rule, no
exception:

```bash
git fetch origin
git worktree add .claude/worktrees/wt-audit-p<N> -b wt-audit-p<N> origin/main
git -C .claude/worktrees/wt-audit-p<N> rev-parse HEAD   # the pinned SHA for this phase's permalinks
```

The worktree must sit under `.claude/worktrees/` (a sibling path breaks test resolution). All reading
and all scratch tests happen in the worktree, so every `path:line` in the register refers to one
known commit. Prefix every shell command with `cd <worktree> &&` or `git -C <worktree>`; the shell's
working directory does not reliably persist between calls, and a check run in the wrong tree looks
exactly like a check run in the right one.

### 6.3 Find

Split the phase's scope into independent units (the phase sections in §9 suggest a split) and
dispatch one **finder** sub-agent per unit, concurrently. A finder is read-only. Give it:

- the unit's file list, the four classes (§2), the invariant checklist (§7), and the phase's focus
  list (§9);
- the instruction to report **candidates**, not findings: for each, `path:line`, the class, the claim
  in one sentence, why it believes the claim, and how it could be demonstrated;
- the instruction to report what it read and what it skipped, so coverage is auditable.

A finder that reports nothing must say what it read. A unit the finder skipped is re-dispatched.

### 6.4 Verify

For each candidate, dispatch a **verifier** sub-agent (batch candidates that share a mechanism).
The verifier's brief is adversarial: *try to disprove this*. It reads the code path end to end, runs
the scratch test or measurement, checks the deliberate-decision sources, and returns one of:

- **CONFIRMED** — with the demonstration (§3.1 item 2) and the severity;
- **REJECTED** — with the reason (the code handles it at `path:line`; the behaviour is documented as
  intended in `<source>`; the test passed);
- **UNVERIFIABLE** — with exactly what would settle it.

The lead does not accept a CONFIRMED verdict without the demonstration attached. A verifier that
returns "looks right to me" has not verified anything — send it back. The lead spot-checks at least
one CONFIRMED and one REJECTED verdict per unit by re-running the demonstration.

### 6.5 File and record

For each CONFIRMED candidate: dedupe (§4.1), re-check against `main`'s tip (§3.1 item 5), write the
body (§5.2), file (§5.4), and add the register row (§10) with the issue number. For each REJECTED and
UNVERIFIABLE candidate: add the register row with the verdict and reason. **Every candidate gets a
row** — the register's value to later phases is mostly its rejections.

### 6.6 Land

1. Update §8 (phase status, counts), §10, §11 and §13.
2. `git -C <worktree> status` must show no `*.audit-scratch.test.*` file and no file outside
   `docs/todo/`. If it does, remove the scratch files and revert anything else — the audit does not
   change code.
3. Format (`npx prettier --write docs/todo/codebase-audit_2026-08-30.md`), run
   `npx vitest run --config vitest.worktree.config.ts src/lib/docs-todo-status.test.ts` from the
   worktree, commit with a message that names the phase, merge into `main` with `--no-ff`, and push
   with the shell's cwd in the primary checkout (`cd p:/Source/TypeScript/Gubbins && git push origin
   main`), never `git -C` from the worktree.
4. Remove the worktree. If a `node_modules` junction was created for a dev server, remove the junction
   **first** and confirm the link path is gone before `git worktree remove`.
5. Hand the maintainer the next phase's prompt (§9) in a raw fenced markdown block, with the counts:
   candidates, confirmed, rejected, unverifiable, duplicates, issues filed.

### 6.7 What a phase must not do

- Fix, refactor, format or "tidy" any file outside this document.
- Re-file a §4.2 programme, a §4.1 decision, or a rejection already in §10.
- File without a demonstration, or file a performance claim without a measurement.
- Leak process into an issue (§5).
- Estimate or ration by time. There are no time constraints on Gubbins work; a phase that has not
  finished its scope is not finished.

## 7. Project-invariant checklist

Each row is a seam or rule the project has established, with a grep that surfaces a *possible* bypass.
A hit is a candidate, never a finding — several seams have sanctioned exceptions, and a guard test
already covers some rows (a hit there means the guard has a hole, which is itself a finding). Finders
run the rows relevant to their unit; Phase 17 runs every row across the whole repository.

| Invariant (source) | Bypass looks like | Grep (ripgrep) |
| --- | --- | --- |
| Calendar-day arithmetic is DST-safe (`lib/calendar-days`) | `+ n * MS_PER_DAY`, `86400000`, `24 * 60 * 60 * 1000` | `rg -n "MS_PER_DAY|86400000|24 \* 60 \* 60" src bridge/src --glob '!**/calendar-days*'` |
| Date inputs convert through `lib/date-input` | local-midnight `new Date(y, m, d)` or `T00:00` near an `<input type="date">` | `rg -n "type=\"date\"" src` then read the handler |
| Money rounds through `lib/money` | `Math.round(x * 100) / 100`, `toFixed(2)` on a price | `rg -n "\* 100\) / 100|toFixed\(2\)" src bridge/src` |
| Prices render via the `Money` control | `.currency(` in JSX, under any formatter binding | `rg -n "\.currency\(" src --glob '*.tsx'` (Phase 17 correction: the original `fmt\.currency\(` finds only 2 of the 8 real hits — the other 6 are bound as `f.currency(` or `formatters.currency(`) |
| Natural keys compare through `lib/name-fold` | `LOWER(`, `COLLATE NOCASE`, `.toLowerCase() ===` on a name/tag/username | `rg -n "COLLATE NOCASE|LOWER\(" src/db bridge/src` (note #577 covers sorting) |
| Thrown values become copy via `useErrorMessage` | `e instanceof Error ? e.message : ` | `rg -n "instanceof Error \? " src` |
| Every `switch` over a domain union ends in `assertExhaustive` | a `default:` that returns or throws by hand, or no default | `rg -n "switch \(" src bridge/src -A 30 \| rg -n "default:" ` then inspect |
| Import file reads go through `features/import/file-source` | `.text()`, `readAsText`, `arrayBuffer()` on a `File` elsewhere | `rg -n "\.text\(\)|readAsText|\.arrayBuffer\(\)" src --glob '!**/file-source*'` |
| A destructive path saves via `lib/save-file` and proves it landed | `downloadBlob(` followed by a delete/overwrite | `rg -n "downloadBlob\(" src` then read what follows |
| `gubbins:` storage keys are registered (`lib/storage-keys`) | a literal key elsewhere, or an unprefixed key | `rg -n "localStorage\.(get\|set\|remove)Item\(" src --glob '!**/storage-keys*'` |
| Persisted Zustand stores reconcile on read (`lib/persisted-state`) | `persist(` without a custom `merge` | `rg -n "persist\(" src/state src/features -A 15 \| rg -n "merge"` |
| `driver.query<TRow>` is build-checked against literal SQL | runtime-assembled SQL passed to `query<` | `rg -n "query<" src/db -B 2 -A 2` and look for template concatenation |
| Stock operations have derived, idempotent ids (#633, #696) | `randomblob()` or `crypto.randomUUID()` for a row that a retry could recreate | `rg -n "randomblob\(\)|randomUUID\(\)" src/db bridge/src` |
| Sync-set tables are classified (`sync-table-classification.test.ts`) | a new table in the baseline with no classification | run the test; read the list for tables whose classification looks wrong |
| LWW is decided in JavaScript, restore has no gate | an immutability trigger on a synced table; a restore path that assumes a gate | read `reconcile.ts` and the trigger list together |
| "Mirrors X" comments have a drift test | `mirror`, `must stay in sync`, `keep in sync`, `identical to`, `matches the` with no named test | `rg -n -i "mirrors\|stay in sync\|keep in sync\|kept in sync\|identical to" src bridge/src extension/src` |
| The bridge runs app `.ts` under Node's strip-only loader | parameter properties, `enum`, `namespace`, `import x = ` in any file the bridge imports | `rg -n "^\s*(export )?(const )?enum \|namespace \|constructor\((private\|public\|readonly)" src/db src/features/search src/lib` |
| Design tokens, not literals | `#[0-9a-f]{3,6}`, `rgb(`, `oklch(`, `-500`, `cubic-bezier(`, `@keyframes` outside `index.css` | `rg -n "#[0-9a-fA-F]{6}\b|rgb\(|oklch\(|cubic-bezier\(|@keyframes" src --glob '!src/styles/index.css'` |
| Foundry primitives, not native controls | `<select`, `<input` outside foundry and outside the sanctioned list in `foundry-native-inputs.test.ts` | run that test; then `rg -n "<select\|<textarea" src --glob '!src/components/foundry/**'` |
| Content hides on `handset:`, not a bare breakpoint (#546 is open) | `max-md:hidden`, `md:hidden`, `hidden md:` | `rg -n "max-\w+:hidden\|\bmd:hidden\|\bsm:hidden" src` |
| Hover reveals have a `touch:` counterpart (guard test) | run `hover-reveal-touch.test.ts`; hits are a guard hole | — |
| Every user-facing string in a converted screen goes through `t()` | a literal in Dashboard, About, global chrome, Modules | `rg -n ">[A-Z][a-z]+ [a-z]" src/features/dashboard src/features/about src/components/nav --glob '*.tsx'` |
| No inline `<script>` (CSP) | `<script>` in `index.html` or injected markup | `rg -n "<script" index.html src` |
| Errors are not swallowed | `catch {}`, `catch (e) {}`, `.catch(() => {})` | `rg -n "catch \{\s*\}\|catch \(\w+\) \{\s*\}\|\.catch\(\(\) => \{\}\)" src bridge/src extension/src` |
| Types are not lied about | `as any`, `as unknown as`, `@ts-ignore`, `@ts-expect-error` without a reason, `!` on a value that can be null | `rg -n "as any\|as unknown as\|@ts-ignore\|@ts-expect-error" src bridge/src extension/src` |
| Lint is not silenced without a reason | `eslint-disable` | `rg -n "eslint-disable" src bridge/src extension/src scripts` |
| Tests can fail | `.skip(`, `.only(`, `expect(true)`, a test body with no `expect`, a `try { … } catch { /* ok */ }` around the assertion | `rg -n "\.skip\(\|\.only\(\|expect\(true\)" src bridge/src` |
| Effects clean up | `addEventListener`, `setInterval`, `setTimeout`, `new Worker`, `observe(` inside `useEffect` with no matching removal in the return | `rg -n "useEffect" src -A 25 \| rg -n "addEventListener\|setInterval\|observe\("` then inspect |
| Async work is cancelled on unmount or on the next call | `useEffect(() => { … fetch/query … })` with no abort or "stale" flag | read every effect that awaits |
| Lists have stable keys | `key={i}`, `key={index}` | `rg -n "key=\{(i\|index\|idx)\}" src` |
| Queries are bounded | `SELECT *`, no `LIMIT` on a list, `OFFSET` pagination | `rg -n "SELECT \*" src/db bridge/src` ; `rg -n "OFFSET" src/db bridge/src` |
| Agenda writes sweep the `['agenda']` key prefix | a mutation that reshapes a lane without the sweep | `rg -n "agendaKeys\|\['agenda'\]" src` against the list of lane-shaping mutations |
| Serialised items: at most one open checkout; bookings: no overlap; one preferred supplier part | enforced post-merge in `reconcile.ts`, plus repair on every write path | read each write path for the repair |

## 8. Phase map

| Phase | Title | Scope (paths) | Status | Cand. | Filed | Rejected | Unverif. |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | Baseline, build, tooling, CI and repository configuration | `package.json`, `package-lock.json`, `tsconfig*.json`, `vite*.config.ts`, `vitest.*.ts`, `eslint.config.js`, `prettier.config.js`, `components.json`, `index.html`, `public/`, `scripts/`, `.githooks/`, `.github/`, `Dockerfile`, `docker/`, `docker-compose.yml`, `Run.bat`, `Run.ps1`, `hacs.json`, `.gitignore`, `.gitattributes`, `.dockerignore`, `.editorconfig`, `.env.example`, `.git-blame-ignore-revs`, `.nvmrc`, `.prettierignore`, `vitest.timeouts.ts`, `LICENSE`, `bridge/{package.json,Dockerfile,tsconfig.json,vitest.config.ts,*.mjs}`, `extension/{build.mjs,tsconfig.json,manifest.json}` | complete | 54 | 31 | 11 | 3 |
| 1 | Database engine, driver, migrations and shared repository seams | `src/db/*.ts`, `src/db/worker/`, `src/db/rpc/`, `src/db/migrations/`, `src/db/search/`, `src/db/repositories/{base,mappers,constants,like,tombstone,text-limits,name-lookup,location-count,receipt-guard,reservations,stock,stock-batches,supplier-cost-sql,checkout-plan,gauge,location-history,index}.ts`, `src/db/repositories/types/`, `src/test/` | complete | 77 | 34 | 30 | 1 |
| 2 | The item repository family | `src/db/repositories/ItemRepository.ts`, `src/db/repositories/item/`, `ItemRepository.*.test.ts`, `serialised-placement.test.ts`, `batched-item-reads.test.ts`, `*-parity.test.ts` | complete | 56 | 33 | 10 | 1 |
| 3 | Every other repository | remaining `src/db/repositories/*Repository.ts` and their tests, `src/db/repositories/project/`, `permissions.enforcement.test.ts`, `wishlist.test.ts`, `revaluation.test.ts`, `item-relations.test.ts`, `tare-presets.test.ts`, `test-record.test.ts` | complete | 110 | 52 | 33 | 3 |
| 4 | Data integrity: sync, backup, archive, storage, danger zone | `src/features/sync/`, `backup/`, `archive/`, `storage/`, `danger-zone/`, `clock-skew/`, `events/`, `src/lib/{save-file,download,read-all-pages}.ts` | complete | 64 | 40 | 14 | 1 |
| 5 | App shell, platform and shared libraries | `src/{main,App,sw,csp,base-path}.ts(x)`, `src/app/`, `src/routes/`, `src/routeTree.gen.ts`, `src/state/`, `src/lib/` (all), `src/lib/env/`, `src/styles/index.css`, `src/features/{errors,hotkeys,modules,i18n,not-found,lab,about,achievements}/`, `src/components/{OfflineIndicator,PwaUpdatePrompt,useConfirmSaved}.tsx`, `public/recovery.js`, `public/coi-bootstrap.js` (runtime behaviour) | complete | 112 | 55 | 31 | 1 |
| 6 | Foundry primitives and shared components | `src/components/foundry/`, `src/components/background/`, `src/components/nav/`, `src/components/icons/`, `src/components/Brand*.tsx` | complete | 79 | 32 | 20 | 1 |
| 7 | Inventory domain logic | `src/features/inventory/*.ts(x)` (top level), `dedupe/`, `importers/`, `labels/`, `ocr/`, `regions/` | complete | 88 | 69 | 5 | 0 |
| 8 | Inventory components | `src/features/inventory/components/` | complete | 123 | 72 | 22 | 0 |
| 9 | Commerce, projects and reporting | `src/features/{purchasing,suppliers,sales,projects,lifecycle,reports,dashboard,export,import}/` | complete | 80 | 59 | 16 | 0 |
| 10 | Capture and discovery | `src/features/{search,command-palette,scanner,scraping,lookups,share,images}/` | complete | 112 | 74 | 21 | 1 |
| 11 | People, time and configuration screens | `src/features/{users,contacts,bookings,calendar,alerts,activity,maintenance,tags,settings,home-assistant,webhooks}/` | complete | 120 | 56 | 25 | 1 |
| 12 | Bridge core and HTTP API | `bridge/src/*.ts` (top level), `bridge/src/api/`, `bridge/src/fixtures/`, `bridge/openapi.yaml`, `bridge/loader.mjs`, `bridge/README.md` | complete | 77 | 43 | 13 | 0 |
| 13 | Bridge integrations | `bridge/src/{events,feeds,ical,mqtt,mdns,mcp,homeassistant}/`, `bridge/webhooks.example.json`, `bridge/gubbins-bridge.service`, `bridge/scripts/` | complete | 95 | 50 | 28 | 0 | |
| 14 | Satellites: browser extension and Home Assistant component | `extension/src/`, `extension/README.md`, `custom_components/gubbins/`, `homeassistant/`, `README-HA.md` | complete | 52 | 31 | 8 | 0 |
| 15 | Prompt and guidance surface | MCP tool text in `bridge/src/mcp/tools.ts`, `bridge/src/spoken.ts` output, HA sentences and `strings.json`/`translations/`, OpenAPI and OData descriptions, `src/features/i18n/catalogs/*.json` (text quality), `CLAUDE.md`, `AGENTS.md`, `.claude/skills/`, `.github/ISSUE_TEMPLATE/`, `.github/pull_request_template.md`, `docs/dev/`, live `docs/todo/*.md`, `README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/modular-ui-plan.md` | complete | 74 | 27 | 30 | 0 |
| 16 | Wiki parity | `docs/wiki/` (every page, every image) against the app at the pinned commit; `docs/todo/wiki_2026-07-11.md` page map | complete | 391 | 73 | 85 | 0 |
| 17 | Cross-cutting static sweeps | the whole repository: every §7 row; dead code and unused exports; duplicate implementations of an existing seam; TODO/FIXME/HACK; test-suite quality; dependency licences and `npm audit` | complete | 43 | 20 | 9 | 0 |
| 18 | Runtime performance at scale | the running app and bridge with a seeded large vault; ties to #112 | not started | | | | |
| 19 | Close-out | this document; every issue filed by the audit | not started | | | | |

Coverage check: every top-level path in the repository appears in exactly one phase's scope above
(`dist/` and `node_modules/` are build output and are not audited; `extension/dist/` likewise).
Phase 0 adds any path this table missed before it starts, and records the addition in §13.

Phase 0 ran that check. All 12 top-level directories and all 30 top-level files are covered. Four
paths below the top level were named by no phase and are now assigned: `bridge/.env.example` and
`bridge/.gitignore` (Phase 0), `src/vite-env.d.ts` (Phase 5), and `docs/todo/done/` (Phase 15, as a
reference read — archived plans are decision sources under §4.1, not audit targets). Two deliberate
overlaps are left as they are: `bridge/loader.mjs` sits in both Phase 0 (`bridge/*.mjs`) and Phase 12,
and `.github/ISSUE_TEMPLATE/` sits in both Phase 0 (mechanics) and Phase 15 (text quality).

## 9. Phases

Each phase below has a **scope** (its row in §8), a **focus** list (what a finder should look for
beyond §2 and §7 in this area), the **verification** methods that fit the area, and the **prompt**
that starts it. The prompt is complete on its own; it does not need this section pasted with it.

### Phase 0 — Baseline, build, tooling, CI and repository configuration

**Focus.** Every gate that is supposed to fail: does it? (`#599` found that no push builds the app;
`#283` found the bundle check cannot fail.) `tests.yml`, `e2e.yml`, `deploy.yml`, `docker.yml`,
`publish-wiki.yml`: what each runs, on which trigger, with which permissions, and which of the repo's
own checks it omits. The pre-commit and pre-push hooks against what CI runs (the hook says CI is the
authoritative gate — is it?). `scripts/*.mjs`: correctness of the secret scanner (`secret-detect.mjs`
has tests — do they cover the shapes in `CLAUDE.md`?), the bundle-size check, the lockfile script,
`run-unit-tests.mjs` and `flake-retry.mjs` (a retry that masks a real failure), `wiki-check.mjs`,
`wiki-screenshots.mjs`, `browser-smoke.mjs` (steps that cannot fail, selectors that match nothing).
The three tsconfigs and the ESLint config: which files each actually covers (tests are excluded from
`tsc`, a known gap — is anything else excluded by accident?). `vite.config.ts`, the PWA manifest,
precache globs and the `cspMetaPlugin`; `index.html`. The Docker image and `nginx.conf.in` headers
against `docker.yml`'s assertions. `Run.ps1`/`Run.bat`. `package.json` `engines`, scripts and the
four ways the Node version is declared (#262). `dependabot.yml` scope. `hacs.json` and the HACS
requirements. `.gitignore` tightness against the "never commit" list in `CLAUDE.md`.

**Verification.** Run every gate and record the result in §12: `npm run type-check`, `npm run lint`,
`npm run format:check`, `npm run test:run`, `npm run test:bridge`, `npm run smoke:bridge`,
`npm run build`, `npm run check:bundle`, `npm run test:e2e` (needs a dev server), `npm run wiki:check`,
`npm run lock:check` (needs Docker), `npm audit`, `npm outdated`, `npm run build:extension`. A gate
that passes when it should fail is demonstrated by making it fail on purpose in the worktree (an
unused import, an oversized asset, a fake key in a staged file) and showing it stays green — then
reverting. Read each workflow's `on:` block and step list against the scripts it claims to run.

**Prompt.**

```
Audit Gubbins — Phase 0: baseline, build, tooling, CI and repository configuration.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 0 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch worktrees, tests, hooks, the lockfile, CI or the bundle.

Scope: the Phase 0 row of the plan's §8 map — package.json and the lockfile, every tsconfig, vite and vitest config, ESLint and Prettier config, index.html, public/, scripts/, .githooks/, .github/ (workflows, templates, dependabot), Dockerfile, docker/, docker-compose.yml, Run.bat, Run.ps1, hacs.json, .gitignore, .gitattributes, .dockerignore, .editorconfig, .env.example, .git-blame-ignore-revs, .nvmrc, .prettierignore, vitest.timeouts.ts, LICENSE, and the bridge's and extension's own package, tsconfig, vitest and build files. Before starting, check that every top-level path in the repository appears in exactly one phase's scope in §8; add any that is missing to the right phase and note it in §13.

Follow §6 exactly: mark Phase 0 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p0 from origin/main and pin its SHA; run every gate listed under Phase 0 in §9 and record each result in §12 as the baseline; dispatch finder sub-agents per scope unit (workflows; hooks and scripts; TypeScript, lint and Vite configs; Docker and run scripts; package metadata and ignore rules) with the §2 classes, the §7 checklist and the Phase 0 focus list; dispatch verifier sub-agents per candidate with an adversarial brief; dedupe every confirmed candidate against open and closed issues and against §4.2 (note #454, #599, #283, #262 and #186 are already open in this area — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 0 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree.

Verify everything, never infer: a gate that "cannot fail" is demonstrated by making it fail on purpose in the worktree and showing it stays green, then reverting. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 0 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 1 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 1 — Database engine, driver, migrations and shared repository seams

**Focus.** The worker boundary: `database.worker.ts`, `local-driver.ts`, `worker-driver.ts`, the RPC
`protocol.ts` — message ordering, error propagation across the boundary, a request whose response
never arrives, a transaction left open when a message throws (`transaction.ts`), the tab lock
(`tab-lock.ts`) under two tabs and a closed tab, OPFS file handling (`db-file-store.ts`,
`db-file.ts`, `sqlite-header.ts`, `verify-binary.ts`, `restore-candidate.ts`) including a truncated
or foreign file, the rescue driver. `sqlite-bootstrap.ts`: pragmas (journal mode, foreign keys,
busy timeout) and what happens when the wasm fails to instantiate. Migrations: the squashed v1
baseline against the schema snapshot fixture, `enum-checks`, `baseline-revision`, and the engine's
behaviour on a database newer than the app. Search: `ast.ts`, `parseASTtoSQL.ts` and `fts.ts` —
injection through a field name or an operator, tokenisation of quotes and unicode, a query that
produces a full scan. The shared repository seams: `base.ts` (query row-shape guard), `mappers.ts`
(a column read into the wrong type, the tri-state thumbnail), `tombstone.ts`, `text-limits.ts`,
`name-lookup.ts`, `stock.ts` and `stock-batches.ts` (FEFO, negative stock, the derived-id
idempotency of #696, the race test), `reservations.ts`, `receipt-guard.ts`, `checkout-plan.ts`,
`gauge.ts`, `location-history.ts`, `supplier-cost-sql.ts`. The `types/` barrel against the schema
(a nullable column typed as required). `src/test/drivers` — is the `:memory:` driver faithful to the
worker driver (FTS5, foreign keys, `RETURNING`)?

**Verification.** Scratch tests against the `:memory:` driver for repository seams; `EXPLAIN QUERY
PLAN` (no stats) for any query flagged as a scan; two-tab and mid-transaction-throw scenarios driven
in the browser with the `verify` skill where the worker is implicated; a fabricated foreign or
truncated `.sqlite` for the file paths.

**Prompt.**

```
Audit Gubbins — Phase 1: database engine, driver, migrations and shared repository seams.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 1 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the database, migrations, the query row-shape guard, stock, batches, LWW, sync classification, name-fold, tombstones, the tab lock, or benchmarking without stats.

Scope: the Phase 1 row of the plan's §8 map — src/db/*.ts, src/db/worker/, src/db/rpc/, src/db/migrations/, src/db/search/, the shared (non-entity) files in src/db/repositories/ listed in that row, src/db/repositories/types/, and src/test/. Phase 0 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 1 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p1 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (worker and RPC; file store, header, rescue and tab lock; bootstrap and migrations; search AST and FTS; shared repository seams and types; test drivers) with the §2 classes, the §7 checklist and the Phase 1 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests against the :memory: driver, EXPLAIN QUERY PLAN with no statistics for query shapes, and the verify skill for anything only the real worker shows; dedupe every confirmed candidate against open and closed issues, §4.2 and §10; file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 1 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 1 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 2 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 2 — The item repository family

**Focus.** `ItemRepository.ts` and every mixin in `item/`: `core`, `create`, `normalise` (against the
SQL `CHECK`s — the parity test exists; does it cover every column?), `sql`, `list-order` and
`keyset-pagination` (the seek predicate against `ORDER BY` for every sortable column, ties, nulls,
a name that changes mid-scroll), `status-filter` and `attention-sql` (the correlated `EXISTS`
ceiling — measure, do not assume), `search` and `relevance`, `stock`, `cycle-count`, `kits`,
`variants`, `aliases`, `relations`, `capabilities`, `availability`, `revaluations`, `history` and
`feeds` (projection, since-cursor, clear), `dedupe`, `maintenance-default`, `section-presence`,
`test-records`. Every parallel list the item model keeps (searchable columns, tracking modes, history
actions, status filters — the memory note `Item model parallel lists`) checked for a member missing
from one registry. The `ItemRepository.*.test.ts` files themselves: a test that cannot fail, a
fixture that defaults a field to `undefined` silently, a `phaseNN` test that asserts a behaviour since
changed. Batched reads (`batched-item-reads`) and the three parity tests.

**Verification.** Scratch tests on the `:memory:` driver for every functional claim; `EXPLAIN QUERY
PLAN` for every list, filter and search shape at a seeded 50k-row table with no stats; a mutation of
each parity test's other side to prove the test goes red.

**Prompt.**

```
Audit Gubbins — Phase 2: the item repository family.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 2 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch items, the item model's parallel lists, keyset pagination, the attention/status-filter ceiling, stock, batches, cycle counts, serialised placement, kits, dedupe, the query row-shape guard, or benchmarking without stats.

Scope: the Phase 2 row of the plan's §8 map — src/db/repositories/ItemRepository.ts, src/db/repositories/item/, every ItemRepository.*.test.ts, serialised-placement.test.ts, batched-item-reads.test.ts and the *-parity.test.ts files. Phase 1 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 2 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p2 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (core, create and normalise; list order, keyset pagination, status filter and attention SQL; search and relevance; stock, cycle count and batches; kits, variants, aliases, relations and capabilities; history, feeds, revaluations and dedupe; the test files as a unit of their own) with the §2 classes, the §7 checklist and the Phase 2 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests on the :memory: driver, EXPLAIN QUERY PLAN with no statistics on a seeded 50k-row items table, and — for each parity test — a deliberate mutation of the other side to prove the test can fail; dedupe every confirmed candidate against open and closed issues, §4.2 and §10; file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 2 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 2 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 3 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 3 — Every other repository

**Focus.** `Location` (tree moves that create a cycle, path recomputation, the non-items backlog
#617, history), `Category` (inheritance, lookup sources, the preset library — #715 is open),
`Project` and `project/` (assembly draw, BOM lines, budget, costing, picking, procurement — the
"one physical thing must not be drawn by count" rule), `PurchaseOrder` (open-count parity, receipt
guard, partial receipt), `Supplier` and `SupplierPart` (the one-preferred / one-price-source
partial unique index and its repair on every write path), `Contact`, `Checkout` (the one-open-loan
serialised invariant, a return of more than was lent, borrower union), `AssetBooking` (overlap,
#496/#658/#660 are open), `Maintenance`, `Report` (every aggregate against a hand-computed value on
a small fixture; currency mixing; the "dead stock" tri-state), `User`, `Role` and
`permissions.enforcement` (every repository write checked against the ACL — find a write that is
not), `ApiToken` (hashing, comparison, expiry), `Webhook`, `Image` and `Attachment` (size caps,
orphan cleanup), `LocationPhoto`, `Diagnostics`, `Settings` (typed read of a stored JSON value),
`Storage`, `Suggestion`, `Tag` (name-fold), `TarePreset`, `Wishlist`.

**Verification.** Scratch tests on the `:memory:` driver; for the ACL, a scratch test that calls each
write as a role lacking the permission and lists the ones that succeed.

**Prompt.**

```
Audit Gubbins — Phase 3: every other repository.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 3 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch locations, categories, projects and assembly, purchase orders, supplier parts and the one-of-n flag, checkouts and the serialised loan invariant, bookings and overlap, users and ACLs, name-fold, or reports.

Scope: the Phase 3 row of the plan's §8 map — every *Repository.ts in src/db/repositories/ other than ItemRepository, with its tests; src/db/repositories/project/; permissions.enforcement.test.ts; and the remaining test files the row names. Phase 2 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 3 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p3 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (Location and LocationPhoto; Category and Tag; Project and project/; PurchaseOrder, Supplier, SupplierPart and Wishlist; Checkout, AssetBooking, Contact and Maintenance; Report and Diagnostics; User, Role, ApiToken and permissions enforcement; Webhook, Image, Attachment, Settings, Storage, Suggestion and TarePreset) with the §2 classes, the §7 checklist and the Phase 3 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests on the :memory: driver and — for the ACL — a scratch test that attempts every write as a role without the permission; dedupe every confirmed candidate against open and closed issues, §4.2 and §10; file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 3 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 3 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 4 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 4 — Data integrity: sync, backup, archive, storage, danger zone

**Focus.** This is the tier-1 area of #403: the user cannot undo any of it. Sync: the snapshot trust
boundary (`parseBackupJson` — an allow-listed identifier, a foreign column, a value of the wrong
type), FK repair in `buildLocalSnapshot`, `reconcile.ts` (LWW in JavaScript, the byte-identical
skip, the post-merge cardinality repairs for loans, bookings and preferred parts, the
`items.quantity` re-derive, the `item_history` union and prune watermark), the Google Drive and
File System Access providers (token expiry mid-sync, a 4xx that is retried forever, a partial
upload treated as success, the conflict path of #638), the offline buffer, clock skew. Backup and
restore (`restore-backup`, the raw `.sqlite` path, a restore of a newer schema, the in-flight guard
of #654), archive (`restore-archive`, the mobile weekly auto-archive), storage triage (quota
estimates, history pruning and image downgrade — what they delete and whether the "proves it landed"
seam is honoured before each delete), the danger zone (every destructive action against the
save-before-destroy seam; the history watermark parity), `events/`, `save-file.ts`,
`download.ts`, `read-all-pages.ts`.

**Verification.** Scratch tests for every merge and reconcile claim with two hand-built snapshots;
the trust boundary fed a crafted snapshot; provider failures simulated with a stubbed `fetch`; the
destructive dialogs driven in the browser with the `verify` skill, including Escape and backdrop
during the in-flight state.

**Prompt.**

```
Audit Gubbins — Phase 4: data integrity — sync, backup, archive, storage and the danger zone.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 4 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch sync, LWW, reconcile, snapshots, the trust boundary, FK repair, the offline buffer, Google Drive OAuth, backup and restore, archive, storage, the save-before-destroying seam, derived quantity, or the history watermark.

Scope: the Phase 4 row of the plan's §8 map — src/features/sync/, backup/, archive/, storage/, danger-zone/, clock-skew/, events/, and src/lib/save-file.ts, download.ts and read-all-pages.ts. Phase 3 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 4 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p4 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (snapshot build and trust boundary; reconcile and merge; providers, offline buffer and clock skew; backup and restore; archive and storage triage; danger zone, events and the save-file seam) with the §2 classes, the §7 checklist and the Phase 4 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using two-snapshot scratch tests for every merge claim, crafted snapshots for the trust boundary, a stubbed fetch for provider failures, and the verify skill for the destructive dialogs including Escape and backdrop while a restore is in flight; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#502, #638 and #654 are the known tier-1 items — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only, and stop to tell the maintainer first if a finding is exploitable; record every candidate in §10 under Phase 4 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 4 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 5 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 5 — App shell, platform and shared libraries

**Focus.** Boot (`src/app/boot`, `main.tsx`, the cross-origin-isolation waiver, `coi-bootstrap.js`,
`recovery.js`, Safe Mode and the rescue actions), the router and `routeTree.gen.ts` (a route file
with no generated entry, deep links, the share target, `base-path.ts` under a non-root base), the
service worker (`sw.ts` — precache manifest dedupe, the update prompt and snooze, the schema-safety
promise keyed on `baselineRevision`, `stale-chunk-reload`), `csp.ts` against `index.html` and
`nginx.conf.in`, every Zustand store in `src/state/stores/` (persisted shape, `merge`, version
bumps in `persisted-store-versions`), the query client defaults, every module in `src/lib/`
(the seams §7 names, plus `format`, `measurement-format`, `weight`, `volume`, `dimensions`,
`colour` rounding, `fuzzy`, `highlight` (XSS through a highlighted term), `external-href`,
`host-match`, `fetch-timeout`, `version-compare`, `plural`, `text-terms`, `useFullscreen`,
`print-document`, `image-data-url`), `src/lib/env/` (device id stability, feature detection
false positives, install prompt, motion), `src/styles/index.css` (a token defined in one theme
block only, a contrast pair below WCAG — #209/#210 are open, so only new pairs), the i18n seam
(`i18n.ts`, `messages.ts`, plural selection, placeholder escaping, the drift test's reach),
hotkeys (a chord that collides, a shortcut that fires inside an input), modules (the gating map
and the deep cascade), errors, not-found, lab flags, about, achievements.

**Verification.** Scratch tests for every pure module; the boot and rescue paths driven in the
browser with a deliberately broken database file; the service worker only via two production builds
served statically (the memory note `Verifying service worker updates`); a non-root base path served
via the Docker image.

**Prompt.**

```
Audit Gubbins — Phase 5: app shell, platform and shared libraries.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 5 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch boot, safe mode, the service worker or PWA update, CSP, routes, persisted state, storage keys, the i18n seam, hotkeys, modular UI, money, colour, DST, date input, name-fold, or the error-copy seam.

Scope: the Phase 5 row of the plan's §8 map — src/main.tsx, App.tsx, sw.ts, csp.ts, base-path.ts, src/app/, src/routes/, src/routeTree.gen.ts, src/state/, all of src/lib/ including env/, src/styles/index.css, src/features/errors, hotkeys, modules, i18n, not-found, lab, about and achievements, the three shared components at src/components/*.tsx, and the runtime behaviour of public/recovery.js and public/coi-bootstrap.js. Phase 4 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 5 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p5 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (boot, rescue and isolation; router, routes and base path; service worker, PWA update and CSP; stores and query client; lib — one finder per third of the file list; lib/env; styles tokens; i18n seam; hotkeys, modules, errors and the small features) with the §2 classes, the §7 checklist and the Phase 5 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests for pure modules, the verify skill with a deliberately corrupted database file for boot and rescue, two static production builds for anything in the service worker, and the Docker image for a non-root base path; dedupe every confirmed candidate against open and closed issues, §4.2 and §10; file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 5 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 5 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 6 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 6 — Foundry primitives and shared components

**Focus.** Every primitive in `src/components/foundry/` for: keyboard and focus (the modal stack —
topmost-only Escape and Tab, focus restoration when the opener unmounted (#212 is open), the
`initialFocusRef`; menus and submenus; the combobox `Select` — type-ahead is #550, so only new
defects; roving radio groups; the glyph and emoji pickers; tooltips and their focus-open gotcha;
toasts and their timer (#543); `Autocomplete`; `Pagination`); ARIA (a role with a missing required
attribute, `aria-describedby` pointing at nothing, `LiveRegion` politeness, `Kbd`); the numeric
controls (`NumberInput`, `MoneyInput`, `numeric-bounds` — reports, never rewrites;
`evaluate-expression` — what can be evaluated); `Textarea` autogrow measuring at mount; `Markdown`
(sanitisation — what tags and URLs it lets through); `region-canvas` (pointer maths, touch,
zoom); `reorder-list` drag; `view-transition`, `reveal`, `success-burst`, `animated-number`,
`pointer-tilt`, `useCountUp` (reduced motion honoured; frames released on unmount); `unsaved-changes`
and `dialog-busy`; `use-anchored-popover` (position at viewport edges, scroll); `useMediaQuery`,
`useOnlineStatus`, `useInstallPrompt`, `usePwaUpdate`. `src/components/background/` (the precipitation
and surface engines — CPU per frame, allocation per frame, pause when hidden). `nav/` (`AppNav`,
skip link, `main-content`). Icons and brand marks (`aria-hidden`).

**Verification.** Component scratch tests with Testing Library and `user-event`; the browser with the
`verify` skill for focus, stacking and pointer behaviour; a performance trace of the background
engines under 4× CPU throttling with allocations counted per frame.

**Prompt.**

```
Audit Gubbins — Phase 6: Foundry primitives and shared components.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 6 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch Foundry, the modal stack, tooltips, textarea sizing, the number field, the glyph picker, menus, the select combobox, the dialog scroll bleed, the touch and handset variants, the ease-emphasized token, component-test gotchas, the precipitation engine, or Chromium blend modes.

Scope: the Phase 6 row of the plan's §8 map — src/components/foundry/, src/components/background/, src/components/nav/, src/components/icons/ and src/components/Brand*.tsx. Phase 5 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 6 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p6 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (modal, drawer, rail modal, modal stack, focus trap and dialog behaviour; menu, tooltip, popover, toast and banner; select, autocomplete, select-field, radio and segmented groups; input, textarea, number, money and colour inputs and their bounds and expression helpers; pickers, markdown, region canvas and reorder list; motion, reveal, count-up, tilt, view transitions and the hooks; background engines; nav, skip link, icons and brand) with the §2 classes, the §7 checklist and the Phase 6 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using Testing Library scratch tests, the verify skill for focus, stacking and pointer behaviour, and a throttled performance trace with per-frame allocation counts for the background engines; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (the accessibility sweep issues #209–#223, #491, #515 and #541–#553 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 6 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 6 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 7 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 7 — Inventory domain logic

**Focus.** The pure seams at the top of `src/features/inventory/`: `queries.ts` and
`queries.infinite` (query keys, `invalidate.ts` — which keys each mutation sweeps and which it
misses; #714 found one), `mutations.ts` and the non-optimistic writes, `catalog-import.ts` and
`importers/` (the plan builder, the `createMany` cliff, unknown category (#407), single-column
header (#408), a mistyped barcode (#489)), `custom-fields.ts`, `field-*` (number format,
prominence, suggestions, the Image/File/Colour field types), `category-presets.ts` (#715),
`category-capabilities`, `location-*` (tree, path, inheritance, fullness, map, colour, media,
export), `kits`, `kit-availability`, `batches`, `bulk-edit`, `clone`, `dedupe/`, `labels/` (barcode
fit, mm geometry, quiet zone), `ocr/` (asset staging, worker lifetime, what text is trusted),
`regions/`, `asset-lifecycle`, `low-stock-policy`, `dead-stock-options`, `price-history`, `rarity`,
`asin`, `attachment-link`, `history-*-format`, `item-dnd` and `item-drag`, `list-window`,
`density-layout`, `id-buckets`, `operational-metadata`, `item-requirements`, `item-total-value`.

**Verification.** Scratch tests for every pure seam; an import of a crafted CSV through the real
pipeline in the browser; a query-invalidation scratch test that performs each mutation and lists the
keys that stayed stale.

**Prompt.**

```
Audit Gubbins — Phase 7: inventory domain logic.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 7 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the import pipeline, import row problems, the import file-read seam, CSV import bugs, custom fields and field types, the field dictionary, category presets, location inheritance, dead stock, low stock, kits, the assembly draw, labels and barcodes, the agenda invalidation seam, or the item model's parallel lists.

Scope: the Phase 7 row of the plan's §8 map — every file directly in src/features/inventory/ plus its dedupe/, importers/, labels/, ocr/ and regions/ subfolders. src/features/inventory/components/ is Phase 8, not this one. Phase 6 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 7 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p7 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (queries, invalidation and mutations; catalog import and importers; custom fields, field helpers and category presets and capabilities; locations — tree, path, inheritance, fullness, map, media and export; kits, batches, bulk edit, clone and dedupe; labels and OCR; regions, lifecycle, pricing seams and the remaining helpers) with the §2 classes, the §7 checklist and the Phase 7 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests for the pure seams, a scratch test that performs each mutation and lists the query keys left stale, and the verify skill with a crafted CSV for the import pipeline; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#407, #408, #489, #714 and #715 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 7 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 7 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 8 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 8 — Inventory components

**Focus.** All 178 files in `src/features/inventory/components/`: the item detail dialog and its
tab rail (render, focus and the smoke gotchas in the memory note), the item editor and every
sub-editor (custom fields, capabilities, reorder point, supplier parts, batches, serial numbers,
kits, variants, relations, attachments, images, regions), the create flow, the inventory list in
every density (cards, compact, table — #215 is open for headers), the location sidebar and tree
editors, the print-label dialogs, cycle count and audit day (#547 open), bulk edit and multi-select
(#130), the export wizard, the import dialog, the category and preset pickers, the dedupe sheet.
For each: unsaved-changes wiring, error copy through the seam, `FormField` and `aria-invalid`
association (#552/#553 open), i18n readiness where converted, design tokens, `handset:` and
`touch:` variants, re-render cost on a keystroke in a list of 1,000 items, virtualisation of every
long list, image decoding for thumbnails, effect cleanup.

**Verification.** Component scratch tests; the browser with the `verify` skill against a vault
seeded with the test-records seam; a React Profiler count of renders per keystroke in the list
filter and per item toggle in the multi-select.

**Prompt.**

```
Audit Gubbins — Phase 8: inventory components.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 8 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the item detail dialog, the unsaved-changes guard, the modal stack, component-test gotchas, Playwright item-card locators, the option card, the touch and handset variants, cycle count and audit day, the tabular export seam, the info hint, or the dialog scroll bleed.

Scope: the Phase 8 row of the plan's §8 map — src/features/inventory/components/, all of it. Phase 7 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 8 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p8 from origin/main and pin its SHA; list the folder and split it into finder units of roughly twenty files grouped by dialog or screen (item detail and its tabs; item editor and sub-editors; create flow and pickers; list, cards, table and density; locations sidebar and editors; labels and print; cycle count, bulk edit and multi-select; export, import and dedupe); dispatch one finder sub-agent per unit with the §2 classes, the §7 checklist and the Phase 8 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using Testing Library scratch tests, the verify skill against a vault seeded with the test-records seam, and a React Profiler render count for any re-render claim; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (the open accessibility and usability issues in §4.2 and #130, #215, #476, #547, #552 and #553 are known — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 8 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 8 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 9 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 9 — Commerce, projects and reporting

**Focus.** Purchasing (reorder policy, wishlist, purchase orders, receipt, the shopping list, price
breaks, currency conversion at the line and total, the money seam), suppliers (parts, price history,
preferred and price-source flags), sales and disposals (margin, COGS, the item's state after a sale),
projects (BOM, kits, assembly, picking, budget, buildability — #460 asks for it, so only claims),
lifecycle (warranty, depreciation straight-line at year boundaries, condition grading, current
value and revaluation), reports (every report against a hand-computed fixture; ABC, turnover,
aging, dead stock, valuation trend and its plan, data hygiene, item flow, the insurance schedule
print and its totals, the catalogue letterhead), dashboard (every widget's query, its invalidation,
its threshold preferences, the low-stock widget, the maintenance-due lane of #714, `DASHBOARD_WIDGETS`
against `en.json`), export (the tabular serialiser for every format — CSV quoting, the BOM of #579,
XLSX cell types, HTML escaping — and the export wizard scopes), import (the dialog, the file-source
seam, the migration mappers of #488).

**Verification.** Scratch tests with small hand-computed fixtures for every aggregate and every
money path; an export of each format opened and parsed back; a browser drive of a PO from creation
to full receipt and of a project from BOM to finalised assembly.

**Prompt.**

```
Audit Gubbins — Phase 9: commerce, projects and reporting.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 9 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch money rounding, the Money control, currency select, supplier parts and price history, the one-of-n flag, the assembly draw, purchase orders and receipt, dead stock, low-stock thresholds, the tabular export seam, the import pipeline, dashboard customisation, or reports.

Scope: the Phase 9 row of the plan's §8 map — src/features/purchasing/, suppliers/, sales/, projects/, lifecycle/, reports/, dashboard/, export/ and import/. Phase 8 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 9 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p9 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (purchasing; suppliers and sales; projects; lifecycle; reports — split in two; dashboard; export and import) with the §2 classes, the §7 checklist and the Phase 9 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using hand-computed fixtures for every aggregate and money claim, a parse-back of every export format, and the verify skill for the purchase-order and assembly flows end to end; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#460, #463, #467, #488, #579, #595 and #714 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 9 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 9 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 10 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 10 — Capture and discovery

**Focus.** Search (the text-query parser and the visual builder round trip — a query that parses to
something other than what it shows; `nl-query`; field registries against the item model's parallel
lists; saved searches and their persisted shape; enum options), the command palette (every
registered command reachable and correctly gated by modules and ACL), the scanner (decoder worker
lifetime and the worktree gotcha; camera permission denial; continuous mode and batch actions;
manual entry; hardware-scanner keyboard-wedge input; barcode check digits and the ASCII rule;
announcements — #541 open), scraping (the extension bridge protocol and its negotiation, timeouts,
request correlation, every supplier parser against a saved fixture page, the merge of scraped data
into an item, price refresh, the supplier URL allow-list), lookups (Wikidata and Open Food Facts —
rate limits, a missing field, a non-English label, the live-lookup verification note), share
(share target and file handlers — a file of the wrong type, a payload with no file), images
(resize, WebP encoding, EXIF orientation, the size cap of #641).

**Verification.** Scratch tests for the parsers and the reducers; the scanner and share target in
the browser with the `verify` skill (a synthetic barcode image for the decoder); supplier parsers
against fixtures already in the repository, never a live site; lookups against a stubbed `fetch`.

**Prompt.**

```
Audit Gubbins — Phase 10: capture and discovery — search, command palette, scanner, scraping, lookups, share and images.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 10 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch search syntax, the decode worker, the scanner, the scraping bridge, product lookup and the live-lookup verification, images and thumbnails, the item thumbnail tri-state, or the share target.

Scope: the Phase 10 row of the plan's §8 map — src/features/search/, command-palette/, scanner/, scraping/, lookups/, share/ and images/. Phase 9 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 10 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p10 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (search parser, builder and saved searches; command palette; scanner; scraping protocol and merge; supplier parsers; lookups; share and images) with the §2 classes, the §7 checklist and the Phase 10 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests for parsers and reducers, the verify skill with a synthetic barcode image for the scanner and a crafted payload for the share target, repository fixtures only for supplier parsers, and a stubbed fetch for lookups — never a live third-party site; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#18, #390, #489, #541 and #641 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 10 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 10 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 11 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 11 — People, time and configuration screens

**Focus.** Users (sign-in, session store, roles, the permission gating of every screen and control
— a control that is hidden but whose mutation still succeeds; the discoverability note of #428),
contacts and loans (borrower union, renewals and their timezone, returns), bookings (#496, #658,
#660 are open — only new defects), the calendar and Upcoming agenda (`agendaKeys`, day-grained dates
and the frame of #495, DST, the invalidation seam), alerts (every lane's query, badge counts,
invalidation), activity (the feed's since-cursor and its projection), maintenance (schedules,
due-date rollover, the default seam), tags (name-fold, rename and merge), settings (every tab —
each control writes the preference it names, reads it back, and the value survives a reload;
Notifications; Keyboard shortcuts; the Focus-search-bar request of #440), the Home Assistant screen
and the webhooks screen in the app (URL validation, the SSRF rules the bridge enforces echoed or
not, test-delivery result copy).

**Verification.** Scratch tests for the pure seams and stores; the browser with the `verify` skill
for every settings control (set, reload, read back) and for the permission gating as a
low-privilege role; a scratch test that runs each lane's query at a DST boundary and a west-of-UTC
zone.

**Prompt.**

```
Audit Gubbins — Phase 11: people, time and configuration screens.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 11 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch users and ACLs, the settings dialog, the agenda invalidation seam, DST calendar days, date input, bookings and overlap, the serialised loan invariant, the tool use case, webhooks, or the Home Assistant screens.

Scope: the Phase 11 row of the plan's §8 map — src/features/users/, contacts/, bookings/, calendar/, alerts/, activity/, maintenance/, tags/, settings/, home-assistant/ and webhooks/. Phase 10 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 11 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p11 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (users and permission gating; contacts and loans; bookings and calendar; alerts, activity and maintenance; tags and settings; Home Assistant and webhooks screens) with the §2 classes, the §7 checklist and the Phase 11 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using scratch tests for seams and stores, a scratch test that runs each time-based lane at a DST boundary and in a west-of-UTC zone, and the verify skill for every settings control (set, reload, read back) and for the permission gating as a low-privilege role; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#428, #440, #495, #496, #658 and #660 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only; record every candidate in §10 under Phase 11 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 11 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 12 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 12 — Bridge core and HTTP API

**Focus.** `server.ts` and `serve.ts` (request lifecycle, body limits, a slow client, a request
after shutdown), `config.ts` (every option's validation, the loopback default, a misconfiguration
that starts anyway), `cors.ts`, `identity.ts` and the API tokens (constant-time compare, scope
checks on every route), `rate-limit.ts` (per what key; a burst that is not limited), `idempotency.ts`
(key scope and expiry), `resilience.ts` (retries that double-apply), `sqlite-source.ts`,
`node-driver.ts` and `hydrate.ts` (the snapshot load, a snapshot that changes under a read,
`watcher.ts` debounce and a rename), `snapshot-io.ts` and `snapshot-health.ts`, `write.ts` (the
peer-device write path — the cost of #712, LWW correctness against the app's merge, what happens
when the app is offline), `query.ts` and `inventory-scan.ts`, `low-stock-*`, `push.ts`,
`item-detail.ts`, `api/` (OData `$filter` parsing and injection, `$count`/`$top` of #362,
`field-select`, `conditional.ts` ETags, `head.ts`, `items-csv.ts` escaping, `dto.ts` against the
app's types, `limits.ts`, `respond.ts` error shapes), `openapi.ts` against `openapi.yaml` against
the routes actually mounted (#556), `version.ts`, `bridge-id.ts`, `node-version.mjs` and
`loader.mjs` (the strip-only loader — any app import that would fail under it), `cli.ts`.

**Verification.** `npm run test:bridge` scratch tests from the primary checkout; the bridge started
against a fixture snapshot with `curl` for every route (auth missing, auth wrong scope, malformed
`$filter`, oversized body, slow body); `npm run smoke:bridge`; a timed write against a 50k-item
snapshot for #712-adjacent claims.

**Prompt.**

```
Audit Gubbins — Phase 12: bridge core and HTTP API.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 12 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the bridge, the strip-only loader, bridge writes, the ecosystem integrations, the schema version and baseline revision, API tokens, or the push-from-primary-checkout rule.

Scope: the Phase 12 row of the plan's §8 map — every file directly in bridge/src/, bridge/src/api/, bridge/src/fixtures/, bridge/openapi.yaml, bridge/loader.mjs and bridge/README.md. The events, feeds, ical, mqtt, mdns, mcp and homeassistant subfolders are Phase 13. Phase 11 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 12 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p12 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (server, serve, config, cors and cli; identity, tokens, rate limit and idempotency; sqlite source, node driver, hydrate, watcher and snapshot io/health; write, resilience, query, inventory scan, low stock and push; api — OData filter, service and metadata; api — v1 routes, dto, field select, conditional, head, csv and respond; openapi against the mounted routes; loader and node version) with the §2 classes, the §7 checklist and the Phase 12 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using bridge scratch tests run from the primary checkout with --config bridge/vitest.config.ts, the bridge started against a fixture snapshot and driven with curl for every route including missing auth, wrong scope, malformed $filter, oversized and slow bodies, npm run smoke:bridge, and a timed write against a 50k-item snapshot for any cost claim; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#362, #556, #712 and #718 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only, and stop to tell the maintainer first if a finding is exploitable; record every candidate in §10 under Phase 12 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 12 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 13 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 13 — Bridge integrations

**Focus.** Events (the change pipeline and generation counter, `lookup.ts`, SSE — a client that
disconnects mid-event, backpressure, heartbeat; webhooks — SSRF guard against DNS rebinding and
IPv6 literals, the blocked list, target validation, the delivery log, retries and their idempotency,
the test-delivery shape, signature headers if any), feeds (emitters, the feed model, item status,
metrics and their formatting — a Prometheus label with an unescaped value), iCal (escaping and
control characters — #368 open; folding; timezone; recurrence), MQTT (the packet codec, retained
locations, discovery payloads against what the publisher publishes — the parity test exists;
reconnect and the offline buffer of #565; the ghost retraction), mDNS (the hand-rolled responder —
a malformed query, the loopback gate, the opt-in), MCP (`stdio.ts` framing, the dispatcher's
error shapes, every tool's arguments validated before the repository is touched, pagination
cursors, the write tools' idempotency), the Home Assistant client and scale stream (reconnect,
a scale that reports garbage, backpressure).

**Verification.** Bridge scratch tests; a real MQTT broker is not assumed — the packet codec is
tested against captured bytes in the fixtures; SSE and webhooks driven with `curl` and a local
listener; the MCP server driven over stdio with `bridge/mcp.mjs` and hand-written JSON-RPC frames.

**Prompt.**

```
Audit Gubbins — Phase 13: bridge integrations — events, webhooks, feeds, iCal, MQTT, mDNS, MCP and the Home Assistant client.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 13 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch webhooks, MQTT and the ghost retraction, mDNS, the Home Assistant phases, the scale stream, the ecosystem integrations, or the bridge.

Scope: the Phase 13 row of the plan's §8 map — bridge/src/events/, feeds/, ical/, mqtt/, mdns/, mcp/ and homeassistant/, bridge/webhooks.example.json, bridge/gubbins-bridge.service and bridge/scripts/. The MCP tool descriptions as text are Phase 15; here the MCP scope is the mechanics — framing, dispatch, validation, cursors and idempotency. Phase 12 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 13 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p13 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (events pipeline and SSE; webhooks — SSRF, targets, delivery, log and test; feeds and metrics; iCal; MQTT; mDNS; MCP mechanics; Home Assistant client and scale stream) with the §2 classes, the §7 checklist and the Phase 13 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using bridge scratch tests from the primary checkout, captured packet bytes for the MQTT codec, curl and a local listener for SSE and webhook delivery, and hand-written JSON-RPC frames over stdio for the MCP server; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#368, #469, #559 and #586 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only, and stop to tell the maintainer first if a finding is exploitable; record every candidate in §10 under Phase 13 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 13 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 14 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 14 — Satellites: browser extension and Home Assistant component

**Focus.** Extension: `manifest.json` permissions against what the code uses (`host_permissions`
narrowed — is every parser's host present and nothing more?), `background.ts` (the fetch gate,
message validation from the content script and from the app origin — `app-origins.ts` — a page
that spoofs the app), `content-script.ts` and `active-tab-scrape.ts` (what DOM it reads, injection
into the page), `build.mjs` output against the manifest. Home Assistant component: every file in
`custom_components/gubbins/` (the config flow and zeroconf step, the coordinator's polling and
error handling — #718 is open, the entity unique ids and their stability across a bridge id change,
the sensors against the bridge's attention statuses (#559), `services.yaml` against the services
registered, `intent.py` against the sentences, `manifest.json` requirements and version,
`strings.json` and `translations/` key parity), `homeassistant/custom_sentences/en/gubbins.yaml`
(sentence syntax, slot names against the intent handler), `homeassistant/README.md` and
`README-HA.md` (#578 open), `hacs.json`.

**Verification.** The extension built with `npm run build:extension` and loaded unpacked in Edge;
its messages replayed from a page on a non-app origin; the Python component checked with
`python -m py_compile` and its config flow and coordinator exercised with a stubbed bridge (a
minimal HTTP server returning the fixture snapshot's shapes) — if Home Assistant itself is not
available, the candidate is recorded as unverifiable with what would settle it.

**Prompt.**

```
Audit Gubbins — Phase 14: the browser extension and the Home Assistant custom component.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 14 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the Home Assistant phases (HA-4, mDNS, writes), the security audit's extension fetch gate, scraping, or app origins.

Scope: the Phase 14 row of the plan's §8 map — extension/src/, extension/README.md, extension/manifest.json (behaviour, not build), custom_components/gubbins/, homeassistant/ and README-HA.md. Phase 13 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 14 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p14 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (extension background and fetch gate; extension content script and active-tab scrape; HA config flow, coordinator and api; HA entities, sensors, services and intent; HA strings, translations, sentences and the two READMEs) with the §2 classes, the §7 checklist and the Phase 14 focus list; dispatch verifier sub-agents per candidate with an adversarial brief, using the extension built and loaded unpacked in Edge with messages replayed from a non-app origin, python -m py_compile for every Python file, and a stubbed bridge HTTP server for the config flow and coordinator — recording as unverifiable, with what would settle it, anything that needs a live Home Assistant; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#559, #578, #586 and #718 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only, and stop to tell the maintainer first if a finding is exploitable; record every candidate in §10 under Phase 14 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 14 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 15 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 15 — Prompt and guidance surface

**Focus.** Shipped text first. Every MCP tool in `bridge/src/mcp/tools.ts`: does the description
say what the dispatcher does, name every argument's format and default, say what the result looks
like and what an error means, and give a model enough to choose between `gubbins_search` and the
locate tool? Is any description ambiguous in a way a model would resolve wrongly (units, sign
conventions, "id as returned by" for a tool that returns none)? `spoken.ts`: every shape's output
for zero, one, several, a plural item name, a location with a leading "the", a quantity of zero, a
serialised item, an item with no location, a name with a quote. HA `custom_sentences` and
`strings.json`: sentences that the parser cannot match, slots with no handler, translations missing
a key. OpenAPI `summary`/`description` text and the OData `$metadata` annotations against
behaviour. The two catalogs (`en.json`, `de.json`): the du/Sie inconsistency is #583; look for a
placeholder that a translation dropped, an English value that differs from its code-side twin, a key
whose German changes the meaning, a plural form that is wrong. `docs/wiki/AI-Assistant-Query-MCP.md`
against the tool list.

Then agent and contributor guidance. `CLAUDE.md` and `AGENTS.md`: every command named (run it),
every path named (open it), every count stated (count it), every rule against every other rule
(two that conflict), the parity test's reach. `.claude/skills/verify/SKILL.md` and
`auto-review/SKILL.md`: the recipe against what actually works today (the memory notes record that
the verify skill carried a stale "worktree dev server can't boot" claim). The issue and PR templates
against the house style the repository actually uses. `docs/dev/PHASE_HANDOVER.md`,
`deferred-features.md`, `releases.md`, `google-drive-sync.md`: live instructions that are stale
(records of what happened are evidence and are not corrected — the `CLAUDE.md` rule). Every
`🟢 ACTIVE` plan in `docs/todo/`: is it still live, and does its "next step" still exist? (#584 is
open for the banner rule.) `README.md` (#270, #50 open), `CONTRIBUTING.md`, `SECURITY.md`,
`docs/modular-ui-plan.md` (why is it outside `docs/todo/`?).

**Verification.** For shipped text: the MCP server driven over stdio against the fixture snapshot
with each tool called as its description suggests, and the response compared to the description;
`speakWhereIs` called with the edge cases; the catalog tests plus a scratch diff of placeholders.
For guidance: every command executed, every path opened, every count recomputed. A guidance defect
is filed as `documentation` and cites the exact sentence.

**Prompt.**

```
Audit Gubbins — Phase 15: the prompt and guidance surface.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 15 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the auto-review skill, the verify skill, the i18n catalog seam, the Home Assistant intent (HA-4), the spoken answer, MCP, the docs/todo status convention, or deferred-work tracking.

Scope: the Phase 15 row of the plan's §8 map. Shipped text: the tool names, schemas and descriptions in bridge/src/mcp/tools.ts, the output of bridge/src/spoken.ts, homeassistant/custom_sentences/, custom_components/gubbins/strings.json and translations/, the description text in bridge/openapi.yaml and the OData metadata, src/features/i18n/catalogs/en.json and de.json as text, and docs/wiki/AI-Assistant-Query-MCP.md. Guidance: CLAUDE.md, AGENTS.md, .claude/skills/*/SKILL.md, .github/ISSUE_TEMPLATE/, .github/pull_request_template.md, docs/dev/, every ACTIVE plan in docs/todo/, docs/modular-ui-plan.md, README.md, CONTRIBUTING.md and SECURITY.md. Phase 14 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 15 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p15 from origin/main and pin its SHA; dispatch finder sub-agents per scope unit (MCP tool text against the dispatcher; spoken answers; HA sentences, strings and translations; OpenAPI and OData text; the two catalogs; CLAUDE.md and AGENTS.md; skills and templates; docs/dev and the ACTIVE plans; README, CONTRIBUTING, SECURITY and the modular-ui plan) with the §2 classes, especially §2.4, and the Phase 15 focus list; dispatch verifier sub-agents per candidate with an adversarial brief — for shipped text, drive the MCP server over stdio against the fixture snapshot and call speakWhereIs with the edge cases; for guidance, run every command and open every path the sentence names and recompute every count; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#50, #270, #556, #583, #584 and #586 are open — file only what they do not name); file one issue per confirmed finding in the §5 format with existing labels only, quoting the exact sentence at fault; record every candidate in §10 under Phase 15 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 15 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 16 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 16 — Wiki parity

**Focus.** All 99 pages under `docs/wiki/` and every image under `docs/wiki/images/`, each read
against the app at the pinned commit. For every page: each claim of behaviour (a setting exists, a
button does X, a limit is N, a format is supported) checked in the code or in the running app; each
screenshot checked against the current UI (a stale screenshot is a finding when it shows a control
that moved or no longer exists); each `[[link]]` resolved (`npm run wiki:check` does this — run it
first and audit what it does not check); the sidebar and page map in `docs/todo/wiki_2026-07-11.md`
against the pages that exist; every feature the app ships that no page mentions (the modular UI
registry and the settings tabs are the checklist); house-style consistency where it changes meaning.
The sanctioned exception: the `/lab` flags and seasonal easter eggs are deliberately undocumented.

**Verification.** Each behavioural claim traced to code with `path:line`, or driven in the browser;
each screenshot compared to a fresh capture from `scripts/wiki-screenshots.mjs` against a dev server
(run it once; keep the captures in the scratchpad; do not commit any image). File one issue per
page that has drift, listing each claim, not one issue per claim — the fix is per page.

**Prompt.**

```
Audit Gubbins — Phase 16: wiki parity.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 16 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, the wiki plan docs/todo/wiki_2026-07-11.md, and the memory notes the session-start hook lists whose titles touch the wiki, wiki screenshots, the hidden lab flags, or modular UI.

Scope: the Phase 16 row of the plan's §8 map — every page and image under docs/wiki/, and the page map in docs/todo/wiki_2026-07-11.md, each checked against the app at the pinned commit. Phase 15 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 16 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p16 from origin/main and pin its SHA; run npm run wiki:check and note what it does not check; split the 99 pages into finder units of about ten pages grouped by the wiki sidebar's sections, and dispatch one finder sub-agent per unit to list every behavioural claim, screenshot and cross-link on its pages with the code path or UI state that should support it; dispatch verifier sub-agents per page with an adversarial brief to trace each claim to path:line or drive it in the browser with the verify skill, and to compare each screenshot with a fresh capture from scripts/wiki-screenshots.mjs kept in the scratchpad; also list every shipped feature, setting and module with no page; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (the wiki issues in §4.2 are open — file only what they do not name); file one issue per page with drift, listing every claim on it, in the §5 format with the documentation label and the area label; record every candidate in §10 under Phase 16 with its verdict; update §8, §11 and §13; commit only the plan document — no image and no wiki page — merge --no-ff into main, push from the primary checkout, and remove the worktree.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 16 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 17 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 17 — Cross-cutting static sweeps

**Focus.** Every row of §7 run across the whole repository, each hit triaged (sanctioned exception,
already-filed, or candidate). Then: exports with no importer and files with no importer (a
`tsc`-driven or grep-driven sweep; a dead module is a finding when it still claims to be a seam);
re-implementations of an existing seam (two functions that fold names, two that format money,
two pluralisers — the memory notes list the canonical one for each); `TODO`, `FIXME`, `HACK`,
`XXX` and their age; test-suite quality across all 798 test files (a test with no assertion, an
assertion on a mock rather than behaviour, `.skip`, `.only`, a snapshot that asserts everything and
therefore nothing, a "mirrors" comment with no drift test, a drift test that cannot fail — mutate
and check); ESLint rules disabled repo-wide in `eslint.config.js` and whether each disable is
still justified; the licence of every dependency against MIT compatibility and `npm audit` against
the baseline in §12; the `dist/` and `extension/dist/` folders — are they tracked, and should they
be?

**Verification.** Every grep hit opened and read; every "cannot fail" test claim proven by mutating
the code under test in the worktree and running the test; the licence sweep with `npm ls --json`
and each package's `license` field.

**Prompt.**

```
Audit Gubbins — Phase 17: cross-cutting static sweeps.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 17 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, and every memory note the session-start hook lists whose title names a seam (name-fold, money, date input, DST, error copy, save-before-destroying, import file read, storage keys, exhaustive switch, query row shape, the Money control, the tabular export seam, plural, the touch and handset variants) — each is a canonical implementation that a duplicate would bypass.

Scope: the whole repository, as the Phase 17 row of the plan's §8 map describes — every §7 row across src/, bridge/src/, extension/src/, scripts/ and custom_components/; unused exports and files; re-implemented seams; TODO, FIXME, HACK and XXX; test-suite quality across every test file; repo-wide lint disables; dependency licences and npm audit against the §12 baseline; whether dist/ and extension/dist/ belong in the tree. Phase 16 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 17 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p17 from origin/main and pin its SHA; dispatch finder sub-agents per sweep (one per group of §7 rows, one for unused code, one for duplicated seams, one for TODO markers, one per third of the test files for test quality, one for lint config and dependencies) and require each to open and read every hit rather than report the grep; dispatch verifier sub-agents per candidate with an adversarial brief — a "cannot fail" test claim is proven by mutating the code under test in the worktree and running the test, then reverting; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 — many §7 rows have an open issue already (#546, #577, #390, #225) and earlier phases will have filed area-specific hits, so this phase files only what remains; file one issue per confirmed finding in the §5 format with existing labels only, grouping hits of one mechanism into one issue with a list; record every candidate in §10 under Phase 17 with its verdict; update §8, §11 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree with no scratch test or mutation left behind.

Verify everything, never infer. Nothing is filed without a demonstration. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 17 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 18 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 18 — Runtime performance at scale

**Focus.** This phase executes the pass #112 asks for, and every issue it files links #112. Seed a
vault at three sizes — 1k, 20k and 100k items with proportionate locations, history, stock rows,
batches, images and custom-field values — through the test-records seam or a generated backup
restored through the real path. Measure, under 4× CPU throttling and again unthrottled: cold boot to
interactive and the precache install; inventory list first paint, scroll at each density, a filter
keystroke, each status filter (the known correlated-`EXISTS` ceiling — measure it and file the
number), sort changes, keyset page fetch; search (text, builder, FTS); the item detail open and each
tab; the dashboard (every widget's query time, and the total refetch on one item write); every
report and the insurance schedule; the calendar and Upcoming; a full sync merge of two divergent
snapshots; backup, restore and archive; a CSV import of 10k rows; bulk edit of 1k selected; the
scanner's decode loop; the background engines while idle; memory after 30 minutes of the app idle
and after 200 dialog open/close cycles. On the bridge: hydrate time at 100k; every list route and
OData `$filter` at 100k; a write (#712); SSE fan-out to 20 clients; MQTT discovery of 5k locations.
Bundle: chunk map, what loads eagerly, precache size against a first install on a phone.

**Verification.** Numbers, plans and traces, per §3.3, recorded in §12 as the performance baseline
even where no issue results. Any query claim carries `EXPLAIN QUERY PLAN` with no stats. Any render
claim carries a render count or a long-task count. Report ratios and shapes; absolute milliseconds
only as context.

**Prompt.**

```
Audit Gubbins — Phase 18: runtime performance at scale.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 18 of it — no other phase, and no fixing. Then read CLAUDE.md and AGENTS.md, issue #112, and the memory notes the session-start hook lists whose titles touch benchmarking without stats, the inventory attention scaling ceiling, keyset pagination, pagination app-wide, the import pipeline's createMany cliff, batched item reads, the precipitation engine, the verify skill's port and worktree gotchas, or bridge writes.

Scope: the running app and the running bridge, seeded at 1k, 20k and 100k items with proportionate related rows, as the Phase 18 row of the plan's §8 map and its focus list describe. Phase 17 must be marked complete in §8; if it is not, stop and say so.

Follow §6 exactly: mark Phase 18 in progress in §8 and commit that alone; create the worktree .claude/worktrees/wt-audit-p18 from origin/main and pin its SHA; build the three seeded vaults through the test-records seam or a generated backup restored through the real restore path, and keep them in the scratchpad; dispatch measurement sub-agents per surface (boot and precache; inventory list, filters, sort and paging; search; item detail; dashboard and its invalidation; reports and the insurance schedule; calendar and alerts; sync merge, backup, restore and archive; import and bulk edit; scanner and background engines; memory over time; bridge hydrate, reads, writes, SSE and MQTT; bundle and chunk map), each measuring under 4× CPU throttling and unthrottled and reporting plans, render counts, long-task counts and ratios per §3.3; record every measurement in §12 as the performance baseline whether or not it yields a finding; dispatch verifier sub-agents to re-measure every candidate that exceeds what the target hardware can pay; dedupe every confirmed candidate against open and closed issues, §4.2 and §10 (#112, #145, #283, #362, #641 and #712 are open — file only what they do not name, and link #112 from every issue); file one issue per confirmed finding in the §5 format with the performance label and the area label, stating the hardware, the seed size and the measurement so it can be re-run; record every candidate in §10 under Phase 18 with its verdict; update §8, §11, §12 and §13; commit, merge --no-ff into main, push from the primary checkout, and remove the worktree, and stop every dev server and bridge process you started.

Verify everything, never infer: a performance claim without a measurement is not filed, and visual effects are not to be proposed for removal. No issue mentions the audit's process. There are no time constraints; finish the whole scope.

When Phase 18 is landed, reply with the counts (candidates, confirmed, rejected, unverifiable, duplicates, issues filed) and hand back the Phase 19 prompt from §9 of the plan inside a raw fenced markdown block.
```

### Phase 19 — Close-out

**Focus.** The audit's own integrity. Every row in §10 has a verdict and, if confirmed, an issue
number; every issue the audit filed is still open (or was closed for a stated reason), carries the
attribution trailer, carries at least one nature and one area label, contains no process leakage,
and has a title that is still true on `main`'s tip. Every §11 item has a note of what would settle
it, and is put to the maintainer as a list. The §8 counts are recomputed from §10, not copied. The
§12 baselines are complete. A summary section is written at the top of this document: findings by
class and severity, the phases that produced the most, the areas that produced nothing (and whether
that is coverage or health). Then the banner flips to `✅ COMPLETE`, the file moves to
`docs/todo/done/`, the memory note that points here is updated, and inbound links are fixed.

**Prompt.**

```
Audit Gubbins — Phase 19: close-out.

Read docs/todo/codebase-audit_2026-08-30.md in full before doing anything else. It is the single source of truth for this audit, and this session executes exactly Phase 19 of it — the close-out, with no further auditing and no fixing. Then read CLAUDE.md and AGENTS.md, and the memory notes the session-start hook lists whose titles touch the docs/todo status convention, deferred-work tracking, or memory conventions.

Scope: the plan document itself and every issue the audit filed. Phases 0 to 18 must all be marked complete in §8; if any is not, stop and say so.

Follow §6 for the worktree and landing mechanics, then: reconcile §10 so that every row has a verdict and every confirmed row an issue number; recompute the §8 counts from §10; for every filed issue, confirm with gh that it is open or closed for a stated reason, that it carries the attribution trailer, a nature label and an area label, no process leakage, and a title still true on main's tip — list any that fail and correct the label or body only (never the plan's findings); compile §11 into a list for the maintainer with what would settle each item; check §12 is complete; write a summary at the top of the document — findings by class and severity, the phases and areas that produced the most and the least, and whether a quiet area reflects health or thin coverage; flip the banner to COMPLETE, git mv the file into docs/todo/done/, grep for inbound links (docs/dev/deferred-features.md, docs/dev/PHASE_HANDOVER.md, the memory vault) and update them, run src/lib/docs-todo-status.test.ts, commit, merge --no-ff into main, push from the primary checkout, and remove the worktree. Finally, record in the memory vault at P:/Source/!Memories, following its Index.md convention, one note that points at the archived plan and states what the audit found and what remains in §11.

Verify everything, never infer. There are no time constraints. When the close-out is landed, reply with the final counts across all phases, the §11 list, and the path of the archived document. There is no next phase.
```

## 10. Findings register

One row per candidate, in every phase, whatever the verdict. IDs are `P<phase>-<n>`. **Where** is
`path:line` at the phase's pinned SHA. **Verdict** is one of `confirmed`, `rejected`, `duplicate
#N`, `unverifiable`, `fixed on main` (the re-check of §3.1 item 5 found it already gone). The
register is append-only within a phase; a later phase that revisits a row adds a new row that cites
the old one.

### Phase 0 — Baseline, build, tooling, CI and repository configuration

Pinned SHA: `9924e6a7faab7bb59748b59398f8f615eb43c72f`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| P0-1 | Mechanical | `.github/workflows/e2e.yml:98-104` | The failure screenshot is a dotfile, which `upload-artifact` excludes by default, so the step goes green having uploaded nothing | confirmed | #720 | Both runs logged `No files were found`; `artifacts` returns `total_count: 0`. The screenshot is also taken at a fixed point, not at the failure |
| P0-2 | Functional | `.github/workflows/e2e.yml` | The nightly has failed on 100% of its runs, contradicting #600's closing claim | confirmed (narrowed) | #735 | The 11 step failures are CI-environmental — none reproduces locally. The 4 page errors do, on a machine where every step passes. Filed as the page-error defect only |
| P0-3 | Functional | `bridge/Dockerfile`, `.github/workflows/docker.yml:220-224` | No workflow builds the bridge image; `docker compose config` validates schema only | confirmed | #721 | Proved `docker compose config` passes with the Dockerfile deleted. Two commits touching `bridge/Dockerfile` triggered no `docker.yml` run |
| P0-4 | Functional | `.github/dependabot.yml` | No `docker` ecosystem, so three base images on mutable tags are never reviewed | confirmed | #723 | Filed `enhancement`: nothing claims the images are pinned. Zero docker-ecosystem Dependabot alerts exist |
| P0-5 | Mechanical | `.github/workflows/docker.yml:10-33` | The `paths:` filter omits files three of the workflow's own assertions depend on | confirmed (OCR leg only) | #722 | `index.html` leg is inside #599; `src/sw.ts` leg **rejected** — `vite.config.ts:205` fixes the emitted name and *is* in the filter |
| P0-6 | Prompt | `.github/workflows/e2e.yml:8` | The "~25 minutes" figure is ~2.5× the measured runner cost, and justifies not gating merges | rejected | — | Line 45 labels it "the ~25-minute **local** run", which is accurate. Not gating is documented as deliberate in #600, so this is a feature request (§1) |
| P0-7 | Mechanical | `.github/workflows/tests.yml:181-185`, `scripts/lockfile.mjs:43-44` | "Keep in step with X" comments with no test behind them | confirmed | #724 | Mutation: raising the bridge floor in both tested places leaves 1191 tests green while CI still boots 22.18.0. `e2e.yml`/`deploy.yml` leg is a duplicate of #262 |
| P0-8 | Mechanical | `.github/workflows/tests.yml:52-68` | Under `workflow_call` from Deploy the secret-scan base collapses to `HEAD~1` | confirmed | #725 | Real deploy log shows `EVENT: workflow_dispatch`, `PUSH_BEFORE:` empty. Deploying `main` is covered by accident (merge commits); an unmerged branch tip is not |
| P0-9 | Prompt | `.github/ISSUE_TEMPLATE/bug_report.yml:46` | The version placeholder shows a date-shaped format the app never used | rejected | — | Field is optional and its description already points at the About screen, which renders `0.32.0` literally. Below the consequence bar |
| P0-10 | Functional | `scripts/secret-detect.mjs:22,29-38` | The detector misses most shapes CLAUDE.md names, including the bridge's own documented `.env` syntax | confirmed | #733 | 26 of 36 probed shapes missed. Narrowed by GitHub push protection, but `secret_scanning_non_provider_patterns` is disabled |
| P0-11 | Functional | `scripts/secret-scan.mjs:57` | The CI scan diffs endpoints, so add-then-remove within one push is invisible | confirmed | #734 | Reproduced on a scratch branch: scan exits 0, blob still in history |
| P0-12 | Prompt | `.githooks/pre-commit:11-13` | The comment claims CI catches a `--no-verify` commit "before it lands"; `main` is unprotected | confirmed | #725 | Folded into P0-8's issue rather than filed alone — same region, same fix |
| P0-13 | Mechanical | `.githooks/pre-push:57` | An unresolvable `remote_oid` makes the hook run no checks and exit 0 silently | confirmed (narrowed) | #727 | Only reachable via `--force`: git rejects an ordinary push before the object can matter. Sub-claim about the `:40-42` comment **rejected** — it scopes itself correctly |
| P0-14 | Mechanical | `.githooks/pre-commit:43-61` | Prettier and ESLint read the working tree, not the index | confirmed | #726 | The candidate's own recipe was falsified (it goes red); the real false negative is the reverse direction, proved against the committed blob |
| P0-15 | Mechanical | `scripts/check-bundle-size.mjs:11,20` | The reporter counts a different file set from the precache it claims to mirror | confirmed | #719 | Found independently by the lead and a finder. 5 files / 108.32 KiB precached and uncounted; `sw.js` counted and not precached |
| P0-16 | Mechanical | `scripts/check-bundle-size.mjs:48-53` | A bare `catch` reports every failure as "dist/ not found" and exits 0 | duplicate #283 | — | #283 already names every exit path being `process.exit(0)` |
| P0-17 | Functional | `scripts/lockfile.mjs:147-156` | `lock:check` cannot detect the `libc` stripping its own docstring describes | confirmed | #728 | Mutation: stripping all 14 `libc` arrays still prints "Lockfile OK". Latent today because the wasm32 fault co-occurs and *is* caught |
| P0-18 | Prompt | `scripts/lockfile.mjs:148` | `stdio: 'ignore'` discards npm's diagnosis, and the failure branch asserts a cause it destroyed the evidence for | confirmed | #728 | Filed with P0-17: same file, same function, one fix |
| P0-19 | Mechanical | `scripts/browser-smoke.mjs:546-554` | The first-run-chooser step cannot fail | confirmed | #743 | The only one of 125 step bodies with no assertion. The helper is correct; the step's name is the defect |
| P0-20 | Mechanical | `scripts/browser-smoke.mjs:2262,2286-2288` | The QR decode assertion is silently skipped where `BarcodeDetector` is absent | confirmed | #743 | Absent on both local channels, headed and headless — it has never run anywhere. Not a coverage hole: `scanner.test.ts:482-525` round-trips the encoder through a real decoder |
| P0-21 | Functional | `scripts/wiki-check.mjs:91-100` | Images are matched by basename only, in both directions | confirmed | #744 | Three faces reproduced: false negative on a nested path, false positive on a correct one, and a directory reported as an orphaned image |
| P0-22 | Functional | `scripts/wiki-check.mjs` | Nothing checks a page is reachable from `_Sidebar.md` | rejected | — | The docstring's check 4 promises only that sidebar links *resolve*, which the code delivers. No claim to falsify. 98 pages / 96 sidebar targets / 0 unreachable. Belongs in the open wiki programme |
| P0-23 | Functional | `.github/workflows/tests.yml` | No push or PR check runs the production build | duplicate #599 | — | — |
| P0-24 | Functional | `scripts/setup-ocr-assets.mjs:74-77,198-201` | The models are fetched unpinned with no checksum, and `--require` accepts any non-empty file | confirmed (reframed) | #729 | 7 bytes of ASCII passes the gate. Reproducibility is fine in practice — upstream has one commit, 2017-09-14 — so filed as hardening, not a live break |
| P0-25 | Prompt | `scripts/wiki-screenshots.mjs:30` | The mandated screenshot script hard-codes Edge with no override | confirmed | #745 | Filed with P0-26. Its docstring declares the dependency and Playwright's error names its own remedy, so `cosmetic` |
| P0-26 | Mechanical | `scripts/generate-icons.mjs:95` | A literal `replace` silently emits cropped icons and reports the size it was asked for | confirmed | #745 | Mutation: reordering the SVG attributes yields a top-left crop at the right pixel dimensions, exit 0. Self-catching at review since the PNGs are tracked |
| P0-27 | Mechanical | `scripts/browser-smoke.mjs:4438` | The screenshot path is cwd-relative | confirmed | #720 | Folded into P0-1's issue |
| P0-28 | Functional | `scripts/browser-smoke.mjs:4617-4632` | The PWA block skips silently when `dist/` is absent and the run reports success | unverifiable | — | Not demonstrated this phase; `e2e.yml` builds first so CI is unaffected. See §11 |
| P0-29 | Mechanical | `tsconfig.node.json:25` | Only `vite.config.ts` of the four root config `.ts` files is in any TypeScript program | rejected | — | Fact true, harm limb disproved: both worktree configs use `as` casts, which erase the check anyway; a probe tsconfig including all four compiles clean. Residue folded into P0-31 |
| P0-30 | Mechanical | `.prettierignore` | `npm run format` rewrites files inside other agents' worktrees | confirmed | #740 | ESLint and Vitest both exclude the path deliberately, with comments. Measured: 54.6 s of 78.6 s spent on other trees |
| P0-31 | Prompt | `vitest.worktree.config.ts:5-9` | The docstring's reason for the file existing is no longer true | confirmed | #742 | Root config collects the same 800 files from inside a worktree; the two file lists `diff` empty. The claim is repeated in `CLAUDE.md`, `AGENTS.md` and the verify skill |
| P0-32 | Mechanical | `public/recovery.js` | The pre-mount escape hatch is covered by no tsconfig, no ESLint config and no test | confirmed | #741 | Mutation introducing a guaranteed `ReferenceError` leaves type-check and lint green. Its sibling `coi-bootstrap.js` *is* tested from disk |
| P0-33 | Mechanical | `eslint.config.js:54-57,227-233` | `no-undef` is off and no `.mjs` is in any TypeScript program | rejected | — | Forcing the rule on gives 94 errors, 100% false positives (browser globals inside `page.evaluate`), and zero real hits. The proposed sharpening onto `secret-scan.mjs` was disproved: it has no cold branches |
| P0-34 | Prompt | `eslint.config.js:29` | The ignore-list comment mis-describes `public/**` as generated | confirmed | #741 | Folded into P0-32's issue — same root cause, same fix |
| P0-35 | Mechanical | `.prettierignore:24-25` | `**/fixtures/**` excludes two ordinary modules and misses the byte-exact golden | unverifiable | — | Not verified this phase. See §11 |
| P0-36 | Mechanical | `bridge/Dockerfile:34-45` | The bridge image ships no `node_modules` but the bridge imports `zustand` | confirmed | #736 | Severity `unusable`. Reproduced by the lead independently. Built and run from three historical trees — it has never worked |
| P0-37 | Mechanical | `.dockerignore:83-104`, `src/lib/docker-context-ignore.test.ts` | SQLite sidecars reach the published image layer while the `.sqlite` is excluded | confirmed | #737 | `strings` recovered every row from a copied `-wal` while the excluded `.sqlite` held 4096 bytes of nothing. The guard test compares the two pattern lists, so a shape in neither is never sampled |
| P0-38 | Mechanical | `bridge/serve.mjs:24`, `bridge/mcp.mjs:27` | The documented quick start never loads `bridge/.env` | confirmed | #738 | Severity `unusable`. Run A (as documented) refuses to start; run B from `bridge/` reads both values from the same file |
| P0-39 | Mechanical | `docker/nginx.conf.in:64` | `always` puts `immutable` on a 404, so a missing asset is cached for a year | confirmed | #739 | A real browser honours it across reload and restart; only `cache: 'reload'` escapes. One poisoned URL fails the whole service-worker `addAll` |
| P0-40 | Functional | `docker/nginx.conf.in:91-97` | Under a sub-path the catch-all answers the whole origin | rejected | — | Documented intent contradicts it: `/healthz` sits at the origin root deliberately, and every deployment doc puts a reverse proxy in front. No doc invites sharing the origin |
| P0-41 | Prompt | `Run.ps1:204`, `index.html:459` | Both tell users to install Node 20 | confirmed | #746 | On Node 20 `npm ci` and `npm run dev` work; `npm run build` dies on `registerHooks` with an error naming no version. Not in #262's list |
| P0-42 | Mechanical | `Run.ps1:211` | The documented first-touch path runs bare `npm install` | confirmed | #747 | A real install strips all 14 `libc` arrays and drops `@emnapi/*`; `npm ci --dry-run` then refuses the result |
| P0-43 | Functional | `docker/nginx.conf.in:46-47` | The manifest is served as `application/octet-stream` and the `gzip_types` entry is dead | rejected | #748 | Confirmed factually; no consumer cares — Chromium parses it, `ERRORS: []`. The one-line `types` fix is noted as a rider on #748 |
| P0-44 | Prompt | `docker/nginx.conf.in:88-90,73` | The comment explaining the repeated `Cache-Control` describes a mechanism that does not exist | confirmed | #748 | Marker headers show a deep link is answered by the regex location. Line 94 *is* load-bearing — for `recovery.js`, `404.html`, `50x.html`, `icons/`, `ocr/` |
| P0-45 | Functional | `hacs.json`, `custom_components/gubbins/manifest.json` | Nothing runs `hacs/action` or `hassfest` | confirmed | #749 | Filed `enhancement`: the repo claims HACS compatibility, never that anything validates it. #674 is the precedent that already shipped |
| P0-46 | Functional | `Run.ps1:75` | `$BasePath` duplicates `DEFAULT_BASE_PATH` untested | rejected | — | Audiences are disjoint: every documented use of `GUBBINS_BASE_PATH` is a Docker build arg; `Run.ps1` is the double-click quick start. No parity comment either, so the "mirrors X" rule does not fire. See §11 |
| P0-47 | Mechanical | `package-lock.json:3` | The lockfile records `0.31.0` against `package.json`'s `0.32.0` | rejected | #728 | No consumer found: nothing in the repo reads it, and GitHub's SBOM names the root from the branch (`versionInfo: main`). Recorded as a rider comment on #728 |
| P0-48 | Functional | `package.json:89` | `typescript: "^6.0.3"` is wider than `typescript-eslint`'s `<6.1.0` peer cap | rejected | — | Premise dead: TypeScript's `latest` is 7.0.2 and the 6.x line ends at 6.0.3, so the caret cannot reach the cap. The live boundary is #186's. See §11 |
| P0-49 | Mechanical | `.gitignore` | `.claude/worktrees/`, `.claude/settings.local.json` and `.tanstack/` are ignored by nothing tracked | confirmed | #730 | Hidden only by `.git/info/exclude` and a per-user ignore, neither written by anything in the repo. A worktree stages as one gitlink with a warning, not a second tree — hence `cosmetic` |
| P0-50 | Mechanical | `.gitignore:77-91`, `.dockerignore:81-88` | The data-artefact rules miss every compressed or renamed dump | confirmed | #731 | A staged `.gz` produces zero added lines, so neither the hook nor CI can see inside it. `inventory.csv` and `backup.bak` dropped from the claim as wrong |
| P0-51 | Mechanical | `.gitignore:102-106` | `id_rsa` / `id_rsa.*` is narrower than the `id_rsa*` CLAUDE.md states | rejected | #731 | The detector flags a PEM/OpenSSH body regardless, and CI runs it. Folded into #731 as a one-line rider rather than filed |
| P0-52 | Functional | `.editorconfig:5-11` | 2-space indent applies to the 4-space Python integration | confirmed | #732 | The 2- and 6-space outliers in the histogram are all docstring prose, not code. `.editorconfig` is the only style declaration reaching those files |
| P0-53 | Prompt | `.env.example:3-4` | Claims to be the "single tracked template" when `bridge/.env.example` also exists and holds the credentials | unverifiable | — | Not verified this phase. See §11 |
| P0-54 | Mechanical | `scripts/lockfile.mjs:43`, `Dockerfile:35`, `bridge/Dockerfile:34` | Three more Node declarations #262 does not name | confirmed | #724 | The `lockfile.mjs` one carries a parity comment and is filed with P0-7; the two Dockerfiles are values, recorded for #262 |

### Phase 1 — Database engine, driver, migrations and shared repository seams

Pinned SHA: `16ef2e07edec6d7156ef600d0f8636a8d0079ec8`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| P1-1 | Mechanical | `src/db/worker/database.worker.ts:36` | One rejected message wedges the FIFO chain, so every later request is dropped silently | rejected | — | No reachable trigger: only the driver posts to this worker, and it posts a typed envelope built in code. Every other path through `handle` was walked and none can reject. Hardening, not a defect |
| P1-2 | Mechanical | `src/db/client.ts:79-88`, `src/db/repositories/index.ts:140-319` | `disposeDatabase()` permanently poisons all 26 repository singletons, and a failed replace-restore never reloads | confirmed | #750 | Severity `unusable`. Drove the real `overwriteDatabaseFile` with the storage layer forced to fail; the stack lands on the real `client.ts:84` inside the real `safe-mode-actions.ts:63`. Split brain found beyond the claim: `getDatabaseDriver()` self-heals while repositories do not, so Merge succeeds and every screen still errors |
| P1-3 | Mechanical | `src/db/client.ts:73-88` | A concurrent `getDatabaseDriver()` during the restore write window opens a second worker over the file being overwritten | unverifiable | — | The absence of a quiescing state is proved; the consequence is not. The candidate's three named callers are all user-gesture only. See §11 |
| P1-4 | Prompt | `src/db/errors.ts:168` | The comment's arithmetic `19 | (9 << 8)` yields 2323, a different SQLite code | confirmed | #751 | Filed with P1-5. Verified against sqlite.org and against four real constraint violations. The existing test pins the value, so it cannot catch the derivation |
| P1-5 | Mechanical | `src/db/errors.ts:189-192` | The collapse rule is described generally and implemented for constraints only | confirmed (narrowed) | #751 | Every behavioural consequence **rejected**: the shipped VFSes have no shared memory, so WAL is impossible and the busy/read-only extended families cannot arise. Comment accuracy only |
| P1-6 | Performance | `src/db/rpc/worker-driver.ts:297`, `src/db/worker/database.worker.ts:220` | Whole-database payloads cross the worker boundary by copy, with no transfer list | confirmed | #752 | Measured in a real COI worker: copy 2.00× payload vs transfer 0.99×. Filed with P1-7. The candidate's remedy was corrected — `writeDatabaseFile` cannot transfer, because `BackupDialog` reads `parsed.sqlite.byteLength` in render |
| P1-7 | Prompt | `src/app/error/safe-mode-actions.ts:32-33`, `src/features/backup/build-backup.ts:143-145` | Two `.slice()` copies justified by a WASM-memory dependency this engine has never had | confirmed | #752 | Both sites hold a `WorkerDatabaseDriver`; the export arrives as a structured clone, and the engine's own export already slices. The module's memory is not shared (import table read) |
| P1-8 | Functional | `src/db/rpc/worker-driver.ts:372-374`, `src/app/error/safe-mode-actions.ts:62-73` | The plain-OPFS restore reports storage failure but never success, so a latched Hard Stop cannot clear | rejected | — | The tier store is **not persisted** (its own header says so) and every successful `opfs` overwrite reloads. On the crash screen the observer is not even installed — it is registered after a successful boot |
| P1-9 | Mechanical | `src/db/worker/database.worker.ts:166,187-196` | `runMerge`'s local driver closes over a connection `discard()` may close underneath it | rejected | — | Disproved by test, not by grep: a wrapper making the first `transaction` throw recorded **zero** driver calls afterwards in both merge modes. No catch-and-continue exists in the 13 modules `runSnapshotMerge` reaches |
| P1-10 | Mechanical | `src/db/rescue-driver.test.ts:56-63` | The non-leakage assertion cannot fail | rejected | — | Mutating `#teardown` to drop `terminate()` turns **three** other tests red. Mutating `disposeDatabase` to skip `close()` turns this one red, so it is not a no-op. Below #252's bar |
| P1-11 | Prompt | `src/db/worker/database.worker.ts:8-13` | The header attributes request serialisation to the synchronous VFS rather than the FIFO chain | rejected | — | The two comments do not disagree — the header's own first sentence already credits the FIFO chain. No consequence above the bar |
| P1-12 | Functional | `src/db/search/parseASTtoSQL.ts:648,:644` | The tag predicate folds ASCII only, contradicting the dictionary's own fold | confirmed | #753 | Severity `wrong-data`. Lead spot-checked: `Ölkanne`=['i1'], `ölkanne`=[], folds equal. **Not** a duplicate of #390, which names three sites and explicitly delegates tags to #342 (write side, shipped). The `CONTAINS` arm at :644 is a first-class second site — it is what `tag:` produces |
| P1-13 | Functional | `src/db/search/parseASTtoSQL.ts:280`, `src/db/repositories/item/sql.ts:114` | The ranking lower-cases in JS and compares in SQL, so a non-ASCII capability key always scores 0 | confirmed (broadened) | #754 | Broader than claimed: **no** spelling both finds and ranks. Reaches the default Inventory ordering and the bridge's `/search`, both of which pass no sort |
| P1-14 | Performance | `src/db/search/parseASTtoSQL.ts:635,:644,:648` | The tag predicate's correlated `EXISTS` should be the set-based `IN` the list filter uses | rejected | — | Premise false — the two surfaces test different things (name vs id). Counter-measured: the `IN` rewrite is 3–14× **slower** on any multi-condition tree, because `EXISTS` streams to the page LIMIT. #168 chose `EXISTS` deliberately and measured it |
| P1-15 | Functional | `src/db/search/fts.ts:39-47` | A separator-only term compiles to an empty FTS phrase: nothing alone, ignored beside a word | confirmed | #755 | Severity `wrong-data`. `"M3 -"` returns the same rows as `"M3"`, so adding a term narrows nothing. Reachable from every surface but the plain-English box. Filed with P1-18 |
| P1-16 | Mechanical | `src/db/search/parseASTtoSQL.ts:446-471` | The `EQUALS` kind chain falls through to text equality with no exhaustive guard | rejected | — | `field-registries.test.ts` (#247) goes red at runtime on both routes a new kind can arrive. All four bare `default:` legs throw a typed `SearchAstError`. No live wrong result |
| P1-17 | Functional | `src/db/search/parseASTtoSQL.ts:314-330` | A location term anywhere in the tree drops the sidebar's location scope | confirmed (narrowed) | #756 | Negated-location only, reachable via the plain-English box ("not in the attic"). The OR-arm leg is **rejected** — no user surface offers a location term. #626's rationale is stated in terms of unsatisfiability, which does not hold under negation |
| P1-18 | Prompt | `src/db/search/fts.ts:34-35` | The `null` sentinel means "match everything" in one caller and "match nothing" in two | rejected standalone | #755 | No consequence today — `core.ts`'s branch is unreachable, because only whitespace produces `null`. Folded into P1-15: fixing that would activate it and return the whole catalogue |
| P1-19 | Prompt | `src/db/search/parseASTtoSQL.ts:14-16`, `src/db/search/ast.ts:73-78` | The depth cap is presented as the guard against a runaway tree; node count is unbounded | confirmed (narrowed) | #757 | Mechanism corrected: the real limit is `SQLITE_MAX_EXPR_DEPTH` at **998 terms / ~4.9 KB**, not 32,767 parameters — ~33× more reachable. `astError` returns null, so it surfaces as "Your items couldn't be loaded" with a Retry that fails identically |
| P1-20 | Prompt | `src/db/search/parseASTtoSQL.ts:435-437` | The comment overstates what `unicode61` folds | rejected | — | Misread: the passage is about **case** folding, which `unicode61` does perform for accented letters in every script tested (`Ố`, `Ö`). The counter-examples test diacritic stripping, which the comment does not claim |
| P1-21 | Functional | `src/db/sqlite-header.ts:96-103` | The header gate accepts a WAL-format database the shipped build cannot open | confirmed (narrowed) | #758 | Severity `unusable`. Driven in a real COI browser worker: header gate `ok:true`, deep gate `SQLITE_CANTOPEN`, bytes written verbatim, next boot fails. VFS asymmetry confirmed — `importDb` rewrites bytes 18/19, `writePlainDatabaseFile` does not. The `data-loss` half is **disproved**: a restore point is captured and proven on every route. Provenance speculative — see §11 |
| P1-22 | Mechanical | `src/db/worker/db-file-store.ts:56-58` | The sahpool sidecar cleanup discards `unlink`'s result and lets a post-commit throw escape | confirmed (narrowed) | #759 | The return-value leg is **disproved** (false = absent = benign, and the comment is right). The throw leg is real: reported as "restore failed" after the bytes committed, skipping both `acknowledgeDbLoss()` and `disposeDatabase()`. Only `-journal` is reachable; `-wal`/`-shm` are impossible without shared memory |
| P1-23 | Functional | `src/features/backup/restore-backup.ts:203-231` | The backup Replace path overwrites the live database after a 16-byte magic check only | confirmed (narrowed) | #760 | Severity `unusable`. Three residues driven against real zips: a manifest-less backup, a malformed checksum block, and a checksum-correct copy of an already-corrupt database. **Not** a duplicate: #198 and #501 both hardened only the two *rescue* restores, and #501's body cites Replace as the exemplar |
| P1-24 | Mechanical | `src/features/backup/backup-format.ts:488-497` | `looksLikeSqlite` duplicates `isSqliteFile` byte for byte | rejected | — | Substitution proved safe (type-check clean, 256 tests green), but the magic string cannot change, so "only one would be updated" cannot occur. The real residue is gating strength, which is P1-23 |
| P1-25 | Mechanical | `src/db/worker/verify-binary.ts:118-125` | The temp-file cleanup calls `sqlite3.wasm.FS`, which the shipped build does not provide | confirmed (narrowed) | #761 | `typeof sqlite3.wasm.FS` is `undefined` in both builds; re-opening after "cleanup" succeeds. A working `sqlite3__wasm_vfs_unlink` **does** exist (return code 0). No memory consequence — the throwaway worker is terminated moments later. The comment's stated lifetime is wrong |
| P1-26 | Performance | `src/db/restore-candidate.ts:131`, `src/db/worker/verify-binary.ts:45` | Verifying a candidate holds several full copies of the database | confirmed | #762 | Measured in a real COI browser worker: **5.7× at 50.4 MiB (+285 MiB)** and 5.2× at 201.6 MiB (+1041 MiB), two runs each, <1% spread. **Four** live copies at peak, not three. The "MEMFS counts against WASM linear memory" leg is **disproved** — MEMFS is plain JS heap. Filed with P1-27 |
| P1-27 | Performance | `src/app/error/safe-mode-actions.ts:237-238` | The whole file is read into memory before the 16-byte check that would reject it | confirmed (narrowed) | #762 | +202.3 MiB in 120–160 ms before the check, measured. Narrowed: an `accept` filter exists, the read follows a pick and a save-destination choice, and the accept path needs the read anyway. Not a duplicate of #641, which is scoped to the image pickers |
| P1-28 | Mechanical | `src/lib/storage-keys.ts:366`, `src/db/tab-lock.ts:23` | The Web Locks name is a bare literal in two places | rejected | — | Claim collapses: `storage-keys.test.ts` scans the whole source tree for `gubbins:` literals and names the file and key. Renaming it turns the guard red. The drift is loud, not silent |
| P1-29 | Mechanical | `src/db/tab-lock.test.ts:88` | The only `release()` assertion cannot fail | rejected | — | Mutating `release` to **throw** turns it red, so it is not a no-op. `release()` has no production caller. Below #252's bar |
| P1-30 | Functional | `src/db/tab-lock.ts:9-11` | A bfcached holder never releases, so the waiting tab's automatic switch never fires | confirmed | #763 | Driven in real Chromium with bfcache enabled. A queued request does **not** evict the frozen holder (30 s+), so `whenReleased` never settles — but a *fresh* request does, so the button rescues it (`WebLocksContention`). The copy promises an automatic switch that silently stops. Chromium only — see §11 |
| P1-31 | Prompt | `src/app/boot/BootScreens.tsx:317-318` | "Use this tab" promises a takeover the code cannot perform | rejected | — | Disproved by P1-30's browser run: in the one scenario where the button is pressed, it **does** acquire the lock. Both catalogs carry the keys |
| P1-32 | Performance | `src/db/migrations/v1-initial.ts:747` | `idx_items_is_active` is preferred over all three purpose-built partial indexes, so every list page full-sorts | confirmed | #764 | **A regression against #164 and #172.** Across 54 `is_active` statements captured from 69 real read methods, zero plans use any of the three. Interleaved A/B at 50k: default list 4.49 → 0.24 ms (0.054×), ratio worsening with row count. `git log -S` shows the boolean index predates #164's. Prescription corrected: a bare drop makes ~25 of the 38 statements *slower* |
| P1-33 | Performance | `src/db/migrations/v1-initial.ts:1328` | `items_fts_au` has no `OF` list and the auto-stamp trigger makes it fire twice | confirmed | #765 | Control proves the mechanism (setting `updated_at` explicitly drops firings 2→1). Ratios 6.2–7.4× on single updates, 7.9× on a location delete, 12.1× on a category erase. The **space** claim is false — segment bytes are identical and `optimize` converges all variants. Biggest payer is stock movements via the three-deep recompute chain |
| P1-34 | Mechanical | `src/db/worker/sqlite-bootstrap.ts:60,:75` | A rejected module or pool load is memoised for the worker's life and never retried | confirmed | #766 | A rejected RPC response never sets `isUnavailable`, so the worker is not replaced. All three sahpool rescue actions stay dead. The library memoises its own rejection too, so the fix needs `forceReinitIfPreviouslyFailed` as well |
| P1-35 | Prompt | `src/db/worker/sqlite-bootstrap.ts:2-8,:108` | A wasm-instantiate failure is untyped, and `openConnection` maps every failure to `OPFS_UNAVAILABLE` | rejected | — | `UNKNOWN` and `INIT_FAILED` render the same screen, hint and actions. The second leg is **disproved**: `fromUnknown` prefers the extracted result code, so a disk-full is `SQLITE_FULL` and a corrupt file `SQLITE_ERROR` |
| P1-36 | Mechanical | `src/db/worker/sqlite-bootstrap.ts:111-117` | Only `foreign_keys` is set; no `busy_timeout`, `journal_mode` or `synchronous` | rejected | — | `SQLITE_BUSY` is unreachable: the tab lock fails closed, one connection, a FIFO chain, and no WAL. The residual busy comes from the OPFS proxy's own retry, which `busy_timeout` does not govern |
| P1-37 | Mechanical | `src/db/worker/sqlite-bootstrap.ts:188-196` | `probeFts5`'s bare catch reports any failure as "this build lacks FTS5" | confirmed | #767 | The boolean return destroys the result code, so this is the one leg P1-35's rebuttal does not also kill. #500 is the precedent for exactly this distinction |
| P1-38 | Functional | `src/db/migrations/__fixtures__/schema-snapshot.ts:105` | The golden fixture sorts by name, so it cannot pin trigger creation order | confirmed (reframed) | #775 | **The corruption invariant is not demonstrable** — a fresh build in either order is healthy across five configurations (`integrity_check` ok, FTS ok, correct results), and a drop/recreate on a populated database is too. What survives: moving the statement leaves the whole suite green, and no comment records the relation. Folded into the sweep |
| P1-39 | Prompt | `src/db/migrations/v1-initial.ts:2069-2070` | The comment claims parity with `checkouts.source_location_id`, which is `NO ACTION`, not `SET NULL` | confirmed | #775 | Both deletes driven: the location delete aborts, the supplier delete nulls. `fk-refs.ts` and `tombstone.ts` label it correctly, so the migration comment is the odd one out. Folded into the sweep |
| P1-40 | Prompt | `src/db/migrations/enum-checks.test.ts:97-103` | The `UNCONSTRAINED` registry claims to be one of two complete halves | confirmed (narrowed) | #775 | The "invisible to the guard" claim is **false** — adding a CHECK does trip it, though with the wrong diagnosis. Four vocabulary-bearing columns sit in neither map. Residue of #605, not a duplicate. Folded into the sweep |
| P1-41 | Performance | `src/db/migrations/v1-initial.ts:1653,:1659,:1521,:1294,:1006,:1032,:1063` | Seven indexes duplicate the leading columns of a UNIQUE index | confirmed | #768 | All seven pairs plan identically to their UNIQUE fallback (seek→seek, covering scan→covering scan). The **write-amplification** leg is **rejected**: no measurable difference with the real schema, and under 2% of an insert isolated — the six triggers dominate by ~16×. The real cost is storage: 867 pages, 11.5%. Filed with P1-48 |
| P1-42 | Performance | `src/db/migrations/v1-initial.ts:1770` | `idx_stock_deltas_placement` cannot order the delta replay | rejected | — | The claimed query does not exist. The replay reads the **whole** ledger once via `keysetPage` and partitions in JavaScript. The proposed index is a net loss (0.2632 vs 0.2251 ms). Separate observation in §11 |
| P1-43 | Functional | `src/db/migrations/migration.ts:48-78` | `baselineFingerprint` hashes comments, so a comment edit refuses every existing database | confirmed mechanically, rejected as a finding | — | Both halves reproduced, including the end-to-end `SCHEMA_STALE` on a one-word comment edit. **#274 chose this deliberately** — the derived fingerprint replaced a hand-maintained counter precisely because it cannot *miss* a change, and the project documents wipe-on-schema-change as the accepted workflow. The Prettier half is false: Prettier does not reformat template-literal contents |
| P1-44 | Mechanical | `src/db/migrations/migration.ts:67-74` | Positional params concatenate with no delimiter, so two arrays can collide | rejected | — | Lead spot-checked the collision (`79e59669` both ways). But every seeded param is a non-null string — fixed UUIDs, slugs, prose, a grant blob — and no realistic edit collides. Hardening |
| P1-45 | Mechanical | `src/db/migrations/v1-initial.ts:1593,1607,1621,1682,1700,1716` | The recompute switch has no `COALESCE`, so a missing row silently stops the projection | rejected | — | Mechanism demonstrated (both levels and the whole capture stop, no error). But no shipped code can delete the row: it is `NOT_SYNCED`, in no clone-wipe set, in none of the 20 erase statements, and the only statements touching it are six `UPDATE … WHERE id = 1`. Hardening |
| P1-46 | Prompt | `src/db/migrations/v1-initial.ts:2601-2608` | `settings` carries no length CHECK, though two docstrings say every user-editable column does | confirmed (narrowed) | #769 | `settings` only — `webhooks.secret` **rejected** (generated, never typed; `secret_ref` *is* capped). Every text column in the 56-table schema was enumerated; one table remains. #346 scoped itself to nothing and its closing note asserts completeness, so this is its named residue. #349 is the precedent |
| P1-47 | Mechanical | `src/db/worker/sqlite-bootstrap.test.ts` | The suite cannot observe the pragma, the FTS probe or either failure path | confirmed (narrowed) | #770 | Leg (a) only. Deleting `PRAGMA foreign_keys = ON` leaves all 22 worker tests green, and the ~18 repository tests that look like coverage each issue it themselves. `feeds.ts:517` drops a production JOIN naming this file |
| P1-48 | Performance | `src/db/migrations/v1-initial.ts:1662` | `idx_stock_batches_expiry` is not partial, where its siblings are | confirmed (understated) | #768 | Stronger than claimed: referenced by **zero** of 118 captured statements, so it is dead rather than over-broad. The one expiry read drives from `item_id`; FEFO ordering is done in TypeScript. Partial form saves 86% of it. Filed with P1-41 |
| P1-49 | Mechanical | `src/db/repositories/stock-batches.ts:56,:86` | The batch upsert targets `ON CONFLICT(id)` on a table that also has a UNIQUE natural key | confirmed (narrowed) | #771 | Severity `unusable`. Driven end to end through `restoreSnapshot` and the real repository methods. **Not** reachable from any in-repo write path — every id-originating write derives — but contagious once in, and repaired only for SERIALISED. Filed with P1-50 |
| P1-50 | Mechanical | `src/db/migrations/v1-initial.ts:1684-1691` | The `item_stock` recompute trigger has the same unnamed-constraint hole | confirmed | #771 | Same root cause, filed together. A **third** symptom was found: the decrement side addresses by derived id, so it silently no-ops — the user sees success, the ledger records −1, nothing changes. The "raw SQLite text reaches the user" leg is **disproved** |
| P1-51 | Functional | `src/features/inventory/batches.ts:137,:237` | The FEFO tie-break uses `localeCompare`, so two devices in different locales draw different lots | confirmed | #772 | Severity `wrong-data`. The ids do **not** converge, because the order decides *which lots enter the plan*, not just their sequence — 10−5 converges to 0. Reachable without non-ASCII: 0.38% of random UUID pairs reorder between da/nb and everything else. Root cause is a Phase-5 file; the consequence lands in `stock-batches.ts` |
| P1-52 | Prompt | `src/db/repositories/text-limits.ts:8-10` | The module asserts a schema backstop two columns do not have | confirmed | #769 | Merged with P1-46 — same root cause, one fix |
| P1-53 | Performance | `src/db/repositories/reservations.ts:78-101` | `readAvailability` binds one placeholder per id with no chunking | rejected | — | The limit is a fixed 32,766 on **both** shipped engines; `readAvailability` throws at N=32,765, which no caller can reach. `listRelationsForItems` breaks at *half* that N on the same id list, and 13 unchunked sites exist. #561 never mentions bind arity, so there is no policy to violate |
| P1-54 | Performance | `src/db/repositories/checkout-plan.ts:376-384` | `planCheckInAllForTarget` is an unbounded N+1 of awaited round-trips | rejected | — | `1 + 2N` confirmed exactly. But a real browser worker round-trip measures **0.0425 ms**: ~1.7 ms at N=20 on desktop, ~17 ms on a 10×-slower phone, behind a confirmation dialog with a spinner — and not the dominant term (100 statements vs 41 reads) |
| P1-55 | Functional | `src/db/repositories/mappers.ts:613-621` | `parseStringArray` coerces a non-string option to its string form | confirmed | #773 | Severity `wrong-data`. `'["A",null,3]'` reads back as `["A","null","3"]`, renders as pickable options, and a later save relaunders it into storage and syncs it. Reachable via restore, peer sync and bridge push. The same file already *filters* in two siblings, each with a comment saying why |
| P1-56 | Prompt | `src/db/repositories/like.ts:10-11` | "Shared rather than re-declared per repository" — it is re-declared in `parseASTtoSQL.ts:797-800` | confirmed | #775 | Substitution proved safe (type-check clean, 188 tests green), then reverted. No fourth special character is missed (20 probed against a real engine). Folded into the sweep |
| P1-57 | Mechanical | `src/db/repositories/tombstone.ts:380` | `record()` discards the builder's params and re-lists them by hand | rejected | — | Drift demonstrated (a transposed row, whole suite green). But `record()` has **no production caller** — only two throw/no-throw assertions. Below the bar |
| P1-58 | Prompt | `src/lib/text-limits.ts:110-131` | `truncateByCodePoints` claims to cut between whole characters and cuts between code points | rejected | — | In context the docstring means code points — its whole paragraph is about lone surrogates. A ZWJ or regional indicator is representable and round-trips, so the harm the function exists to prevent does not occur. A Phase-5 file |
| P1-59 | Functional | `src/db/repositories/types/history.ts:6-26`, `src/db/repositories/mappers.ts:481-492` | The item ledger's actor is written, indexed and FK-protected, and projected by no read | confirmed | #774 | Severity raised to `data-loss` by P1-60. Three wiki pages, closed #79's own requirement and a COMPLETE plan doc all claim the Activity Log answers *who*; `actorUserId` reaches zero `.tsx` files. The location ledger carries it, so one mapper fix unblocks all three symptoms |
| P1-60 | Functional | `src/features/storage/triage-actions.ts:61-76` | The cold-storage archive omits the actor, and the source rows are then deleted | confirmed | #774 | Drove the real `archiveAndPruneHistory`: archive keys carry no actor, post-prune count 0. **Folds into P1-59** — one root cause. Narrowed: the prune *is* gated on a proven save, and an ordinary backup preserves the column, so the loss bites a user who prunes without a backup predating it. The prune is deliberate and permission-gated |
| P1-61 | Mechanical | `src/db/repositories/types/history.ts:9,:20` | `HistoryAction` is a closed union over a deliberately unconstrained column | rejected | — | Preserving an unknown action is documented **three times** as the forward-compatibility contract (`activity-kind.ts:27`, `history-format.ts:55`, `event-types.ts:136`), and no exhaustive switch or unsound lookup exists. The four normalising siblings are mutable settings rows, not an append-only ledger |
| P1-62 | Prompt | `src/db/repositories/types/items.ts:45,:75,:87-92` | Three raw money columns are documented "in the base currency"; they hold micro-units | confirmed (narrowed) | #775 | Measured ratio raw/DTO = 1,000,000 for all three. #286's residue — that commit touched no file under `types/`. No live consumer is misled (the bridge uses DTOs; the one raw-SQL money site documents it correctly). The eight unit-less columns leg is **rejected** — a missing doc is not a wrong doc. Folded into the sweep |
| P1-63 | Prompt | `src/db/repositories/types.ts:4-6`, `src/db/repositories/types/categories.ts:303-307` | "Mirror the raw SQLite columns exactly" is false both ways, and the LEFT JOIN claim is wrong | confirmed (narrowed) | #775 | The carrying-non-columns direction is **rejected** — the #356 guard catches it at build time (demonstrated) and every such member is self-documenting. The missing-columns direction is real and the guard is blind to it. The join kind is harmless (NOT NULL FK with CASCADE). Folded into the sweep |
| P1-64 | Mechanical | `src/db/errors.ts:196-200` | `extractResultCode` reads only `resultCode`; `node:sqlite` sets `errcode` | confirmed (narrowed) | #776 | "Dead code in the bridge" **disproved** — the bridge never imports the humanising layer. Real costs: the unit suite cannot exercise the constraint path at all, and the bridge answers 500 where `write.ts:387` means 422. Two in-repo comments already name the gap |
| P1-65 | Mechanical | `src/test/drivers/memory-driver.ts:75`, `bridge/src/node-driver.ts:198` | Both Node drivers flatten every batch failure to `TRANSACTION_FAILED` | confirmed | #777 | Independent of P1-64: the fallback is hard-coded where the worker deliberately passes none. A disk-full would never raise the storage tier. `isQuantityFloorViolation`'s own docstring names the divergence |
| P1-66 | Mechanical | `src/test/drivers/memory-driver.ts:48,:65`, `bridge/src/node-driver.ts:168,:188` | `node:sqlite` silently keeps only the first statement of a multi-statement prepare | rejected | — | Instrumented `DatabaseSync.prepare` across **both** suites — 12,470 app tests / 811 files and 1,191 bridge tests / 79 files — with the detector proved live: zero multi-statement calls. A static literal sweep finds only SQL-injection fixtures. Folded into #782 as an axis |
| P1-67 | Mechanical | `src/test/drivers/crashed-driver.ts:29` | The file declares `IDatabaseDriver['exportBinary']`, which does not exist | confirmed (narrowed) | #783 | TS2339 demonstrated under a probe tsconfig, but inert — the cast erases and the object is laundered anyway. The interface leg is **rejected**: that member is one of nine worker-only members, a consistent split both production callers respect. #601 sanctions the test-tree exclusion |
| P1-68 | Performance | `src/db/rpc/worker-driver.ts:243-246` | `queryOne` materialises the whole result set on both real drivers | rejected | — | An independent re-sweep of all 134 non-test sites found every one bounded by a primary key, a unique index, an aggregate, a PRAGMA or an explicit LIMIT. The docstring makes no cost claim and has misled nobody |
| P1-69 | Mechanical | `src/test/drivers/memory-driver.ts:51`, `bridge/src/node-driver.ts:171` | `lastInsertRowId` is 0 where the interface documents null | rejected | — | Zero readers anywhere; every write path uses UUID text ids. Contract-only. Folded into #782 as an axis |
| P1-70 | Mechanical | `src/db/rpc/driver.ts:12` | An integer above 2^53 throws on `node:sqlite` and returns a bigint from sqlite-wasm | confirmed (reframed) | #778 | **Rejected as driver fidelity; real as an unbounded-money-write defect**, severity `wrong-data`. `toStoredMoney` has no exactness ceiling and the price input has no maximum, so a value ≥ ~9.007×10⁹ major units is written, then read back as `null` in the app and as a hard error in the bridge. #677 is the precedent |
| P1-71 | Mechanical | `src/test/drivers/memory-driver.ts:84-88` | A bound `undefined` is NULL in production and a throw in tests | rejected | — | Compile-blocked by `SqlValue` under `strict` + `noUncheckedIndexedAccess` — all three forms error, including the `.map()` case. Every `as SqlValue[]` cast is a variance cast. The 67 runtime hits all traced to another agent's probe file |
| P1-72 | Functional | `src/test/drivers/memory-driver.ts:70-74`, `bridge/src/node-driver.ts:193-197` | Both Node drivers swallow a failed ROLLBACK and keep the connection | confirmed (narrowed) | #779 | The wedge is real when forced (reads back its own uncommitted row). But no reachable path fails a `ROLLBACK` on `:memory:`, and all three bridge paths dispose per request or never run a batch. #555 deliberately scoped itself to the worker — its fix commit touched three files |
| P1-73 | Prompt | `src/db/query-row-shape.test.ts:94,:97,:99-106` | The verified floor carries ~42 sites of slack, though its comment says one widening cannot pass | confirmed | #780 | Measured live counts sites=323 verified=287. The smallest N that trips the guard is **43**; at N=42 the suite is fully green with 42 statements opted out. `git log -L` shows the floor was pinned once (4 sites of slack) and never re-pinned while the read layer grew ~15% |
| P1-74 | Mechanical | `src/test/drivers/keyset-page.ts:26-30` | `pageOf` sorts by ICU collation and seeks by code unit | rejected | — | Divergence demonstrated (row 'B' visited in neither page), but unreachable: every real id is a canonical lower-case UUID, for which all three orderings coincide (2,000-sample check), and both fixtures hold 1–3 rows |
| P1-75 | Prompt | `src/test/segment-layout.ts:2,:4,:85,:86-102` | The comments name jsdom, and `restoreSegmentLayout()` does not restore | confirmed (narrowed) | #781 | Line numbers corrected — the file is 103 lines. Restore leaves `getBoundingClientRect` an **own** property of `HTMLElement.prototype` returning a non-DOMRect. Two tests call restore mid-test to assert the fallback and are therefore testing a stub. The jsdom naming is a repo-wide ~30-file matter — see §11 |
| P1-76 | Mechanical | `bridge/src/node-driver.ts:1-23` | Three parity claims with the app's test driver, held up by no test | confirmed | #782 | `node-driver.test.ts` covers only the #174 statement cache. Rule 3 is feasible and was proved: the bridge **can** import the app test driver, and both agree with each other while disagreeing with the worker. P1-66 and P1-69 fold in as axes |
| P1-77 | Mechanical | `src/db/repositories/mappers.ts:220` | The tri-state thumbnail discriminates with `in`, which is prototype-sensitive | rejected | — | Cannot be made to fail: `Object.prototype` has no such member and no path builds an inherited row. The `errors.ts:138` comment guards the **opposite** direction (an arbitrary key against a fixed registry). An 8-site sweep of the same pattern is uniformly safe |

### Phase 2 — The item repository family

Pinned SHA: `9072d22073d7821f9161292c4734ddb8e87054ab`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| P2-1 | Mechanical | `src/db/repositories/item/core.ts:617-628` | The unlimited-supply guard tests the **pre-update** tracking mode, so a Bulk→Untracked conversion of an unlimited item hits the raw CHECK | confirmed | #798 | Severity corrected to `degraded` — the transaction rolls back atomically, so nothing is wrong-data. Reachability corrected the other way: `ItemDetailsEditor` carries **both** controls and reads the checkbox's applicability from the *saved* mode, so it is a two-click path, not the latent hole the finder claimed. The raw text never reaches the screen: a CHECK abort inside a transaction returns `TRANSACTION_FAILED` (`UNKNOWN` in the worker) and both fall through to call-site copy. The three sibling guards are safe by construction — CONSUMABLE_GAUGE is never convertible — which is why the defect is singular. Unit 7 found the same root cause independently, as a coverage hole in `tracking-mode.test.ts` |
| P2-2 | Performance | `src/db/repositories/item/core.ts:199-210` | `findByBarcode` never uses `idx_items_barcode`; the planner takes `idx_items_is_active` and scans | confirmed | #764 (comment) | **Not filed separately — root cause is #764's verbatim.** 82× on the shipped `sqlite-wasm` engine at 50k, ratio 140/1922/6729× at 5k/50k/200k, identical plan on a hit and a miss. Attribution corrected: the `is_active = 1` term alone causes it; removing the `ORDER BY` changes nothing. Four candidate resolutions were applied and measured — **V2 and V3, the composite shapes #764 steers toward, leave it unchanged or worse (6.37 ms)**; only dropping the boolean index or a partial index without a leading equality fixes it. That constraint and the false comment at `v1-initial.ts:1192-1194` were added to #764 |
| P2-3 | Performance | `src/db/repositories/item/core.ts:226-236` | `findByShortCode` matches with `id LIKE ? \|\| '-%'`, which SQLite can never index | confirmed | #800 | **Survives #764**: 0.87–1.15× under its lever, the plan merely degrading to `SCAN items USING INDEX idx_items_name`. Two independent blockers proved separately — the concatenation blocks indexing under any pragma, and the collation blocks it independently (`LIKE` is case-insensitive, `items.id` collates BINARY), so a JS-side concatenation alone would not fix it. `GLOB` and an explicit range both work. 22/321/1406× at 5k/50k/200k; 62× on the shipped engine. Caveat recorded in the issue: the range rewrite assumes lower-case ids, true of every id the app mints but unchecked |
| P2-4 | Functional | `src/db/repositories/item/create.ts:118-126`, `item/gauge.ts:150-153` | `unit_of_measure`'s length CHECK is mirrored by no guard | confirmed | #799 | `operational_metadata` folded in — same root cause, two columns, both bypassing `normaliseText`. Residue of **#346**, which added the CHECKs and wired the guard into the normaliser. Reachability confirmed rather than assumed: the Foundry `Input` deliberately sets no native `maxLength`, so 501 characters are enterable and are sent. Impact lower than claimed — the gauge editor warns inline first, then leaves Save enabled |
| P2-5 | Performance | `src/db/repositories/item/core.ts:344-345` | `createMany` fans out one `getById` per created item | confirmed | #801 | **Two of three limbs disproved.** The thumbnail is NULL on this path, and `getManyById` projects it too, so it is not a differentiator; and P1-54's 0.0425 ms round-trip figure is for a *sequential* loop — `Promise.all` pipelines at ~0.005 ms, 5× less. What survives is stronger than the batch-shape claim: **neither production caller uses the rows** (`LabScreen.tsx:373` does not even assign the return). ~40% on top of the whole operation at any N. `cycle-count.ts:32-46` already documents the chunking policy (#561) this asks for |
| P2-6 | Mechanical | `src/db/repositories/item/core.ts:344-345` | `createMany` promises input order but `.filter()`s absent reads out | confirmed (latent) | #801 | Filed with P2-5 — one fix removes both, and splitting them invites a batch-shape fix that leaves the filter in place. Mechanism proved (row B's tags land on item C, row C's are dropped, the run reports success). **No reachable trigger**: `getById` filters on nothing but the id, the insert has no `OR IGNORE`, no trigger deletes items, the bridge cannot reach the browser's file, the tab lock forbids a second writer. The present-tense consequence is that `catalog-import.ts:1852`'s missing-item branch is written against a shape the method never produces |
| P2-7 | Functional | `src/db/repositories/base.ts:173-176` | `hasMore` reports false truncation at an exact page boundary | rejected | — | The published integrator contract says `hasMore` is "True when a further page **may** exist", and `ReportRepository.test.ts:2487-2496` pins that with a comment reasoning about the exact-boundary case by name. Eight sites document it; consumers needing certainty pair it with a `COUNT` (#606). The picker's copy is literally true and asserts nothing about a remainder. The `limit + 1` fix is not simple — 40 `toPage` call sites plus the published OData paging semantics |
| P2-8 | Prompt | `src/db/repositories/item/core.ts:791-803` | `softDelete` bypasses the storage Hard Stop as a "deletion that frees space"; it frees none | confirmed | #802 | Measured: 200 removals grew `page_count` 324→339 (+61 KiB) with the free list unmoved and 200 permanent ledger rows added; 200 hard deletes released 89 pages. **`hardDelete` has zero production callers**, so this is the only per-item removal the app offers, and `Storage-Triage.md:61-62` promises it reclaims space. A 14-site sweep shows `softDelete` is the only method in the repository claiming the exemption while appending — nothing to hand Phase 3 |
| P2-9 | Functional | `src/db/repositories/item/stock.ts:349-369` | `adjustQuantity` validates against the grand total but draws from the home placement only | confirmed | #784 | The state is the **intended outcome of the shipped split-stock feature** — `transferStock` deliberately leaves `items.location_id` alone and nothing re-homes an emptied placement. Both ledgers disagree and both sync: `stock_deltas` nets to 5, `item_history` records −8. The UI snaps back with no toast. **No Prompt limb** — the MCP description does say "at its home location". `drawTooWideToLand` (from #592) already guards one of the three callers |
| P2-10 | Functional | `src/db/repositories/item/stock.ts:291` | `transferStock` discards an explicitly chosen untracked lot and draws FEFO instead | confirmed | #786 | Separate from P2-9 — no shortfall arises; it draws the *wrong units*. Reachability proved by driving the real control: it offers "Untracked (5)", caps the quantity at 5 and sends the key. Worse variant found: where the tracked lot is smaller, it is emptied outright. `sell` and `checkout` both honour the same key — driven. One line to fix |
| P2-11 | Performance | `src/db/repositories/item/cycle-count.ts:445-451` | `loadTouched` re-reads every touched item with the full projection | confirmed | #809 | **Severity corrected to `cosmetic`.** It is 2.2% of `authoriseCount` (15.9 ms of 721 ms), which is dominated 78:1 by its own write. Two framings corrected: the read issues once per non-empty list, not three times; and the list is bounded by *drift*, not sheet size, so a clean count reads nothing. A photo-less inventory pays literally nothing (0% coverage measured identical). Real: 12.4 MiB read, structure-cloned with no transfer list and retained by `rowToItem`, entirely unread. Residue of #529 |
| P2-12 | Mechanical | `src/db/repositories/project/assembly.ts:310`, `item/kits.ts:613` | The gauge floor is not recognised by the draw seam's message translation | confirmed | #813 | **The "raw DDL reaches the screen" limb is disproved** — the code is `TRANSACTION_FAILED`, `describeDbError` returns `undefined`, and the raw-text marker blocks it, so the user sees call-site copy either way. **Widened**: the finder said one unclamped gauge write; there are two, and the kit one is repeatable rather than one-shot. The race is a single-user double-tap, not the second device the finder posited. Root cause is Phase 1's `stock-batches.ts`; the two sites are Phase 3 and Phase 7 |
| P2-13 | Functional | `src/db/repositories/gauge.ts:171-183` | Attrition is applied only in the gauge adjust dialog | rejected | — | **#89's research comment scopes it by surface**, verbatim: "Resolve it in the React layer, not the repository… **It must not go into the batch-consumption layer**, which has no idea why stock is moving and would silently tax transfers and reconciles too." The divergence reproduces (dialog 390, bridge 400, BOM 400) but is the designed behaviour. The MCP tool describes a raw delta and claims no attrition, so no Prompt limb either |
| P2-14 | Prompt | `src/db/repositories/item/stock.ts:220-249` | `listSerialisedAtLocation`'s docstring claims a seek; the plan scans | duplicate #764 | — | Duplicate of **#764** — same mechanism, reproduced (21× on dropping the boolean index), and inside #764's stated 54-statement population. The docstring's "only the rows the sheet renders" is in strict parallel with "only the three columns it shows", so it is a claim about the **result set**, which is true and demonstrated. Unlike the two comments #764 already flags, it asserts nothing about a plan. The other four `stock.ts` location docstrings were swept: clean |
| P2-15 | Functional | `src/db/repositories/item/kits.ts` (whole file) | Kits treat an unlimited-supply component as finite stock | confirmed | #792 | `grep -c is_unlimited kits.ts` = 0. Both halves reachable: a fresh unlimited item has quantity 0, so the kit reads "0 buildable"; and an unlimited item **can** hold stock — a committed test asserts the toggle is lossless. The Phase 82 plan's own semantics table states the intended behaviour, and the word "kit" appears zero times in it — an omission, not a decision. `assertDiscreteKit`'s false docstring folded in. Finder's `∞` claim corrected: the kit editor shows the raw integer |
| P2-16 | Functional | `src/db/repositories/item/variants.ts:37-45` | Adding a variant to an item holding stock strands it | confirmed | #787 | #155 and #156 are both read-side; `ReportRepository.ts:211-216` justifies the **read** predicate and records nothing about the write. **There is no UI path to un-parent** — `useSetParent` and `hardDelete` have no callers outside tests, the Variants panel has no detach control, and P2-22 means archiving the last variant does not restore the parent. The three-level chain is reachable through `createVariant` alone, contrary to the finder's `setParent` attribution. No app string states the rule |
| P2-17 | Functional | `src/db/repositories/item/kits.ts:683-700` | `assemble` screens tracking mode over the whole graph, not the plan's draws | confirmed | #793 | The conservative-by-design defence **fails**: with `cascade: true` the plan still never draws the offending item. A DISCRETE control with 0 stock assembles fine, proving it is the mode not the stock. The two unguarded parity claims (`kits.ts:22-24`, `kit-availability.ts:55-61`) stay **in** this issue — C17 is the counter-example that makes them false, so the drift test the rule asks for is the same test. Neither is among #775's six |
| P2-18 | Performance | `src/db/repositories/item/kits.ts:409-423,585-604` | `readKitGraph` and `preload` are two N+1 loops | rejected | — | Counts reproduced exactly (42 reads to render a 40-component kit, 85 to assemble). Rejected on measured cost, on the precedent of **P1-54**, which rejected the identical shape at the identical magnitude: 1.79 ms desktop / 17.9 ms phone at 40 components. The wiki's own worked kit has **four** components (0.30 ms). Decisively, the finder's proposed fix is a wash — the CTE shape it names costs 1.85–1.90 ms of SQL against the 1.70 ms of round trips it removes, and gets worse as `kit_components` grows. #70 is not the precedent claimed (a feature request naming no performance concern) |
| P2-19 | Functional | `item/{relations,capabilities,kits}.ts` | Child-table edits record nothing in `item_history` | confirmed (narrowed) | #804 | **Narrowed from five families to three.** The alias limb is false — the method with no log entry has no caller; the path the app uses writes `SCRAPE_APPLIED`. The variant re-parent limb is latent — no caller outside tests. Two supporting limbs disproved: the wiki's exemption list is explicitly open ("and more", "A few things change quietly"), and where it *does* close a list it says so, as it does for locations; and #144 is `enhancement`-labelled and scoped to the `items` row. The real precedent is **#691**, the same shape accepted and fixed for locations |
| P2-20 | Functional | `src/db/repositories/item/relations.ts:145-146` | A relation's note is stored, never shown, and discarded on re-add | confirmed | #805 | Grep widened to exports, sync and the bridge: exactly one other reader exists, and it is the dedupe merge *preserving* it. Finder's success-toast claim corrected — there is no toast; both fields clear and the list refreshes, which reads as acceptance without claiming it. Filed as one issue: the discard only bites because nothing renders the note for the user to notice |
| P2-21 | Functional | `src/db/repositories/item/aliases.ts:190-203` | `findByMatchKey` matches archived items and picks arbitrarily among duplicate MPNs | confirmed (as two) | #790, #791 | Found independently by units 5 and 3. Filed as two: the archived scoping is two SQL strings with a precedent in the sibling lookups; the identity question needs the method's signature and both importers' UX to change. **A stronger finding emerged**: matching on the part number alone ignores the manufacturer, and fires with **no duplicate present at all** — the app's own dedupe tool keys the same identity by manufacturer *and* part number and says the two rows are different parts. #593 read `findByMatchKey` and left it alone on the *fold* axis only |
| P2-22 | Functional | `item/attention-sql.ts:44-46`, `item/variants.ts:20-28` | "Variant parent" counts archived children | confirmed (as two) | #788 | Filed as one issue with two named halves. Trigger pinned precisely: ALL children archived; one-of-two is correct. The restore-symmetry defence was **run**, not argued — A→B→C→D is exactly self-inverting and state D is byte-identical to state B, so no double count is possible. Under current behaviour case C values a family that holds stock at £0. `stock-attention-parity.test.ts` is structurally blind: both sides descend from the same definition, so they agree while both being wrong |
| P2-23 | Functional | `src/db/repositories/item/search.ts:180-184` | `searchByAst`'s offset path drops the favourites-first lead | confirmed | #797 | The second leg (missing id tiebreak) was **dropped as not demonstrated**: `itemOrderByClause` does append the tiebreak, and four attacks on the bare fall-through — 5k/20k rows fully tied on every term, eleven forced access paths, varied page depths — all produced a stable order. Marked in §11 as needing the real browser engine to settle. The finder's export-integrity hypothesis was **disproved** — the CSV export always seeks. A different inconsistency was found: three orderings across one bridge endpoint family |
| P2-24 | Prompt | `src/db/repositories/item/search.ts:127-140` | The relevance weights are positional against the FTS column order, with no drift test | confirmed | #816 | The finder's own escape hatch — that a column swap "IS caught by luck" at the relevance test — was **disproved**: 1829/1830 pass with `name` and `description` weights exchanged, and the one failure is the golden-schema gate that the change requires you to regenerate anyway. `bm25()` given fewer weights than columns raises no error and silently defaults the rest — proved standalone. Residue of #248, which locked the column *list* and left this fifth consumer of the order out. Carries **P2-56** |
| P2-25 | Mechanical | `src/db/repositories/item/search.ts:114,257,266` | `searchByRelevance` accepts an offset and ignores it | confirmed | #814 | `cosmetic`. No caller passes one, and neither the bridge nor the extension references the method or its type, so no integrator can reach it. Filed on the "future maintainer" limb: the type is public API, the method already reports how many matches sit behind the ones it returns, and the neighbouring method documents its own omissions where this one is silent |
| P2-26 | Performance | `item/search.ts:255-270`, `ItemPicker.tsx:97-102` | Relevance search scores the whole match set; the picker fires it per keystroke | confirmed (narrowed) | #808 | **One limb rejected**: scoring the whole match set is the documented point of the read (#629). Two survive. The *projection* over the whole match set is not required — deferring it past the limit is behaviour-identical and 1.5–2.1× faster. And the picker genuinely issues one search per keystroke: driven in the real component, "resistor" produced 8 searches, no gate, no debounce, no dedupe across prefixes — against the command palette's 200 ms debounce 30 lines away. Residue of #484 |
| P2-27 | Performance | `src/db/repositories/item/aliases.ts:196-203` | The alias arm scans all of `item_aliases` on every call | rejected | — | Measurement reproduced (22 ms at 100k rows, ~10,800× the indexable shape), but the premise fails: **there are exactly two writers, both per-item and dialog-gated**, and the scrape adds at most one alias per apply. No bulk path exists — not the catalogue importer, not the bridge, not a script. 100k rows needs 100k dialog confirmations. At a reachable 500–2,000 aliases a 200-line import costs 25–55 ms. Anti-correlated with its own worst case: the scan runs only when the MPN arm misses, which correlates with a small catalogue. §11 carries a note for whoever adds a bulk alias path |
| P2-28 | Functional | `src/db/repositories/item/dedupe.ts:286,290` | The duplicate-name advisory compares SQLite `LOWER()` against `foldName()` | confirmed | #794 | The docstring's "honest limit" concession is about *differing prefixes*; here the strings are identical and the first two characters ASCII, so it does not reach the case. **Threshold measured: 200 rows** sorting ahead within the two-character prefix — below that the prefix arm still returns the row, so the miss appears only as a catalogue grows. The docstring's parity claim at `:267` is directly falsified: the tool groups the pair, the advisory reports zero. Fresh site of the **#342** class. Fix needs no scan — the non-ASCII case belongs in the ranking key only |
| P2-29 | Performance | `src/db/repositories/item/feeds.ts:354-378` | `idx_items_warranty` is never chosen; the warranty feed and count scan | confirmed | #764 (comment) | **Not filed separately.** Applying #764's own prescription cures it outright — the index is already the right shape. It is a *fourth* affected index, conditional on the warranty date rather than the active flag, so outside #764's enumeration. Three of the finder's claims corrected: only the export walks page-by-page, not the alert centre; the agenda's since-bound cuts the walk to 6 pages at 50k, not 18; and the whole-walk ratio (4.1×) is half the single-page one (7.6×) because the indexed walk still pays the offset |
| P2-30 | Functional | `src/db/repositories/item/revaluations.ts:57` | `recordRevaluation` overwrites `current_value` unconditionally | confirmed | #795 | No upper bound on the date, no clamp, no schema between the control and the write — backdating is the field's purpose. Both figures appear in one panel. Driven through the real reports: the insurance schedule prices the item at the backdated figure. **Second reachability path with no backdating at all**: revaluations sync last-write-wins, so an out-of-order arrival does the same |
| P2-31 | Mechanical | `src/db/repositories/item/dedupe.ts:373-379,592-593` | `mergeItems` re-points loans and bookings plainly, breaking two cardinality rules | confirmed | #789 | Reachability is the *common* case, not an edge: the duplicate scan never reads the tracking mode, and two units of one model pair on name, barcode and MPN even with different serial numbers. The dialog collapses twelve reference kinds into one integer, so an open loan is indistinguishable from a PO line. **The sync repair makes it worse, and is not conditional on a peer conflict** — driven against an empty peer, it stamps a real loan as returned at the instant it was taken out and cancels a real booking. #193/#194/#542 all scope the cause to two devices; this is a fourth route needing one |
| P2-32 | Functional | `src/db/repositories/item/history.ts:99` | The clear-history delete is unbounded while the guard skips only strictly older rows | confirmed | #796 | Driven end to end through the real merge, not the arithmetic alone. Every premise attacked: timestamps are never frame-shifted (`item_history` is not even in the shifted section), nothing clamps an incoming stamp, and the only trigger on the table fires on UPDATE. The clock-skew feature corrects the evaluation clock only. Permanent, not transient — the peer never deletes its copy either. Window is the peer's lead: under 2 s ignored, under 5 min unwarned, under a year unrejected. The global prune pair agrees with itself, so this is not a `<`/`<=` mismatch — the delete simply has no time bound |
| P2-33 | Prompt | `src/db/repositories/item/feeds.ts:250-267,367-380` | The counts take no `since`; the named parity test never passes one | confirmed | #811 (comment) | Folded into P2-41's issue — same file, same cause (the suite does not drive what its description claims), one fix. The word does not appear in the file at all. Driven with the bound supplied, both lanes diverge 2 vs 1. No caller pairs them that way today, so nothing is wrong for a user; the docstring's "can **never** answer different questions" is false, and the Upcoming agenda is the screen that uses the bound |
| P2-34 | Functional | `src/db/repositories/item/revaluations.ts:76-85` | `listRevaluations` returns a bare array capped at 50 | confirmed | #815 | **Two of three symptoms false**: the editor's trend arrow and percentage come from the item's own current and purchase prices, not the series, and the reports' sparkline uses a different, uncapped read. What survives is one misstated accessible label plus a latent seam. Reach needs 51+ manual submissions — no bridge, import, script or backup path writes revaluations |
| P2-35 | Mechanical | `src/db/repositories/item/list-order.ts:133,148-154` | A backward seek renders NULLs at the wrong end | confirmed | #785 | Found independently by unit 2 and by the lead. Re-derived through the real repository rather than the finder's transcription, with the offset path as ground truth and both a not-null control and a default-order case. **Exactly four of eight combinations fail, all ascending**; the four descending ones and the default order are correct, and the mechanism explains why. The finder's `serialNo` case nearly passed vacuously — `serialNumber` and `serial_no` are different columns — and needed re-seeding. Every link of the reachability chain was verified separately. `searchByAst` and the bridge are forward-only and unaffected |
| P2-36 | Prompt | `src/db/repositories/item/attention-sql.ts:35` | The comment's "~4× faster" contradicts a recorded measurement saying the opposite | rejected | — | **The comment is defensible and the finder's counter-measurement was of the wrong shape.** Rebuilt the pre-#168 fragment from history: every *count* shape lands at 2.26–2.96×, reaching 3.97× at higher variant-parent density; #168's own commit records 66→15 ms for the low-stock count, the same family as my 58→20 ms. The finder's 1.28× is the *list page* shape. All the comment's other claims hold verbatim, including both plans and the NULL-semantics footgun. The disagreeing record is in the maintainer's private vault, outside the audit's scope — carried to §11 |
| P2-37 | Performance→Prompt | `src/db/repositories/item/status-filter.ts:76-87` | Since #684 added `expiring`, the split applicability query's cost premise is inverted | confirmed (reclassified) | #807 | **Reclassified by its own numbers**: the split still avoids 45% (50k) / 52% (200k) of a tap's work, so nothing is slow that would otherwise be fast — four comments are simply false. Measured before and after #684's change in isolation: the skipped half *was* 1.83× the recomputed one and is now 0.74×. A **fourth** false comment was found that the finder missed. Moving `expiring` to a third key is not the fix — it is correctly stock-dependent; the cost is the expression, which is #806 |
| P2-38 | Prompt | `src/db/repositories/item/list-order.ts:101,115-117` | The claimed parity with `itemOrderByClause` has no drift test | confirmed | #816 | The brief's rejection condition fired — `sql.test.ts` goes red — but that file is a **restatement of the implementation**, four hard-coded strings, not a comparison. With those four literals updated in the same breath, as any maintainer would: **798 files / 12410 tests green** with one builder placing NULLs first and the other last. `keyset-pagination.test.ts` compares the two walks as a *multiset*, so it cannot see an ordering difference. `itemOrderByClause` has exactly one production consumer, and `searchByAst` picks between the two builders inside one method — one of those paths is the bridge's CSV export |
| P2-39 | Prompt | `item/status-filter.ts:2-4`, `InventoryFilterBar.tsx:19-20`, `item/core.ts:113,962` | Three doc comments enumerate five status filters; there are eight | rejected | (folded into #807) | Staleness confirmed as fact, and git shows it *is* a maintained enumeration — #88 edited that exact clause to insert its own status, so the three from an earlier change were simply never added. Rejected on consequence: the authoritative list sits a few lines below in every case, nothing but a human reads it, and §3.1 rejects a defect with no consequence. Contrast **#559**, the same class *with* teeth. The two `core.ts` sites are glosses that defer to the SSOT and should not be touched. Folded into #807 as a free correction, on the verifier's own recommendation |
| P2-40 | Mechanical | `ItemRepository.history-feed.test.ts:207-214` | The index guard restates the SQL by hand, so it cannot fail | confirmed | #810 | The finder's mutation is caught **by accident and only sometimes** — 4 red in 10 runs, because a random tie-break column randomises order within a same-timestamp group. The decisive mutation is order-preserving (`+h.created_at`): 16/16 green three times, 86 files / 1582 tests green, while the plan degrades to a temp b-tree and the read goes 0.064 → 18.381 ms at 100k entries — **287×**, the #524 behaviour restored. No other test in the repository asserts a query plan |
| P2-41 | Mechanical | `attention-count-parity.test.ts:56,91,116` | Parity cases seed no removed item, so a count losing its active scope passes | confirmed (broadened) | #811 | **Three of five cases, not two** — the maintenance-due case was missed by the finder. Each mutation left the parity file *and* the whole 82-file suite green; the one case that does seed a removed row goes red under the identical edit. `softDelete` appears once in the whole 193-line file. Tests outside the file were run under the mutation and also passed. The fifth case needs nothing: neither its count nor its feed applies the scope, so they agree by construction |
| P2-42 | Prompt | `item/normalise-db-check.test.ts:43-53` | `PAIRINGS` covers 9 of ~16 CHECK-constrained columns; the gauge block and attrition are unguarded | rejected | — | **The load-bearing triage is false.** Deleting the gauge guards turns `ItemRepository.test.ts:482-497` red — it drives `reconfigureGauge` with a zero capacity, a negative tare and a whitespace unit and asserts the *friendly* refusal. Widening the attrition guard past the CHECK turns three tests red, one of them precisely because the raw text reached the caller. The CHECK side cannot drift silently either: the golden schema snapshot changes on any CHECK edit. "This test could cover more" does not clear the bar |
| P2-43 | Prompt | `src/db/repositories/ReportRepository.ts:1423-1435` | `outOfStockCount` restates the SSOT predicate inline | rejected | (rider on #816) | Neither limb of the rule fires: the duplicate carries **no parity comment**, and both sides are individually pinned (each mutation goes red). The one parity claim in play is on the SSOT, and it is true and held up by `stock-attention-parity.test.ts`, which drives both sides over a shared dataset rather than comparing text. The substitution was nonetheless verified — 280 files / 4766 tests green, type check clean — so the copy is provably redundant and is carried as a rider |
| P2-44 | Mechanical | `batched-item-reads.test.ts:126,155,196,236` | Tests named for a round trip they never assert | confirmed (narrowed) | #812 | **Four of five sites, not five.** The finder's premise about an empty `IN` list is wrong — SQLite permits it — so all four guards delete with the whole suite green. The fifth is **rejected**: removing it produces an empty select list, which *is* a syntax error, so that test does hold its property up indirectly and must not be "fixed" |
| P2-45 | Mechanical | `v1-initial.ts` `item_history.actor_user_id` | The DEFAULT names a `users` row that need not exist | rejected | — | A harness artefact. The sentinel is seeded by the same baseline migration that creates the table, on every path: fresh install, bridge hydration (which migrates before loading a snapshot), and restore (the two principals are declared always-present and deliberately excluded from snapshots). It cannot be deleted either — a trigger aborts a delete of the built-in users, proved at both the driver and the repository. The observation came from building the schema out of the DDL-only snapshot fixture, which carries no seed rows |
| P2-46 | Functional | `features/inventory/item-total-value.ts:34-42` | The card's value seam excludes unlimited items but not variant parents | duplicate #787 | — | Already covered by **#787**, which names this exact symptom in its body and takes the opposite remedy ("fix the write, not the read"). The Prompt limb is false: the docstring claims parity for the *unlimited* case only and the file header states it deliberately differs from the reports' seam. The proposed fix also does not compose — the same card renders quantity with no such gate, so it would read "Quantity 10 / Total value —". #156 aligned an attention *verdict*, not a fact about the row |
| P2-47 | Prompt | `src/db/repositories/item/stock.ts:512-513` | "(mirrors `CheckoutRepository.checkout`)" is untested, and the third copy has drifted | confirmed | #816 | The strongest of the three in the sweep. Driven over one dataset: `sell`, `writeOff` and `checkout` all honour an explicitly chosen untracked lot; `transferStock` does not (that drift is P2-10 / #786). Making `checkout` — the seam the comment names — collapse the same value leaves **10232/10232 green, zero red**. The value is a documented member of all three public input types with no test behind it anywhere. The test the comment asks for fails on the transfer path the day it is written, which is the rule's own justification demonstrated on live code |
| P2-48 | Functional | `item/core.ts:791` vs `:807` | At the paused storage tier, removal succeeds but its Undo is refused | confirmed | #803 | Found while verifying P2-8. The asymmetry is deliberate; the consequence is not. Driven in one repository with saving paused: the restore is refused and all 200 removals succeed, while the toast offers the action unconditionally. No storage-tier gate exists anywhere in the inventory feature |
| P2-49 | Functional | `item/test-records.ts:100-108` | `removeTestRecord` is gated on `items:write` where an audit trail arguably warrants `audit:delete` | rejected | — | Driven with a real Purchaser role, which is exactly the shape the concern needs: the record is removed but the ledger the `audit` subject is *defined over* keeps its `TESTED` entry, and `clearHistory` is genuinely refused to that role. Creating a test record is `items:write` too, so gating removal higher would let a role write a record it could never correct. `removeRelation` and `removeKitComponent` behave identically — the pattern, not a drift |
| P2-50 | Functional | `features/danger-zone/erase-targets.ts:308-313` | The Danger Zone history erase advances the watermark only to "now" | confirmed | #796 | Found while verifying P2-32; filed with it, since they share a root cause and a reader of one needs the other. Driven through the real erase target and the real merge. Note that `history-watermark-parity.test.ts:81-110` *demonstrates* the gap rather than catching it — it deliberately seeds two future-stamped rows, asserts the table is empty, and never checks the guard against the watermark it just wrote |
| P2-51 | Mechanical | `src/features/sync/reconcile.ts:772` | The post-merge serialised-loan repair is gated on the tracking mode, so a local merge of two single-unit DISCRETE duplicates leaves two open loans that neither the write path nor the repair touches | unverifiable | — | Raised while verifying P2-31 and never given a pass of its own. #542 raised the same filter as too tight for bookable single-unit DISCRETE assets and was closed by giving booking conversion a derived id rather than widening it — which addresses the two-device race but not a local merge, and unlike the serialised case a DISCRETE loan does move stock. What would settle it: a decision on whether two open loans on a single-unit DISCRETE item is an invariant violation at all, then the same merge reproduction P2-31 used |
| P2-52 | Performance | `src/db/migrations/v1-initial.ts:1441` | `idx_items_expiry` is used by no query | confirmed | #768 (comment) | **Not filed separately** — it belongs in #768's existing list, and is a different index from the `stock_batches` expiry index already named there. Stronger than "unused": since #684 the engine **refuses to be forced onto it**, because no expiry read mentions the column in an indexable position. The search layer's `expiry:` predicate never picks it either, before or after the boolean index is removed. Knock-on: `parseASTtoSQL.ts:139-142` names `expiry_date` as one of "the three that do carry an index" |
| P2-53 | Performance | `src/db/repositories/item/attention-sql.ts:122-128` | The effective-expiry expression makes *expiring* the most expensive item read | confirmed | #806 | Found while verifying P2-37. 6.7×/8.7× a plain list page at 50k/200k, and 2.5× the next-worst status; it is the whole of the 2.3× that *expiring* adds to the recomputed half of the filter-bar counts. Distinct from #764 (the count form has no `ORDER BY` at all and no temp b-tree; the cost is the per-row subquery) and from P2-52 |
| P2-54 | Mechanical | `item/create.ts:94-102`, `item/core.ts:618-625` | Both unlimited-supply guards delete with the suite green | confirmed | #812 | Found while verifying P2-42. The reason is the finding: `unlimited-integration.test.ts:48` asserts only that *some* database error is thrown, and the raw rule violation throws one too — so it cannot distinguish the guard from the constraint it claims to mirror, which is the one distinction the guard exists to make. The neighbouring gauge and attrition tests assert the *message* and do go red. `create.ts:94`'s stated justification also appears inaccurate |
| P2-55 | Mechanical | `item/gauge.ts:150-154`, `item/create.ts:120` | The gauge comment mis-describes the CHECK, and the two guards mirroring it disagree | confirmed | #812 | Found while verifying P2-42. The CHECK requires only that the unit is present, with no emptiness clause. Creating rejects an empty string but accepts whitespace and stores it untrimmed; reconfiguring rejects both — so a gauge can be created with a unit the reconfigure dialog would refuse to set. Three assertions, all passing |
| P2-56 | Mechanical | `ItemRepository.relevance-search.test.ts:36,51-54` | The only test guarding relevance ranking cannot fail for the thing it is named after | confirmed | #816 | Found while verifying P2-24, and the reason its demonstration is stronger than the finder's. The fixture puts the search term in **both** the 10.0-weighted name and the 1.0-weighted description, so it passes whatever the relative weights are — 6/6 green with the two exchanged. One word in the fixture makes it red |

### Phase 3 — Every other repository

Pinned SHA: `52fa966f97ed1a55d46cba47578e1e869f2d1aee`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| L-1 | Mechanical | `LocationRepository.ts:201-208` | `PARENT_MOVE_CYCLE_GUARD` uses `UNION ALL`, the only recursive walk in the layer that does, so a pre-existing `locations` cycle makes every parent move never terminate | confirmed | #818 | Widened three ways. The ingress needs no hand-edited file: `conflict-restore.ts:34-71` ("restore my version" on the Sync screen) UPSERTs a captured row with no cycle check. The hang was driven on the real repository call and killed by an OS timeout; Vitest's own `testTimeout` never fires because `node:sqlite` is synchronous. Two corrections to the finder: substituting `UNION` does not throw, it *completes* the legitimate move in 0 ms while still vetoing a real self-descendant one; and the freeze lands on **correct** usage, because `EXISTS` short-circuits exactly when it should veto. `unusable`, not data-loss — the transaction never commits, but nothing cancels a running statement, so the 300 s budget expires and every later request queues behind it. #190 is the same defect on `items.parent_id`, and its body cites this guard as the good example |
| L-2 | Mechanical | `LocationRepository.ts:168-180` | `SELECT_WITH_COUNT` is unexported, so the row-shape guard cannot check the three location list reads | rejected | — | Premise false. `exportedConstantOf` is one of **three** resolution steps; step 1 is constant folding, and `LOCATION_COLUMNS` has a string-*literal* type, so the template folds and never consults the export list. Proved as prescribed: deleting `l.archived_at` turns the guard **red** naming all three sites. Enumerating the unverified population with `MAX_UNVERIFIED = 0` confirms `list`/`listAll`/`getTree` are not in it. `CategoryRepository.ts:105`/`:178` are the same shape and equally fine. Follow-up, not an issue: the memory note `Query row shape guard` omits the constant-folding step, which is what the finder generalised from |
| L-3 | Functional | `useLocationSidebar.ts:177-184`, `:240` | The delete confirmation is keyed on directly-homed items, so a location holding only sub-locations, photos, regions, tags and field values deletes on one click | confirmed | #823 | Sole gate on all three entry points; the mutation has no undo, no toast action and no rollback. Narrowed: only **four** of the seven consequences are loss (photos → regions → item placements, tags, field values). Stock re-home and forced check-ins are lossless; child promotion is already recorded behaviour. The counter is narrower in a second way — `location_item_counts` is maintained `WHERE is_active = 1`, so removed items still homed there also read as zero. #588 is the accepted precedent for the class |
| L-4 | Mechanical | `v1-initial.ts:1084-1090` | `item_field_effective_values` walks the location tree with an unbounded `UNION ALL`, so the same cycle hangs every read of it | confirmed | #819 | **This query is what crashed the host mid-phase.** Re-demonstrated safely under an explicit `LIMIT`: the cyclic closure fills any limit (100k → 100000, 1M → 1000000) where the acyclic one is 9. `UNION` would **not** save it — the incrementing `depth` makes every tuple distinct, confirmed by measurement. Precondition narrower (needs one `mode='inherit'` row), blast radius much wider: the second reader is `item/feeds.ts:440` → `useAlerts.ts:203` → `AppNav.tsx:49`, the global nav on every screen, so it fires on **load**. Phase 1's file, found by Phase 3 |
| L-5 | Mechanical | `LocationPhotoRepository.ts:79`, `:158-171` | Photo captions and region names skip `assertTextLimit`, so an over-long value fails with a message naming neither field nor limit | confirmed | #843 | Joined the sweep with `Attachment`/`Webhook`/`TarePreset`. Narrower on reach (only caption and region name are user-typed; geometry and colour are app-serialised), wider on the trim: `addRegion` trims its name and `addPhoto` does not trim its caption — two adjacent methods disagreeing |
| L-6 | Mechanical | `LocationPhotoRepository.ts:267-269` vs `:280-281` | A region Move half-lands at the storage Hard Stop, leaving the item placed nowhere | confirmed | #844 | Framing corrected: "unlink should assert writable too" is the **wrong** reading — `base.ts:153-155` states the deletions-free-space exemption and open #802 already counts `unlinkItem` among the thirteen that legitimately free space. The defect is the **caller**: `useSetItemPlacement` composes a Move from two independently gated awaited calls. The panel has no tier check; the tombstone is written before the refusal |
| L-7 | Performance | `LocationRepository.ts:624-644` | Deleting a location builds one history INSERT per orphaned item into one transaction array | rejected | — | The finder's named mechanism is measurably not the cost. Like-for-like against the same transaction with only the history INSERTs collapsed: **1.67x / 1.64x / 1.62x** at 2k / 8k / 32k items — flat, no cliff. Structured clone is **5%** of the cost (13.3 ms of 189.3 ms at 8k), not the payload the claim named. 0.8 s at 32k against a 300 s budget, behind a confirmation dialog with a spinner — P1-54's rejection ground. Unlike P2-5/#801 the rows are needed; only the shape is suboptimal. Second limb is P1-54 verbatim. §11 carry-over: one `INSERT … SELECT` removes a flat ~38% at any size |
| L-8 | Mechanical | `LocationRepository.ts:667`, `snapshot.ts:940` | The derived stock-lot id is rebuilt by hand in SQL with nothing holding the copies to the helper | confirmed | #868 | Narrowed from three copies to **two**: mutating `stock.ts:302` turns six tests red, so that one is incidentally pinned; mutating the other two leaves **5,328 tests green**. Mutating the helper's separator leaves the whole location suite green. The drift test drives both sides and reproduces the predicted failure — the upsert stops matching, the unique constraint aborts, and the location becomes undeletable with a message naming neither. Rule 1 is available: the separator is already a module constant, and `OPEN_PURCHASE_ORDER_WHERE` is the in-repo precedent. Adjacent to #771; not a #775 member (nothing claims parity) |
| L-9 | Functional | `LocationRepository.ts:903-912` | `createPath` reuses a level by SQL case-insensitive match, which folds ASCII only, so a non-ASCII path forks | confirmed | #845 | Wider than claimed — the **ancestor** forks too, so a whole branch duplicates. Severity does not rise: one production caller, and it is **not** on the import path (the catalogue importer resolves, never creates), so the "import creates duplicate shelves" scenario does not exist. #342/#343 are the precedents that fixed the identical class |
| L-10 | Mechanical | `LocationRepository.ts:369`, `:546`, `:594` | Non-null assertions on a row re-read across worker messages | rejected | — | Below the bar. §7's preamble forecloses it ("a hit is a candidate, never a finding"). The consequence if the interleave happens is smaller than assumed: `hasAuthoredMessage` returns false for a `TypeError` by explicit name test, so the user reads the ordinary fallback copy — no jargon leak, no crash, and the write already committed. Worth one line inside #826 if wanted |
| L-11 | Performance | `LocationPhotoRepository.ts:41-47` | The placements panel reads every photo of a location, blobs included, to find one; `getPhoto` is dead | rejected | — | Zero callers for `getPhoto` confirmed across all four trees. At the shipped thumbnail size the cost is **43-140 microseconds**, once per location, React-Query-cached and shared with the gallery — narrowing would *lose* that shared entry. The one durable widening was handed to #855 as a comment: `location_photos.thumbnail_blob` is a second unbounded column, where the same amplification gives 4-15 ms |
| C2-1 | Mechanical/Prompt | `CategoryRepository.ts:390-401` | Deleting a category strands its items' field values, and the docstring claims they cascade | confirmed | #825 | Widened by two symptoms the finder did not claim: the stranded row still satisfies a `field:` search via the inheritance view, and section presence reports custom fields the editor cannot render. "Permanently" corrected to "no targeted route exists" |
| C2-2 | Functional (ACL) | `TagRepository.ts:279-312`, `SupplierRepository.ts:211-232` | `merge` hard-deletes and tombstones while asserting only `<entity>:write` | confirmed | #821 | Widened to a second site. The "merging is editing child rows" counter-argument fails on the project's own precedent: `item/dedupe.ts:344-354` asserts **both** keys for a merge that only *soft*-deletes. No UI gate — the Tags screen has no permission check at all. Passes the ACL sweep because it **is** guarded, just mis-keyed |
| C2-3 | Functional/Prompt | `CategoryRepository.ts:881-901` | `updateField` narrows a shared definition's option list for every category using it | confirmed | #851 | One root cause with C2-4. The `updateField` arms are unreachable today (no caller passes those inputs), so the reachable arm is `addField` → `resolveFieldDef.applyOnReuse`, which writes unit, range, precision and prominence onto a shared definition with **no sharer check at all** — demonstrated retuning another category's kg/max-100 field to g/max-10, so a stored 50 reads 50 g. Not #715 (that is the preset library and options *discarded* on reuse; its reasoning corroborates this) |
| C2-4 | Functional | `CategoryRepository.ts:888-901` | The retype guard counts only category sharers, ignoring location offers and uncategorised items | confirmed | #851 | Same root cause as C2-3: the block asks the wrong question about who is bound by a definition. The unused-definition query next door already knows all three reference tables |
| C2-5 | Functional | `TagRepository.ts:45-49`, `:189-200` | Tag filter and autocomplete match with SQL `LIKE`, folding ASCII only | confirmed | #830 | ASCII control proves the intent. Second half driven: the app says the tag does not exist, then the save reuses exactly that tag. Sharper consequence than claimed — the merge-target picker leaves **Merge** disabled with no explanation. Not #753/#390/#577/#342, all of which name other files |
| C2-6 | Functional | `CategoryRepository.ts:493` vs `:1034` | At the Hard Stop, removing a field is permitted but pruning the definition it orphans is refused | confirmed | #860 | Rationale corrected, and the measurement **rejects the finder's framing**: none of the three actions frees a page (the tombstone outweighs the row), so the exemption's premise does not hold for this family — the same phenomenon #802 names, in a second repository. Corrected claim: the dialog offers a Delete at the paused tier that is always refused, telling the user to do what they just tried |
| C2-7 | Mechanical | `name-fold-coverage.test.ts:73-75` | The name-fold guard exercises one of the three writers that mint a tag row | rejected | — | The hole is real but harmless: reverting `create` to its pre-#342 shape turns `TagRepository.test.ts:64-70` red on the same pair, and `setForLocation` delegates to the same `applyTagSet` the guard drives. Shape already rejected as P2-42 |
| C2-8a | Mechanical | `TagRepository.ts:236-237` | `create` returns `updatedAt: 0` on the insert branch and the real stamp on the reuse branch | confirmed | #866 | Narrowed: the `applyTagSet` half is **not** a defect (local map, `.id` only), and no caller reads the field. What stands is one public method returning a value that is right or wrong depending on which branch ran |
| C2-8b | Mechanical | `TagRepository.ts:250-258` | `rename` of a missing id resolves silently | rejected | — | No caller relies on a throw; the docstring promises one for a name clash only. No demonstrated consequence |
| C2-8c | Mechanical | `TagRepository.ts:265-271` | `remove` of a missing id writes a tombstone | rejected | — | The decisive question — does it shadow a row another device is about to create? — is **no**: `reconcile.ts:579` deletes only where the tombstone beats the row, and `reconcile.test.ts:185-191` pins the converse. The shape is the layer's convention across four repositories |
| C2-9 | Prompt | `CategoryRepository.ts:111-126` | A parity docstring does not name the test that holds it up | confirmed | #863 | Marginal. The test **does** bite in both directions (both sides mutated). Only the third limb of the rule fails. Widened: the twin at `field-def-prominence.ts:155-157` has the same omission. Not a #775 member — that sweep collects claims that are *false* |
| C2-10 | Mechanical | `CategoryRepository.inheritance.test.ts:505` | An assertion that cannot fail | rejected | — | The test itself can fail — `:503` goes red when the clause it guards is removed. `:505` is a lint-appeasing dead line inside a sound test. Shape already rejected as P1-10 |
| C3-1 | Functional | `features/projects/assembly.ts:131` | A zero-quantity BOM line still consumes or relocates a whole-item part | confirmed | #827 | The `mode !== 'WHOLE'` term is load-bearing but over-broad. Four failures with a passing DISCRETE control — same line, opposite outcome, decided only by tracking mode. The fix leaves 310 committed tests across 17 files green, so nothing pins the current behaviour. Reachable only via the BOM importer, demonstrated end to end. Severity corrected **down**: an archived item is restorable; the immutable `CONSUMED` entry and the completed state are not |
| C3-2 | Mechanical | `project/assembly.ts:113` | The one-shot guard is keyed on an editable status, so re-finalising a re-opened project clashes | confirmed | #828 | Widened. Face 1 (same outcome) aborts on a duplicate id with untranslated copy; stock rolls back atomically, so **not** data-loss. Face 2, new: a **different** outcome *succeeds* — the derived ids are namespaced per artefact kind, so a second container is minted and the BOM applied again as a relocation. Full outcome matrix checked: no pairing double-consumes. Links #195, which introduced the guard |
| C3-3 | Performance | `project/budget.ts:104-131` | `listBudgetAlerts` is unbounded and scans every BOM line in the vault | confirmed | #852 | **Diagnosis corrected and the finder's fix would be harmful.** Row count is not the cost; the unscoped derived table is. A `LIMIT` would be a *defect* — `useNavCounts.ts:146` counts the whole result for the over-budget badge. Measured 4.53x / 4.20x / 5.08x against a project-scoped rewrite. The three consumers share one query key, so React Query dedupes to one execution per load |
| C3-4a | Performance | `project/procurement.ts:229-243` | `listInTransit` scans and sorts the whole table on every page | confirmed | #852 | Folded into C3-3's issue. Deep pages cost the same as page 1 (1.03x), which is the evidence the limit bounds rows and not work; an index flips it to a search, 2.14x / 2.86x |
| C3-4b | Mechanical | `project/procurement.ts:241` | The ordering has no id tiebreak, against the rule three siblings state | rejected | — | Rejected for a reason that is **not** the engine, so §11's `search.ts:184` question did not need settling: the sole consumer never asks for a second page, and #149's invariant is specifically about paging. §11 hardening — ties are very reachable, so the tiebreak must go on before anyone gives this feed a paged consumer |
| C3-5 | Functional | `project/bom-lines.ts:117` | Re-pointing a BOM line keeps the previous item's cost snapshot and part number | carry-over | — | Real, and a **second limb** the finder missed: `updateLine` leaks a raw engine error where `addLine` throws a named one. But the zero-consumer claim is verified across all three trees and no edit-BOM-line UI exists. §11, on the precedent of the Phase 2 `aliases.ts` row |
| C3-6 | Functional | `project/assembly.ts:198` +4 | A part archived after being added to a BOM is still drawn, costed and shopping-listed | confirmed | #829 | Widened 3 → 5 sites; the one the finder missed is `budget.ts:126`, the **dashboard** budget-alerts feed. Not a duplicate of #790: there the importer binds an already-archived item, here the binding was correct when made — disjoint fixes. `procurement.ts:239` excluded (label fallback only) |
| C3-7 | Performance/Money | `project/costing.ts:47`, `:170` | Published money figures bypass the rounding seam | confirmed | — | **Half disproved.** `totalCost` sums in SQL over integer micro-units; over **2,000,000** sums rounding changed the rendered string **0 times**. The `estimatedCost` limb survives: a 2,800,000-case sweep found 3,446 mismatches (0.12%), all exact half-penny ties and all requiring a sub-penny unit cost. The finder's own suggested case renders correctly. No export path. `cosmetic`; not filed alone |
| C3-8 | Functional | `project/procurement.ts:100-106` | The `PROCURED` entry logs the full requirement, not the outstanding remainder | confirmed | #867 | Reachability verified in the UI: the BOM table offers all four statuses unconditionally and a partially received line deliberately stays in transit, so re-selecting it is two clicks with no guard. Reports unaffected (the delta is correctly null per #652) — the damage is the human-readable ledger. Residue of #652's deliberate decision |
| C3-9 | Mechanical | `features/projects/assembly.ts:145-150` | `AssemblyDrawMode` is branched by if-chains with no exhaustiveness guard | rejected | — | No live defect: all four members check out against every consumer. One producer, no pending member, if-chains rather than switches. §11 hardening |
| C3-10 | Mechanical | `project/bom-lines.ts:81`, `:124` | A non-numeric quantity reaches SQL as a raw constraint failure | rejected | — | Not reachable. Both UI dialogs coerce, the importer's cell reader rejects non-numeric, negative, fractional and overflow values, `updateLine`'s only caller has zero consumers, and the bridge has no BOM write in its five-operation union and no BOM tool |
| C3-11 | Performance | `project/assembly.ts:138`, `:304` | One awaited batch read per matched part before the transaction opens | rejected | — | Identical shape at identical magnitude to P1-54 and P2-18, with the same mitigations (draws deduped per item, behind a one-shot confirm dialog). Honest residue for §11: an imported BOM can carry hundreds of lines where a kit cannot; ~500 parts would be ~21 ms desktop / ~210 ms phone, not measured and so not claimed |
| C4-1 | Functional | `PurchaseOrderRepository.ts:568-581` +3 | PO receipts commit stock writes with no derived operation key, so two offline devices land the delivery twice | confirmed | #817 | Widened to **data-loss**. Driven through the **real sync engine**, not a hand-rolled replay: shelf 10 where the order says 5, and the Activity Log shows the delivery twice. The **return** path destroys real stock — 15 on hand, one 5-unit refund each side, 5 left. Permanent, not self-healing. The bracketed loan return passes as a control, so the harness is not double-counting by construction. The wiki's "from two devices" promise is false. PO and project paths are one issue, four call sites |
| C4-2 | Functional | `lib/supplier-name.ts:34-43` | `supplierNameKey` bare-lowercases, so a case-expanding character mints two suppliers | confirmed | #831 | Widened — also reached from `nameFilter:280`, the Suppliers search box, which the finder missed, so `findByName('GROSSE BAUTEILE')` misses the stored name too. Does **not** self-heal over sync. The safe fix keeps the key's own shape and inserts the round trip: a control re-ran every committed expectation against it and passed, and the Greek final-sigma pair stays correct — recommending the shared helper alone would *regress* it |
| C4-3 | Performance | `PurchaseOrderRepository.ts:197`, `:831-839` | `list` fires one lines query per order, and the export walks every page through it | rejected | — | Count reproduced (101 reads per page; 10,100 for the export) — it fails on cost. `Promise.all` means these are **pipelined** ~0.005 ms round trips (P2-5's figure), not sequential: ~1.7 ms desktop / 17 ms phone per page, numerically identical to P1-54 and P2-18, both rejected. At the export cap the ratio *falls* (3.15x → 1.82x → **1.38x**) because both shapes pay the same OFFSET header walk, which is 435 ms of 599 ms. Same index both sides. §11: that OFFSET walk is the real cost, and is keyset-pagination territory |
| C4-4 | Functional | `docs/wiki/Purchase-Orders.md:18-20` | The page says raising an order puts stock in transit; a draft contributes nothing | confirmed | #581 (comment) | Narrowed to `documentation`, `cosmetic`. Quote verbatim and current; "Mark as ordered" appears **zero** times in `docs/wiki/`. The app is right and signposts it well (a Draft chip and a primary button on the same screen). Sits inside §4.2's wiki programme and adjacent to #581, so folded there as a comment rather than filed |
| C4-5 | Functional | `ReceiveLineDialog.tsx:67-70` | The per-line receive accepts any quantity and the repository silently clamps it | confirmed | #846 | The clamp is deliberate, documented in two seams and pinned by four tests, so the defect is the **missing upper bound** in this one dialog, not the clamp. Traced: nothing throws, nothing warns, and the only signal is a screen-reader-only region stating the new total. The sibling dialog already has the message, the variable and the translation key |
| C4-6 | Performance | `PurchaseOrderRepository.ts:122-136` | The open-order count runs a correlated aggregate per order row | rejected | — | Shape confirmed and linear, but the finder's **stated cause is disproved**: adding the missing status index gives 1.01-1.05x and an identical plan — the cost is the per-row `NOT EXISTS` (25x the status-only filter), which no status index touches. 5.6 ms desktop / ~56 ms phone at 10,000 orders, once per load, cached. The derived status is a documented trade (#573) and `open-count-parity.test.ts` holds both derivations together |
| C4-7 | Functional | `SupplierPartRepository.ts:347-359` | `listPriceHistory` caps at 50 and the sparkline's change is computed over the truncated window | confirmed | #832 | Wider than #815 on impact (a **visible** "+£49 (+445%)" where the truth is +£59 (+5900%)), narrower on reach — a row per genuine change, not per poll, and no bulk path, so 51 points needs 51 real movements |
| C4-8 | Mechanical | `PurchaseOrderRepository.delete:319-333` +3 | Deletes tombstone unconditionally, unlike `WishlistRepository.delete` | rejected | — | No reachable caller can name an id the device never held: the bridge write union has no delete of any kind, the MCP tool set has none, the four deletes have exactly four UI callers over already-loaded rows, and sync writes tombstones directly. (The finder's line numbers for the Wishlist guard were stale: it is `:146-159`) |
| C4-9 | Prompt | `PurchaseOrderRepository.ts:6-8`, `:421` | A "mirrors" comment with no drift test | rejected | — | The comment names the shared seams it reuses and claims no behavioural identity, so rule 1 is already applied to each. The two bodies differ in nine listed ways and the comment is still true. Not a #775 member — that sweep collects claims that are *false*. No test could fail for the stated reason |
| C5-1 | Functional | `AssetBookingRepository.ts:131`, `:394` | An unlimited-supply item is bookable but can never be checked out | confirmed | #840 | Two definitions of "bookable" disagree about the unlimited flag: the picker query and the pure gate both ignore it, the conversion checks it. A definition mismatch, not the concurrency window filed as #826 |
| C5-2 | Mechanical | `CheckoutRepository.ts:231-238` | The serialised-loan probe is not paired with its insert, so two overlapping lends both open | confirmed | #826 | New against #193, whose fix is a **sync-merge** repair that never runs on an unsynced device, so both loans persist indefinitely |
| C5-3 | Mechanical | `AssetBookingRepository.ts:140-152` | Two overlapping bookings both succeed | confirmed | #826 | Same post-merge-only caveat (#194). No shipped UI route found — the screen latches |
| C5-4 | Mechanical | `ContactRepository.ts:123-140` | `resolveOrCreate`'s docstring claims the unique index closes the race; it folds ASCII only | confirmed | — | **Both of the finder's extra claims are wrong.** It *does* need the concurrency (`findByName` uses the full Unicode fold, so sequential is clean), and the data-loss consequence is **refuted** — `unique-keys.ts:640-654` has handled the two-rows-one-key case explicitly since #679. What survives is `degraded` plus a **false, stale docstring** that the code two lines above already contradicts. Not filed separately; the docstring correction rides with #826 |
| C5-5 | Functional | `AssetBookingRepository.ts:394-405` | The booking picker is capped at 100 with no offset, search or truncation signal | confirmed | #841 | Demonstrated with 120 assets: limit 500 still returns 100, and offset is silently ignored. A hard reachability ceiling with no alternative route, not a deep-page cost. #573 is the same shape on the dashboard tiles |
| C5-6 | Mechanical/Functional | `AssetBookingRepository.ts:157-167`, `:251-299` | Concurrent cancel and convert leave the booking both cancelled and converted | confirmed | #826 | The sharpest of the four and **uniquely un-self-healing** — no repair pass exists for this shape, so the corrupt row survives every merge on every device. Sequential path correctly refused; stale-cache hypothesis closed off |
| C5-7 | Mechanical | `CheckoutRepository.ts:391-421` | `renew` writes without carrying `returned_at IS NULL` into its WHERE | confirmed | #826 | **The finder's demonstration was wrong** — their run showed the benign ordering, and the damaging one is structurally unreachable on the `:memory:` driver. Reachable under the shipped driver model: an offset sweep found 2 of 41 windows where both calls report success, the closed loan carries a due date 30 days out and the renewal is logged after the return. #296 **is** a partial fix that missed `renew` and does not say so |
| C5-8 | Mechanical | `AssetBookingRepository.ts:343`, `MaintenanceRepository.ts:154`, `:183` | Three `readAllPages` walks have non-total orderings | carry-over | — | The decisive question **is** settled and cuts the opposite way from C3-4b: all three genuinely OFFSET-walk, so the invariant is live here. But no duplicate or drop could be demonstrated, and Phase 2 failed four attacks on the identical question. Sharpened claim §11's `search.ts:184` row lacks: `AssetBookingRepository` carries **both** shapes — `list:331` has the tiebreak with a docstring arguing it is needed because the export offset-walks it, and `listUpcoming:343`, walked since #149/#606/#607, omits it |
| C5-9 | Functional | `AssetBookingRepository.ts:200-211` | A partial update naming only the end date can move the start | rejected | — | Real behaviour, no reachable caller: the edit dialog refuses a reversed range before calling update and the date round-trip on the untouched field is exact; the bridge has no booking write at all (its only booking surface is the read-only calendar feed). Even if reached, the swapped range is still overlap-checked. §11 note: live the moment a booking write is added |
| C5-10 | Mechanical | `checkout-plan.ts:86-88` | `borrowerColumn` is a ternary chain with no exhaustiveness guard | rejected | — | Mechanism real and **wider** than claimed: adding a fourth member and running `npm run type-check` across all three projects gives **zero** errors anywhere (with a control proving the run was live), so the union has no coverage at any site. But the consequence is narrower: `checkouts` has three FK columns under an XOR CHECK, so a fourth type needs a migration, a column, a CHECK, a mapper branch and a UI option. Consistent with the C3-9 rejection |
| C5-11 | Prompt/Mechanical | `MaintenanceRepository.ts:36-49` +2 | Three definitions of the maintenance due instant, with a "must agree" comment and no test | confirmed | #839 | Demonstrated across **two processes** (setting the timezone inside one Vitest worker is inert — proved with a control first): written under one zone, read under another, the delta is 3,600,000 ms and the SQL due count flips 1 → 0 while the pure seams still say due. So the badge and the agenda say due while the tile, the count and the filter say not — and the item is absent from the very list the badge points at. Related to #325 but not covered by it |
| C5-12 | Performance | `MaintenanceRepository.ts:65-94` | The maintenance-due filter nests a correlated aggregate per items row | duplicate #806 | #806 (comment) | Same seam and mechanism as #806/P2-53, already filed. Measured 6.8x the unfiltered page and **2.7x the already-filed `expiring` arm** at 20k, scaling linearly. 12 ms at 20k is imperceptible, so the measurement was added to #806 as a comment |
| C5-13 | Mechanical | `CheckoutRepository.test.ts:569-585` | The borrower-delete atomicity test cannot fail for the property it names | confirmed | #850 | Splitting the delete into two transactions — exactly the #301 regression — leaves 61 tests green, and 26 more elsewhere. The stub replaces the transaction function **wholesale**, so no transaction runs and neither implementation can write. The ~12-line poison-one-statement remedy works and was demonstrated both ways; the identical shape guards two more deletes |
| C5-14 | Mechanical | `CheckoutRepository.ts:391-392` | `renew` appends to the ledger without `assertWritable` | confirmed | #858 | Narrowed to `renew` alone — the class rule explicitly exempts check-ins, so `checkInAllForTarget` is consistent with what is written. Not a duplicate of #802 (which sweeps *delete*-permission sites). Note #802's own conclusion — correct the reasoning rather than add the gate — may govern here too |
| C5-15 | Mechanical | `item/maintenance-default.ts:48-49` | The category-default interval is neither validated nor truncated | rejected | — | The crux resolves against the finder: `v1-initial.ts:563` puts the guard on the **column**. Both named failure modes are blocked one layer up, and because it is a column CHECK on a STRICT table it also covers sync apply and backup restore |
| C6a-1 | Functional | `ReportRepository.ts:278` | The catalogue filter drops the unlimited-supply term, so those items are priced and counted | confirmed | #837 | Narrowed. The *inclusion* is a recorded decision (#410: "a catalogue is a list of what you have, not a valuation") — but the docstring convicts itself, since its own premise that the product is undefined is exactly what the catalogue computes, and #410 never mentions unlimited items. Corrected claim: it **prices and counts** one, and prints its placeholder count as a finite quantity. No unlimited flag anywhere in the reports feature |
| C6a-2 | Performance | `ReportRepository.ts:1139-1212`, `:908-999` | The paged document reads sort the whole scope with the full payload per page | confirmed | #853 | The crux was **tested, not reasoned**: both shipped engines have the sorter-reference optimisation compiled out. The photos-on ratio **grows with N** (1.89 → 2.21 at page 1; 4.30 → 9.67 late) — flat is what it would be if the blob were fetched per page. A two-step shape is cheaper than today's photos-off read and 3-44x cheaper than photos-on. Amplifier the finder missed: the print path pages the whole document at 100, so printing is quadratic. The docstring is simply false. #163/#410 are the incomplete predecessors |
| C6a-3 | Functional | `ReportRepository.ts:593-607` | The foreign-currency notice misses a fully-depreciated item whose only base price is zero | confirmed | #838 | Narrowed as anticipated: the zero is right, the silence is not. Nothing else tells the user — the unpriced count renders in only two places, neither on the schedule or the catalogue. Stronger argument than the finder's: the effective-value expression prefers the supplier cost **above** the book value, so the currency exclusion silently demotes the item to a lower-precedence source that happens to be zero. Arrives by the passage of time with no user action |
| C6a-4 | Functional | `CatalogueScreen.tsx` | The catalogue prints a base-currency total with neither exclusion notice | confirmed | #838 | Grepped: neither notice appears, and the printed document is the same DOM. The repository gives the screen nothing to render one from. Extra: the dash is itself misleading — the wiki says it means nothing prices the item, but something does |
| C6a-5 | Mechanical | `ReportRepository.ts:474-486` | `preferredSupplierNameSql` claims to mirror the cost lookup but omits its currency guard | confirmed | #838 | Half **disproved** then re-widened: the "two preferred rows" limb cannot happen (the partial unique index rejects it). The other half is real and user-visible — the line prints "Yen Co" with a blank price. Joined C6a-3/C6a-4 because the named supplier is the very evidence that makes the silent drop look like a bug |
| C6a-6 | Mechanical | `ReportRepository.ts:745-753` vs `:1272-1279` | A recursive location CTE duplicated with a mirroring comment and no drift test | confirmed | #818 (comment) | **And critically NOT a second cycle-hang instance** — both use `UNION`, neither has a depth column, and both were driven against a real 3-node cycle, returning in 0.04 ms. Byte-identical after whitespace. Worth flagging: the safe and unsafe spellings coexist with no shared definition, which is the drift the rule predicts. Folded into #818 as its derive-from-one-helper remedy |
| C6a-7 | Mechanical | `ReportRepository.ts:1281-1282` | The empty-subtree guard is unreachable | rejected | — | Mechanism right (the CTE base case yields the seed row unconditionally) but the reachable outcome is **identical** — the filter matches nothing either way |
| C6a-8 | Functional | `ReportRepository.ts:671`, `:780` | "Unpriced" counts a deliberate zero, so the copy explaining it is false | confirmed | #865 | The **count** is a recorded decision (a test pins it with its reasoning, and the helper that distinguishes the cases names exactly two surfaces that must — Reports and Location stats are deliberately not on the list, per #706). The **copy** is the defect, shown decisively by an item that depreciated out with no user action |
| C6a-9 | Mechanical/Performance | `ReportRepository.ts:754-760` | Unbounded `IN (…)` lists, bound twice in location stats | rejected | — | On scale, consistent with P1-53. There is **no select-all** (the toggle adds one id at a time), and the variable limit is 32,766 on **both** drivers. The double bind is real but puts the break at a 16,383-location subtree |
| C6a-10 | Performance | `ReportRepository.ts:1152` | The catalogue scope CTE is re-resolved per page | rejected | — | On scale, measured with a 401-location **chain** (the worst shape): a location-scoped page is **not slower** than an unscoped one (0.90-1.11x). No bulk location import exists |
| R-1 | Functional | `ReportRepository.ts:1568-1569` | `deadStock` skips the location tree unless some other item is on inherit, dropping an explicit opt-in's threshold | confirmed | #833 | Widened: **both** halves driven — the report contradicts the editor's note, and creating one unrelated inherit-mode item elsewhere flips the verdict, so the answer is non-deterministic across ordinary use rather than merely defaulting. Also reaches the CSV export. Existing tests never take the path |
| R-2 | Functional | `ReportRepository.ts:2080` | "Never counted" keys on a ledger row that a clean count never writes | confirmed | #834 | A well-run inventory produces **more** false flags than a badly-run one. No other signal clears it (the durable stamp is on locations only). #637 is the location-level sibling, and its own reasoning is the argument for fixing this |
| R-3 | Mechanical | `ReportRepository.ts:1651-1654` | A stored reorder quantity of zero removes a low item from the shopping list | confirmed | #835 | **Live, not latent** — reachability established through the shipped UI, which the finder had not done: the control declares a minimum of zero, the value converts to 0 rather than null, and the invalid check guards only the reorder *point*. One keystroke. The tab then says "No items below their reorder point" while the dashboard counts it low. Links #156, whose parity test covers the low-stock predicate but not the shortfall |
| R-4 | Functional | `ReportRepository.ts:1625`, `:1716` | The reorder tab and agenda lane ignore the user's low-stock threshold | confirmed | #836 | **Not** a documented decision: Phase 46 named no exclusion and the reorder list did not exist yet; every other in-app consumer passes the preferences, and the plan hook *accepts* thresholds while its only caller passes none. Proof it is wiring: passing the same preferences produces the row. #483 asserts the opposite of what this shows |
| R-5 | Mechanical | `ReportRepository.ts:1424-1433` | `outOfStockCount` restates the SSOT predicate outside its parity test | duplicate #816 | #816 (comment) | Duplicate of the **recorded rejection** P2-43, same location, carried as a rider on #816. One fact added as a comment: P2-43's reasoning does not cover a **deliberate** semantics change, where the editor updates the parity test's expectations and the tile silently keeps the old definition |
| R-6 | Performance | `ReportRepository.ts:1548-1574` | `deadStock` scans the whole inventory before applying the opt-in, eagerly | confirmed | #854 | Rejected **as framed** — #528 records the ungated headline cards as deliberate, and the finder is wrong twice (stock aging and data hygiene both cost more; the missing gate is intentional). What was filed is the residue #528's own closing note names and offers to track: pushing the policy filter into SQL. Nobody raised it and #528 is closed |
| R-7 | Performance/Prompt | `ReportRepository.ts:1748` +3, `:18` | Four reports return one row per item with no limit, against the file header's claim | confirmed | #863 | Rejected as a performance finding — restates #528, which pre-empts the framing. The claim is also **wider than the truth**: payload growth for 3x rows is 3.0x for two of them and **1.0x constant** for the other two, and all four are viewport-gated. The surviving piece is the false header claim, filed with R-11's comment |
| R-8 | Functional/Prompt | `DiagnosticsRepository.ts:61-64` | The item count includes removed items and abstract variant parents | confirmed | #864 | The **defect is the copy, not the count**: the count is documented as a row count, the panel groups it under "Size of your data", and the storage repository agrees with it. The wiki sentence and the bare "Items" label mislead, and the gap grows without bound |
| R-9 | Mechanical | `DiagnosticsRepository.test.ts:48-53` | A growth assertion that cannot fail | confirmed | #861 | Stubbing the size to a constant leaves all three tests green. Nuance for the fixer: `>` is **not** generally safe — page granularity means one insert moves nothing (measured delta 0); it is safe only at 200 rows. Clears §2.1 on the honest reading: it can fail if the size shrinks, but not for the reason its name states. The second sub-claim is **disproved** — the permission gate *is* asserted |
| R-10 | Mechanical | `ReportRepository.ts:2349-2363` | `parseSalesMetadata` swallows a parse error | rejected | — | Documented as tolerant, and **no figure is wrong** — the costed total correctly excludes the unreadable lines and the caveat string is shown precisely because they were. Only the cause is undiagnosable, which §1 excludes. Adjacent real gap for the test-quality phase: `salesAnalytics` has no repository-level test at all |
| R-11 | Mechanical/Prompt | `ReportRepository.ts:2080`, `:2288`; `consumption-actions.ts:84` | Untyped history-action literals, and a comment citing a precedent that does not exist | confirmed | #863 | Literals **rejected** — no live mismatch, and the action values are persisted, so a rename is a migration rather than a refactor. The **comment** is confirmed: it cites the typed constant as an inlining precedent when that code does the exact opposite, pushing a contributor the wrong way |
| R-12 | Performance | `supplier-cost-sql.ts:70` | A defensive tiebreak costs a per-row temp b-tree in six reports | rejected | — | **Do not remove the guard.** Re-measured at 1.48x / 1.46x, stable, plan confirmed — but a sync merge really can transiently leave two rows flagged, and without the tiebreak an item's cost and every valuation built on it become non-deterministic and can differ between devices. Recorded so a later phase does not re-find the ratio and reach for the obvious optimisation |
| R-13 | Functional | `locations.dead_stock_days` | An out-of-range value inverts the dead-stock report | carry-over | — | The finder's **premise was wrong** — the repository clamps too. Reachable only from a tampered snapshot or a hand-edited database. §11: it names the consumer the Phase 2 carry-over row said was missing (an invalid date makes every comparison false, so every opted-in item reports as dead). One-line tightening: the CHECK should match the clamp |
| U-1 | Mechanical | `permissions.enforcement.test.ts` | The ACL guard test exercises 12 of 157 gated writes | confirmed | #847 | Two mutations run twice over the **whole suite** (800 files / 12,448 tests): both lose their permission check with not one test red. Corrections: 12 gated *write* methods, not 14; and "nine or more repositories" understates it — **sixteen** whole repositories with gated writes are never constructed. U-5 folded in |
| U-2 | Functional/Prompt | `SettingsRepository.ts:11-14`, `:48` | `publish` writes the cross-device settings table with no permission, justified by a false claim | confirmed | #849 | Split. The Prompt half is unambiguous — the key it says does not exist is asserted in eight places. The functional half is real but bounded, settled by listing the fields: most shared settings are cosmetic, but four groups are not (base currency, which feeds money rounding into a **persisted** sale total; alert thresholds; retention windows; scanning network switches). Note the asymmetry — the consent-host list is deliberately device-local, yet the master switch travels |
| U-3 | Prompt | `base.ts:26-30`, `:57-61`; `permissions.enforcement.test.ts:5-8` | Three docstrings say the guards are inert until a phase that shipped | confirmed | #848 | `degraded`, not cosmetic: `index.ts:183-189` in the **same directory** already contradicts `base.ts` directly. Combined with U-1, a maintainer has two independent signals saying the guard does not matter and none saying it does. Cross-reference #775, do not merge — that sweep is unguarded *parity* claims; this is temporally stale guidance |
| U-4 | Mechanical | `UserRepository.ts:304-308` vs the kind-keyed triggers | Two definitions of "built-in account", and an import path that does not filter what the export withholds | confirmed | #822 | **Exploitable — §5.3 was honoured and the maintainer was asked before filing** ("This software hasn't been released yet - please go ahead and file a standard github issue"). Widened from "no reachable divergence" to demonstrably reachable, with four passing tests. The visible harm to lead with is an account the app can neither delete nor disable. The resolver's ordering is not the defect; the set of rows reaching it is larger than the schema comment claims |
| U-5 | Mechanical | `UserRepository.signin.test.ts:196-203` | A test asserts the plaintext password is absent, a value never stored | confirmed | #847 | Narrower than claimed: leaking the real triple through the mapper shows this file passes entirely while the sibling test goes red — so there is **no coverage hole** behind it. Test theatre, not a gap |
| U-6 | Functional | `item/core.ts:822` vs `:909` | `hardDelete` cascades the ledger on `items:delete` where `clearHistory` demands `audit:delete` | rejected | — | Zero callers confirmed across four trees, and §10's P2-8 **already records** that fact (filed as #802). A dead method names no consequence. §11: P2-8 argues `hardDelete` is the method that would reclaim space, so the gate is a trap in its path if it is ever wired up |
| U-7 | Prompt | `en.json` `users.subject.checkouts.help` | The Loans help claims Delete covers cascaded loans | rejected | — | Shares U-6's root cause. Both limbs check out for **live** behaviour — the shipped per-item removal is a soft delete, which erases no loans — so the sentence is imprecise rather than false |
| U-8 | Prompt | `features/users/permissions.ts:147-148` | `normaliseGrants` says registry order and sorts alphabetically | rejected | — | Nothing depends on registry order: three non-test callers, none reads position, and the fingerprint hashes the order actually produced. One word wrong, no one suffers. A line on #775 if recorded at all |
| C8-1 | Mechanical/Functional | `mappers.ts:376` | A webhook filter stored as an array, a scalar or malformed JSON reads back as no filter | confirmed | #842 | Widened. The delivery chain was re-verified line by line into the bridge. The **write path is correct and states the same invariant**, so it is honoured on one side only. Routes in: the sync merge writes peer values verbatim and `webhooks` is a synced table; a restored backup; a truncated write. The existing test **pins the defect** — git history shows it predates the parser being wired in — and must be corrected as part of the fix. The plan document states the invariant outright |
| C8-2 | Functional | `erase-actions.ts:207-209` | Erasing item photos removes the whole shared OPFS directory, destroying location photo originals | confirmed | #820 | Widened twice: the wiki has **no** Location photos row at all, and "All items" also clears images. The repo states the hazard in the sibling module — the maintenance sweep is careful, the Danger Zone is not. OPFS files are not in the sync artefact, so no peer can restore them. Root cause is Phase 4's area, filed by Phase 3 per §1 |
| C8-3 | Mechanical | `StorageRepository.ts:100-108` | The history prune writes its delete and its watermark outside a transaction | confirmed | #859 | Three corrections: the failure is **not** silent (the second call is awaited, so the user sees an error) — what is silent is the state left behind; the likelier route is the **first** statement, because a timed-out request is documented as not cancelled, so a large delete can commit in the worker and reject in the caller; and both alternative silent-failure theories are impossible. Not data-loss — the archive is written and confirmed first |
| C8-4 | Functional | `AttachmentRepository.ts:75-127` +2 | Three repositories write length-limited columns without checking first | confirmed | #843 | **The finder's claimed output was wrong** and the verifier nearly reproduced the error: the raw SQLite text does **not** reach the user. The defect is a field-less, limit-less sentence. The `maxLength` sub-claim was dropped as misleading (the primitive deliberately omits the native cap). One real sub-finding survives: the attachment inputs are not wrapped in the form-field primitive, so nothing warns inline either. L-5 joined the sweep |
| C8-5 | Performance/Functional | `ImageRepository.ts:66-70` | The stored thumbnail has no size cap on any write path | confirmed | #855 | The **measured** blast radius is what lifts it: one 8 MB row makes a 25-row page **94.9x slower and 110x larger**. Narrower in one respect — there is no *app* ingress, since compression caps every picked photo. Same reachability band as #769/#349, both accepted. #641 is not a duplicate (it caps the picked file; fixing it leaves the column and the sync decode unguarded). L-11 handed it a second unbounded column |
| C8-6 | Functional | `triage-actions.ts:94-115` | Downgrade images destroys the only local copy with no save seam | confirmed | #824 | Restated and sharpened to **data-loss**. The method has no save parameter at all. The backup zip **does** carry the originals, so a safety net exists — the defect is that nothing tells the user to take one and **two** documents say the opposite: the wiki's "each keeping a copy" and the dialog's "your cloud backup is left untouched", which names an artefact that never held the bytes. Separate from #820: different path, failure and fix |
| C8-7a | Performance | `SuggestionRepository.ts:42-53` | `distinctValues` is an unbounded unindexed full scan with two temp b-trees | confirmed | #856 | Severity narrowed by the cache the finder was asked to check: **not** every dialog open — a 60-second stale time and nothing invalidating it means at most one refetch per field per minute. Honest caveat carried into the issue: at 8k rows it is only 1.8x a bounded list page; the defect is that it is the one item-table read with no cap at all |
| C8-7b | Functional | `SuggestionRepository.ts:42-53` | The same read includes removed items | rejected | — | No consequence. The seam's own contract is "the distinct values the user has already entered" and "suggestions are a shortcut, never a constraint"; excluding archived items would make re-adding a part from a supplier you stopped stocking worse |
| C8-8 | Performance | `SettingsRepository.ts:29-31` | An unbounded `SELECT *` over a synced table | duplicate #769 | — | Restatement of #769/P1-46. The row **count** is bounded by construction (one row per registry field); the read is only expensive if the value is unbounded, which *is* #769 |
| C8-9 | Mechanical | `settings-sync-runtime.ts:197-208` | The adopt fallback bypasses the stores' normalisers | confirmed | #857 | The finder's **example is wrong**, the defect is real. The preferences-store case is unreachable (the sync toggle lives in that store, so enabling it writes the blob). The genuinely fresh-device route is a different store: the layout key is never written at boot, so a fresh device takes the fallback for **every** field and the density normaliser — whose comment says its table relies on being exhaustive — never runs. Phase 11's file, found by Phase 3 |
| C8-10 | Mechanical | `WebhookRepository.test.ts:200-204` | A dead assertion naming a literal that appears nowhere | rejected | — | Decisively. Two mutations: the schema's own CHECK stops the first; the second fails at `:190` and, with `:190`/`:191` removed, at `:196` itself. A redundant assertion inside a test that bites is not a §2.1 finding. Consistent with C2-10 |
| C9-1 | Mechanical | `item-relations.test.ts:124-132` | The relations parity test gives each item one relation, so both orderings coincide | confirmed | #862 | **Much narrower** than the finder claimed: the parity claim *is* false and the test *is* vacuous, but all five consumers funnel through a presentation helper that re-sorts totally, so nothing user-visible differs — which also makes **both** SQL orderings dead. Lead with the false claim, not the vacuous test |
| C9-2 | Mechanical | `item/revaluations.ts:63` +2 | Three mixins name atomicity as their central invariant and no test holds them to it | confirmed | #850 | The gap is **cheaply closable**, which the finder had not established: the crashed-driver helper cannot test it (it rejects the precondition read too), but a ~12-line poison-one-statement decorator does, proved red on the mutation and green on real code. Failure modes are a wrong insurance valuation and a resurrected relation. C5-13 joined it |
| C9-3 | Mechanical | `item/revaluations.ts:76-79`, `item/test-records.ts:82-84` | No test passes page parameters, so the pagination limb is unasserted | confirmed | #861 | Not a duplicate of #815 — **complementary**. #815's fix *changes* this code and would ship the new envelope with the same zero coverage, and the test-records half is outside #815 entirely |
| C9-4 | Mechanical | `revaluation.test.ts:76-85` | The rowid tie-break is never exercised, though ties are the ordinary case | confirmed | #861 | Day-resolution claim verified — the date helper parses to midnight UTC and says so — and the editor is the only write path, so two revaluations on one day tie exactly |
| C9-5 | Mechanical | `item-relations.test.ts:55-63` | A test named for two behaviours asserts one | confirmed | #861 | #805 cites the early return, never the normalisation. Note for its fixer: if #805 makes the note renderable, the current coalescing would start rendering an empty note row |
| C9-6 | Prompt/Mechanical | `revaluation.test.ts:62-73` | A test title asserts the invariant #795 disproves | confirmed | #795 (comment) | Same root cause as an open wrong-data issue, so a separate ticket would be actioned into a duplicate. Filed as a comment naming the file, the line, the title and the failing fixture |
| C9-7 | Mechanical | `test-record.test.ts:88-94` | The ordering tie-break limbs are timing-dependent | confirmed | #861 | **The finder's measurement was wrong.** Over 20 runs the id limb went undetected 5 times (25%), not "3 of 3 solid". Corrected claim is better: **both** limbs are timing-dependent and mutually exclusive, so on any run at least one is unheld. A test that misses a regression 25% of the time is absent, not flaky |
| C9-8a | Mechanical | `revaluation.test.ts` | Fractional and zero values are never recorded | rejected | — | Rejected as a defect — both round-trip exactly. Confirmed as a coverage gap only, carried into the sweep |
| C9-8b | Functional | `item/relations.ts:46` | A soft-deleted counterpart still appears in the Related list | rejected | — | A **recorded decision**: the checkout dialog disables the checkbox and labels it "removed from inventory", citing #661, and its comment states that one with nothing on hand is shown but not selectable "so the gap is visible rather than silently omitted". Filtering in SQL would hide a gap the code deliberately shows |
| C9-8c | Mechanical | `test-record.test.ts:105-109` | The activity-log note wording is never checked | confirmed | #861 | Wider than the finder claimed — it applies to the revaluation file too. Both templates can be replaced with a placeholder and 21 tests stay green. These are user-visible strings |

### Phase 4 — Data integrity

Pinned SHA: `983986fd425a6f0c6a538470a41aa7ec5dbb5aa7`

Six finder units (U1 snapshot and trust boundary; U2 reconcile and merge; U3 providers, engine and
clock skew; U4 backup and restore; U5 archive and storage triage; U6 danger zone, events and the
save-file seam) produced 63 candidates; a 64th (U2-3b) was found by a verifier while disproving
U2-3. Nineteen verifier passes ran. Four candidates were reported independently by two finders and
are recorded once, with both IDs.

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Mechanical | `sync/snapshot.ts:1111-1117`, `reconcile.ts:2059-2103` | The gauge correction re-stamps `items.updated_at` on every sync for every gauge item, so a no-op beats a peer's real edit | confirmed | #869 | `data-loss`. Trigger fires on an identical-value UPDATE — settled against `node:sqlite` first. Converged pair still emits a resolution while `upsertWouldNoOp` correctly suppresses the LWW upsert, so this is #161's uncovered residue, not a duplicate. Edit loss driven end to end with `conflicts: []`. Verifier corrected the finder's `withRecomputeDeferred` aside as a red herring, and narrowed the population to gauges whose ledger reconstructs on both sides |
| U1-2 ≡ U4-4 | Functional | `sync/backup.ts:238-241`, `types.ts:15`, `backup/restore-backup.ts:218-225` | A newer build's backup restores partially and silently on the Merge and clone paths, and the wiki promises the opposite | confirmed | #870 | `data-loss`. Found independently by U1 and U4. `SYNC_FORMAT_VERSION` has one line in its entire git history against 103 commits editing the baseline. Decisive evidence: the same table name is REFUSED in the tombstone section and ACCEPTED in `tables`. Not #501's residue — a sibling gap it never examined |
| U1-3 | Mechanical | `sync/backup.ts:54` | A tombstone's `deletedAt` is checked for finiteness but not range, so an absurd value is never pruned and wins LWW for ever | confirmed | #876 | `data-loss` + `unusable` for a bridge host. Verifier found a limb the finder missed: the bridge's driver **throws** on the oversized integer, so that host can no longer build a snapshot at all. The un-crafted (fast-clock) route is real but cannot compound, so it does not raise severity. Residue of #351, whose own exclusion list does not mention this clock |
| U1-4 | Mechanical | `snapshot-integrity.ts:87-98`, `fk-refs.test.ts:73-89` | Four FK exemptions defer to a private registry no test reads | confirmed (widened) | #901 | Filed as a five-item sweep with U2-6, per #254's precedent. Widened from one repair to three: `location_history.actor_user_id`, `locations.parent_id` (the twin of #602, asymmetry reversed) and the whole stock-delta registry can each be deleted with **12448/12448 green**. Instrumenting every repair showed which fire at all |
| U1-5 ≡ U2-7 | Mechanical | `conflict-restore.ts:37-38,56` | An unvalidated persisted `tableName` reaches an interpolated SQL identifier; the column fallback beside it is dead code | rejected | — | Both limbs factually true and demonstrated (the fallback is unreachable because the dictionary yields `[]`, not `undefined`). **Threat model empty**: the key is `backupIncluded: false` and outside the settings groups, so no backup can seed it; shared settings live in a DB table. Sole writer is same-origin script, which already holds the driver. Genuine residue of #176's stated intent — two lines of hardening if that is revisited |
| U1-6 | Prompt | `sync/snapshot.ts:3-6` | The module docstring claims ≤100 paging for reads that are single unbounded SELECTs | rejected | — | Premise fails on the sentence's own grammar: the parenthetical follows the first list item and does not distribute over the rest. The three membership joins are documented as **not** syncable tables, so the claim never covered them. No data loss; a single statement is strictly more atomic than a paged walk, which is #204's own reasoning. The only reachable consequence of acting on the misreading is a regression |
| U1-7 | Mechanical | `snapshot.test.ts:10-21` and two siblings | No tsconfig covers `src/**/*.test.ts`, so three fixtures omit required fields and leave two loops untested | rejected | — | The type-lies are real (proved under a probe tsconfig) but the causal chain is broken: **13 tests** enter the clone's stockDeltas loop and **46** enter the repair loop, from end-to-end tests rather than these fixtures. Only the repair's *drop branch* is uncovered, which is U1-4's territory. Root cause already recorded in #601's closing comment; re-filing would duplicate it |
| U2-1 | Mechanical | `sync/merge.ts:479-525` | The TTL clone's salvage re-union has no survival filter, so a parent the remote dropped aborts the whole clone | confirmed | #875 | `unusable`. Filed as ONE issue with U2-4 on the verifier's recommendation — same loop, same fix. Widened: a **tag edge alone** aborts it, not only ledger rows. "Can never complete" proved by running the same merge twice. Control with the item present clones cleanly |
| U2-2 | Performance | `reconcile.ts:1948` | Every surviving region edge is passed as `reinsertKeys` and re-upserted on every sync | confirmed | #890 | `degraded`. Two identical snapshots still produce **403 statements where 3 would do**. The tag join on the same fixture emits 0, which is the control. **Correctness limb disproved**: row count unchanged, nothing re-stamped, so unlike U1-1 it cannot feed #161's ping-pong |
| U2-3 | Functional | `reconcile.ts:771` | The open-loan collapse is gated on SERIALISED, so a single-unit DISCRETE asset ends a merge on two loans | rejected | — | **This is the register's P2-51, now given its pass.** Deliberately rejected in #542's closing comment, asserted by `reconcile.test.ts:288`, and justified at `reconcile.ts:752` (serialised loans move no stock, so closing one needs no restore). The proposed widening also fails to cover the multi-unit case, demonstrated as CONTROL E. **P2-51's `unverifiable` verdict should be read as `rejected` from here** |
| U2-3b | Functional | `delta-crdt.ts:169`, `reconcile.ts:2171-2172` | The over-consumption clamp is written back, breaking the completeness invariant, so returning both loans invents units | confirmed | #877 | `wrong-data`. **Found by the verifier while disproving U2-3** — no finder proposed it. A one-unit item ends at 2, a three-unit item at 6, on both devices, permanently. Four controls isolate it. Contradicts the design record, which chose this clamp precisely because it was believed self-correcting |
| U2-4 | Mechanical + Prompt | `merge.ts:474-488` | The clone's salvage applies the user rekey but not the System fallback, while its comment claims parity | confirmed | #875 | `unusable`. Folded into U2-1. `ON DELETE SET DEFAULT` verified not to rescue an INSERT. The delta path re-attributes correctly on the identical fixture — the asymmetry is the finding. The comment "exactly as `reconcile` does it" is false as written |
| U2-5 | Mechanical | `snapshot.ts:107-111` | `WIPE_FILTER` is interpolated into the clone DELETE with no schema check, so a renamed column breaks it silently | rejected | — | The mutation produced **28 failures across 11 files** with an error naming the column. "Silently" was the whole finding and it did not survive; the finder predicted 1–2 integration tests. Surviving but inert: it is genuinely absent from the guard test its neighbour is exported for — a one-line tidy, not an issue |
| U2-6 | Prompt | `loan-split-stock.ts:114-129,143-179` | Two comparators promised "kept in step by hand" with the replay engine, with no test | confirmed | #901 | Filed in the sweep with U1-4. Both pairs verified to currently agree, tier by tier. Each inverted separately: **12448/12448 green** both times. Consequence named by the code itself — rank the movement ahead of the assertion and #711's loss returns |
| U2-8 | Mechanical | `reconcile.ts:208-210` | `num()` asserts rather than converts, so a string timestamp concatenates and stamps the row centuries ahead | confirmed | #878 | `wrong-data`. Crux settled first: a STRICT INTEGER column **does** accept a well-formed integer string losslessly, so `backup.ts`'s documented bind-time defence fails. Two limbs disproved — the plain LWW path stores correctly, and the skip comparison folds a string and its numeric twin together. Reachability capped: no first-party writer can emit it |
| U2-9 | Performance | `reconcile.ts:219-223` | The tombstone list is rescanned once per table per side, ~88 times per merge | rejected | — | Arithmetic correct (41 tables × 2 + 6 = 88). Measured: at a realistic 10,000 tombstones the whole thing costs **2.4 ms** and the best possible fix saves **1.5 ms**, in the worker. The ratio *falls* with size, because the per-entry `Map.set` is the cost and both shapes pay it. Below the threshold for recording as a defect |
| U2-10 | Mechanical | `reconcile.ts:201-206` | `upsertWouldNoOp` folds `null` and `''` together, suppressing a real write on an exact tie | rejected | — | The fold is real and permanent, but the state is unreachable: **no schema default is `''`**, every repository normalises `'' → null` before writing, and no merge repair writes one. The looseness is also partly load-bearing — it is what makes a `bigint` and a `number` compare equal, so a strict comparison would break #161's fix |
| U2-11 | Mechanical | `reconcile.ts:272-284`, `:2015` | The surviving-item set is derived before the cycle guard splices rows out | confirmed (widened) | #889 | `unusable`. **Finder rated it LOW and could not reach it; the verifier confirmed it, found the route via #818, and widened it to three limbs** — the locations set has the identical fault, and the third is not an ordering bug at all but a survivor left pointing at a removed row. A first-time device pairing is the reliable trigger. Note the fix for limbs 1–2 does not address limb 3 |
| U3-1 | Mechanical | `sync-engine.ts:352-355` | The tombstone prune's cutoff is server-frame while `deleted_at` is local-frame | confirmed | #871 | `data-loss`. Controls at 0 days and 100 days pass; 200 days fails — isolating the frame rather than "any skew". Consequence worse than claimed: the device drops the marker from the snapshot it publishes, so the whole vault forgets the deletion. The adjacent sweep in the same pass converts correctly, with a comment saying why |
| U3-2 | Functional + Performance | `providers/google-drive-api.ts:107-136` | The Drive push is non-resumable, unbounded, and the create path builds a second full copy | confirmed (split) | #907 | `degraded`. Memory limb measured at exactly **1.00× extra on create, 0.00× on update** — and **narrowed to the first push per connection**, since the file id is cached. New limb found: above 512 MiB the concatenation throws where the update path would have succeeded. **The size-ceiling limb is UNVERIFIABLE — see §11** |
| U3-3 | Mechanical | `sync-engine.ts:226-231`, `clock.ts:17-20` | The sync offset has no plausibility check, while the skew feature refuses the same reading | confirmed | #872 | `data-loss`. Both paths verified to share one measurement. A peer's genuinely newer edit destroyed with `conflicts: 0`. **The implied fix was disproved**: a ceiling alone would break the genuine-slow-clock case, and a four-hour bogus header does the same damage as a 400-day one. Converse of the reasoning recorded when #326 and #393 closed |
| U3-4 | Mechanical | `sync-engine.ts:231` | `??` does not catch `NaN`, so `effectiveNow` is unguarded where the offset is guarded | confirmed (folded) | #872 | Folded into U3-3 as a limb rather than filed alone. Latent — no shipped provider can emit it, and it fails loudly (a NOT NULL abort) rather than silently. Residue worth carrying: the throw is a bare `DbError`, so the screen discards the merge's conflict records |
| U3-5 | Functional | `SyncScreen.tsx:513-516,277-289` | Disconnect is the only control not gated on `busy`, and using it mid-sync reports an expired sign-in | confirmed | #896 | `degraded`. The 401 is not a stub — the real transport path was driven. Wider than claimed: the token is consulted on every request, not only the upload. Second limb (a stale stamp landing on a newly connected remote) narrowed but real |
| U3-6 | Prompt | `providers/file-system-provider.ts:89-90` | The zero-byte guard's stated reason contradicts its own file 13 lines later | rejected | — | The inaccuracy is real and quotable. But deleting the guard is behaviourally a no-op — the next line throws the same class with the same message — so the worst outcome of acting on the wrong comment is removing two unobservable lines. The comment's *conclusion* also survives by a mechanism it does not name (the handle is created before the write) |
| U3-7 ≡ U4-7 | Functional | `sync-status-format.ts:5-129`, `SyncScreen.tsx`, `BackupDialog.tsx` | Both screens nest fully translated sections inside untranslated ones, and the sync outcome is English-only | confirmed (reframed) | #906 | `degraded`. Found independently by U3 and U4; filed once, since it is one shape and the same picker is used by both. **The "convert these screens" limb is rejected as #60's.** What survives: the i18n programme's screen-by-screen breakdown (#230–#244) has no ticket for Sync, Backup, Archive, Storage or Danger Zone, and #233 records that it came from a walk of every source file — so it is a gap, not a deferral |
| U3-8 | Functional | `SyncScreen.tsx:342-353` | The missing-remote guard is disarmed for any newly connected remote | rejected | — | A recorded decision whose stated rationale covers this exact trigger, and the harm does not reach the guard's purpose — publishing into an empty folder discards nothing. The finder's basename argument runs the wrong way: two folders sharing a basename **keep** the watermark, so the guard stays armed for the most confusable case. Residue recorded in §11 |
| U4-1 | Mechanical | `restore-backup.ts:52-55`, `backup-format.ts:737` | The chosen zip is inflated whole, synchronously, on the main thread, with no size cap | confirmed (reframed) | #883 | `degraded`. A 0.50 MiB file allocating **512 MiB** and blocking 1936 ms, measured. **The SECURITY framing was deflated**: no automatic or remote ingress, one caller behind a file picker and a permission check, consequence confined to the tab. Project precedent (#641, #762) is not security-framed either. Verifier also found the identical shape at `restore-archive.ts:128-136` |
| U4-2 | Mechanical + Prompt | `restore-backup.ts:152` | `applySettings` runs past the commit unguarded, so a refused storage write fails a landed restore | confirmed | #880 | `unusable`. **Limb disproved**: the user does not see "The restore failed." — `useErrorMessage` passes the raw `DOMException` through, which is worse. Two limbs found beyond the claim: the write is partial, and the count of what landed is lost. Every other storage write in the app is guarded, with comments naming this failure |
| U4-3 | Mechanical + Prompt | `settings-groups.ts:448-453` | The merged preferences keep the backup's envelope, so store migrations re-run over un-restored local fields | confirmed | #881 | `wrong-data`. **The verifier found the reachable instance the finder missed** (a v0 backup silently zeroing low-stock thresholds) and showed the finder's own example needs a same-day window in July 2026. `git log -S` proves the docstring's premise died in the same commit that wrote it |
| U4-5 | Mechanical | `backup-format.ts:619-644,690-713` | The manifest is an allow-list in one direction only, so an undeclared entry is used unverified | rejected | — | Mechanically true and demonstrated. Consequence-free: no non-adversarial route (the manifest is derived from the entries written), and it grants an adversary nothing, because the digests are unkeyed and live inside the file — squarely inside `checksum.ts`'s stated scope. The images half **is** bidirectional. The one real consequence is #760's |
| U4-6 | Mechanical | `safe-mode-actions.ts:61-76` | The database is disposed before it is written, so a failed write destroys data and reads as "nothing happened" | duplicate #750 | — | Same mechanism, same four locations, same severity as #750. **"Partly overwritten" is false**: the writable is aborted and the original keeps its contents (#203). The genuine third corner — a failure that *had* touched the data — is U4-2, which is outside both #750 and #759 |
| U4-8 | Mechanical | `backup-format.ts:738-740` | A memory failure while unzipping is reported as "not a Gubbins backup" | confirmed (folded) | #883 | Folded into U4-1 on the verifier's recommendation — a cap removes most of the failures that produce the wrong sentence, and the fix is one line in the same function. Cause discarded, nothing logged; the sibling zip worker deliberately keeps its cause |
| U4-9 | Functional | `BackupDialog.tsx:606-616` | The settings-group picker is the one control not held while a restore runs | confirmed | #900 | `cosmetic`. Filed with U4-11 as one small change to one file. **Widened slightly**: the control does not merely do nothing, it contradicts what is happening — the boxes read unticked while those groups are being restored. Wiki limb disproved: its "stays put" promise is about dismissal |
| U4-10 | Functional | `snapshot.ts:1206-1232` | A wipe-and-clone Replace resets every photo's downgrade marker | rejected | — | Mechanism confirmed and demonstrated, with a merge-mode contrast proving the test falsifiable. But the resulting state is **indistinguishable from the ordinary post-sync state** — full-resolution bytes never travel, so every peer already holds that combination, and every consumer is written for it and says so. All three asserted harms disproved. Residue in §11 |
| U4-11 | Mechanical | `BackupDialog.tsx:397` | Nothing pins #654's wiring at this call site | confirmed (folded) | #900 | Test-suite limb true — **887 tests stay green** after deleting the line. But **"nothing pins it" is disproved**: `npm run type-check` fails on that exact mutation, because the flag becomes unread. A narrower rewiring would still slip through, so the gap is real and smaller. Folded rather than filed alone |
| U4-12 | Prompt | `BackupDialog.tsx:100` | "Guaranteed recovery" describes a copy the app refuses after any schema change | confirmed (narrowed) | #887 | `cosmetic`. Filed with U4-13. Fingerprint shown to move on a one-column edit, against 103 commits touching the baseline. **Narrowed**: the exact copy is not wholly dead — it survives via the recovery screen's archive route, which is undiscoverable but real |
| U4-13 | Prompt | `docs/wiki/Backup-and-Restore.md:221-236` | The wiki's override paragraph covers a restore that offers no override | confirmed (one limb disproved) | #887 | `cosmetic`. The function's arity is the proof — there is no force parameter to call. **Limb disproved**: the same sentence's claim that a restore point is saved first **is** true for this dialog; only the override half is wrong. The page contradicts itself four lines apart |
| U5-1 | Mechanical | `images/full-res-policy.ts:43-52` | A photo added at the critical tier writes a global fact into a per-device column | rejected | — | Every mechanical premise true and demonstrated. Failed on two axes: it is a **recorded decision** (`full-res-policy.ts:9-12` states the conflation as the design; `opfs-images.ts:62-68` mandates the pairing), and the consequence is not distinctive — a control row proved the peer counts **2 of 2**, because it holds the same belief about every synced photo |
| U5-2 | Mechanical | `triage-actions.ts:109-113`, `opfs-images.ts:323-332` | A failed image delete still stamps the row downgraded | confirmed (narrowed) | #885 | `wrong-data`, low frequency. **Narrowed on three limbs**: the space IS reclaimable by deleting the photo or item (demonstrated); the toast reports a count, not bytes; and no access is lost, since no read path consults the marker. Field frequency could not be established and is not claimed |
| U5-3 | Mechanical | `opfs-images.ts:101-121` | A partial image read is archived and described as complete | confirmed (widened) | #884 | `data-loss`. **Widened** — the finder missed `build-backup.ts:147`, the portable backup path, which matters because that zip is the only other carrier of full-resolution bytes. The container ends self-consistent, so no reader can detect it. Three-way contrast in one file: two siblings make the opposite, correct choice |
| U5-4 | Functional | `StorageRepository.ts:39-48,100-108` | Triage measures and prunes only the Items half of the activity log | confirmed (narrowed) | #886 | `cosmetic`→`degraded`. Demonstrated at 1,000 rows reporting 0. **Narrowed hard**: "no route to reclaim" is FALSE — a dedicated Danger Zone target exists — and growth is bounded by hierarchy edits, so this is not a plausible cause of exhaustion. The defect is the mislabelling; #690 is the precedent on the neighbouring screen |
| U5-5 ≡ U6-9 | Functional | `StorageTriageDialog.tsx:271-299`, `EraseDataDialog.tsx:505-513` | Arming a destructive confirmation drops focus to `<body>` and the alert is never announced | confirmed | #897 | `degraded`. Found independently by U5 and U6; filed once, since `EraseConfirmRow`'s docstring says it mirrors `ConfirmRow`. Trap-escape limb **rejected** and independently re-confirmed. Distance measured: 3 tabs in one dialog, 6–8 in the other. The row is a `<span>` with no `tabindex`, so a naive `.focus()` fix would be a silent no-op |
| U5-6 | Functional | `auto-archive.ts:156-161` | The weekly archive stamps itself done on a save it never observed | confirmed (re-footed) | #905 | `degraded`. **The save-before-destroying rule was attacked and does NOT carry this** — it governs paths that *destroy*, and `download.ts` scopes itself the same way. What survives is narrower and does clear the bar: an unverified save writing a durable fact that retires the prompt for seven days. Corroborated in a real browser with downloads denied |
| U5-7 | Prompt | `StorageTriageDialog.test.tsx:3-5` | The test file asserts a permission rule its component rejects, and cannot fail if the keys are collapsed | confirmed | #899 | `cosmetic`. Mutation is the evidence: collapsing the two keys leaves **3/3 green**. The missing case written and shown to discriminate. Finder's line numbers were wrong (the file is 82 lines) |
| U5-8 | Functional | `StorageTriageDialog.tsx:271-292,331-352` | A failed candidate count renders "0 entries affected" beside a permanently dead button | confirmed | #898 | `wrong-data`. Driven through the real hooks with only the repository boundary stubbed. Widened: the failure is entirely silent and the healthy sibling section stays enabled, so nothing signals that half the dialog is broken rather than empty. The breakdown directly above does consult its state |
| U5-9 | Prompt | `StorageRepository.ts:12-15` | The docstring says `item_images` is not a synced table, when it is | confirmed | #888 | `cosmetic`. Asserted rather than eyeballed. Conclusion true, stated reason false; the same file states it correctly 80 lines below. Earns its place only because the wrong reason invites omitting the next per-device column from the exclusion list |
| U5-10 | Performance | `triage-actions.ts:62-77`, `triage.ts:145-158` | The history archive materialises everything and pretty-prints it, on a device short of space | confirmed (widened) | #902 | `degraded`. **Widened to the larger defect beside it**: the paged read is quadratic — 226× a single statement at 100k rows, ~30 s behind an un-dismissable modal — because each page re-scans and re-sorts. **Limb disproved**: indent 2 costs **1.48×**, not "roughly double". Memory measured at 2.78× the payload |
| U5-11 | Performance | `triage-actions.ts:108-113` | The image downgrade is two sequential awaits per image with no batching | confirmed (reason corrected) | #903 | `degraded`. 2,000 images cost **13.1 s** against **37 ms** batched, on real OPFS in a real browser. **The stated reason is wrong by two orders of magnitude**: the worker round trip is 34 µs/call, 68 ms of 13,098. The cost is the per-row autocommit. The proposed remedy survives, but the issue had to say "transaction boundary" or it would be fixed in the wrong place |
| U5-12 | Prompt | `tiers.ts:53-61`, `StorageBanners.tsx:272-273` | A parity claim between a predicate and the critical banner's English, with no drift test | confirmed (re-aimed) | #908 | `degraded`, latent. **The finder's own mutation disproved its framing** — changing the predicate fails 3 tests, so that side is well guarded. Inverting it onto the copy shows the banner can be made to say the opposite with **12448/12448 green**. The docstring was authored by #199's fix, which was this drift live. Critical and warning banners are rendered by no test |
| U5-13 | Performance | `useStorageStore.ts:41-46,293-317` | The quota poll runs for the tab's life with no visibility gating | rejected | — | Mechanical premises all true, including that `stopMonitoring` is called nowhere in app code. Measured on a populated origin: one tick is **0.071 ms**, so the worst cadence costs **0.41 s of main-thread work per day**. No leak either — start is idempotent and the timer is cleared before re-arming. Contradicts the recorded decisions at `:8-18` (#200, #504) |
| U5-14 | Performance | `auto-archive.ts:119-149`, `zip-in-worker.ts:117-119` | The archive holds every image plus two database copies in main-thread memory | confirmed (narrowed) | #904 | `degraded`. **611 MiB peak for a 260 MiB vault**, measured. #752's boundary read line by line: it owns the export copy and names the redundant slice at two sites, not this third one, and never mentions images. Decisively, #752 **asserts the opposite** of what is true here — the vault worker transfers on its *return* leg only — so folding would have buried it |
| U6-1 | Mechanical | `erase-targets.ts:189-194,543-545` | "Empty custom locations" ignores surviving children, so the whole erase aborts at COMMIT | confirmed | #873 | `unusable`. Verifier re-ran with the **complete** predicate (the finder had narrowed it) and read the FK actions from the built schema rather than the migration source. Control with parent links removed erases cleanly. Found an escape hatch the finder missed: selecting items and locations together succeeds |
| U6-2 | Functional | `erase-targets.ts:189-194,498-548` | The predicate omits the borrower column, so a lending location is deleted and its stock never returned | confirmed (widened) | #874 | `data-loss`. Mirror test shows the ordinary delete path restores the stock and writes a ledger row; this one does not. **Widened**: no tombstone is written for the destroyed checkouts either. Permission limb confirmed — `locations:delete` alone destroys checkouts. **Copy limb partially disproved**: "holding" is defensible; the omission is the defect |
| U6-3 | Functional | `erase-targets.ts:729`, `erase-actions.ts:190-195` | Clearing sync links in the same erase destroys the markers that erase just wrote | confirmed | #879 | `wrong-data`. Statement-index check first, then the merge driven with an items-only control that survives. **Limb disproved**: the zeroed cursor does **not** force a full re-pull — resurrection is the missing markers alone. Second effect observed but not driven: conflict detection appears disabled for the following pass |
| U6-4 | Functional | `local-store-resets.ts:76-78`, `useSessionStore.ts:46` | "Cloud sign-in" also signs the user out of Gubbins, skipping every safeguard | confirmed | #882 | `degraded`. **The escalation limb was REJECTED — not exploitable.** Reads are not enforced at the data layer by decision (#522), so there is nothing in the window to bypass; only already-active queries refetch; nothing writes; and the window self-closes in one macrotask, measured. Bridge-token residue of #521 survives as a limb |
| U6-5 | Functional + Prompt | `EraseDataDialog.tsx:265-270` | The failure toast claims nothing was removed and points at an empty console | confirmed (narrowed) | #893 | `degraded`. The console half is wrong **100% of the time** — zero logging in the feature, verified. **Narrowed**: most post-commit steps *cannot* throw (two swallow internally, the invalidations are internally caught), so the "no data removed" half needs a storage failure. Consequence found beyond the claim: the store reset is skipped, so a local target un-erases itself |
| U6-6 | Prompt + Functional | `erase-targets.ts:651-660` | "App preferences" silently destroys the bridge token and address | confirmed | #892 | `degraded`. Demonstrated, including that the narrow target keeps the address as its copy promises. Recovery cost is load-bearing: the token cannot be looked up, only replaced. **A permission-escalation angle was tested and does not exist** — the same two roles may erase both |
| U6-7 | Mechanical + Prompt | `lib/save-file.ts:150-172` | The save seam aborts a failed write but not a failed close | confirmed (narrowed) | #895 | `cosmetic`. Controls prove the abort path works and the failure propagates, so nothing is destroyed. **Three narrowings**: the comment sits inside the write branch and claims nothing about close; half the sibling's rationale does not transfer; and the harm itself is **unverified** — see §11. Ceiling set by #650, which calls the same shape "a papercut" |
| U6-8 | Functional | `EraseDataDialog.tsx:517`, `erase-targets.ts:245` | "Sync deletion: off" does not keep the erase local, and the next sync restores the data | confirmed (widened) | #891 | `wrong-data`. **Widened to the irreversible half the finder missed**: the items return but their history cannot, because the erase advances a device-local watermark regardless of the toggle. Not a two-device edge case — every push is a full snapshot, so one device syncing to a folder undoes its own erase. The wiki actively recommends the broken setting |
| U6-9 | — | — | — | see U5-5 | #897 | Reported independently by U6; recorded on the U5-5 row |
| U6-10 | Prompt | `erase-targets.ts:13-20` | The catalogue claims to list every cascaded child table and omits seven | confirmed | #894 | `cosmetic`. 19 tables cascade from `items`; 12 are listed. The rescue was driven and holds, with a control proving the fixture live. **Two parts of the claim disproved**: not all seven are in the synced set, and the FK registry does **not** carry an entry for three of them — they are rescued by different mechanisms, which is itself an argument the header misleads |

### Phase 5 — App shell, platform and shared libraries

Pinned SHA: `97a7e06c93ddd1629069103a8ec455867ccac478`

Twelve finder units (U1 boot and rescue, U2 router and base path, U3 service worker and CSP,
U4 stores and query client, U5–U7 the shared libraries in three parts plus the `src/lib` guard
tests, U8 `lib/env`, U9 design tokens, U10 the i18n seam, U11 hotkeys and modules, U12 errors and
the small screens). Roughly thirty verifier passes. Four candidates were reported independently by
two units and are recorded once, on the row named in the Notes.

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Mechanical | `SafeMode.tsx:40`, `useDatabaseBoot.ts:77-89,207` | The tab lock is never released, so Safe Mode's "Try again" shows "Already open elsewhere" for a single tab | confirmed | #929 | `degraded`. Settled in **three** real browsers before the app: a document queues behind its own lock, `held` and `pending` sharing one `clientId`. Driven to a screenshot. `whenReleased` is unsatisfiable by construction, so the promised auto-takeover cannot fire. #763 is a genuine two-tab bfcache case, not this |
| U1-2 | Functional | `en.json:191`, `BootScreens.tsx:243` | "Start without it" never says the fallback store is permanent for the origin | confirmed (narrowed + widened) | #942 | `degraded`. Pinning proven in the browser; a seeded fallback origin opens the fallback on every later boot **while isolated**, with the control picking the primary store. Narrowed: the gate already waits 20 s, so "a slow first install" overstates the trigger. Widened, and this is the stronger half: "everything works the same" is also untrue — the Safe Mode rescues cannot reach the database from the main thread there, and #766 exists only on that layout |
| U1-3 | Functional | `RescueActions.tsx:242-245`, `useConfirmSaved.tsx:12-16` | The boot/rescue family is half-translated, and the comment justifying the untranslated half is false | duplicate #243 | — | Both mechanical limbs true and demonstrated, including a German card with English buttons. **Both novelty limbs fail**: the partial split *predates* #243, and #243 pre-emptively refuses the justification (*"This ticket needs to confirm `useT()` is safe pre-hydration"*). The four-state disproof was posted as a comment there, since it answers that ticket's own blocking caveat |
| U1-4 | Mechanical | `SafeMode.tsx:14` | Reads the thrown value directly instead of going through `useErrorMessage` | rejected | — | Recorded decision. `error-copy-idiom.test.ts` excludes `src/db` and `src/app/error` **wholesale**, with the reason in its docblock and in the memory vault. `RouteErrorScreen` carries the same reason as an explicit `ALLOWED` entry. The finder's "the reason is spent because `RescueActions` uses the hook" fails — `RescueActions` does not match the idiom at all |
| U1-5 | Functional | `safe-mode-actions.ts:405-408`, `app-shell-reset.ts:17-28` | "Reinstall app files" cannot report having done nothing | confirmed (narrowed) | #963 | `cosmetic`, marginal. The alarming half **dies**: every surface rendering `RescueActions` sits downstream of a passed OPFS check, so both APIs exist and the escalation-to-purge chain is unreachable. What survives is a result type and a docstring promise no caller can keep |
| U1-6 | Mechanical | `safe-mode-actions.ts:441-445` | `indexedDB.deleteDatabase` is not awaited before `location.reload()` | rejected | — | Ordering reproduces; consequence **measured at zero**. 60/60 deletes completed across Chromium, Firefox and WebKit. The only shape that loses is a second tab holding a live connection, which `fs-handle-store`'s close-after-every-operation and the single-tab guard prevent. The `sessionStorage` limb is documented scope (`storage-keys.ts:14-17`) |
| U1-7 | Functional | `index.html:402-427`, `:85-88` | The `file://` guide reveals for **any** pre-mount failure and states one cause as fact | confirmed (narrowed) | #912 | `unusable`. Screenshotted on a COOP/COEP origin with a corrupt entry chunk; `?recover=1` proven to work on the same broken build. The `main.tsx` limb **dies** — three hostile-input probes (invalid JSON, hostile types, malformed OAuth fragment) all mounted normally — so the path narrows to bundle delivery. Not #741, which is the hatch's test coverage |
| U1-8(a) | Mechanical | `coi-bootstrap.js:48` | The reload counter uses `gubbins-`, so the storage-key registry cannot see it | rejected | — | The registry documents itself as covering `gubbins:` keys. Demonstrated that the scanner is blind to `public/` **regardless of namespace**, using a correctly-namespaced probe. Six registered `sessionStorage` keys are equally unsweepable by design. `coi-bootstrap.test.ts:104` makes a rename loud, so P1-28's reasoning applies |
| U1-8(b) | Mechanical | `coi-bootstrap.js:48-55` | The budget is never reset on success | confirmed (narrowed, re-framed) | #943 | `degraded`. Re-framed: an exhausted budget **manufactures a false `settled` verdict**. #260 decided the cap and the fallback boot, not the reset. On `layout === 'none'` the gate falls through with **no screen and no waiver** — proven by the repository's own passing test. Reachable in two clicks in the order `RescueActions` presents them. Narrow fix at `coi-bootstrap.js:28` |
| U1-9 | Functional | `RescueActions.tsx:229-232` | A landed restore is presented as a red failure | confirmed (widened) | #930 | `degraded`. Premise verified: `StaleJournalError` is only reachable after the bytes commit, on both paths. **Widened beyond tone**: the generic tail leaves three buttons live that re-open the database beside the hot journal, which the error class's own doc forbids. Limb 2 (the lost image shortfall) **rejected** as documented, tested precedence. #759 is the same symptom one layer down; this is the residue its fix would leave |
| U1-10 | Mechanical | `safe-mode-actions.ts:442`, `erase-targets.ts:724`, `fs-handle-store.ts:14` | `'gubbins-fs'` is a triplicated literal with no exported owner | confirmed | #931 | `degraded`, latent. Renaming `DB_NAME` left **zero new test failures** (1200 passed either way) while both erase paths silently no-op. No comment claims parity, so the rule does not fire on its face — but L-8/#868 and U1-4/#901 are the governing precedent for an unstated-parity case with a demonstrated consequence, and a live File System Access grant survives both erases |
| U1-11 | Performance | `safe-mode-actions.ts:330-351,454-458` | The JSON rescue dump materialises the database twice in memory | confirmed (narrowed) | #911 | `unusable`. **The mechanism is not the one claimed.** Not memory pressure: a hard V8 string-length ceiling of 536,870,888 chars, verified identical in Chromium, failing on a 64 GiB desktop. Reachable at ~7,000 photographed items with a cover field. Three limbs disproved — pretty-printing is only 10.8%, it is not a phone problem, and the failure *is* reported, as the bare string "Invalid string length". `uint8ToBase64` measured at 69–70% of stringify time |
| U1-12 | Functional | `RescueActions.tsx:335-339` | "This overwrites all local data" is not what a `.sqlite` restore does | rejected | — | Consequence **disproved**: the thumbnail lives in the database and travels with the `.sqlite`, and the app is written for "database present, full-res absent" because that is the ordinary state on every synced peer. A cross-device restore yields thumbnails, not blanks. The copy is an **over**-promise, the safe direction. Fold the one-line tweak into #395 if wanted |
| U1-13 | Mechanical | `recovery.js:84-95` | `clearShell()`'s ordering guarantee races the app bundle | rejected | — | **Two independent runs, reconciled.** Run 1 served assets `no-cache` and saw 0/3; run 2 served them `immutable`, as a static host does, and saw 5/8 — the race *is* reproducible on a **healthy** bundle. Both agree the dead-bundle case, the only one `recovery.js` exists for, holds 3/3. Limb 2 also rejected: `showMessage` does remove `#root`, but `main.tsx` is a deferred module whose null check has already passed, and nothing throws in ~20 loads |
| U2-1 | Functional | `NotFoundScreen.tsx:38-42,80-90` | Hovering a 404 suggestion opens the Settings dialog with no click | confirmed (widened) | #915 | `degraded`. Router source read: `onFocus`, `onMouseEnter` **and** `onTouchStart` all enqueue a preload at a 50 ms delay, and `preloadClientRoute` runs `beforeLoad` even for a redirecting route. Driven in a browser to a screenshot, plus a component test with a preload-off control. **Two verifier runs disagreed on dismissal; the lead settled it** — `[SPOTCHECK] reopened after a second focus: true`. Sweep: this is the only unguarded `<Link>` |
| U2-2 | Mechanical | `Dockerfile:95-96` vs `base-path.ts:31` | The two normalisations disagree on the empty string | confirmed (narrowed) | #913 | `unusable`. Built and run: the image builds, `nginx -t` passes, `HEALTHCHECK` reports healthy, `curl /` returns 200 — and 86 `.js` requests answer `200 text/html`. The browser shows the `file://` fallback, which blames the user. **Whitespace limb rejected** — it fails `nginx -t`, so that image never builds. CI covers only `/` and `/gubbins/` |
| U2-3 | Functional | `DeepLinkScreen.tsx:32-36` | An errored item lookup leaves a permanent spinner | confirmed (widened) | #938 | `unusable`. Three green controls; premise verified against the real `useItem` and a real query client (`isError` latches in ~1.0 s after one retry). Reachability is named by `worker-driver.ts:9-19` itself — `WORKER_TIMEOUT` and a latching `WORKER_UNAVAILABLE`. **Rose from `degraded` once U4-C1 was confirmed**: a paused query is a third uncovered state and needs only `navigator.onLine === false`. #306 fixed the same habit on four other screens |
| U2-4 | Mechanical | `ShareTargetScreen.tsx:34-36` | The stash is cleared before the `cancelled` check | confirmed (narrowed) | #910 | `data-loss`. The finder's StrictMode probe is **refuted**: under StrictMode the draft is seeded with no name and no `sourceUrl`, so a **dev build always produces an empty draft**. Production limb is latent — no routine unmount named; the strongest candidate is a Back press during a multi-megabyte photo read. `pruneStaleShares` proven unable to restore a cleared share |
| U2-5 | Performance | `__root.tsx:6` | `CommandPalette` is eagerly imported into the entry chunk | rejected | — | Measured: entry chunk −10.48 KiB raw / −2.88 KiB gzipped, eager graph −0.89% gzipped, and the precache **grows** by 5 entries. Three of the five claimed payloads (inventory queries, mutations, `lib/fuzzy`) never leave the eager graph. Ctrl+K would not break — the hotkey lives in the store — so the rejection is on magnitude alone. Incidental lead recorded in §11 |
| U2-6 | Prompt | `browser-smoke.mjs:180-186` | The comment describes `index.html` code that no longer exists | confirmed (narrowed) | #961 | `cosmetic`. False in the present tense and contradicts `index.html:11-16`; git history dates the drift to two efforts a week apart. **The "masks a genuine 404" limb dies** — nothing but re-introducing the exact bug can emit that URL, and the sibling `recovery.js` would fail correctly. Phase 0 territory; no Phase 0 row covers it. `PHASE_HANDOVER.md` is a dated record and was left alone |
| U2-7 | Mechanical | `Run.ps1:75` | The hard-coded base path hangs the readiness probe | rejected (duplicate of the Phase 0 §11 carry-over) | — | The **new** behavioural limb is false: the probe returns 200 (Vite's history fallback plus dev isolation headers), so the launcher opens immediately and the user gets a recoverable in-app not-found screen. Stripped of it, this is P0-46 restated |
| U2-8 | Mechanical | `route-suggestions.ts:47-58,88-91` | The `basepath` parameter is unreachable and its docstring describes an impossible case | rejected | — | Premise true — the router strips the basepath, verified over six real pathnames. **The trap is disproved**: passing a basepath changes the segment list in 3 of 6 cases and the suggestions in **none**, because `gubbins` scores below the threshold. Below §1's consequence bar. Belongs to Phase 17's dead-code sweep (precedent #377) |
| U3-1 | Mechanical | `sw.ts:257-259` | `activate` deletes every Cache Storage entry on the origin | confirmed | #925 | `degraded`. Control passes, case fails with foreign names taken from the neighbours' live `sw.js`. **Two other offline-capable apps share the origin today**, both with the same bug — so the harm is bidirectional, and opening one of them deletes all five Gubbins caches. Filed as one issue with U3-8 |
| U3-2(a) | Mechanical | `sw.ts:151-158`, `:316-331` | The superseded-precache prune runs after `addAll` | confirmed (narrowed) | #935 | `degraded`. Placement is **not forced** — the prune reads only `SW_STATE_CACHE`, `caches.keys()` and the module constant. Peak is ~6.1 MiB above the comment's claim. Sharper harm: the prune is also the recovery, gated behind the write that needs recovering |
| U3-2(b) | Mechanical | `sw.ts:154` | No `catch` around `cache.addAll` | confirmed (narrowed) | #935 | `degraded`. Missing catch real; **stated consequence disproved** — Docker sends real COOP/COEP so the chain breaks there, and on Pages the boot gate waits 20 s then offers the waiver, so the database does open. What survives: no offline capability, silent failure, possible permanent fallback pinning. **Partial overlap with open #739**, which already names this line and the 404 trigger; scoped to the missing catch |
| U3-3 | Mechanical | `sw.ts:55-59`, `:274-277` | The manifest dedupe keys on the raw URL, `addAll` on the resolved Request | rejected | — | Latent only. The real manifest at **both** bases has 209 entries, 205 unique raw **and** 205 unique resolved. Identical by construction — vite-plugin-pwa emits icon entries from the glob match, not the configured `src`. The secondary cache-mode-flip claim is false today: all four duplicate pairs share a revision |
| U3-4 | Functional | `PwaUpdatePrompt.tsx:95-97` | "Skip this version" permanently suppresses newer data-resetting builds | confirmed (narrowed) | #936 | `degraded`. **Shared premise refuted for the hosted app** — `deploy.yml` has carried a version-uniqueness gate since #276, and one run failed on it. Survives for self-hosting (measured: two `0.32.0` builds, different `baselineRevision`) and for a documented rollback, which needs no premise. The update still lands silently; what is lost is the advance warning |
| U3-5 | Mechanical | `stale-chunk-reload.ts:62,69` | The recovery marker is keyed on `APP_VERSION`, so a tab can recover only once per release | rejected | — | Mechanism reproduces, but the docstring's premise is **upheld by the same deploy gate**; the existing test is a faithful model of the post-recovery state rather than theatre; a second stranding needs three builds under one version; and the cost is a browser refresh |
| U3-6 | Mechanical | `OfflineIndicator.tsx:26,29,40` | Three hard-coded English strings in already-converted global chrome | duplicate #224 | — | The novelty premise is **false**: the converted "global chrome" is `AppNav` and the nav labels only. #224 covers the two live-region strings, #60 the visible pill. Register note: the #230–#244 breakdown enumerates `features/*` only, so `src/components/` shared chrome has no owning ticket — a line for #224, not a new issue |
| U3-7 | Functional | `OfflineIndicator.tsx:26` | The offline announcement promises a sync the user may not have configured | confirmed (widened) | #937 | `degraded`. **Wider than claimed**: untrue for *every* user, because `runSync` has one caller (the Sync screen button) and `getActiveProvider()` is module-level state that is `null` on every page load. No connectivity-driven trigger exists anywhere. #73 is an open request to *make* it true |
| U3-8 | Functional | `recovery.js:118-131` | `?recover=1` empties Cache Storage wholesale | confirmed (narrowed) | #925 | `degraded`. Same root cause as U3-1, and worse — it is unfiltered on **both** sweeps, so it also unregisters the neighbours' service workers. **The bridge-outage limb is rejected**: `BridgeReloadNotice` re-registers the origin on every mount before its early return, and those three screens are the only places the app talks to the bridge |
| U3-9 | Mechanical | `useConfirmSaved.tsx:58-64` | A re-entrant call strands the first caller's promise | rejected | — | Unreachable at four independent points: a single-shot call graph (one `confirmUnverified` call site, three non-looping callers), the modal's own backdrop, an independent busy guard at each call site, and no keyboard or palette route. Both documented exits settle. Recorded: `useConfirmSaved` does not prove a save landed and does not claim to |
| U3-10 | Performance | `sw.ts:373,381` | `ignoreSearch` forces a scan of the whole precache on every GET | confirmed (narrowed) | #944 | `degraded`. Measured in-worker on a real activated registration: 0.43 ms vs 0.17 ms median, ~24 ms per warm load, **0 disagreements over 352 real requests**. Cross-origin limb **dies** (0 cross-origin GETs on a warm load); `caches.open` limb dies (0.09 ms); the low-end-phone multiplier is **not established** — CDP throttling does not reach off-main-thread work. Applies to both call sites, including the one the finder defended |
| U4-C1 | Mechanical | `queryClient.ts:12-23` | `networkMode` is left at TanStack's `'online'` default | confirmed (widened) | #909 | `data-loss`, and the phase's most serious finding. Driven in a real browser with the app's **own** bundled react-query singleton read in-page. Three limbs: writes hang on a spinner indefinitely (427 ms online); closing the tab **discards** the paused write, with no mutation persister anywhere; and reads return **wrong** answers rather than blanks, because `keepPreviousData` keeps the previous result set. The pill promises "changes are saved locally" throughout. Narrowing that also explains why it went unnoticed: `onlineManager` never reads `navigator.onLine`, so a cold offline launch is unaffected and any reload clears it |
| U4-C2 | Mechanical | `useSavedSearchesStore.ts:29-34` | No custom `merge`, while two seams document one | confirmed (widened) | #922 | `wrong-data`. Both remote paths driven end to end through the **real** `applySharedSettings` and `applySettings`. `sameSettingShape` returns true for **any** array when the reference is the shipped empty default. The only one of the group with `backupIncluded` **and** `liveSyncable` — the only remote input. Complement of open #857, whose premise is false for this store |
| U4-C3 | Mechanical | `useModulesStore.ts:88-93` | A corrupt `intent` reaches `resolveEnabled` unreconciled | confirmed | #918 | `unusable`. Also reported independently by U11-C3. `null` throws from 46 call sites including `AppNav` and every gated screen. Fixture correction for whoever re-runs it: the stringy-`"false"` case reproduces only against a **real** registry id |
| U4-C4 | Mechanical | `useLabStore.ts:72-79` | The `merge` spreads the persisted blob after guarding one field | confirmed (widened) | #918 | `unusable`. Also reported independently by U12-C5. The **only** store whose merge spreads the persisted object; `git log -S` puts that merge in a **feature** commit (#455), not the #376 reconcile pass. `eraseGroup: null`, so the Danger Zone cannot clear it and `/lab` is behind the router — the only escape is the full purge. Lead demonstration for the issue |
| U4-C5 | Mechanical | `useAchievementsStore.ts:62-86` | `migrate` ignores the stored version and wipes a later blob | confirmed (narrowed) | #918 | `wrong-data`, latent. The **only** one of 17 stores that discards the other direction, against a posture two siblings state explicitly. Widened: the empty map is re-persisted at version 2, so rolling forward does not recover it. Not reachable today — version 2 is the highest ever declared |
| U4-C6 | Mechanical | `main.tsx:46,52`, `clock.ts:135` | `startLabClock` runs before `startClockSkew`, double-counting the device error | confirmed | #927 | `wrong-data`, developer-facing. Reported independently by U12-C6. The decisive evidence neither finder cited: `clock.test.ts:103-113`, the #326 regression test, sets the skew **first** — the reverse of `main.tsx` — so the fix is green in CI and absent at runtime. Skew ceiling is a **full year**, and measurement is unconditional |
| U4-C7 | Functional | `SettingsDialog.tsx:1405-1413`, `ReminderNotifications.tsx:31,63` | Reminders and their settings are not gated on the `alerts` module | confirmed (widened) | #939 | `degraded`. **Widened** with a surface the finder missed: `AppNav` renders an alert-count badge on the menu trigger while filtering the Alerts row out of the menu, plus a dashboard live region announcing that badge's count. The registry defines `alerts` as a page, and that settles it **against** the code — those surfaces exist solely to point at that page. #636 is the identical shape, actioned as a bug |
| U4-C8 | Mechanical | six `*.test.ts` files | Guards sweep `process.cwd()` rather than their own checkout | confirmed | #940 | `degraded`. Demonstrated in **both** cwds with one probe: red from the worktree, green from the primary checkout, and the probe was never seen (18 tests, not 19). Two aggravating facts: all six carry a stale comment blaming the test environment, which `repo-path.ts:15-21` explicitly corrects, and CLAUDE.md **mandates** worktrees |
| U4-C9 | Mechanical | `useAuthStore.ts:53-58`, `useLocationExpansionStore.ts:54-59`, `usePwaUpdateSnoozeStore.ts:48-53` | Three more stores on the default merge | confirmed | #918 | `degraded` / `unusable`. The snooze store **widened**: a numeric `skippedVersion` makes `compareVersions` call `.split` on a number, from a component at the composition root — app-wide Safe Mode. `useLocationExpansionStore` takes out the Inventory screen. **A vector cited by the existing record is disproved**: a truncated blob fails `JSON.parse` and falls back to the defaults |
| U4-C10 | Mechanical | `persisted-store-versions.test.ts:30,54-69` | The version/migrate guard is per-file, and its floor is loose | confirmed (narrowed); floor limb rejected | #948 | `cosmetic`. Per-store limb survives, but the guard's own header **already documents** the precondition ("Every such file holds exactly one store today") — the residue is that nothing enforces it. Floor limb **rejected**: 17 files against a floor of 14 discharges its stated anti-empty-sweep purpose. Folded into the storage-key issue; same function, same fix |
| U5-1 | Functional | `calendar-days.ts:104`, `agenda.ts:452` | `addCalendarDays` overshoots a non-existent local midnight | confirmed (widened, then narrowed) | #919 | `wrong-data`. Reproduced with an **independent** control. Widened: **all three** forward bucket edges shift, in **five** zones (Cairo ~110M, Havana, Santiago, Beirut, Azores), one day per year each. Narrowed on blast radius: only the field-due and maintenance lanes are attributable — #495 already dominates the other three. `localDayWindowCutoff` and `fieldDueStatus` measured **immune**. The docstring, the module header **and** the memory note are all wrong |
| U5-2 | Functional | `bridge-url.ts:18-30` | A base URL with a query or fragment routes every call to `/` | confirmed (widened) | #941 | `degraded`. **Four** join sites, not two. Ingress proven live: the address field is not inside a `<form>`, so `type="url"` validation never runs, and rehydration checks only `typeof`. **The #385 framing is rejected** — CORS is per-request, so the app reads the error body and produces a *confident wrong* diagnosis, which is arguably worse but is a different failure mode |
| U5-3(a) | Mechanical | `date-input.ts:20-23` | `toDateInputValue` throws on an out-of-range instant | confirmed | #924 | `degraded`. Same root cause as U6-3 and filed with it. Adds five unguarded call sites and the blast radius: no error boundary, so the whole screen dies and a booking becomes permanently un-openable. `export-data.ts:314` guards the **unreachable** `NaN` case and misses the reachable band |
| U5-3(b) | Functional | `date-input.ts:20-23` | `.slice(0,10)` truncates an expanded ISO year | confirmed | #923 | `wrong-data`, and **distinct** — it is silent. `Date.parse('+010000-01')` is **not** `NaN`; it snaps the day to the 1st, so the round trip drifts by up to 14 days with no error. The corrupt band is ~34× wider than the throwing band, and breaks the module's headline "It round-trips exactly" |
| U5-4 | Mechanical | `date-input.ts:26-31` | `fromDateInputValue` delegates to a lax `Date.parse` | rejected | — | Non-UTC probe run: the non-ISO forms do resolve at local midnight. But `history-change-format.ts:60-64` records the split as deliberate in prose, `catalog-import.ts`'s guard is a shape check **plus** a rollover check that cannot move into the seam, and every remaining caller is a native date input whose value the HTML sanitiser guarantees. Noted as a secondary bullet on #923 |
| U5-5 | Mechanical | `clock.ts:135-142` | A shape-valid, calendar-invalid date rolls over instead of returning 0 | confirmed (narrowed) | #952 | `degraded`, developer-facing. `'2026-13-45'` gives +395 days. Ingress is exactly the case the docstring names — a malformed **stored** value, reachable through `useLabStore`'s unreconciled merge. The interface control cannot produce it |
| U5-6 | Mechanical | `derived-uuid.ts:21-28` | An unvalidated namespace collapses two different inputs to one id | rejected | — | Collapse reproduces, but the **name** argument — which the finder did not check — is clean at all nine call sites: 48/48 distinct on the free-text `batchKey`, safe by construction because the UUID fields pin the separators. Determinism holds. All five namespaces are literal constants, so §1's style-preference exclusion applies. `contacts.ts:221`'s prose rule is true **by construction** |
| U5-7 | Mechanical | `date-input.ts:42-46,75-80` | The year is not zero-padded | rejected | — | Real, and one half reaches an input (`RenewLoanDialog` seeds from `toDueDateInputValue`, and the asymmetric round trip would make a loan open-ended). Requires a loan due in years 1–999. Noted as a secondary bullet on #923 |
| U6-1 | Mechanical | `fuzzy.ts:55,61,64`, `CommandPalette.tsx:745` | Code-unit indices are rendered as code points | confirmed | #955 | `cosmetic`. One issue, two limbs, one root cause. Driven in the browser with screenshots. Limb 2 **narrowed**: FTS5 discards the emoji, so rows are **not** dropped — the loss is the highlight and the fuzzy re-rank. Scoring limb **rejected** (it never inverts an order). Screen-jump mode unaffected: no shipped label has an astral char or a length-changing fold |
| U6-2 | Functional | `highlight.ts:108`, `HomeAssistantSetupScreen.tsx:46` | `behavior: 'smooth'` overrides the reduced-motion catch-all | confirmed (widened) | #932 | `degraded`. Also reported independently by U9-C4. Settled in **three engines** with five controls, including the pair that proves the catch-all works when CSS is the mechanism. Widened to two sites. A side finding: **both** `scroll-behavior: auto !important` lines are inert, since nothing in the app ever sets `smooth` in CSS — which is what produced the two false comments. Not in the accessibility sweep, which names no motion issue |
| U6-3 | Mechanical | `format.ts:370-378` | Three date formatters throw where their siblings return a placeholder | confirmed (narrowed) | #924 | `degraded`. **`NaN` limb rejected as unreachable** — a `STRICT INTEGER` column coerces a `NaN` bind to `NULL`, and every `Date.parse` is guarded. The **surviving** limb is new: finite values in `(8.64e15, 2^53)` are legal `STRICT INTEGER`s that pass every `isFinite` guard and then throw. No ingest path range-checks a date column, though `backup.ts:87` already owns the predicate |
| U6-4 | Functional | `format.ts:341-346` | `bytes()` picks the unit before rounding the mantissa | confirmed (narrowed) | #956 | `cosmetic`. The band is exactly 0.0501% of every decade, computed. **Sub-1-byte limb rejected as unreachable** — every caller supplies an integer. The `ImportFileBanner` scenario does not arise at the 16 MB cap |
| U6-5 | Functional | `format.ts:379-390` | `relativeTime` compares unrounded and formats rounded | confirmed (narrowed) | #956 | `cosmetic`. `Intl` does **not** normalise it (checked). "Every rung" is wrong — the week rung is immune, its band empty. Real surfaces are the inventory cards, not the activity feed |
| U6-6 | Performance | `format.ts:251-261` | A rejected currency code is never negatively cached | rejected | — | Mechanism real, and a malformed code **is** storable — no `CHECK`, no normaliser. But measured: ~1.06 ms at the 100-row page cap, ~4.8 ms scaled to 4×, which is the band this phase rejected twice. The naive fix is not free either — caching `null` would key the map on unvalidated strings from a 64-char column, losing the bounded-cache property `moneyDecimals` guards for explicitly |
| U6-7 | Mechanical | `money.ts:193` | `apportionMoney`'s clamp silently mis-apportions | rejected | — | Arithmetic true, and an **OVER** case the finder missed (there is only a carry-up path). But all **ten** call sites — not four — partition by construction. 1,800 adversarial report builds: every live column re-adds to its headline. The only red was forced with a cast. Both breakdowns already ship a top-N cut and do it the safe way, folding the tail rather than dropping it |
| U7-GUARD-1 | Mechanical | `storage-keys.test.ts:28`, `persisted-store-versions.test.ts:136` | A camelCase key is invisible to both guards at once | confirmed | #948 | `degraded`. **Widened**: underscore and dot escape too, not just casing. Nothing enforces kebab-case — contrast `permission-registry.test.ts:32`, which does exactly that for another registry. `extension/src/background.ts:319` proves the spelling occurs. **P1-28 does not reject it** — that row turned on the scan catching the spelling |
| U7-GUARD-2 | Mechanical | the same two files | A key built by concatenation is invisible | confirmed | #948 | Same predicate, same fix; filed with GUARD-1 |
| U7-GUARD-3 | Mechanical | `foundry-native-inputs.test.ts:252` | `<select>` and `<textarea>` are not guarded | confirmed | #957 | `cosmetic`. The docstring's stated exclusion argues the **opposite** way — `Select` and `Textarea` supersede rather than delegate, and CLAUDE.md names all three controls. Structural note the finder missed: `GUARDED_CONTROLS` keys on a `type=` attribute, which neither element has, so the fix needs a second pattern shape. Computed-type limb **rejected** as documented |
| U7-GUARD-4 | Mechanical | `hover-reveal-touch.test.ts:35` | Display and width reveals are not matched | confirmed (narrowed) | #957 | `cosmetic`. **The headline limb is false and must not be filed**: #258's own controls used `opacity-0` and `max-w-0`, both inside the guard's list. No live offender — all seven reveal sites use opacity or max-width and are correctly paired |
| U7-GUARD-5 | Mechanical | `dialog-scroll-bleed.test.ts:386-391` | The sweep is line-scoped, so a wrapped `cn()` defeats it | confirmed | #957 | `degraded`, latent. Stronger than the finder had it: a **live shipped call site** (`rail-modal.tsx:289-291`) already has the vulnerable shape, and adding one argument re-adds the #417 bleed while prettier and the guard both stay green. Measured: any *conditional* bleed passes 110 columns and wraps |
| U7-GUARD-6 | Mechanical | `query-key-factories.test.ts:30,37` | Hoisting the key to a const defeats the guard | confirmed (narrowed) | #926 | `wrong-data`. **The finder's `agenda` limb is refuted** — `agenda-invalidation.test.tsx:145-152` catches it. What survives is wider: `agenda` is the only prefix with its own literal sweep, so a hoisted const or an unnamed factory claiming the `reports` prefix walks past everything. Type-annotation limb confirmed but latent |
| U7-GUARD-7 | Mechanical | `docs-todo-status.test.ts:239-247` | The walk is non-recursive and `\b` accepts a malformed status | confirmed (narrowed) | #957 | `cosmetic`. Both limbs confirmed, including the `done/`-side variant the finder only reasoned about — a malformed banner **inside `done/`** passes outright. Limb 3 (the `m` flag) is a **duplicate of open #584**. Governs this audit's own plan document: splitting it into `docs/todo/audit/` would take every phase record out of the guard's sight |
| U7-GUARD-8 | Mechanical | `safe-area-token.test.ts:201-211` | The raw-`env()` sweep cannot see CSS or HTML | rejected | — | Ran live and the hole is real, but scanning `index.html` would produce a **false positive** — its inline `<style>` is a documented tokens-only exception — and including `.css` would report the definition file as its own first offender. No consequence under §2 |
| U7-CAND-1 | Functional | `volume.ts:187` | A fixed volume unit renders ordinary containers as `0` | confirmed (widened) | #921 | `wrong-data`. Screenshotted: "Volume ≈ 0 m³" beside 150×100×50 mm. **Widened** — `volumetric-fullness-text.ts`'s own mitigation comment describes this failure and its fallback is a no-op under a fixed preference, so a 46%-full location is **announced** as "0 m³ of 0.01 m³". The finder's #416/#457 ordering is **backwards**: `aa8910b9` (#416) edited `volume.ts` in the same commit and skipped its formatter — a half-applied parallel edit, one decimal worse than the state #416 called broken |
| U7-CAND-2 | Prompt + Functional | `name-fold.ts:48-51` | `namesMatch`'s docstring claims spacing is folded and it is not | confirmed (narrowed) | #920 | `wrong-data`. Forks reproduced end to end against the real migrations and repositories. Reachability unguarded — `validateFieldInput` trims only. **Prompt limb rejected**: "spacing" is imprecise, not false, and the module's own test names the trim. Filed with CAND-3 |
| U7-CAND-3 | Functional | `name-fold.ts:45` | Invisible format characters and NBSP survive the fold | confirmed (narrowed) | #920 | `wrong-data`. Same root cause and one added step closes both. `name-lookup.ts:74`'s claim verified and **half-corrected**: the GLOB arm hands the stored side to JS, but the needle side is excluded before JS sees it — folding fixes both. Ingress documented by the repository itself (`label-template.ts:752-754`) plus a mechanical route through numeric HTML entities. **Full-width Latin dropped** — it needs `NFKC`, a much larger decision. The fix is not a one-liner: it falsifies `name-lookup`'s printable-ASCII invariant and changes sync unique keys |
| U7-CAND-4 | Functional | `name-fold.ts:45` | Turkish `İ` does not fold to `i` | rejected | — | Standards-correct (Unicode default full case folding), NFC cannot compose it, and the docstring never claims this direction — it records the **opposite** Turkish trade-off as an accepted cost. "Fixing" it needs locale-aware folding, which the module's step 3 explicitly rejects |
| U7-CAND-5 | Mechanical | `utils.ts:31` | Four spacing token families are not declared to tailwind-merge | rejected | — | Line numbers wrong (the file is 42 lines). **Two of the four families are vacuous** — never spelled as spacing utilities anywhere. Complete census: six dynamic candidates, all six fail. The false-contract premise fails; the JSDoc is written entirely about the safe-area family. Two by-products in §11 |
| U7-CAND-6 | Functional | `DashboardNav.tsx:326` | A converted screen builds a screen-reader string by concatenation | confirmed (narrowed) | #953 | `degraded`. A **third** limb the finder missed and the strongest: the count bypasses the seam's locale grouping, so a German user hears "100000" on one tile and "100.000" on the one beside it — and `DashboardNav.test.tsx:150` **pins** the ungrouped form. The `alertsAria` sibling one line away proves the catalog can express the whole sentence. Plural limb belongs to #225; the noun is comment-covered and #682-shaped |
| U7-CAND-7 | Functional | `useFullscreen.ts:26` | `supported` ignores `document.fullscreenEnabled` | rejected | — | The JSDoc measures exactly what it claims. The iframe limb is closed, and **not for the finder's reason** — a same-origin iframe reports `fullscreenEnabled: true` with or without `allow`. The policy limb is a deliberate opt-out. And the proposed fix would not close the stated symptom |
| U7-CAND-8 | Functional | `text-terms.ts:11` vs `autocomplete-filter.ts:42` | Two picker-search folds disagree | rejected | — | Mechanism **disproved**: `autocomplete-filter.ts:42` is `indexOfValue`, not a filter. The real type-ahead is `filterSuggestions`, which folds with `toLowerCase` — identical to `text-terms`. The stated symptom does not exist. The "single home" comment claims the AND-substring model, which holds across eight consumers |
| U8-C1 | Mechanical | `device-id.ts:36-46` | `getDeviceId` throws when `setItem` is refused | confirmed (narrowed) | #914 | `unusable`. **The boot gate does not catch it** — proven by driving `useDatabaseBoot` with a write-throwing `localStorage`: status reached `ready`, diagnosis never called. The finder's Safari-private-mode browser is **refuted** (WebKit bug 157010, fixed 2017). A real shape exists instead: genuine quota exhaustion within ~40 bytes of the cap, reproduced in Firefox and Chromium. This is the **only** unguarded `localStorage.setItem` in `src/`; four siblings guard theirs |
| U8-C2 | Mechanical | `feature-detection.ts:26-32`, `useDatabaseBoot.ts:101` | `hasOpfs()` is a presence check, so the actionable diagnosis is unreachable | confirmed (narrowed) | #949 | `degraded`. **The finder's headline example does not reproduce**: on the hosted deployment blocked site data also blocks the worker, so isolation fails first and the correct `site-data-blocked` screen renders. Reachable only where the origin is isolated **without** the worker — self-hosted nginx and the HA add-on. `feature-detection.ts` still has **no test file** |
| U8-C3 | Functional | `useInstallPrompt.ts:42-57,72-106` | The install event is captured per hook instance | confirmed (narrowed) | #950 | `unusable`. **Phase 6 file.** Second-instance limb demonstrated with the real seam and a control; `SettingsDialog` unmounts when closed, so its hook is always created after the event fired. New finding: `browser-smoke.mjs:4126-4153` opens Settings **first** then dispatches synthetically — the one ordering production cannot produce, so the end-to-end step passes on an affordance that never works. Timing limb **unverifiable** — see §11 |
| U8-C4 | Mechanical | `device-id.ts:24,36-45` | The memoised id can diverge and is stamped into a permanent column | confirmed (narrowed to path b) | #914 | `wrong-data`. **Path (a) rejected** — the tab lock denies the second tab and `BootGate` never renders children, so no consumer mounts. **Path (b) confirmed with the browser the finder could not name**: Firefox with `dom.storage.enabled=false` has `localStorage` undefined while OPFS opens fine. Harm demonstrated end to end — after a reload the attachment renders "Unlinked Local File" for a file linked on this very device, in a column that syncs |
| U8-C5 | Mechanical | `device.ts:43,59` | `COMPACT_LAYOUT_QUERY` claims parity with `md:` and has no drift test | confirmed (narrowed) | #961 | `cosmetic`. Demonstration holds (12,448 tests green with the breakpoint patched). **The claimed harm is rejected and must not be asserted**: `src/` has exactly one genuine `md:` variant, and both master-detail screens swap on a JavaScript ternary, so exactly one branch renders by construction |
| U8-C6 | Prompt | `decoration-motion.ts:7-8` | The docstring names an animation-level scale that no longer exists | confirmed (widened) | #961 | `cosmetic`. **Phase 6 file.** Root cause is the v3 remap the memory note records. **Widened** with two further false claims in the same docstring: it says "two thresholds" where there are three, and claims every JS-driven effect routes through the gate when `BackgroundEffects.tsx:67` reads a threshold directly |
| U9-C1 | Functional | `index.css:96`, `modal.tsx:230-232` | Light-mode dialogs composite `bg-card` over their own scrim | confirmed (narrowed + widened) | #933 | `degraded`. #209's **twelve published figures reproduced exactly**, then the composition measured in a browser: the panel is `#DFDFE0`. **Narrowed** — `muted-foreground` passes at 4.52 on the default dialog and fails only under a background effect or Soft/Sheer. Confirmed for `destructive` (3.60, every Foundry field's validation error) and `primary` (3.89). **Widened** to `Drawer`, and: `setAnimationLevel('headache')` switches Snow on, so the 70% column is reached by default. The token's own comment claims modals stay opaque |
| U9-C2 | Functional | `money.tsx:192` | `opacity-80` takes the currency symbol below AA | confirmed | #934 | `degraded`. Measured live: `#6C8FBC` on `#FEFEFF` = 3.31:1. #209 omits the token because the raw value passes. The comment reasons about 1.4.1 and never 1.4.3. Not redundant — per-record currency overrides ship, so in a supplier-parts or PO table the symbol is the only per-row discriminator. One `text-2xl` instance is exempt |
| U9-C3 | Functional | `index.css:2463-2467`, `RarityBadge.tsx:30-42` | The tier word is 2.13–4.37:1 against its own pill | confirmed (narrowed) | #934 | `cosmetic`. Measured live: Legendary 2.13:1. **A narrowing that cuts the other way** — it is *not* doubly opt-in: `gamifyCards` defaults **true**, so the only gate is the animation level, and choosing that gate also turns Snow on, making the default surface the 70% panel (1.83:1). `data-contrast=high` does **not** rescue it. Capped at cosmetic because the tier is worthless by the feature's own design |
| U9-C4 | Mechanical | `highlight.ts:108` | `behavior: 'smooth'` overrides the reduced-motion catch-all | see U6-2 | #932 | Reported independently by U9; recorded on the U6-2 row |
| U9-C5 | Functional | `ItemRow.tsx:135-136`, `AppNav.tsx:75` | Content hides on a bare `sm:` breakpoint rather than `handset:` | duplicate #546 | — | #546 names these sites **verbatim**, including the same code block, and had already adjudicated and excluded the `AppNav` limb. A comment was posted there with four sites its sweep missed, plus a correction: at exactly 1280 px / 200% the content survives by one pixel, and the unconditional failure is WCAG 1.4.10 at 320 px |
| U10-C1 | Mechanical | `messages.ts:38-55`, `SettingsDialog.tsx:151-156` | Adding a language per the documented recipe leaves it unreachable | duplicate #575 + #227 | — | Facts all true, but the **seam works** — `prefs.locale` is deliberately an open BCP-47 set, so a restored or synced blob can reach a language the picker does not list. Only the picker is narrow, which is #575; the unguarded third catalog is #227(b). Residue posted as a comment on #575: two false docstrings, and deriving the list rather than widening it |
| U10-C2 | Functional | `index.html:2`, `useApplyLanguage.ts:14-32` | `<html lang>` is never updated | confirmed | #945 | `degraded`. Demonstrated with a **green control** — the sibling `useApplyTheme` does write to `documentElement`. WCAG 3.1.1 Level A: ~593 keys of German announced by a document declaring `en-GB`. #229 covers `dir` and explicitly not `lang`. Gets sharply worse if #575 lands |
| U10-C3 | Mechanical | `useApplyLanguage.ts:26` | A failed catalog import is an unhandled rejection | confirmed (narrowed) | #962 | `cosmetic`. Also raised by U4 and U12. **The build settles the open limb**: `de.json` **is** emitted as `.js` and **is** precached, so "every offline German session hits it" is false. The reload limb is real but is the **designed cure** for #279. What survives is a third instance of the class #315 accepted and closed, at a site that issue does not cover |
| U10-C4 | Performance | `i18n.ts:72-83` | `Intl.PluralRules` is rebuilt on every pluralised call | rejected | — | Measured in **Chromium**, not Node. The 45× ratio holds, but a realistic render costs 2–3 ms, ≤7.5% of the mount, and ~2 ms of a 16.7 ms frame. Two of the five named call sites are not row renders. Also established: **CDP 4× throttling over-applies ~6× to `Intl` construction**, so throttled figures here are an instrument artefact |
| U10-C5 | Mechanical | `i18n.test.ts:74-76` | The "malformed locale" test cannot fail | confirmed | #958 | `cosmetic`. The fixture is a **valid** BCP-47 tag resolving to the host default, which is also `DEFAULT_LOCALE`, so the assertion cannot distinguish the two paths even in principle. Deleting the guard leaves 25 tests green |
| U10-C6 | Mechanical | `catalogs.test.ts:2-3,22-43` | The catalog guards are written against `de` by name | duplicate #227(b) | — | Already filed verbatim as limb (b). The demonstration was posted there, since #227 carried none: an `es.json` probe with an orphan key, 2,459 missing keys and a renamed placeholder shipped 56/56 green, with a control proving the `de` guards do work |
| U10-C7 | Mechanical | `db-error-message.test.ts:132-149` | The "resolves every emittable key" fixture is hand-typed | confirmed | #958 | `cosmetic`. Adding an entry under any of five unlisted codes leaves 44 tests green. `permission-labels.test.ts` is a genuine in-repo model 40 lines away, and its docstring names this exact failure (#429) |
| U10-C8 | Mechanical | `messages.ts:30-33` | `MessageKey` admits `import.problem`, a key no catalog defines | confirmed (narrowed) | #959 | `cosmetic`. Enumeration re-run: 91 bases, exactly one lacking `.other`. Type-checks, with a control proving the harness rejects a genuinely absent key — and **TS2820 offers the broken key as its quick-fix**. Second limb found: under a locale with a CLDR `zero` category it would render the reason-code sentence as a plural variant |
| U10-C9 | Mechanical | `i18n.ts:112-114,132-136` | Catalog lookup walks `Object.prototype` | rejected | — | Four independent sweeps: all twelve `as MessageKey` casts build prefixed dotted keys, every template-literal `t()` is prefixed, every identifier argument is a catalog literal, and the untyped seam is not exported. The catalog route is shut too — `en.json` has zero single-segment keys. Folded into #959 as a hardening bullet |
| U10-C10 | Functional | `SettingsDialog.tsx:151-156` | The language picker's own labels are untranslated literals | duplicate #232 | — | #232 names the screen **and** this control, counting label string-literal props. A comment was posted there with the important qualification: the fix is to render **endonyms**, not to add four catalog keys — a language's own name is never translated |
| U10-C11 | Mechanical | `receipts.test.ts:135-142` | The parity test skips the arm that has no tracking mode | confirmed | #958 | `cosmetic`. 429 tests green with the two Englishes visibly disagreeing. The comment says "per arm"; the test iterates modes, and one arm has no mode. The surrounding docstring explicitly advertises the out-of-band case, which makes it a documented promise |
| U11-C1 | Mechanical | `HotkeySettings.tsx:283,316` | The recorder omits the Mac flag, so a Command chord is stored dead | confirmed (widened) | #917 | `unusable`, Mac-only. Not a mismatch but a **permanently dead string**: `chordFromEvent` sets `meta = metaKey && !commandIsPrimary`, so on a Mac no key press can produce any `Meta+` binding. Line `:316` has it too, so a sequence's second chord is dead the same way. `hotkeys.ts:20-24` and the wiki both document the intent |
| U11-C2 | Functional | `useGlobalHotkeys.ts:166-167`, `hotkeys.ts:849-850` | A global hotkey fires while a Foundry `Menu` owns the keyboard | confirmed (widened) | #916 | `unusable`. **Widened past the Menu case to a much sharper limb**, driven in a browser: `nav.dashboard` defaults to **F2**, and the location tree's F2 inline rename is its only affordance — so with shipped bindings a keyboard user cannot rename a location. Control: clearing the binding makes the rename box appear. Menu limb also confirmed (`degraded`), with focus dropped to `<body>`. **The four other roles the finder listed are all rejected as unreachable.** The IME gap is already open #582 |
| U11-C3 | Mechanical | `useModulesStore.ts:88-93` | A corrupt persisted `intent` white-screens the app | see U4-C3 | #918 | Reported independently by U11; recorded on the U4-C3 row |
| U11-C4 | Functional | `hotkeys.ts:689-711`, `ShortcutsOverlay.tsx:73-87` | A bare prefix binding kills its sequences, unflagged and unreflected | confirmed (narrowed) | #951 | `degraded`. All four halves demonstrated, including the live dispatcher confirming the sequences are dead. **The overlay limb is the defect** — its docstring says "or it is misinformation" and it already implements four cannot-fire filters, so this is a missed fifth. The Settings-conflict limb is weaker and is explicitly not filed alone. Six `G` sequences in the default preset, **seventeen** in the vim preset |
| U11-C5 | Functional | `hotkeys.ts:545-579`, `useGlobalHotkeys.ts:185` | `rejectBinding` accepts `Tab`, `Enter`, `Space` and the arrows | confirmed (narrowed) | #951 | `degraded`. The recorder **does** produce it, by an accidental route: activating "Change" with Enter and then tabbing away binds `Tab`, and the app announces it. **Recovery is not mouse-only** — the modal stand-aside means `Ctrl+,` and `?` still work. `EXTEND_EXEMPT_KEYS` one function away is the evidence of oversight |
| U11-C6 | Mechanical | `useHotkeyScope.ts:242-247` | `register` appends rather than replacing in place | confirmed (narrowed) | #962 | `cosmetic`. Reproduced, plus a narrowing that matters to the fixer: splicing in place would **not** fix the hook, because the effect cleanup calls `unregister` first, so the filter is unreachable through the only consumer. The comment is wrong twice over. The covering test cannot fail — mutation-proved at 155 passed |
| U11-C7 | Functional | `useGlobalHotkeys.ts:166`, `ShortcutsOverlay.tsx:90` | `?` cannot close the cheat sheet it opened | rejected | — | Mechanical fact reproduces, but nothing user-facing claims it closes: the catalog says "Show keyboard shortcuts" and the wiki says "see" and "Show". Escape and the close button both dismiss it. An internal name/behaviour mismatch with no consequence — §1's style-preference exclusion |
| U12-C1 | Mechanical | `db/errors.ts:175-194` | Extended result codes outside the constraint family degrade to `SQLITE_ERROR` | duplicate #751 | — | Already filed as register row P1-5. Premise **re-confirmed** against the real engine, but the consequences are not reachable: no shipped VFS implements shared memory (no `xShm*` anywhere), so WAL is impossible, cross-tab contention returns **primary** `SQLITE_BUSY`, and the reachable `IOERR` family has no case at all so the collapse changes nothing |
| U12-C2 | Mechanical | `error-copy-idiom.test.ts:42` | The guard matches only the ternary, so five bypasses pass | confirmed (narrowed + widened) | #946 | `degraded`. **The finder's five shapes have zero live offenders** — that limb is rejected. But a hole they did not look for has one: the regex hard-codes the class name `Error`, so `e instanceof DbError ? e.message` at `StockBreakdown.tsx:136` slips through — and `DbError` is the worst subclass to narrow on. Reachable via `assertWritable`'s `WRITE_SUSPENDED`, `WORKER_TIMEOUT` and `MULTI_TAB_LOCKED` |
| U12-C3 | Functional | `CommandPalette.tsx:503-516`, `mutations.ts:411-431` | A third live mixed-hook double-report | confirmed | #962 | `cosmetic`. Real hook, real toast provider, three tests plus a control. The finder's control verified: `useCheckoutItem` has no `onError`, so it does **not** widen to check-out. A sweep of all fifteen `mutateAsync` callers found the palette is the only unmanaged one. Drifted because #308 added the inline catch before the #307/#389 seam wired `onError` app-wide, and the palette's own test mocks the hook |
| U12-C4 | Functional | `db-error-message.ts:40-54`, `assert-permission.ts:25` | `PERMISSION_DENIED` has no catalog sentence | confirmed | #947 | `degraded`. Exactly one wording, one producer, reached from every gated repository mutation plus ten bulk sites. **Strengthened**: `PermissionGuard.tsx:106-108` already renders a translated, humanised permission ("Stock · Change"), so the seam is inconsistent with itself and the machinery exists. Not #224 (toasts and live regions) and not #243 (boot and error screens) |
| U12-C5 | Mechanical | `useLabStore.ts:72-79` | The merge reconciles one field of four | see U4-C4 | #918 | Reported independently by U12; recorded on the U4-C4 row. This unit added the escape analysis: `AppErrorBoundary` **does** catch it, capping it at `unusable`, but Try again, "Reinstall app files" and the Danger Zone are all no-ops, so only the purge works |
| U12-C6 | Mechanical | `main.tsx:46,52` | The lab clock is installed before the skew | see U4-C6 | #927 | Reported independently by U12; recorded on the U4-C6 row |
| U12-C7 | Functional | `lab-flags.ts:11-14`, `lww-tie-override.ts:32` | `sync-lww-tie` overwrites a newer local edit while three docblocks say it cannot | confirmed (narrowed) | #928 | `wrong-data`. Demonstrated with a control and a **survival** case: after `resetLab()` and `localStorage.clear()` the row still holds the remote value, so the header's contract is false rather than imprecise. **Narrowed** — a `SyncConflict` record *is* raised carrying the losing row, so there is a recovery path. Two caveats keep it real: it fires only under `localEditedSinceSync`, and the conflict store is itself `localStorage`, so the offered reset deletes the only copy. **Not a privilege finding** — `/lab` is already permission-gated |
| U12-C8 | Functional | `db-error-message.ts:56-72` | `FIELD_KEYS` claims seven single-column UNIQUE indexes | confirmed (narrowed) | #960 | `cosmetic`. Re-enumerated: 12 UNIQUE indexes, 11 single-column, 7 mapped — the comment is false. **The copy limb is rejected**: all four unmapped indexes are unreachable in normal use, and adding field labels would produce *worse* copy ("That default location is already in use"). The finding is the drifted comment |
| U12-C9 | Functional | `db-error-message.ts:80-102` | `isRawSqliteMessage`'s markers match user-supplied text | confirmed | #960 | `cosmetic`. Reproduced, and the anchoring premise checks out against **real SQLite** for all seven markers. Exposure **narrowed**: "~40 repositories" is overstated — nearly all interpolate an opaque id; under a constraint code only `assembly.ts:114` interpolates user free text |
| U12-C10 | Mechanical | `useDismissedAlertsStore.ts:80,118` | `nowMs()` stamps a persisted record against the clock module's rule | confirmed (narrowed hard) | #928 | `cosmetic`, and folded into the lab-flag issue's sibling work. "Outlives it permanently" is **false** — survival is exactly the override distance plus 30 days (measured: 517). `pruneDismissals` has a second exit the finder missed. Almost nothing is user-visible. The real finding is the **missing guard**: the invariant is prose in four files, honoured across all 62 call sites, and held by no test |
| U12-C11 | Mechanical | `AboutScreen.tsx:19-27`, `DashboardVersion.tsx:13-18` | A "mirrors X" comment on a duplicated formatter with no test | confirmed | #962 | `cosmetic`. No test exists for either surface; drift demonstrated and reverted while 266 tests stayed green. **Limb 2 is new and belongs on #228**: both use `Intl.DateTimeFormat(undefined)` at module scope, so they use the browser locale and can never react to the preference — a third and fourth site that issue's sweep missed, and its "everything else formats correctly" claim is false. **Limb 3 rejected** — `Intl` with no `timeZone` preserves the calendar day by construction |
| U12 negatives | — | `diagnostics.ts`, `Starfield.tsx`, achievements | Three finder verdicts of "checked and clean" | agreed, with three corrections | — | Diagnostics carries no personal data (21 keys re-enumerated; the redaction split is deliberate and test-pinned). `Starfield` is CSS rather than canvas — but renders **56** spans, not 52. Achievements are idempotent at two layers, both load-bearing — but the watcher's monotonic-ref comment **overstates** what the ref does: it only decides the `celebrate` flag, and clearing the record then remounting does re-award |
| U12 seed data | — | `seed-data.ts` | The seeded sample data is synthetic and removable | verified clean | — | All 104 lines read. Every generator input is a fixed literal list, the maker names are invented, `SEED_PREFIX` is applied to both name and MPN, the PRNG is deterministic, the count is clamped to 10,000, and `createMany` is all-or-nothing. The removal gap is real but is already #145 (no bulk delete) and #130 (no select-all). Residue: the confirmation copy promises the items are "easy to spot and delete afterwards" — spot yes, delete one at a time |
| U12 lab seed error | Functional | `LabScreen.tsx:375-380` | `catch {}` discards a nameable failure | confirmed (narrowed) | #954 | `degraded`. The comment's premise is **false**: it names the environmental class and calls it unactionable, but the seam has an authored instruction for each of `WRITE_SUSPENDED`, `SQLITE_FULL` and `MULTI_TAB_LOCKED`. Also reachable via a permission mismatch — the section is gated on `storage:write` while `createMany` asserts `items:write`. Closed precedent #309 and #311 |
| U12 diagnostics timer | Mechanical | `DiagnosticsCard.tsx:76` | An uncleared timer cuts the "Copied" state short | confirmed (narrowed) | #963 | `cosmetic`. Early-reset limb confirmed with a control. **Unmount limb refuted** — React 19 removed the warning, and unmounting inside the window produced zero console output |

### Phase 6 — Foundry primitives and shared components

Pinned SHA: `7ae84bd30209e0467a8f716a747b7eee1029b416`

Eight finder units (U1 modal, drawer, rail modal, focus trap and dialog behaviour; U2 menu,
tooltip, popover, toast and banner; U3 select, autocomplete, radio and segmented groups and
pagination; U4 input, textarea, number, money and colour and their bounds and expression helpers;
U5 pickers, markdown, region canvas and reorder list; U6 motion, reveal, count-up, tilt, view
transitions and the hooks; U7 background engines; U8 nav, skip link, icons and brand). Thirteen
verifier passes. Six verifiers were killed mid-run by a session limit and were restarted; five of
those had left scratch tests behind, which were re-run and read rather than re-derived, and one had
left a mutation and an instrumentation patch in the tree, both reverted before landing.

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Functional | `modal.tsx:197-201` | The busy focus-keeper steals focus from the portaled unsaved-changes prompt | confirmed (latent) | #982 | `degraded`. Demonstrated: focus jumps from the prompt's Keep-editing button to the parent dialog container. Not reachable today — every dirty-and-busy dialog raises `busy` from a button press, and those buttons sit under the prompt's own backdrop. The dialog's own `onDiscard` re-checks `blocked` for exactly this window, so the codebase treats the state as supported and only the focus effect mishandles it |
| U1-2 | Mechanical + Performance | `modal.tsx:230`, `drawer.tsx:70` | The scrim hard-codes `bg-black/60 backdrop-blur-sm`, bypassing the `--backdrop-surface` token | confirmed (widened) | #977 | `degraded`. The token is `none` under `(pointer: coarse)` by #419, whose own comment gives the per-frame readback reason; `Surface` routes through it, the scrims do not. `git log -S` shows no deliberate exemption — #419 scoped itself to `Surface`. **Widened**: seven more raw `backdrop-blur-*` sites (menu, menu-sub, tooltip, toast, banner, OfflineIndicator). High-contrast limb **narrowed** — the CSS's own rationale there is invisible work, which does not transfer to a scrim. Not #933, which is the panel's contrast over this scrim |
| U1-3 | Functional | `use-dialog-behaviour.ts:71-76` | Escape has no `isComposing` guard | duplicate #582 | — | #582's body names this file and line range explicitly, and states that `isComposing` appears nowhere in `src/` — still true at the pin |
| U1-4 | Mechanical | `rail-modal.tsx:136,143` | `tabs[0]!` twice crashes a rail dialog given an empty tab list | confirmed (latent) | #982 | `degraded` if reached. Throws in the `useState` initialiser before the second site is reached. Two of the four call sites (Edit location, Settings) build their tab list by filtering on capabilities and permissions, so the empty case is one gate change away rather than impossible |
| U1-5 | Prompt | `rail-modal.tsx:148-154` | The `visitedRef` purity comment overclaims | rejected | — | The sentence claims idempotence of **a re-run of this render** — same inputs — which is true. The finder's scenario is a different render with a different `active.id`. Also unreachable: `grep` for `startTransition`/`useDeferredValue` across `src/` returns nothing, so no discardable render can carry a divergent id |
| U1-6 | Functional | `focus-trap.ts:19-21`, `use-dialog-behaviour.ts:89-94` | A native radio group inside a dialog takes one Tab stop per radio | confirmed (**live**) | #969 | `degraded`. Live in five dialogs (LookupMatch, FinaliseAssembly, DeduplicateItems, CategoryManager ×2, Backup). Unchecked native radios carry no `tabindex="-1"` attribute — the browser's grouping is not attribute-visible — so the trap's index walk replaces it. `SegmentedRadioGroup` is immune because it manages a roving index; that is what makes the gap easy to miss, and the `Radio` primitive's own docstring promises "a single arrow-key-navigable tab stop" |
| U1-7 | Functional | `modal.tsx:223-248` | The `description` prop is never wired via `aria-describedby` | duplicate #217 | — | #217 is exactly this |
| U1-8 | Mechanical | `interstitial.tsx:48-52` | `key={line}` collides on duplicate paragraphs | rejected | — | Neither call site can pass duplicates (a module description beside a fixed sentence; two distinct catalog keys), and both interstitials are static and never update after mount. Below §1's consequence bar |
| U2-1 | Functional | `menu-sub.tsx:213,215-218` | A real-mouse click closes the flyout hover has just opened | confirmed | #970 | `degraded`. `onPointerEnter` opens, then `onClick` sees `open` and toggles shut. The shipped test passes only because it fires a bare click with no hover, which no mouse can do. Live on the inventory More menu's three submenu rows. A second click reopens it, and focus is not lost |
| U2-2 | Functional | `menu-sub.tsx:85-92,153-167` | ArrowRight/Enter cannot enter an already-hover-open flyout | confirmed (limb 1); limb 2 rejected | #970 | `degraded`, low. `setOpen(true)` on an open flyout re-runs no effect, so `focusFirstRef` is never consumed. **Limb 2 disproved**: `openSelf` always overwrites the ref before setting open, so a stale `true` cannot survive to a later hover-open |
| U2-3 | Functional | `menu-sub.tsx:94-97,219-243` | A hover-away close strands keyboard focus on `<body>` | confirmed | #970 | `degraded`. The 140 ms timer calls bare `setOpen(false)` with no containment check, unlike `closeSelf`. No `focusin` rescuer exists. Escape does recover, because the sub's unmount cleanup clears the root's open-sub ref. Same class as #897 |
| U2-4 | Functional | `tooltip.tsx:353-369` | Escape closes the containing dialog as well as the tooltip | confirmed | #971 | `degraded`. Both listeners are bubble-phase on `document`, and a tooltip is not on the modal stack, so the dialog believes itself topmost. `Menu` solves the identical layering with capture + `stopPropagation`. Also reaches Drawer and the scanner overlays. Not #515 |
| U2-5 | Functional | `tooltip.tsx:27-28,501,521-526` | Markdown links in the bubble are keyboard-unreachable | confirmed (residue of #515) | #971 | `degraded`. #515 is confined to the `triggerTabIndex={-1}` wrapper idiom and says the default case "**is** keyboard-reachable"; this is what happens where the bubble does open. The only keep-open is a mouse `pointerenter`; `onBlur` arms a 120 ms close and no focus path cancels it |
| U2-6 | Mechanical | `tooltip.test.tsx:38-51` | The hover-cancel test waits 400 ms against a 1000 ms delay, so it cannot fail | confirmed | #984 | `degraded`. Proved by mutation: with `cancelOpen` removed the test stays green. Two neighbouring tests already wait 1200 ms |
| U2-7 | Functional + Prompt | `button.tsx:101-102,162` | `menuLabel` is documented required, typed optional, and falls back to `aria-label=""` | confirmed (latent) | #983 | `cosmetic`. Both split-button call sites pass it |
| U2-8 | Functional | `button.tsx:147,161-171` | `disabled` disables only the primary half; the chevron still runs actions | confirmed (latent) | #983 | `cosmetic` today. No call site passes `disabled` to a split button. The sharpest of the three latent Button traps — a control that reads disabled would still act |
| U2-9 | Mechanical | `button.tsx:139-144` | `asChild` discards the child's own ref under React 19 | confirmed (latent) | #983 | `cosmetic`. `ref` is placed after `...child.props`; the converse (the Button's own forwarded ref) was verified to attach. No `asChild` child carries a ref today |
| U2-10 | Functional | `toast.tsx:177-184` | Nested live semantics: an `aria-live` viewport wrapping per-toast `role="status"` | unverifiable | — | Structure confirmed and no justifying decision exists (`git log -S`, no comment, no test). Needs a real screen reader to count announcements. Not #219 (which names three other sites), #224 or #543. §11 |
| U2-11 | Prompt | `kbd.tsx:48-49` | The comment claims a reading assistive tech cannot produce | confirmed | #990 | `cosmetic`. Accessible text computes to "G R"; the only element holding "then" is `aria-hidden`. The justification is circular — the separator is hidden *because* of a reading the hiding prevents |
| U2-12 | Functional + Prompt | `menu.tsx:19` vs `:207` | The trigger's `aria-label` is unconditional | duplicate #214 | — | #214 is exactly this |
| U2-13 | Prompt | `live-region.tsx:16-21` | The docstring omits the identical-text re-announcement trap | rejected | — | Its one affirmative claim (an always-mounted region) is true; it never claims any content change announces. An omitted caveat is not a false comment |
| U3-1 | Mechanical | `select.tsx:402,439` | The filter input's keydown bubbles to a trigger div carrying the same handler, so Enter fires `onChange` twice | confirmed | #990 | `cosmetic`. Needs ≥12 options to filter. Harm sweep found **no** non-idempotent live handler at that size: the sentinel "＋ New…" rows set the same state twice in one event (one dialog, not two), and every mutating `onChange` sits on a short enum list. The double-dialog fear does not materialise |
| U3-2 | Mechanical | `currency-select.test.tsx:35-40` | The "reports the bare ISO code" test cannot fail | confirmed | #984 | `cosmetic`. Proved by mutation (report the label instead — the file stays 9/9 green), because the picker appends any off-list value verbatim, so the trigger text is identical either way. **Narrowed**: seven select-level tests do catch the same regression; the hole is confined to the currency wrapper's own claim |
| U3-3 | Functional | `autocomplete.tsx:251-253,285` | `activeIndex` is never clamped when prefiltered suggestions shrink | confirmed (live) | #973 | `degraded`. `aria-activedescendant` points at a non-rendered id, and Enter is swallowed committing nothing (in creatable mode the typed text is skipped too). Live in all three async pickers (supplier search, project, item). `Select` clamps on read and says why |
| U3-4 | Functional | `pagination.tsx:235-243` | Typing "100" commits page size 10 mid-keystroke | confirmed (narrowed) | #979 | `degraded`. Eager commit-on-preset-match is the recorded design; the defect is the **prefix collision**, which is exactly what the component's own docstring says the draft exists to prevent |
| U3-5 | Functional | `pagination.tsx:244` | Enter on a free-typed page size commits nothing | rejected | — | Blur-commit is the recorded design and Autocomplete's Enter fall-through is its documented contract. The form-submit half is moot: no `<form>` encloses any of the ten `Pagination` call sites |
| U3-6 | Prompt | `select-options.ts:41` vs `use-anchored-popover.ts:35` | A `240` parity claim with no drift test | rejected | — | The claim is **true today**, so it fails the Prompt bar, and divergence has no consequence: the constant is a first-paint viewport guess absorbed by six rows of overscan either side (≈448 px rendered against a 240 px assumption) and a floor that fires only on a literal 0 measurement. Noted for a later hardening pass: the value is restated a third time in prose at `select-options.ts:24` |
| U3-7 | Performance | `autocomplete.tsx:130,352,368` | A non-prefiltered browse renders every suggestion unwindowed, and each `mouseenter` re-renders the control | confirmed | #986 | `degraded`. Measured: 300 rows rendered against a declared cap of 10; ≥1 commit per row entered; `aria-activedescendant` follows the pointer. **Scale established**: every other browse list is bounded (suppliers/tags 100, regions 20/page, currencies 21) — manufacturer and unit-of-measure are capped at **no layer**, since `distinctValues` has no `LIMIT`, `mergeSuggestions` adds none, and the browse path skips `maxOptions`. Distinct from #808, which is the per-keystroke search cost |
| U3-8 | Functional | `segmented-radio-group.tsx:50-53,86,100` | An unrecognised value renders option 0 as `aria-checked` with selected styling | rejected (documented) | — | The live ingress is a blank BOOLEAN custom field via `YesNoToggle`, and `TypedFieldControl.tsx:330-337` documents the behaviour deliberately: *"falls back to showing 'No' selected without committing it … an untouched default still submits as unset"*. §4.1 decision. §11 records the residue — the UI reads "No" while the field saves as unset |
| U3-9 | Functional | `select.tsx:143,173,350` | Duplicate option values collide React keys and mark two rows selected | confirmed (**live**) | #987 | `degraded`. Reachable by typing `Red, Red, Green` into a SELECT custom field's Choices: nothing de-duplicates in the editor (`parseChoices`), the validator or the schema. **Narrowed**: option ids stay unique (index-derived) so nothing dangles, the trigger shows the right label, and either row commits the same string — the harm is the listbox announcing two selected options, plus a duplicate-key error per open |
| U3-10 | Functional | `entity-picker.ts:41,139` | A padded label is suggested but resolves to `null` | rejected (no ingress) | — | Mechanism confirmed, including that it **clears** an existing selection. No live ingress: item create trims (`item/create.ts:79`), project create and update trim (`project/core.ts:162,189`), and `itemDisplayName` adds no padding. A crafted snapshot or hand-edited database remains the only route |
| U3-11 | Functional + Prompt | `select.tsx:61-76,90-91` | No `onBlur` seam, so RHF touched/blur validation never fires | rejected (latent) | — | Not #552 or #553. No live consumer: no `useForm` in `src/` sets a `mode`, and `touchedFields`/`isTouched` appear nowhere. The docstring is accurate about the binding it describes and claims nothing about blur |
| U3-12 | Prompt | `autocomplete.tsx:215` | `commitTyped`'s docstring describes its caller | confirmed | #990 | `cosmetic`. It says "accept whatever is highlighted, else the typed text"; the function never reads `activeIndex` or `matches`, and its single call site is the *else* branch of the behaviour it claims to implement |
| U4-1 | Functional | `evaluate-expression.ts:39-48`, `number-input.tsx:222-227` | A signed exponent is classified as a calculation, suppressing the range report | confirmed | #965 | `degraded`. The bare-number pattern has no `e` branch and the exponent's `-` matches `OPERATOR_CHARS`, so `1e-7` reports nothing while `0.0000001` reports out of range and unsigned `2e3` behaves. The control does this to **its own output** — `formatCalcResult` emits `1e-7` and `1e+21`. Downstream of #676, whose three jobs it defeats |
| U4-2 | Functional | `number-input.tsx:196-213`, `numeric-bounds.ts:132-134` | An arrow key replaces a non-blank unparseable draft with the bottom of the range | confirmed | #964 | **`data-loss`**, the phase's most serious finding. `'250,00'`→`"1"`, `'40+'`→`"1"`, `'1,250.75'`→`"10"`. `stepFrom`'s `null` branch is documented and tested for a **blank** box only; the comma form is a figure the seam deliberately preserves. One unmodified keystroke, no undo |
| U4-3 | Functional | `number-input.tsx:148,306` | A repeated calculation result is never re-announced | confirmed | #967 | `degraded`. The catalog string is `"Calculated {result}"` — result only — so an identical string bails out of the state write and the live region never mutates. One mutation record for two commits; two for distinct results. `modal.tsx:156-159` counts refusals for exactly this reason |
| U4-4 | Mechanical | `number-input.tsx:101,123-125` | An external write leaves the reported state stale | confirmed | #967 | `degraded`. After a form reset the box reads 5 while `aria-valuenow` is 99 and `aria-invalid` still true; an imperative 900 into a `max={9}` field reports **valid**. Self-heals on focus. `text-limit.ts:149-156` solves the same case with a dependency-less layout effect and names it in a comment |
| U4-5 | Functional | `use-search-escape.ts:25-33` | Escape during an IME composition clears the whole search filter | duplicate #582 | — | #582 names this file and line range verbatim and treats key handlers as a class. `isComposing` still appears nowhere in `src/` |
| U4-6 | Functional | `number-input.tsx:152-174`, `numeric-bounds.ts:36` | Full-width digits are stripped, and the element is rewritten mid-composition | confirmed (ii); mechanism-only (i) | #968 | `degraded`→`unusable` for a CJK user. `sanitiseNumericText('１２３') === ''`. The mid-composition rewrite was shown to fire on an event carrying `isComposing`, but what a real input method then does needs a real IME — recorded in §11. The full-width half is outside #582 entirely |
| U4-7 | Functional | `input.tsx:25-31` | `maxLength` never reaches the number branch, and no length is bounded | confirmed | #966 | `degraded`. No live call site passes it, so the drop itself is latent — but the recorded rationale (#346's exemption: "it bounds a value rather than a length") fails twice: an unbounded field bounds nothing, and a `min=1 max=9` field accepts 50,000 digits and reports itself **valid**, because the value parses to `Infinity` and thus to `null`. Parsing is linear (2.84 ms over 64k chars), so this is correctness, not denial of service |
| U4-8 | Functional | `number-input.tsx:233-236` vs `input.tsx:94` | The caller's `className` lands on the wrapper, not the input | confirmed (narrowed to latent) | — | Not filed. The mechanism holds, but the wrapper tailwind-merges the utilities live call sites actually pass (`h-8 w-20 text-xs`) and the inner input relays them (`h-full w-full text-[length:inherit]`). No live call site passes a non-relayed utility (padding, background, border) |
| U4-9 | Mechanical | `textarea.tsx:141-144,193-195` | A value change while hidden clears the fitted height and never restores it | confirmed | #991 | `cosmetic`. `fitToContent` clears the height before the unmeasurable check and returns. **Live path established**: `ItemDetailsEditor`'s two `autoGrow` boxes sit in `ItemDetailDialog`'s `keepPanelsMounted` rail, and the re-seed effect's deps include `item.description`/`item.notes` — so an external write (sync merge, bridge peer write) while the user is on another tab loses the fit. Self-heals on the next keystroke |
| U4-10 | Mechanical | `text-limit.ts:165-168` | A limit removed mid-life leaves the last report standing | confirmed | #978 | `degraded`. No `null` withdrawal is sent; withdrawal happens only at unmount |
| U4-11 | Functional + Prompt | `text-limit.ts:126-132` | The docstring credits the re-read with catching a programmatic write, which it does not | confirmed | #978 | `degraded`. Demonstrated with a control: a form reset reports `aria-invalid` correctly, while a programmatic write of the same over-limit value reports nothing at all — the re-read runs only when the component re-renders. Lands the user in #843's experience |
| U4-negatives | — | `colour-input.tsx` | An adversarial pass over the one in-scope control no finder flagged | verified clean | — | Invalid pastes (`not-a-colour`, `#12`, `#12345`) keep the raw text, set `aria-invalid`, and never hand the native picker a non-`#rrggbb` value. 8-digit alpha is kept, opaque alpha folds to 6 digits, 3- and 4-digit shorthand expand |
| U5-1 | Functional | `GlyphPicker.tsx:118-123`, `EmojiPicker.tsx:142-147` | Enter commits a selection the filter has hidden | confirmed (narrowed) | #985 | `cosmetic`. The footer names the pending value at the moment of commit, so the Use-button limb is defensible; what survives is Enter with **no active option** (`activeIndex` is −1), which gives a screen-reader user nothing to act on |
| U5-2 | Prompt + Functional | `EmojiPicker.tsx:27,73` | An out-of-catalogue `initialEmoji` is not "ignored" as documented | confirmed | #985 | `cosmetic`. Seeded unchecked; Use is enabled and commits it, and a junk string renders twice in the footer. `GlyphPicker` guards the same seam and has a test pinning it. Chiefly a false docstring — the value round-trips unchanged |
| U5-3 | Performance | `EmojiPicker.tsx:309` | The index-suffixed key remounts every persisting cell per keystroke | confirmed (mechanism only) | #985 | `cosmetic`. DOM identity is lost across one keystroke where `GlyphPicker`'s `key={name}` keeps it; 381 cells render unwindowed, 322 after one letter. **No frame timing measured** — filed on the reconciliation, not a claimed cost. All 381 chars are distinct, so the suffix buys nothing |
| U5-4 | Mechanical | `GlyphPickerButton.tsx:9`, `EmojiPickerButton.tsx:10` | A failed lazy chunk escapes `Suspense` to the app error boundary, unretryably | confirmed | #972 | `unusable` when triggered. Six call sites, none with an intermediate boundary; React caches the rejection, so a fresh tree fails instantly and the pickers stay dead until a reload, discarding the open form. `Glyph.tsx` was engineered for the **same chunk's** failure under #315 |
| U5-5 | Functional | `markdown.tsx:241-244` | An escaped `\|` does not survive table-cell splitting | confirmed | #974 | `degraded`. Rows split on every `|` before escapes are read: the cell breaks at the backslash, a stray `\` renders, and the row's surplus cell is **dropped**, not shifted. Live surface — user-authored location descriptions |
| U5-6 | Functional | `markdown.tsx:97-100` | Prose containing `\|` after a table is swallowed as a row | rejected | — | Faithful GFM: the spec has a table consume every following non-blank line, even without pipes. This renderer is stricter (it requires a `|`), and the author remedy is the blank line GFM already requires |
| U5-7 | Functional | `markdown.tsx:303-304` | Intraword `_` italicises inside identifiers | confirmed | #974 | `cosmetic`. `use_persisted_size` renders with an `<em>`; GFM forbids intraword `_`. Live surfaces: location descriptions and field help |
| U5-8 | Prompt | `markdown.tsx:6` | The "trusted, app-authored" threat model is stale | confirmed | #974 | `cosmetic`. User-authored location descriptions flow through it. **The injection re-review found no hole**: `safeHref` is an allowlist, there is no `dangerouslySetInnerHTML`, image syntax is unimplemented, and `javascript:`, mixed-case, `data:`, `vbscript:`, whitespace-obfuscated, protocol-relative and entity-encoded probes all produced zero anchors |
| U5-9 | Functional | `markdown.tsx:388` | `target="_blank"` is unconditional, including on in-app paths the sanitiser permits | confirmed (latent) | #974 | `cosmetic`. The code half-knows — it marks external links with an arrow — but new-tabs everything. Zero authored in-app markdown links exist today, and new-tab is defensible for tooltip content |
| U5-10 | Mechanical | `region-canvas.tsx:228` | `DrawState.image` is seeded once and no event refreshes it | confirmed (latent) | #975 | `degraded` if reached. A rerender at a new aspect produced a committed radius 4× out. No live path: the two read-only canvases never gesture, and the editable one is conditionally mounted per photo |
| U5-11 | Functional | `region-canvas.tsx:399-405` | Handle grab-testing is anisotropic and order-wins rather than nearest-wins | confirmed (**stronger than claimed**) | #975 | `degraded`. Distances are compared in normalised space, so the grab zone is an ellipse (9.6 px × 7.2 px on a 400×300 photo). **Threshold corrected upward**: adjacent handles sit half a side apart, so mis-resolution begins below ~9.6% of the photo per side — not ~5% — which makes it common for shelf bins. The literal "se press grabs nw" framing is near-unreachable; the live defect is corner versus adjacent edge |
| U5-12 | Functional | `reorder-list.tsx:158` | The drag-handle icon promises a drag that does not exist | confirmed (re-based) | #992 | `cosmetic`. On the icon alone this is an affordance preference and would be rejected — it confirms because `docs/wiki/Tags-Attachments-and-Related-Items.md:69` **tells users to drag**. The same sentence also names a settings path that does not exist, which other wiki pages get right |
| U5-13 | Functional | `reorder-list.tsx:112-124` | An asynchronously applied move loses its announcement and focus re-home | confirmed (latent) | — | Not filed. Demonstrated (announcement empty, focus to `<body>` at an end row), but the sole consumer applies moves synchronously. The bail it turns on is deliberate and test-pinned for *declined* moves, so a fix must distinguish declined from not-yet-applied |
| U6-1 | Mechanical + Functional | `usePointerTilt.ts:137-141` | A gate flip mid-hover leaves a queued frame and frozen inline tilt vars | confirmed (3 limbs) | #980 | `cosmetic`. `cancelFrame` is a `[]`-dep callback, so its cleanup runs only at unmount: the queued frame flushes after the gate closes, and the handlers — including the only writer of the rest state — are gone. Limb 3 reframed as the amplifier: no `data-anim-level` rule touches the hover transform, only the `::after` glare, so the freeze re-applies at every reduced level including the ones a motion-sensitive user picks |
| U6-2 | Functional | `reveal.tsx:59-67`, `useRevealOnScroll.ts:94`, `animated-number.tsx:72-83` | Raising the level replays one-shot entrances on settled content | confirmed | #980 | `cosmetic`. `revealed` is seeded `!canReveal` once, so a later flip combines a pre-seeded `revealed` with a newly-true `armed`. `AnimatedNumber` pops a figure that never changed, contradicting its own docstring. The reverse flip is safe for revealed content; a not-yet-intersected element re-hides but self-heals on the observer's first callback |
| U6-3 | Functional | `usePwaUpdate.ts:237-260` | The update lab flags are inert when flipped mid-session | confirmed | #981 | `degraded`, `/lab` only. `registeredRef` returns early, so the pretend source — the sole caller of `onNeedRefresh` — is never registered; flipping off strands a prompt wired to a no-op updater. The flag's own copy promises the prompt "without deploying anything". A reload does work, and nothing says so |
| U6-4 | Prompt | `usePointerTilt.ts:10-12`, `pointer-tilt.ts:101-103`, `success-burst.tsx:27-29` | Three docstrings name the motion gate where the code calls the flourish gate | confirmed | #990 | `cosmetic`. Substantively wrong, not nominal: flourish fires one tier earlier (Balanced+) and projects no `data-reduce-effects`, so all three tell a maintainer that tilt and the burst run at the shipped default when they do not. The repo's own test file says the opposite. #961 covers `decoration-motion.ts` only |
| U6-5 | Prompt | `reveal.tsx:10`, `animated-number.tsx:21-22`, `view-transition.ts` ×5, `bar.tsx:22` | Stale "F9 Reduce effects" copy | rejected | — | Two checks sank it: **F9 is not a hotkey** — it is the visual-flair feature number, used the same way across a dozen files — and the binary switch's successor exists, with `data-reduce-effects` still projected at exactly the tier these files describe. Stale name for a live, behaviourally identical threshold: imprecise, not wrong |
| U6-6 | Mechanical | `usePwaUpdate.ts:97-109` | The dynamic import has no `.catch`, so a failed load silently ends update detection | confirmed (narrowed) | #981 | `degraded`. An unhandled rejection from the real path was observed directly; the consequence follows from the code (no updater assigned, no registration captured, and the owning component mounts once at the root so there is no remount). **The candidate's stated evidence source does not exist** — §12 carries no chunk map and `dist` is untracked — so the chunk-fetch likelihood is *not* asserted; a `.catch` is right regardless |
| U6-7 | Functional | `spinner.tsx:19` | The default `label = 'Loading'` is a hard-coded English `aria-label` | duplicate #213 | — | #213's enumerated examples include the identical construct on a sibling Foundry primitive (`close-button.tsx:22`, `label = 'Close'`). Scale is real — 90 call sites, 2 passing a label — but scale does not make it a separate defect |
| U6-8 | Prompt | `success-burst-geometry.ts:25-26` | "Matches the CSS" names a counterpart that does not exist | confirmed | #990 | `cosmetic`. No such value appears in the stylesheet; durations reach CSS per particle from JS. The real invariant is already tested JS-side |
| U7-1 | Prompt | `precip-engine.ts:965-967` vs `flow-field.ts:178-181,324` | "The seeds sit clear … so the two never correlate" is false | confirmed | #989 | `cosmetic`. Complete seed table built: the schedulers take 0–3, 7–12, 20–22, 30–32, 40–42, 50–52, 60–62, 70–72; the mound noise takes 60 and 61 — warm snow's own block. **Visual consequence nil** (columns versus epochs), but the sentence *is* the seed registry, so it misdirects the next field added |
| U7-2 | Prompt | `flow-field.ts:174` vs `flow-field.test.ts` | "span ≤ epoch by construction — asserted by the configs' tests" is false twice over | confirmed | #989 | `cosmetic`. It holds by hand-picked arithmetic, not construction (margins 16–88 s), and no test asserts it. Proved by a **carefully sized** mutation: a gross violation is caught incidentally by the smoothness check, so `DIAMOND_DUST.hold` was set to overrun in epochs 16/27/29 — inside the sampled hour, outside the 640 s smoothness window — and 42/42 stayed green |
| U7-3 | Performance | `precip-engine.ts:3395-3404,1804-1951` | `onResize` is rAF-throttled but not debounced, so a viewport-height change re-rasterises every sprite and drops settled snow | confirmed | #994 | `degraded`. Measured at 4× on a 390×844 viewport with a 100-item vault: 20 height-only changes cost 6.04 ms median each and took settled snow **from 332 lit pixels to 0**, with 12% back after 8 s; a 2.3 s continuous storm spent **19.3% of the main thread**, two-thirds of it re-rasterising sprites whose inputs never changed. **The timings alone would not clear the bar** — this audit has twice rejected the 1–5 ms band, and 0–1 frames dropped per ~140-frame storm. What carries it is the visible state loss plus 5/5 no-op resize events doing the full job. The desktop drag case was measured, but its dropped frames are Chromium relayout rather than the engine and are not claimed |
| U7-4 | Performance | `surface-map.ts:63-64,684` | The tracker's periodic full rescan is a standing tax | confirmed (narrowed) | #995 | `degraded`. Cadence is **2.00 s**, not 2.15. At 4× on Inventory with 625 candidates: 11.92 ms median per tick, **355.6 ms per minute** while idle. Cost tracks candidates in the DOM rather than surfaces found — the `MAX_SURFACES` cap never binds (31 of 625 accepted), so every off-screen row still costs a rect read before being rejected. **Explicitly narrowed to a CPU and battery tax, not jank**: over 60 s idle, frames containing a rebuild dropped at 26.7% against 24.3% without one — no measurable difference, and the snow layer's own 4× baseline is already 24% |
| U7-5 | Mechanical + Performance | `BackgroundEffects.tsx:137-139`, `precip-engine.ts:3425-3429` | Every layer start builds the sprite set twice | confirmed | #993 | `cosmetic`. The theme effect runs at mount, after the start effect, and `refresh()` rebuilds unconditionally. Narrowed by its own control: it does **not** re-fire on a restart whose trigger is not a theme axis, so the duplicate is confined to the start |
| U7-6 | Prompt | `precip-engine.ts:3120` vs `:372` | `0.1` and `0.34` silently restate `deepLayers`' first entry and span | confirmed | #988 | `degraded`. An **unstated** parity, which the audit files only with a demonstrated consequence — met here: changing the deepest layer to −0.6 (the plausible "push it further back" edit) left 65/65 green while the new stratum draws at identical scale and alpha to the one it should sit behind, collapsing the depth cue. The opposite edit compresses the strata instead |
| U7-7 | Prompt | `flow-field.test.ts:143,227-233` | The first-epoch tests hard-code each config's epoch, so lengthening one weakens the test silently | confirmed (folded) | #989 | `cosmetic`. Demonstrated both ways: lengthening leaves 42/42 green while covering little more than half the window it names; shortening correctly fails. Same root cause and same one-line fix as U7-2 (export the configs), so filed with it |
| U8-1 | Functional | `AppNav.tsx:76-84,114-121` | The alerts count has no assistive-technology path | confirmed (**narrowed**) | #976 | `degraded`. The headline claim is false — the count reaches the Alerts menuitem's name, and the Dashboard has an announce-only live region. What survives, and no open issue names: that twin exists **only on the `/` route**, so on every other screen the badge is a visual-only status change — the WCAG 4.1.3 failure the dashboard's own comment says it exists to prevent. The "Alerts 3" bare concatenation is a secondary residue |
| U8-2 | Prompt | `AppNav.tsx:43-44` | The "live badge" docstring claims liveness nothing backs | rejected | — | The plain reading is "updates reactively", which it does. Imprecise at worst |
| U8-3 | Functional | `skip-link.tsx:36` | "Skip to content" is a hard-coded English literal | duplicate #213 | — | #213's body names `skip-link.tsx:37` in its own example list |
| U8-4 | Prompt | `nav-destinations.ts:41-52`, `:289-314` | The off-nav enumeration omits `/tags` | confirmed | #990 | `cosmetic`. Five entries outside the nav; both docstrings list four. `/tags` is in the union, in the palette extras and in the route tree. A reader auditing which routes bypass the nav takes the prose as exhaustive |
| U8-5 | Mechanical | `icons/index.ts:225-227` | `PhoneIcon`, `EmailIcon`, `AddressIcon` are exported and imported nowhere | rejected (Phase 17) | — | Confirmed dead across `src/`, `bridge/` and `extension/`, with no doc claiming they are wired. Dead code belongs to the Phase 17 cross-cutting sweep by this audit's own precedent |
| U8-6 | Mechanical | `page-header.tsx:27` | `homeTo?: string` launders the router's literal typing | rejected | — | The laundering is real (a bad literal compiles through the prop and is rejected on `Link` directly), but no call site passes `homeTo` at all, no doc claims type safety for it, and `MenuLink.to` does the same. Hardening with no live offender |
| U8-7 | Functional + Prompt | `menu.tsx:19` vs `:207` | The trigger's `aria-label` is unconditional | duplicate #214 | — | Recorded on U2-12 |
| U8 negatives | — | `AppNav`, `nav-destinations`, `BrandMark`, `BrandTagline`, the foundry barrel | Five finder verdicts of "checked and clean" | agreed | — | Menu keyboard model sound (arrows with wrap, Home/End, Escape, focus restored, listeners released); `aria-current` set and tested; every routed screen renders `<main id>` with the skip link's focus move plus a hash fallback; every `AppRoutePath` member has a route file; the `NAV_DESTINATIONS` ↔ `en.json` drift test loops **every** destination; the barrel's heavy catalogues stay out of the eager bundle (dynamic import plus `type`-only re-exports) |

### Phase 7 — Inventory domain logic

Pinned SHA: `a28a061c64eb760d47aec89cdde46194a6394561`

Eight finder units (U1 queries, invalidation, mutations, undo, tags and stock hooks; U2 catalogue
import, text import and the migration importers; U3 custom fields, field helpers, categories,
capabilities and presets; U4 locations — tree, path, inheritance, fullness, map, colour, media and
export; U5 kits, batches, bulk edit, clone, dedupe, relations and requirements; U6 labels and OCR;
U7 regions, lifecycle, stock policy, pricing, history formatting and valuation; U8 the inventory
screen, list and view helpers, drag, media, and the scale and weighing seams). Every one of the 181
files directly in `src/features/inventory/` and its five in-scope subfolders was allocated to exactly
one unit. Verification ran in **41 detached worktrees**, one per verifier, so that a verifier's
deliberate mutations could not corrupt a concurrent run — a practice Phase 2 introduced with two
trees and this phase needed at scale.

A session rate limit killed **eleven verifiers mid-run**, two of them holding uncommitted source
mutations. Because every verification tree was detached at the pin and never merged, `main` was never
at risk; all 41 trees were reset to the pin and the eleven verifiers re-run from clean. Nothing was
salvaged from the killed runs, so no verdict rests on a partial transcript.

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Functional | `mutations.ts:104,124,130,157,165` | `itemPagesFilter`'s exact-length key test matches only the flat list and the AST search, so the grouped and paginated lists are never patched or cancelled | confirmed (narrowed, widened) | #999 | `degraded`. Scratch test: both stay at 10 where the flat key gives 15; widening the predicate turns 3 green. **Narrowed** — the row self-corrects on settle, so this is lost optimistic feedback, not a stale cache. **Widened** — `patchItem` still patches `item(id)`, so the detail dialog updates while the card behind it does not, and the filter's own docstring names only the two count queries as excluded. The `isMutating()===1` burst is documented as deliberate and compounds rather than causes |
| U1-2 | Functional | `tags.ts:188-196,199` | `useSetItemTags` never sweeps `items()`, so a tag-filtered list keeps the item and the header count keeps counting it | confirmed (widened) | #1002 | `wrong-data`. Four keys left fresh with `itemTags`/`itemsTags`/`tagList` swept in the same run; a mounted observer re-reads 1 time against the control's 2. **Widened** by two more live readers on the same screen — grouped mode, and the Visual-Builder `tag:` result cache, a child of `items()` that `itemsTagsAll()` does not reach |
| U1-3 | Functional | `mutations.ts:715-718,791-794` | Bulk edit and its Undo write tags through the auto-creating seam but never sweep `tags()` | confirmed | #1002 | `degraded`. Four dictionary keys left fresh for both mutations. The module header states the invariant every single-owner write honours; these two are the only writers of `setForItem` that do not |
| U1-4 | Prompt | `queries.ts:281-284` | The batch-relations key's comment claims any relation write refreshes it by prefix; neither write reaches it | confirmed (narrowed) | #1018 | `cosmetic`. Differential: the single-item key **is** swept in the same run. **Narrowed** — the editor is unreachable from the Projects screen, so the functional edge is a ≤30 s cross-screen window. The three sibling batch keys carry the same comment and for them it is true; the same hazard is documented one key above |
| U1-5a | Functional | `invalidate.ts:127-134` | `invalidateItemStock` never sweeps `locations()`, so a ± tap leaves the cube-fullness bar stale | confirmed (split) | #1004 | `wrong-data`, gated on a measured location existing. Split from U1-5b because the fixes differ: #525's pinned argument is about which *reads* ask for the aggregate, and a blanket sweep would re-run an O(stock) aggregate per tap. Filed with that constraint attached |
| U1-5b | Functional | `mutations.ts:270-273` | `useUpdateItem` is the sole item write that never names `locations()`, so a dimension edit leaves the bar it exists to correct | confirmed (split) | #1002 | `wrong-data`. Thirteen other item writes name `locations()`; this one-at-a-time editor save is the only omission, and carries no cost tension |
| U1-6 | Functional | `invalidate.ts:48-71,127-134` | `reorderKeys.plan` is swept by exactly one write in the application, so the shopping list shows pre-movement shortfalls | confirmed (upgraded) | #1007 | `wrong-data`. **Upgraded from the finder's grading**: the Reorder tab is rendered inside a `hidden`-gated tabpanel, so it holds a live observer for the whole visit, and the receive controls are on the adjacent tab. "Create draft PO" then drafts a real order from the stale plan — #157's outcome by another route |
| U1-7 | Mechanical | `mutations.ts:706-708,673` | Bulk edit discards every per-item cause and carries no `onError`; a failure before the loop is an unhandled rejection | confirmed (widened) | #1009 | `degraded`. Limb 2 produced a real Node-level `unhandledRejection` with the dialog open and nothing announced. The documented exemption does **not** apply — the dialog never reads `isError`. Fixing the hook alone does not fix limb 2, because the caller awaits with no catch. #389's closing comment asked for exactly this sweep |
| U2-1 | Mechanical | `tsconfig.app.json:44` | No `src/` test file is in any TypeScript program | duplicate | — | #601's closing comment names the tsc test-tree exclusion verbatim and leaves it open; register rows P1-67 and P4-U1-7 already rely on it. **Third re-finding** of the same mechanism |
| U2-1a | Mechanical | `catalog-import.test.ts:1583` | A misspelled `required` key means the fixture does not model what the test claims | rejected | — | The test asserts column *inference*, which reads only `name` and `id`; requiredness is asserted by a different test using the correct key. Inert |
| U2-1b | Mechanical | five test files | Under-specified `Item`/`CategoryField`/`GaugeState` fixtures | rejected | — | All six type errors inert: the plan builder reads only `name`, `mpn`, `id` and `trackingMode` off an existing item, and both stubs carry the docstring "only fields the importer reads" |
| U2-1c | Mechanical | `catalog-import-stock.test.ts:302,21` | The drift test's mode list restates the SSOT, typed against a symbol its module does not export | confirmed (reshaped) | comment on #783 | `cosmetic`, latent. Dropping a mode silently removes a parametrised case (16 → 15, nothing red); a *correct* import would not have caught that either, since an array annotation checks membership and never coverage. The defect is the restatement; the remedy is rule 1 |
| U2-2 | Functional | `catalog-import.ts:247` | `type` is a tracking-mode synonym, so an ordinary "Type" column fails every row | confirmed (widened) | #1012 | `unusable`. **Widened**: the mapping dropdown offers no custom-field option, so a field named "Type" is unreachable — the plain case is recoverable via the mapping table, that one is not. The "make it a warning" alternative is **rejected**: it would invent a mode, which #339 outlawed |
| U2-3 | Functional | `importers/migrations.ts:79-183` | The migration specs never gained #141's `serialNumber`/`expiryDate`/`tags` targets | confirmed (narrowed) | #1013 | `degraded`. Three corrections: the generic-path contrast holds only for two of four sources; the Grocy best-before limb is **unverifiable** here and is filed as "no `expiryDate` rule under any key"; the wiki sentence is false on tags and serials, still true on warranty dates, and its category clause is stale too. #488 covers the barcode limb only |
| U2-4 | Functional | `catalog-import.ts:1013-1015` | A blank mapped `sku` cell leaves the MPN alone while every sibling column clears | confirmed (widened, narrowed) | #1014 | `wrong-data`. End-to-end: `mpn` stays `OLD-MPN`, row reported `kind:"updated"`, no error, while eight sibling columns in the same re-imported file produce `null`. Narrowed to the update path. **Caveat for the fixer**: the naive one-liner flips the deliberate precedence the memory note records |
| U2-5 | Functional | `catalog-import.ts:366,1560-1565` | The `condition` column rejects the labels the app and wiki print | confirmed (narrowed) | #1015 | `unusable`. Real Zod message pasted. **Narrowed**: the export writes the constant, so the round-trip is safe and only hand-authored or foreign files bite. The untranslated half belongs to #233 |
| U2-6 | Prompt | `importers/migrations.ts:209-220` | The docstring promises an ambiguity guarantee and a uniqueness property the code and data lack | confirmed (narrowed, widened) | #1016 | `degraded`. Reordering the spec array flips the answer with **no test red**. All 15 pairs enumerated: only one is a routine real file. **Widened differently**: single-column signatures fire on ordinary spreadsheets that are no tool's export, which disproves "unique to that tool's export" more decisively. The drift test needs the signatures exported |
| U2-7 | Mechanical | `catalog-import.ts:1846-1861` | The apply loop index-zips against an array `createMany` may shorten | duplicate | — | Open **#801** names the same lines, reproduces the same shift, reaches the same latent grading, additionally measured the N+1 properly, and carries two sites this candidate missed |
| U2-8 | Functional | `test-records.ts:45,88-92` | An unrecognised test result softens to `PASS` on read | confirmed (narrowed) | #1017 | `wrong-data`, **latent but armed**. The sync ingress is real (a synced LWW leaf whose merge upserts the column verbatim) but no shipped build emits a fifth value. The named model's default is genuinely neutral and this vocabulary already owns the neutral member. The seam's 25-test file asserts the softening against the constant, so changing it leaves everything green |
| U2-9 | Mechanical | `catalog-import.ts:602-608` | An unusable `isUnlimited` cell is answered as "not supplied", so it becomes `false` on a create | confirmed (narrowed) | #1019 | `wrong-data`, narrow but the only silent one. Realistic ingress: a German-locale spreadsheet writes `WAHR`/`FALSCH`, so an unlimited source imports holding 0. This reader predates #339's fix and was never swept in; the wiki promises the opposite |
| U2-10 | Prompt | `catalog-import.ts:368-478,1563` | Twelve fields share one length message and the field path is discarded | confirmed | #1023 | `degraded`. Negative UI check: the preview renders four fixed cells and no per-column highlight, so nothing mitigates it. Correction: prose fields sit at the 20 000 tier, not 500. Not a restatement of #233 — a catalog key would preserve the defect |
| U2-11 | Functional | `components/ImportDataDialog.tsx:207-218` | A custom-field-mapped column renders as "(ignore)" and re-picking it drops the column | confirmed (widened) | #998 | `data-loss`. The trigger text is identical before and after the click while the plan's field values go from present to `undefined`. **Widened**: no part of the dialog reflects a custom-field mapping at plan time or apply time. **Site is `components/`, i.e. Phase 8 scope** — verified and filed here; recorded against Phase 8's area |
| U3-1 | Functional | `CategoryRepository.ts:1217` | The `<inherit>` sentinel is tested before any type check, so the literal string is never stored as text | confirmed (widened, narrowed) | #1003 | `wrong-data`, three live ingresses. **The CSV import is the serious path**: plan errors empty, apply `{created:1, skipped:0}`, stored value the location's — completely silent. The editor paths are visible by comparison. The constant's own doc comment ("the UI never produces" it) is separately false, and considers neither LONG_TEXT, FILE nor the importer. The export never emits the token, so this is no round-trip convention |
| U3-2 | Functional | `audited-item-fields.ts:86-262` | The four gauge-geometry columns ride plain LWW but are absent from the merge-audit registry | confirmed (narrowed, widened) | #1006 | `wrong-data`. Two real peers: the gauge reverts while the last ledger line still names the user's own change. A capacity revert flips the low-stock lane with nothing in the log; a reverted tare corrupts every later weigh-in. **Narrowed** — the conflict review does list it, so what is missing is the durable synced entry. **Widened** — a *mixed* conflict names the other fields and omits the geometry, reading as complete. The exclusion's stated reason ("the edit path leaves it silent") is false. The wiki's "changes quietly" list omits it too |
| U3-3 | Functional | `builtin-field-names.ts:34` | The add-field advisory names a built-in "Model" field that does not exist | confirmed (widened) | #1008 | `degraded`. Exhaustive negative across schema, registries, every screen, the bridge, OpenAPI, exports and the wiki. The MPN-alias reading fails on the function's own docstring ("canonical label"). **Widened**: 13 shipped presets create `Model` as a custom field, and steering a user onto another name lands them on a different definition, so location inheritance silently misses |
| U3-4 | Mechanical | `categories.ts:213-224` | `useUpdateCategoryField` never sweeps `locations()` | confirmed (narrowed, widened) | #1002 | `degraded`. **The finder's call site is wrong** — filed against the Location detail card and the ⋯ → Categories action; the fields panel's create button is unreachable when any row exists. Only the update mutation is live. Demonstrated: the card keeps the old label, and after a retype the panel shows the old control whose save is refused naming a field and type not on screen. **Widened** to the field-search text key; the item-fields key is missed by all three but latent |
| U3-5 | Mechanical | `CategoryRepository.ts:831,1000-1003` | A category field's default is stored with no validation against its own type | confirmed (narrowed, widened) | #1011 | `degraded`. **The live half is not what the finder claimed**: the Add-field form is a `div` submitting on click, so no constraint validation runs. The same string is refused as an item value and accepted as the default, then rendered on every unset item's card. Reachable shapes are an out-of-range number, a rating outside its scale and a non-URL; a date and letters-in-a-number are not. The retype limb is confirmed but **latent** — no ingress found anywhere |
| U3-6 | Mechanical | `custom-fields.ts:370-389` | The ordering seam claims to mirror the SQL `COLLATE NOCASE` and does not | confirmed | #1018 | `cosmetic`; **production-unreachable, the claim is live**. Real SQL against real seam; adding an accented name to the *named guard* makes it fail. A fourth claim site exists. The seam has no production caller and the helper it uses is production-dead. Remedy **rule 1** (delete it) is free — fixing the comparator would hard-code the behaviour #577 wants changed |
| U3-7 | Prompt | `measure-input.ts:67` | An interface comment says "outside 1–100" where the floor is 5 | confirmed | #1018 | `cosmetic`, comment-only. The function's own docstring four lines below is correct and uses links that cannot drift; the rendered hint and the input bounds are right. The smallest item in the sweep |
| U3-8 | Mechanical | `category-presets.test.ts:135-170` | The #715 guard keys field names verbatim while the dictionary folds them | confirmed | comment on #715 | `cosmetic`, latent. A colliding preset leaves the guard green at 23 passed while the real import throws. Trim and NFC variants slip past identically. The same file already keys preset *category* names case-insensitively ninety lines above. #715's own fix section edits these two maps, so it belongs there |
| U4-1 | Mechanical | `v1-initial.ts:1070-1105` | The effective-value view omits the category default, so a visible value is unsearchable | confirmed (widened) | #1000 | `wrong-data`. Five-limb drift test, all five red, seam assertions passing in each. **Widened**: a second live consumer (the field-due-date feed) silences alerts, and there is a **mirror-image false positive** — a value for a definition the category no longer declares is searchable but not shown. The bridge uses the seam, so bridge and in-app search disagree. Fixer caveat: the obvious join shape drops uncategorised items |
| U4-2 | Performance | `locations-export.ts:59` | The path resolver rebuilds a whole-list index per call, making the export quadratic | confirmed (narrowed, widened) | #1001 | `degraded`. Real function, four tree shapes, ratios **4.2–5.0× per doubling**; crossings 31 ms / **130–150 ms** / 1.3 s / 5.2–6.5 s. **Narrowed** — filed on the ~2 000 crossing, not the 10 000 ceiling. **Widened** — the import plan builder is the same curve and re-runs on **every** mapping change. The placement picker is **not** a limb (bounded, under 40 ms). Main thread confirmed; no spinner. Fixer caveat: the shipped walk is cycle-safe |
| U4-3 | Functional | `LocationRepository.ts:1065-1077` | A parentage loop hides the loop and everything beneath it from the tree and map | confirmed (narrowed) | #1005 | `wrong-data`. Real `getTree()`: four rows rendered where the flat list holds seven, and a location *not* in the cycle disappears with it. The subtree is unreachable from the sidebar including search. **Narrowed** — a self-parent and a dangling parent are both impossible, so only a genuine ≥2-node cycle is live. **Not** #818/#819/#889, which all describe hangs; this is the silent fourth consequence. The flat export path was deliberately hardened against this data and the tree builder was not |
| U4-4 | Prompt | `location-fullness.ts:156-160` | The docstring says "meet or exceed" where the code tests strictly greater | confirmed (narrowed) | #1018 | `cosmetic`. **The functional limb is disproved**: with an integer capacity and a count the two forms are identical at the default, and the warning and the gauge already agree at every pair. Mutating to `>=` makes an empty bin of capacity 1 read "is at capacity (0/1)". The introducing commit's test name and inline comment show the boundary was chosen deliberately — the comment is the defect |
| U4-5 | Functional | `tree-keyboard.ts:111,147-148` | ArrowLeft alone does not re-enter the tree on a stale focus | confirmed (narrowed) | #1018 | `cosmetic`, latent bordering on unreachable. **Both the consequence and the reachability are refuted**: delete re-homes focus, every row corrects the focused id, a filtered-away row loses focus outside the tree, and nothing overflows horizontally to scroll. Mutating it to match its five siblings leaves all 25 tests green, so nothing pins the current behaviour |
| U4-6 | Mechanical | `location-tree.ts:218-235` | Two path walks documented as identical spell a path differently | confirmed (widened, narrowed) | #1018 | `cosmetic`. **The two-node examples are unobservable** — the matcher is term-based. The real defect is at a cycle of three or more, where the memoised walk **drops a real ancestor**: across all six orderings exactly one node loses one, and an ordinary one-word query then misses it. **Narrowed** — those rows are already off the tree for a worse reason, so it unmasks only when cycle-broken rows are surfaced as roots |
| U4-7 | Mechanical | `location-media.ts:250-251` | Unlink-then-link across two awaits leaves the item placed nowhere on a failure | duplicate | — | Open **#844** names the same lines, the same gate asymmetry and the same fix. Reproduced independently to confirm it is still live. Two notes passed to #844 rather than filed: both tombstone helpers are already exported so a transactional move is a few lines, and the failure copy asserts nothing changed when the placement is gone and tombstoned |
| U5-1 | Functional | `checkout-plan.ts:249`, `item/dedupe.ts:376,592` | A cross-mode merge decides the loan's return from the kept item, so the return destroys or invents stock | confirmed | #996 | **`data-loss`**. Direction A: 11 units → 8 after the loan → **still 8** after the return, with `returned_at` stamped and a `CHECKED_IN` row carrying `quantity_delta: null`. The ledger and the totals agree at the wrong number, so no reconciliation can see it, and `restore` does not recover it. Direction B mints a unit that never left. **Not #789** — instrumented: its suggested gate sees 1 open loan and 0 bookings and passes the merge. **Narrowed on remedy**: fixing `restoreDelta` alone aborts on the serialised CHECK; the restore must target the drawing item |
| U5-2 | Functional | `kit-availability.ts:34-43`, `item/kits.ts:680-700` | The buildable count ignores tracking mode, so Assemble is enabled for a build that is then refused | confirmed | #1010 | `unusable`. A serialised component gives `count: 1` and an untracked one `count: 20`, both with Assemble enabled; the build throws an authored message. No warning in the picker or on the row — a transient toast only. **Not #793**, tested by driving the planner: for this defect the offending item **is** in the plan's draws, so #793's suggested fix leaves the refusal correctly in place. Closed **#608** is the fix idiom, applied to two other pickers and never to this one |
| U5-3 | Functional | `mutations.ts:672-712` | The count and the undo snapshot are appended only after all four writes, so a partial item is reported as failed with no Undo | confirmed (widened) | #1022 | `wrong-data`. **Widened by a third route needing nothing exotic**: a user with write-but-not-delete permission doing Category + State→Removed gets `{succeeded:0, failed:2}` while both items carry the new category. The others are the storage hard stop and an over-length tag name. The wiki promises Undo restores every item; closed **#301** fixed this shape at four other sites and did not touch this loop. Open #145 mentions atomicity in passing but identifies none of the three routes |
| U5-4 | Mechanical | `mutations.ts:709-711` | The bare `catch` binds nothing, so a wholly-failed batch reports a count with no cause | confirmed | comment on #1009 | `degraded`. Side by side under the same hard stop: bulk edit resolves with `error = null` and an empty toast region; the sibling reversal rejects and names the real cause. **Correction to the finder**: four other neighbouring mutations also lack a reporter — the defensible form is that those can still be reported at their *call sites*, whereas here the error is destroyed before any surface can see it. Folded into the issue another unit had already filed for the same hook |
| U5-5 | Mechanical | `batches.ts:37-40,48-55` | Batch and lot text is trimmed but never Unicode-normalised, so one lot becomes two stock rows | confirmed (widened, narrowed) | #1024 | `wrong-data`. **Widened**: it needs no second device — two receipts on one device with differently-normalised paste fork the ledger — and combining-mark order and Hangul Jamo fork it too, neither having a precomposed alternative. **Narrowed on prescription**: the fix is NFC in the attribute normaliser, **not** the app's name-fold, which also case-folds and would come back through the key round-trip as the displayed lot number. Fix-design note: the write path keys on the normalised value but stores the raw text |
| U5-6 | Mechanical | `batches.ts:137,237` | FEFO ties break on a locale comparator while the docstrings claim a device-independent order | duplicate | — | Open **#772** names the identical sites, the same expected fix and the same grading, and goes further — showing non-ASCII is not even required. Verified substantively anyway, and two things fed to #772 rather than filed: the ordinal is **not** order-dependent (the divergence is that the order decides which lots enter the plan at all), and the comparator returns 0 for unequal strings, which leaves an order-independent second divergence path the issue does not cover |
| U5-7 | Performance | `dedupe/duplicate-groups.ts:209-235` | The opt-in similar-name signal runs an unbounded pairwise pass on the main thread | confirmed (narrowed, widened) | #1060 | **`unusable`**, live, opt-in. The main-thread premise was traced, not assumed: only query, execute, transaction and file operations cross the worker boundary. Measured at the 50 000 ceiling: **311 s** worst shape, 149/75/38/14 s as family size falls, 0.3–0.6 s where members genuinely union. Instrumented calls match the analytic bound **exactly** (19 900 000) and are reached with an **ordinary** name shape. **File it on the crossings, not the ceiling**: 100 ms at ~550 items and 1 s in the low thousands for families of ten. Dropping the block cap to 1 collapses a 10 000-item run from 51 581 ms to 16.8 ms. **The finder's "stops painting" is wrong and the truth is worse** — the spinner is composited, so it keeps turning while the main thread is dead. Neither the cost docstring nor the wiki says where the work runs |
| U5-8 | Performance | `item/kits.ts:403-423` | `readKitGraph` issues one round trip per node and its docstring claims level-by-level batching | rejected | — | The N+1 is real and counted exactly (edge reads == node count). **The measurement does not support a finding**: ≤3 ms desktop and ≤71 ms at 20× CPU throttle for a 100-node graph, against a bounded, hand-authored kit — versus #801, the open N+1 in this area, which scales with an import file. **The Prompt limb is false**: the docstring's "level by level" describes traversal *order* and attaches "batched" only to the item rows, which genuinely are one query. Recommended as a one-line note under #112, not an issue |
| U5-9 | Functional | `docs/wiki/Bulk-Edit-and-Clone.md:20-21` | The wiki promises a tag Remove and a bulk Check out the selection bar does not offer | confirmed (narrowed) | #1039 | `degraded`. Both surfaces rendered and quoted. The check-out bullet describes the **scanner's** batch actions, documented correctly on their own page. **The third limb is rejected as a defect** — that clearing tags is impossible is *pinned as intended* by a shipped test, and belongs only as the reason there is no workaround. Open **#145** already tracks the capability gap; the untracked part is that the wiki claims it exists |
| U5-10 | Functional | `TagRepository.ts:317-400` | Tag changes are written to no activity log, so the wiki's undo promise is false | confirmed (widened) | #1040 | `degraded`. After adding and after removing a tag the action list is still `['CREATED']`, with the tag set asserted changed first. **Widened to the true statement**: tag changes are audited **nowhere in the app**, across all four call sites; the bulk-edit page is merely where the wiki makes an absolute claim that catches it. The same page hedges correctly elsewhere, which shows the author knew the log was incomplete |
| U5-11 | Prompt | `clone.ts:23-40` | The docstring conflates two differently-named serial fields and omits fields it does not copy | confirmed (narrowed) | #1041 | `cosmetic`. The field a user knows as *Serial number* is dropped and left blank, not reassigned; the parenthetical describes an unrelated internal index. **Heavily narrowed**: of eleven claimed omissions only **three** hold — the rest are either not creatable at all or child records outside the function's remit, with the calling hook's docstring stating which it copies. The finder's line numbers were wrong (the file is 135 lines). One genuine addition: two more gauge fields are copied than the Copied line names |
| U5-12 | Mechanical | `clone.test.ts:5-36,56-65` | The fixture omits 13 required fields, so the strip test cannot catch a copied barcode or serial | confirmed | #1042 | `cosmetic`, latent. **The mutation is the finding**: adding those two fields to the clone leaves **2811 tests across 170 files green**, while a complete 41-key fixture with the same assertions goes red under the identical mutation. The file has been edited thirteen times, several adding fields, and the fixture was not touched for any of them; ten test files each hand-roll their own fixture with no shared factory. Kept narrow — the type-checking mechanism is settled elsewhere |
| U5-13 | Functional | `dedupe/duplicate-groups.ts:281-288`, `item/dedupe.ts:235` | The dedupe card shows a gauge's stock as 0 and ranks the keeper on it | confirmed (narrowed, widened) | #1025 | `wrong-data`. Two gauges at 5 L and 950 L both report 0, so the keeper heuristic's first comparison is a no-op and it selects the near-empty one — at the destructive decision the wiki tells the user to base on stock. **The untracked half is DISPROVED**: bulk and untracked are storage-identical, so that quantity is a real figure. **Widened, replacing it**: the card diverges from the app's shared per-mode stock renderer for two modes — 950 L in the list against 0 in the card; "Not counted" in the list against a number in the card |
| U5-14 | Functional | `dedupe/reference-remap.ts:190-193`, `item/dedupe.ts:503-506` | A merge deletes **and tombstones** a relation whose kind this build cannot name | confirmed | #997 | **`data-loss`**, live path, latent trigger. The unknown row is absent from the table and present in tombstones while a control re-keyed and survived; skipping instead leaves it intact **with its note** and writes no tombstone. **The finder's hedge is rejected** on three grounds, chiefly that doing nothing is already a shipped state (unticking the remap leaves every relation pointing at the removed item). The vocabulary has already grown once post-release. **New**: the outcome copy gives a false reason ("because the kept item already had them"), foreclosing investigation |
| U6-1 | Mechanical | `labels/label-sheet.ts:388-389,332-340` | The QR fit check runs before the short-code line is appended | confirmed (widened) | #1026 | `wrong-data`, live **on the shipped default template**. Die-cut 40×30: measured 0.3176 mm/module against a drawn **0.2413** on a 0.25 floor, reported `ok`; confirmed in real Chromium at 0.2420 mm. A sweep of every shipped preset × symbology × field set found **11 mis-reporting shapes**, including the default plain-paper layout two controls from a first-run state, and two presets with **no optional field enabled**. **One of the finder's three examples was wrong** and is not used. Printing is never blocked |
| U6-2 | Functional | `ocr/receipt-ocr.ts:177-198,245-249` | The amount tokeniser lifts a digit substring out of a mixed run | confirmed (widened) | #1029 | `wrong-data`. `Total 1O.99` → **99**; `Total for order AB1234` → 1234. **Widened past letter confusion**: a lost decimal point (`Total 12 99` → 99) needs no letter at all, and a reference on the total line outranks a correctly-parsed priced line. Filed **merged with U6-5**, a grouping *tested* by mutation rather than judged |
| U6-3 | Functional | `labels/label-sheet.ts:447-455,562` | The barcode width check ignores the stylesheet's height cap, so the symbol letterboxes | confirmed (narrowed, widened) | #1027 | `degraded`. Real browser: CSS box 40.00 mm, printed symbol **29.68 mm → 0.1799 mm/module** on a 0.19 floor, fit still `ok`. **The condition is cleaner than "17+ rows"**: when the cap bites the printed module is independent of the encoded value, so it bites below ~16.83 mm of cell height — reached at **13 rows** on the default margins, three interactions from a first-run state. The die-cut path can **never** be affected. No mutation needed: the falsifier is the app's own 40.00 mm against the browser's 29.68 mm |
| U6-4 | Functional | `ocr/receipt-ocr.ts:255-267,194` | On a label the fallback picks the largest two-decimal number | confirmed (widened, restated) | #1030 | `wrong-data`. **The finder's examples are contrived** — a real EAN does not trigger it. Ordinary label furniture does: a VAT rate → 20, a net weight → 250, a voltage rating → 230, each beating a price carrying a currency symbol. **"Add 'price' as a keyword" was tested and is not the fix** — it repairs three cases, introduces a new collision, and all 39 existing tests pass under it. The honest claim is that a *receipt* heuristic is applied to an input where the biggest figure is almost never the price |
| U6-5 | Mechanical | `ocr/receipt-ocr.ts:192,177-180` | The negative guard is dead because the tokeniser admits no sign | confirmed (widened, narrowed) | #1029 | Dropping the guard leaves **12,447 of 12,448** tests passing. The test *named* for a refund line calls the parser directly and never goes through the receipt path. **Frequency stated honestly**: photographing a refund receipt to *add* an item is rare — but a receipt covering a purchase **and** a return is ordinary at a trade counter, and there the stripped sign wins the fallback |
| U6-6 | Functional | `ocr/receipt-ocr.ts:317-340` | An ambiguous date is read day-first with no seam to say otherwise | confirmed (reshaped) | #1031 | `wrong-data`. **The reshaping is the finding**: the user's locale is *not* the right signal — the **receipt's** origin decides the order, so a UK user scanning a US invoice gets the same transposition and threading the locale through would not fix it. Filed as "stop guessing silently". The review step shows the date only in a native date input, whose field order comes from the **browser's** UI locale (four renderings measured). **Narrowed**: a month-first browser does give a visible cue; the cue-free case is the reverse pairing |
| U6-7 | Functional | `ocr/OcrPrefillDialog.tsx:69-73,125` | The detected currency is shown and then discarded | confirmed (widened, reshaped) | #1020 | `wrong-data`. The row is labelled with the detected currency and the bare amount is written to a base-currency column. **The decisive point**: the same component already withholds a *scraped* foreign price with an explanatory message, because closed #666 recorded that the column cannot hold it honestly — so the finding is a **missing guard**, not a misleading label. A bare dollar sign also defaults to one currency, so the label can itself be wrong |
| U6-8 | Mechanical | `ocr/ocr-asset-cache.test.ts:17-25` | One constant is pinned to two independently-versioned packages | confirmed (narrowed) | #1032 | `degraded`, latent. **The finder's strongest limb does not hold** — no patch is published, so it is not one refresh away. **Better evidence found**: registry history shows the highest release satisfying the declared range differs in **three of the five majors** this project could have shipped, and a real published pair legal under the range (and accepted by the sibling guard) makes the two assertions unsatisfiable, with a control passing in the same run |
| U6-9 | Performance | `ocr/useReceiptOcr.ts:48-81` | Closing the dialog mid-scan cannot stop the recogniser | confirmed (narrowed) | #1033 | `degraded`. On unmount terminate is called **0** times; a reopen leaves **2** recognisers live, with a control showing a completed pass does terminate. Measured: ~**7.7 MB** resident per worker on the default tier, ~**18.5 MB** on the higher one. **Narrowed — not a leak**: each abandoned worker self-terminates when its own pass ends, so the overlap is bounded at one. Real scan duration was not measured, so the window's length is unverified |
| U6-10 | Mechanical | `src/sw.ts:414-427` | A quota failure discards a successfully fetched asset and blames the network | confirmed (widened) | #1035 | `unusable` for that feature. Fetch returned 200, the write was attempted once, the response came back as an error; all three of the recogniser's real failure strings map to "check you're online". **The docstring reasons carefully about three cases and every one is a case where the write's failure is also the response's failure.** Reachability is not exotic *for this app*: it ships a storage-exhaustion subsystem, and first scanner use is the largest cache write the app makes. **Widened**: the path also swallows the exhaustion signal that subsystem exists to act on |
| U6-11 | Functional | `ocr/OcrPrefillDialog.tsx:153-173` | The picker's copy offers a choice its markup asks the platform to remove | confirmed (narrowed) | #1021 | `cosmetic` for the demonstrated part. One render shows the copy, the button and the attribute together; it is the **only** use of that attribute in the app. **The mobile behaviour was NOT observed** and the issue says so — the specification says only "SHOULD", and the reference documentation is if anything contrary. Settling it needs a real Android and iOS device. Supporting fact: no other route into the scanner accepts an existing file |
| U6-12 | Functional | `ocr/receipt-ocr.ts:352,405-418,307` | A best-before date is taken as the acquired date | confirmed (widened) | #1034 | `wrong-data`. On a whole receipt the best-before line beats a genuine purchase date outright. **The widening changes the fix**: of nine realistic label shapes, **six carry no "date" word** and still land through the unlabelled fallback, so removing that word fixes almost none of it — a **negative-label** concept is what is missing. **The strongest framing**: the app has an indexed expiry field on the same form, and the scanner never fills it. The future bound admits next year, exactly where a best-before sits |
| U6-13 | Mechanical | `labels/code128.ts:57-164` | 105 of 107 hand-transcribed pattern rows have no assertion | confirmed (narrowed) | #1028 | `degraded`; the gap is live, the corruption latent. Swapping two rows real vectors exercise, and flipping one bit in another, each leave the full suite passing but for one environmental flake; there are **no snapshot files anywhere in the source tree**. **The table is correct today**, corroborated by an independent derivation of all 107 rows with **0 mismatches**. **The checksum limb is dropped** — it *is* guarded, by two full symbol vectors. Second limb: the start-code assertion admits a value the encoder cannot emit |
| U6-14 | Functional | `label-sheet.ts:552`, `en.json:482` | The A4-only sheet gives unconditional print-at-100% guidance that crops on Letter | rejected | — | **The notice is not shown to the population the claim describes** — it renders only for a named A4 die-cut stock or a hand-entered layout, and every preset is A4 by id, code and docstring, so a user with that stock is feeding A4. The dialog states A4 twice within two lines, and the guidance is not wrong even in the bad case. Nothing claims Letter support, so §1's feature-gap exclusion applies. Browser behaviour was **not observed** — no printer on the host. §11 |
| U6-15 | Prompt | `labels/location-path.ts:36` vs `location-tree.ts:309` | Two exported `locationPath` functions with different meanings | rejected | — | **The finder's stronger limb is factually wrong**: the completeness claim is about separate *walks*, and the second function is a documented three-line wrapper over the first, with its own comment saying so — the enumeration is complete. The hazard had to be **manufactured by editing correct code**; no shipped call site is wrong and each is documented at its definition. Two correctly-used same-named exports is the naming preference §1 excludes. §11 |
| U7-1 | Mechanical | `reorder-policy.ts:97`, `item/attention-sql.ts:85-87` | The pure and SQL low-stock forms round the gauge threshold differently, so a gauge exactly on its floor disagrees | confirmed (**substantially narrowed**) | #1036 | `degraded`. The 74-pair sweep reproduces exactly; two at-floor gauges make the existing parity test fail. **Which side is right, settled with an exact-rational referee** over 18 800 cases: the pure form is wrong in **720 (3.83 %)**, the SQL form in **0**. **Three of the finder's claims disproved**: the Estimate quick-set snaps to exact binary fractions (0 mismatches), **no app screen disagrees** (the pure seam's gauge fields are inert and its state field has no consumer), and the shipped default floor is safe (0 of 345 plausible pairs). **Widened**: the disagreement is confined to the bridge, where a file asserts the counts "cannot part company" *because of this parity test* — so the cost is a missed automation trigger. Contradicts the register's own P2-43 |
| U7-2 | Functional | `item-total-value.ts:34-43` | The card prices from `unitCost` alone where the reports price from four sources | confirmed (widened, narrowed) | #1037 | `wrong-data`, opt-in on all three card surfaces. The hero metric prints **"—unpriced"** — a positive false statement, which is what lifts it above `degraded`. **Widened in cause**: not a #688 residue — the divergence predates it by three weeks, so the docstring's "differs in exactly one way, deliberately" was **false on the day it was written**. Closed **#706** is the identical defect one layer over, and the helper that exists to stop the next such divergence is not called here. Three of the four sources are cheaply recoverable |
| U7-3 | Functional | `regions/region-keyboard.ts:94-95`, `region-canvas.tsx:439` | Escape on a focused region closes the whole editor as well as deselecting | confirmed | #1052 | `degraded`. **Observed in a real browser**: one Escape fired both, and the dialog unmounted. **An in-harness control passes** — a Foundry select in the same modal leaves the close handler uncalled — so the harness reproduces the layering and the only difference is the missing call. The photos capability defaults on; the existing regression test renders with **no enclosing modal**, so it cannot see this. **#971 is a sibling, not a duplicate** |
| U7-4 | Functional + Performance | `region-canvas.tsx:448-453`, `location-media.ts:158-167` | Each arrow nudge writes non-optimistically and recomputes from a stale base | confirmed (quantified) | #1053 | `degraded`. **Measured in a real browser** reading the final value from the database: unthrottled, 30 presses land 30 steps to ~53/s, then 12/30 at 78/s and **1/30** free-running; at 4× throttling **15 of 30 at every interval**. The machine's own key-repeat rate is ~30/s — exactly where the throttled device loses half. Cost: ~**20 ms of database work per press**, so ~60 % duty cycle while held; at 4× the pipeline saturates, **which is why the two limbs are one phenomenon**. **Third cost not claimed**: ~30 sync-relevant row versions per second (cf. #890). Filed as one issue |
| U7-5 | Functional | `price-history.ts:38-69` | The price series subtracts and ratios across different currencies | confirmed (widened) | #1043 | `wrong-data`. A ¥9,800 → £60 re-quote renders `−GBP9740.00 (−99%)` above a `GBP60.00` row and a `JPY9800.00` row. The repository **neither rewrites history nor refuses a currency change**. **Widened**: the strongest route is the refresh button scraping a foreign marketplace with no review; **the same feature already implements the correct refusal one module away**, so no "intended" defence exists; and the comment justifying the line is itself false (it is not "the latest two points") |
| U7-6 | Functional | `low-stock-policy.ts:37-41`, `CreateItemDialog.tsx:790-808` | Custom with a blank trigger saves "Default"; zero saves "Never" | confirmed (widened) | #1045 | `wrong-data`, driven end to end in a real browser. The app's own copy confirms both outcomes. The zero branch writes a real value asserting an intent never expressed. The "Default means never flagged" claim was verified through the constants, the store's migration and both predicates. **Widened — there is no gate in any layer**: the numeric field renders as a text input with a spinbutton role, so its minimum never participates in form validation. The zero case does mark the field invalid and submits over it; the blank case is silent |
| U7-7 | Mechanical | `regions/geometry.ts:374-376` | A hand-written default suppresses the exhaustiveness error the seam exists to raise | confirmed (re-shaped) | #1055 | `degraded`, latent. Adding a fourth shape compiles clean across the whole app (**exit 0**); the three trailing returns are provably unreachable. **Three corrections**: three dead returns not four; the six functions the finder cited **do not** fail (they switch over a *separate* union — the parser is the **bridge** between the two, which is why it is the dangerous site); and the draw-tool half is weaker, since that union has a gate and the shape union has none. **#562 settles "worth filing?"** in its own words, and swept two functions **in this module** while missing this one because the switch *has* a default. A fourth unguarded site found: a ternary labelling any unknown shape "Polygon" |
| U7-8 | Mechanical | `dead-stock-options.ts:14-18` | The options list is a hand-maintained parallel of the SSOT with no derivation and no test | rejected | — | **False in both directions.** Removing a member is type-checked (one app-wide error). Adding one **is** caught by an existing parity test — it simply lives in a file named after the search surface, not this module, which is why the finder concluded none existed. The docstring's claim is upheld, so the Prompt half fails too. Residual for §11: the addition-direction guard is *indirect*, holding only because dead stock is one of three enum search fields |
| U7-9 | Prompt + Mechanical | `operational-metadata.ts:79-95,57-77` | A nested value is documented as "preserved" but retyped; a `__proto__` parameter is dropped | confirmed (both limbs, split) | #1046, #1047 | Limb A `wrong-data` **latent** — nothing in the seam *can* preserve a nested value, and no shipped in-app path writes one; the honest fix is to correct the word. **New symptom**: such an item opens the editor **spuriously dirty**, arming the unsaved-changes guard. Limb B **`data-loss`** live — renaming an item's only parameter wipes the field while the button reads "Saved", with no ledger row because the field is deliberately untracked; frequency negligible. **Widened**: a *second* drop site — the schema layer rebuilds with plain assignment and discards the key again |
| U7-10 | Functional | `regions/region-keyboard.ts:190-195`, `geometry.ts:264-313` | The move-clamp is applied to resizes, sliding the documented anchor | confirmed (narrowed, widened) | #1054 | `degraded`. Coarse step moves the left edge **5 % of the photo width in one press**; the circle drifts at twice the rate and also vertically; **grow-then-shrink does not restore the position**, so oscillating walks the region across the photo. **Narrowed — the parity half of the docstring is TRUE** (both paths byte-identical), so only the anchor half is false — **which widens it to both paths**. Removing only the clamp turns the five keyboard assertions green while the pointer case correctly stays red |
| U7-11 | Functional | `item/revaluations.ts:51-62` | A revaluation's log entry uses a metadata shape the renderer does not read | confirmed (widened) | #1038 | `degraded`. The row draws only "Revalued | Recorded a manual revaluation." while the same column edited in the item editor draws the before and after. A search for that metadata field returns **zero** non-test readers. **Widened**: it is a residue of the *rendering* work, not the tracking work, and the comment beside it records a decision about **scale**, whose stated purpose ("for display") is now false. **Second widening**: neither log answers "who set this value, and to what" — the revaluations table has no actor column, the history row has no figure |
| U7-12 | Prompt | `constants.ts:320-322` | The docstring says a serialised item is low at or below the threshold; every implementation excludes it | confirmed (widened) | comment on #1018 | `cosmetic`. **Five sources against one**, not two — both report-repository docstrings state the exclusion too, so the constants file is the lone outlier in its own directory. Behaviour is pinned by the parity test, so **a fix must edit the comment, not the code**. All six non-test importers traced; none acts on it. The introducing commit was a tooltip pass |
| U7-13 | Functional | `regions/draw-machine.ts:350-355` | The lower vertex bound is enforced and the upper one is not | confirmed (narrowed) | #1056 | `degraded`, live only by deliberate effort. Rendered, the region at the cap is a named button and the one past it has **no element at all**; selecting it yields zero handles. **Reachability is the honest weak point**: no freehand or drag mode, a move places nothing, touch is identical, the keyboard cannot add vertices — **every one of the 513 vertices is a separate deliberate click**. **Not `wrong-data`** — the geometry is stored faithfully and it is the reader that refuses it. The database bound would need ~22,000 vertices; the constant is untested and its docstring frames it as a trust boundary, not a product rule |
| U7-14 | Functional | `asin.ts:20,105-108,132-138` | The order-code check accepts any ten alphanumerics, so a mistyped code becomes a dead link | rejected | — | A **documented decision** recorded in four independent places: the reference plan doc defines the subject as a ten-character identifier, the introducing commit states the strict/permissive split as the design, the code says so twice, and the shipped test is *titled* "rejects anything not exactly ten alphanumerics". The tightest defensible pattern was tried and the suite still passed, but it would reject legacy codes and still miss the commoner error of a correctly-shaped wrong one. **Not #489** — that is an *asymmetry* (a validator exists and one caller skips it); here none exists and none can, since the code carries no check digit |
| U7-15 | Mechanical | `price-history.ts:39`, `valuation.ts:86` | No tie-break for equal timestamps, and the twin claims to mirror one that does not exist | confirmed (narrowed, widened) | #1044 | `wrong-data` when it fires, **latent**. Read through the shipped order a tied pair comes out **deterministically backwards** — not arbitrary, reliably inverted, with arrow, sign, colour and percentage all flipped. Ties are easy: 40 sequential updates spanned 3 ms and produced **4** distinct stamps. **Narrowed** — no shipped screen writes two points to one part within a millisecond; what would make it live is a batch path or **sync**, since peer rows carry the peer's own stamp. **The parity claim was never true**, written in the same commit as the tie-break it names. **Rule 2, not rule 3** — the weaker seam lacks a *fact*, so no drift test can exist until the row gains an order discriminator |
| U8-1 | Mechanical | `inventory-empty-state.ts:23,98-99` | An unguarded record lookup on the URL's location id returns an object where a string is declared | confirmed (narrowed, widened) | #1061 | **`unusable`**, live. `?loc=__proto__` replaces the Inventory route with the error screen; the control `?loc=nonexistent-id` renders identically to the baseline, which is the documented behaviour. **Narrowed twice**: it is **not** the app-wide safe mode — the router's route-level component catches it and the chrome, search bar and Dashboard survive; and `?loc=constructor` / `?loc=toString` do **not** crash, they silently drop the banner's second line. **Widened — the crash is DURABLE**: *Try again* and a full reload both re-crash, since the value comes only from the URL. Contradicts the params docstring's "the worst a bad link can do is show the default view". **#959** is the same mechanism graded `cosmetic` *because no path reaches it* — which is the argument that a live instance differs |
| U8-2 | Functional | `InventoryScreen.tsx:675-681`, `components/pagination.tsx` | A page turn never resets the list's scroll offset | confirmed (widened) | #1063 | `degraded`, live. Real browser: the offset is unchanged across Next while the pager moves to `101–200 of 300`, so the top visible row is item **191** — items 101–190 are never seen; the page is 5.2 viewports tall at the default size. The search case is live with pagination **off**, the default. **Widened beyond the inventory list**: the Activity feed reproduces it on the *document* scroller, so the gap is in the shared pagination seam; no list anywhere scrolls to the top. **Two checks came back negative**: focus is correct, and there is **no in-app inconsistency** — it is uniform. The status-chip variant is unverified |
| U8-3 | Mechanical | `list-window.ts:60-66,88` | A row field is computed and read by nothing, while four components hand-roll its predicate | confirmed (widened) | #1064 | `cosmetic`. **The finder's proposed mutation does not work** — flipping the comparison leaves the test green, because no fixture has the two values equal, so the assertions do not pin the boundary the predicate is about. Forcing the field false reddens **only** its own file. Flipping the **live** predicate — a real behaviour change — leaves the **entire 12,442-test suite passing**. **Widened to four consumers**. **Narrowed on the #785 link**: that bug's root cause is elsewhere, so a live field would not have caught it; the accurate statement is that the live trigger is untested while its dead twin is over-tested |
| U8-4 | Mechanical | `item-drag.tsx:430-469,399-409` | The drag ends only on a release, a cancel or unmount, ignoring every other signal | confirmed (narrowed) | #1057 | `wrong-data`. **Live and demonstrated in a real browser**: a window focus loss *does* arrive mid-drag and is discarded — afterwards the body class and preview chip persist and the drag is still armed, and an **ordinary single click on a location row** then moved the item. **Narrowed on the trigger**: it could **not** be shown that a shipped browser drops the release, so the filed defect is the **absence of any recovery path**, which is live; the wrong-drop consequence follows whenever a release is lost, but its frequency is unestablished. Exposure is largest on desktop mouse, since touch gets a reliable cancel |
| U8-5 | Performance → Prompt | `item-drag.tsx:213-228` | The hook documents a stability requirement its only caller violates | confirmed as a contract defect; **performance limb rejected on measurement** | #1059 | `cosmetic`. **The finder's mechanism is disproved**: a live drag hovering 53 rows with the highlight re-homing on 20 produced **0 registrations and 0 ms** — the provider's children element is stable, so React bails out of the subtree and the memo holds. "The churn peaks during a drag" is false. The real churn is 104+104 calls in **0.04–0.085 ms** against a 0.065 ms baseline, on a ~1.6 s interaction. The row component is unmemoised anyway. **#419 does not cover this** |
| U8-6 | Prompt + Performance | `usePhotoImageSrc.ts:51-54,95` | A typed-array dependency defeats structural sharing, so every refetch re-reads and re-allocates | confirmed | #1048 | `degraded`, live. Two reads where the comment promises one; the sharing helper bails unless both sides are a *plain* array or object. Three refetches gave four reads, four created and **four revoked** URLs — **churn plus a false comment, not a leak**. **The finder's trigger set was wrong and is replaced**: region and placement writes hit *sibling* keys; the real triggers are photo writes, ~28 locations-prefix sweeps (including the move-item write from the section directly above the panel), and **every panel open with a cache older than 30 s** |
| U8-7 | Performance | `usePhotoImageSrc.ts:75,86` | Each card reads and decodes the full-size photo separately, with no sharing | confirmed (**materially narrowed**) | #1049 | `degraded`. **The finder's premise is false**: the stored file is bounded at 1080 px / q0.8 — 16 KB to **231 KB**, not a multi-megapixel original — so the byte argument largely collapses. **What survives is the decode**: six cards on one photo cost **62.8 ms / 1.39 MB** against **13.3 ms / 231 KB** shared, pixel-identical; twelve cards at 4× throttling cost **780 ms** against 46 ms, nearly 17×. **Narrowed on prescription too**: the thumbnail is not a free substitute at 320 px by the app's own layout reasoning, so **sharing** is the cost-free win and a size hint the secondary half |
| U8-8 | Mechanical | `item-drag.tsx:490-498` | The unmount cleanup undoes two of the five things ending a gesture does | confirmed (widened) | #1058 | `degraded`, narrow. A long-press timer firing after unmount re-applies both body properties the cleanup removed and schedules **17 frames in 500 ms** on a torn-down tree. **Widened — a mouse variant the finder did not name**: the leaked move listener can arm a drag after unmount with no touch and no timer, via a navigation shortcut held with the button down, which makes it a desktop defect too. Self-heals on the next release, so the window is under a second — but **compounds with U8-4**, where that release is the one that goes missing |
| U8-9 | Mechanical | `scale-stream.ts:75-82,179` | An unguarded record lookup on a bridge frame's failure code defeats the fallback | confirmed | #1062 | `cosmetic`, **effectively unreachable against a stock bridge** — its issue vocabulary is a closed six-member set with no collision and the field is typed to it, so this needs a modified or hostile bridge. The precise outcome, resolved through the real translator, is the **raw message key** — no throw, no blank — with a control frame mapping correctly in the same run. Nothing is stored and no reading is accepted. **The sibling module on the bridge side already uses an own-property guard for this exact hazard** |
| U8-10 | Functional | `tare-preset-queries.ts:27,42-46` | The saved-container read is capped and the "there are more" flag is discarded | confirmed (widened, narrowed) | #1050 | `degraded`. 105 seeded, 100 offered. **Worse than silence**: searching for a saved container announces **"No containers match …"** — a wrong statement about the user's own data. **Narrowed**: there is no total available, but the flag **is** already computed and thrown away, so the fix is one line. **#149 scoped itself to a named list of six plus one deliberate gap; this read was not considered and set aside, it was simply not looked at**, having landed a week earlier. Frequency stated plainly as rare — the set does not grow with routine use |
| U8-11 | Prompt | `scale-reading.ts:71,185`, `en.json:570`, `de.json:570` | A failure reason and its two catalog strings enumerate a table nothing ties them to | confirmed (**reclassified**) | #1051 | `cosmetic`. Not latent drift — the copy is **unreachable**: an earlier change removed the outcome from the bridge and never swept the app half, and the bridge's error-code list is typed, so emitting it is a compile error. **A test certifies the dead branch.** The fix is deletion, which dissolves the drift concern entirely. **The finder's supporting sub-claim was checked and is accurate today** — the shared factors are byte-identical and the extra units are a documented deliberate difference, so "make them equal" would be wrong. **The bridge half is Phase 13's**, and the published API description that restates the same list is *live* where this copy is dead |

### Phase 8 — Inventory components

Pinned SHA: `59ce7d19ad6ac3787e7dc06c54c3d9a74af29260`

Eleven finder units, splitting all 178 files in `src/features/inventory/components/` so that each was allocated to
exactly one unit (U1 item detail dialog and its presentation components; U2 item editor and the supplier, pricing
and operational sub-editors; U3 custom fields, typed controls, tags, capabilities, relations and media; U4 create
flow, category manager and pickers; U5 list, grouped list, table, rows and treemap; U6 cards, card fields, badges,
actions and the filter bars; U7 location sidebar, tree and detail cards; U8 location editors, dimensions, fullness
and photos; U9 labels, print, QR and regions; U10 gauge, quantity, weighing and tare; U11 bulk edit, import,
placements and reservations). Verification ran in **60 detached worktrees**, one per verifier, so a verifier's
deliberate mutations could not corrupt a concurrent run.

A session rate limit killed **23 verifiers mid-run**, two of them holding uncommitted source mutations
(`wt-p8v6/vite.worktree.config.ts`, `wt-p8v14/CustomFieldsEditor.tsx`). Because every verification tree was
detached at the pin and never merged, `main` was never at risk; all 60 trees were reset and the killed passes
re-run from clean, so no verdict rests on a partial transcript. One killed verifier had also written a Playwright
probe into the **primary checkout**, which was removed. Separately, one verifier tore down its dev server with a
command-line filter rather than a port filter and killed 14 `node` processes, taking out some siblings' servers;
four survived and the affected runs were re-driven.

The table carries **130 rows for 123 candidates**: seven candidates took different verdicts on different
limbs and are recorded once per limb. Two rows (`U5-9b`, `V18-c`) are findings a **verifier** raised while
disproving or proving something else, which no finder had named.

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1a | Functional | `ActivityLog.tsx:101-106` | The log's `aria-label` is dropped because the div is role-less | rejected | — | Chromium exposes it (`{"role":"generic","name":"Activity log"}`), and a visible `<h3>` with the same text sits directly above — redundant, no consequence |
| U1-1b/c | Functional | `ActivityLog.tsx:101-163` | No list semantics, and the box cannot be scrolled by keyboard | confirmed | #1100 | `degraded`. (c) was the finder's weakest limb and proved strongest: 14 Tab presses reach it in **neither Edge nor Firefox**, and PageDown/Arrow/End/Space leave `scrollTop` 0 against `scrollHeight` 1694, so entry 51+ never loads. Residue of closed #208, which swept `ItemList`/`GroupedItemList` only; the sibling `LocationActivityLog` gets it right |
| U1-2 | Functional | `ChangeFlash.tsx:26` | The glow plays on mount, not only on change | confirmed (mechanism RESHAPED, wider) | #1102 | `degraded`. Counted, not eyeballed: **19 pulses on first paint**, 14 per 900px scroll — **all on nodes that did not exist before it** — and 80/80 cells in Table view. **The finder's `getItemKey` mechanism is wrong**: `onExistingNodes: 0` disproves instance reuse. Reduced-motion control gives 0 |
| U1-3 | Functional | `ItemStockValue.tsx:23-31` | `aria-label` on a bare span is not exposed | rejected | — | **Disproved by measurement**: the CDP AX tree reports the name, with a control page proving the tool can report a missing one. The real narrow-width limb is **#546**, which names `ItemRow.tsx:120-121` verbatim |
| U1-4 | Prompt | `ItemDetailDialog.tsx:481-484` | A parity claim with no drift test | confirmed (latent) | #1122 | `cosmetic`. Mutation left 558 files / 8402 tests green. Sides agree today; filed as a sweep with U7-5 |
| U1-5 | Functional | `ItemDetailDialog.tsx:180` | The sub-title names sections the modules removed | confirmed | #1124 | `cosmetic`. Widened: the category axis reaches it too, so **one** module off is enough. `docs/wiki/Modular-UI.md:54-56` promises tabs disappear "everywhere at once" |
| U1-6 | Mechanical | `ItemDetailDialog.tsx:163,617` | Sections keyed by display title remount the Lifecycle editor | confirmed (WIDENED) | #1078 | **`data-loss`**. The finder could not find a single-device path; the verifier traced one — **change the item's Category in the same dialog**, via an optimistic write. Third surviving route of closed #576 |
| U1-7 | Mechanical | `ActivityLog.tsx:55-60,115` | A hidden panel's zero-rect poisons the virtualiser | confirmed (RESHAPED, engine-specific) | #1101 | `degraded`. The named disproof **fails** (no zero-rect guard in the installed library) but **the finder's consequence is also wrong** — React unmounts every row in the same commit. **Chromium: rejected**, byte-identical over 5 cycles. **Firefox: confirmed** — +53px per visit, and an unrequested page fetch by cycle 3 |
| U1-8 | Functional | `ItemDetailDialog.tsx:842-856` | An InfoHint inside `<h3>` pollutes the heading name | confirmed | #1103 | `cosmetic`. Merged with U2-3. The heading half is the **documented convention**, so the issue asks for a decision rather than a fix |
| U2-1 | Mechanical | `OperationalMetadataEditor.tsx:37-49` | `dirty` gated on validity: Save dead, button says "Saved", error branch dead code, Escape discards silently | confirmed (4 limbs) | #1076 | **`data-loss`**. No other route to `save()`, so the `role="alert"` block is unreachable. Widened into a 3-site sweep |
| U2-2a | Mechanical | `ItemDetailsEditor.tsx:105-154` | 16 setters run when any one of 16 values changes | confirmed | #1077 | **`data-loss`**. Two in-dialog triggers, one of them (**"Fill from a database"**) the finder missed. An ordinary write is *not* a trigger — structural sharing makes it inert. Residue of closed #158 |
| U2-2b | Prompt | `ItemDetailsEditor.tsx:108-113` | The comment asserts the opposite | rejected | — | The comment claims per-*editor* granularity and delivers it. Incomplete, not false |
| U2-3 | Functional | `ItemDetailsEditor.tsx:533-546` | InfoHint inside a `<label>` pollutes the checkbox name | confirmed (WIDENED 1→6) | #1103 | `degraded`. **Plus: the `field.test.tsx` guard cannot fail** — `getByLabelText` matches label text, not the computed name, so the mutation it exists to catch left all 12 green. Two sites are immune and must not be "fixed" |
| U2-4 | Functional | `SupplierPartFormDialog.tsx:105-125` | Eight draft fields, never reported | confirmed | #1075 | **`data-loss`**. Confirmed topmost in a real stack |
| U2-5a | Functional | `RevaluationEditor.tsx:55-64` | Three draft fields, never reported | confirmed | #1075 | `degraded`. `AssetEditor`'s own `dirty` covers only its four fields, so the parent cannot see them |
| U2-5b | Functional | `RevaluationEditor.tsx:60-64` | The effect clears the draft on any peer write | **rejected as stated; reshaped** | comment on #815 | The effect already keys on scalars. The headline trigger is **unreachable** — sync is manual-only, the bridge is read-only. Reshape: re-recording the **same** figure never clears and re-arms Record |
| U2-6 | Functional | `SupplierPartFormDialog.tsx:42-54,159` | A malformed price-break line is silently dropped | confirmed (WIDENED) | #1071 | **`data-loss`**. History proves it is residue, not a decision: `730e93e5` fixed the identical posture for four siblings and its diff starts one line below `parseBreaks`; the test came 11 weeks later as characterisation. **Second shape: `100:` stores unitCost 0**, so orders ≥100 price at £0.00 |
| U2-7a | Functional | `RefreshPricesButton.tsx:85-135,236-252` | Only the cheapest is reported | confirmed (WIDENED, narrowed) | #1094 | `degraded`. `RefreshPlan.skipped` and `RefreshSummary.noPriceCount` have **zero consumers anywhere in the app**. NARROWED: nothing wrong is persisted. Failures are invisible elsewhere too — the table shows the stale price identically |
| U2-7b | Mechanical | `RefreshPricesButton.test.tsx:210` | The settle path is untested | confirmed | #1094 | Unconditional `throw`s in `finalize` and `showSummary` left 6/6 and 4/4 green. `update.mutate` — the component's only write — is never exercised |
| U2-8 | Functional | `ReorderPointEditor.tsx:61-64` | No upper bound, and the top-up field is never checked | confirmed (NARROWED + WIDENED twice) | #1132, #1076 | **`wrong-data`**. **Widened**: retyping a stored top-up as `2,5` writes **`null`** and the write **succeeds**; and a refused save wipes the entry *and* the policy choice. NARROWED: `Math.trunc` hits the point/top-up only. Corrects #1045's framing |
| U2-9 | Functional | `SupplierPartsTable.tsx:184-192` | Removing a supplier leaves no preferred one | confirmed (heavily RESHAPED) | #1070, comment on #1002 | **`data-loss`**. **The finder's limb-1 mechanism is wrong** — the siblings cited are *demotions*; nothing promotes, and at-least-one is not an invariant. **Auto-promotion measured and is NOT a safe fix.** The strongest limb is the one the brief said to doubt: the unconfirmed bin **cascades and destroys the whole price history**. Limb 2 wider (the reorder plan loses supplier, cost, pack size, MOQ) |
| U2-10 | Performance | `SupplierPartsTable.tsx:170` | One history query per supplier row | confirmed (RESHAPED, narrowed) | #1113 | `degraded`. **N² confirmed exactly**: 10 suppliers → **100** round trips, dedupe absorbs none. **CORRECTION — the per-write fan-out is documented deliberate design**; only N writes in one action is the defect. The unwindowed-`.map` limb **rejected** |
| U2-11 | Mechanical | `OperationalMetadataEditor.tsx:74` | `key={index}` re-binds inputs on removal | confirmed (RESHAPED) | #1076 | `degraded`. **Three of the finder's four consequences do not reproduce** (focus, undo, aria). What does: keyboard Enter keeps the focused **destructive** control alive with an unchanged name while it retargets, so a second press deletes a row the user did not choose |
| U2-12 | Functional | `DeadStockEditor.tsx:41-43` | A hand-rolled plural reads "1 days" | confirmed (WIDENED) | #1119 | `cosmetic`. 1 is reachable — both inputs are `min={1}` and the app advertises "Range 1–3650 days". Second site: `ReportsScreen.tsx:486`. Outside #225 **by construction** — no plural is attempted |
| U2-13 | Functional | `DeadStockEditor.tsx:60-95` | The note resolves the saved mode, not the draft | confirmed (NARROWED) | #1123 | `cosmetic`. The picker does **not** save on change, so the candidate survives. Docstring limb dropped as too weak. Mitigation recorded: the Save label flips and the unsaved guard arms |
| U3-1 | Functional | `InheritableFieldControl.tsx:93` | The inherited value is spliced in as its raw storage string | confirmed (NARROWED per type, +1) | #1090 | **`unusable`** for IMAGE (a 30,048-char base64 payload becomes the `<Select>` option text *and* a polite live-region announcement); `degraded` for BOOLEAN/ON_OFF/NUMBER. **DATE dropped** (the shared seam also returns raw ISO); **COLOUR dropped** (readable). **NUMBER added by the verifier** — unit and precision both lost |
| U3-2 | Functional | `InheritableFieldControl.tsx:78-90` | A stranded inherit intent renders an empty control | confirmed (RESHAPED) | #1091 | `wrong-data`. **"Uncorrectable" is FALSE** — typing and clearing writes `null`. The real defect: the editor shows blank while every other surface resolves the **category default**, which `docs/wiki/Custom-Fields-and-Capabilities.md:334-336` states as the contract. Scope limit: only where a default exists |
| U3-3 | Mechanical | `CustomFieldsEditor.tsx:31-41` | The re-seed keys on array identity | confirmed (mechanism RESHAPED, NARROWED) | #1077 | **`data-loss`**. Finder's "~40 write sites" **disproven** — structural sharing makes a same-payload sweep inert. Three single-user triggers, no sync, including **the editor's own in-flight Save**. Guard disarmed, verified through a real `Modal` |
| U3-4 | Functional | `TypedFieldControl.tsx:78-88` | A NUMBER field never receives its range or precision | confirmed (RESHAPED + WIDENED 1→3) | #1106 | `degraded`. **Not a broken promise** — the catalogue promises only a save-time refusal, honoured for both. A control-semantics gap, proven by the `RATING` branch in the same component doing it correctly. Inverse residue of closed #676 |
| U3-5a | Functional | `CapabilityEditor.tsx:22-31` | A non-numeric weight is silently defaulted to 1 | confirmed | #1083 | **`wrong-data`**. **The InfoHint is not the defect — the weight is live**: it is the `match_score` the default ordering and every bridge search sorts on. Two orderings pasted; **the ordering flips**. The editor suppresses an error path that already works |
| U3-5b | Functional | `CapabilityEditor.tsx:127-129` | Add with a blank field is inert | confirmed | comment on #553 | `degraded`. Button not disabled, no `role="alert"` in the file, Enter wired on Value but not Key. **Inverse of #553** |
| U3-6 | Functional | seven facet editors | Explicit-commit drafts, never reported | confirmed 7/8 | #1075 | **`data-loss`** (2 sites) / `degraded` (5). `TagEditor` **rejected** — a half-typed token would raise the prompt on a stray keystroke. **Contradicts two shipped documents**: #576's close comment and `docs/wiki/Items.md:87-90` |
| U3-7a | Functional | `TestRecordsEditor.tsx:137-141` | The summary region is inserted with its first message | confirmed (NARROWED) | #1107 | `cosmetic`. Only the first record is affected, and a correctly-wired region announces at the same moment |
| U3-7b | Functional | `TestRecordsEditor.tsx:310-313` | `isSuccess` latches, so later adds announce nothing | **rejected** | — | **The finder's mechanism is wrong**: a plain `useMutation` returns to pending and back, so the second add produces **two** commits. The region also correctly pre-exists |
| U3-8 | Functional | `ImageManager.tsx:59-67` | An `sr-only` file input is an invisible tab stop | confirmed (WIDENED 1→5) | #1104 | `degraded`. Label computed styles **identical** focused and unfocused. A PNG-hash diff was **discarded as evidence** after a noise-floor control. Two correct patterns exist in the same codebase |
| U3-9 | Functional | `ActiveTabScrapeListener.tsx:52-54` | A push is swallowed with the module off | confirmed (RESHAPED + NARROWED) | #1111 | `degraded`. The silent *ignore* **is** documented intent; the payload never being cleared and the error toast going with it are not. **Load-bearing new fact: the extension does not gate itself.** #664's docstring is precedent *for* it. NARROWED — the queue is not persisted, so it re-surfaces within one session |
| U3-10 | Functional | `CustomFieldsEditor.tsx:128-137` | The default badge is not gated on the draft | confirmed (NARROWED twice) | #1133 | `cosmetic` at the floor. Of three assertions only the verb is false; it is a hover-gated tooltip, not visible text; it self-corrects on save. The case for filing is the internal inconsistency with the gated sibling nine lines below |
| U3-11 | Functional | `CustomFieldsEditor.tsx:175`, `TestRecordsEditor.tsx:143` | Hand-rolled plurals in converted files | **rejected** | comment on #225 | **The premise is factually wrong**: `TestRecordsEditor` does not use `useT` at all. Neither is wrong in English — the guards mean n ≥ 1. #231 names both files |
| U4-1 | Functional | `CategoryManagerDialog.tsx:396-403` | The field ✕ deletes every item's stored value, unconfirmed | confirmed | #1069 | **`data-loss`**. Verified against a real database, including that **re-adding the field does not restore** (same `def_id`, no values). Tombstoned, so it syncs. Blast radius 250/250 items tested. Project posture (#588's close comment) is explicitly confirmation-not-undo, live at six sites; the wiki never mentions field removal |
| U4-2 | Prompt | `CreateItemDialog.tsx:750-769` | The docstring says the raw message is shown | confirmed (narrowed) | comment on #1018 | `cosmetic`. `useErrorMessage` suppresses it for the SQLite class the docstring names as its example. Residue of #311 |
| U4-3 | Mechanical | `CreateItemDialog.test.tsx:743` | `not.toBeNull()` cannot fail on an omitted key | confirmed | comment on #1018, #1121 | `degraded`. Mutation proof both ways; the correct idiom is used two lines above for the same shape of claim |
| U4-4 | Functional | four create dialogs | Drafts discarded on Escape | confirmed | #1073 | **`data-loss`**. `EditLocationDialog` **already computes `dirty`** and uses it to relabel its dismiss button — the dialog knows and discards anyway. #576 never scoped the create dialogs out |
| U4-5 | Functional | `CreateItemDialog.tsx:914` | Initial quantity silently rewritten | confirmed | #1088 | `wrong-data`. `1,250` creates an item with **0 on hand**, with no `aria-invalid` either. NARROWED: the `abc` path is unreachable — letters are stripped as typed. `count` in the same schema is refined and reported |
| U4-6 | Functional | `CategoryRepository.create`, two dialogs | Duplicate category names unguarded | confirmed (RESHAPED) | #1089 | `degraded` + `wrong-data` tail. Every other flat natural key has UNIQUE + fold; categories is also the only one absent from the sync unique-key specs. Consequences proved: NL search returns half the items; the catalogue CSV regresses to UUIDs, reinstating #596. No rename exists. Manager list *is* weakly distinguishable — narrows the finder |
| U4-7 | Mechanical | `CategoryPresetPicker.tsx:162-167` | The raw thrown message is appended ungated | confirmed (RESHAPED) | #1125 | `degraded`. The site bypasses the **whole** error-copy precedence, losing a translated actionable sentence — not just the gate. One-line fix leaves all 13 shipped tests green. The guard's blind spelling is **exactly one site** |
| U4-8 | Performance | `CategoryManagerDialog.tsx:566-588` | A write and a broad invalidation per keystroke | confirmed (cost limb only) | #1134 | `degraded`. **7 DB round trips per character**, 1.58 s of exclusive worker occupancy to name one tab. **Two limbs rejected**: no characters are lost (this box re-seeds only on `category.id`), and "each keystroke is a sync row" is **false** — sync is a whole-snapshot payload |
| U4-9 | Functional | `CategoryPresetPicker.tsx:389-407` | A failed import settles on "Added" and disables the row | **rejected** | comment on #715 | **#692's close comment names this outcome verbatim** as scoped out, and #715's rollback question dissolves it. **The finder's strongest limb is false** — deleting the half-built category re-enables the row |
| U4-10 | Functional | `CategoryManagerDialog.tsx:1674-1724` | Typed Choice options silently discarded | **rejected** | comment on #715 | #715 names `options` discarded on reuse verbatim and raises the warn-vs-discard question. Comment adds the hand-entry ingress and that the item editor then **refuses the value the user defined** |
| U4-11 | Functional | `CreateLocationDialog.tsx:102` | Capacity passes the gate and stores NULL | confirmed | #1088 | `wrong-data`. **The fix exists one file away** — `EditLocationDialog` defines `capacityValid`, gates Save and renders the error, with the key shipped in **both** catalogues. Residue of closed #345 |
| U4-12 | Functional | `CategoryManagerDialog.tsx:1108-1121` | An invalid warranty sits in the box indefinitely | **rejected as stated; reshaped** | #1093 | Both settling questions came back against it: the box **is** `aria-invalid`, and it does **not** persist (switching category restores it). Real defect at the same site: `parseInt` stores **5** for `5.9` while the box shows `5.9` |
| U4-13 | Mechanical | `CreateItemDialog.tsx:853-867` | `done()` and `handleClose()` have drifted | **rejected** | — | **No consequence by construction** — all four call sites mount only while open, so `onClose()` discards everything. The `dialog-history` route was checked. `setOcrOpen(false)` inside `done()` is itself dead code |
| U5-1 | Functional | `item-table-columns.ts:76-84` | The table's fr tracks collapse to zero | confirmed (WIDER) | #1066 | **`unusable`**. Driven in a real browser plus a true 320px phone context: cells at **0 px**, scroller 288/332, header 44 px out of register. **Threshold corrected to ≤392 CSS px**, so every mainstream phone fails. Both halves of the comment measured false. WCAG 1.4.10 + 1.4.4 |
| U5-2 | Functional | `GroupedItemList.tsx:309-326` | A failed section read renders "No items here" | confirmed | #1079 | `wrong-data`. Forced through a real rejection, not a mocked `isError` |
| U5-3 | Functional | `ValueTreemapView.tsx:55-79` | A failed valuation renders the empty state | confirmed | #1079 | `wrong-data`, and worse — it **prescribes a remedy** for a condition that does not exist. **WIDENED 2 → 8 sites**: #306's fix sits at the tail of a ternary whose earlier arms are the map/treemap/grouped forks, which run the *same* `queryFn` |
| U5-4 | Functional | `GroupedItemList.tsx:180-182` | The header count disagrees with the filtered body | **count claim rejected; narrower defect confirmed** | #1118 | `cosmetic`. "What lives here" is the intended reading — the header renders `N / capacity`, matches the sidebar, and the wiki says so twice. Confirmed instead: the empty note blames the **search** for every facet |
| U5-5 | Functional | `ItemRow.tsx:120`, `ItemTable.tsx:199` | The select label is English in three of five view modes | **rejected** | comment on #230 | Demonstrated, but **#213 names `ItemCard.tsx:174` verbatim** with this symptom and the WCAG 2.5.3 framing; #230 scopes the other two |
| U5-6 | Mechanical | `ItemTable.test.tsx:104-110` | `toBeGreaterThan(0)` cannot fail | confirmed | #1121 | `cosmetic`. Survives deleting the Name track **and** the whole field loop. The count is wrong too (8 for 5), and the comment's stated reason is itself false |
| U5-7 | Mechanical | `ItemGalleryTile.test.tsx:146`, `ItemCompactRow.test.tsx:146` | Both assert against an empty document | confirmed | #1121 | `cosmetic`. A scan of every test file found **only these two** genuine (seven look-alikes render via a helper) — one copy-paste from a single commit |
| U5-10 | Mechanical | `ItemList.tsx:224-255` | Table rows reconciled by position | confirmed (WIDENED) | #1081 | **`wrong-data`**. The finder found no path; the verifier traced a complete one — **saving does not close the dialog**, the list re-sorts by name, the row holds a different item. Because facet editors re-seed on *value* not identity, and most values are `null`, Save writes A's draft to **B** |
| U6-1 | Functional | `InventoryFilterBar.tsx:124-136` | A gated-off active filter keeps filtering with no chip | confirmed (WIDENED to 2 sites) | #1080 | `wrong-data`. Driven in the real app: **still narrowed 1 of 5**, chip gone, Clear gone, bar gone, sidebar reading "All items 5". Browser-Back reproduces it. Contradicts **its own comment** and `docs/wiki/Modular-UI.md:53-57`. Second site: `InventoryFacetBar.tsx:91` |
| U6-2 | Functional | `InventoryFilterBar.tsx:28-34` | The chip count ignores search and facets | **rejected** | — | Facts reproduced (chip says 3, returns 1), but the location-only scope is documented in **three** places, one calling a stale count "only cosmetic", and view-scoping it would undo shipped fix #166. The finder's "hide direction is safe" concession is itself wrong for `includeInactive` |
| U6-3 | Functional | `useCardClickAction.ts:11-15` | "Show label" is a dead click without `labels:print` | confirmed | #1095 | `degraded`. Three of eight shipped roles lack the grant, `/settings` is ungated, and the preference **survives sign-out**, so it is inherited on a shared device. Only `qr` is affected |
| U6-4 | Functional | `ItemActions.tsx:204-212` | An ellipsis promises a dialog; no undo; the hint is unreachable | **rejected** | — | Limb (c) is **#515**, and #515's own "onFocus never fires" is disproved (`focusin` bubbles). Limb (b) dead — the conversion is freely reversible via the Tracking select. Limb (a) survives as a one-character copy nit |
| U6-5 | Mechanical | `ItemActions.tsx:311` | `checkout` is the one dialog with no guard | confirmed (latent) | #1128 | `cosmetic`. **Zero reachable callers** — the imperative handle accepts only details/move/qr. The larger half: the comment is false for **every** guard it defends |
| U6-6 | Prompt | `ItemActions.test.tsx:5-6` | The comment describes the pre-#162 shape | confirmed (reshaped) | comment on #1018 | `cosmetic`. No test guards the on-demand mounting (mutation proof). **The finder's `useUndoItemChanges` half is wrong** — it reaches the module via `useUndoToast`. A different stale line found at `:24-25` |
| U6-7 | Mechanical | `CardFieldsSetting.tsx:30-34` | The picker persists the reconciled value | confirmed (RESHAPED) | #1085 | `wrong-data`. Pending race narrow; **the error path is unbounded and deterministic**. Correction: the store's merge does **not** normalise `cardFields`, so the loss is permanent. Second defect at the site: no error state at all |
| U6-8 | Performance | `ItemCardFields.tsx:76-79` | An IMAGE field decodes a 512px bitmap into a 32px slot | confirmed (mechanism WRONG, waste real) | #1114, comment on #560 | `degraded`. 170.3 ms decode and a long task in **7 of 7 runs** vs 14.2 ms and none at 150 px; ratio **12.0×**. **Three corrections**: the decode is **not** on the main thread, `decoding="async"` is **not** the fix, and recycling does **not** re-decode. Realistic image 65.7–71.1 KB, not the 512 KB ceiling. Second, deterministic defect: a 16→32 px reflow |
| U6-9 | Functional | `CardBadge.tsx:38-45` | The money badge is unlabelled | confirmed (narrowed) | #1108 | `degraded`. Accessible trees byte-identical across unitPrice/totalValue, and the class strings too. Overlaps #515, but its fix would not reach this — `aria-describedby` is a description, not a name |
| U7-1 | Functional | `LocationRowActions.tsx:44-86` | The row actions have no keyboard path | confirmed (WIDENED) | #1065 | **`unusable`**. 28 keys tried in jsdom **and** a real browser; the sidebar exposes 3 tab stops and neither button is among them. **Archive/Restore has no other route in the app.** Correction: `F2` *is* bound, but to an inline rename that is itself dead (#916). #916 is a sibling, not a duplicate; closed #258 assumed keyboard was the fallback |
| U7-2 | Functional | `LocationTreeItem.tsx:168` | The row's name suppresses its count and volume | confirmed (RESHAPED) | #1109 | `degraded`. **Widened**: the default-location marker is suppressed too, so deleting the label is **not** the fix. **Narrowed**: descendants are not pruned, so browse mode still reaches them |
| U7-3 | Functional | `LocationStats.tsx:96-102` | An over-full location reads exactly 100% | confirmed | #1092 | `wrong-data`, narrow. **Correction**: the existing test's comment is accurate; its assertion never looks at the number. **Stronger evidence**: the Statistics tab is the odd one out of **three** surfaces |
| U7-4 | Functional | `LocationInfoCard.tsx:74,115` | Two bare-breakpoint hides #546 omits | confirmed | comment on #546 | `degraded`. **Widened to four** — `ItemCompactRow.tsx:123` (Compact density's **only** field) and `HygieneChecklist.tsx:55`. So #546 lists 5 of at least 9, which makes its own guard the load-bearing half |
| U7-5 | Prompt | `LocationStats.tsx:22-23` | A parity claim with no drift test | confirmed; behaviour **duplicate of #787** | #1122, comment on #787 | `cosmetic`. Reachability easier than assumed — the live route is the Variants panel, not `useSetParent` (no UI caller). **Narrowed**: the gauge clause is inert, so the *entire* divergence is `NOT EXISTS(child…)`. New: a **fourth surface #787 does not list** |
| U7-6 | Functional | `LocationMapView.tsx:99` | "1 sub-locations" | confirmed | #1120 | `degraded`. Self-contradiction in one expression — `:96` gets it right. **Outside #225 by construction**; #234 asks for translation, this is wrong English. Reachable and **not** behind a flag. Invisible today: the string is `aria-label`-only, the component has **no test file**, and the one screen test stubs it out |
| U7-7 | Functional | `LocationMapView.tsx:50-59` | Drilling drops focus to the body | confirmed | #1068 | **`unusable`** for keyboard/AT. Reachability settled first: the map is shipped and ungated. `Crumb` swaps its button for a span when current, destroying its own focus target |
| U7-8 | Performance | `LocationMapView.tsx:109` | Four layout properties animated per tile | **rejected as stated; reshaped** | comment on #112 | **The headline mechanism is wrong — a drill does not run the transition**: tiles are keyed by id and the post-drill set is disjoint, so **zero tile nodes survive**. Resize and refetch remain, measured at **8.0×** style+layout, but paid only while dragging a window edge |
| U7-9 | Mechanical | `SubLocationNav.tsx:126-143` | A hand-rolled lift, and a dead chevron animation | confirmed | comment on #211 | `cosmetic`. `Surface` never emits `group`, so the card path's `group-hover:` is dead — **which corrects #211**, since it reasons *from* that chevron. Also flagged: #211's primary claim may be partly stale |
| U7-10 | Functional | `LocationSidebar.tsx:392-416` | The move cue is stale and never re-announces | confirmed | #1107 | `degraded`. **Limb 1 is not AT-dependent**: after a completed move the toast says "Moved…" while the region still says "Moving…", a false statement standing for the session. No `onError` either. The shipped test **pins the stale string as correct** |
| U7-11 | Mechanical | `LocationStats.test.tsx:143-148` | The spinner test asserts nothing | confirmed | #1121 | `degraded` (maintainer-facing). Passes when the component renders **nothing at all**. Worse: deleting the file's only `role="alert"` breaks nothing across all 172 inventory test files |
| U7-12 | Mechanical | `src/lib/hover-reveal-touch.test.ts:35` | The guard's regex misses several reveal shapes | **rejected (duplicate of #957 item 4)** | comment on #957 | Hole demonstrated, but #957 names this line and regex. **Live-offender sweep: none.** Two new facts contributed: `group-hover:scale-*` is **already live at two sites**, and named groups plus `peer-hover:` are equally invisible |
| U8-1 | Mechanical | `EditLocationDialog.tsx:106-261` | The whole location draft, never reported | confirmed | #1073 | **`data-loss`**. It **already computes `dirty`** and swaps its dismiss label to "Cancel" because *"Cancel implies discarding work"* — then discards it |
| U8-2 | Mechanical | `LocationFieldsEditor.tsx:213-261`, `LocationPhotoManager.tsx:147-151` | Blur-only commits lost to Escape | confirmed (NARROWED) | #1074 | **`data-loss`**. **The finder's mechanism is wrong** — `blur` and `focusout` *do* fire on removal and reach `document.body`; React's delegated dispatch resolves no handler because the fiber is being deleted. **NARROWED to Escape only** — the ✕ and backdrop move focus first, so those values are kept |
| U8-3 | Functional | `EditLocationDialog.tsx:401-405`, `LocationFieldsEditor.tsx:137-154` | InfoHint inside a label | confirmed | #1103 | Folded into the six-site sweep with U2-3 |
| U8-4 | Functional | `LocationDimensionsFields.tsx:146-157` | The live region is mounted with its first message | confirmed (NARROWED) | #1107 | `cosmetic`. Only the first appearance is at risk; later edits mutate an existing region |
| U8-5 | Functional | `LocationFieldsEditor.tsx:42-44` | "Saving…" is shown while a read is in flight | confirmed | #1099 | `cosmetic`. That `<p>` is the panel's entire output at that moment. Reachable on opening any non-selected location. Both catalogues carry the same wrong word |
| U8-6 | Functional | `LocationFieldsEditor.tsx:18-19,59` | The copy claims nested locations inherit | confirmed | #1117 | `cosmetic`, bordering `degraded`. Repository-level proof. **The counter-reading is rejected on grammar**: the German dative plural `den … Orten` is a *recipient* and cannot carry the elided-noun sense. The wiki says the true thing differently |
| U8-7 | Functional | `EditLocationDialog.tsx:170-172` | A capacity of `0` is stored and read as "no limit" | confirmed (WIDER) | #1116 | `degraded`. **Not a sentinel** — blank already is, `normaliseCapacity`'s docstring omits `0` from what collapses to NULL, and the schema uses `> 0` where zero is meaningless. **Widened three ways**: `CreateLocationDialog` has no validation at all; it survives export; the bridge publishes a null ratio |
| U8-8 | Prompt | `EditLocationDialog.test.tsx:6-18` | The docstring claims it pins the whole surface | confirmed (WIDER) | #1121 | `cosmetic`. Deleting the `photos` tab left 46/46 green; deleting `stats` and `history` too — **three of four tabs** — still 46/46. Its "flat `Modal`" clause is false as well |
| U8-9 | Functional | `parent-options.ts:26-29,44` | An English-only row and a hand-rolled plural | **rejected** | comment on #234 | `plural()` is literally #225's enumeration and `EditLocationDialog` is named in #234; nothing is wrong in English. The German transcript contributed, including that the German help text says *"Lassen Sie **Oberste Ebene**"* — naming a row that does not exist |
| U8-10 | Functional | `LocationDimensionsFields.tsx:69-70` | An overflowing volume renders `∞` while the gauge vanishes | **rejected** | — | **The stated mechanism does not occur**: `formatVolume` opens with a finite check and returns `—`, so the preview and the gauge **agree**. Threshold is 5.6e96 km per side; no plausible unit slip is within eighty orders of magnitude |
| U8-11 | Functional | `LocationAdvancedVolumeFields.tsx:78-96` | The disclosure toggle silently refuses while invalid | **rejected** | — | The comment names this control, this condition and the consequence of the alternative, so it is the recorded decision. `aria-expanded="true"` is **accurate** throughout — nothing is misreported |
| U9-1 | Functional | `PrintLocationLabelDialog.tsx:181-202` | A QR-less location label prints with no warning | confirmed | #1096 | `degraded`. Residue of closed #329, whose close comment names three surfaces that explain an empty QR. **Worse**: a location link is 4 bytes longer than an item one and Settings measures an *item* URL, so in a 4-byte band **no warning fires anywhere** |
| U9-2 | Functional | `SheetLayoutControls.tsx:123-177` | A custom sheet can exceed the printable page | confirmed (mechanism CORRECTED, WIDENED) | #1084 | `wrong-data`. Real print emulation: a 215 mm grid in a 190 mm box, one page, so the last column is **never printed**. **Correction**: the cell floor hides it only on the columns axis. **Widened — the trigger is gentle**: 20 rows with a 10 mm gutter already spills |
| U9-3 | Performance | `PrintLabelsDialog.tsx:90` | The closed dialog re-encodes every selected QR per tap | confirmed | #1112 | `degraded`. A/B against a one-word control over 60 taps: **120 encodes vs 0**, per-tap 420→895 ms (linear) vs 304→376 ms (flat), **+17.8 s** long-task time. Sharpened: a user with the Labels module **off** still pays it |
| U9-4 | Performance | `PrintLabelsDialog.tsx:245-259` | The preview grid is unwindowed and unmemoised | confirmed (mechanism RESHAPED) | #1112 | `degraded`. 1,581 ms to open, 785 ms per field toggle at 60 selected. **The candidate's attribution is wrong** — the QR strings are byte-identical so React skips the DOM write. **New**: the open effect re-seeds `template`, so the encode is paid **again** on open |
| U9-5 | Mechanical | `RegionEditorDialog.tsx:454,515-531` | A blur-only region rename lost to Escape | confirmed | #1074 | **`data-loss`**. The box is auto-focused *and* auto-selected on create, so typing a name and pressing Escape is the expected flow |
| U9-6 | Functional | `RegionEditorDialog.tsx:515-519` | A blank name is refused silently | confirmed | #1098 | `degraded`. Every channel silent; the panel shows `"   "` while the list shows the old name. The existing test pins the silence. WCAG 3.3.1 |
| U9-7 | Functional | `QrCodeDialog.tsx:152-158` | Download SVG saves only the QR | confirmed | #1097 | `degraded`. **The brief's `download.ts` suspicion is disproved** — it already uses the shared seam, with a comment naming #646. The only defect is `qr ?? barcode` |
| U9-8 | Functional | `PrintLabelsDialog.tsx:147-169` | The size and symbology banners carry no role | **rejected** | — | **The finder read the wrong block.** `banner.tsx:89` destructures `role = "status"`; `:21-35` is the `cva` styling block. Runtime confirms all three carry it — the WCAG 4.1.3 mechanism. The "inconsistency" is the correct alert-vs-status distinction |
| U9-9 | Prompt | `LabelCellPreview.tsx:11-13` | The docstring describes a die-cut-only path | confirmed (widened) | comment on #1018 | `cosmetic`. Both callers always pass `size`; a throw-probe left 800 files / 12,448 tests green. `size?`, the undefined style arm and four Tailwind branches are all dead |
| U9-10 | Functional | `RegionEditorDialog.tsx:114,338` | An identical announcement mutates nothing | confirmed (RESHAPED) | #1107 | `degraded`. **The select path the finder led with is the weak limb**; the strong one is **delete** — two *different* regions sharing a name, only the first announced. The collision is the default state, since `defaultName` is fixed and nothing uniquifies |
| U9-11 | Prompt | `LabelSizeControls.tsx:98` | The notice names an untranslated option label | **rejected** | — | The two **agree exactly today**, so there is no consequence. Trigger misattributed (#682 is the Modules screen). The "mirrors X" rule needs a *comment* claiming parity; there is none |
| U10-1 | Mechanical | `WeighCountDialog.tsx:205-233` | Two children drop FormField's error ARIA | confirmed (NARROWED) | #1105 | `degraded`. Git provenance verified — one commit lifted the *tare* note out and added the comment, leaving the gross one. **Narrowed**: a sweep found four multi-child FormFields; the other three own no errors, so this is the **only** site that has both |
| U10-2 | Functional | `GaugeAdjustDialog.tsx:54-55` | `parseFloat` accepts a truncated prefix | confirmed (NARROWED) | #1086 | `wrong-data`. `1,250`→1, `1.2.5`→1.2, applied to stock, with no error slot. **NARROWED**: `40+3` is not reachable via Apply (pointer-down blurs and commits first). Unrepaired residue of #676 |
| U10-3 | Mechanical | `GaugeAdjustDialog.tsx:177` | Enter reads state the control has not flushed | confirmed | #1086 | `wrong-data`. `500/2`: the box settles to **250**, the ledger gets **500**. `WeighCountDialog` documents and fixes both hazards |
| U10-4 | Functional | `GaugeConfigEditor.tsx:103-109` | Validity-gated `dirty` reports an unsaved draft as clean | confirmed (NARROWED + widened) | #1076 | **`data-loss`**. Narrowed: needs *every* edited field unusable. Widened: reaches tare and attrition, and the shipped test's `'0'`/`'-1'` cases already sit on `dirty === false` |
| U10-5 | Functional | `QuantityStepper.tsx:68-71` | Escape propagates to the enclosing frame | confirmed (WIDENED) | comment on #1052 | **`wrong-data`** in the palette, `unusable` in the scanner. **Worse than claimed — Escape silently COMMITS the abandoned quantity as a stock adjustment**: `dismiss()` → `closeActions()` → `focus()` fires the still-mounted input's `onBlur={commit}` |
| U10-6 | Functional | `QuantityStepper.tsx:41-62` | A rejected entry is discarded with no message | confirmed (RESHAPED) | #1135 | `degraded`. **Finder wrong** that it re-implements the calculator — it imports the seam. Load-bearing half exact: three silent returns, no error slot, no ARIA. `12.7` saves 12 |
| U10-7 | Functional | `GaugeAdjustDialog.tsx:74,85` | Weigh-in previews and logs an unclamped delta | confirmed (NARROWED) | #1087 | `wrong-data`. **Narrowed**: `gaugeAfterDelta` clamps in SQL, so the stored delta is correct and there is **no replay corruption**. The defects are the preview (+450 approved, +200 applied) and the note contradicting its own row |
| U10-8 | Functional | `gauge.ts:70-73,82-85` | "Fill to full" writes a 17-digit float | confirmed (WIDENED) | #1130 | `degraded`. **Two corrections**: the gauge *does* reach capacity, and **the button is not the root cause** — `refillDelta` re-introduces the noise on any top-off from a fractional net, demonstrated with a user typing a plain `38` |
| U10-9 | Prompt | `WeighCountDialog.tsx:101-103` | A whole-codebase parity claim that is false | confirmed | #1086 | `cosmetic`. Filed as a rider on U10-2 — actioning it makes the comment true by construction (rule 1), so it is not filed independently |
| U10-10 | Prompt | `WeighCountDialog.tsx:152-154` | The comment inverts the mounting contract | confirmed (reshaped) | comment on #1018 | `cosmetic`. #162 inverted it the day after #122 wrote it. The four `setState` calls are dead, not just the comment |
| U10-11 | Functional | `measure-draft.ts:26-32` | A thousands space is rejected here and accepted there | confirmed | #1129 | `degraded`. One element reports `aria-valuenow=1000` **and** `aria-invalid=true`. **The rejection is not intended** — the control's reader documents the opposite decision and keeps the space deliberately. Mitigating: the error copy does say "without separators" |
| U11-1 | Functional | `ImportDataDialog.tsx:1017-1041` | A roving tabindex with no arrow keys | confirmed (RESHAPED) | #1067 | **`unusable`**. 40 Tab presses cycle three stops. **Both named disproofs refuted at source** — no delegated handler exists, and the focus trap explicitly excludes `tabindex="-1"` with its docstring naming this case. No other route; the OS launch makes it symmetric |
| U11-2 | Functional | `ImportDataDialog.tsx:966-1006` | The largest draft in the app, cleared on close | confirmed | #1073 | **`data-loss`**. Reopened with the pasted CSV at length 0 |
| U11-3 | Performance | `ImportDataDialog.tsx:413-483` | Every keystroke re-indexes the whole catalogue | **rejected** | comment on #1001 | Not a textual duplicate of #1001, but **fails on measurement**: 2.57 ms at the 10,000-item ceiling, while the already-filed location limb is **~50× larger**. The ratio does not grow from 5k to 10k, so 75% of the cost is present with an empty catalogue |
| U11-4 | Performance | `ImportDataDialog.tsx:90-151` | The catalogue is paged serially on every open | confirmed (WIDENED) | #1115 | `degraded`. **93 sequential round trips**, nothing cached, and **60 of 93 landed after Escape**. Residue of closed #527, whose keyset walk and batched read landed in the **export path only** |
| U11-5 | Functional | `ImportDataDialog.tsx:727-744` | An assertive region rebuilt per keystroke | confirmed | #1131 | `degraded`. **The finder's disproof fails** — 10 distinct changes over 17 keystrokes, plus two unmount/remounts. Graded on structure; no real screen reader was driven, and the issue says so |
| U11-6 | Mechanical | `MoveItemDialog.tsx:55-68` | The confirmation and Undo are lost on dismissal | confirmed (WIDENED) | #1110 | `degraded`. **Reachability stated honestly** — not a race: the strict-FIFO worker, no global pending indicator, and a fully-enabled Cancel. Residue of closed #654, whose audit could not reach a dialog with no busy flag |
| U11-7 | Functional | `BulkEditDialog.tsx:134-144` | The Location control looks disabled but is not | confirmed | #1082 | **`wrong-data`**. Its sibling reports `aria-disabled="true"` **in the same render**; this one keeps `tabindex="0"`, accepts a location, **displays** it, and the spec omits it. Not #221 — that is the inverse |
| U11-8 | Mechanical | `BulkEditDialog.tsx:71-84` | A derived dep wipes the form | **rejected (zero reachability)** | — | Mechanism confirmed, but **the head location can never move**: system rows sort first and cannot be renamed, archived or deleted — `assertMutable` throws and DB triggers abort it. Narrowed from LOW to zero |
| U11-9 | Functional | `BulkEditDialog.tsx:231` | A hand-rolled 4px label gap | confirmed | #1126 | `cosmetic`. **A guard exists and this slips through it** — its regex needs a literal control within 400 chars and `FieldRow` renders `{children}`. Residue of closed #668, missed for that reason |
| U11-10 | Functional | `InventoryScreen.tsx:277` | The selection is never reconciled with the filter | **rejected** | — | **Documented as intended** at `:272-275`: the selection "spans filter changes". The review half fails for 2 of 3 consumers. A hard-deleted item is counted into `failed` and reported |
| U11-11 | Mechanical | `location-media.ts:237-265` | A placement move is two writes with no transaction | **rejected (duplicate of #844)** | comment on #844 | Demonstrated before the issue was found; #844 is verbatim. The finder's #633/#696 exclusion is **correct**. Residue contributed: `useSetItemPlacement` has no `onError`, so the report rides an unmountable observer |
| U11-12 | Mechanical | `PlacementPickerDialog.tsx:283-302` | A second definition of the option-card tint | confirmed | #1127 | `cosmetic`. Retinting the SSOT moved every real consumer and left this **byte-identical**. The counter-evidence fails: the region editor it cites has **no selection tint at all** |
| U11-13 | Functional | `ItemReservationsPanel.tsx:100-106` | The unbacked tooltip is unreachable by keyboard | **rejected** | comment on #515 | The mechanical half is true, but **the premise fails**: any visible unbacked badge implies a `role="alert"` is already on screen, saying more. Also not distinct — 13 passive sites share the shape, and #515 scopes itself by its grep |
| U5-8 | Performance | `GroupedItemList.tsx:697-712` | Two forced layouts per open section per scroll frame | **rejected** | — | **Disproved by measurement**: `LayoutCount` is **59 at K=8 and 59 at K=1**, `LayoutDuration` ratio **0.88**, zero forced-reflow violations. Instrumented reads scale 4x in count and **1.05x in time** (~36 ms per 17.7 s gesture) because nothing writes between them. A neutered-`measure` mutant was discarded as invalid. Residual K-scaling is the 7 extra virtualizers, not the reads. Premise narrowed: the threshold compares **loaded** items, so K=12 needs twelve deliberate page-ins |
| U5-9 | Functional | `GroupedItemList.tsx:697-712` | A lower section positions rows against a stale scroll margin | **rejected** | — | **Premise inverted**: section B re-rendered **38 times** during its sibling's page-in and the margin corrected in the **same frame**; `emptyViewportPx` 0 throughout. The depless `useLayoutEffect` U5-8 calls waste is what holds it correct. Ruled out idle, wheel, resize and `overflow-anchor: none` as the rescue. **Mechanism impossible as stated** — `start` already includes `scrollMargin`, so the subtraction cancels. Limit: a ~40-60 px `scrollAdjustments` shift could not be eliminated, but no painted frame was stale. The `:697-712` docstring is muddled in two of three examples yet operatively correct, so not filed |
| U5-9b | Functional | `GroupedItemList.tsx:336`, `:294-307` | Paging a section in throws the reader's place away | confirmed (**found by the verifier while disproving U5-9**) | #1136 | `degraded`. The plain-DOM → virtualiser switch resets the shared scroller **7486 → 0**, observed twice, with the element proven not remounted; later page-ins do not. Second limb: no anchoring against growth above, ~2960 px of movement. Neither needs a click — the pager button **is** the 200 px sentinel. **The comment above the switch claims the opposite.** Inverse of #1063 |
| V18-c | Mechanical | `foundry/unsaved-changes.ts`, `BookingEditDialog.tsx:55`, `BookingCheckoutDialog.tsx:41` | The seam itself: a component that renders its own `Modal` sits above the provider, so the hook is a no-op there | confirmed (**found by the verifier while proving the sweep's fix**) | #1072 | `degraded`, and it is why the obvious fix for U4-4 / U8-1 / U2-4 does not work. **Two shipped guards are already phantoms** — both call the hook from above their own `Modal`, so a reviewer grepping for the hook would score them done. Second limb at every site: the footer Cancel/Close wires straight to `onClose`, bypassing `requestClose`, and `Modal` exposes no seam to reach it |

### Phase 9 — Commerce, projects and reporting

Pinned SHA: `f1965c417e029a82f5bf1ec9529acf1ae0513814`

Eight finder units, splitting all 279 files in the nine Phase 9 directories so that each was allocated to exactly
one unit (U1 purchasing; U2 suppliers and sales; U3 projects; U4 lifecycle; U5 reports — the pure analytics half;
U6 reports — the screens, print and components; U7 dashboard; U8 export and import). Verification ran in **58
detached worktrees**, one per verifier, so a verifier's deliberate mutations could not corrupt a concurrent run.

The register carries **83 rows** for **80 candidates**: three candidates were split into limbs that took different
verdicts (U3-2, U4-3, U7-10), and **four rows were raised by verifiers that no finder had named** (U1-10, U4-6b,
U7-new, U8-7b).

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Mechanical | `features/purchasing/queries.ts:340-357` | A PO line return is the one PO write that does not sweep the on-order caches | confirmed | #1137 | `wrong-data`. Key roots printed: `onOrderKeys.all` is `["on-order"]`, matched by no prefix `invalidateItems` sweeps. A live observer showed 0 against a repository answer of 4, and a remount inside the 30 s `staleTime` issued **no** query at all. The doc-comment's parity claim is a CLAUDE.md "mirrors X" case with no test |
| U1-2 | Functional | `features/purchasing/ReorderTab.tsx:43-52`, `reorder-plan.ts:58-78` | A quantity override is not re-priced against the supplier's price breaks | confirmed | #1138 | `wrong-data`. All four surfaces wrong in **both** directions, and the DRAFT PO **persists** `unit_cost 0.10` on a 500-unit line whose part records 0.07. `ReorderPlanLine` carries no `priceBreaks`, so the tab structurally cannot re-price. #37's closing comment claims the fixed behaviour |
| U1-3 | Functional | `features/purchasing/queries.ts:378-387` | "Create draft PO" reports nothing and a second press duplicates the order | confirmed (narrowed) | #1139 | `degraded`. **The screen-reader limb is disproved** — a `MutationObserver` recorded `["", "Draft…", "", "Draft…"]`, so the region does re-announce. Narrowed to the sighted user. Strengthened instead: a DRAFT is excluded from the on-order predicate, so **no** cache sweep could ever supply feedback. A missed #389 call site |
| U1-4 | Functional | `features/purchasing/components/ReturnLineDialog.tsx:13-21,58,75-85` | The return dialog promises stock movement and collects a discarded location | confirmed (narrowed) | #1140 | `degraded`. **Wiki limb dropped** — `Purchase-Orders.md:142-144` is about the ledger entry and its claim is met. Proved by returning through a location holding **none** of the item: the guard never fires and the location appears nowhere in what was written. Residue of #608; the only purchasing dialog with no test file |
| U1-5 | Mechanical | `features/purchasing/purchase-list-queries.ts:156-163` | A wishlist import launders every write failure into "couldn't be read" and clears the paste | confirmed | #1141 | `degraded`. Reproduced for a Hard Stop **and** a denied permission. The `skipped` field's own docstring says "**written**" while both catalogs say "read". The loop hammers the suspended path 20 times. Removing the `catch` left 297 tests green, so nothing pins the swallow. Outside the #350 row-problems seam |
| U1-6 | Functional | `features/purchasing/components/ImportPurchaseListDialog.tsx:360` | The import preview prices in base currency, the write records the order's | confirmed (widened) | #1142 | `wrong-data`. Widened twice: the **new-order** destination is affected too, *because of* #569's supplier inheritance; and under a 0dp currency the **digits** differ (`£1.50` previewed, `JP¥2` recorded). Narrowed: the wishlist destination is correct (no currency column). #285's diff never touched this file |
| U1-7 | Mechanical | `features/purchasing/ReorderTab.tsx:184,249,278` | Three rendered prices bypass the Foundry `Money` control | confirmed | #1143 | `cosmetic`. Measured 3 prices, **0** carrying the `money-symbol` token; #934 is an open contrast complaint about that tint, so it renders visibly. Invisible to §7's grep because `f.currency` arrives as a prop. #479 is the precedent that this is fileable |
| U1-8 | Mechanical | `features/purchasing/ReorderTab.tsx:109-111` | The reorder export hand-rolls its filename, dropping the `gubbins-` prefix | confirmed | #1143 | `cosmetic`. Folded with U1-7 — one file, one screen, two seam bypasses. 11 adapters use the helper, including `po-export.ts` on the same screen. The helper's docstring asserts universality and is already false |
| U1-9 | Performance + Mechanical | `features/purchasing/purchase-list-queries.ts:43-50,58-77` | The purchase-list import is an unbatched N+1 outside any transaction | confirmed (re-based) | #1144 | `wrong-data` (partial write) + `degraded` (cost). **Re-based: the named mechanism is 11% of the cost.** 7 round trips per line, not 9; **94% is 400 unindexable `item_aliases` scans** — 200 lines/5k aliases = 640 ms desktop / 2.6 s at 4×, 20k aliases = 13 s. Sharper limb: a mid-list failure left **14 of 20** lines on the order while the toast said the import failed. Distinguished from the register's C4-3 (that one pipelines; this awaits every call). This is §11's Phase 2 alias carry-over arriving from the **read** side |
| U1-10 | Functional | `features/purchasing/ReorderTab.tsx:270` | The reorder Qty box discards a typed calculation on Enter | confirmed | #1149 | `degraded`. **Raised by the U2-2/U2-5 verifier, named by no finder.** Third site of the family after #1086 and U2-5; filed with U2-5 |
| U2-1 | Functional | `features/sales/components/OutboundSourceFields.tsx:32,38-40,78` | One non-home placement shows full stock and refuses every draw as "0 on hand" | confirmed (widened) | #1145 | `unusable`. **Widened to `CheckoutDialog.tsx:148-156,226` — Loan out is broken identically.** Reachable with **no transfer at all**: receive a delivery into a non-home location. The dialog says "10 on hand", shows no picker, and reports "0 on hand" about a location never chosen. Distinct from #784 (same root state, opposite symptom, different path) |
| U2-2 | Mechanical | `features/sales/components/SellDialog.tsx:69`, `WriteOffDialog.tsx:62` | Enter is not gated by the in-flight mutation, so stock is drawn twice | confirmed | #1146 | `wrong-data`. Two Enters → two mutations, with the **button already disabled** at the second press. No repository dedupe: 5 → 3 with two `SOLD` rows. The latch is shipped twice already (`CheckoutDialog`, and `CreateItemDialog` for #294), both with comments naming this hazard |
| U2-3 | Functional | `catalogs/en.json:1115`, `de.json:1115`, `Suppliers.md:64`, `types/suppliers.ts:30` | The supplier default currency is never applied to a part, contradicting its own hint | confirmed (widened) | #1147 | `wrong-data`. **The defect is the copy, in FOUR places — the finder found three.** Measured: `unitCostAtSale 9800`, valuation £29,400 with `unpricedItemCount 0`, `foreignCurrencyCostCount 0`, margin −£9,680 with `unitsWithoutCost 0`. **The "or order" half is also false** for a hand-raised PO (a blank field sends an explicit null; only an omitted key inherits) |
| U2-4 | Functional | `features/sales/components/SellDialog.tsx:73-80`, `WriteOffDialog.tsx:22` | "No cost recorded" for an item priced by its preferred supplier part | confirmed | #1148 | `degraded`. Ledger books £4 and the report counts £8 while the dialog denies a cost; a manual-cost control renders correctly. **Beside #1037, not inside it** — cost-of-goods precedence (two sources) vs valuation precedence (four), different seam and fix. Trap recorded for the fixer: a naive client-side `effectiveUnitCost` drops #687's base-currency guard and inverts the defect |
| U2-5 | Functional | `features/sales/components/SellDialog.tsx:69`, `number-input.tsx:214-216` | Enter after a calculator sum runs the sum and then rejects the sale | confirmed | #1149 | `degraded`. Box reads `15` under "Enter a sale price (0 or more)", no mutation. **Closure capture, not batching** — so not a jsdom artefact. The seam's contract is pinned by `number-input.test.tsx:75-86` and **no call site in `src/` checks `defaultPrevented`**. Same mechanism as open #1086, where `parseFloat` makes it `wrong-data` |
| U2-6 | Functional | `features/sales/components/OutboundSourceFields.tsx:42` | Quantity floors at 1 for a zero-stock item, so the action always fails | confirmed (re-based) | #1150 | `degraded`. **Re-based: the cause is the missing availability gate, not the floor** — `CheckoutDialog` computes `maxQty` differently and seeds 1 against 0 on hand too. Mutation proved the "mirrors CheckoutDialog" docstring has nothing behind it (3,148 neighbouring tests green with the predicate changed) |
| U2-7 | Functional | `features/suppliers/components/SupplierFormDialog.tsx:188-205` | Deleting a supplier destroys all price history and unprices items, undisclosed | confirmed | #1151 | `data-loss`. Measured: 6 price points destroyed, valuation 56 → 0, 2 items unpriced, the reorder line losing supplier, cost, MOQ and pack rounding (100 → 10), **no promotion** of the surviving part. "Both consequences" is asserted in **four** places including a committed test's name. New issue cross-referencing #1070 — different screen, repository, fix and doc page |
| U2-8 | Mechanical | `features/sales/components/SellDialog.tsx:56,101-103`, `sales.ts:22-35` | Cancel stays live in flight and the failure surfaces nowhere | confirmed | #1152 | `degraded`. **Mechanism corrected:** React Query never *invokes* `onError` on an unmounted observer, so the callback is dropped, not delivered to a dead component. The sale still commits (9 invalidations after dismissal). One defect, one fix (`busy`); the missing hook-level toast is **compliant** with #389's double-report rule. Widened to `CheckoutDialog`. Distinguished from #1110, whose mutation does report failures |
| U3-1 | Functional | `features/projects/components/ProjectDetail.tsx:324` | Project delete force-returns loans while the copy says stock is unaffected | confirmed (narrowed, re-based) | #1153 | `wrong-data`. Measured saw 1 → 4. **Limb (c) dropped:** the closed-loan cascade is documented as intended at `v1-initial.ts:1364-1370` and `item_history` keeps the readable record — filing it would restate a decision and overstate the severity. The two sibling delete dialogs (contacts, locations) both name the consequence |
| U3-2a | Functional | `catalogs/en.json:1583`, `BomLineTable.tsx:261-264`, `projects.ts:415` | The blocked-finalise message names a remedy no screen offers | confirmed | #1154 | `unusable` for that flow. Exhaustive grep: zero callers of `useUpdateBomLine` anywhere, and the Qty cell's only control is Remove. **Widened:** delete-and-re-add also **re-snapshots the unit cost at today's price** (12.5 → 40), which the finder missed. Register row C3-5 saw the absence from the repository side and filed it as a note |
| U3-2b | Functional | `features/projects/components/AddBomLineDialog.tsx:91`, `AddItemToProjectDialog.tsx:81` | Both add dialogs rewrite the typed quantity | confirmed | #1155 | `wrong-data`. Measured table: `0`→1, `2.7`→2, `1,250`→**1**, `1 000`→**1**. `0`/`2.7`/`-5` set `aria-invalid` **and save anyway**; the comma and space set nothing. Contradicts #350, the CHECK, `addLine`'s own clamp and the number-field note. `abc` is unreachable (stripped as typed) |
| U3-3 | Functional | `features/projects/bom-export.ts:64`, `project/costing.ts:23` | The BOM export ignores the project's costing mode | confirmed | #1156 | `wrong-data`. Screen 1.00 vs exported 0.40; **the `unpricedLineCount` verdict flips both ways** (priced-after exports blank; price-withdrawn exports an invented figure). A `POINT_IN_TIME` control agrees exactly, isolating the mode. Three of four project cost surfaces honour it; the export is the outlier |
| U3-4 | Functional | `features/projects/components/AddItemToProjectDialog.tsx:33,138-151` | Add-to-project offers a newest-first first-100 list with no notice | confirmed (narrowed, sharpened) | #1157 | `degraded`. `aria-setsize 101` against 131. **Two corrections:** the Select *does* offer a filter above 12 options — but it filters only the page in hand, so typing an existing project's exact name reports **"0 matches"**; and the DOM option count is not the offered count (virtualised). `ProjectPicker` is a drop-in. #484 is a partial fix, not a decision |
| U3-5 | Functional | `features/projects/components/AddItemToProjectDialog.tsx:36,113-131` | A failed project read renders "You have no projects yet" | confirmed | #1158 | `wrong-data`. **NOT inside #1079** — its eight sites are all `features/inventory/components/` with a cause specific to `InventoryScreen.tsx`'s ternary. Narrower than those: the dialog unmounts, so a transient failure self-heals on reopen; a latching one does not |
| U3-6 | Mechanical | `features/projects/components/AddBomLineDialog.tsx:77-83` | The form guard swallows the rejection its comment claims to report | confirmed (widened) | #1159 | `degraded`. **Widened: an entirely empty form behaves the same**, not just manufacturer-only. Mutation: deleting the guard makes the toast appear. **The `reset(values)` limb is inert** and dropped — zod has already passed, so there are no errors to clear |
| U3-9 | Mechanical | `features/projects/projects.ts:167-175`, `FinaliseAssemblyDialog.tsx:44,65` | Reopening Finalise paints the previous plan with the button live | **rejected** | — | **Disproved on the call count: 2, not 1.** TanStack's `shouldFetchOptionally` refetches on the `enabled: false → true` edge, so the docstring's promise holds. Residue is a millisecond-scale stale-while-revalidate frame (`listAssemblyParts` median **0.33 ms** at 200 parts) over a write that re-plans from live rows; `takeQty` cannot change, only `takesAll`. §11 hardening line at most |
| U3-10 | Functional | `features/projects/components/BudgetCategoryEditor.tsx:33-46` | A negative or unreadable budget allocation is silently created at zero | confirmed | #1160 | `wrong-data` (**raised** from the finder's `degraded`). Negative → stored 0 with `aria-invalid` set **and overridden**; `1,250` and `250,00` set nothing and store 0; the row then renders destructive over-budget (`budgetStatus` OVER above 1e-9). `ExpenseDialog` two files away is correct. The number-field note names this erasure shape (#675) |
| U3-11 | Functional | `features/projects/components/BudgetMeter.tsx:30-36` | Every budget meter is an unnamed `role="progressbar"` | confirmed (narrowed) | #1161 | `cosmetic` (finder guessed `degraded`). `computeAccessibleName` = `""` ×3; axe `aria-progressbar-name` **serious**. But every call site states the figures in adjacent text and the repo's canonical `Bar` is `aria-hidden` for exactly that reason, so the correct end state is probably *fewer* announcements. **Widened: `GaugeBar.tsx:34-41` has the same fault** (Phase 8 scope, missed). WCAG 4.1.2 is the wrong citation — the ARIA role definition is |
| U4-1 | Functional | `features/lifecycle/useLocationCycleCount.ts:151,183,186` | The count sheet reads its own field with `Number()`, so the whole location fails | confirmed (widened) | #1162 | `unusable`. Measured matrix: `1 000`, `1,250`, `1.2.3` → `NaN` with **no** `aria-invalid`; `2.5`, `-3` → plausible chips with an `aria-invalid` that has **no visual treatment anywhere in the app**. All five reach `authoriseCount`; nothing written, shelf not stamped, message names no row. **Residue of #676**, which named this code and converted only `WeighCountDialog`. Two wiki claims are false here. 28 files read a number field with a bare coercion; one imports the parser |
| U4-2 | Functional | `features/lifecycle/useLocationCycleCount.ts:121`, `item/stock.ts:237-243` | An on-loan serialised unit is on the presence sheet with no loan state | confirmed | #1163 | `wrong-data`. Measured: `is_active 0` + a `−1 RECONCILED` entry while the checkout stays OPEN, and the returned unit is then **unlendable** (#661's guard). The DISCRETE contrast is real (a bulk loan draws the placement down). Recorded in-repo as unbuilt task **I1**, which records the false-missing but **not** the retirement, the ledger row or the unlendable state |
| U4-3a | Functional | `features/lifecycle/maintenance.ts:71,146-150` | The overdue figure is `ceil`, and is stamped into the append-only ledger | confirmed | #1164 | `wrong-data`. 1 ms late prints "1 day(s) overdue"; verified end to end into `item_history`, and **no production path edits a note**. Not a boundary artefact — wrong for every non-exact-multiple span (25 h reads "2 days"). The seam's own docstring says "whole days" |
| U4-3b | Functional | `features/lifecycle/maintenance.ts:71` | A spring-forward makes a 30-day schedule read "29 days left" | **rejected** | — | Documented at `calendar-days.ts:20-23` and in the DST memory note: duration→count division by `MS_PER_DAY` is **correct**, and the note names `idleDays`/`remainingDays` explicitly. A no-DST control shows "29 on the day it is created" happens with **no DST at all**; the hour shifts the tick-over once, by one hour |
| U4-4 | Functional | `features/lifecycle/CycleCountContext.tsx:161-171`, `FoundHereField.tsx:75-78` | A restored draft re-admits a found row without the `items:write` check | confirmed | #1165 | `degraded`. `useSignOut` clears session, authority, cache and device credentials — **not** the zustand stores, so the drafts map is device-wide and actor-agnostic. Technician is `stock:write` without `items:write`. Authorisation refused atomically; recoverable only by removing the row, which nothing points at. Precedent #521, same class on a different key. The guard itself has **no test** |
| U4-5 | Functional | `db/repositories/MaintenanceRepository.ts:154-155` | `listDue` sorts every USAGE schedule ahead of every TIME one | confirmed (re-based) | #1166 | `degraded`. **Re-based off the 100-row cap onto the 3-row widget**: `listRowCount` is 3 at the default size, so *three* due usage schedules hide a 400-day-overdue calibration. Three artefacts assert the opposite (the docstring, the wiki, #606's body). `listUpcoming` three lines below uses the correct two-key form. Folds in C5-8's missing tiebreak |
| U4-6 | Performance | `features/lifecycle/components/FoundHereField.tsx:58-62`, `useLocationCycleCount.ts:143-170` | Five O(n) derivations per keystroke on the count sheet | **rejected** | — | **Disproved by measurement.** Variant E — *every* named cost removed — measured **28.46 ms against a 28.38 ms baseline** at n=4000/3600 entered, inside a ±3 ms spread. Isolated on an empty sheet the derivations are **5.08 ms**; at n=1000 the whole keystroke is 9.45 ms. The `ItemPicker` memo it invalidates filters ≤20 rows and never appears in the profile. Squarely the band this audit has rejected three times (U6-6, U11-3, U10-C4) |
| U4-6b | Performance | `features/lifecycle/CycleCountContext.tsx:228-233`, `count-draft.ts:74-111` | The draft mirror rebuilds, deep-compares and re-serialises the whole sheet per keystroke | confirmed | #1168 | `degraded`. **Found by the U4-6 verifier while disproving it**, and **4.8× the cost the finder attributed to the derivations** (206 ms vs 43 ms over 20 keystrokes). Baseline 28.38–34.54 ms vs 14.04–16.01 ms with the mirror off, at 4× on a production build; 85.7 KiB written per character. Honest caveats recorded: O(**entered** lines, and the per-keystroke *cadence* is documented — the whole-sheet rebuild is not |
| U4-7 | Functional | `features/lifecycle/components/KitEditor.tsx:309,312-319` | Emptying a kit quantity rewrites the recipe to 1 and leaves the box lying | confirmed | #1167 | `wrong-data`. Headline moves 5 → 10 kits; `-5` and `0` likewise; the box never resyncs from props. **Sub-claim disproved:** `2.5` over a stored 2 restores correctly — it misbehaves only when the coercion *changes* the stored value. The number-field note forbids exactly this, and `QuantityStepper` one file over does it correctly |
| U4-8 | Mechanical | `features/lifecycle/components/CycleCountDialog.tsx:159,239` | `void authorise()` lets a failed authorisation escape unhandled | **rejected** | — | Rejection is real (observed) but **nobody suffers**: `onError: reportFailure` already shows the authored toast, the dialog stays open and the draft is kept. `void p` is the sanctioned idiom at **77 sites across 45 files** (`no-floating-promises` with `ignoreVoid`). #315's closing comment is the precedent — it was actioned only because a *user-visible* limb rode along |
| U5-1 | Functional | `db/repositories/ReportRepository.ts:2176` | Spend analytics inner-joins suppliers, so a deleted supplier's spend vanishes | confirmed | #1169 | `wrong-data`. **The brief's rejection route did not close**: the pinned test at `:2297-2318` uses a **USD** order, so its `total === 0` is over-determined and still passes under the fix. Measured £500 → £0 with `excludedForeignCurrency 0`. **Regression traced to `f537ef04` (#384)**, which added the join for the name only while making every sibling join LEFT. The finder's excluded-count reasoning is **inverted** — `:2268` must also become LEFT. Contradicted by the schema comment, the delete dialog and two wiki pages |
| U5-2 | Functional | `features/reports/abc-analysis.ts:150-169` | The tier is taken after the item, so the last priced item is always C | **rejected** | — | Recorded in the plan the feature shipped from (`advanced-analytics_2026-06-30.md:61-63`, `:190-191`), pinned by a named test, and self-correcting by ~8 consuming items. **Mutation: the symmetric tail guard yields `['A','B','B','C','B']` — it inverts the ranking.** The head guard's comment reasons only about the head, correctly. The wiki describes the convention rather than contradicting it |
| U5-3 | Functional | `db/repositories/ReportRepository.ts:1745-1754,1791-1802` | ABC and turnover value every gauge's consumption at zero and include it | confirmed (widened) | #1170 | `wrong-data`. **Widened: it corrupts the other items too** — ABC's denominator halves, the screws report class A where a valued spool puts them at B, and class B comes back empty. **Two independent zeros**: the missing `net_value_delta` arm *and* the cost axis (a gauge is deliberately never priced from `unit_cost`). #683's closing comment names the residue under "Not covered"; #571 later edited both queries without closing it. Wiki contradicted |
| U5-4 | Functional | `features/reports/stock-aging.ts:160` | The open-ended bucket is labelled `180+ days` but starts at 181 | **rejected** | — | Mechanism real, **no consequence**: one production call site and no caller passes custom bounds; ranges are contiguous, exhaustive and correctly counted; and the labels always render adjacent, so `180+` can only mean "over 180". No CSV consumer can be misled into a wrong number. Hardening note only |
| U5-5 | Mechanical | `features/reports/data-hygiene.ts:175-179` | The duplicate-MPN check folds with `.toLowerCase()` rather than `foldName` | confirmed (re-based) | #1171 | `degraded`. **Re-based: the fold limb is the weak half** — `.toLowerCase()` is full Unicode, so ordinary accented case agrees, and `aliases.ts:181-184` (#679) records a decision *not* to fold MPN. **The real defect is that the key ignores the manufacturer**: two different parts sharing an MPN read "2 of 2 items need attention" with no way to clear it, contradicting both wiki pages and the checklist's own omit-what-cannot-be-fixed rule |
| U5-6 | Functional | `features/reports/sales-analytics.ts:204-209` | An unpriced write-off is published as £0 of loss with no caveat | confirmed (re-based) | #1172 | `degraded`. **The load-bearing find is a broken parity claim**, not an omission: `item/stock.ts:472-474` declines a foreign price *on the strength of* a disclosure the report makes only for sales. The mixed case is worse than the all-unpriced one (£8.00 over 6 units reads as complete). #694 scoped itself out on a premise the render falsifies. No test covers a `cost: null` write-off |
| U5-7 | Functional | `features/reports/reports.ts:585`, `data-hygiene.ts:251` | Inclusion uses calendar days, the reported age uses fixed 24-hour days | **rejected** | — | Mechanism reproduces deterministically in `Europe/London` (89 against a threshold of 90, clean under UTC and `America/New_York`) and reaches the CSV — but the **disagreement window measures exactly one hour**, measured two ways, on the least-idle row, in DST zones, for spring-crossing spans only. And the DST memory note names `idleDays` explicitly as sanctioned duration→count division. Tension recorded: the same note applies the normalised form to `daysUntilExpiry` |
| U5-8 | Functional | `features/reports/schedule-export.ts:53` | The schedule export writes the screen's em-dash placeholder into a data cell | confirmed (re-based) | #1173 | `cosmetic`. **Re-based as a half-applied edit**: five of six optional columns are stripped to `null` and Warranty alone is not, because its placeholder lives in a shared label map rather than at the call site. XLSX proof: `I2` self-closing, `H2` an `inlineStr` holding U+2014 **on the same row**. Mutation proved the existing assertion hollow (8 green with a garbage label). **Secondary observation rejected** — a pure adapter importing UI label maps is the recorded house shape |
| U6-1 | Functional | `features/reports/InsuranceScheduleScreen.tsx:184-187`, `styles/index.css:3046-3049` | The print CSS hides both exclusion notices, so the paper's total omits stock unexplained | confirmed (widened) | #1174 | `wrong-data`. Proved in **real Chromium under `emulateMedia({media:'print'})`**, with a print-media screenshot and a PDF; the repository half confirmed separately (`grandTotal 150` excluding a JPY-priced item, notice count 1). **A #163 regression** — `900fc8ee` printed it, `2dc1784c` moved it, `1e9a8cb3` added the second notice into the broken place. `Insurance-and-Estate-Schedule.md:53` states the promise outright. The screen's test mocks both counts to 0 |
| U6-2 | Functional | `features/reports/ReportsScreen.tsx:414-544` | Eight panels render a failed read as an empty state | confirmed (narrowed) | #1175 | `wrong-data`. **Narrowed to 3 false facts + 5 blank panels**, which are different severities. The live-region cover is wider than the finder said (a second announcement covers the analytics block) but **both regions are `sr-only`** and neither names a panel. #306's body asserts "**Reports** … check it", which is false for two thirds of the screen. Mutation: adding two error arms leaves 23/23 green |
| U6-3 | Functional | `InsuranceScheduleScreen.tsx:189-201`, `CatalogueScreen.tsx:797-810` | Neither printable report checks its paged read's error state | confirmed | #1176 | `degraded`. TanStack state on a first-read rejection printed (`isPending:false, isError:true, data:undefined`), then both real screens rendered: a pager reading "1–50 of 300" above **zero rows**, under a correct US$2,000.00 total, with no alert. Mutation: adding an arm leaves 43/43 green. Narrowed: the pager only renders past one page |
| U6-4 | Mechanical | `features/reports/usePreparedDocumentPrint.ts:58-62,85-106` | A settings change nulls the document but does not abort the load | confirmed | #1177 | `wrong-data` (a) + `degraded` (b). **Widened on consequence:** the catalogue artefact *contradicts itself* — a "Full catalogue — 0 items" heading over a section claiming 1 item, 3 in stock and a £6.00 subtotal, with no rows. Controls confirmed the checkbox and the group-by picker are live while busy, and that Cancel *does* abort — so only the unmount path is missing. **The drop-on-change effect is near-dead code**: mid-flight it sets `null` over `null` |
| U6-5 | Functional | `SalesBreakdown.tsx:193`, `SpendBreakdown.tsx:159,177` | The "Other" row is scaled against the top row, not the largest | confirmed | #1178 | `degraded`. Measured widths: first and last bars both `100%` beside labels reading `16%` and `62%`. `TOP_N` is 6, so it fires from ~8 groups; "Other" carries no distinguishing tone. **Widened:** the "By source" list in the same panel scales to the total, so a £40 · 4% row draws twice the bar of a £110 · 12% one. `ValueBreakdown` has the correct form **and a test pinning it** |
| U6-6 | Functional | `features/reports/components/MovementChart.tsx:33,37-41` | The movement chart carries its data only in a hover `title` | confirmed (mechanism corrected) | #1179 | `degraded`. **The finder's mechanism is wrong and must not be asserted**: a CDP accessibility-tree dump shows Chrome *does* expose the `title` as a description. What survives, measured: the chart's entire text is `In 51Out 25` — no bucket date, no figure, neither window endpoint; it is the **only** graphic in the folder not `aria-hidden`; and **8 of 15 columns share a duplicate date label on the 7d window** (sub-day buckets, no time in the label). Both sibling strips print the endpoints from props this chart receives and ignores |
| U6-7 | Functional | `features/reports/components/HygieneChecklist.tsx:55` | A failing check's description hides on a bare `sm:` breakpoint | **rejected (duplicate)** | #546 | Already enumerated **twice** in #546's comments *and* already recorded in this register as Phase 8's **U7-4**, which widened #546 to four sites including this one. A re-find. The finder's repro is also wrong in the way #546 already corrected (`sm:` is `min-width`, so 640 px survives by one pixel). Full grep run: 9 information-bearing sites, **all 9 in #546** — the residue is zero |
| U6-8 | Functional | `InsuranceScheduleScreen.tsx:553-562,391-402` | Printed group headings come from the summary while their rows come from the load | **rejected** | — | Mechanism reproduces (and the **footer** is the worse limb — £40.00 over lines summing to £30.00), but there is **no deterministic path**: both reads share one time-independent filter, `includePhotos` touches only the SELECT, a second tab cannot write (the exclusive Web Lock fails closed), every in-tab write invalidates the summary, **and that invalidation is what drops the prepared document**. Needs a same-tab write during the few seconds between Print and the dialog, on a screen with no mutation control |
| U6-9 | Mechanical | `features/reports/ReportsScreen.test.tsx:102-222` | The test's key union omits `sales`, so three "set every query" helpers never set it | confirmed (narrowed) | #1180 | `degraded`. **The type-check limb is a recorded duplicate** (register U2-1, #601) and must not be re-filed; this stands on the coverage hole alone. **Proved by mutation: `SalesBreakdown` throwing unconditionally left `12419 passed`, 0 test failures.** A second mutation deleting the panel's body and its assertive live region also passed. Completing the helpers is **necessary but not sufficient** — proved. Five shuffled orders all pass, so it is a coverage hole, not a flaky suite |
| U6-10 | Functional | `features/reports/components/SalesBreakdown.tsx:207` | The write-off line hand-rolls its plural and skips the quantity formatter | **rejected (duplicate)** | #236 | **Two of the finder's three premises are false**: `formatters.quantity` appears nowhere in the file, and the file is not a converted screen. #236 names `components/SalesBreakdown.tsx` verbatim, and #225 wants `lib/plural.ts` retired — so the proposed remedy is itself scheduled for deletion. Register precedent: U8-9 and Phase 8's U3-11 both rejected this shape; U2-12 survived only because its English was *wrong*. The grouping limb is cured by #236's own conversion |
| U7-1 | Functional | `features/dashboard/widgets.tsx:540,546` | The Projects card counts "active" as not-archived while the nav tile counts not-terminal | confirmed (narrowed, widened) | #1181 | `wrong-data`. Measured: nav pill 2, card headline 5. **Three limbs disproved:** the card is titled "Project statuses" and does not claim "active" in its headline; the "No active projects" copy is *unreachable* while completed ones list; and the healthy probe's copy is deliberately kept in step with the card. **Widened:** the same line is also page-capped — 105 live projects render "100", and 101 with the newest 100 archived render **0**. The constant's docstring warns about this exact drift and the card does not import it |
| U7-2 | Functional | `features/dashboard/widgets.tsx:513` | The In-Transit card states its bounded page's row count as the total | confirmed (narrowed) | #1182 | `wrong-data`. 101 seeded, `rows.length` 100, `hasMore` true and discarded; the rendered card is a bare "100" with **no** hedge. **Narrowed:** it is a *feed* card by the app's own taxonomy, so the wiki's whole-inventory promise does not cover it — the defect is a page size presented as a headline beside four real totals. Reachability stated honestly: ~100 individual dropdown selections. #606's table names neither this card nor Projects |
| U7-3 | Mechanical | `features/inventory/queries.ts:337`, `invalidate.ts:51-76` | `inTransit()` is swept by only two mutations, so the widget goes stale | confirmed | comment on #714 | `wrong-data`. Key arrays printed; three real mutations (remove a BOM line, delete a project, rename an item) left the rendered card showing rows the repository confirms are gone. **Routed as a comment because the fix is literally #714's one line in the same function** — and because #714's proposed lane map is framed as *the alert-centre feeds*, so an implementer would rebuild the gap for this dashboard feed. Narrowed: no always-mounted reader, so the window is bounded by `staleTime` |
| U7-4 | Functional | `features/dashboard/widgets.tsx:557` | The Projects widget prints the raw status enum lowercased | confirmed (widened) | #1183 | `cosmetic`. Rendered under the real `de.json`: `"Projektstatus3Regalbauplanning…"` — German title, English statuses. **Widened: it is wrong in English too** (`planning` against the Projects screen's `Planning`), from a map a drift test holds byte-identical to the catalogue. The Dashboard is inside the converted slice by three independent sources, and the programme has a ticket for every feature directory **except** dashboard |
| U7-5 | Functional | `features/dashboard/useNavOrder.ts:143`, `DashboardNav.tsx:214-226` | The reorder announcement speaks the English registry label | confirmed (widened) | #1184 | `degraded`. Measured: button `Projekte nach oben verschieben`, live region `Projects an Position 1 von 5 in Arbeitsbereiche verschoben`. **Widened: wrong in English too** — the Purchasing tile announces "Purchase orders", falsifying the field's own docstring ("the tile's visible label"). The conversion commit touched only `DashboardNav.tsx` and named one deliberate exception, which is a different value. Distinguished from #224 (this *is* routed through the seam) and #953 (a different variable) |
| U7-6 | Mechanical | `features/dashboard/useBoardPointerDrag.tsx:308-317` | The unmount cleanup cancels neither the long-press timer nor the auto-scroll rAF | confirmed (widened) | #1185 | **`unusable`** — raised from the finder's `degraded` by a real browser drive. Reachable with a **shipped default hotkey** (F1) held mid-drag: after navigation and release the loop still ran **485 frames / 485 `scrollBy` calls per 2 s**, and both `scrollTo(0,0)` and a real wheel-up were undone within a frame — **the page cannot be scrolled until reload**. A release-first control went flat at 0. This hook is the app's only `scrollBy` caller. Materially worse than #1058, which does *not* abort its listeners and so self-heals |
| U7-7 | Functional | `features/dashboard/DashboardVersion.tsx:33-34` | The version badge reports "Up to date" whether the check ran, failed or was skipped | confirmed (narrowed) | #1186 | `degraded`. Five renders all land on the same copy. **Measured in real Chromium**: a 500, a 404 and a dead origin all reject, and a genuinely newer worker **resolves in 5 ms with the worker still `installing`**. Narrowed to the failure path — the success case self-corrects within seconds via `PwaUpdatePrompt`. The helper's own docstring states the caveat the call site drops. The `resetTimer` limb is **not** a finding (a no-op under React 19) |
| U7-8 | Performance | `features/dashboard/widgets.tsx:930-948` | The hide-healthy probe runs seven feeds with no feature/permission/visibility gating | confirmed (narrowed) | #1187 | `degraded`. Query counts in three configurations, with a control-within-a-control: in the gated board the widgets' own counts correctly vanish while **all seven probe reads fire with no subscriber**. Cost on the **shipped wasm engine**: 15.89 ms vs 10.84 ms at 5k items (1.47×), 43.15 ms vs 42.46 ms at 20k (1.02×) — ~50% of the screen's database time discarded. **The ACL/privacy limb is REJECTED**: read enforcement at the screen boundary is #522's recorded decision, and nothing renders or persists the rows. Residue of #522, which gated the alert lanes and left this probe |
| U7-9 | Mechanical | `features/dashboard/useWakeLock.ts:80-104` | Two overlapping syncs acquire two sentinels and leak the first | **rejected** | — | Mechanism real and the leak would matter (spec §11.5 checked, and a browser probe confirms two coexisting sentinels). But **the window is sub-millisecond**: 200 timed `wakeLock.request()` calls gave p50 0.0999 ms, p99 0.3 ms, max 0.5 ms, and the API neither prompts nor waits for a gesture. Three realistic sequences leak nothing. The untested `visibilitychange` limb does not stand alone — the behaviour works, and the docstring is not a parity claim |
| U7-10a | Functional | `features/contacts/overdue.ts:31`, `widgets.tsx:472` | The overdue badge is hardcoded English and hand-rolls its plural | **rejected (duplicate)** | comment on #241 | #241 names `overdue.ts` verbatim and #225 owns the plural. Register precedent: Phase 8's U3-11 rejected the same shape. Two residue facts posted to #241: its "also surfaced in Alerts" is **wrong** (it is the Calendar agenda), and it misses the Dashboard consumer entirely — which lands on a *converted* screen |
| U7-10b | Functional | `features/dashboard/widgets.tsx:314` | `reorder N` renders untranslated inside an otherwise-German row | confirmed | #1183 | `cosmetic`. Rendered: `"Niedriger Bestand1Schraube×2 · reorder 84 bestellt"` — the untranslated word sits in the **same span** as a correctly translated `4 bestellt`. Not a plural, so #225 does not reach it; no programme ticket owns `features/dashboard` |
| U7-11 | Functional | `features/dashboard/DashboardVersion.tsx:14-18` | The release date formats in the browser locale, computed once at module load | **rejected (duplicate)** | #228, #962 | Claim reproduced (`de-DE` badge `23 Aug 2026` against a sibling's `23. Aug. 2026`) — and already filed. A maintainer comment on #228 names `DashboardVersion.tsx:13-18` explicitly with the same mechanism, the same table and the same fix; the parity limb is #962 limb 4; and this register's Phase 5 row **U12-C11** already records it |
| U7-new | Functional | `features/dashboard/widgets.tsx:689`, `inventory/history-format.ts:17-50` | The Recent-activity widget prints English action titles on a German board | confirmed | #1183 | `cosmetic`. **Raised by the U7-4/U7-10 verifier while sweeping for the count, named by no finder.** Rendered: `"Letzte AktivitätSchraubeQuantity changedKameraChecked outRegalMoved"`. Structurally #682-shaped — an English-only registry consumed by a converted screen |
| U8-1 | Mechanical | `features/export/xlsx-export.ts:18-20,42-43,86` | C0 control characters go into the sheet XML, so the workbook is not well-formed | confirmed | #1188 | `unusable`. **Proved by parse-back**: Python's expat and openpyxl both refuse the produced package, on both the worksheet and the workbook parts. **Reachability proved end to end** from an ordinary CSV through the shipped import path (the reader whitelists U+000C by name, and nothing between the file and the column strips it). The existing test asserts only a `PK` prefix and one grep. Distinguished from #368, whose fix is scoped to the bridge's two escape helpers |
| U8-2 | Mechanical | `features/export/xlsx-export.ts:56-62` | `sheetName` truncates with a UTF-16 slice, leaving a lone surrogate | confirmed | #1188 | `cosmetic`. Encoded bytes read from the real adapters: both titles end `ef bf bd`. Both title sources are user data (a project name, an item name). **Residue of #346, not a regression** — the fix (`truncateByCodePoints`) landed a month after this file was written. Folded with U8-1: same module, and the sheet-name limb needs both a code-point cut *and* escaping |
| U8-3 | Functional | `features/export/export-data.ts:878,646-654` | The vault master note wiki-links raw names while notes are filed under sanitised ones | confirmed | #1189 | `degraded`. Six shapes measured; item names are unconstrained (trim, non-empty, 500-code-point cap, no character rule). Obsidian's own documentation settles the slash case — a `/` in a link is a **vault-root path**, and clicking creates a folder and an empty note. **The collision case has no visual cue at all** and silently opens the wrong component. The image embeds in the same export are consistent, so this is the one divergence. The double-space shape was **dropped** as unverifiable |
| U8-4 | Functional | `features/export/TabularExportMenu.tsx:19-27` | Every list-export menu renders its seven format names in hardcoded English | **rejected (duplicate)** | comment on #233 | Mechanism fully demonstrated (both option lists rendered side by side under `de`), and the memory note's file-content carve-out genuinely does not reach menu rows — but **#233's body names `TabularExportMenu` verbatim**. Register precedent: U10-C10 → comment on #232, Phase 8's U8-9 → comment on #234. Three residue facts posted: the component is now *partly* converted, this slice is **not** blocked by #233's sequencing caveat, and nothing pins the two format lists together |
| U8-5 | Mechanical | `features/export/export-every-page.ts:104-106` | `isoCalendarDay` throws where its twin deliberately does not | **rejected (duplicate)** | #924 | Mechanical claim true, and the `NaN`/`Infinity` limbs are **unreachable** (the create guard and a STRICT `INTEGER NOT NULL` both refuse; the date-input seam cannot produce one). The reachable band — finite, above 8.64e15, via a verbatim sync/restore upsert — is exactly #924, which names the same ingress, the same band, and already cites the sibling site as "a sharper illustration of the same gap". Register rows U5-3(a) and U6-3 already carry it. **Two corrections recorded:** the doc-comment is `isoTimestamp`'s own, not a module invariant, so the "mirrors X" rule has no trigger (as register U9-11 held) |
| U8-6 | Mechanical | `features/import/tabular.ts:159-161` | The codec discards a lone CR, so a CR-terminated file reads as one row | confirmed (re-based) | #1190 | `unusable` per file. **Re-based onto the disagreement**: the detector returns a clean 2-column CSV while the extractor returns one 4-cell row, producing an empty result with **no note** — and the note seam is populated for every other unreadable shape. Driven end to end in the real dialog (LF twin offers "Import 2 rows"); the purchase-list message "every row was blank" is **false**. Mutation: 3,498 tests green, so the swallow is untested. **Narrowed:** paste cannot produce it (a textarea normalises), so it is a picked/dropped/OS-launched file only. **The doc-comment limb is dropped** — `nonEmptyLines`' claim is scoped to itself and is true. The more likely near-miss (one stray CR) silently merges two rows |
| U8-7 | Mechanical | `features/import/tabular.ts:515-526` | `decodeHtmlEntities` passes an out-of-range code point to `String.fromCodePoint` | confirmed (widened) | #1191 | `unusable` for that input. **Widened on three axes:** `extractImport` carries the same false "Never throws" docstring; the inventory dialog's failure is **app-global Safe Mode** with a raw error and a purge button, not a banner, and the staged paste is lost; and the BOM/purchase-list dialogs mislabel it as a file-shape problem. **Narrowed:** a browser copy-paste cannot produce the entity, and it must sit in the first table's cells. `&#0;` and `&#xD800;` do not throw but inject a NUL or lone surrogate |
| U8-7b | Mechanical | `features/import/tabular.ts:524` | The named-entity branch is an unguarded record lookup | confirmed | #1191 | `cosmetic`. **Raised by the U8-7 verifier.** `&constructor;` decodes to `function Object() { [native code] }` and imports into the item's name; only `constructor` fires, because the regex is `[a-z]+` and every other prototype member has a capital. Same family as #1061/#1062, same one-line fix as U8-7, so folded |
| U8-8 | Functional | `features/export/export-data.ts:454-455,463-471` | A custom-field header is written unchecked against the importer's own fold | confirmed (widened) | #1192 | `data-loss`. **Widened well beyond the finder's framing:** the empty-key limb is not confined to `Ω`/`°C` — *any* name written wholly in a non-Latin script folds to `""`, so a user who names fields in their own language loses **every** custom-field column on **every** round-trip, with no clash needed. And `Tolerance` / `Tolerance (%)` is an ordinary deliberate pair needing no invisible character. A control with two ordinary names passes. `buildCatalogNameLookup` applies exactly this check to the location and category columns, with the reason written down. Not #920, #1012, #998 or #595 |
| U8-9 | Functional | `features/export/ExportWizard.tsx:371-376`, `run-export.ts:368-370` | "Include removed items" is hidden for the LOCATION scope but still applied to it | confirmed | #1193 | `wrong-data`. Both directions measured, plus the persistence leak (a tick on ALL survives the switch and reaches `runExport`). **Narrowed:** ITEM and PROJECT never read the filtered list, so the flag is structurally meaningless there and the gate was correct when written — the commit that added the LOCATION scope wired the flag through and did not widen it. LOCATION is the wizard's **default** from the Inventory sidebar, and its flag is a *separate* store from the list's "Show removed". Wiki promises "every item whose home is there" |
| U8-10 | Functional | `features/export/tabular-export.ts:153-168` | `toTextTable` pads by UTF-16 code units, so CJK and full-width misalign | confirmed (narrowed) | #1194 | `cosmetic`. Measured five distinct column starts where a fixed-width table has one. **Widened** to the `=` heading rule at `:309`, which is drawn from a user-derived title. **The divider claim is narrowed away** — every column header is an English literal constant, so header and divider always agree; only body rows drift. No seam is bypassed: the repo's code-point length helper is not display width, so this is new code. Clears the bar on the documented promise, not on aesthetics |
| U8-11 | Functional | `features/export/zip-in-worker.ts:52-55` | The vault-zip timeout copy offers a remedy the Export wizard has no control for | **rejected** | — | The comment's structure is deliberate **alternation**, and it says so — "the remedy names what each of them can actually leave out" — with the rejected single-remedy alternative named in the next clause. And the third caller never renders this string: the weekly archive substitutes its own authored copy with a **Try again** action, so both surfaces that do render it offer one of the two named levers. No consequence. Residue recorded: the comment's caller list is now stale |
| U3-7 | Performance | `features/projects/projects.ts:616-638`, `project/bom-lines.ts:82-103` | The BOM import is an unbatched N+1 behind a bare spinner | confirmed (re-based) | #1195 | `degraded`, at the boundary of `unusable`. **The finder's mechanism is disproved and re-based.** The counts are exact (6 RPCs/line on a miss, 7 on a hit) — but **in the real app a 500-line import takes 14.25 s**, i.e. 4.75 ms per call, ~100x P1-54's measured round trip, and 4x CPU throttling barely moves it (13.6 s). The cost is the **500 unbatched implicit commits**, one OPFS sync each — the cliff the `Import pipeline` note already records for the item importer. A batched mutation measured **28.9x faster** (14250 -> 493 ms). **The alias-scan limb is dropped as a re-find of the rejected P2-27.** Distinguished from C4-3/C4-6/P1-54/P2-18/L-7/C3-11: those are *read* N+1s at 0.0425 ms a trip; this is a *write* N+1 whose unit is a disk sync |
| U3-8 | Mechanical | `features/projects/projects.ts:624-636`, `ImportBomDialog.tsx:150-172,326` | A part-way BOM import failure keeps its rows and a retry duplicates them | confirmed (widened) | #1195 | `wrong-data`. **Widened: no mocked rejection was needed** — the parser keeps a row carrying only a manufacturer, which `addLine` refuses, so an ordinary CSV aborts mid-file with **zero row problems** in the preview. Measured 3 lines after the failure, **6 after the retry**; the create-from-BOM route leaves two half-filled projects of the same name. No unique index, no upsert, no match key. Residue of **#301**, which converted this family and left only one documented best-effort path — not this one. One fix closes U3-7 and U3-8 together |

### Phase 10 — Capture and discovery

Pinned SHA: `c36e822d31518a787b2598d756d0c00b59bf74f0`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| CO-1 | Performance | `src/App.tsx:11,72`, `features/inventory/components/ActiveTabScrapeListener.tsx:23` | The always-mounted scrape listener statically imports `CreateItemDialog`, so it rides the cold-start graph | confirmed | #1218 | The §11 carry-over from Phase 5, verified here. Its framing was wrong in both directions: the **entry chunk loses nothing** (+385 B for the lazy wrapper), and the eager set loses **129 KB raw / 41.7 KB gzip (7.6%)**, not the dialog's 83.5 KB — `zod` and ten smaller chunks leave with it. Two production builds; the reverted control reproduced the baseline byte-for-byte |
| U1-1 | Mechanical | `features/search/parse-text-query.ts:617-640,651-677` | `cap:`/`field:` never `unquote` the key, so a quoted name binds its quote characters | confirmed (widened) | #1196 | **The space is irrelevant** — `cap:"mykey"` breaks too, because the operator finders return `-1` on seeing a quote. And the unquoted spelling is not an alternative: `field:Voltage rating` lexes as two terms, so a multi-word custom field has **no working `field:` spelling at all**. 212 of 541 preset field definitions have a space in the name |
| U1-2 | Functional | `features/search/parse-text-query.ts:249-253,443` | A quoted value followed by an operator is not lexed as a quoted span | confirmed | #1196 | Filed with U1-1: the closer-set fix alone leaves `field:"Voltage rating"=5` hitting U1-1, so the two must be fixed together. Not `cap:`/`field:` specific — `name:"a b"=c` breaks identically. The fix was applied and all 263 existing tests, including every #625 regression case, stayed green |
| U1-3 | Functional | `features/search/fields.ts:54,121-144`, `db/search/parseASTtoSQL.ts:633-652` | The builder offers Tag × "has any value", which `translateTag` throws on | confirmed (widened) | #1197 | A drift test over all 27 builder fields × every offered operator found **exactly one** failure. Browser-driven: the list does not freeze or empty — `astActive` goes false and it **silently widens to the whole inventory** while the header asserts "3 items match" and the builder still shows the condition |
| U1-4 | Functional | `features/search/parse-text-query.ts:174-219` | `has:` silently answers for columns outside its table | confirmed (narrowed) | #1198 | **Limb (a) rejected** — the wiki documents the narrow list *and* the custom-field fallback, and `has:somethingRandom` takes the identical path, so it is the documented unknown-name behaviour. **Limb (b) confirmed and widened to three**: `has:active`, `has:tracking` and `has:deadstock` are all `NOT NULL` and all missed by the guard whose stated purpose is to name them |
| U1-5 | Functional | `features/search/nl-query.ts:530,554,611-624` | A second location name is not consumed and leaks into the residual text match | confirmed (widened) | #1199 | `matchCategories` has the identical leak, and two negated locations drop the second while the negator "not" becomes a text keyword. The existing guard filters conditions to `field === 'location'` before asserting length 1, so it passes while the leak is invisible |
| U1-6 | Functional | `features/search/nl-query.ts:199-229,426-430,436-468` | A spelled-out compound number is read as its first word | confirmed | #1200 | `"exactly one hundred"` → `quantity = 1`; `"more than twenty five"` → `> 20`. `NUMBER_WORDS` deliberately carries `twenty`…`ninety` and `hundred`, words that exist almost solely as compound components. Inversion worth noting: `"more than hundred"` works and `"more than a hundred"` is understood as nothing |
| U1-7 | Functional | `features/search/nl-query.ts:387-393` | A decimal splits into two integers and can emit two contradictory quantity conditions | **rejected** | — | The mechanism reproduces, but `items_fts` uses the default `unicode61` tokenizer, which splits on the same characters — so the leaked `5` is emitted as a prefix match and **matches**. The query-side split is symmetric with the index-side split. `items.quantity` is `INTEGER NOT NULL` |
| U1-8 | Mechanical | `features/search/saved-searches.ts:55-56` | Saved-search names de-duplicate with `toLowerCase()`, not `foldName` | confirmed | #922 (comment) | `cosmetic`, so a comment rather than an issue. Every escalation route failed: sync and restore replace the whole array rather than merging, chips key on `id`, and each chip's tooltip shows its query. Also **outside the invariant as the repo defines it** — `name-fold-coverage.test.ts` scopes it to columns with a `UNIQUE … COLLATE NOCASE` index |
| U1-9 | Mechanical | `features/search/parse-text-query.test.ts:831-868` | The round-trip block asserts nothing beyond `ok === true` | **rejected** | — | It **does** fail for the reason it names: lowering `MAX_AST_GROUP_DEPTH` from 4 to 2 turns the depth case red. The finder's proposed mutation is a *content* regression the block never claimed to catch. Below the #1028 / #984 bar |
| U1-10 | Mechanical | `features/search/nl-query.ts:374-383,677-685` | The final gate widens an untranslatable tree to match-everything | confirmed | #1201 | Reachability settled: a 309-digit number overflows to `Infinity`, which `toNumber` rejects — so the search matches everything under chips reading "Quantity over Infinity". Every other throw in `parseASTtoSQL` was checked and is unreachable from the NL matchers, **including the `tag` × `HAS_CAPABILITY` pairing of U1-3** |
| U1-11 | Performance | `features/search/nl-query.ts:645-654`, `db/search/parseASTtoSQL.ts:403-418` | One plain-English phrase compiles to dozens of FTS subqueries | confirmed (re-based) | #1211 | **The subquery count is not the cost.** 4 → 48 subqueries adds ~15 ms at 100k; the OR-of-LIST-subquery *shape* costs 8.5× at **two** keywords, because `rowid=?` disappears from the plan and SQLite walks every active item. 100k: 74 ms against 9 ms. `ANALYZE` does not rescue it (6.5×). Filing on "48 subqueries" would prescribe a keyword cap, which fixes nothing |
| U2-1 | Functional | `features/search/fields.ts:160-162,174-177`, `components/ConditionEditor.tsx:121-133` | The composers `.trim()` per keystroke, so a space cannot be typed into the name box | confirmed | #1202 | Browser: typing `Model number` yields `Modelnumber`; pasting works. Silent and self-confirming — the tooltip directly above offers a multi-word name as its worked example and says unknown names "simply match nothing" |
| U2-2 | Functional | `components/ConditionEditor.tsx:87-93,174-182`, `features/search/fields.ts:185-196` | A plain-English location loads a row the builder renders as a blank dropdown over a raw id | confirmed | #1203 | **The finder's chronology argument is false** — the NL layer landed 2026-07-10, the `fields.ts` comment 2026-07-25 and the registry record 2026-08-28, so the decision was taken *with* the NL layer in view. That sharpens it: `fields.ts:130-133` treats "an operator the dropdown cannot render" as worth fixing for category, and the same reasoning was not applied to location. Touching the row also silently reinstates the sidebar scope |
| U2-3 | Functional | `features/search/builder-reducer.ts:32-34`, `components/ConditionEditor.tsx:67-69` | A blank condition value is run as a real predicate | confirmed (re-based) | #1204 | Re-framed from a taste argument to a **contract violation**: `ConditionEditor.tsx:67-69` states "a blank would translate to an error", and the parser enforces that for boolean, enum and date and skips it for text, number and tag — 17 of 27 fields. A fourth behaviour was found (blank Tag → `LIKE '%%'`). The "harmless default" rejection collapses: 8 of 9 number fields are nullable, so `> 0` returns nothing |
| U2-4 | Functional | `components/SavedSearchMenu.tsx:34,122`, `InventoryScreen.tsx:350,1086` | A builder or plain-English query cannot be saved, though two wiki lines say it can | confirmed | #1205 | Filed with U2-5 — one root cause: the menu is fed a text input's local state and no AST→text formatter exists anywhere. Worse than claimed: recalling a saved search *into the builder* blanks the quick box, so "recall, tweak, re-save" is blocked too |
| U2-5 | Functional | `components/TextQueryInput.tsx:19`, `VisualBuilder.tsx:34-42`, `builder-reducer.ts:98-105` | The boxes go stale and "Clear" clears neither | confirmed | #1205 | Browser-proved, and the consequence is durable: **Save pressed immediately after the documented Clear persists the query that was just discarded** |
| U2-6 | Performance | `components/ConditionEditor.tsx:162,171,179`, `InventoryScreen.tsx:484,505` | Every keystroke in a value box runs two undebounced worker queries | confirmed | #1230 | 20 calls for ten characters against 2 for the same characters in the debounced box beside it. Browser at 4× throttle, 10k vault: keystroke latency median ~151 ms debounced against **~867 ms**, long tasks to 1559 ms. **Compounds with U1-11**: a plain-English phrase loads a 17-leaf tree, so correcting one word costs 385 ms at 50k. Two limbs rejected — `astError` is 2.33 µs per call, and `gcTime` bounds the cache |
| U2-7 | Functional | `components/GroupEditor.tsx:67-76`, `ConditionEditor.tsx:187-194` | Removing a condition strands focus, and index keys retarget the button | confirmed (re-based) | #1207 | The two halves are **mutually exclusive per delete**: the last row strands focus on `<body>`; a non-last row **reuses the node**, so the same button now removes the next condition — two presses of one button destroyed a row the user never aimed at. Mutating only the key proves causation |
| U2-8 | Functional | `components/GroupEditor.tsx:168-188` | The AND/OR switch claims `radiogroup` and ignores the arrow keys | confirmed (sharper) | #1208 | `SegmentedRadioGroup` **already is this control**, with a roving tabindex and all six keys, used on seven screens — so the fix is a substitution. Recorded honestly: axe reports zero violations, because it has no keyboard-interaction rule |
| U2-9 | Functional | `components/VisualBuilder.tsx:57-59` | The translation error is unannounced and written for a developer | confirmed (narrowed) | #1209 | **Three limbs rejected**: `queries.ts` is on the documented ALLOWED list of `error-copy-idiom.test.ts`; "structurally un-routable through `t()`" is false (`db-error-message.ts` is the pattern); and the quoted depth message is unreachable from either UI route. A cheaper repro was found — choosing "Capability" makes the error the *default* for that field |
| U2-10 | Functional | `features/search/saved-searches.ts:51-61`, `components/SavedSearchMenu.tsx:39-44,87-96` | Saving over an existing name destroys the old query silently | confirmed (widened) | #1206 | Browser-proved: no warning, one chip, same label, old query gone. **Widened**: the name input has no `maxLength` while `addSavedSearch` truncates at 60, so two names the user typed as visibly different collide past character 60 |
| U2-11 | Prompt | `features/search/nl-query.ts:374-377` | The gate's "mirrors parseTextQuery" comment is false | confirmed | #1201 | Folded into U1-10 — same three-line fix. The adjacent `TEXT_SEARCH_FIELDS` sub-claim is **rejected**: `nl-query.test.ts:15` is a working tripwire, and widening that list reddens 18 tests |
| U2-12 | Mechanical | `features/search/builder-reducer.ts:107-108` | The reducer's hand-written `default:` suppresses the exhaustiveness error | **rejected** | — | House pattern, confirmed by survey: **4 of 4** action-union reducers in `src/` use a bare `default:`, zero reducers call `assertExhaustive`, and `lib/exhaustive.ts` scopes its rule to *domain* unions. A five-union probe produced exactly one error, from the only reducer *without* a `default:` |
| U2-13 | Mechanical | `VisualBuilder.tsx:34-42`, `GroupEditor.tsx:50-57`, `ConditionEditor.tsx:187-194`, `SavedSearchMenu.tsx:64-127` | Nine controls hand-roll where Foundry primitives exist | confirmed (widened) | #1210 | Measured in a real browser: the Foundry ✕ draws a 3 px token ring, the hand-rolled ones fall back to Chrome's 1 px UA outline, because `index.css` has no global `:focus-visible`. **Three targets are under WCAG 2.2 AA's 24×24** (16×16, 27×16, 16 high). Scope narrowed to seven — `NegateToggle` and `OperatorToggle` have no Foundry equivalent |
| U2-14 | Mechanical | `features/search/builder-reducer.ts:112-126`, `GroupEditor.tsx:13-21,55,70` | `replaceGroupAtPath` no-ops silently, and `depth` is never read | **rejected** | — | Limb A is unreachable from the UI — every dispatch site builds a group path by construction. Limb B is dead code for the Phase 17 sweep, with one caveat recorded: a *self-referential* prop (`depth + 1` feeding only its own recursion) reads as live to `noUnusedParameters`, ESLint and knip-style tooling, so an automated sweep will not find it |
| U3-1 | Functional | `components/nav/nav-destinations.ts:207-214,224-231` | Alerts and Upcoming are ungated, so a denied role sees their vault data | **rejected** | — | A **recorded decision**: #522's closing comment names both rows ("they stay open to everyone and leave out the entries a role can't view"), and `docs/wiki/Roles-and-Permissions.md:114-115` says the same. The gating is in `useAlerts.ts:169-179` and `useAgenda.ts:105-114`, not the screens — a zero-grant authority yields `events: []` and `alerts: []` with every feed disabled. **No disclosure** |
| U3-2 | Functional | `lib/fuzzy.ts:52,55`, `CommandPalette.tsx:191` | The client re-ranker is accent-blind while `items_fts` is not | confirmed (re-based) | #1212 | FTS half settled by **execution**, not by reading the DDL: `searchByRelevance('cafe')` returns `Café latte`. **The `Größe` examples are not evidence** — both layers agree there, since `ß` is a letter — and `foldName` would not fix them. Driven: an accented exact match is pushed off the 7 visible rows |
| U3-3 | Mechanical | `lib/fuzzy.ts:55,63-64,73,79` | Indexing the lower-cased copy shifts positions into the original | duplicate #955 | #955 (comment) | #955 already names the same string and the same site. The #955 fix repairs the score limb **for free**, proved by mutation. The **boundary-bonus** half is not recorded there, so it went as a comment: `İstanbul kit` scores 8 lower than `Istanbul kit` and sorts below it |
| U3-4 | Mechanical | `CommandPalette.tsx:169,190-208,374-377` | A failed search is reported as "No items match" | confirmed (widened) | #1213 | Driven through the **real** hook and a real `QueryClient`. Widened: on a *refining* keystroke `keepPreviousData` **withdraws** the real rows and replaces them with the false empty state. Failure routes are real — `WORKER_TIMEOUT` (non-latching), `WORKER_UNAVAILABLE` (latching, so every later search reads as "No items match"), `SQLITE_ERROR`. A **ninth** surface, not one of #1079's eight |
| U3-5 | Functional | `features/command-palette/HeaderSearch.tsx:40-42` | The launcher hard-codes `Ctrl /` instead of using the hint seam | confirmed (narrowed) | #1219 | **The macOS limb is disproved**: `bindingFromEvent(Cmd+/, isMac=true)` returns `Ctrl+/`, so ⌘+/ does fire and the cap is mis-spelled, not dead. Real limbs: a rebind, an unbind and `hotkeysEnabled = false` all leave it advertising `Ctrl /`; it is the **only raw `<kbd>` left in `src/`**; and it disagrees with `displayBinding` even at Windows defaults |
| U3-6 | Functional | `CommandPalette.tsx:362` | The Settings row asks `forRoute` for a hint filed under `byCommand` | confirmed | #1220 | Enumeration confirmed and **not wider**: 10 navigate-effect and 12 command-effect actions, and `/settings` is the only one affected. Sharpest point the finder missed — `CommandPalette.tsx:243-247` already special-cases `/settings` as a command and then asks `forRoute` anyway |
| U3-7 | Functional | `CommandPalette.tsx:165-167,368-371` | A role denied `items:read` gets a search box that never answers | confirmed | #1215 | With `resistor` typed, the pane still reads "Start typing to find an item by name." and nothing matching `/permission/i` renders, while the header keeps advertising "Search items…". Only a **custom** role is affected — all built-in roles hold `items:read` — but the code path exists because the author expected it. No existing palette test sets an `authority` |
| U3-8 | Functional | `CommandPalette.tsx:296-300,339-343,368-377,692-700` | The listbox's options are grandchildren, `aria-expanded` is hard-true, and no count is announced | confirmed (3 of 4 limbs) | #1216 | A **CDP accessibility-tree dump** settles the impact the finder could not: Chromium rejects `role="option"` for want of a parent and falls back to `button`, so **`selected=true` disappears from the tree** — present after the mutation, absent before and after the revert. 21 buttons, 0 options. Limb 2 (the empty-state `<li>`s) is **inert** — Chromium reparents the text and they never coexist with options |
| U3-9 | Mechanical | `CommandPalette.tsx:139-143` | A `setTimeout(0)` steals focus the `Modal` prop is built to give | confirmed (narrow) | #1221 | Variant B is decisive: a plain effect with **no timeout** also focuses the input, so the comment names the wrong mechanism. Behavioural limb **unverifiable** — a real screen reader is needed to know whether the container is announced first |
| U3-10 | Functional | `CommandPalette.tsx:334` | Escape discards the query where nine sibling surfaces clear it first | **rejected** | — | `use-search-escape.ts:3-14` names its adopters rather than claiming universality; the help footer honestly says "Esc to close" in both states; no wiki page promises otherwise; and nothing is lost. The divergence is real, the consequence is not |
| U3-11 | Prompt | `useCommandPaletteStore.ts:3-7`, `state/stores/usePreferencesStore.ts:503` | The store's docstring names the wrong key and the wrong owner | confirmed (widened) | #1222 | A **second site** was found, on the very preference that gates the shortcut. `git show 68e51ab1` proves both claims were true when written and were orphaned by #32/#127 — exactly #848's shape. The finder's line reference was wrong (the file is 18 lines) |
| U3-12 | Mechanical | `CommandPalette.tsx:311-326`, `InventoryScreen.tsx:810,824` | The clear button is hand-rolled and undersized | confirmed (re-based) | #1217 | **The WCAG 2.5.8 framing fails on measurement** — the 24 px circle spans x 868..892 against an input ending at 862, so the spacing exception is satisfied, and the project has never adopted §2.5.8. The real defect is bigger: `InputClearButton` defines 28×28 and is used by **eight** call sites, while three hand-roll 20×20 on the app's two busiest search boxes |
| U3-13 | Performance | `CommandPalette.tsx:73-80`, `db/repositories/item/search.ts:254-269` | Every keystroke reads 50 full item records to render eight names | duplicate #808 | — | The stated mechanism is **disproved**: the structured clone costs 0.29 ms and the payload delta is 56 KB. The real cost is `COUNT(*) OVER ()` forcing the full projection across the whole match set (1.3 ms without it, 40.6 ms with). Re-measured on the shipped sqlite-wasm at 1.90–2.10×, independently confirming #808's own 1.50–2.13× |
| U3-14 | Functional | `components/nav/nav-destinations.ts:331-334` | "Manage tags" stays offered when the tags capability is off | **rejected** | — | The decision is recorded in place, and the premise is false: `EditLocationDialog.tsx:303-306` renders `LocationTagEditor` **unconditionally**, so tags stay editable and the dictionary is not orphaned. #939 does not transfer — `alerts` is `kind: 'page'` with a route; `tags-attachments` is `kind: 'capability'` with none |
| U3-15 | Functional | `CommandPalette.tsx:179` | Screen-jump ranks the English label only | confirmed | #1223 | 14 of 21 nav destinations render a German label the palette matches with nothing. **Not inside #238**: that issue's own body says `NAV_DESTINATIONS[].label` is "deliberately retained as palette search text", so actioning it as written leaves this standing. Repository precedent (#1183, #1184, #906, #945) files each English-leak surface separately |
| U3-16 | Functional | `CommandPalette.tsx:169,308`, `features/inventory/queries.ts:646` | The spinner is gated on `isPending`, so it appears only on the first search | confirmed | #1214 | Driven, plus a mutation proving `isFetching` restores it. Measured: a narrow query answers in 2–8 ms (invisible), a broad prefix costs ~500 ms at 100k in-process. **The obvious fix is wrong** — `loading` drives both the input spinner and the list's "Searching…", so a straight swap blanks the list; the flag must be split |
| U4-1 | Functional | `features/scanner/gtin.ts:84-93,167-195` | An NS-1 UPC-E that is also a valid EAN-8 is stored compressed and found in one direction only | confirmed (narrowed) | #1232 | The **precedence is a recorded decision** (`gtin.ts:70-74`, raised in #508) and is not the defect. What is wrong is `barcodeMatchForms` returning one form, and `gtin.ts:182`'s "both directions are covered" being false for the ambiguous class — **528,000 codes**, derived algebraically (four inserted zeros leave the surviving digits on identical weights) and proved end to end on a real repository |
| U4-2 | Mechanical | `features/scanner/zxing-decode.test.ts:6-36` | Every assertion for the WASM decode path expects `null` | confirmed | #1234 | **12,442 tests pass** with the production decoder stubbed to `() => null`. The docblock's excuse is disproved — the round-trip takes ~12 lines using the repo's own `encodeQr`. The named backstop decodes with the **native** detector and skips silently where it is absent, so Firefox and Safari are unguarded |
| U4-3 | Mechanical | `features/scanner/upce.test.ts:99-128` | The whole-space property test cannot see a `compressUpcA` reorder | confirmed | #1233 | Reordering the branches leaves 43/43 green while changing **140,700** `parseGtin` verdicts. The control matters: swapping `expandUpcE`'s bodies *does* go red, so the property is sensitive to one function and structurally blind to the other. `gtin.ts:78-82` cites this test as its warrant |
| U4-4 | Prompt | `features/scanner/due-date.ts:1-59` | The module is dead and its docstring claims two false parities | confirmed | #1268 | Deletion proved: 800 files / 12,445 tests green. Measured 15 h anchoring gap against the real path. `git` dates the drift — the "mirrors `expiryStatus`" claim was **true when written** and was orphaned by #498. Two corrections: `DUE_SOON` does exist (`FieldDueStatus`), just not for loans; and #377 deliberately **kept** the module, which caps the value |
| U4-5 | Prompt | `features/scanner/cooldown.ts:1-54`, `scan-gate.ts:4`, `queue-reducer.ts:9-10` | `CooldownMap` has no production caller but three docblocks call it live | **rejected** | — | The central claim is **false**: `ScannerQueueContext.tsx:30,34,36,51` constructs and calls it on every accepted continuous scan, and deleting the module breaks the production type-check. #512 explicitly kept it ("the queue's existing id-keyed guard can stay as the belt-and-braces check"), so all three docblocks are accurate |
| U4-6 | Functional | `features/scanner/qr-code.ts:252-262,279-281` | Byte-mode UTF-8 with no ECI 26 declares the wrong character set | **rejected** | — | Mechanism real (forcing ISO-8859-1 gives mojibake), reach nil: **0 non-ASCII payloads** from 28 adversarial override × origin combinations, because `URL.href` percent-encodes and punycodes everything. Residue recorded: `fitsInQr`'s doc treats multi-byte payloads as supported, which is a one-line note rather than an issue |
| U5-1 | Mechanical | `features/scanner/useScanner.ts:373-380`, `ScannerOverlay.tsx:802,867` | `SUSPEND` has no resume, so backgrounding kills the scanner and strands a batch | confirmed | #1224 | Browser-proved (`srcObject: false`, `tracks: []` after the return). **The specification itself requires the missing half** — `docs/todo/done/_specification.md:432`: "It should automatically request to re-initialise when the user returns." The wiki's "Leaving clears the queue" covers only the ✕, Escape and Back. Widened to `BarcodeScanDialog`. Limb (c) is `data-loss` |
| U5-2 | Functional | `ScannerOverlay.tsx:489-506`, `batch-actions.ts:33-48` | A partly-failed batch clears the queue and reports a bare count | confirmed (widened) | #1226 | Widened in both directions: `useCheckoutItem` has **no `onError` at all**, so a failed batch check-out is silent apart from the count; and `useMoveItem`'s toast **coalesces** identical failures for 3 s on a signature that names no item, so a three-failure batch gets one nameless toast |
| U5-3 | Mechanical | `ScannerOverlay.tsx:173-341`, `scan-gate.ts:54-64` | A failed repository read during a scan is entirely silent | confirmed | #1227 | The gate then returns `'suppress'` for 2 s, so the retry never reaches the repository. `WORKER_UNAVAILABLE` **latches**, so after one worker crash the scanner is permanently and silently dead. The wiki tells the user the code "wasn't read at all" and to hold steadier. Not one of #1079's eight — a direct repository call from an event handler with no `try`/`catch` |
| U5-4 | Prompt | `components/ScannerViewfinder.tsx:19` vs `:76` | The header says the decoder reads the whole frame | confirmed | #1268 | Measured: the decoder analyses **36.8%** of the frame on desktop and **15.4%** on a phone, and a code outside the reticle is not analysed. `roi.ts:17-24` records that #59 was reopened once already |
| U5-5 | Prompt | `useScanner.test.ts:4-6`, `ScannerOverlay.test.tsx:39-43` | The docstring claims coverage from tests that mock the hook away | confirmed | #1225, #1268 | **The briefed mutation goes red for the wrong reason.** The real demonstration is Mutation B — the unmount cleanup nulls the refs but never stops the tracks, i.e. the camera light stays on — which leaves **330/330 green**, as does disabling the visibility dispatch. Correction: `track.stop()` *is* tested, via camera swap |
| U5-6 | Functional | `useScanner.ts:260-270` | Every `getUserMedia` rejection but `NotAllowedError` collapses to one sentence | confirmed | #1228 | Full `DOMException` matrix pasted. Better "no camera" copy exists at `:187` and is reachable **only** when `mediaDevices` is absent. The wiki says the scanner "says so plainly" for three causes; two are true. Secondary limb (every rejection dispatching `PERMISSION_DENIED`) **rejected** — the reducer treats both actions identically |
| U5-7 | Functional | `BarcodeScanDialog.tsx:283`, `BarcodeField.tsx:109` | Two barcode boxes hint a numeric keypad for values that may contain letters | confirmed | #1229 | Proved to be **#506 residue**: commit `c7b8d8c7` widened `BarcodeField`'s hint to admit letters and did not touch the `inputMode` six lines above. On-device keyboard cost **unverifiable** — it needs a physical iPhone and Android |
| U5-8 | Functional | `ScannerOverlay.tsx:284-341`, `labels/label-sheet.ts:471`, `location-label.ts:77` | A printed Code 128 encodes a value the scanner's ladder cannot resolve | confirmed | #1231 | Proved on a real migrated database for **both** item and location labels: the label resolves only when its preferred value did *not* fit and fell back to the short id. The premise attack **flipped toward the finding** — `docs/todo/done/label-customisation_2026-06-30.md:31-32` records as a shipped outcome that "printed barcodes round-trip through Gubbins' own scanner", conflating decoding with resolving |
| U5-9 | Performance | `useScanner.ts:313-341`, `decode-cadence.ts:4-5` | The native decode path is exempt from the cadence and crops every frame | confirmed | #1235 | **The forced layout read is disproved** — 0.16–0.21 ms at 4×, 1.3% of budget and 32–83× cheaper than the crop. The crop measures 6.7 ms phone-shaped and 12.9 ms desktop-shaped at 4×, p95 20.2/28.2 ms, and the loop fell to 47.9 fps with nothing else running. ROI is 263k–778k px, not ~10⁶. The stale rationale predates `roi.ts` by two weeks |
| U5-10 | Mechanical | `ScannerOverlay.tsx:166-171`, `feedback.ts:45-58,114-121` | A late decode mints an `AudioContext` after `dispose()` | confirmed | #1236 | **The endpoint is disproved**: no reachable per-document cap in current Chrome — 400 and 3000 contexts created with no throw — so beeps do not stop working. What remains is a beep and haptic firing after the scanner closed |
| U5-11 | Mechanical | `ScannerOverlay.tsx:450-456,700` | A failed single-item move leaves an unhandled rejection | confirmed | #1236 | Mitigation is stronger than claimed: the toast container is `aria-live="polite"` with `role="status"` per toast, so screen-reader users are told too. Its own peer `moveHere` in the palette does the `try`/`catch`. The screen-reader-through-the-modal limb is **unverifiable** |
| U5-12 | Mechanical | `ScannerViewfinder.tsx:79` | The reticle scrim is a raw `rgba()` literal | **rejected** | — | The `CLAUDE.md` exception applies: no scrim token exists, Foundry's own `modal.tsx:230` and `drawer.tsx:70` paint `bg-black/60`, `index.css:441-445` records that a photo backdrop has nothing for the mode switch to compensate, and the same element carries three other raw literals the finder accepted |
| U5-13 | Prompt | `feedback.ts:8-10` | The docstring says `prime()` is called from the opening tap | confirmed | #1268 | Both call sites are passive mount effects. Behavioural half **unverifiable and partly disproved** — Firefox shows post-mount and in-handler identical, and the Chromium run was contaminated by harness-granted activation. No gestureless open path exists |
| U5-14 | Prompt | `docs/wiki/Camera-Scanning.md:26-29` | A tip links to the page the reader is already on | confirmed | #1268 | `npm run wiki:check` passes (98 pages OK) because the target resolves — it is this page. It is the **only** self-link in the wiki. Correction: the finder's "that page never says where the setting lives" is false; the link *text* is correct and the target is what is wrong |
| U5-15 | Mechanical / Prompt | `scanner-machine.ts:110-111`; `docs/wiki/Camera-Scanning.md:127-128` | A bare `default:`; and the wiki mis-times the NFC prompt | split | #1268 | **(a) rejected** — see U2-12; an independent survey agreed, 4 of 4. **(b) confirmed**: `useNfcScan` arms `reader.scan()` when the overlay opens, so the prompt appears then, not on the first tag tap — and the module's own docstring says so, contradicting the wiki |
| U6-1 | Mechanical | `features/scraping/ScrapeBridgeContext.tsx:229-231` | The PWA never checks `event.source`, so a same-origin page can impersonate the extension | **rejected** | — | Forgery reproduced, including into the review dialog — but **no consequence**: a same-origin attacker already owns the OPFS database and `localStorage` outright, and the shipped COOP and `frame-ancestors` close both delivery routes. #493's own reasoning ("it defends against the page the extension no longer runs in, and not against the one it does") applies here. Flagged to the maintainer as a defence-in-depth chore, not a vulnerability |
| U6-2 | Functional | `merge.ts:89-94`, `supplier-part-plan.ts:64-69`, `lookups/fill-plan.ts:115` | `classifyString` compares with `toLowerCase()` where the alias block uses `foldName` | confirmed (re-based) | #1241 | Re-based onto **missing NFC normalisation**: the `ß` pair renders *visibly differently*, so only the NFD/NFC pair is unanswerable — code points pasted. **Three** copies of the function, not two. The error is one-directional, so it can only over-report. Same class as open #1024 |
| U6-3 | Functional | `ScrapeReviewDialog.tsx:92-93,212`, `SupplierDataEditor.tsx:66-67` | "Nothing new to apply" disables the only route to creating the supplier part | confirmed (a); rejected (b) | #1237 | **(a) confirmed**, browser-proved, and widened to a pure-`FILL` update; the wiki's own recommended workflow does not work. **(b) rejected** — dropping a supplier `CONFLICT` with no opt-in is the mandated no-overwrite rule, recorded in four places, with `RefreshPricesButton` as the sanctioned overwrite route. (c) is true but documented, so it is (a)'s reachability premise. Residue: `CreateItemDialog.tsx:1499-1502`'s comment is wrong about both halves |
| U6-4 | Functional | `ScrapeSupplierPanel.tsx:97` | The failure toast ships a dead "Enter manually" action | confirmed | #1239 | Proved to be **#439 residue by history**: the fix commit touched four files and never this one, and a grep for `onClick: () => {}` across `src` returns exactly one hit. `CreateItemDialog` renders both panels, so one form shows a live link and a dead one |
| U6-5 | Mechanical | `ProductLookupPanel.tsx:106-123` | The extension-free lookup has no unmount or stale-request guard | confirmed (widened) | #1238 | The unmount-warning limb is **void on React 19**; the real defect is a missing **stale-request** guard. Product A is delivered under barcode B, the panel then hides so the user cannot retry, and `createFromBarcode` passes B's GTIN with A's name into the new item. A third case needs no unmount at all |
| U6-6 | Functional | `RefreshPricesButton.tsx:162-166` | The refresh rewrites the supplier part's currency unconditionally | confirmed (widened) | #1261 | Two directions proved against the real SQL seam. The worse one: a deliberately-EUR row hit by a bare-number page's GBP default is then **summed into a £ total as £5**, feeding eleven call sites including `item/stock.ts:483`'s append-only cost snapshot. No decision licenses it — #28 says "price", #666 set the opposite policy, the wiki says the part "keeps its own currency". **Contradicts a standing claim in open #1094** |
| U6-7 | Prompt | `scrape-errors.ts:108-116`, `protocol.ts:220-237` | The forward-compat fallback is unreachable, and a taxonomy addition is ungated | confirmed (both limbs) | #1242 | All eight members take the left branch, so the fallback cannot be reached through the wire, and its only test casts past the type. Limb 2 is **latent**, not firing: `UNSUPPORTED_SITE` was added *before* negotiation landed, so no taxonomy addition has yet been made under that regime |
| U6-8 | Functional | `merge.ts:96-101`, `ScrapeReviewDialog.tsx:39-56` | A sub-penny cost renders identically on both sides of the conflict | confirmed (re-based) | #1240 | The finder's residual is **settled from the repository**: `parsers.test.ts:225-253` pins `lcscParser` to `0.0904`, and `unit_cost` is `INTEGER` micro-units with nothing rounding to the minor unit. It is a **rendering** defect, not a classification one. Widened: the `FILL` row has it too and has **no checkbox** |
| U6-9 | Mechanical | `supplier-part-plan.ts:216,219-232` | The write resolvers switch with no exhaustiveness guard | confirmed (corrected) | #1243 | **Two corrections**: "compiles clean" is false for `ScrapeField` (a total `Record` in the dialog catches it), and `resolveSupplierPartWrite` has **no review dialog at all**. `SupplierPartField` is the clean half — and `wrote = true` set *before* the switch turns a no-op into an empty update tagged `source: 'SCRAPE'`. A **fourth** unprotected shape `exhaustive.ts` does not cover |
| U6-10 | Mechanical | `db/repositories/SupplierPartRepository.ts:257-266` | The price-history dedupe compares the cost but not the currency | confirmed | #1262 | Mutation proves causation, and the suite stays 30/30 green either way. **Reachability is higher than rated**: `SupplierPartFormDialog` always submits both fields, so correcting only the currency picker and pressing Save is the trigger — no scrape needed. Sibling of #1043, not its cause |
| U7-1 | Mechanical | `parsers/types.ts:135,168-170` | `parsePrice` takes the first digit run, so a leading quantity becomes the price | **rejected** | — | `types.ts:127-131` documents this class **by name**, with a worked example of the same shape ("`Buy 2 500ml bottles £9.99` … joins into 2500"), and accepts it on an explicit frequency judgement. Removing `GROUPING_GAP_RE` does not change the outcome, so the acceptance covers the whole class |
| U7-2 | Prompt | `parsers/digikey-parser.ts:6-9` vs `:27-43` | The docstring promises a metadata fallback the parser does not have | confirmed | #1263 | Filed with U7-3 and U7-4 — one root cause. The prose is **worse than claimed**: `git show 206dfb39` shows the phrase predates `metadata.ts` existing at all, so it has never been true in either reading. It fails *loudly* (a `DomDriftError` becomes "the page layout changed. Manual entry required."), which caps the severity |
| U7-3 | Functional | `parsers/digikey-parser.ts:37-38` | DigiKey prices default to GBP and ignore the page's `priceCurrency` | confirmed (narrowed) | #1263 | The `priceCurrency` limb needs **no assumption**; the symbol-free limb is conditional and rests on U7-4. Both prior issues point the other way: #666's closing comment names DigiKey as *handled*, and #663's fix landed in `metadata.ts` — the factory DigiKey does not use. The unreviewed refresh path writes it with no dialog |
| U7-4 | Mechanical | `parsers/digikey-parser.ts:30,33,37` | The `meta[itemprop=…]` fallback selectors can never match | confirmed | #1263 | Precise: `meta[itemprop="mpn"]` is **unconditionally** dead (a void element has no `textContent`); the bare `[itemprop=…]` forms are conditionally dead. `digikey-parser.ts` is the only file carrying the unconditional form, and the same entries are harmless in the factory parsers because `metaContent` reads `getAttribute('content')` |
| U7-5 | Mechanical | `parsers/metadata.ts:73-82,152-155` | A nested related product outranks the real, anonymous listing | confirmed (widened) | #1244 | Widened well past `isRelatedTo`: an ordinary top-level `ItemList` carousel does it too. **LCSC is fully exposed**, being deliberately selector-free. The docstring at `:130-134` claims protection against exactly the carousel case. Siblings **rejected**: the `offers`-array order is documented and tested, and `AggregateOffer` yields a *missing* price that is reported to the user |
| U7-6 | Mechanical | `parsers/host-permissions.test.ts:42-56` | The parity test cannot fail | **rejected** | — | The finder's own mutation **is** caught — two tests go red — and the coverage assertion fails on both drift directions. The manifest parity assertion is genuine too. The residual `id`-versus-`domains` gap is fail-safe (the URL is refused) with no live offender |
| U7-7 | Functional | `parsers/amazon-parser.ts:99-102` | The `data-asin` fallback can adopt a sponsored tile's ASIN | confirmed | #1264 | Reachability settled **from the extension's code**: `scrapeAmazonTab` gates on `isAmazonHost` only, with no path check, and the menu and toolbar are offered on every Amazon page — so a search or category page is one click away. The ASIN becomes the supplier part's order code and link, shown only as free text in the notes |
| U7-8 | Mechanical | `parsers/amazon-parser.ts:37-51` | The first `.a-offscreen` in the core block wins, so a "was" price can be taken | rejected / unverifiable | — | The docstring limb is **rejected**: "elsewhere on the page" scopes the claim to prices *outside* the core block, and the ordering does prevent those. The frequency limb is **unverifiable** without fetching a page. Severity would be capped anyway — Amazon is never background-fetched |
| U7-9 | Prompt | `parsers/metadata.ts:282-298` | A host selector, including a blanket `h1`, outranks the structured metadata | confirmed (narrowed) | #1265 | The "documented precedence is violated" framing is **rejected** (`:8-9` and `:265-269` state the implemented order plainly), and the `gubbins:*` limb is **rejected on consequence** (such a page routes to a parser that honours it). What survives: a bare `h1` pre-empts the JSON-LD description on six of eight parsers, and `parsers.test.ts:263` passes only because LCSC has no selectors — proved by adding that one selector |
| U7-10 | Functional | `parsers/metadata.ts:115`, `adafruit-parser.ts:15`, `sparkfun-parser.ts:15` | The MPN and the supplier's order code share one payload slot | confirmed (narrowed) | #1266 | The **alias limb is rejected** — `item/aliases.ts:20` defines that table as supplier identifiers, so an order code there is correct by design. What survives: `ActiveTabScrapeListener.tsx:26-31` states the rule ("the ASIN is an Amazon order code, not an item MPN") and that guard exists **only** in the Amazon listener. Reaches the printed label and BOM auto-match. Cross-references #488 |
| U7-11 | Functional | `parsers/metadata.ts:303`, `protocol.ts:185-191` | A page-supplied `priceCurrency` is persisted verbatim | confirmed (widened) | #1267 | No validation anywhere — `create` and `update` are `cleanText` only — and the junk reaches both `supplier_parts.currency` and the price-history row. Safe Mode confirmed **not** reachable (both `Intl` sites guard). **Widened**: a currency over 64 code points aborts the write on a schema `CHECK` |
| U7-12 | Mechanical | `parsers.test.ts:168-179,202-212` | Three parsers ship with no page fixture, so their selectors cannot regress detectably | confirmed (not filed) | — | Deleting all three `selectors` blocks leaves the whole scraping suite green (19 files, 333 tests). Not filed standalone: the best-effort selector posture is a recorded decision in all six parser docstrings, the metadata fallback is the backstop, and no live offender is establishable without fetching a page. Sweep material if a sibling appears |
| U7-13 | Prompt | `extension/src/background.ts:296-317` vs `features/inventory/asin.ts:57-82` | The context-menu marketplace list claims parity and diverges by seven | confirmed | #1269 | 15 against 22, with no reverse gap; the toolbar claim in the same comment **is** true and was verified. `git log -S` shows the lists were **never** in step — the 15 were introduced at a commit where `asin.ts` already held 22 |
| U7-14 | Functional | `parsers/metadata.ts:33-39` | An out-of-range numeric entity silently corrupts a scraped MPN | **rejected** | — | Gubbins has **no decoder here**: U+FFFD is the substitution the HTML standard mandates, so every browser sees the same string and there is no correct alternative value. Distinct from #1191, which has a hand-rolled decoder that throws. The value is shown as a labelled proposal before anything is written |
| U7-15 | Mechanical | `parsers/registry.ts:69-71,82-84` | The "no parser" branch is unreachable | **rejected** | — | Fact confirmed across eight URL shapes, consequence nil: the unreachable string is never rendered, and `registry.ts:65` already states the invariant out loud ("the generic fallback always does") |
| U8-1 | Mechanical | `CategoryLookupPanel.tsx:276-299`, `LookupMatchDialog.tsx:59-67` | Cancelling mid-fetch reopens the review dialog | confirmed (widened) | #1245 | Proved to be **#654 residue**: its merge commit applied `busy` and a disabled Cancel to the *sibling* dialog in the same directory and not this one. The window is not a race — the provider's `minIntervalMs` is 1000 ms. Widened: after the dismissal the start button reads ready, so a **retry is swallowed** by the stale run |
| U8-2 | Functional | `lookups/binding.ts:38-46`, `fill-plan.ts:128-142` | The fill plan validates against a shape stripped of range and precision | confirmed | #1246 | The whole-batch loss is the severity-bearing half and was proved: the valid `Director` value never landed either. **Not data-loss** — the refusal is reported by an authored sentence through `describeDbError`. Narrower than it looks: no shipped preset sets a range, so a user must set one by hand (#851 is a second route) |
| U8-3 | Functional | `fill-plan.ts:53-54,167-172`, `LookupReviewDialog.tsx:86-89` | The "couldn't be applied" reason is English inside a translated sentence | confirmed | #1249 | The sibling `TYPE_MISMATCH` branch is wholly German, which is the control. Reachable via a range on `Runtime (min)` and via `genre` exceeding `TEXT_LIMITS.line`. **Not inside #1125**, whose fix (`hasAuthoredMessage`) would classify this string as authored and pass it through |
| U8-4 | Functional | `providers/wikidata-film.ts:121-130,200-201` | Everything is asked for and read in English only | confirmed (both limbs) | #1251 | Limb (a) `degraded`, limb (b) `wrong-data`: a partially-labelled list concatenates only its labelled members with **zero problem rows**. Two bounds hold — no bare identifier can reach the item's name, and a wholly unlabelled list is skipped. **A cross-cutting fact was found**: `FILL` rows are not tickable at all, so the "the user reviews every value" mitigation both candidates leaned on does not exist |
| U8-5 | Prompt | `lookups/types.ts:129`, `CategoryLookupPanel.tsx:81-83` | A 429 does not read as a rate limit, contradicting the code that says it does | confirmed (a) | #1250 | Direct 429 is indistinguishable from direct 500, while the extension path says "try again shortly" — and `lookup.error.refused` already exists in both catalogues, unreachable from `code: 'HTTP'`. **(b) rejected as independent**: the test's assertion is real; only its title's trailing clause is unbacked, and one copy fix makes it true |
| U8-6 | Functional | `providers/wikidata-film.ts:130` | `MIN` over the truthy runtime value is unit-blind | confirmed | #1252 | The emitted SPARQL was printed: no `psv:`, no `wikibase:quantityUnit`, five hard `LANG = "en"` filters. `MIN` deterministically selects the value in the **largest** unit, and no shipped preset sets a range to catch it. The real-world frequency of mixed units is **unverifiable** without querying the service — recorded in §11 |
| U8-7 | Functional | `fill-plan.ts:207-213`, `CategoryManagerDialog.tsx:989-1023` | Two output keys can target one field; the dialog promises both | confirmed | #1247 | The dialog really does render two rows under one label with the problems section **absent**, and the order is deterministic but arbitrary. Needs only **one** remap, which the repository's own passing test shows the picker offers. `preset-binding.test.ts:62-70` already names the invariant — for the zero-config path only |
| U8-8 | Functional | `CategoryLookupPanel.tsx:157,180,217` | A failed field read reports that the category has none of its fields | confirmed | #1248 | Seven false statements plus "Nothing new to apply", after two network round trips — verbatim the outcome the `fieldsLoading` branch's own comment exists to prevent. A **ninth** surface: `CustomFieldsEditor` calls the same hook and is one of #1079's eight, so the failure route is the one that family already accepts |
| U8-9 | Prompt | `CategoryLookupPanel.tsx:204-209` | A parity claim with no drift test, and the gates have diverged | confirmed | #1122 (comment) | `cosmetic`, so a comment on the open parity-sweep issue. The finder's premise is **false** — `restoreSnapshot` and `runSnapshotMerge` raw-UPSERT, so a blank name can land — and that is what makes the divergence reachable: an empty "Fill from a database" card |
| U8-10 | Mechanical | `fill-plan.ts:115` | A `!` assertion on a nullable the guard has excluded | **rejected** | — | The assertion is **required to compile** — `isBlank` returns `boolean`, not a type predicate, and could not correctly be one. Removing it is `TS18047`. Not a defect at all |
| U8-11 | Functional | `LookupReviewDialog.tsx:196-200` | The conflict row's sentence is assembled in JSX | duplicate #226 | — | It is not a sentence — a label ending in a colon plus a two-column comparison — and the repository uses that idiom in already-converted code. The general form is ticketed as #226, which names this trade-off explicitly |
| U8-12 | Performance | `providers/wikidata-film.ts:223-237`, `LookupMatchDialog.tsx:71-108` | The candidate list is uncapped and unvirtualised | **rejected** | — | The parser handles 10,000 hits in 7.6 ms; the cost is the unvirtualised commit, which scales linearly. But the input is bounded by `limit=10` and the API's own 50 cap, over an exact-matched allow-listed host. Contrast #986, whose input is the user's own vault and is therefore genuinely unbounded |
| U9-1 | Functional | `InventoryScreen.tsx:368,386,841,1577`, `DeepLinkScreen.tsx:46-58` | Add-item entry points are not gated on `items:write` | confirmed (re-based) | #1253 | Re-based off the deep link, which is the fifth of five: a grep finds only two `usePermission('items:write')` call sites, gating duplicate and bulk edit. A **shipped test pins the ungated button**. `/share-target` is the odd one out, and the commit that added its gate names only the `item` intent. The refusal-copy limb is #947 |
| U9-2 | Functional | `share-inbox.ts:46-48` | A multi-image share keeps only the first photo | confirmed | #1254 | `getAll` returns three where `get` returns one. The singular shape is **six layers deep**, so a one-line change fixes nothing. `share-draft.ts:82` states the property the code lacks ("nothing the user shared is silently dropped"), and the item image manager supports many photos. Not inside #641, which is about size and type checks on the adjacent line |
| U9-3 | Functional | `share-inbox.ts:105-125`, `sw.ts:263` | An abandoned share is swept only on service-worker `activate` | confirmed | #1255 | Shares accumulate rather than overwrite. **Two corrections**: "Reinstall app files" *does* clear it, and the Triage-invisibility point is already disclosed to the user. The real trigger is not a dismissed tab (seconds wide) but **BootGate**: a share landing while Gubbins holds the tab lock elsewhere never reaches the screen |
| U9-4 | Mechanical | `share/deep-link.ts:40` | `split('?', 2)` truncates rather than keeping the remainder | confirmed | #1256 | The percent-encoded control isolates the cause. The consequence is not only a missing title: the notes and the scraper seed carry a **different, still plausible page**, which the scraper then fetches. Integrator-facing — `README.md` documents the form with no encoding instruction. The case-sensitivity limb is **rejected** (WHATWG does not case-fold an opaque host) |
| U9-5 | Functional | `vite.config.ts:300-311` vs `ImportDataDialog.tsx:882` | `file_handlers` omits HTML, which the in-app picker accepts | confirmed (widened) | #1257 | Widened: **Gubbins' own HTML export cannot be opened with Gubbins**, and a real export round-trips through the import seam. `.xlsx` being absent is consistent, since the seam refuses a zip by signature. Plus a three-way list drift across the manifest, the picker and a JSDoc. Chromium's parser accepts the entry; whether an OS would offer it needs an installed PWA |
| U9-6 | Functional | `ImportLaunchScreen.tsx:34-36` | A multi-file launch imports the first and says nothing | confirmed (narrowed) | #1258 | Narrowed to the silence: `ImportDataDialog`'s input has no `multiple` and every seed prop is singular, so importing one file is all the app can do. Whether the OS ever hands over three at once is **unverifiable** without an installed PWA — if it splits the launch there is no consequence at all |
| U9-7 | Mechanical | `ShareTargetScreen.tsx:31-46` | A failed locations read strands the landing screen after the payload is consumed | confirmed | #1259 | The terminal state is genuinely terminal (`retry: 1`, no refetch, and `WORKER_UNAVAILABLE` latches). **The paused/offline case self-heals** in the same mount, so only the errored case is lossy — and that destruction is already **#910**, on the same file and lines, so this was filed on the render gate and cross-references it. Distinct from #938 and #909 |
| U9-8 | Performance / Functional | `features/images/compression.ts:38-48,84-92` | The 150 px thumbnail is resampled in one step at default smoothing | confirmed (re-based) | #1260 | **The performance reasoning is wrong and was dropped**: the 27× draw costs 1.8 ms of a 164 ms pipeline (1.1%), and the two-step rewrite measured **1.23× slower**. The quality half is real and worse than claimed — SSIM 0.868 against 0.993, visible moiré at true size, and thumbnails are displayed at 176 px and up to 848×477. The fix the measurement found is `imageSmoothingQuality = 'high'` on the thumbnail draw only: +22 ms, and **28% smaller files** |
| U9-9 | Mechanical | `features/images/opfs-images.ts:43-59` | A failed full-res write leaves a zero-byte OPFS file | **rejected** | — | Real residue, no consequence: `saveImageFile` throws *before* any row is written, so the orphan is unreferenced and #206's sweep reclaims it. The sibling's cleanup is needed precisely because its rows are restored alongside |
| U9-10 | Mechanical | `features/images/compression.ts:39,57` | Nothing states the reliance on the platform's EXIF-orientation default | **rejected** | — | Rejected as **currently correct** on Chromium and Firefox, proved with a synthetic `Orientation = 6` JPEG and a positive control; **unverifiable** on WebKit, which is not installed here. Nothing downstream assumes pre-rotation dimensions. A fourth call site was found (`catalogue-branding.ts:26`) |
| U9-11 | Functional | `share/LandingScaffold.tsx:28-30` | The spinner announces "Loading" beside its own visible message | **rejected** | — | The visible message is a plain `<p>`, not a live region, so nothing is duplicated; focus is confined to the dialog; and ~85 of ~90 `Spinner` sites are non-decorative, so this is house convention. The "never stops" limb is **duplicate #938** |
| V38-1 | Mechanical | `ScannerOverlay.tsx:173-341,248-252`, `InventoryScreen.tsx:1568-1571` | An in-flight decode calls the still-mounted parent after the overlay closes | confirmed | #1236 | **Raised by a verifier; no finder named it.** `onLocationScanned` runs `setSelectedLocationId`, so the inventory list jumps to a location the user never chose, after they closed the scanner. The window is widened by #800's unindexed short-code scan |

### Phase 11 — People, time and configuration screens

Pinned SHA: `2b48ca708411e8da973b4f0cc5f377fe349214d3`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Functional | `features/users/role-grants.ts:144` | The role editor’s "Everything under &lt;area&gt;" row writes an explicit enumeration, never the `&lt;subject&gt;:*` wildcard the wiki and the badge promise | confirmed | #1270 | Proved on the pure seam; `can()` differs for a future key. The wildcard is unrecoverable once the row is touched, so editing seeded **Manager** downgrades 23 subjects. `mode:'wildcard'` is written only by the *parser* — the badge is unproducible, not dead. No decision recorded anywhere for enumeration |
| U1-2 | Functional | `features/users/components/ApiTokensDialog.tsx:129` | Revoking a Bridge API token is a single unconfirmed click on an irreversible action | confirmed | #1272 | Same candidate as U2-2; merged. The docstring’s "the confirm wording" has been false since the feature’s first commit |
| U1-3 | Mechanical | `state/stores/useSessionStore.ts:57` | `signOut()` resets the derived authority to the store’s **unrestricted** default until the async refresh lands | **rejected** | — | Mechanism proved (`{"mode":"unrestricted"}` immediately after `signOut()`), consequence not. The app tree unmounts *before* `signOut()` on both paths, the only DB work in the window is the deliberately-ungated sign-in candidate read, no background task performs a guarded write, mutations are `retry: 0`, and `guardUsersIntent` passes a signed-out device anyway. No sequence found in which a guarded operation runs in the window. Carried to §11 |
| U1-4 | Mechanical | `db/repositories/ApiTokenRepository.ts:72` | `mint` accepts `SYSTEM_USER_ID`, and that token resolves to an unrestricted, un-disableable bridge identity | **rejected** | — | Both halves demonstrated. Rejected because the **supported** path already grants the same thing: a `users:manage`-only role can mint a token *for Admin* through the shipped UI and gets a byte-identical unrestricted bridge identity, which `identity.ts:66` documents as intended. `mint` has one production caller, gated `!isSystem`. The only non-file route is a synced privileged row, which #822 covers end to end |
| U1-5 | Mechanical | `db/repositories/constants.ts:722`, `features/users/builtin-user-labels.ts:56` | A parity claim ("exactly as a built-in role’s description does") with no drift test, where the role case has one | confirmed | #1276 | Whole suite green after mutating `SYSTEM_USER_DESCRIPTION`. The baseline fingerprint changes (forcing `SCHEMA_STALE`) but no test pins it. **Consequence corrected**: the label seam compares the row against the *constant*, not the catalog, so catalog drift never stops translation — what actually happens is that a changed constant never reaches the screen. Rider: the *roles* guard’s own comment overstates what it prevents, for the same reason |
| U1-6 | Functional | `features/users/queries.ts:36`, `UsersScreen.tsx:307` | Users and roles read one capped page and discard `hasMore`, so the member count under-reports | confirmed | #1273 | Same root expression as U2-3; merged there as the truncation limb. Not a regression of #149, whose scope (POs, bookings, contacts, projects, BOM lines) never named users |
| U1-7 | Prompt | `features/users/permissions.ts:148` | `normaliseGrants`’ docstring says "in registry order"; the code sorts lexicographically | confirmed | #1277 | `normaliseGrants([items:delete,items:read,items:write])` ≠ `permissionKeysFor('items')`. Both named tests use sets whose two orders coincide. No consumer depends on order, and the docstring’s stated *purpose* is met by sorting, so `cosmetic` and a doc-only fix — changing the code would reset every database via `baselineFingerprint` |
| U1-8 | Functional | `features/users/SignInScreen.tsx:246` | Every sign-in tile carries `aria-describedby`, but the target renders only for a disabled or password-less account | **rejected** | — | Same candidate as U2-7; see there |
| U2-1 | Functional | `features/modules/ModuleGuard.tsx:91` | "Show this module" on the `/users` interstitial enables the sign-in gate with no confirmation and none of the four warnings | confirmed | #1271 | Reproduced in a real browser: one click → `{"confirmDialog":false,"mentionsSignIn":false,"usersIntent":true}` and straight to the sign-in gate. **Severity reduced from the finder’s framing**: `SignInScreen` renders `LockedOutEscape` unconditionally and `guardUsersIntent` passes a signed-out device, so no lockout and no data loss is possible. What is bypassed is the advisory content, on the path `docs/wiki/Users-and-Accounts.md:96` sends readers down |
| U2-2 | Functional | `features/users/components/ApiTokensDialog.tsx:129` | Token revoke is unconfirmed and irreversible while the module docstring claims a confirmation exists | confirmed | #1272 | Absorbs U1-2. Both readings of "the confirm wording" fail — no string in the dialog says revocation is irreversible; the control’s label is one word. Delete-user and delete-role on the *same screen* both confirm, and #588 fixed the identical shape on PO lines. `data-loss`: the plaintext is never stored, so the secret cannot be re-minted |
| U2-3 | Functional | `features/users/UsersScreen.tsx:307` | The role member count is derived from the accounts list, so it reads a confident "0 people have this role" when that query failed | confirmed | #1273 | Three failing assertions with a passing control on the same fixture. `usersQuery.isError` is read at exactly one place, guarding only the accounts section — the roles section does what that section’s own comment forbids. Absorbs U1-6’s truncation limb (one expression, one fix). Not `data-loss`: `RoleRepository.delete` checks only `isBuiltin` |
| U2-4 | Mechanical | `features/users/components/RoleFormDialog.test.tsx:157` | The unknown-grant test’s one substantive assertion cannot fail | **rejected** | — | The central claim is **false** and was disproved by mutation: `within(container)` includes the container itself, so a banner that names the raw grant *is* caught (red on mutation 1). Mutation 2 (submit drops the unknown grant) does stay green, but `role-grants.test.ts:109` covers that round trip at the seam. What survives is a test name overclaiming by a clause — too thin to file |
| U2-5 | Functional | `features/users/UsersScreen.tsx:431` | The delete-account confirmation omits that the account’s Bridge API tokens are cascaded away with it | confirmed | #1274 | Cascade confirmed by an existing repository test plus `PRAGMA foreign_keys = ON` in the app driver — rows are **deleted, not orphaned**, so this is a copy defect only. The dialog already names the non-obvious *history* consequence and omits the one that breaks something outside the app |
| U2-6 | Prompt | `docs/wiki/Bridge-API-Tokens.md:30` | The wiki says to choose "New token"; the control is "Create token" | confirmed | #1275 | Widened: `bridge/README.md:67` carries the identical drift, and the same page’s "Revoking one" section says **Delete** where the control is **Revoke**. Every other bolded control name across the four users pages was resolved against `en.json` and does check out |
| U2-7 | Functional | `features/users/SignInScreen.tsx:246` | A healthy account’s tile points `aria-describedby` at an id that does not exist | **rejected** | — | Structural fact confirmed by render (`target = null`). Rejected on consequence: a dangling IDREF is discarded during description computation, and for a healthy account an absent description is the *correct* outcome. There is no axe tooling in the repo, so no gate flags it either. It is genuinely the only unconditional-attribute/conditional-target site in `src/`, which cuts against filing rather than for it |
| U2-8 | Functional | `features/users/components/ConfirmUsersEnableModal.tsx:34` | The enable confirmation lists every account’s display name to a session that may lack `users:read` | **rejected** | — | Premise unreachable: the modal renders only while the users module is **off**, and with it off `resolveAuthority` returns `UNRESTRICTED_AUTHORITY` for every session — proved by a passing test. Independently, display names are not withheld anywhere (the sign-in screen lists them with no authority at all; the activity log bakes a name into `HISTORY_CLEARED`; `users` is a sync table), so this is consistent with the recorded soft-boundary decision |
| U3-1 | Mechanical | `features/contacts/contacts.ts:151` | Renaming a contact invalidates no checkout or agenda key, so every loan surface keeps the old borrower name | confirmed | #1278 | Seven caches driven on a real `QueryClient`: rename invalidates 2 of 7, delete invalidates 7 of 7. **Does not self-repair** — a remount inside the 30 s `staleTime` re-renders the stale name (`callsAfterFreshRemount: 1`). Two of the finder’s listed surfaces were wrong and dropped: the export re-reads outside React Query, and the dialog’s own history *is* refreshed. Not #1002, whose four sites are all item mutations |
| U3-2 | Functional | `features/contacts/components/CheckoutDialog.tsx:217` | After a prerequisite fails, the retry re-reads the borrower and due date while skipping the committed main loan | confirmed | #1279 | Three call arrays pasted: the prerequisite lands on a different contact, a different borrower *type* (project vs contact), and a different due date. `mainLent` is keyed `[open, item.id]` so nothing re-arms it. Reachable without a race: the prerequisite sends no `fromLocationId` (#1145), an unlimited-supply prerequisite is pre-ticked and always refused, or a permission/storage failure |
| U3-3 | Mechanical | `features/contacts/components/CheckoutDialog.tsx:253` | The prerequisite loop discards the thrown error and asserts a stock cause it does not know | confirmed | #1279 | Merged with U3-2 — one root cause (a second write pass that neither freezes its inputs nor carries its failures’ reasons) and one fix. Control in the same dialog twelve lines up routes through the seam correctly. Invisible to `error-copy-idiom.test.ts`, which matches only the ternary |
| U3-4 | Functional | `features/contacts/components/CheckoutDialog.tsx:389` | The "Due back (optional)" date input has no accessible name | confirmed | #1280 | `computeAccessibleName` returns `''`; the two sibling controls in the same dialog are named. **Shape checked**: all 31 uses of the same class string classified — this is the only bare span beside a real form control, so the narrow issue is the right shape. Not covered by #541, which is scoped to the scanner overlay |
| U3-5 | Functional | `features/contacts/ContactsScreen.tsx:128` | Deleting a contact with no open loans destroys their loan history on one unconfirmed click | confirmed | #1281 | Cascade proved (4 → 0 rows) and the unconfirmed path proved on the real screen. **Claim narrowed for the issue**: `item_history` survives, and the *return* note survives as the `CHECKED_IN` ledger note. What is irrecoverable is the `checkouts` rows and the **loan note**, which has no other home. Strengthens #1153’s premise rather than duplicating it |
| U3-6 | Functional | `features/contacts/components/LoanRow.tsx:51` | The ordinary loan line is hard-coded English beside a translated one, and both live regions are English-only | **rejected** | dup #241 | #241 (open, "i18n: convert Contacts and loans") names ~73 strings across the 9 files, and specifically the three `plural()` calls and the hand-rolled `=== 1 ?` this candidate cites. All 19 `contacts.*` keys were enumerated against both catalogs: **no** literal duplicates a translated value, so there is no drift-within-a-converted-surface limb to salvage |
| U3-7 | Prompt | `docs/wiki/Loans-Check-Out-and-In.md:3` | The loans page documents neither renewal nor lending to a project or a location | **split** — renewal **rejected**, borrower union confirmed | dup #581 / comment #581 | Renewal limb is a duplicate: #581’s first section is titled "Renew a loan" and cites the same page and the same evidence. Borrower limb confirmed — the page names a contact and only a contact across 108 lines, while the "Loan to" select is the *first control* in the Check out dialog. **Two finder claims corrected**: the union *is* documented elsewhere (`Cloud-Sync.md:190`, `Locations-and-Stock.md:438`), so this is a page-level gap not a wiki-wide one; and `BorrowerLoansSection` mounts on **projects only**. Not a contradiction — the page omits, it does not deny. `cosmetic`, so carried to #581 rather than filed |
| U3-8 | Mechanical | `features/contacts/components/CheckoutDialog.test.tsx:277` | The due-date preset test asserts only `expect.any(Number)`, so the DST-safety comment beside it is unheld | confirmed | #1282 | Three mutations, each green against the existing 28-test file: the naive `+ days * MS_PER_DAY`, the UTC-slice trap, and a hard-coded past date. No other test holds the call site. A discriminating test was written and shown red on both mutations — but only because this host’s worker sits in `Europe/London`; on a UTC runner it too would pass, so the durable fix is the child-process recipe |
| U3-9 | Mechanical | `features/contacts/components/RenewLoanDialog.test.tsx:105` | The renew error test cannot distinguish a surfaced error from the fallback, because the strings are identical | confirmed | #1282 | Merged with U3-8 and U4-6 as one "these tests cannot fail" issue. Removing the `useErrorMessage` seam entirely leaves the file at 5 passed. `CheckInDialog.test.tsx:173` shows the correct shape one file over |
| U3-10 | Mechanical | `features/contacts/ContactsScreen.test.tsx:134` | Fixtures claim DTO types they do not satisfy, and no tsconfig type-checks tests | **rejected** | — | Facts all true — a scratch tsconfig reports `TS2741: Property 'returnNote' is missing`. Rejected here as the wrong shape: extending that config across `src` gives **717 errors in 711 test files across 174 files**, so this is the Phase 17 cross-cutting sweep, with #1042 as the existing per-file precedent. The claimed consequence also fails at this site — `EditContactDialog` uses a truthiness check, and this test never opens that dialog |
| U3-11 | Functional | `features/contacts/components/CheckoutDialog.tsx:169` | A zero-stock item still offers "Loan out…", with `max={0}` under a `value={1}` and a live submit | **rejected** | dup #1150 | Behaviour reproduced exactly (`<input min="1" max="0" value="1">`, caption "0 on hand", button enabled). #1150 (open) names `CheckoutDialog.tsx:158` and `:169` explicitly with the same severity and the same suggested direction. Two riders recorded: the repository refusal *is* certain, so this is not a phantom-stock bug; and the appeal to the number primitive’s bounds note is off-target, since `calc={false}` bypasses `NumberInput` entirely |
| U4-1 | Mechanical | `features/bookings/BookingsScreen.tsx:293` | Card action failures are written only to two `visuallyHidden` live regions, bypassing `useReportWriteFailure` | confirmed | #1283 | Every node carrying the message has an `sr-only` ancestor; the card’s HTML is byte-identical before and after. **Worst limb**: `list()` has no `assertPermission`, and Viewer and Auditor hold `bookings:read` only — so both roles get every action enabled and every press refused into a hidden region. Sibling of #1139 |
| U4-2 | Functional | `features/bookings/BookingsScreen.tsx:401` | Two identical outcomes in a row are announced once | confirmed | comment #1107 | MutationObserver: one record for two presses. `LiveRegion` has no key, nonce or effect. **Not filed** — 30 of 59 non-test call sites hold a bare `useState(string)`, and #1107 already scopes the fix to the primitive. Bookings is correct on #1107’s other limb |
| U4-3 | Functional | `features/bookings/BookingsScreen.tsx:352` | Cancel is one unconfirmed tap on a terminal action, rendered as a neutral `ghost` button | confirmed | #1284 | **One limb rejected and dropped from the issue**: the slot is *not* lost — `LIVE_BOOKING` excludes cancelled rows, so an immediate re-create succeeds. The `data-loss` grade is carried by **Delete** of a *converted* booking, which hard-deletes plus tombstones with no re-create path. Confirming is the house pattern (tags, webhooks, contacts, and #588) |
| U4-4 | Functional | `features/bookings/bookings.ts:102` | The asset picker reads 100 rows through a type that cannot carry a truncation signal, with no search | **rejected** | dup #841 | #841 (open) names the same three sites at the same line ranges and already contains the "bare array with a limit and no offset" limb. The finder’s second additional limb is **false**: the wiki sentence it cites describes the bookings *list*, which does render `bookings.list.truncated` on `hasMore`. Nothing to add |
| U4-5 | Mechanical | `features/bookings/BookingsScreen.tsx:68` | The list-load error hand-rolls the `instanceof Error ?` idiom | **rejected** | — | A **sanctioned** exception, not a bypass: `error-copy-idiom.test.ts:16` names "a site that pairs `error.message` with its own diagnosis" as deliberately unmatched, and the memory note names `BookingsScreen` specifically. `describeBookingsLoadError` is module-scope, so the hook is genuinely unavailable. The finder’s "resolver already in scope" evidence was inaccurate |
| U4-5b | Functional | `features/bookings/BookingsScreen.tsx:78` | The load-error guidance triages on English substrings, so `SQLITE_FULL` ("database or disk is full") misses `quota`/`storage`/`space` | confirmed | #1285 | Spin-off found while disproving U4-5. A user out of disk is told to close tabs and restart their browser, while `describeDbError` already holds `db.error.full` in both catalogs. Fix is to branch on the stable code |
| U4-6 | Mechanical | `features/bookings/booking-count-scope.test.ts:13` | The file’s one stated guarantee — a UTC cut-off — cannot fail on a UTC runner | confirmed | #1282 | Substituting `startOfLocalDay`: **green under `TZ=UTC`**, red in `Europe/London`. Nothing pins a zone in `.github`, `vitest*.config.ts`, `vite.config.ts`, `package.json` or `scripts`, and hosted runners are UTC — so the guard is alive only on a developer’s laptop, and dead on the runner that gates every merge. Merged into the "cannot fail" issue with U3-8 and U3-9 |
| U4-7 | Functional | `features/bookings/bookings.ts:52` | `startingThisWeek` anchors its seven-day window on `startOfUtcDay(now)`, so the window slides a whole day | confirmed | comment #496 | Zone-pinned child processes: at 20:01 New York a booking starting **today** is not counted; in Tokyo at 07:00 one starting **yesterday** is. **Not filed separately** — #573 moved `countBookings` into this function, so #496’s own third site *is* `bookings.ts:52`, and the two frame-bound branches sit two lines apart sharing one anchor. Limb 3 (`+ 7 * MS_PER_DAY`) rejected as a present defect but recorded as a trap for the fix |
| U4-8 | Functional | `features/bookings/BookingsScreen.tsx:131` | Two English literals duplicate catalog keys already translated into German, six lines from the dialog that uses them | **rejected** | dup #242 | #242 (open) names `features/bookings` (10 strings) explicitly and counts JSX text nodes plus `label`/`description`/`aria-label` props. Values confirmed byte-identical, but a converter reaching this form would reuse the keys by construction. A drift guard is only viable *after* #242 lands, which is noted on that issue |
| U4-9 | Functional | `features/bookings/booking-status.ts:83` | "Overdue" labels a lapsed reservation in `text-destructive`, where the same word means genuine lateness for loans | **rejected** | — | Style preference under §2. The card beneath the badge offers **Check out** — you cannot be invited to check out something you believe is already out. Every candidate shared surface checked: bookings are not an alert source, the agenda never puts a booking in Overdue, there is no bookings-status dashboard widget, and the exports are separate files. The two senses never co-occur, and `lab.date.intro` shows "overdue" is the app’s general word for a dated thing whose date has passed |
| U4-10 | Prompt | `docs/wiki/Bookings.md:13` | The page never states that only serialised or single-unit items can be booked | confirmed | comment #585 | Zero matches for the rule across the whole wiki, and `Tracking-Modes.md` / `Items.md` / `Core-Concepts.md` never use the word "book". **Severity downgraded on the attack**: the picker’s hint is *permanent*, not empty-only — the Asset field carries an always-rendered `InfoHint` whose tooltip states the rule and names the workaround. The empty-only inline sentence is a second surface. `cosmetic`, so carried to #585’s accuracy bundle. Confound noted there: a missing item is at least as likely to be #841’s 100-row cap |
| U5-1 | Mechanical | `features/calendar/CalendarScreen.tsx:229` | The latched announcement is re-homed between the polite and assertive regions by the live `isError` | confirmed | #1286 | Reachability settled first: query-core sets `status:"error"` while retaining `data`, and three things refetch a mounted agenda query with no focus event (`adoptAuthorityChange`, the background-sync wake, `refetchOnReconnect`, which is left at its default `true`). Both directions demonstrated. `AlertsScreen:301` is the same construction, so one issue covers both screens |
| U5-2 | Functional | `features/calendar/useAgenda.ts:195` | `isError` is the OR of seven lanes and gates every content block, so one failure hides six healthy lanes | confirmed | #1287 | Single-lane failure is documented, not hypothetical: `worker-driver.ts:21` says a timeout "deliberately does **not** latch", `RPC_TIMEOUT_MS.query` is per request, and six lanes issue up to 100 sequential RPCs against the reorder lane’s one. Screen holds six complete lanes and renders none. **`isLoading` limb disproved** and dropped — a disabled query is pending but not fetching |
| U5-3 | Performance | `features/calendar/CalendarScreen.tsx:298` | The agenda is unwindowed and rebuilt on every render | **unverifiable** | — | **The rebuild limb is DISPROVED by measurement and must not be filed**: the pure `build → filter → bucket` chain is 0.20 ms at 200 events and 5.2 ms at 5,000, against 17.3 ms and 279 ms for the render chain — 1–2% at every size. A `useMemo` could not help anyway, since `now` is fresh each render. The unwindowed-DOM limb is the load-bearing half and needs a browser profile. Lane predicates are O(catalogue), not O(due soon) — warranty and expiry ask 36,500 days ahead and field-due has no lower bound. §11 |
| U5-4 | Functional | `features/calendar/CalendarScreen.tsx:169` | Filter chips render for all seven kinds regardless of module gating or read permission | confirmed | #1288 | Driven with three modules off and again with a two-grant role: seven chips both times, card count unchanged by toggling. Three become dead tab stops in a `role="group"`. The same screen suppresses its truncation and look-back notices for exactly this reason. Not inside #939, which scopes itself to the `alerts` page feature |
| U5-5 | Functional | `features/calendar/agenda.ts:422` | Bucket headings and most chrome are hard-coded English while the chips are translated | **rejected** | dup #242 | #242 names `features/calendar` (9 strings) and calls out the `plural()` use in both `agenda.ts` and `CalendarScreen.tsx` by name. The two `setAnnouncement` literals sit outside its counting heuristic, so that is a scope note on #242 rather than a separate issue |
| U5-6 | Functional | `features/calendar/agenda.ts:315` | A booking emits one event anchored at its start, so its return date never reaches the calendar | confirmed | #1289 | Two scratch assertions. The lane’s docstring records a decision about *anchoring an active booking*, never about a return entry. `git merge-base` shows the lane (2026-06-30) predates the wiki line (2026-07-11), so this is a capability the wiki claimed and that was never built — framed that way in the issue. Distinct from #495 (frame) and #496 (one entry behaving wrongly) |
| U5-7 | Mechanical | `features/calendar/agenda.ts:325` | `startOfUtcDay(x) + MS_PER_DAY` hand-writes what `nextUtcDay` exists to own | **rejected** | — | The sweep found two such sites, not one (`agenda.ts:325` and `booking-status.ts:68`) — but **#496 already owns both lines and its fix deletes them**, replacing the UTC step with a *local* calendar step. Filing this would ask a maintainer to formalise a boundary an open issue says must stop being a UTC boundary. Arithmetic is correct today and both sites carry an explanatory comment |
| U5-8 | Functional | `features/calendar/useAgenda.ts:86` | The agenda’s clock advances only when something else re-renders it | confirmed | #1290 | Fake timers across local midnight: buckets unchanged 20 minutes later, then an unrelated **Reorder** chip click moves a loan from Today to Overdue and another from This week to Today. The unrelated-interaction re-bucketing is the finding; the stale heading alone would be `cosmetic`. Orthogonal to #495/#496/#919, which are all about *which day* a stored date belongs to |
| U5-9 | Mechanical | `features/calendar/agenda-invalidation.test.tsx:130` | The lane-key guard is a textual check, so a factory member missing the `...agendaKeys.all` spread passes it | confirmed | #1291 | Reproduced by adding such a member: all eight guard tests green, and the stray key is genuinely missed by `invalidateQueries({queryKey: agendaKeys.all})`. No helper spreads `all` by construction and no type constrains a member, and the factory has been **hand-edited twice since the guard landed** (#621/W1a, #607). The missing nine-line assertion already exists in `report-invalidation.test.ts:84` |
| U6-1 | Functional | `features/alerts/alerts.ts:285` | The expiry lane re-grades with the hard-coded 30-day default, so a widened window adds nothing | confirmed | #1292 | `buildAlerts` takes no window parameter at all, so the hook cannot thread the preference in. Narrowing works; widening does not — 335 of 365 valid values affected. `buildFieldDueAlerts` in the same file carries its window per row *and states the reason*. **Secondary limb rejected as a misread**: `laneTotals` is parameterised too; there is no `90` in the feature |
| U6-2 | Mechanical | `features/alerts/useReminderNotifications.ts:39` | The notified set is reconciled against an unsettled feed, so it is wiped on every load and reminders re-fire | confirmed | #1299 | Store value across the sequence: `{a}` → **`[]`** → `{a}`, with `show` called once. `kept` is computed before the quiet guard, so the wipe happens on the quiet path too. Two dampeners recorded and neither is fatal — the `tag` suppresses a re-buzz only while the notification is still in the tray, and the summary threshold caps a volley at one. The sibling `useAlerts` guards the identical hazard, with a test pinning it |
| U6-3 | Performance | `features/alerts/ReminderNotifications.tsx:52` | A background-sync wake invalidates the entire query cache | **rejected** | — | Blast radius smaller than claimed — query-core v5 defaults `type: "active"`, so only observed queries refetch, and the always-active set is the 5 shared alert lanes. Frequency kills it: `REMINDER_PERIODIC_SYNC_MIN_INTERVAL_MS` is 12 h and browsers throttle further, so the ceiling is ~2 wakes a day. Residue is that the comment says "the alert feeds" while the call sweeps everything — one of five such sites, the other four being whole-database moments |
| U6-4 | Prompt | `docs/wiki/Alerts.md:5`, `Reminder-Notifications.md:3` | Both pages list overdue loans as an Alerts lane; there are five lanes and none is loans | confirmed | #1293 | `git log -S` shows the wiki always over-promised — the claim arrived with the page (2026-07-11) and `AlertKind` has never changed shape; `grep -rni loan src/features/alerts/` returns nothing. `Reminder-Notifications.md` contradicts itself two paragraphs apart. #684 is the precedent for the same class on this page and was resolved by making the claim true |
| U6-5 | Functional | `features/alerts/AlertsScreen.tsx:275` | "Show all (N hidden)" names the feed and clears the whole dismissal store | confirmed | #1294 | Button read "Show all (1 hidden)", store went 6 → 0. A store holding more records than `hiddenCount` is the *designed* steady state — `pruneDismissals` names three preserved cases. `clearAll` has one production caller. Real cost is that the wiped records are the ones *not* on screen, so alerts resurface later with nothing connecting them to the press |
| U6-6 | Functional | `features/alerts/AlertsScreen.tsx:213`, `ReminderSettings.tsx:153` | Literals left on two converted screens, plus a concatenated description | **rejected** | dup #242 + #213 | The framing is also wrong: `git log -S"useT"` shows these screens were never converted — the three `useT()` calls are new strings added through the seam as CLAUDE.md requires. All three proposed carve-outs fail: no `en.json` value is duplicated as a literal here; the concatenation is a `description` prop, one of the categories #242 counts; and the `aria-label` literals are two of #213’s 183 |
| U6-7 | Mechanical | `features/alerts/alerts.ts:600` | `maintenanceDueAtMs` carries a three-way parity claim with no drift test, and its tests avoid every DST transition | **rejected** | dup #839 | Both limbs true and demonstrated — mutating to `+ N * 86_400_000` leaves 66 tests green, and a zone-pinned probe shows a 1-hour divergence at a New York spring-forward. Rejected because **#839 names the same three sites, quotes the same comment, and its *Expected* is already "One definition, or a drift test that drives all three."** Merged with U7-10 into one comment there |
| U6-8 | Functional | `features/alerts/AlertsScreen.tsx:416` | A truncated lane’s notice quotes a post-dismissal count against a pre-dismissal total | confirmed | #1294 | Merged with U6-5 — one root cause (counts computed over the filtered view, presented against unfiltered sets). "Showing the 1 most urgent of 4,231" over 1 card. The claim is not merely odd: dismiss the most urgent row and the surviving N are *not* the N most urgent. Low magnitude — the user caused the hiding and `hiddenCount` is on screen |
| U6-9 | Mechanical | `features/alerts/useNotifiedRemindersStore.ts:40` | The custom `merge` checks only `Array.isArray`, not that the elements are strings | **rejected** | — | Invariant genuinely broken (`["a",42,null,{"x":1}]` rehydrates verbatim), but every reader traced and none can break: `planReminders` drops non-matches on the **first** pass and `replace` re-persists clean, `sameIds` does no string work, and nothing else reads the set. No `.startsWith`, template literal or `join` reachable. `version`/`migrate` both present, so the #372 guard covers it |
| U6-10 | Functional | `features/alerts/AlertsScreen.tsx:204` | Every alert card’s link has the accessible name "View in inventory" | confirmed | #1295 | Two links, one distinct name; the two sibling controls on the same card are named per alert, and the card’s own comment states that rule. The catalog already has the `{title}` interpolation pattern in both. Checked against the whole accessibility programme — those name a *wrong or unexposed* accessible name, not N controls sharing one correct one |
| U7-1 | Functional | `features/activity/activity-kind.ts:98` | An unknown action displays as Lifecycle but `actionsForKinds` can never select it | confirmed | #1296 | Two passing assertions show the asymmetry directly. Reachability is designed-for, not hypothetical: `item_history.action` has no CHECK precisely so a new action needs no migration, and `activity-export.test.ts:68` already pins the newer-peer row. Default path is safe (no `WHERE` while all chips are on); the defect fires on the first toggle and reaches the export |
| U7-2 | Functional | `features/maintenance/db-maintenance-actions.ts:367` | The drift report states an 11-row-per-level sample as the true count | confirmed | #1302 | 30 drifted rows reported as 11. No qualifier anywhere, and the `+1` sentinel only makes the overflow always read "…and 1 more". `result.ok` is correct, and there is **no repair action anywhere in the app**, so the advice does not change with the number — which is what keeps it `cosmetic`. The health check two cards up prints a true count, so the modal is internally inconsistent |
| U7-3 | Mechanical | `features/maintenance/db-maintenance-actions.ts:318` | `checkSearchIndex` rebuilds the FTS index outside the gate the module says covers all its writes | confirmed | #1303 | A `storage:read` session rebuilt the index where compact and sweep both refused. **Narrowed by measurement**: snapshotting `items_fts_data` shows `integrity-check` leaves the bytes identical, so there are two ungated-question actions, not three. Blast radius nil — the index is fully derived — so the finding is the three false comments, not an escalation. Not #847, which is about repository guard *test coverage* |
| U7-4 | Functional | `features/maintenance/DatabaseMaintenanceDialog.tsx:256` | "Compaction failed — nothing was changed" is false after a partial compaction | confirmed | #1301 | FTS segments merged 12 → 3 before the VACUUM threw. Merged with U7-5 — the same `catch` block, one fix. On its own this limb would not clear the bar: the sentence is substantively true about the user’s *data*, which is what the reassurance is for |
| U7-5 | Mechanical | `features/maintenance/DatabaseMaintenanceDialog.tsx:97` | Seven bare catches replace the cause with a fixed sentence and log nothing | confirmed | #1301 | Rendered generic pasted beside what `useErrorMessage` yields for the same `DbError`. All seven handlers are inside the component body, so the hook is callable. Invisible to the build guard, which matches only the ternary — a site #681’s fix never covered, not a regression. VACUUM needs ~2× the file size free, so `SQLITE_FULL` is the *most likely* failure of the tool a user reaches for when space is tight |
| U7-6 | Functional | `features/activity/ActivityFeedScreen.tsx:162` | The item-name link targets the bare `/inventory` route with no search params | confirmed | #1297 | Anchor pasted: `data-to="/inventory" data-search="ABSENT"`, and no comment records the choice — unlike the location lane, which has a nine-line rationale and a test. `view-params.ts:158` makes an exhaustiveness claim this site falsifies. Dead field `itemIsActive` folded in: joined, typed and mapped, read nowhere, and it is exactly the fact needed to reach a soft-deleted item |
| U7-7 | Functional | `features/activity/ActivityFeedScreen.tsx:521` | A failed load announces "No recent activity", and the error text has no role or live region | confirmed | #1298 | Three readings pasted, including the false first-load announcement. The latch holds the last announced *lane*, so it never re-announces on a count, filter or error change. **The `role="alert"` framing was dropped** — for a list-level failure the house pattern is a live-region announcement with a plain `Surface`, which is what `AlertsScreen:310` does. Calendar’s dead-code error branch folded in as the sibling half |
| U7-8 | Functional | `features/activity/ActivityFeedScreen.tsx:159` | A decorative `HistoryIcon` lacks `aria-hidden` where its two siblings have it | **rejected** | — | Disproved by rendering: lucide-react’s `Icon.mjs:36` adds `aria-hidden="true"` itself when there are no children and no a11y prop, so both rows are byte-identical in the accessibility tree. The explicit attribute on the siblings is redundant, not a missing counterpart. Not #223, whose concern is the *opposite* branch of the same line |
| U7-9 | Functional | `features/lifecycle/maintenance.ts:71` | "days overdue" floors a signed span whose operands are not midnights, so one minute late reads "1 day(s) overdue" | confirmed | comment #1164 | #1164 (open) already names the site, the mechanism and the ledger permanence. **Two additions carried there**: the early side is wrong too (23 h early reads "on the day it was due", which #1164 says is fine), and a New York fall-back crossing adds a *second* whole day — both zone-pinned. The arithmetic is exactly `ceil(lateness)`, so the defect is the calendar-day phrasing, not the floor |
| U7-10 | Prompt | `features/lifecycle/maintenance.ts:64` and 4 others | Five sites derive the due instant independently, three claiming parity, with no drift test | confirmed | comment #839 | Corrected the count from four to **five** (`create` and `logPerformed` are separate derivations) and found the site #839 does not list — `item/maintenance-default.ts:68`, whose comment says "Mirror `MaintenanceRepository.create`". Three whole-suite mutations: two left **12448 tests green**, the third failed only its own unit test. No test drives any pair. Merged with U6-7 |
| U7-11 | Mechanical | `db/repositories/MaintenanceRepository.ts:154`, `:183` | Two reads are OFFSET-walked under a non-total `ORDER BY`, so a page boundary could duplicate and drop | **rejected** | — | **Six attacks, zero reproductions** on a real SQLite: 250 and 5,000 and 20,000 tied rows, a forced plan change via a new index plus `ANALYZE`, `cache_size = 1`, and inserts between pages — all returned every row exactly once. Already recorded twice as a carry-over (register row C5-8 and §11’s Phase 3 row), and the live half of the same `ORDER BY` is open as #1166, which states the tiebreak belongs in its change |
| U7-12 | Performance | `features/maintenance/db-maintenance-actions.ts:493` | `gatherDatabaseStats` issues one worker round-trip per table (51 tables, 57 calls) | **rejected** | — | Count established (51 reportable of 56). The in-memory driver cannot measure the transport the claim is about — 51 serial `COUNT(*)`s measured 0.15 ms against 0.17 ms for one `UNION ALL`. Bounded instead by this audit’s own browser-measured 0.0425 ms/round-trip: ~2.4 ms desktop, ~24 ms on a 10×-slower phone, for one deliberate press behind a spinner. Same band as P1-54, P2-18, C4-3 and U5-8, all rejected |
| U7-13 | Performance | `features/maintenance/DatabaseMaintenanceDialog.tsx:322` | The orphan sweep invalidates every query in the app | **rejected** | — | query-core 5.102.6 read: a bare `invalidateQueries()` refetches only **active** observers. The call is not gratuitous either — the sweep changes `imagesBytesOnDisk()`, which `useStorageBreakdown` reads. One of five bare-sweep sites, the other four being whole-database or whole-authority moments. Consistent with U6-3’s rejection. Residue: the *automatic* sweep invalidates nothing at all |
| U7-14 | Prompt | `docs/wiki/` | Database maintenance — seven user-facing tasks — has no wiki page | **rejected** | dup #580 | Facts confirmed (zero hits for "database maintenance", "orphan", "integrity", "Verify stock totals" across 99 wiki files; no page-map entry). #580 (open) states this almost verbatim, down to the same seven functions and the same greps. One claim refused for any issue body: "deletes user photo files with no confirm gate" is misleading — the sweep only removes OPFS files with no owning row in any table |
| U7-15 | Functional | `features/activity/activity-kind.ts:17` | The six filter chips and every row verb render in English on a translated screen | **rejected** | dup #242 | #242 names `features/activity` (8 strings). The row *verbs* are not in this feature at all — they come from `inventory/history-format.ts:17`, which **#1183** already names with pasted German output. Carried to #242 as a scope note: the chip labels double as **export column content**, which the tabular-export seam keeps in English, so the naive "route the constant through `t()`" fix would silently translate the file |
| U7-16 | Prompt | `docs/wiki/Activity-Log.md` | The wiki says every entry records who made it; no surface shows or exports an actor | **rejected** | dup #774 | Every factual claim holds and every one is already in #774, which quotes the same wiki sentence plus two more, names the same drop point (`mappers.ts:481` — "Eight fields are mapped; the ninth is not"), notes the same location-ledger asymmetry, and goes further by grading the Storage-Triage prune as `data-loss`. Not #522, which was about read-permission enforcement and was fixed |
| U7-17 | Functional | `features/export/export-every-page.ts:72` | Export filenames are stamped with the UTC calendar day | confirmed | #1304 | Zone-pinned both ways: a 17:30 export in Los Angeles is named for tomorrow, an 08:30 export in Tokyo for yesterday. Ten list exports use the helper and four more sites repeat the pattern. Graded `cosmetic` with the counter-argument recorded in the issue — the same module deliberately uses UTC for `isoTimestamp`, so making the *name* local while contents stay UTC is a considered inconsistency |
| U8-1 | Functional | `features/settings/SettingsDialog.tsx:1147`, `:577` | Two number fields clamp on every keystroke, so 10–49 cannot be typed | confirmed | #1319 | Browser-driven: typing `2`,`0` gives per-keystroke `["5","50"]` and a final `50`, with `setting-expiry-days` (min 1) as a passing control. **Narrowed from seven fields to two** — the five with min 0 or 1 have no untypeable targets. Escape hatches measured: 15 arrow presses work on desktop; on a Pixel 7 profile typing still gives 50 and tapping the spinner corner does nothing. These rows pass `calc={false}`, so `NumberInput`’s bounds seam is bypassed |
| U8-2 | Functional | `features/settings/SettingsDialog.tsx:286` | With the custom accent on, the Colour swatches are inert while giving full feedback | confirmed | #1320 | Browser-driven in **both** modes: `--primary` identical for Orange and Green while `data-accent` tracks the swatch and the preference persists. All 32 `[data-accent]` rules declare exactly the four properties `CUSTOM_ACCENT_PROPS` writes inline on the same element, with no `!important`. Swatches self-preview because the rule matches the button. **Correction**: the Branding hint *does* state the relationship; the Colour row does not |
| U8-3 | Prompt | `features/settings/SettingsDialog.tsx:646` | *Accent hue* is the one `SettingRow` with no `hint`, so the search cannot match its vocabulary | **rejected** | — | Count right (1 of 56), consequence wrong: the search matches `[scope, label, description, hint]`, and five of six plausible queries find the row. Shape also wrong — across every file rendering a `SettingRow` it is **10 of 69** without a hint, in six files, including the Danger zone’s "Erase data". Only the docstring claim would be filable, and that is `cosmetic` sweep material |
| U8-4 | Mechanical | `features/settings/SettingsDialog.tsx:1744` | `LabelBaseUrlControl` hand-rolls a bare `<input type="url">` copying `fieldClasses` | **rejected** | dup #671 | Diff reproduced (14 of 18 tokens identical; `placeholder:text-muted-foreground` among those dropped). #671 (open) names the same file and the same four utilities with a *better* diagnosis — Tailwind preflight makes the placeholder 50%-alpha `--foreground`, and the `data-contrast="high"` block never reaches it, so it is a live contrast regression. The `type="range"` exclusion checked and correct |
| U8-5 | Functional | `features/backup/settings-groups.ts:191` | `allowOnlineProductLookup` sits in a live-syncable group, so a network consent travels between devices | confirmed | comment #849 | #849 already records the asymmetry in its own body ("The master switch above it does travel"). **Not filed separately**; the comment adds the single-user multi-device case #849 does not cover and the shared-backup case. Harm narrowed honestly: device B makes no unprompted call — what is skipped is the one-time consent modal, the only surface naming the host. `git log -S` shows the placement predates the #616 principle |
| U8-6 | Functional | `db/repositories/TagRepository.ts:48`, `:194` | The Tags filter and merge picker match through SQLite `LIKE`, which folds ASCII only | **rejected** | dup #830 | #830 (open) names **both** cited line ranges explicitly and states the merge-button consequence in its opening paragraph. #753 is a genuinely different surface (the search parser’s `tag:` predicates), and #830 says so itself. One detail carried to #830: the screen test’s stub uses a **JS** fold and no `LIMIT`, so it is strictly more forgiving than the SQL and can never observe either defect |
| U8-7 | Functional | `features/tags/TagsScreen.tsx:388` | The merge picker is two capped reads (100 names, 20 matches) whose comment claims it reads the whole dictionary | confirmed | #1321 | `suggest("pro")` returned 20 of 30, and `listNames` stopped at `tag-100`. A target outside both caps leaves **Merge** permanently inert with no explanation, on the screen built for de-duplication. #386 (closed) is the same defect on Suppliers and was filed and fixed; this instance is worse, since Tags discloses nothing *and* asserts completeness |
| U8-8 | Mechanical | `features/tags/TagsScreen.test.tsx:177` | "keeps the list usable when the count query fails" asserts only that a row rendered | confirmed | #1322 | Deleting the fallback leaves all 16 green. The obvious strengthening is **not** enough — `Pagination` returns `null` below two pages and the stub supplies no `offset`, so `undefined + 1 = NaN` and no pager renders either way. A discriminating test needs an `offset` and 11 rows; shown red on the mutant and green on real code |
| U8-9 | Prompt | `features/settings/shared-low-stock.test.ts:58` | The reset comment claims three stores are reset; `setState({})` resets one | **confirmed (folded)** | #1322 | Zustand 5.0.15 merges an empty patch, so `useLayoutStore` and `useSavedSearchesStore` survive it (`density` stayed `compact`). Nothing fails today and plausibly will not — this file’s assertions read specific row ids. Folded into U8-8’s issue as the second bullet rather than filed alone, since it is the same habit with a one-line fix already written in the sibling file. (Line reference corrected from the finder’s `:271`.) |
| U8-10 | Performance | `features/settings/SettingsDialog.tsx:205`, `:1657` | The dialog subscribes to the whole store, and the search mounts all ten tabs per keystroke with no debounce | confirmed | #1323 | **Store-subscription limb rejected**, search limb confirmed. Long tasks collected in-page against a control group typing into a plain field: unthrottled the control produces **zero** long tasks and the search two, worst 111 ms. At 6× the search totals 4294 ms against the control’s 2558 ms. Caveats recorded: 6× is a ratio not a phone timing, and CDP over-applies to `Intl` |
| U8-11 | Prompt | `features/settings/theme.ts:8`, `theme-registry.ts:331` | Docstrings say four data attributes where eight are set, and 14 accents where 16 ship | confirmed | #1324 | Counted: 15 `root.dataset.*` lines = eight attributes; `ACCENTS.length` = 16. A **third** stale site found — `applyAppearance`’s own JSDoc omits `data-anim-level` and `data-surface`. `data-reduce-effects` is no longer a switch but derived from `animationLevel`. Mitigation recorded: the in-body comments are accurate, so only the two docstrings a reader meets first are wrong |
| U9-1 | Prompt | `features/home-assistant/steps/BridgeStep.tsx:136` | The step says to create `bridge/.env` and start from the repository root, where `serve.mjs` resolves `.env` against the cwd | confirmed | comment #738 | Reproduced with the real `serve.mjs`: from the repo root it exits 1 on a missing snapshot path; from `bridge/` the same file is read. The `catch {}` swallows the ENOENT, and the guide’s "Something else" outcome then tells the user to re-check env values that are correct. **#738 (open) is the same defect** but names only `serve.mjs`, `mcp.mjs` and the two READMEs — the screen is a third site, carried there |
| U9-2 | Prompt | `features/home-assistant/steps/BridgeStep.tsx:159` | The `docker run` omits the `/state` volume the Dockerfile mounts, so the bridge mints a new identity per container | **rejected** | — | The command divergence and the random mint are both real. The claimed consequence is not: `custom_components/gubbins/__init__.py:237` runs `_async_reconcile_unique_id` on **every** setup and its docstring names this exact case — "Adopting it keeps the entry, its entities and its history intact". The config-flow path falls back to host/port matching. Entities key on `entry_id`, not the bridge id. This is the shipped fix for #672 |
| U9-3 | Prompt | `features/home-assistant/steps/SentencesStep.tsx:163` | "Where did I put the nails?" is not in the sentences file the user was told to install | confirmed | #1305 | Both grammars listed in full; the two differences are exactly the two phrases the guide prints. Merged with U9-4 — one root cause, one fix. **Both sites verified unbranched**: the Google-speaker card opens *after* the wiring conditional closes, and `FinishStep` structurally cannot branch, since `GuideState` carries only the token |
| U9-4 | Prompt | `features/home-assistant/steps/FinishStep.tsx:33` | "How many M3 washers do I have?" is not in the conversation-automation grammar | confirmed | #1305 | Merged with U9-3. The "Assist doesn’t react" branch never mentions it, and instead offers "where is my M3 bolt", which *does* match — so the real gap is never diagnosed |
| U9-5 | Prompt | `features/home-assistant/steps/TokenStep.tsx:59` | The guide recommends a narrow role and never names the permissions the bridge requires, so a 403 reads as unreachable | confirmed | #1306 | Every link verified by driving the shipped roles: **six of eight built-in roles are 403 on every bridge route including `/health`**, because none holds `bridge`. `api.py` maps only 401, so the 403 becomes `ClientResponseError` → `GubbinsConnectionError` → `cannot_connect` → "Could not reach the bridge…". `grep` for `403\|bridge:read` across the feature returns nothing. Screen half merged into U9-6; the `api.py` mis-mapping belongs to Phase 14 |
| U9-6 | Prompt | `features/home-assistant/steps/DataStep.tsx:158` | The reply-interpretation list omits the 503 a wrong snapshot path produces | confirmed | #1306 | The 503 precedes token resolution (`server.ts:484`) and the bridge’s own test pins it; the watcher treats a missing file at boot as non-fatal, so a typo yields a running bridge that 503s everything. No catch-all row. `docs/wiki/Running-the-Bridge.md` states the fact correctly, so the screen is the outlier. Absorbs U9-5’s screen half and U9-7 |
| U9-7 | Prompt | `features/home-assistant/steps/FinishStep.tsx:90` | The quoted body is `{"error":"Unauthorized"}`; the bridge sends `{"error":"Unauthorised"}` | confirmed | #1306 | `respond.ts:255` settles the shape — the legacy body carries the *message*, and the step sends the user to a legacy path. Driven: `{"error":"Unauthorised"}`. The v1 envelope’s lowercase `"unauthorized"` is a nested `code` and never produces the quoted literal |
| U9-8 | Prompt | `features/home-assistant/steps/BridgeStep.tsx:152`, `:171` | The Docker and systemd branches still name an inbound bridge token that #79 removed | confirmed | #1307 | Every `GUBBINS_*` name enumerated: the only credentials are **outbound**. The generated env file emits only the snapshot path and host. Both sentences contradict their own file’s docstrings — a half-applied edit. The shipped unit file carries the same stale phrasing on its line 48 |
| U9-9 | Functional | `features/home-assistant/HomeAssistantSetupScreen.tsx:35` | Branch choices live in per-step `useState`, so leaving a step discards them | confirmed | #1308 | Round trip driven: both choice cards unchecked, and the `.env` snippet, start command and "Did it start?" section all gone. **The intro sentence does not carry the finding** — it promises free navigation, not state preservation. What does is that the step itself tells the user to go to the next step and come back |
| U9-10 | Functional | `features/home-assistant/guide.ts:28`, `:101` | Two docstrings assert a `?step=` deep link the route does not accept | confirmed | #1325 | Greps pasted: `isGuideStepId` is referenced only by itself and its test; no `validateSearch` on the route; no `?step=` anywhere in `docs/`. The dead-code half is *not* the finding — the docstring says `@internal Exported for unit tests only`, which is honest. Same class as #990 and #1018 in two other areas |
| U9-11 | Functional | `features/home-assistant/HomeAssistantSetupScreen.tsx:43` | Step navigation smooth-scrolls regardless of `prefers-reduced-motion` | **rejected** | dup #932 | #932 (open) names `HomeAssistantSetupScreen.tsx:46` by permalink alongside `lib/highlight.ts:108`, with a cross-engine Playwright proof. An independent sweep returns exactly those two sites, matching #932’s "a complete sweep found exactly these two" |
| U9-12 | Prompt | `features/home-assistant/steps/OverviewStep.tsx:21` | "Where are my M3 screws?" names an item the synthetic fixture does not have | **rejected** | — | All three sites quoted: the phrase is about the user’s **own** inventory, and none tells the user to test against the sample data. "Screws" is the repository’s standard generic example, appearing across 13 wiki pages. The memory note’s fixture caveat scopes itself to manual testing. The internal-inconsistency sub-point is too thin (the "answer" line is an example of the *shape*) |
| U9-13 | Mechanical | `features/home-assistant/HomeAssistantSetupScreen.tsx:34` | `GUIDE_STEPS[indexOfStep(stepId)]!` asserts over an expression that is `undefined` at `-1` | **rejected** | — | Every writer of `stepId` traced — three, all typed `GuideStepId` and all derived from `GUIDE_STEPS`. No persisted value, no URL read, no message, no test helper (`grep` for `localStorage\|useSearch\|URLSearchParams\|postMessage` across the feature returns nothing). Latent with no reachable path. Recorded on #1325: if `?step=` is implemented, `isGuideStepId` must be applied at that boundary |
| U9-14 | Mechanical | `features/home-assistant/components.tsx:176` | The copy button’s 2 s reset timer is never cleared | confirmed | #1309 | Fake timers: the second confirmation dies at 1.1 s. **The unmount limb is a non-finding** and was dropped — React 19 makes a `setState` on an unmounted component a silent no-op. The `aria-live` half is the sharper cost and shares the root cause (two copies inside 2 s), but its fix belongs in the primitive — #967 |
| U9-15 | Mechanical | `features/home-assistant/components.tsx:213` | Live status is a hand-rolled `<span aria-live>` rather than the Foundry primitive | **rejected** | — | Sweep found **19** hand-rolled `aria-live` sites outside tests, of which #219 (open) names three. An issue for one of the 19 would be the wrong shape; carried to #219 as a scope note instead. The HA span is always-mounted, so it functions today |
| U9-16 | Mechanical | `features/home-assistant/guide.ts:122` vs `StepRail.tsx:20` | The progress percentage is computed twice, and the tested copy is the one production does not use | confirmed | #1310 | Mutation proof: `guide.test.ts` goes red while the rail’s `aria-valuenow` stays 25. `progressFor(...).percent` has no production consumer. Merged with U9-17 — the same root cause (values re-derived where the module already exports them) and CLAUDE.md’s "delete one of them" |
| U9-17 | Prompt | `features/home-assistant/steps/OverviewStep.tsx:120` | Overview hardcodes `<YOUR_BRIDGE_TOKEN>` where five sibling steps call `tokenForDisplay` | confirmed | #1310 | **The ordering objection was tested and fails**: `GuideState` is unpersisted but lives for the whole mount, and the rail lets a user return to Overview with a token entered — demonstrated, with `FinishStep` splicing the real token on the same run while Overview shows the literal. Carries the user-visible half of the merged issue |
| U9-18 | Functional | `features/i18n/catalogs/en.json:308` | The nav tooltip promises the guide will "generate an access token"; the generator was removed by #79 | confirmed | #1311 | Both catalog values quoted (German says "erzeugen" too), against the step’s own docstring recording the change and the shipped test asserting no generator control exists. **The detour is real**: `TokenStep` imports no `Link`, while `DataStep` links to `/sync`. Two further stale internal claims found in the screen’s and the test’s docstrings |
| U9-19 | Functional | `features/home-assistant/steps/TokenStep.tsx:24` | "Save to this device" writes only `bridgeToken`, while Push to bridge also needs `bridgeUrl` | **rejected** | — | The factual half holds and there is no working default for the URL (`bridgeUrl: ''`, and both consumers gate on both). But the toast says *where the token will be used*, not that the feature is configured — and the guide sets the URL one step later, naming it twice with a `<Link to="/sync">`. No half-configuration is left |
| U10-1 | Functional | `features/webhooks/WebhooksScreen.tsx:145` | Every test-fire failure collapses to "could not reach the bridge", discarding six distinct reasons | confirmed | #1312 | All six driven through the real screen to one string. The six keys exist in both catalogs and are already used by the delivery log. **Two corrections**: a failed test writes no log row (the contradiction arrives via the log’s *banner*), and the announcement is `sr-only`. Adjacent observation folded in — a sighted user sees nothing at all on a transport failure |
| U10-2 | Functional | `features/webhooks/WebhooksScreen.tsx:196` | The coverage note says location changes cannot raise a webhook; seven location events ship | confirmed | #1313 | `git log -S` dates the copy to 2026-07-18 and the events to 2026-07-31 (#691). A new subscription is created subscribed to all seven, captured live. **The rest of the sentence checked and still true** — all 26 catalogue entries enumerated, and no PO/supplier/project/tag event exists. Absorbs U10-12 |
| U10-3 | Functional | `features/webhooks/components/WebhookFormDialog.tsx:160` | The `ref` signing branch has no completeness check, so a blank secret name saves an unsigned webhook | confirmed | #1314 | Driven: save succeeds with `secret: null, secretRef: null`, no alert, button enabled — while the `inline` control branch refuses. `ref` is the **default** mode. Scope corrected for the issue: an unsigned webhook is a legitimate configuration (`subscription.ts:20` says so), so the defect is that the *chosen mode* is silently discarded and the reassurance shown beside it is false |
| U10-4 | Functional | `features/webhooks/subscription.ts:81` | A URL with embedded credentials is accepted, can never deliver, and the password is echoed into the log | confirmed | #1315 | Whole chain driven: userinfo survives `new URL().href`, the SSRF guard classifies `hostname` only, Node’s `fetch` throws with the password in the message, and `recordOutcome` redacts `url` but not `detail`. **Not third-party exploitable** — the user types their own secret and it is shown back to them. URL confirmed rendered as text, never an `href`. Not a reopening of #520, whose disclosure now covers row data |
| U10-5 | Mechanical | `features/webhooks/filter-form.ts:150` | An empty `any` filter round-trips from "match nothing" to "no filter" | confirmed | comment #842 | Round trip demonstrated; `{kind:'all', of:[]}` does not invert. **Latent** — `formToWebhookFilter` can never emit an empty combinator, so the only writers are a synced peer, a restore or a hand-edit. Not a duplicate of #842 (whose mechanism is the *mapper* collapsing an unreadable shape on every read) but the same consequence by a second route, so carried there as a scope note |
| U10-6 | Functional | `features/webhooks/components/WebhookFilterBuilder.tsx:243` | The tag filter offers only the first 100 tags, and shows a raw UUID for a stored id past them | confirmed | comment #1050 | UUID fallback demonstrated in the chip *and* in its remove button’s accessible name, with a passing control. The two sibling kinds on the same line use `listAll()`. Carried to #1050 rather than filed — four open issues already describe the same cap — with the distinct UUID consequence and the note that `useTagDictionary` has no `listAll` counterpart |
| U10-7 | Functional | `features/webhooks/queries.ts:25` | The webhook list reads one capped page and discards `hasMore` | **rejected** | — | A recorded decision ("a webhook is a deliberate, hand-made thing"). The sync counter-argument was tested and fails: `create` has exactly one call site, one subscription per submit, no import and no bulk path, so 101 rows needs a person to hand-build them. Cheap residue (`hasMore` is in hand and thrown away) carried to #1050 |
| U10-8 | Prompt | `features/webhooks/components/WebhookDeliveryLog.tsx:149` | "Blocked" is explained as an exhaustive pair of causes; the bridge blocks for nine | confirmed | #1316 | All nine ranked by reachability: three are impossible because the app rejects them at save, five are the private family the hint covers, leaving **exactly one** uncovered — DNS failure, which `homeassistant.local` hits routinely. Kept low because the correct reason renders one line above — but that line is English-only, so a German user gets a correct English sentence over an incorrect German one. **Not a residue of #643**: the hint predates the row it describes |
| U10-9 | Functional | `features/webhooks/header-rows.ts:43` | Two header rows with the same name collapse silently | confirmed | #1317 | Five assertions. Case-differing names survive as two keys and are combined by undici into one — proved with a real Node snippet, not asserted. The filable half is the missing `duplicate` issue: the editor renders a `role="alert"` for all three members of its issue union and has no member for the mistake that changes what is sent |
| U10-10 | Functional | `features/webhooks/components/WebhookHeadersEditor.tsx:109` | Header values are not validated against the grammar the transport enforces | confirmed | #1317 | Merged with U10-9. **The premise was corrected**: names are not validated either — `webhookHeaderIssue('X Foo')` and `('X-Föö')` both return `null` while undici throws. Real boundary for values is U+00FF: Latin-1 passes to the wire as mojibake, an em dash or curly quote throws. Every delivery then fails permanently with a raw `TypeError` in the log |
| U10-11 | Functional | `features/webhooks/template.ts:131` | A custom template interpolates without escaping and is sent as `text/plain` | confirmed | #1318 | Invalid JSON demonstrated with `1/2" brass elbow`, and five template paths are free text an item name reaches. Every editor string read — no hint mentions escaping or the content type, and the field’s placeholder is plain text. The shipped test blesses the JSON pattern with a quote-free value, and the preview fixture has no `"`, `\` or newline. One issue, two limbs, one symptom |
| U10-12 | Functional | `features/webhooks/components/WebhookFilterBuilder.tsx:108` | Any filter silently excludes every location event, and only the wiki says so | confirmed | #1313 | Mechanism confirmed by reading (`filter.ts:22` — "An unconfirmable fact does not match"). **The proposed mitigation was disproved**: the test fire builds its item event with `data.item = null` too, so *every* filtered subscription reports `unmatched` and the signal cannot distinguish a broken configuration from a healthy one. Re-anchored onto U10-2, since the fix is one edit in one place |
| U10-13 | Prompt | `features/webhooks/WebhooksScreen.tsx:56` | Copy exists for a `skipped` test outcome the test path cannot produce | **rejected** | — | Mechanism confirmed — one producer, guarded by a circuit `createWebhookTestFirer` deliberately constructs fresh. Rejected for this phase: nobody reads the string, and the key is required by `satisfies Record<WebhookTestOutcome, MessageKey>`. The real half is `bridge/src/openapi.ts:2111` advertising `skipped` on the test endpoint — **Phase 12/13 territory**, carried to §11 |
| U10-14 | Performance | `features/webhooks/useWebhookDeliveries.ts:204` | The delivery poll never backs off against a failing bridge | **rejected** | — | Measured: **361 requests in one simulated hour**, state `failed` throughout, no `clearInterval` on any failure path. Rejected on consequence — polls cannot pile up (a generation guard returns early), browsers clamp a hidden tab’s interval to ~1/minute, the screen is only mounted while looked at, and a `role="alert"` banner is telling the user the bridge is down the whole time |
| U10-15 | Functional | `features/webhooks/WebhooksScreen.tsx:243` | A repeated identical announcement produces no DOM mutation, so it is never re-announced | confirmed | comment #1107 | MutationObserver: one record for two identical presses, two for a differing pair (the control). `live-region.tsx` read in full — no key, nonce or effect. Not filed: the same mechanism reaches Bookings, Calendar and `number-input` (#967), and 30 of 59 call sites share the shape, so the fix belongs in the primitive |
| U11-1 | Functional | `features/alerts/ReminderSettings.tsx:103` | Choosing **On** for Reminders without notification permission silently reverts the control with no explanation | confirmed | #1300 | Found while driving all 56 settings controls in a browser. With `Notification.permission = default` the control returns to Off, `remindersEnabled` stays `false`, no live region fires and the panel shows no permission copy. Re-run with the permission granted: On sticks and persists — so the refusal is correct and only the silence is the defect. Also noted: the component captures `api.permission()` once at mount |

### Phase 12 — Bridge core and HTTP API

Pinned SHA: `41f919088feb0bfe80f281f50f72c3341f7eb835`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Mechanical | `bridge/src/server.ts:385` | A client-chosen request target defeats `requestBase`'s Host validation, so every absolute URL the bridge emits is caller-controlled | confirmed (**reshaped**) | **held** | Two defects on one line. The **poisoning** half is `cosmetic`: authz is unaffected (`url.pathname` normalises independently of the authority) and **no token leaks** — `feedChannel` deletes `token` from the self URL and the OData `nextLink` does the same, grepped to zero across RSS/Atom/JSON/calendar. To poison a victim's feed rather than one's own needs their URL *and* their token. The **throwing** half is a `unusable` unauthenticated remote crash, found during verification and recorded as P12-DOS |
| P12-DOS | Mechanical | `bridge/src/server.ts:385` | `new URL(...)` sits outside the `try` that begins at `:454`, so an unparseable request target is an unhandled rejection, and ten of them trip the fault budget | confirmed | **held** | **SECURITY, unauthenticated, no configuration prerequisite.** Reproduced independently by the lead against real `serve.mjs` on a stock read-only bridge: ten raw sockets sending `GET http:// HTTP/1.1` → `Bridge exiting after 10 faults in 60s`. Loopback by default, but the documented `GUBBINS_BRIDGE_HOST=0.0.0.0` opens it to any LAN host. systemd restarts (so sustained traffic is a permanent flap); documented `docker run` without `--restart` stays down. Not filed at the maintainer's direction |
| U1-2 | Mechanical | `bridge/src/serve.ts:792-795` | A bridge that fails to start hangs instead of exiting, so a supervisor never restarts it | confirmed | #1330 | `unusable`. The boundary is exactly `await watcher.start()`: `loadConfig` throws exit cleanly, everything after the watcher owns a handle hangs. Six configurations enumerated. **`EADDRNOTAVAIL` hangs identically** and is a boot-order race, not just a port conflict. `Type=simple` shows `active (running)` for ever; Docker shows `Up` with no healthcheck. Not #305, which is the post-bind path and is intact |
| U1-3 | Functional | `bridge/src/server.ts:66` | The 10 s whole-request timeout makes the documented 64 MiB push cap unreachable, and the caller gets a bare `408` | confirmed (**quantified differently**) | #1345 | The finder's "needs ≳51 Mbit/s, anything slower is killed" is **false**: `connectionsCheckingInterval` defaults to 30 s, so enforcement is coarse and a 1.6 Mbit/s push succeeded 3 of 4 times. Correct rule: at risk past ~10 s, always killed past ~40 s total. 64 MiB is only reliably reachable above ~54 Mbit/s |
| U1-4 | Functional | `bridge/src/server.ts:696-702` | An over-large write body is reported as `400 "Request body must be a JSON object."`, inconsistently with the push path's `413` | confirmed (**more reachable**) | #1346 | `TEXT_LIMITS.note = 20_000` while `MAX_BODY_BYTES = 8 * 1024`, so a note of 8k–20k characters is domain-valid and storable yet refused with a message that misdescribes the cause. `payload_too_large` already exists in `API_ERROR_CODES`. Aside recorded: `bridge/README.md:1243` claims the note bound is 500 characters |
| U1-5 | Functional | `bridge/src/server.ts:389-390`, `bridge/src/api/v1.ts:673-676` | The `405` `Allow` header contradicts itself and breaks the parity `api/v1.ts` claims | confirmed | #1347 | Found independently by two finders; one defect, two locations. `cosmetic` — no known consumer reads `Allow`; browsers use `Access-Control-Allow-Methods`, which is correct. **The `v1.ts` comment does not acknowledge the mismatch** — read in full it records widening `v1.ts` and believes the two now agree, so it is a parity claim that was already false when written. Also found: `OPTIONS` sends no `Allow` at all |
| U1-6 | Functional | `bridge/src/server.ts:447` | The CORS preflight omits the conditional-request headers, so the documented `304` path is unusable from a browser | confirmed (**narrowed**) | #1349 | `cosmetic`. The finder's promise-quote is imprecise: `README.md:2564`'s "every response" is about `Access-Control-Allow-Origin` being reflected so error bodies are readable, and that **is** honoured; every `304` example uses curl. So no promise is broken — conditional caching is simply unreachable from a browser, and no shipped consumer sends those headers |
| U1-7 | Mechanical | `bridge/src/serve.ts:461-470` | SIGINT/SIGTERM never closes keep-alive connections, so a polling client holds the bridge open | **rejected** | — | Premise is a pre-Node-19 fact: `server.close()` has closed idle connections since Node 19. Driven through the real SIGINT handler: `mode=polling` (the finder's exact scenario, `/health` every 1 s on one keep-alive socket) exited in **0.0 s**. The HA integration polls at 5 and 15 minutes against a 5 s `keepAliveTimeout`, so no socket survives between polls. Residue — a socket that never *completes* a request blocks shutdown for ever — is adversarial-only and `cosmetic` |
| U1-8 | Mechanical | `bridge/src/server.ts:440-452` | `OPTIONS` is answered before the rate limiter, so one request shape is unlimited | **rejected**; disclosure limb confirmed | #1348 | Bypass confirmed (12/12 `204` at capacity 5) but **rejected**: the handler is `writeHead`+`end`, answering preflights before auth is required by CORS, and it grants no amplification over the TCP an attacker already gets free. The **capability-disclosure** limb (identical to U3-3) is confirmed and filed |
| U2-1 | Functional | `bridge/src/config.ts:90-92`, `:315-322`, `serve.ts:509-510` | Discovery selects the destination of the Home Assistant token while five claims say it supplies an address only | confirmed | **held** | **SECURITY, unauthenticated.** One spoofed mDNS datagram sends `Bearer <token>` to any host, including public and cloud-metadata addresses. `resolveHaDiscoveryPlan` takes no `host`, so the loopback gate that guards the advertiser does not guard the discoverer; `discoveredUrl` prefers TXT over the A record with no correspondence check; `discover.ts` discards `rinfo` and short-circuits on the first answer. Mechanism module is Phase 13; escalated for exploitability. `serve.ts:509-510`'s "safe to log… it is a LAN host advertising itself" is factually false |
| U2-2 | Prompt + Functional | `bridge/src/version.ts:33-41` | `BRIDGE_SCHEMA_VERSION` is documented as the stored-schema generation, which #274 says it is not | confirmed (**stronger**) | #1331 | `wrong-data`. Driven with a real one-month-old checkout: `returned_quantity` silently dropped, outstanding reported as 5 where the truth is 2; against a 2026-07-19 checkout two whole tables vanish. **The app does worse than stay silent** — it renders "It is still reading your data correctly". The escalation cannot fire because `schemaVersion` correctly never moves. #870 explicitly defers this arm |
| U2-3 | Mechanical | `bridge/src/config.ts:346` | An over-long `GUBBINS_BRIDGE_MDNS_NAME` half-kills startup after the server is listening | confirmed | #1360 | `degraded`. Composite state driven: still serving (401), `exitCode` 1, **zero** SIGINT and SIGTERM listeners. **The limit is 63 bytes, not characters** — an ordinary 27-character Japanese name is 81 bytes and throws, which is a far better reachability case than the finder's em-dash example. `bridgeId` and `os.hostname()` are both guarded; the operator's own name is not |
| U2-4 | Functional | `bridge/src/mqtt/client.ts:123` | A broker password embedded in `GUBBINS_BRIDGE_MQTT_URL` is echoed into a startup log | confirmed | #1335 | `degraded`, security-shaped. Two plausible typos print `hunter2` verbatim. The docs **invite** the shape (`.env.example:140`: "Any `user:pass@` in the URL is ignored") and it genuinely is ignored on the happy path. Amplifier: the throw precedes `listen`, so `Restart=on-failure`/`RestartSec=5` writes it every five seconds indefinitely. Contradicts `README.md:2243` and the unit file's `# logs never contain a token`. Filed rather than held: not exploitable, one-line redaction |
| U2-5 | Prompt | `bridge/src/config.ts:229-234` | The documented default location of the webhook target file is wrong | confirmed | #1361 | `degraded`. Both placements staged: repo root → `0 target(s)`, `bridge/` → `1 target(s)`. Reads as deliberate because the two neighbouring path options in the same file make the same claim and **are** correct. Opposite direction to #738 |
| U2-6 | Functional | `bridge/src/bridge-id.ts:37`, `:120-121` | The persisted identity is keyed to the working directory, so it collides and changes | confirmed | #1362 | `wrong-data` / `degraded`. Both halves driven. Two bridges on one machine is a **supported setup in the project's own words** (`custom_components/gubbins/__init__.py:300`). The shipped unit and Dockerfile pin a *fixed absolute* path, which makes the collision more likely for two copies, not less. Residue of #672, whose fix introduced the identity but not where it is stored |
| U3-1 | Functional | `bridge/src/identity.ts:130-132` | `GET /api/v1/events` demands `bridge:read` only but streams the item DTO and the Activity Ledger | confirmed | **held** | **SECURITY, credential-gated.** A `[bridge:read, bookings:read]` token — the wiki's own recommended calendar account — gets 403 on eleven routes and 200 on the stream with the ledger `detail` note and the whole `ItemSummaryDto`. **The decisive argument is internal**: the stream is a live superset of `/api/v1/activity.*`, which the same file gates on `audit:view` with a written rationale. `GUBBINS_BRIDGE_WEBHOOKS=on` silently implies it. `LOOKUP_EVENTS` widens it to other principals' `/where` answers. Not #79 or #522 |
| U3-2 | Functional | `bridge/src/api/v1.ts:708-713` | A `stock:write` token with no read permission reads item detail from a write response | confirmed (**much narrower**) | **held** | **No shipped role is affected** — every built-in role with `stock:write` also holds `items:read`, and only Administrator and Manager hold any `bridge:*`. No enumeration (listing stays 403); every probe writes an attributed ledger row and rewrites the snapshot. The `delta: 0` limb is a **red herring** here (`+1`/`-1` gets the same body) and is a separate finding |
| U3-3 | Functional | `bridge/src/server.ts:389-391`, `:440-452` | An unauthenticated caller can enumerate which write capabilities are enabled | confirmed | #1348 | Same defect as U1-8's second limb; recorded once, filed once. Contradicts `README.md:123`'s explicit "deliberately indistinguishable" promise, so it is a defect regardless of the low value of the bit disclosed |
| U3-4 | Functional | `bridge/src/server.ts:341-348`, `bridge/src/api/v1.ts:234-236` | `POST /api/v1/webhooks/test` validates and advertises an `Idempotency-Key` it then ignores | confirmed | #1365 | `cosmetic`. Two identical keyed calls → `seq` 1 then 2, two outbound requests, two log rows, no `Idempotency-Replayed`. **Reframed**: the route should not be in the key path at all — `server.ts:543-546` already routes the other non-write POST *before* `readIdempotencyKey` with a comment saying why. `openapi.yaml` documents no key on this operation, and the only in-repo caller sends none |
| U3-5 | Performance | `bridge/src/rate-limit.ts:82-85`, `:101-108` | The "soft cap on tracked client keys" bounds nothing under a spray and turns every new key into an O(maxKeys) scan | **rejected** | — | Measured three ways. The frozen-clock model reproduces, but is not a deployment. The guard holds cleanly until 5,000 distinct keys/sec; the bridge's measured ceiling for the cheapest request is **2,745 conn/sec on loopback** (keys need distinct source addresses, so no keep-alive). Growth is self-limiting — equilibrium ≈8,600 entries, predicted 8,575, measured 8,780, about 1 MB. `README.md:2541-2551` and the module header both scope it as "a backstop, not the security boundary" |
| U4-1 | Mechanical | `bridge/src/hydrate.ts:56-58` | A hydrate that fails after the driver is created never closes it | confirmed (**bounded**) | #1357 | `degraded`. Re-measured: ~2 MB/failure on 4 items, ~14 MB on 20k. **Not unbounded and not permanent** — forced GC reclaims fully (300 failures → 122 MB), and left alone it sawtooths around a 2–2.5 GB ceiling. It matters because that ceiling is V8's cadence, not available memory, so a small host OOMs first. **The finder named the weakest trigger**: the watcher has no retry loop; `write.ts:483-486` and `push.ts:244-256` leak one per request |
| U4-2 | Mechanical | `bridge/src/watcher.ts:134-138` | The directory watch goes permanently deaf if the directory is replaced, and `/health` keeps reporting healthy | confirmed | #1329 | `wrong-data`. Established on **both** Windows and Linux. Understated by the finder: an atomic directory swap leaves `reloadFailures: 0` and `lastReloadError: null` — a *clean* report, not a low one. Also found: a **Windows-only event storm**, ~90,000 callbacks/sec pinning a core for the process's life, which recreating the directory does not stop. The finder's `'error'`-listener limb was **not reproducible** and is not the claim. Reachability is the weak limb and is stated as such |
| U4-3 | Mechanical | `bridge/src/watcher.ts:95` | The previous driver is closed the instant the swap happens, so an in-flight request fails | confirmed | #1339 | `degraded`. Driven end to end: 4,400 of 50,000 rows delivered, `curl` exit 18, probe confirming `database is not open`. **Only one genuinely wide window exists** — a sweep for non-driver awaits between driver reads across the whole API finds exactly `items-csv.ts:114`. The finder's second path is **wrong**: the webhook test-fire writes to the in-memory log, not the driver, and `webhook.ts:470-472` says so |
| U4-4 | Functional | `bridge/src/sqlite-source.ts:130-187` | A zero-length `.sqlite` hydrates successfully into an empty inventory and is swapped in as healthy | confirmed (**broadened**) | #1328 | `wrong-data`. **Broader than claimed**: any `user_version: 0` file qualifies, including an unrelated application's `.db`. **The finder's reachability story was disproved** — a slow copy resets the trailing debounce and never exposes the empty window (65 samples, all 4). Real triggers: a copy that dies after create, and a mispointed path. MQTT propagates it destructively (every HA location entity retracted); events correctly emit nothing. #507 is structurally unable to fire; the JSON path has the equivalent guard from #153 |
| U4-5 | Functional | `bridge/src/snapshot-io.ts:196`, `:229` | The atomic publish discards the snapshot file's permissions | confirmed | #1356 | `degraded`. Merged with U5-5 — one defect, three call sites. **Settled on real Linux** (`node:24-slim`, umask 0022): `0600` → `0644` after one `adjust-quantity`. The temp file is `0644` for its whole lifetime, a wider window than the published file. The project picks `0o600` deliberately for its two *least* sensitive files. Framing settled as "not preserved", not "should be 0600" — the latter would break folder sync |
| U4-6 | Mechanical | `bridge/src/watcher.ts:134` | `start()` throws if the containing directory is absent, though the line above promises a missing snapshot is survivable | confirmed (**reshaped**) | #1330 | Merged into U1-2's issue. **The comment is literally accurate** — it promises a missing *file* is survivable, and that is kept (bind, then 503). The divergence is a missing *directory*. Not uniformly benign: systemd gives a self-healing 5 s loop, Docker without `--restart` stays down, and **with MQTT on it stops exiting at all** because `mqtt?.start()` runs before `watcher.start()` |
| U5-1 | Mechanical | `bridge/src/resilience.ts:87-95` | The `clientError` handler replies but never destroys the socket, so every malformed request leaks a connection | **rejected** | — | Mechanism real, claim false. Node's `connectionsCheckingInterval` (30 s default, gated on the non-zero `headersTimeout`/`requestTimeout` the bridge sets) reaps them: real bridge, 25 half-open malformed clients, `getConnections()` 25 at t+1s → **0 at t+26s**. The finder's every observation window closed before the 30 s tick. Non-differentiating besides: a peer that sends *nothing* is held indefinitely with or without this handler. Residual is `cosmetic` |
| U5-2 | Functional | `bridge/src/push.ts:244-258` | Any hydrate failure of the served snapshot destroys the whole served dataset and answers `200` | confirmed | #1327 | `data-loss`. Live route proved: `SYNC_TABLES` grew three times this year (`asset_bookings` 2026-06-30, `settings` 2026-07-25, `location_history` 2026-07-31), so cancelling a booking on a newer build writes a tombstone an older bridge rejects wholesale. Observed `api_tokens` 1 → 0, de-authenticating every other client. The `formatVersion` route is **not** currently reachable (#870). `write.ts` on the byte-identical file 503s and touches nothing. #196 fixed this exact shape app-side only |
| U5-3 | Functional | `bridge/src/write.ts:553` | The advertised 15-minute idempotency window is really a fixed 128-entry global cap | confirmed (**sharper**) | #1355 | `wrong-data`. 10.3 s elapsed against a 900,000 ms window, `replayed: false`, quantity moved twice. **The budget is not per-owner although the docs say it is** — `alice replayed after bob 130 writes = false`. `openapi.yaml`'s "remembered in memory, **per token owner**, for about 15 minutes" affirmatively implies a budget the code does not provide. Residue of #567 |
| U5-4 | Mechanical | `bridge/src/write.ts:476-487` | A persistent write-path `503` discards its cause entirely | **rejected** | — | Premise false. The watcher hydrates the same file and logs the cause; `/health` carries `reloadFailures`, `lastReloadError` and `lastReloadErrorAt` live. Driven for all three cases — guard refusal, `ENOENT`, `EPERM` — each distinguishable from the logs alone, including when the bridge boots against an already-broken snapshot. Residue is a one-off transient with no log line: `cosmetic` |
| U5-5 | Functional | `bridge/src/push.ts:195` | The published snapshot inherits the temp file's default permissions | confirmed | #1356 | Same defect as U4-5, recorded once and filed once |
| U6-1 | Functional | `bridge/src/spoken.ts:63-65` | The spoken answer states the page size as the number of items found | confirmed (**stronger**) | #1363 | `wrong-data`. 30 matching items spoken as "I found 5". **Neither `/where` route accepts a limit at all** — `openapi.ts:1360-1366` declares only `q`, so 5 is unconditional, while `/api/v1/search` does honour `$top`. `countByAst` already exists and returned the true 30. `custom_sentences` maps "how many {item} do I have" onto this intent, so the count *is* the answer. MCP raises the ceiling to 25 |
| U6-2 | Functional | `bridge/src/spoken.ts:42-46` | A consumable gauge and an unlimited item are both spoken as "there's none in stock" | confirmed | #1333 | `wrong-data`, merged with U6-3 — one defect, two surfaces. An 80%-full spool is spoken as empty, unconditionally for gauges. HA reaches it unfiltered. The app's own `ItemStockValue.tsx` branches correctly, so this is a bridge-only divergence, and `events.py:71-85` already handles the same input shape deliberately. Not #1025. **The unlimited arm is narrower** — only when `quantity` is 0 |
| U6-3 | Functional | `bridge/src/query.ts:122` | `ItemMatch.quantity` contradicts the bridge's own published schema for an unlimited item | confirmed | #1333 | Merged with U6-2. **It is not only `0`** — a stocked unlimited item reports its raw finite count. The sharpest limb is **one endpoint contradicting itself**: `?fields=…` answers `null`, the same URL without it answers `3`. Provenance: commit `4d917daa`'s message records the review finding "ItemMatch.quantity nullability for /api/v1/search" as *fixed*, and its file list does not include `bridge/src/query.ts` — the specification was rewritten instead of the code |
| U6-4 | Performance | `bridge/src/item-detail.ts:22` | Every item-detail read drags the thumbnail BLOB the bridge can never surface | confirmed | #1334 | Found independently by two finders (≡ U8-2); recorded once. See U8-2 for the measurement and the corrected fix location |
| U6-5 | Functional | `bridge/src/spoken.ts:53-54` | The multi-location sentence recites every placement, with no cap | confirmed | #1364 | `cosmetic`. 13 placements = 250 characters, 40 = 601 (~40 s of speech). `MAX_SPOKEN_ITEMS` exists three lines above and is applied only in the sibling branch. The docstring ("a voice device should speak a sentence, not recite an inventory") is unkept. Kept separate from U6-1: this fix is one file, U6-1's changes the `/where` response contract |
| U6-6 | Functional | `bridge/src/inventory-scan.ts:102` | `forEachPage` silently truncates the location walk and `locationsTotal` publishes the truncated length | **rejected** (carried) | — | Mechanism confirmed by driving 10,100 locations: `/metrics` reports 9,998, and the per-location gauge series truncates too (boundary is 9,998 not 10,000 — the cap counts rows read, not rows kept). **Rejected on reachability**: there is no bulk-creation path for locations at all, and #1001 measures the location UI freezing from ~2,000. Both call sites are locations-only. §11 carry. The finder's `Keyset pagination seam` citation was **wrong** — the note says nothing about locations; the rejection stands on better grounds |
| U6-7 | Performance | `bridge/src/query.ts:118` | One location read per search row, then one stock read per row again | confirmed | #1359 | Same family as U8-3 and folded into its issue — the bounded per-row fan-out in the bridge's read projection |
| U7-1 | Functional | `bridge/src/api/odata-service.ts:82-96` | `$search` is accepted alongside `$filter` and silently discarded | confirmed (**reshaped**) | #1337 | `wrong-data`. **The finder's framing is partly wrong**: the drop *is* documented — `README.md:682-683`, `:706` and the shared `filterParam` all say so, and that parameter is attached to the OData paths. What survives is (a) the CSDL declaring `Searchable="true"` while the module and `README.md:794` promise "an explicit 400 rather than silently ignored", (b) `README.md:794` saying the non-`$` options "stay usable alongside", and (c) **the widening**: `?location=X` returns 1 row, `?location=X&$filter=quantity gt 1` returns 4. #626 judged the identical defect a bug in the app and fixed it |
| U7-2 | Functional | `bridge/src/api/odata-metadata.ts:160-164`, `:411`, `:421` | The `categories` CSDL contradicts the single-category payload in both directions | confirmed | #1353 | `degraded`, merged with U7-3. `fieldCount` is declared non-nullable and never sent; `fields` is sent and never declared; `$select=fieldCount` is a 400, so **no request produces the declared shape**. **The collection route is correct** — the divergence is entity-route-only, which the finder overstated. `locations` handles the analogue properly. Not #370 |
| U7-3 | Functional | `bridge/src/api/odata-metadata.ts:74` | `expiryDate` is published `Edm.Int64` and filterable, but only a quoted `'YYYY-MM-DD'` is accepted | confirmed | #1353 | Merged with U7-2. The capability annotations exist so "a client does not push down a filter the service will refuse". **`warrantyExpiresAt` is NOT the same defect** — declared `Edm.String`, stored ISO, and a string comparison works. The error naming the internal AST field `expiry` is a separable `cosmetic` limb in the same fix |
| U7-4 | Functional | `bridge/src/api/odata-filter.ts:308`, `:339` | Every `not` and `ne` spends a level of the 4-group depth budget | **rejected** | — | **The finder's demonstration does not reproduce**: at 3 nested groups both spellings return 200. The real trigger needs four alternating and/or levels *and* the `ne` on a leaf inside the innermost one; redundant parentheses — what a folding client actually emits — cost nothing. `ast.ts:91-108` documents the cost deliberately and by name. No realistic client filter reaches it |
| U7-5 | Functional | `bridge/src/api/odata-service.ts:427-432` | `$count`'s value is never validated, so any spelling but `true` silently omits the count | confirmed | #1336 | Merged with U8-6 — one line reads it for both surfaces. **Reachability downgraded**: no mainstream OData library emits `$count=1`; the producer is a hand-rolled client whose boolean stringifies capitalised. The OData surface rejects an *unknown option* with a 400 while swallowing a malformed *value* for a known one |
| U7-6 | Mechanical | `bridge/src/api/odata-filter.ts:167-171`, `:330` | `COMPARISON_OPS` is a plain object, so `constructor`/`__proto__` bypass the unknown-operator guard | confirmed | #1354 | `cosmetic`. **Not exploitable, checked hard**: every translator switch ends `default: throw unsupported(...)` (a 400), `parseASTtoSQL` runs before any SQL is assembled, and no other consumer of `FilterCondition.operator` exists anywhere. Stronger than the finder knew — **three** siblings are already hardened with comments saying why, including `OPERATOR_DISPLAY_NAMES` naming this exact hazard |
| U7-7 | Mechanical | `bridge/src/api/odata-metadata.ts:246` | A stray `export` orphans the parity docstring and widens the public surface | **rejected** | — | Source shape confirmed; stated consequence false. The parity claim is also stated at `odata-service.ts:66-73` and in `odata-metadata.test.ts:145-148`, so a reader arriving at either end finds it. Below §1's consequence bar; folded into the U7-2/U7-3 fix as a tidy-up |
| U8-1 | Mechanical | `bridge/src/api/params.ts:16-27`, `bridge/src/api/reads.ts:352-359` | A NUL byte in `q`/`$search` is a `500 internal_error` on eight routes | confirmed | #1338 | `degraded`. **The class is exactly one byte** — all 31 C0 controls, a lone surrogate, an overlong NUL and an invalid byte all return 200; outside FTS the NUL is harmless. **Reachable by accident**: a UTF-16LE string byte-copied into a UTF-8 request produces `q=E%00S%00P%003%002%00` → 500 where `q=ESP32` → 200. The log leaks absolute paths and the full SQL, contradicting `server.ts:374-378`'s "or the logs"; the caller's input never appears |
| U8-2 | Performance | `bridge/src/api/reads.ts:387-412` | Every bridge item read drags the thumbnail BLOB out and throws it away | confirmed (**fix location corrected**) | #1334 | `degraded`. Measured on 5,000 items × 6 KB thumbnails: page 2.44x, 1000-row 6.56x, full walk 9.53x; end to end over HTTP a full CSV is **1.74x for byte-identical output**. **The bridge cannot swap the constant** — the projection is hard-coded in `core.ts` (`:138`, `:154`, `:204`, `:271`) and `search.ts:196`, shared with the app, which genuinely renders thumbnails. Not #529, whose closing comment scopes its fix to the three attention feeds |
| U8-3 | Performance | `bridge/src/api/reads.ts:119-131`, `bridge/src/api/v1.ts:960-962` | A projected item page fans out extra queries per row | confirmed (**counts corrected**) | #1359 | `degraded`. Measured at the 100-row ceiling: plain **2** statements, `include=relations` **402**, `include=all` **802** — so 4 and 8 per row, not the 8/8 or 3/7 the finders reported. At 2,002 locations `include=all` is **140.9 ms vs 8.0 ms plain (17.6x)**, dominated by a whole-`locations` scan per row. The batched seam exists (`resolveItemFieldsMany`, built for #527) and is not used. `?fields=id` still pays a full location-tree read |
| U8-4 | Functional | `bridge/src/api/v1.ts:742-744` | `delta: 0` is accepted over REST, writing a phantom history entry the README says is refused | confirmed | #1358 | `degraded`. The row is unwanted, not false. `openapi.yaml` never promised refusal — **the README's 1:1 parity sentence is what makes it a defect**, and nothing holds it up. Cost measured: one zero-delta call against a 5,000-item snapshot took 775 ms and rewrote 3.3 MB → 9.9 MB. Kept **separate** from U3-2: that defect survives if zero is refused, and this one survives if the response body is narrowed |
| U8-5 | Functional | `bridge/src/api/v1.ts:673-676` | `/api/v1/items` advertises two different `Allow` sets for one resource | confirmed | #1347 | Same defect as U1-5, recorded once and filed once. **Only manifests with a write capability enabled** — with writes off the two agree |
| U8-6 | Functional | `bridge/src/api/reads.ts:358`, `:97` | `includeInactive` and `$count` accept only the byte string `true` | confirmed | #1336 | `wrong-data`. Driven against a snapshot with one inactive item: `True`, `TRUE`, `1`, `yes` and a bare flag all silently return the narrow page. **The `requests` serialisation was verified against the real library** — `params={'includeInactive': True}` produces `includeInactive=True`. Merged with U7-5 |
| U8-7 | Functional | `bridge/src/api/v1.ts:1273-1283` | `hasMore: true` on a page the same envelope proves is the last | **rejected** | — | Documented as intended in two places, and the hedge is honest: `openapi.yaml:4986-4987` "True when a further page **may** exist", and `src/db/repositories/types/pagination.ts:23` states the exact rule ("a full page was returned"). `hasMore` is a cheap upper bound costing no extra query; `total` is the exact answer when asked for. No client loops — the follow-up page terminates on the first request. Worst outcome is one wasted request per walk |
| U9-1 | Mechanical | `bridge/src/api/items-csv.ts:114` | Every backpressured chunk leaks two listeners on the response | confirmed (**consequence overturned**) | #1369 | `cosmetic`. **There is no permanent leak** — the finder's harness never emitted `'close'`; a real `ServerResponse` always does, sweeping every pair (`AFTER THE RESPONSE FINISHED: {"close":0,"error":0,"drain":0}`). The real consequence is two spurious `MaxListenersExceededWarning` lines per stalled export, on exactly the spreadsheet refresh the endpoint exists for, sending an operator after a memory leak that is not there |
| U9-2 | Mechanical | `bridge/src/api/head.ts:31-70` | Deferring the head leaves `res.headersSent === false`, disabling the server's committed-response guard | **rejected** (carried) | — | Mechanism reproduced (HEAD answers 500 where GET answers 200, `content-length` 81 = 19 discarded + 62 error). **The live trigger does not exist**: `itemsCsvRow` is total over real rows — nothing threw across 20 adversarial values — and `items-csv.ts` is the only incremental writer HEAD can reach, with later-page failures caught before `server.ts`. A latent trap for the next streaming route. §11 carry. GET/HEAD headers verified across ten endpoints: only `Transfer-Encoding` vs `Content-Length`, which RFC 9110 permits |
| U9-3 | Mechanical | `bridge/src/api/items-csv.ts:88-94` | A snapshot reload during an export closes the driver under the walk | confirmed | #1339 | Merged with U4-3 — same root cause, and this is the user-visible half. `degraded`, not `wrong-data`: the terminating chunk is absent so `curl` exits 18 and `fetch` rejects. **But the partial file on disk is a syntactically perfect CSV ending on a row boundary**, and `README.md:866`'s own one-liner does not check the exit status. Nothing is logged server-side — `items-csv.ts:129-132`'s claim that the failure "surfaces on the response as an `'error'`" is false |
| U9-4 | Functional | `bridge/src/api/dto.ts:169-182` | `LocationDto` drops `archivedAt`, so the API cannot tell a live location from an archived one | confirmed (**wider**) | #1332 | `wrong-data`, strongest of its unit. Archived through the app's own repository, then every read surface driven: byte-identical to a live location, `fields=archivedAt` and `$select=archivedAt` both 400, `$filter` unsupported on the set — **no workaround**. Wider than scoped: the same gap is in the EDM, the OpenAPI schema, and `mqtt/state.ts:92-103`, which leaves a **retained** HA sensor for ever. Framing settled as expose-the-field, not filter-the-rows |
| U9-5 | Functional | `bridge/src/api/dto.ts:143` | `expiryDate` alone contradicts the bridge's own `expiring` count | confirmed | #1367 | `degraded`. Seeded through the app's own receipt path: `/api/v1/status` reports `expiring: 2`, the item reports `expiryDate: null`, `include=all` exposes no lot date among 40 fields, and no route lists the rows. Newly reachable *because* of #684's fix. **Finder citation corrected** — the "pair it through `effectiveExpiryDate`" rule is at `src/db/repositories/types/items.ts:185-196`, the app's types; the bridge states it nowhere |
| U9-6 | Functional | `bridge/src/api/conditional.ts:212-216` | The entity-tag is floored to whole seconds, so two swaps inside one second share a tag | confirmed | #1368 | `degraded`, low. Both halves staged: identical tags 300 ms apart with differing bodies and a `304` on revalidation, and the real watcher publishing twice 318 ms apart without coalescing. Vulnerable band is a 200–999 ms gap; a single write measures 70–86 ms. **The docstring's rationale does not hold** — `If-None-Match` takes strict precedence so the two validators are never both evaluated, and `private, no-cache` forbids a shared cache. The fix requires inverting `conditional.test.ts:36-40` |
| U10-1 | Prompt | `bridge/src/openapi.ts:1447-1448`, `bridge/README.md:842-843` | The CSV export's published column list is four columns short | confirmed | #1350 | `degraded`. Endpoint emits fourteen; both documents name ten. **The parity claim is in the README, not `openapi.ts`** — and it is true as written (`itemsCsvHeader()` is reused verbatim); what drifted is the parenthetical list inside the sentence asserting it cannot. Residue of #250, whose guard covers two prose lists and not this sixth one, and whose closing comment records these same four fields already missing |
| U10-2 | Functional | `bridge/src/openapi.ts:1875`, `:1895` | The two location reads omit the `400` their own parameters promise | confirmed | #1351 | Merged into one issue with U10-3 and U10-4. Checked across all 42 operations: **exactly two** are affected and every other is clean, so #367's fix landed and missed this pattern. #367's own closing comment names the identical shape for `404` |
| U10-3 | Prompt | `bridge/src/openapi.ts:1866-1867`, `:1890-1891` | The location endpoints publish item examples for `$select`/`$expand` that they reject with a 400 | confirmed | #1351 | The sharpest limb. Driving **every** documented parameter example against the server, exactly four are rejected and all four are these. Swagger UI and Redoc prefill from `example`, so the first thing a reader clicks returns a 400. The reverse mistake occurs nowhere |
| U10-4 | Prompt | `bridge/src/openapi.ts:1432`, `:1455`, `:1223` | Three operations serve `location`/`category`/`includeInactive` and declare none of them | confirmed | #1351 | All three parameters confirmed on all three operations. `$format` is a fourth (accepted on every OData resource, declared nowhere). **The "strict gateway" framing was rejected** — no request-validating proxy is documented; the real consequence is that a generated client has no parameter for them. The `Last-Event-ID` limb was **dropped**: the query alias is declared and the description names the header |
| U10-5 | Prompt | `bridge/src/openapi.ts:1459`, `:1509` | `Content-Disposition` is set on both download responses and declared on neither | confirmed | #1352 | `cosmetic` alone; merged with U10-6 |
| U10-6 | Prompt | `bridge/src/openapi.ts` (whole document) | `X-Gubbins-Snapshot-Stale` rides every authenticated response and is documented everywhere except the API contract | confirmed | #1352 | `degraded`, and the load-bearing half. #394's closing comment lists the README and three wiki pages as updated and omits the OpenAPI document, so the signal added to stop consumers presenting stale data as current is invisible to a generated client. The document is otherwise disciplined — nine other response headers are declared |
| U10-7 | Prompt | `bridge/src/openapi.ts:215-224` | The `$count` parameter's example does not validate against its own schema | confirmed | #1351 | `cosmetic`, folded in. Own ajv run: 75 parameter examples, 2 failures, both this shared object; 22 media-type examples clean. **The "CI would start red" argument is unverifiable** — no linter is installed, so nothing is red today. Runtime consequence nil |
| U10-8 | Prompt | `bridge/src/openapi.ts:209-210` | `$top`/`$skip` are published as unconstrained strings while the OData paths hard-400 | **duplicate #362** | comment on #362 | The plain-REST 200-with-clamp **is** what #362 names as its worst failure mode. The typing residue folded into #1351. **#362 is now partly stale and the comment says so**: `@odata.count` and `@odata.nextLink` are emitted today, presumably with #361 |
| U10-9 | Prompt | `bridge/src/openapi.test.ts:200` | The guard's comment claims the location field list is hand-written; it is interpolated | **rejected** | — | Deliberate, and #250's closing comment states it verbatim: the assertions restate their source by construction and fail the moment someone replaces an interpolation with a hand-typed copy and the registry later moves. **Both mutations run**, and both reproduce that claim exactly. Residue is one stale sentence, below the bar |
| U10-10 | Prompt | `bridge/src/openapi.ts:2965` | `BridgeEvent` is fully specified and unreachable from any operation | **rejected** | — | No defect. Four orphaned schemas, not one. OpenAPI 3.0.3 cannot type an SSE frame stream (3.1 can); the operation description names the schema and its 200 example is a real serialised frame; Redoc and Swagger UI render orphaned schemas anyway. At most a feature request |
| U11-1 | Mechanical | `bridge/smoke.mjs:32-33` | The strip-only guard misses `cli.mjs` and `emit-openapi-yaml.mjs` | confirmed | #1341 | `degraded`. Both mutations run through all four commands: `type-check:bridge`, `test:bridge` (79 files / 1191 tests) and `smoke:bridge` all exit 0, and only the real entry point fails with `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`. The uncovered surface is exactly two files. The openapi case is sharper — the drift guard keeps passing while the only way to regenerate the spec is broken |
| U11-2 | Mechanical | `bridge/serve.mjs:13` and three siblings | The Node-version guard cannot fire on the oldest Nodes it exists to catch | confirmed | #1342 | `degraded`. Four Docker runs: `node:16-slim`, `18.17`, `20.5` all die at link time with `does not provide an export named 'register'`; `18.19` reaches the guard and prints the intended sentence. **Reachability established properly**: `ubuntu:22.04` `apt install nodejs` gives **v12.22.9**, squarely inside the window, on the Pi and NAS hosts the README targets. Residue of #256, in a band it never discussed |
| U11-3 | Mechanical | `bridge/smoke.mjs:86`, `:180-186` | The boot smoke inherits the developer's `bridge/.env` and is not hermetic | confirmed | #1343 | `degraded`, entirely local. Precedence proved (set wins, unset adopted) and staged: the smoke reported `Writes ENABLED` and 11 tools. **A real outbound MQTT CONNECT was captured** on a listener — client-id `gubbins-bridge`, last will on `gubbins/status` — while the smoke still passed. Latent until someone follows the quick start and creates the file. Opposite direction to #738 |
| U11-4 | Functional | `bridge/README.md:2192-2194` | The matrix claims every capability is startup-logged; mDNS never is | **rejected** | — | **The headline claim is false.** The finder's `grep console.` over `mdns/` is correct but the inference is not: the module logs through an injected seam defaulting to `console` (`advertise.ts:30-34`, `:99`), on the success path, unconditionally. A real LAN boot prints `mDNS advertising "Gubbins Bridge" on 224.0.0.251:5353.` The finder's boot was on the loopback default, where the skip *also* logs. The two smaller limbs are real but only omit *disabled*-state lines |
| U11-5 | Prompt | `bridge/README.md:2578-2668` | The Layout tree omits 41 of 83 production files | confirmed | #1344 | `cosmetic`, merged with U11-6. Re-run path-aware (basename matching masks `mdns/discovery.ts` behind `mqtt/discovery.ts`). No phantoms. Missing includes `src/identity.ts` and the complete `events/`, `feeds/`, `ical/` and `homeassistant/` directories, all of which have their own README sections |
| U11-6 | Prompt | `bridge/README.md:338`, `:340` | Two dead internal anchors | confirmed | #1344 | Merged with U11-5. Own slugging over 90 headings and **159** anchor links (the finder undercounted at 52) finds exactly these two; all 18 relative file links resolve. `npm run wiki:check` does not cover this file |
| U11-7 | Functional | `bridge/mcp.mjs:27` | The MCP server loads `.env` from whatever directory its client launched it in | confirmed | **held** | **SECURITY, privilege escalation.** Staged: a scratch directory whose only file is `.env` containing `GUBBINS_BRIDGE_ALLOW_WRITES=on` turns 6 tools into 11 and `read-only` into `reads + limited writes`; the write was driven and landed on disk (42 → 39). Both halves wrong in the same direction — `bridge/.env` is never read, a foreign one is. `README.md:1253-1266` says this transport has "no credential at all". Tied to #738: anchoring the path closes both; documenting a `cd` does not |
| U11-8 | Mechanical | `bridge/loader.mjs:27` | `SUFFIXES` offers `.tsx`, which Node's strip-only loader cannot execute | **rejected** | — | `npm run type-check:bridge` catches it, which the finder missed: `bridge/tsconfig.json` sets no `jsx`, so any `.tsx` reached from the bridge's graph is `error TS6142` at resolution time, and that is a CI gate. Two further finder errors: the runtime symptom is `ERR_UNKNOWN_FILE_EXTENSION` naming the file, not `Missing initializer`; and the `.tsx`-before-`.js` ordering is inert with zero `.js` files under `src/`. The `?inline`/`#frag` limb has zero matches repository-wide |
| LEAD-1 | Mechanical | `bridge/src/api/limits.ts:17-21` | `MAX_PAGE_LIMIT` claims to mirror `MAX_PAGE_SIZE` with no drift test | confirmed | #1340 | `wrong-data`, latent. Raised by the lead; the verifier corrected the mechanism — `sendList` takes `limit` from the **API's** clamp and `count`/`hasMore` from the **repository's**, so one envelope has two ceilings. At `MAX_PAGE_LIMIT=200` the envelope asserts `"limit": 200` while serving 100, and a client paging by it reads 200 of 300 rows. **Nothing catches it**: two tests fail, both literal restatements of the constant, and updating them — what a deliberate cap change entails — leaves 79 files / 1191 tests green. OData and the CSV walk are unaffected. Rule 1 is trivially available: two sibling bridge modules already import the real constant |

### Phase 13 — Bridge integrations

Pinned SHA: `ad513f524edf1f516c0a8485338cb83f5b68babe`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| U1-1 | Functional | `bridge/src/identity.ts:130` | `GET /api/v1/events` demands `bridge:read` only while streaming the item DTO and the ledger | **duplicate** | — | Exactly Phase 12's U3-1, confirmed there and **held** unfiled at the maintainer's direction (§11). Not re-verified |
| U1-2 | Performance | `bridge/src/events/sse.ts:171-177` | `safeWrite` discards `res.write`'s return, so a stalled subscriber buffers without bound | confirmed | #1399 | `degraded`. Merged with U8-9 — one root cause, two surfaces. 60 s soak: linear to 12.25 MB, **no plateau**, `clientCount()` still 1, `destroyed=false`. Verifier added `sse.ts:118`'s `setTimeout(0)`, which removes the idle reaper. Real ceiling 28.5 KB/generation/client (≈142 KB/s), not the finder's figure |
| U1-3 | Mechanical | `bridge/src/events/server-lookup.test.ts:62,69` | The flag-off privacy test asserts against an array nothing writes to | confirmed (**wider**) | #1380 | `degraded`. **Two** such tests, not one — `query.test.ts:117-120` is the same shape. Both proved unfalsifiable by mutation: the server published `lookup.resolved` unconditionally and both stayed green. Config layer *is* covered (`config.test.ts:344-366`); the wiring and emission layers are not |
| U1-4 | Functional | `bridge/src/events/sse.ts:159-168` | An unknown or pre-restart `Last-Event-ID` is answered identically to "nothing happened" | confirmed (**reshaped**) | #1381 | `degraded`. Replaying nothing on an unknown id is **deliberate and tested** (`sse.test.ts:115-124`) — the defect is the silence about it. Four byte streams captured; A and B byte-identical. Parity is sharp: `deliveries` ships a `logId` and spells out the recovery; the scale stream says outright it has no replay |
| U1-5 | Functional | `bridge/src/events/model.ts:436-440`, `:385-403` | Both builders `slice(0, cap)` an oldest-first list, so the newest events are dropped and the cursor advances over them | confirmed | #1378 | `degraded` / wrong-data for a stateful consumer. Scratch test: `item.out_of_stock` absent from the delivered 50; `seenIds` contains the dropped id; next generation returns `[]`. `DEFAULT_FAN_OUT_CAP = 50` is **not operator-tunable**. `model.test.ts:265-278` asserts the count only. Location events are unrecoverable — no history endpoint |
| U1-6 | Mechanical | `bridge/src/events/sse.ts:133-143` | `close()` sets no shutdown latch, so a late connection re-registers and restarts the heartbeat | **rejected** | — | Divergence from `scale-stream.ts:139-140` is real, but unreachable. `watcher.stop()` closes the driver synchronously in the same shutdown tick, and `server.ts:487-490` gates every request on `getState()` 113 lines before the SSE route. Driven twice, including a handler parked across the whole of shutdown with `UV_THREADPOOL_SIZE=1`: it resumes into a closed driver and answers 500. The two hubs legitimately differ — `SseHub.handleConnection` is synchronous |
| U2-1 | Mechanical | `bridge/src/events/webhook-ssrf.ts:95`, `:178` | An IPv4-mapped IPv6 literal is classified publicly routable, so the private-destination guard is bypassed | confirmed | **held** | **SECURITY, EXPLOITABLE, no bridge credential.** `new URL()` always serialises the hex form `[::ffff:7f00:1]`, which the dotted-only fold at `:95` never matches. Delivery to a loopback listener proved end to end with the real deliverer and the shipped default policy (`outcome: delivered`, 204). `169.254.169.254` equally allowed; `GET` is a supported method. URL arrives via the synced `webhooks` table. Non-2xx receiver bodies return through `/api/v1/webhooks/deliveries` (200 chars). `webhook-ssrf.test.ts:45-51` is green because it feeds a string no caller can produce. Every other evasion (octal, integer, `127.1`, `[::]`, `fe80::`, redirects, DNS rebinding) is handled |
| U2-2 | Mechanical | `bridge/src/events/webhook.ts:416-422` | A credential in a webhook URL is written into the delivery log | confirmed | **duplicate #1315** | Reproduced in full (`TypeError` carries the password, 127 chars, under the 200-char cap; reaches `GET /api/v1/webhooks/deliveries`, gated `bridge:read` + `settings:read`). #1315 (opened 2026-09-02) names the same lines and already carries both limbs including "can never deliver". Not re-filed. Strongest invariant is `api/v1.ts:446` ("by construction"), which the finder missed |
| U2-3 | Performance | `bridge/src/events/webhook.ts:534-545` | The delivery `fetch` carries no `AbortSignal` | confirmed | #1384 | `degraded`. Measured to settlement: **306,236 ms** per attempt (undici headers timeout), ≈25.6 min per event over five attempts. `server.requestTimeout` does not apply — it bounds *receiving*. Test endpoint makes **one** attempt, not five, so 305 s — and the app's own 10 s budget fires first and reports `bridge-unreachable`, blaming the bridge for the receiver. `src/lib/fetch-timeout.test.ts:199` sweeps `SRC_DIR` only, so #632's guard never covered the bridge |
| U2-4 | Mechanical | `bridge/src/events/webhook-targets.ts:181`, `:300` | A `secret_ref` naming an `Object.prototype` member resolves to a function and throws out of the worker | confirmed (**wider**) | #1382 | `unusable`. **Six** names get through, not three (`propertyIsEnumerable`, `__defineGetter__` too) — the miss test uses `.length`, which is a function's arity. Rejection escapes the floating `void drain()`; ten in a minute drives the real `installProcessResilience` to `onExit`. **Worse half the finder missed:** over HTTP the Test button answers `outcome: "unmatched"`, blaming the user's filter, at N=1. Arrives over sync, so cross-user in a shared vault. Not #1354 — that has a 400 containment argument, this has none |
| U2-5 | Functional | `bridge/src/events/webhook.ts:427-433` | A queue-full drop records only a console line | confirmed | #1385 | `degraded`. Scratch test: 5 matched events, 3 log rows, 2 vanished. `skipped` does not cover it (circuit-open only, and the wiki defines it narrowly). No `maxQueue` test exists. #643 exists because this exact silence was judged a defect elsewhere. Reachable mainly *via* U2-3 — a stalled receiver gives a 25-minute window per event |
| U2-6 | Prompt | `bridge/webhooks.example.json:4` | The `"//types"` catalogue lists 17 of 26 event types | confirmed | #1386 | `degraded`, merged with U10-5. Diff computed from `KNOWN_EVENT_TYPES`; driven through the **real matcher**, all 9 missing types DROPPED. File/env target path is documented as "stays supported". No hedge in the example — the neighbouring `"//events"` frames it as *the* list. Deliverer never logs a non-match, so the drop is silent |
| U2-7 | Prompt | `bridge/webhooks.example.json:12`, `bridge/.env.example:121` | The shipped examples name a loopback target the default posture refuses, without saying so | **rejected** | — | Every load-bearing claim false. `.env.example:128-132` explains it eleven lines below, in the same block; `README.md:1631-1657` is one subsection below the example reference; the startup console prints it both ways; `docs/wiki/Webhooks.md:159-166` has a section; and the refusal **names the flag to set** on the first delivery (`webhook.ts:378-381`), logged and recorded as a `blocked` row. Not silence |
| U2-8 | Mechanical | `bridge/src/serve.ts:739-744` | A malformed `webhooks.json` is discarded exactly like an absent one | confirmed | #1383 | `degraded`. Staged with a real server: startup output **byte-identical** between a broken file and an absent one. Loud about *shape* (two `console.error` paths), silent about *syntax*. Takes the named secrets with it, so subscriptions are refused with a message pointing at the wrong place. Separate from #1361 (which file is looked for) |
| U2-9 | Functional | `bridge/src/events/webhook-targets.ts:302` vs `:181` | A `secret_ref` is trimmed on definition but not on lookup | **rejected** | — | Mechanism real, unreachable. Every app write path trims: `WebhookFormDialog.tsx:152-183` → `subscription.ts:193` → `normaliseWebhookText`, and `WebhookRepository.create`/`update` both re-run it. `git log -S` shows the trim has been there since #87. The bridge has no webhook write path |
| U2-10 | Performance | `bridge/src/events/webhook.ts:311`, `bridge/src/serve.ts:117` | Two per-process maps are never pruned | **rejected** | — | Measured: **501 bytes** per never-reused target id, **178 bytes** per retained warning sentence. Ten create-and-delete cycles a year for a decade costs ~49 KiB. A correctly-configured bridge adds **zero** warning entries; a persistently broken subscription adds exactly one. Below the bar |
| U2-11 | Prompt | `bridge/README.md:1580` | Calling the signature "the GitHub/Stripe pattern" implies replay protection it does not have | **rejected** | — | GitHub's `X-Hub-Signature-256` signs the body alone, in the same `sha256=` format — so the sentence is accurate for one of the two names it gives. The algorithm is stated in the same cell with working verification code eight lines below. The signed envelope carries a deterministic `id` the README already tells consumers to dedupe on, which *is* replay-proof. Recipe re-verified byte-exact incl. U+2212 and both `rawBody` forms |
| U3-1 | Functional | `bridge/src/feeds/emitters.ts:173-183` | Atom entries carry neither `<content>` nor `<link rel="alternate">` | confirmed | #1370 | `degraded`. Settled with the **W3C Feed Validation Service** (the validator #364's closing comment named): `validity: false`, `MissingContentOrAlternate`. RSS validates clean. #364's public claim that "the Atom feed now validates" is disproved against current `main`. "No item URL to link to" is not a defence — `atom:content` needs none |
| U3-2 | Mechanical | `bridge/src/feeds/emitters.ts:99-112` | An out-of-range `created_at` 500s Atom and JSON and writes NaN into RSS | confirmed (**wider**) | #1371 | `unusable`. Smallest breaking input is `Number.MAX_SAFE_INTEGER`, not `1e18`; epoch-µs renders year 55840 with **no error at all**. Also breaks `events/model.ts:375,449`, a fourth surface. **The guard already exists** — `isTimestamp` (`backup.ts:76-89`), whose docstring names this exact `RangeError`, is applied to `generatedAt` and `gaugeHistory` and not to `itemHistory`. All four ingress routes accept it |
| U3-3 | Functional | `bridge/src/feeds/metrics-format.ts:63`, `:70-74` | Two gauges carry the counter-only `_total` suffix | confirmed (**reshaped**) | #1374 | `cosmetic`. `promtool` unavailable, so its `lintCounter` rule was ported and run: 2 problems, exit 3. **Two finder claims disproved:** the OpenMetrics reference parser accepts both names, and the endpoint is never OpenMetrics anyway (`respond.ts:219` hard-codes 0.0.4, no `# EOF`). Rename is breaking for existing dashboards |
| U3-4 | Prompt | `docs/wiki/Bridge-Overview.md:15-28` | The wiki never introduces the feeds or `/metrics` | confirmed (**reshaped**) | #1373 | `degraded`. Finder said "no page mentions"; truth is sharper — `Webhooks-MQTT-and-iCal.md:106-108` and `:113-114` refer to both with the definite article inside a caching aside, and nothing ever introduces them. One grep hit across 93 pages. Both answer on a bridge with everything disabled. All 15 programme issues read; none names either. Page map allocates no page (#271's mechanism) |
| U3-5 | Functional | `bridge/src/api/v1.ts:1196-1202`, `bridge/src/feeds/feed.ts:44-47` | An empty `?limit=` coerces to 0 and clamps to 1 | confirmed (**wider**) | #1372 | `degraded`. Real server, all four claimed inputs plus the absent case, on all three feed formats. **Wider:** `api/params.ts:71-76` has the same shape, so every REST list endpoint serves a one-row page. Decisive contrast: OData already answers **400** for an empty `$top` on the same server. Not #1336 — different function, different type, different failure mode |
| U3-6 | Prompt | `bridge/src/feeds/feed.ts:16-21` | `DEFAULT_FEED_LIMIT` claims to be the repository's page ceiling; the ceiling is 100 | confirmed | #1375 | `degraded`, latent. Both constants printed. Dropping `MAX_PAGE_SIZE` to 25 makes `buildActivityFeed` serve 25 while the README and OpenAPI still promise 50 — it **truncates rather than pages**, because `feed.ts:36-39` discards `page.hasMore` and `page.limit`, both of which report the clamp correctly. Distinct from #1340 (different constant, different blast radius) |
| U3-7 | Prompt | `bridge/src/server.ts:284-287`, `:425` | The Host-reflection safety argument rests on a `no-store` the feed responses no longer send | confirmed | #1376 | `cosmetic`. Six paths captured, all `private, no-cache`. Clean provenance: the sentence landed 18 July (`573cc332`, #364); #363 changed the header 25 July (`e66e550d`). Separable from Phase 12's held `requestBase` finding in substance (it would stay false if the Host validation were tightened) though **not in location** |
| U3-8 | Prompt | `bridge/src/feeds/emitters.ts:61-72` | The ampersand-first rationale is the inverse of what that ordering does | confirmed (narrowly) | #1377 | `cosmetic`. `AT&amp;T` → `AT&amp;amp;T`, always. No test pins pre-escaped input. Clears the bar on route 2: a maintainer "fixing" it with an entity-preserving lookahead emits an **undefined entity reference**, which makes the whole document unparseable |
| U4-1 | Mechanical | `bridge/src/ical/emitter.ts:76,92,100`, `:124-128` | The year is not zero-padded, and `addDays` re-slices its own short output | confirmed (**reshaped**) | #1387 | `unusable`. **The finder's warranty route is wrong** — the 10-year horizon excludes 9999. Loans are the real route (`listOpen` has no date bound, no `CHECK`, no validation); bookings and maintenance are two more; warranty reaches it for years 0100–0999. `0800` is worse than reported: `addDays` mis-slices into a *well-formed but wrong* DTEND five years out. Python `ical` (Home Assistant's) fails the **whole VCALENDAR**. Both date inputs carry no `min`/`max`, driven in real Chromium |
| U4-2 | Functional | `bridge/src/identity.ts:152-154` | The calendar is gated on `bookings:read` alone but publishes four sources | confirmed (**reshaped**) | **held** | **SECURITY, credential-gated.** Driven: narrow-role token gets 403 on `/items` and returns borrower names, loan notes and quantities, maintenance names and notes, item names and manufacturers. **No built-in role reaches it** — the exposure needs the custom role `README.md:904-907` and the wiki explicitly prescribe. Finder wrong that `feed.ts` ignores `?type=` (it honours it); the **gate** never sees the query string (`server.ts:502`). Fails in **both** directions — a role with `items:read` but not `bookings:read` is denied warranty and maintenance. The app already applies the per-lane rule for the same four lanes (`useAgenda.ts:106-111`, #522) |
| U4-3 | Mechanical | `bridge/src/ical/feed.ts:161-169` | An offset walk over untiebroken orders can duplicate and drop events | **rejected** | — | Tested hard rather than assumed: 250 bookings on one `start_date`, 250 `TIME` + 250 `USAGE` schedules, three insertion orders — all 250/250 and 500/500 distinct. Raw `node:sqlite` probe to 20,000 rows with padded rows and a 32 KiB cache to force a sorter merge: zero duplicates. Holds because the SQL text is identical across pages, the statement is cached, and the bridge has no `sqlite_stat1`. Unguaranteed but not broken; two of four sources are exposed (loans and warranty are safe). Any tiebreak belongs in the repositories — the app's Upcoming agenda offset-walks the same two reads |
| U4-3b | Prompt | `bridge/src/ical/feed.ts:159` | `collect` claims to mirror the CSV export's collector | confirmed | #1388 | `cosmetic`. The claim was **true when written** (`e1032123`); `9b0a93fe` (#533) replaced the CSV collector with a keyset walk whose comment says the opposite, and did not touch `feed.ts`. A silent parity drift of exactly the shape `CLAUDE.md` names |
| U4-4 | Prompt | `bridge/src/openapi.ts:1521` | The OpenAPI example shows `METHOD:PUBLISH`, which the emitter never writes | confirmed | #1419 | `cosmetic`, merged with U4-5 and U4-10. Decision documented **twice** (`emitter.ts:10`, `:211-213`) and tested. Schema is `{type:'string'}` so nothing is validated against the example — the path with teeth is a **mock** (Prism serves `example` verbatim), which reproduces the Outlook-invitation behaviour the emitter exists to avoid |
| U4-5 | Prompt | `bridge/README.md:922-924` | The README says due-backs use UTC components; #321 moved them to local | confirmed | #1419 | Merged. Driven under `America/New_York`: loan emits local, booking emits UTC. Dated drift — sentence in `e1032123` (3 July), #321 landed 24 July touching no README line. **Finder's consequence overturned**: the sharper harm is that the README frames the *correct* booking behaviour as a defect, so an integrator applying a compensating ±1-day offset corrupts every booking date |
| U4-6 | Functional | `bridge/src/ical/feed.ts:72`, `:136` | Every `?type=` calendar is named `Gubbins` | **rejected** | — | Mechanism confirmed; consequence disproved against the clients. Thunderbird does not read `X-WR-CALNAME` at all (`CalICSProvider` names from WebDAV/URL; bug 168176 open 21 years) and HA `remote_calendar` names from the config flow — so in two of four the user names it at subscribe time. Google seeds from it and renaming is one settings field. `README:910-911` promises the *sources* narrow, which is kept. The "Google overrides a rename on refresh" reputation could not be substantiated |
| U4-7 | Mechanical | `bridge/src/ical/emitter.ts:38-39`, `:184` | `VEvent.dtstamp` is typed loosely enough to emit `DTSTAMP;VALUE=DATE` | **rejected** | — | Reproduced, but no reachable consequence. `feed.ts:148` is the single construction site. **Three** assertions pin the shape (`emitter.test.ts:181`, `feed.test.ts:137`, `server-calendar.test.ts:59`), contrary to the finder's implication that nothing does |
| U4-8 | Performance | `bridge/src/ical/feed.ts:134-138` | The whole calendar is materialised before the first byte | **rejected** | — | Measured at the 20,000-event cap: walk **753 ms**, the entire string build **49 ms**, retained +21.5 MB. The cost is the offset walk, not the buffering. A realistic big vault is ~5,000 events (only warranty scales) → 285 ms, ~16 MB transient. `304` costs 4 ms, so a subscriber pays a full build a couple of times a day. The HEAD limb is **documented as intended** (`server.ts:392-398`, #360) — `Content-Length` matched the GET body to the byte |
| U4-9 | Mechanical | `bridge/src/ical/feed.ts:217` | `checkout.dueDate as number` re-asserts a nullability the compiler could keep | **rejected** | — | `null` unreachable — the filter and map are one expression and `loanEvent` has no other caller. Consequence demonstrated anyway (`icalLocalDate(null)` → `19700101`), but it is a trap for a future edit, not a defect. A checklist `as` hit is a candidate, never a finding |
| U4-10 | Prompt | `bridge/src/openapi.ts:278`, `:1496` | The `token` parameter still calls it "the shared bearer token" | confirmed | #1419 | Merged. **Four** rendered occurrences, not two (`feedTokenParam` is reused across the three activity feeds). The same document's `securitySchemes` correctly describes a per-user token, so it contradicts itself. **Finder's consequence overstated** — the security scheme is authoritative and no shared token can be obtained, so the harm is self-contradiction rather than a wrong integrator decision |
| U5-1 | Performance | `bridge/src/mqtt/client.ts:219-237` | The inbound framing buffer is unbounded and re-concatenated per segment | confirmed | #1389 | `unusable`. O(n²) measured: 8/16/32/64 MiB → 361/1337/5987/31416 ms synchronous. Real server: `/health` median 2.3 ms → **12,960 ms**, 6 requests served in 90 s instead of 89, two failures, RSS 118 → 696 MiB. Stopping just short of the claimed length holds it; backoff caps at 30 s so it re-floods. Bridge is publish-only — no legitimate large inbound frame exists. README's "the HTTP API is unaffected" is false for a broker that talks |
| U5-2 | Performance | `bridge/src/mqtt/client.ts:339-343`, `:351-366` | The offline buffer replays superseded retained publishes | confirmed (**reshaped**) | #1394 | `degraded`. Measured: 728 packets / 89,079 bytes over **11 topics**, one topic 240 times, **100% superseded**. **Finder's headline consequence disproved:** no discovery config sets `force_update` (HA default `false`) and the sensors read no field that moves on an idle reload, so 240 reloads present a byte-identical state 240 times and produce no update events. Residual harm is real values at wrong *timestamps* when data moved during the outage. The HA recorder half is reasoned from documented semantics, not read from a table |
| U5-3 | Functional | `bridge/src/mqtt/discovery.ts:22`, `:52-53`, `:120-121`, `:199-200` | Every discovery identity is a constant, so two bridges overwrite each other | confirmed (**wider**) | #1390 | `wrong-data`. Two bridges is documented as supported (`homeassistant/README.md:483`, wiki, #564). Escape hatch does **not** work — HA's MQTT manifest carries `single_config_entry: true`, so a second discovery prefix means HA never discovers bridge B. Three further limbs the finder missed: `availability_topic` flips too; both vaults attach to one HA device; discovery re-emits on **every reconnect**, and both bridges default to the same client id, so a broker evicts and they re-flip continuously. Not #1362 — the identity never reaches MQTT in *any* configuration |
| U5-4 | Functional | `bridge/src/mqtt/client.ts:301-308`, `bridge/src/config.ts:392` | An unvalidated topic prefix produces packets a broker must reject, with no diagnostic | confirmed (**reshaped**) | #1392 | `unusable`. Driven against a **real Mosquitto 2.1.2**. **The finder had the wrong half:** with the will the publisher always sets, the CONNECT itself is malformed and the bridge logs **nothing at all** (`handleDrop` warns only `if (wasConnected)`). Also `reconnectAttempts` resets on CONNACK, so the post-CONNACK variant never backs off — one connection per second, for ever. Silence is not gated on a wildcard: a plaintext URL against a TLS listener does the same. A `/` in the prefix is deliberately supported (`config.test.ts:132-139`) |
| U5-5 | Functional | `bridge/src/mqtt/client.ts:148-161` | `mqtts://` cannot reach a broker with a self-signed certificate | **rejected** | — | Load-bearing claim false. Driven against a real self-signed TLS listener: fails as predicted, then **connects with `NODE_EXTRA_CA_CERTS` set**, same code and config. The error names the remedy on every attempt (`…self-signed certificate; if the root CA is installed locally, try running Node.js with --use-system-ca`). All three doc sources quoted in full: each says what the scheme is, none claims which certificates are accepted — an omission, not a contradicted claim. A doc line is warranted; an audit finding is not |
| U5-6 | Mechanical | `bridge/src/mqtt/discovery.ts:231-237` | The device-level discovery set has no persisted record, so a dropped entity is never retracted | confirmed (latent) | #1414 | `cosmetic`. #565 read in full — its scope was **locations only**, so this is a sibling, not residue. Wider than the finder's prefix framing: `clearRemovedLocations` diffs location ids only, so a dropped device entity survives with prefixes unchanged. The drift test **can** fail on a one-sided change but is blind by construction to a cross-version removal. `git log` shows the set has only ever grown |
| U5-7 | Functional | `bridge/src/serve.ts:208-218` | A failed state projection swallows the fresh staleness verdict | confirmed (**worse**) | #1393 | `wrong-data`. The reachable input was found: **one damaged page in the `items` btree** passes `getUserVersion`, `runMigrations` and `assertExportBaselineCurrent`, so the reload is a success and every `items` read then throws. Two other corruption shapes correctly failed at hydrate. End to end, only `gubbins/status` reached the broker; `snapshot/state` was never published. Worse than claimed — **nothing** is published, the watcher's `catch` is guarded by `if (!swapped)` so the fault is uncounted, and with a persistent cause it never self-heals |
| U5-8 | Functional | `bridge/src/serve.ts:580`, `bridge/src/mqtt/retained-locations.ts:34` | The retained-topic record's path carries no bridge identity | confirmed | #1391 | `wrong-data`. `staleTopics` captured for a `houseA` record under a `houseB` scope; symmetric, so it ping-pongs. Every shipped recipe pins one path (unit `Environment=`, Dockerfile `ENV`, the README's `/state` volume), so same-directory is the **default** for anyone running two. Recovery is narrow: retracted per-location discovery configs are not re-emitted until a reconnect or a location-set change. Sibling of #1362 |
| U5-9 | Functional | `bridge/src/mqtt/client.ts:248-252`, `:320-330` | A permanently-refused connection retries for ever, logs every attempt, and has no jitter | **split** | #1395 | Retry limb **rejected** — documented twice (`README.md:1841-1842`, `client.ts:12-14`) and defensible. Jitter limb **rejected** — one client dialling one broker has no herd; measured a fixed 30 s cap with no drift. Log limb **confirmed**, `degraded`: 2,885 attempts and **2,885 byte-identical warnings** in 24 h, `distinct warnings: 1`, where the sibling at `serve.ts:604-607` uses `if (warned) return;` |
| U6-1 | Functional | `bridge/src/mdns/discovery.ts:166-174` | Discovery selects the destination the Home Assistant token is sent to | **duplicate** | — | Phase 12's held security finding, verbatim (§11). Not re-verified or re-filed |
| U6-2 | Performance | `bridge/src/mdns/records.ts:143-155`, `:299-325` | One 64 KB datagram costs ~56 ms of blocked event loop and tens of MB | confirmed | **held** | **SECURITY, unauthenticated LAN, no credential.** Re-measured: 10,905 questions, 44.6M name chars, 56.1 ms decode + 7.9 ms match per packet. HTTP consequence settled on loopback: `/health` max 8 ms → **131.7 ms** under flood. ~18 packets/s saturates the loop. Gate is `GUBBINS_BRIDGE_MDNS=on` **and** a LAN bind — the documented Home Assistant setup. Distinct code path from Phase 12's held P12-DOS |
| U6-3 | Functional | `bridge/src/mdns/advertise.ts:62-66`, `:80-86` | The responder answers every query immediately with 9× amplification and no rate limit | **rejected as framed** | — | Missing rate limit reproduced (50 queries → 50 × 331 B in 1 ms) and three violations identified, incl. §7.1 Known-Answer Suppression which the finder missed. But the amplification framing collapses: the reply always goes to the **group**, so on-wire fan-out is 1:1 — an attacker who can send 37 B there can send 331 B there. Dedicated service type, so non-malicious cost is near zero. RFC citation corrected: no subsection titled "Multicast Rate Limiting"; the 20–120 ms delay is a SHOULD |
| U6-3b | Functional | `bridge/src/mdns/advertise.ts:62-66` | The responder ignores the query's source and the unicast-response bit | confirmed | #1397 | `degraded`. RFC 6762 §6.7 is a MUST and **four** requirements are violated (multicast reply, ID `0` not echoed, QDCOUNT `0`, cache-flush bit set) — captured on real sockets, the one-shot querier received **zero** unicast replies. The repository documents this failure against itself: `discover.ts:15-18` says a responder that ignores QU "is not heard". Does not affect Home Assistant (binds 5353, joins the group) |
| U6-4 | Functional | `bridge/src/mdns/records.ts:203-214`, `bridge/src/mdns/advertise.ts:56-57` | The advertised address is the first enumerated IPv4, encoded once and never refreshed | confirmed (**wider**) | #1396 | `degraded` / `unusable` for the staleness limb. Real Docker, both shipped shapes: default bridge networking advertises `172.17.0.2`; `--network host` picks `services1` over `eth0`. Order is arbitrary-but-deterministic, not random, and **not** docker0-specific as the finder said. Sniffed: nothing unsolicited after the 1 s burst; every query answered from the frozen buffer. Worse than claimed — a wrong advertisement drives `_async_abort_known` to **overwrite a working entry's host** with no reachability probe, and the staleness silently disables #672's recovery. `addMembership` has no interface argument and `setMulticastInterface` is never called |
| U6-5 | Functional | `bridge/src/mdns/records.ts:103-106`, `:229-244` | The instance name is not validated as a single DNS-SD label | **duplicate #1360** | — | Both limbs reproduced (a dotted name splits; `.` collapses onto the service type). #1360's body **already documents the dot-splitting escape** and asks for validation at the same line (`config.ts:346`). Cosmetic even standalone: the advertisement stays internally consistent and HA never reads the instance name (`config_flow.py` uses `entry_title(host, port)`) |
| U6-6 | Functional | `bridge/src/mdns/records.ts:30`, `:123-130` | PTR and TXT carry the 120 s TTL RFC 6762 reserves for host names | **rejected** | — | The 75-minute figure carries **no RFC 2119 keyword**; only the 120 s figure is even a SHOULD, and §10 prices the trade in bandwidth. Compounding limb fails twice: §5.2 gives a querier four retries at 80/85/90/95%, needing a ~24 s continuous stall; and HA uses zeroconf **only** in the config flow (`iot_class: local_polling`), so a configured entry never consults the cache. With no periodic re-announcement the short TTL is what clears a bridge that died without a goodbye |
| U6-7 | Functional | `bridge/src/config.ts:534-536` | The loopback auto-skip is decided from the config string | **rejected** | — | Reproduced and **wider** than claimed — a capitalisation typo (`Localhost`) trips it too, and the startup line then wrongly says "reachable from the LAN". Rejected on reachability: binding a secondary loopback alias is a deliberate, unusual act nothing documents, and the consequence is identical to U6-4's, which is confirmed. The wildcard 5353 bind is what RFC 6762 requires and exposes only non-secret records by a recorded decision |
| U6-8 | Performance | `bridge/src/mdns/discovery.ts:113-126` | One hostile datagram adds tens of thousands of map entries, and each datagram copies the map | confirmed | **held** | **SECURITY-shaped, heavily gated.** Worse than measured by the finder: 3,445 answers per 64 KB datagram, **272,155 instances and 305 MB peak from 5.2 MB** of traffic; per-datagram ingest 15 ms → 201 ms (quadratic). But it frees completely on GC, the window is a fixed 2.5 s, it needs `HA_DISCOVERY=on` with `HA_URL` unset, it is once per restart which the attacker cannot time, and `discover.ts` never joins the multicast group so it needs unicast to an observed ephemeral port. Genuinely borderline; recorded rather than filed |
| U6-9 | Mechanical | `bridge/src/mdns/discover.ts:96-101` | The discovery socket's `message` handler is not wrapped | **rejected** | — | Two routes tried and reported. **500,000 structured hostile packets** (forward/backward/self pointers, truncated SRV, over-claiming TXT, 274-deep chains) → 0 throws. The mutation demo confirms a throw *would* escape, but `installProcessResilience` is installed at `serve.ts:92` **before** discovery runs (#305), so a single throw is survived. No consequence anyone suffers |
| U6-10 | Mechanical | `bridge/src/mdns/records.ts:143-144` | `decodeQuestions` throws on any buffer shorter than 6 bytes | **rejected** | — | Reproduced for lengths 0/4/5. `isQuery` is `false` for every `n < 12` and is the `&&` guard at the sole call site, which is itself inside a `try`. The discovery sibling has its own length check. Latent maintainer-facing footgun with no reachable consequence |
| U6-11 | Mechanical | `bridge/src/mdns/records.ts:305-323` | The decoder accepts the reserved `01`/`10` length prefixes | **rejected** (folded) | — | Mechanism real (a `0xbf` octet yields a 191-char label). Swept every reserved prefix `0x40`–`0xbf`: **no forged match is possible** — a reserved prefix always consumes 64–191 literal bytes, so the label can never equal a real service label. Outcome is wasted decode work, i.e. a multiplier on U6-2, not a defect of its own |
| U6-12 | Mechanical | `bridge/src/mdns/records.test.ts:69-74` | The goodbye test never reads a TTL | confirmed | #1398 | `cosmetic`. Mutation `goodbye ? 0` → `goodbye ? 1`: `records.test.ts` 21 passed, **whole bridge suite 81 files / 1199 tests passed**. Both assertions hold for any TTL by construction. Lead re-ran this mutation independently and confirmed |
| U6-13 | Mechanical | `bridge/src/mdns/records.ts:21-22`, `custom_components/gubbins/manifest.json` | The service type is hand-copied with no drift test | **rejected** | — | The comment makes **no parity claim** — it describes what the value is for. Same ground as Phase 0's `Run.ps1` rejection. Only **two** code copies, not four (the two READMEs are user docs), and they are **not byte-identical** (the manifest carries the DNS-SD root dot), so a naive text comparison would fail on correct code. A DNS-SD type is a wire contract; renaming it breaks every install regardless |
| U7-1 | Mechanical | `bridge/mcp.mjs:26-30` | `mcp.mjs` loads whatever `.env` sits in the launching client's directory | **duplicate** | — | Phase 12's U11-7, confirmed there and **held** (§11). Re-demonstrated incidentally by the finder (11 tools instead of 6). Correction for the record: the MCP server is **no longer read-only by design** — `serve.ts:72-74` builds five write tools under `ALLOW_WRITES` |
| U7-2 | Mechanical | `bridge/src/mcp/stdio.ts:39-51` | The server never exits when stdin closes | confirmed (**reshaped**) | #1407 | `degraded`. README command run verbatim: **exit 124**. `FSWatcher` is the sole live handle and the SIGTERM handler releases it in 1 ms. **Not a spec violation** — MCP puts all three shutdown steps on the client and gives the server a MAY; the reference SDK escalates literally, so a good client costs ~2 s. The costs are the broken documented command and a permanent orphan when a client is force-killed |
| U7-3 | Performance | `bridge/src/mcp/tools.ts:571-577` | The MCP `q` has no length bound, unlike the HTTP one | confirmed (**reshaped**) | #1406 | `degraded`. 2 MiB in → 8.39 MB out (4.00×); a 20,000-deep nest dies as "The tool failed to run." Bisected: depths to 5,000 are fine, so a 200-char bound is a **sufficient** guard. **Security framing dropped** — a local process, `q` from the user's own model. `tools.ts:33` already imports from `api/limits.ts`. `write.ts:62-67` records the author reasoning the opposite way for the write arguments, naming the missing transport cap. `stdio.ts:49` backpressure is **not** a finding |
| U7-4 | Functional | `bridge/src/mcp/dispatcher.ts:206-215` | `additionalProperties: false` is published and never enforced | confirmed (**wider**) | #1400 | `wrong-data` for writes, `degraded` for reads. **The write limb is the finding:** `fromLocationID` (wrong casing) on `gubbins_check_out` drew 2 units from **Shelf 2** instead of Bin 4 and recorded `sourceLocationId: loc-shelf-2` — a false ledger row with `isError:false`. MCP spec puts validation on the **server** ("Servers MUST: Validate all tool inputs"); the SDK validates server-side and the bridge hand-rolls its dispatcher. Read-side rated down: the list envelope echoes the *effective* limit and offset |
| U7-5 | Functional | `bridge/src/mcp/tools.ts:93-99`, `:101-108` | The schemas declare `type: "string"` while an array is accepted | confirmed | #1416 | `cosmetic`, merged with U8-6. The array support is deliberate and lives **below** the MCP wrapper (`field-select.ts:235`, `RawSelection.fields` typed `unknown`), documented and under test — so the schema is wrong, not the behaviour. Certain effect is that the model always sends the string form and the capability is invisible |
| U7-6 | Functional | `bridge/src/mcp/dispatcher.ts:127-134`, `:270-273` | A non-conformant `id` is rewritten to `null` and the request executes | confirmed (**reshaped**) | #1405 | `degraded`. Write limb driven: quantity 42 → 37 and the snapshot rewritten, acknowledged on an uncorrelatable id, so a retry double-applies. Internal inconsistency is the sharper framing — `coerceId`'s docstring and the guard three lines up read `undefined` one way, `:134` throws it away. `id: null` is also overloaded with the parse error and the batch refusal. Unreachable from a conforming client |
| U7-7 | Mechanical | `bridge/src/mcp/stdio.ts:43-51` | The promise chain has no `catch`, so one failed write drops every later request | **rejected** | — | The finder's probe threw *synchronously*, which no real Node writable does — measured on destroyed and ended `PassThrough`s and a real socket with the peer gone: `write()` returns `false`, never throws. `dispatch` cannot reject: 15 hostile messages → **0 rejections**. The real EPIPE surfaces as an `'error'` event at the moment the client has already gone, so there are no later requests to drop. Missing `installProcessResilience` is defensible — #305 is explicitly the HTTP daemon's problem, and for a stdio server exiting is correct |
| U7-8 | Mechanical | `bridge/src/mcp/tools.ts:234-235` | `gubbins_list_locations` queries before validating | confirmed | #1413 | `cosmetic`, merged with U7-9. Spy-counted: 1 query on the locations tool, 0 on `search` and `get_item`. **Finder's "a real query for each failed guess" corrected** — the error names the whole valid vocabulary, so a model recovers next call. One wasted read, on a local database |
| U7-9 | Prompt | `bridge/src/mcp/tools.ts:93-99`, `:228` | The shared `fields` schema names item-only fields on the locations tool | confirmed | #1413 | Merged. Both worked examples fail on that tool. In scope for this phase — a schema example naming values the enforcement rejects is text contradicting mechanics, not prose quality. The `include` group names **do** match `ITEM_INCLUDE_ALIASES`, so only the `fields` examples drift |
| U7-10 | Functional | `src/db/repositories/base.ts:174-176` | `hasMore` is true on a full final page | **rejected** | — | The documented contract is "a full page came back", in four places: `types/pagination.ts:23`, `base.ts:173`, `openapi.yaml:4988` and a test that pins the exact-boundary case by name. Already rejected twice in this register — Phase 2's P2-7 and Phase 12's U8-7. Not #1340 (that is `limit`, not `hasMore`). Lead re-read all four sources and confirmed |
| U7-11 | Mechanical | `bridge/src/mcp/stdio.ts` | Nothing drives `runStdioServer` | **rejected** | — | Test gap is real, but mutation shows `smoke:bridge` **is** a gate on the outbound framing: dropping the trailing `\n` fails it (exit 1) while the whole Vitest suite passes. Only the blank-line skip, the `-32700` reply, wire-level notification suppression, `crlfDelay` and the ordering chain survive both gates, and their worst outcomes are cosmetic. The one place with teeth is EOF, already reported as U7-2 |
| U8-1 | Mechanical | `bridge/src/homeassistant/client.ts:163-165`, `:188-189` | The abort timer is cleared before the body is read | confirmed (**reshaped**) | #1417 | `unusable`. Measured **306,214 ms** for a read with a 1,000 ms budget — 306× over, ~61× the shipped default. Both documented claims false as written. Slot limb driven end to end: **286 s of continuous 429** after all ten clients disconnected *and* Home Assistant recovered, with the one-shot endpoint answering 200 beside it. **Finder's "permanently" corrected** — undici's 300 s body-inactivity timeout reclaims it, so a ~5-minute lockout per stalled connection (an until-restart wedge only against a trickling upstream). Also wider: a merely slow body runs past the deadline (400 MiB resolved at 2,991 ms against a 2,400 ms slice), so the retry arithmetic is unsound in general. #5ad681a6's fix rests on this assumption |
| U8-2 | Performance | `bridge/src/homeassistant/client.ts:188-196` | The Home Assistant body is buffered whole with no cap | **rejected** | — | Mechanism reproduced (peak RSS ≈ 4× body; under `--max-old-space-size=256` it **dies**, exit 134, at ~110 MiB). Rejected on reachability: a real 2,000-entity `/api/states` is **1.05 MiB and costs 7 MB** — two orders of magnitude below the threshold; an operator-configured hostile HA is a boundary already crossed; and the only strong path is the mDNS discovery finding the maintainer holds, along which credential disclosure strictly dominates an OOM. A hardening note under that finding, not a defect of its own |
| U8-3 | Functional | `bridge/src/homeassistant/client.ts:112-115`, `:149` | A base URL with a path, query or fragment is silently mis-joined | confirmed (**reshaped**) | #1408 | `degraded`. All four joins driven through the real client and end to end through the real server. **Finder's app-side consequence is wrong** — the "not set up" message is only reachable on a *refresh* of an already-successful listing; on first open `listed.current` is `false`, so `ScaleReadPanel.tsx:152`/`:289` remove the whole panel with **no message at all**, identical to `GUBBINS_BRIDGE_HA=off`. Sibling of #941, not a duplicate: different package, different actor, opposite consequence |
| U8-4 | Mechanical | `bridge/src/homeassistant/scale.ts:23-40` | The grams table claims parity with `src/lib/weight.ts` and nothing holds it up | **rejected** (folded) | #1412 | Six factors identical by `Object.is`; no drift test exists. But the mutation **goes red** — `lb` → 453.6 fails 3 tests across 2 files, because each side is independently pinned to the same exact literal. #901's own bar is "the whole suite stays green", which this does not clear. Residual gap is future-only (a factor added later to the bridge table); folded into #1412 as its weakest item with that framing |
| U8-5 | Functional | `bridge/src/homeassistant/scale.ts:205-222` | A negative weight is returned as a valid 200 reading | **rejected** | — | Pass-through reproduced, consequence disproved. The doctrine at `openapi.ts:2194-2196` is about a scale that **cannot be read** and a **zero** weight, not a negative one; a 409 would need a third contract code. The real `WeighCountDialog` and the live watch both render *"A scale reading cannot be negative."* in a `role="alert"` above *"Read -0.3 g from the scale."*, with Apply disabled — so the finder's "no explanation pointing at the scale" is false, and the gross field is the correct place for it |
| U8-6 | Prompt | `bridge/src/homeassistant/scale.ts:75-76`, `:220` | `lastUpdated` is passed through unvalidated while the schema promises a date | confirmed (**reshaped**) | #1416 | `cosmetic`, merged with U7-5. The *absent* half of the promise is honoured; the unparseable half is not implemented. Finder over-rated it by treating OpenAPI 3.0.3 `format` as enforced — it is an annotation standard validators ignore, and none is wired here. A genuine Home Assistant always emits a real datetime |
| U8-7 | Functional | `bridge/src/homeassistant/scale.ts:33-40` | Three mass units the app supports are treated as "not a scale" | **rejected** | — | A recorded decision, twice. `aa8910b9` (#416) — the very commit that added `ozt`/`gr`/`ct` to `src/lib/weight.ts` — **also touched this file** and its whole diff there is a docstring reword. #1051 re-examined the claim and affirmed it ("Making the two tables equal would be the wrong change"). #416's message gives the reason for `gr` specifically: read as grams it misstates a weight fifteenfold. Four doc sources enumerate or hedge; none claims parity. The `404` sub-claim **is** #1051's stated live wrinkle |
| U8-8 | Mechanical | `bridge/src/serve.ts:429-435`, `:524-542` | A Home Assistant URL containing userinfo is echoed into the startup log | confirmed | comment on #1335 | `degraded`, low. Password printed **twice** per start. Amplifier the finder missed: Node's `fetch` refuses a credentialed URL outright, so the configuration can never connect and is misreported as unreachable — guaranteeing the operator is reading the log. `redactUrl` already exists and is reused at six sites. Same root cause as #1335 (two bridge log sites bypassing it); #1315 is app-side with a different audience |
| U8-9 | Performance | `bridge/src/homeassistant/scale-stream.ts:395-402` | Stream writes ignore backpressure and the header credits a drop policy that does not exist | confirmed (**reshaped**) | #1399 | `degraded`, merged with U1-2. **Documentation half disproved** — a client whose socket *fails* **is** dropped (verified: `clientCount` 1 → 0 on RST), and a stalled client is not a write failure, so the header says nothing untrue. Memory half measured over 5 stalled sockets: linear from t≈15 s, **no plateau**, none dropped after 300 s. Frame is 172 B not ~130, so **~2.4 MB/hour** per client — the finder was off by 7.5× |
| U9-1 | Functional | `bridge/gubbins-bridge.service:57` | `RestrictAddressFamilies` omits `AF_NETLINK` and `AF_UNIX` | confirmed (**worse**) | #1402 | `degraded`. No systemd here, so settled from `seccomp-util.c:1663-1746` and reproduced byte-for-byte in libseccomp inside `node:22-slim`: `os.networkInterfaces()` **throws** (errno 97), it does not degrade to an empty map. Real `serve.mjs` then logs `Bridge failed to start:` while `/health` answers 200, and the SIGINT/SIGTERM registrations are never reached. `AF_UNIX` limb driven separately: `.local` fails `ENOTFOUND` while ordinary DNS is unaffected. Runs even when `GUBBINS_BRIDGE_HOST` is concrete |
| U9-2 | Functional | `bridge/gubbins-bridge.service:65-69` | The write-access note names only `ALLOW_PUSH` | confirmed | #1403 | `unusable` for the feature. Sibling temp file observed in the snapshot directory during a real adjust-quantity; then the bridge run in a container with that directory on a read-only bind mount: **HTTP 500 `internal_error`**, `EROFS` in the log. `EROFS` is not a `SnapshotConflictError`, so it bypasses the retry and the `WriteError` branch. Wider than a NAS mount — `ProtectSystem=strict` makes the checkout read-only too. `README.md:2721` reinforces the wrong model |
| U9-3 | Prompt | `bridge/gubbins-bridge.service:44` | The unit still tells the operator a Token lives in the environment file | confirmed (**severity cut**) | #1415 | `cosmetic`, merged with U9-5. `git log -L` proves it is original shared-token-era text that #79 left behind; `serve.mjs:5` carries the same phrasing. **Finder's consequence rejected** — reading "Token" as the outbound HA token is factually true today, and the correcting text is unavoidable (step 2 of the same file, thirty lines above) |
| U9-4 | Functional | `bridge/serve.mjs:24` vs `bridge/gubbins-bridge.service:45` | Node's `.env` reader and systemd's `EnvironmentFile=` parse the same file differently | confirmed | #1404 | `degraded`. Node half measured; systemd half quoted from `env-file.c`'s `PRE_KEY`/`VALUE_ESCAPE` states. **The finder chose the safe direction** — the webhook-secrets case fails loudly with two precise diagnostics. The damaging direction is the backslash: correct locally, wrong under systemd, and MQTT's warn-and-retry means the service reports `active (running)` while publishing never happens. Quoting reconciles both parsers; nothing documents it |
| U9-5 | Functional | `bridge/gubbins-bridge.service:46-47` | `RestartSec=5` never trips the default start rate limit | confirmed (**severity cut**) | #1415 | `cosmetic`, merged with U9-3. Arithmetic shown against `ratelimit.c`'s resetting fixed window: peak `num` 3 against a burst of 5. Complement of #1330, not a restatement — and #1330's fix would *widen* this. Finder's citation corrected (`systemd.unit(5)`, `[Unit]`, not `systemd.service(5)`). **"Invisible to monitors" overstated** — `systemctl is-active` prints `activating` and exits 3 for ~96% of wall clock |
| U9-6 | Mechanical | `bridge/src/openapi.test.ts:21-24` | The drift guard never parses the YAML it guards | confirmed | #1410 | `degraded`. Committed file verified clean (byte-identical re-emission, 0 round-trip differences). Mutation with a plausible `SwitchCapability` schema: the guard **passes**, the round-trip **fails with 5 differences**. README's promise is stronger than the finder said — "the **identical** document … point a client-generator at **either**". Blast radius is YAML 1.1 parsers (PyYAML, SnakeYAML), not YAML 1.2. Sibling of #1341, opposite failure mode |
| U9-7 | Prompt | `bridge/gubbins-bridge.service:62-64` | The comment calls `ReadOnlyPaths=` a way to grant read access | confirmed | #1403 | `cosmetic`, folded into U9-2's issue. `systemd.exec(5)` quoted both ways: `ProtectSystem=strict` leaves the hierarchy readable, and `ReadOnlyPaths=` only ever subtracts. Nothing in this unit removes read access from a NAS mount. A local slip — the unit gets the distinction right at line 69 and lines 33-37 |
| U9-8 | Mechanical | `bridge/scripts/emit-openapi-yaml.mjs:24` | The regenerator truncates `openapi.yaml` in place | **rejected** | — | Nothing at runtime reads the file; the served spec comes from the typed object. Two one-command recoveries demonstrated, and the guard does fire. Write window measured at **1.84–2.81 ms** in a 311 ms script. Matches the repo's convention for dev tooling (four sibling scripts). `snapshot-io.ts`'s atomic publish exists for a *concurrent reader*, which this file has none of |
| U10-1 | Mechanical | `bridge/src/events/model.ts:444`, `:446`, `:378` | A prototype-key `action` makes `kind` and `label` vanish from the frame | confirmed (**wider**) | #1401 | `degraded`. Both shapes: functions are dropped by `JSON.stringify`, `__proto__` survives as `{}`. Wider than the bridge — the same helpers blank the Activity Log's title (React "Functions are not valid as a child") and **crash** it for `__proto__` ("Objects are not valid as a React child"), plus `undefined` in the CSV export. `eventTypeForAction` guards this three lines away and says why. Not #1354 — that has an error path, this has none |
| U10-2 | Functional | `bridge/src/events/model.ts:438`, `bridge/src/events/generation.ts:58-68` | The truncation summary counts only what the scan window returned | confirmed (**reshaped**) | #1379 | `wrong-data`. 200-row generation at shipped defaults delivers a **middle slice** (`h-101`–`h-150`), says "50 more changes not delivered", actually omitted 150; next generation returns 0. The **scan-window bound itself is a recorded limitation** (`docs/todo/done/webhooks_2026-07-18.md:230-233`, `docs/wiki/Webhooks.md:112-117`) and is not filed — the under-count and three false docstrings are. Two issues, not one, with U1-5: different causes, and either fix leaves the other |
| U10-3 | Performance | `bridge/src/events/generation.ts:87-101` | One repository round-trip per new ledger row, un-memoised | **rejected** | — | Measured: 104 and 203 calls, **1.9–2.2 ms median**, against **~65 ms** for the `hydrateFromJson` the pipeline pays unconditionally on every reload — 3–4% of existing work. The two shapes differ by 0.3 ms despite a 2× read count. Scaling to a Pi does not change the ratio. `getManyById` is a legitimate tidy-up, not a defect |
| U10-4 | Functional | `src/features/events/event-catalogue.ts:330`, `:355-360` | The truncation notice does not reach a default subscriber | **rejected** | — | Both legs reproduce and both are **stated intent**: `event-catalogue.ts:88-91` and `EventTypePicker.tsx:15-16` ("Offered, because a subscriber genuinely needs to know it can arrive, but never assumed"), and `docs/wiki/Webhooks.md:119-122` tells users. `filter.ts:23-26` and `webhook-view.ts:211` state the item-scoped rule deliberately. SSE and MQTT **do** receive it, and file/env targets default to `['*']`. The exclusion is confined to the one surface whose picker explains it |
| U10-5 | Prompt | `bridge/README.md:1519-1540` | The README's event-type list omits `item.tracking_changed` | confirmed | #1386 | Merged with U2-6. Coverage computed from the vocabulary: README 25/26, example 17/26, wiki 0/26. `git log -S` shows the type has **never** been in either file. Same root cause as U2-6 and the same fix, which `bridge/src/openapi.ts:2989-2994` already applied to the third copy by derivation |
| U10-6 | Functional | `bridge/src/events/model.ts:478` | A threshold change raises no low-stock event though every count includes it | confirmed (**narrowed**) | #1409 | `degraded`. Both limbs reproduce, but only the **per-item `reorderPoint`** limb is filed: it writes an `ATTRIBUTES_CHANGED` row that is resolved with the item's state attached and then discarded by the action test. The **blanket-threshold** limb writes no ledger row and is the project's documented "no ledger row, no webhook" boundary. **The finder's contradiction claim is overturned** — `README.md:974-976` read against `inventory-scan.ts:5-17` is about the *predicate*, and `README.md:1526-1528` states the trigger accurately. Sibling of #1036, not the same defect |
| U10-7 | Mechanical | `bridge/src/events/lookup.ts:130-133` | `normaliseQuery` folds case by hand instead of through `name-fold` | confirmed (**re-based**) | #1411 | `degraded`, thin reachability. **The invariant limb is a false positive** — `name-fold` is scoped to natural keys with uniqueness constraints, and applying it here would merge Turkish dotless i and expand the sharp s. The `Größe`/`GRÖSSE` example is not evidence either: the second spelling returns no matches, so nothing publishes. The real defect is the missing **NFC** normalisation: NFC and NFD `Café` both resolve to `item-0` and deliver **2 events** in one window |
| U10-8 | Mechanical | `bridge/src/events/lookup.ts:216-228` | The eviction comment claims insertion order tracks emission order | confirmed (comment limb) | #1412 | `cosmetic`. `Map.set` on an existing key mutates in place (ECMA-262 step 3.a), demonstrated; a re-emitted digest keeps its stale head slot and is evicted before genuinely older windows. **The behavioural limb is unreachable** at shipped defaults — three gates: the flag is off, `maxKeys` is 500 and never overridden, and the default rate limit caps one client at ~63 lookups per 3 s. The bound itself is sound |
| U10-9 | Prompt | `src/features/events/event-types.ts:188` | The comment claims a drift test against the bridge that does not exist | confirmed | #1412 | `cosmetic`. Three test sites verified, none does it. The described test was written and **passes** (so the claim is true, just unguarded) and it **bites**: dropping `LOOKUP_RESOLVED_TYPE` reddens it. Trap for whoever writes it — the obvious spec produces a false positive on `location.changed`, which is emitted only by the unknown-action fallback. The finder's "which is how the `item.tracking_changed` drift survived" is unsupported and was dropped |
| LEAD-1 | Functional | `bridge/src/ical/feed.ts:126-128` | `calendarModifiedAt` clamps the validator to the day's start, collapsing a day's changes onto one tag | confirmed | #1418 | `wrong-data`. Carried into this phase by Phase 12 (§11). Driven: three genuinely different documents (7, 6, 5 events), **one `ETag`**, `304` on revalidation, and a control with a fresh `generatedAt` moving the tag. Reachable because `isTimestamp` is a **range** check only — a stale but in-range `generatedAt` passes untouched, and `push.ts:240`/`:257` return the pushed value verbatim. Independent of #1368 and neither fix subsumes the other: the two fetches already receive a byte-identical value, so unflooring the digest changes nothing |

### Phase 14 — Satellites

Pinned SHA: `32c3a6729d5c6f7e472f1bba3f63fb285386a99f`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | Mechanical | `extension/src/background.ts:185`, `:224`, `:259-266`; `src/features/scraping/parsers/registry.ts:79-92` | The fetch gate is applied before the redirect only, so an off-list body comes back — and is parsed **as** the allow-listed supplier | confirmed (**reshaped**) | #1422 | `wrong-data`. Driven in real Edge on the shipped `dist` with `--host-resolver-rules` (no repo edit, no DNS change): the DigiKey parser ran on an off-list body and produced a full part record; the echoed URL still named the requested host, so the app's URL-correlation guard confirms a lie. **Wider than filed** — `runParser` selects by the *requested* URL, so the body is laundered into a typed supplier record. **Narrower on security** — Chromium re-applies CORS per hop, so every off-list target except loopback needs permissive CORS, which the page could read itself. **Not remotely reachable**: no `externally_connectable`, no content script outside `/Gubbins/`. Needs no attacker today: Wikidata `Special:FilePath` 302s off-list. `bridge/src/events/webhook.ts:524-538` already applies the fix (#494) |
| A2 | Mechanical | `extension/src/background.ts:398`, `:412-418` (write-back at `:417`) | Un-serialised read-modify-write of the scrape queue destroys a user-triggered scrape | confirmed | #1420 | `data-loss`. Driven with the real injected bundle and the real listener. 8 simultaneous scrapes over 5 trials: queue held `[2,5,6,5,4]`, expected 8; the serialised control held 8/8 every time. A flush racing 3 fresh scrapes lost at least one in **6/6** trials. The dangerous window is the **flush's**, not the append's (two appends alone lost nothing in 10 trials) and widens with queue length. Cap bites 760× before the quota; the drop is reported nowhere |
| A3 | Functional | `extension/src/background.ts:295-317` | The context menu is missing 7 of 22 marketplaces, and the "kept in step" comment is false | **duplicate #1269** | — | Deduped by the lead before verification: #1269 is open and names exactly this, including the same 7 TLDs |
| A4 | Mechanical | `extension/src/background.ts:385`, `:386-394`, `:397`; `extension/manifest.json:13`, `:25-37` | `chrome.tabs.query({url})` returns **nothing**, so every active-tab scrape is queued instead of delivered | confirmed (**inverted, wider**) | #1421 | `degraded`. The finder said the filter is *ignored* and every tab is returned; the truth is the opposite — Chromium excludes every tab whose URL the extension may not read. **Lead reproduced independently on the pristine `dist`**: 4 tabs, 0 URLs visible, filter returns 0 with two matching tabs open. Also driven against the real published deployment, and by differential with only `"tabs"` added. `:392` is dead code. **Performance limb REJECTED with the measurement**: 3 vs 59 tabs, 0.6 → 0.4 ms. Trap: `chrome.permissions.getAll().origins` *does* list the three app patterns |
| A5 | Mechanical | `extension/src/background.ts:530-535`; `active-tab-scrape.ts:21-23`, `:47-48` | The worker never answers `ACTIVE_TAB_SCRAPE`, so the caller's promise rejects unhandled | **rejected** | — | The mechanism does not exist in this browser. Driven at the real branch: the promise **resolves with `undefined`**. Port-closed is reachable only through `chrome.runtime.lastError` in the callback form, which this code does not use. With the shipped bundle injected by `executeScript`: `page exceptions: []`, `page consoles: []` — not even the `cosmetic` noise it was rated. The synchronous-throw limb is real but needs the extension reloaded inside a millisecond-wide window mid-parse, with an identical outcome |
| A6 | Functional | `extension/src/background.ts:421-431`, `:456-458`; `extension/manifest.json:10-12` | A toolbar click on any non-Amazon tab does nothing at all, silently | confirmed | #1423 | `degraded`. Driven through the real listener body with a `Tab` shaped as `onClicked` delivers one: queue, badge, title, enabled state and worker console all unchanged; the PWA saw nothing. **In-function contrast**: the same tab id with an Amazon URL produces a typed `BLOCKED` error. Zero hits for `setBadgeText`, `action.disable`, `declarativeContent`, `setTitle` or `notifications` in `extension/src`. Distinct from #1269, which frames the unfiltered button as the *workaround* |
| A7 | Mechanical | `extension/src/background.ts:530-533`; `content-script.ts:270-288` | A malformed active-tab outcome is forwarded unvalidated, then cleared as delivered | **rejected** (real, unreachable) | — | The trigger does not exist: 12 adversarial DOM/URL shapes through the real parser and the real schema gave **0/12 invalid**, because `distributor_url` is synthesised from the ASIN and `currency` from the marketplace, never page-derived. The one parser that *could* emit an invalid URL is unreachable here — its selection requires a non-Amazon URL, which is the same document the sender check rejects. Gate driven and held. **The downstream half is real** and was demonstrated (posted, `delivered:true`, cleared, dropped by the app's schema with no log) — recorded as a latent type hazard, not a live defect |
| B1 | Functional | `src/features/scraping/parsers/amazon-parser.ts:99-102` | A non-listing Amazon page yields a payload assembled from unrelated products | **duplicate #1264** | — | Reproduced in full, including the finder's "new" limb. #1264's own second reproduction block already shows description from `<title>`, price from one tile and ASIN from another. Only one sentence of its prose understates it; posted as a clarifying comment rather than a new issue |
| B2 | Functional | `src/features/scraping/parsers/amazon-parser.ts:70-77`, `:57-63` | `cleanBrand` recognises exactly two English wordings, so any other byline is stored verbatim as the manufacturer | confirmed (**wider**) | #1424 | `wrong-data`. Demonstrated with a **wholly invented** wrapper, so it does not depend on guessing Amazon's real localised wording. Not "English-only": `by Acme`, `Acme (Author)` and `Visit the Acme Store on Amazon` all fall through, because both regexes are anchored. Ordering limb also real — `#bylineInfo` beats the localisation-free details cell. Nothing downstream sanitises; the 500-char column accepts the sentence |
| B3 | Mechanical | `extension/src/active-tab-scrape.ts:21-23`, `:48` | The same defect as A5, seen from the caller | **rejected** (merged into A5) | — | Verified once, under A5 |
| B4 | Functional | `src/features/scraping/parsers/registry.ts:87-91`; copy at `scrape-errors.ts:93`, discard at `:115` | "Not a product page" borrows `DOM_DRIFT`, so the user is told Amazon changed its layout | confirmed (**re-located, reframed**) | #1425 | `degraded`. The finder's `active-tab-scrape.ts:46` is the wrong line — `runParser` catches the `DomDriftError` at `registry.ts:87-91`. The reason is discarded for all 8 members **by design**, and #667's precedent was to add a new member rather than surface it; no existing member fits. Real drift and mis-click produce byte-identical toasts. The wiki promises "Gubbins tells you which of those it was". Interacts with #1264: that fix converts B1 cases into these |
| B5 | Mechanical | `extension/src/content-script.ts:79-83`, `:91`; `background.ts:62-70` | The internal reply shapes are declared twice and joined by an unchecked `<R>` | confirmed (**reshaped, downgraded**) | #1450 | `cosmetic`. Mutation run: a consistent rename leaves `tsc` at exit 0, and `--listFiles` confirms both files are in one program — so #557 is genuinely fixed and the hole is the erased reply type. **The finder's cited "mirrors" comment is the one claim that IS held up**: mutating `ParseOutcome` turns the check red at `active-tab-scrape.ts:38`, because it adapts from the imported type in compiler-checked code. Root defect is the `<R>` generic, not the comment. No divergence today; both halves ship from one build |
| B6 | Performance | `extension/src/content-script.ts:134`, `:145` | Supplier HTML is DOM-parsed on the app tab's own main thread | confirmed | #1426 | `degraded`. Measured on an i9-13900K with synthetic pages: worst frame gap 54.3 ms at 3 MB (1×) and 320.9 ms at 4×; a bulk refresh of 8×3 MB starved 354 ms of frames. `DOMParser` is 85-95% of the cost; the parser walk is 0.1-3.3 ms. ~13 ms/MB at 1×, ~78 ms/MB at 4×. **Three finder claims struck**: the `[data-asin]` walk is the Amazon path and runs on Amazon's tab, not the app's; the JSON-LD flatten is not where the time goes; `chrome.offscreen` has a `DOM_PARSER` reason and would move all of it. A page-world `longtask` observer records **nothing** — the frame gap is the instrument. Premise to check before acting: real supplier page weight |
| B7 | Mechanical | `extension/src/content-script.ts:251-258`, `:290-302` | A 1.5 s handshake window with no recovery, and a flush not gated on the app listening | **rejected** | — | Limb 1 unreachable: the announce window is anchored to `document_idle`, and the app's first commit is queued before the load task, so headroom **grows** as the machine slows — 58 → 143 → 459 → 1713 ms at 1×/4×/10×/20×. Driven against the real production build. Limb 2's mechanism was confirmed in real Edge (a flush into a page with no listener is cleared as delivered) but is unreachable in the real app for the same ordering reason, and its general form is already **#1111** — posted there as a comment |
| C1 | Functional | `custom_components/gubbins/api.py:151-160`; `config_flow.py:114-120` | A 403 or a 503 is reported as "could not reach the bridge" | confirmed (**reshaped**, severity down) | #1431 | `degraded`, not `unusable` — an Administrator or Manager token works first time. Driven with a **real** token minted on a real restricted role, and a real 503 from a typo'd snapshot path. **Six** of eight built-in roles are refused, not four. **The finder's sharpest limb is struck**: `const.py:57` names a *permission*, not the Loans desk role. The real chain is that the wiki says "narrow role" and `homeassistant/README.md` never mentions `bridge:read`. The coordinator is more honest than the flow, and `_write` already has the branch `_get` lacks. #1306 explicitly defers this half |
| C2 | Mechanical | `custom_components/gubbins/api.py:126`; `config_flow.py:83-91` | A bare IPv6 host is never bracketed, so an IPv6 bridge reports "could not reach" | confirmed (severity down) | #1438 | `degraded`, not `unusable` — **the finder's headline is wrong**: typing the brackets works and gives a fully working entry. Driven both ways against a real bridge bound to `::1`. The discovery limb is the one with no recourse: no host field on the confirm step, and both escape doors shut (`already_in_progress`, then `already_configured`). **But the bridge never publishes an AAAA record**, so that scenario needs another responder's AAAA on the LAN — the flow's behaviour was driven, the discovery arriving was not. Third instance of one pattern (#1408, #941) |
| C3 | Mechanical | `custom_components/gubbins/config_flow.py:196-217`, `:122-145`, `:147-194`; `bridge_id.py:38`, `:61-65`, `:68-74` | An unauthenticated mDNS advertisement relocates a config entry and is sent its stored bearer token | confirmed, **EXPLOITABLE — fixed at the maintainer's direction** | — (fix `dd2399d6`) | `data-loss` (credential + full inventory). Driven under real HA 2025.1.4 against a real bridge with a listener at the advertised address, which received `Authorization: Bearer <the entry's token>` twice. **Materially wider than the finder claimed**: the bridge id is *not* needed. `address_unique_id` mints `host:port` into the same namespace `bridge_id_from_txt` accepts arbitrary values from, and `:` is a legal id character — so two adverts (one with no `id` at all) re-key the entry to its address and then claim that address from anywhere. Precondition collapses to a port scan or the default 8787. Verifier separated the halves: identity-matched relocation is HA's own sanctioned pattern; the **forgeable namespace** is this integration's own defect. Maintainer directed a fix rather than a filing (pre-release). Fix keeps an advertised id out of the address namespace; re-driven, both credential-free chains now end in a confirm form with the entry unmoved and the listener receiving nothing, and a genuine move still heals |
| C4 | Mechanical | `custom_components/gubbins/api.py:384-385`, `:395-396` | A failure reading a **successful** write's response is reported as "could not reach the bridge" | confirmed (**wider**) | #1429 | `wrong-data`, filed with C6 as one root cause. Driven under real HA against a raw-socket stub: truncated body and `text/plain` both produce "Could not reach the Gubbins bridge", with the stub's log proving the POST arrived first. **Wider than filed**: the write path infers delivery from the *exception class*, so a 502, a 504 and the bridge's own catch-all 500 take the same branch — those go through `raise_for_status()`, not the JSON decode. The real bridge cannot produce the `ContentTypeError` shape; the 5xx cases are the reachable ones |
| C5 | Mechanical | `custom_components/gubbins/__init__.py:188`, `:205-211` vs `:223-233` | A reload or restart while the bridge is down deletes all six services and the voice intent | confirmed (**wider**, severity up) | #1427 | `unusable`, not `degraded`. Driven with the real conversation agent: services all absent, `ServiceNotFound` to callers, and the voice path raises **`UnknownIntent` uncaught** out of `async_converse` — the agent's except list does not include it, so the user does not even get "I couldn't understand that". Both deliberately-worded fallbacks become reachable when the registration block is moved above the probe, with no other change. Backoff caps at 80 s but does **not** bound the outage. HA rejects no automation; it fails at run time. Multi-bridge with one healthy bridge is unaffected. HA's own `action-setup` quality rule names this shape |
| C6 | Functional | `custom_components/gubbins/api.py:388-394`, `:51` | A write that never reached the bridge is reported as "may already have been applied" | confirmed | #1429 | `degraded`, filed with C4. **The finder's own demonstration fails on Windows** (21 s SYN budget beats the 60 s total) — driven instead on a real Linux kernel with a netfilter DROP: `SYN-SENT` for the whole 60 s, no connection-created/headers-sent/chunk-sent callbacks, and `GubbinsWriteTimeoutError` regardless. Platform-independent variant via an unanswering resolver. **The obvious fix is a trap**: `ConnectionTimeoutError` subclasses both `ClientError` and `asyncio.TimeoutError`, and the timeout handler is first — so `sock_connect=5` makes it fire faster and still lie |
| C7 | Mechanical | `custom_components/gubbins/api.py:156`, `:385` | A JSON decode failure escapes the typed-error mapping in both `_get` and `_write` | confirmed | #1430 | `degraded`, filed with D11. Under HA's own session the class is `orjson.JSONDecodeError` — a `ValueError`, not a `ClientError` — so the mechanism holds for the real client. Driven across all five consumers: the config flow's exception **escapes** into an unhandled HTTP 500 (there is no `unknown` reason key), both service handlers raise untyped, the coordinator degrades gracefully, and the voice path is immune via `_ask`'s catch-all. `text/plain` control maps correctly. Reachability narrow: needs a listener answering 2xx while claiming JSON. `_error_message:65` catches `ValueError` around the identical call |
| C8 | Mechanical | `custom_components/gubbins/bridge_id.py:34`, `:37-38`; `__init__.py:389-391` | Two parity claims with no test behind them | confirmed (**narrowed**, severity down) | #1446 | `cosmetic` today, `degraded` hazard. Mutation run both ways: widening the **length** reddens one incidental hard-coded `65` in a config test that names nothing about Python; widening the **charset** leaves all 1191 tests green. Finder's line numbers corrected (the file is 100 lines). Fail-safe in the common case — an unreadable id falls back to address keying. **The `located_matches` claim is REJECTED as a parity finding**: both sides call the same `normalise_matches`, so rule 1 is already satisfied; only the wording is wrong, and it costs a reader who templates `.matches[0].item_name` against the raw camelCase. No Python tests exist anywhere |
| C9 | Performance | `custom_components/gubbins/__init__.py:188`, `:198` | Two serial round-trips before any platform is forwarded | confirmed as fact, **not filed** (below the bar) | — | Measured: setup tracks the status delay 1:1 (0.123 / 5.127 / 9.127 s), and backgrounding it collapses to ~0.11 s with a byte-identical entity end state. **Two supporting claims disproved**: no "taking over 10 seconds" warning is armed around `async_setup_entry` in 2025.1.4, and HA sets entries up concurrently — two 9-second entries finished in 9.157 s, not 18. Against the real bridge the two endpoints are 2.00 vs 2.12 ms on the fixture, and reaching 1 s of status time needs ~10⁶ items. Honest severity `cosmetic`; recorded in §11 as a tidy-up rather than filed |
| C10 | Functional | `custom_components/gubbins/manifest.json:12` | The manifest version is on an independent line from anything published | **rejected** | — | The HACS question was settled from HACS's own source: it reads four manifest keys and `version` is not among them; installed and available versions both come from the release tag. HA renders the manifest value on its integration page and otherwise only requires it to parse. **The finder's second premise is false**: only 5 of 19 commits touching the integration bumped it, and the four most recent did not — so the bumps are sporadic, not per-change. No claim anywhere says the two track. Not #749's instance either: neither `hassfest` nor `hacs/action` validates the value |
| D1 | Functional | `custom_components/gubbins/__init__.py:543-568` | No write service refreshes either coordinator | confirmed (severity corrected) | #1432 | `degraded`. Driven with a real `check_out` against a real writes-enabled bridge: the bridge moved 0 → 1 while both binary sensors stayed `off` until the interval elapsed. **The fix must be a delayed refresh** — measured, the bridge's read path lags its own write by ~252 ms and HA's debouncer fires immediately, so an immediate refresh reads the old value. **Two finder claims disproved**: a `state`-triggered automation does **not** re-fire (a stale sensor never transitions) and a second `check_in` is refused by the bridge. `transfer_stock` moves no exposed figure at all |
| D2 | Functional | `custom_components/gubbins/sensor.py:56-68`, `binary_sensor.py:121-139`, `api.py:143-158` | The bridge's own stale-data verdict is discarded | confirmed (**wider**) | #1428 | `wrong-data`. The bridge was made genuinely stale (a real snapshot truncated repeatedly), not stubbed: `/health` 200 with `ok:false, snapshotStale:true, reloadFailures:5` while one loan was overdue on disk and every entity read `off`/`count:0`, available. **The finder was wrong that `/api/v1/status` carries no marker** — #394's `X-Gubbins-Snapshot-Stale` header rides on *every* authenticated read, verified live on four endpoints, so **both** coordinators already hold the verdict and drop it at `api.py:143-158`. The fix is local, not architectural. The wiki promises an MQTT user a *Snapshot stale* sensor; the first-party integration has none |
| D3 | Functional | `custom_components/gubbins/entity.py:28` | Every entry's device is named "Gubbins bridge" | confirmed (**narrower**) | #1433 | `degraded`, filed with D4. Two entries, two real bridges (the second pinned with `GUBBINS_BRIDGE_ID` after observing #1362's collision): both devices identically named, friendly names **byte-identical**, `_2` only in the entity id. `name_by_user` **is** a real one-time fix, so this is a defaulting defect; renaming the *entry* changes nothing. **Action targeting is not affected** — the service picker lists `entry.title`, which is distinct |
| D4 | Functional | `custom_components/gubbins/intent.py:144`; `bridge_id.py:93-100` | A two-bridge voice answer reads the bridge's IP address and port aloud | confirmed; **security half rejected** | #1433 | `degraded`, filed with D3. Real `conversation.process` with the shipped sentences installed produced `'Gubbins (127.0.0.1:8861): …'`. Both entry-creating paths title the entry that way; the single-bridge path prefixes nothing; only reached when an item matches on two or more bridges. **"Leaks the LAN address" is REJECTED** — the bridge advertises the same host and port in an unauthenticated mDNS record *by design*. Contradicts the wiki's "each behind the name you gave the bridge" |
| D5 | Functional | `custom_components/gubbins/sensor.py:53`, `binary_sensor.py:118`, `entity.py:27` | Unique ids are keyed on the config entry, not the bridge's stable id | confirmed (**materially narrower**) | #1434 | `degraded`. **The finder's damage claim is wrong in its specific form**: `async_remove` purges the old rows (0 orphans) and the re-added entities reclaim **byte-identical entity ids**, so cards, automations and history survive. What is actually lost, driven with customisations set first: the device's `name_by_user` (which is D3's only workaround), its area, per-entity renames, icons, hidden flags and the **device registry id**. The **move** promise holds completely. HA's own rule makes `entry_id` a last resort "if no other Unique ID is available" — one is, and `_async_reconcile_unique_id` has already put it on `entry.unique_id` before the platforms are forwarded. Collision impossible (ULID). A fix must handle #1362 and the `host:port` fallback |
| D6 | Prompt → Functional | `custom_components/gubbins/binary_sensor.py:8-10`; `bridge/src/feeds/item-status.ts:41-45` | "Can never disagree" is false; and the expiry window never reaches the bridge at all | confirmed (**wider**) | #1435 | Two limbs. Limb A (`cosmetic`): the low-stock disagreement is real but **deliberate and documented** (#483, the wiki's heads-up); only the Python docstring flattens it into an absolute — demonstrated app 2 vs bridge 1, and a control with the blanket shared agreeing at 2/2. **Limb B is new and functional (`degraded`)**: `projectItemStatuses` passes only `lowStockThresholds`, so *Expiring soon* always uses the shipped 30-day window — app 1 vs bridge 0 with the window set to 60 and shared. `expirySoonWindowDays` appears nowhere in `bridge/src` outside two OpenAPI strings. Distinct from #1036, which is a third disagreement |
| D7 | Prompt | `custom_components/gubbins/sensor.py:3-5` | The docstring calls `ok` "liveness" and recommends it for the bridge going away; the bridge calls it a data verdict | confirmed (severity down) | #1447 | `cosmetic`, not `degraded` — the wiki is right twice, so the audience is contributors. Driven both ways: bridge alive with a stale snapshot gives `ok: False`; bridge gone gives `unavailable` with **`ok` absent**, so a template reading it gets `None`. The stronger "stale true" variant is therefore **not** available. This is the only place in the integration that says what `ok` means |
| D8 | Performance | `custom_components/gubbins/intent.py:100-108`; `api.py:40` | A voice lookup waits the full ten-second budget on a bridge that stopped answering | confirmed (**reshaped**) | #1436 | `degraded`. **The finder's open question settled**: HA imposes no cap on the intent stage in 2025.1.4 — the handler is awaited unwrapped and the only timeout is the 300 s whole-pipeline one. Measured 0.016 s healthy vs 10.890 s with one wedged bridge, ratio ≥ 680×; 11.00 s single-bridge. **Narrower gate**: a bridge already black-holed at startup costs nothing (setup fails, the client never reaches the store) — the entry must load first. **Wider consequence**: it hits the ordinary single-bridge user too, not only the two-bridge hostage case |
| D9 | Functional | `custom_components/gubbins/__init__.py:104`, `:139`, `:169` | `vol.Coerce(int)` truncates a fractional delta or quantity | confirmed (**wider**) | #1437 | `wrong-data`, low frequency. Wire captured: `-1.5` → `{"delta": -1}`, `2.7` → `{"quantity": 2}`, with the gauge control's float intact; against the real bridge quantity went 3 → 2. **The argument the finder did not have**: the bridge already answers all three with **422** in the app's own wording ("Quantity delta must be a whole number."), so the schema is *suppressing a rejection*, contradicting `__init__.py:123-129`'s stated design. `2.0` is unaffected. `vol.Coerce(int)` is idiomatic HA (`cv.positive_int` is literally this); what is not is applying it in front of a service that validates better. Selectors do **not** enforce `step` server-side |
| D10 | Mechanical | `custom_components/gubbins/sensor.py:56-60` | `native_value` admits a `bool` where the coordinator beside it rejects one | confirmed | #1448 | `cosmetic`. Driven with a stub: the sensor reads `True` on a `MEASUREMENT` entity while the sibling binary sensor is correctly `unavailable` — the same boolean, one guard filtering and one not. With a real recorder and statistics run, `statistics_during_period` returns `{}` and **nothing is logged**; the integer control produces statistics normally. Needs a modified or spoofed bridge, since `itemCount` is a repository count typed as a number. Same shape as #1062 |
| D11 | Prompt | `custom_components/gubbins/intent.py:52` | A single-bridge user is told "one of your Gubbins bridges" didn't answer | confirmed (fold) | #1430 | `cosmetic`, folded into C7's issue. Driven with one entry: the plural sentence is spoken verbatim. Routes mapped rather than assumed — only a non-empty invalid body served as `application/json` reaches it; an **empty** body gives the correct "couldn't find anything" and `text/html` gives the correct singular connection message. So it is reachable **only through C7's hole**, and fixing C7 by mapping the exception makes it unreachable — which is why it belongs in that issue |
| E1 | Prompt | `README-HA.md:179-207` | Step 4's hand-patch breaks a currently working install | confirmed (**wider**) | comment on #578 | `unusable` for that install. Applied Step 4 and Step 6 verbatim to a byte-copy and set it up under real HA: `NameError: name 'GubbinsIntentHandler' is not defined`, `state=SETUP_ERROR` — the **whole entry** fails, not just the intent. A third variant (a reader who spots the class name) breaks *unload* instead, so the entry cannot be removed or reloaded. **The guard is worse than "does not discriminate": it affirmatively selects the shipped component**, because LEAD-1 makes a healthy install log an `AttributeError` naming the exact function Step 4 says to create. Finder's claim 3 (dropped idempotence guard) **struck** — the doc says to keep the middle. Filed as an escalation on #578 rather than a parallel issue |
| E2 | Functional | `homeassistant/custom_sentences/en/gubbins.yaml:17`, `:18`, `:20` | Three shipped sentences shadow HA's built-in "where is <person>" | confirmed (**reshaped**) | #1440 | `wrong-data`. The finder's hand-built matcher was checked against HA's real pipeline line by line and then **replaced with the real conversation agent**: a real exposed `person`, the shipped YAML in the config dir, the real integration on a real bridge. A/B over all twelve phrasings: exactly 3 of 12 hijacked, the three named. **Version-gated, which the finder missed**: the built-in only grew `[<the>]` between intents 2026.5.5 and 2026.6.24 — HA's grammar grew into these sentences. `person` is not exposed to Assist by default, and without exposure there is no regression. The obvious in-file fix (`excludes_context`) was driven and **does not work** — the sentence binds only a wildcard. HA documents the priority rule nowhere |
| E3 | Prompt | `homeassistant/README.md:601-609` | The MQTT entity list omits the staleness sensor, and the closing sentence teaches the wrong signal | confirmed | #1441 | `degraded`, and the lead of the README bundle. Settled by running the **real** bridge against a hand-written minimal broker with discovery on: six retained device-level config topics, `binary_sensor/gubbins/snapshot_stale` among them, name "Snapshot stale". `grep -i stale homeassistant/README.md` returns **zero** hits in 810 lines, while `bridge/README.md` and the wiki both cover it. The list *claims* completeness ("with:", then "All of these") |
| E4 | Prompt | `extension/README.md:119-121`; `src/features/scraping/parsers/suppliers.ts:4-9` | The host grants are documented as supplier-only, and the named test pins a wider list | confirmed (**narrower**) | #1442 | `degraded`. Two of eleven grants are lookup hosts with no parser, and `host-permissions.test.ts:33` asserts equality against `ALL_EXTENSION_HOST_PERMISSIONS`; the per-supplier `toContain` at `:37` is a subset check. The source docstring makes the same claim and contradicts `ALL_EXTENSION_HOST_PERMISSIONS`'s own docstring further down — one claim, two places, one fix. **The `:16` / `storage` half is REJECTED**: that row names the three permissions driving the Amazon flow and never claims to enumerate the array |
| E5 | Prompt | `extension/README.md:3-7` | The README says "all three" capabilities; four ship | confirmed (**partly reshaped**) | #1443 | `degraded`. `grep -i "data.lookup\|wikidata\|open database\|category"` over the file returns no matches, while the flow has its own capability version, host grant, allow-list gate and consent UI — and the extension's own manifest description names four. **The architecture-table limb is REJECTED**: that table is per-file, not per-flow, and every file is listed. The `LEGACY_BUILD_PROTOCOL` sub-claim costs the reader nothing (`peerProtocolVersion` falls through to 1) and was dropped |
| E6 | Prompt | `README-HA.md:240` | The only diagnostic names a response the bridge never sends, and an expiry that does not exist | confirmed (**headline corrected**) | #1444 | `degraded`. Both curls run against a real bridge: `{"error":{"code":"unauthorized","message":"Unauthorised"}}` and the legacy `{"error":"Unauthorised"}`. `api_tokens` has no expiry column and resolution has no time predicate, so a token cannot expire by any mechanism. **"Cannot be followed" is struck** — the command runs fine in its escaped form; the escaping is #578's. Never true: `git log --all -S "Unauthorized" -- bridge/` returns zero commits. #1306 carries the same misquote in a different document |
| E7 | Prompt | `homeassistant/README.md:641-642` | "Home Assistant integrations aren't unit-tested in this repo" is stale | confirmed (half) | #1441 | `cosmetic`. The parenthetical "no HA test harness here" is **still true** — no pytest, no `conftest.py`, no Python test anywhere. The blanket claim is false: `ha-config-flow-strings.test.ts` passes 14/14 and CI runs it. Only that half is the defect |
| E8 | Prompt | `homeassistant/README.md:793-805` | The Files map omits `events.py` | confirmed | #1441 | `cosmetic`. Path-aware diff against `git ls-files`: 14 declared, 15 tracked, one missing, none phantom. It is load-bearing — `events.py` builds the very payload the README documents in step 8 |
| E9 | Mechanical | `src/lib/ha-config-flow-strings.test.ts:64-101` | The catalog drift test cannot fail for the `services` section | **rejected** | — | Both mutations run and both stay green, so the factual claim holds — but the test **claims exactly the coverage it has**: its filename, docstring, `describe` and its own "Two claims are checked" list are all scoped to the config flow. A third, adversarial mutation showed the whole-file assertion **does** fail on services drift. Under §2.1 this is a request for another test, not a test that cannot fail. There is no live gap either (6/6 services, 35/35 fields agree), and the remedy is already **#749** |
| E10 | Functional | `README-HA.md:41-44` | The guide tells the reader to paste a live token into `configuration.yaml` | **duplicate #578** | — | #578 names this three times and prescribes the fix. Two supporting claims also fail: HA's own documentation gives a *sharing* rationale rather than a security one, and "the wiki agrees" is false — `!secret` appears nowhere in `docs/wiki/`. Neither `SECURITY.md` nor `CLAUDE.md` reaches advice about a user's own machine |
| E11 | Prompt | `custom_components/gubbins/manifest.json:12` | The same claim as C10 | **rejected** (merged into C10) | — | Verified once, under C10 |
| E12 | Functional | `custom_components/gubbins/sensor.py:42-43`, `binary_sensor.py:41-87` | No `translation_key` anywhere, so nine entity names cannot be translated | **rejected** (feature gap) | — | The mechanism is genuinely unwired, proved harder than claimed: a **complete** `translations/de.json` with a full `entity:` block was shipped and every friendly name stayed English, because `_attr_name` short-circuits before the translation lookup and `_name_translation_key` is `None`. But §1 excludes a feature gap unless something claims otherwise, and nothing does — #244 scopes itself to the app's screens, `CLAUDE.md`'s i18n rule is about the typed `t()` seam, and every README and wiki mention says "the interface". A legitimate `enhancement`, not an audit finding |
| E13 | Prompt | `homeassistant/README.md:741-743` | The mDNS TXT list omits `server` | confirmed | #1441 | `cosmetic`. `buildTxtEntries` driven directly returns `server=gubbins-bridge` first and unconditionally. The step tells the reader what to compare real `avahi-browse` output against, so it fails its own purpose |
| E14 | Prompt | `homeassistant/README.md:154-156`; `SentencesStep.tsx:76-78` | "Anything after `{item}` is sent" is false for 2 of 9 sentences | confirmed (**second site found**) | #1445 | `cosmetic`. All nine driven through the real agent. The finder missed the duplicate in the shipped UI, and the YAML's own header states it **correctly** — so both prose copies drifted from it. A reader is genuinely misled: a phrasing written from the wrong model is **rejected outright**, not absorbed ("how many M3 bolts do I have in the shed" → no match) |
| E15 | Prompt | `homeassistant/README.md:181` | The documented `gubbins.search` result shape omits `locationId` | confirmed | #1441 | `cosmetic`. Checked at the API **and** through the real service under HA — nothing is stripped, so the finding does not change shape: seven keys documented as six. Narrower than "the one field an automation can act on": the next two lines document the derived `location_ids` and `placements[].location_id` |
| LEAD-1 | Mechanical | `custom_components/gubbins/intent.py` (the filename) vs HA `components/intent/__init__.py:89-90`, `:447-451` | HA treats the module as an intent **platform** and calls `async_setup_intents` on it, which it does not define | confirmed | #1439 | `cosmetic`. Found by the D3/D4/D5 verifier outside its batch, then independently reported by three more verifiers, and the mechanism confirmed by the lead directly in the installed HA 2025.1.4. A traceback in every user's log at every start; the intent still registers and answers. Second-order: it is what makes `README-HA.md`'s step-4 guard fire for a healthy install (E1) |

### Phase 15 — Prompt and guidance surface

Pinned SHA: `4b2bd5613f0cfd7d8fd0f258e9a15e0f01d3f6a2`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| P15-M1 | Prompt | `bridge/src/mcp/tools.ts:126`, `bridge/README.md:1204`, `bridge/src/query.ts:132` | `gubbins_search` advertises relevance ranking; the query orders alphabetically | confirmed | #1451 | `wrong-data`. Lead-verified personally. Driven on a 6-item snapshot: the default call returns 5 and drops the item literally named `Widget`; payload has no `total`/`hasMore`. **Three sites, not two** — `query.ts:132`'s docstring calls it "the same relevance search as `searchItems`", doubly wrong since `searchItems` is not one either. `searchByRelevance` exists and `grep -rn` finds no bridge caller; its own docstring names this exact failure |
| P15-M2 | Prompt | `bridge/src/mcp/tools.ts:444-448`, `:455`; `bridge/src/write.ts:326` | The check-in error names a `checkoutId` no read tool returns | confirmed (**second site**) | #1452 | `degraded`. 14 read-tool/include combinations swept: loan-ish keys `[]` in every one. Every workaround refuted, incl. `{quantity:1}` and a repeat call. **`bridge/README.md:1245-1246` claims the model "can correct itself"** — false for the case it names. The only escape the tool set offers (`adjust_quantity`, whose own description says "checks stock in") leaves 42 on the shelf and 5 still out on loan. HTTP has the id via the calendar UID (`openapi.yaml:3110`); MCP points nowhere |
| P15-M3 | Prompt | `bridge/src/mcp/tools.ts:221-222` | `gubbins_list_locations` promises its ids "filter further"; no location filter exists | confirmed (**reshaped, widened**) | #1453 | `degraded`. Nine spellings driven, all `parse.ok=true`, `isError:false`, zero results, while `M3` returns 2. **Must not file the finder's support**: the grammar's lack of a `location:` term is a recorded decision (`field-registries.test.ts:19-31`). The defect is the description. Widened to `category`, which behaves identically. The sibling `list_capabilities` names both syntax and tool; this one names neither |
| P15-M4 | Prompt | `bridge/src/spoken.ts:65` | The spoken sentence states the page size as the total | **duplicate #1363** | comment | Re-confirmed incidentally by the lead's own M-1 run |
| P15-M5 | Prompt | `bridge/src/mcp/tools.ts:395`, `:402` | `contactId`/`projectId` name ids no MCP tool supplies | **rejected** (feature gap) | — | Every mechanical fact reproduces. But the text claims lending to an existing project *works*, and it does. Three decisive findings: it is a whole-bridge gap (`ODATA_ENTITY_SETS` is items/locations/categories; no contacts or projects route anywhere); the wording is **shared** — `openapi.yaml:3139` is byte-identical on HTTP; and unlike M-2 no text claims recovery. Finder's asymmetry claim corrected: `tools.ts:197` is `get_item.id`, and the pointer is carried by tool *prose*, once, where a source exists |
| P15-M6 | Prompt | `bridge/src/mcp/dispatcher.ts:54` | MCP `serverInfo` hard-codes `1.0.0` | confirmed (**stronger**) | #1455 | `cosmetic`. Three grounds: the MCP spec types `serverInfo` as the *implementation's* version (the contract travels in `protocolVersion`); `bridge/README.md:2765` says "the bridge **has no version of its own**", false while this line exists; and `git show 6a6b0457 --stat -- bridge/src/mcp/` is **empty**, so #282 never reached MCP. Where the contract reading applies it is written down (`v1.ts:863`) and both numbers are returned |
| P15-M7 | Prompt | `bridge/README.md:1238`, `:1324` | The check-in rows omit `quantity` | confirmed (**widened**) | #1455 | `degraded`. All eleven tools diffed against a live `tools/list`: the only MCP mismatch. **Second site** — the HTTP twin at `:1324` omits it too, and the README never mentions partial returns at all. #662 touched `tools.ts`, `openapi.yaml`, `openapi.ts` and the wiki; `git show --stat -- bridge/README.md` is empty for both its commits |
| P15-M8 | Prompt | `bridge/src/mcp/tools.ts:172` | `gubbins_where_is` never states its default limit | **rejected** (subsumed) | comment on #1363 | Survey holds — one of six is silent. But the stated consequence survives the fix untouched: a model told "default 5" and handed 5 rows still cannot tell 5-of-5 from 5-of-7. That consequence is #1363's, filed on `where_is` specifically with identical evidence. `bridge/README.md:1211-1212` states the number for a human |
| P15-M9 | Prompt | `bridge/src/mcp/tools.ts:364` | `adjust_gauge`'s `delta` names no unit | **rejected as stated** | — | `include=gauge` and `fields=name,gauge` both return `unitOfMeasure`, so the finder's own doubt resolves against it. The proposed HTTP-inconsistency test **fails**: `openapi.yaml`'s adjust-gauge body is identically silent. House convention |
| P15-M9b | Functional | `bridge/src/mcp/tools.ts:356` | `adjust_gauge` promises to return the updated item; the result has no gauge | confirmed | #1454 | `degraded`. **Discovered by a verifier while disproving M-9.** `delta: -1000` on a 400 g reel: response is `{updated:true, item:{…}}` with no gauge block; stored value 0; history note "Gauge -400g (now 0g)". Only −400 of −1000 applied, and the caller cannot see the value, the clamp, or report either. Not a clamping complaint — `:355` discloses the clamp honestly. `ItemDetail` has no `gauge` and `v1.ts:740-747` accepts no projection on a write, so **the HTTP twin has the identical gap** |
| P15-M10 | Prompt | `docs/wiki/AI-Assistant-Query-MCP.md:9-10` | The tool-list pointer goes to the repo root | **rejected** | — | Facts hold, but the premise that it departs from a convention is **false — it is the convention**: all eight repo links in the wiki resolve to the root, five naming "the bridge README" in the same sentence, and `docs/wiki/` contains zero deep links. The root README links `bridge/README.md` five times, including in the section a reader arrives for. Register precedent C4-1/C4-4 folded an identical nit into #581 |
| P15-S1 | Prompt | `src/features/home-assistant/steps/SentencesStep.tsx:120-124` | The wizard claims the bridge handles plurals and the system locations | confirmed (**reshaped, merged**) | #1459 | `cosmetic`. The bare singular verb alone would have been **rejected** as taste; the defect is the app's own claim. **Three units found this independently** (S-1, H-3, S-4a). Sharper limb: "is in **In Transit**" — a doubled preposition from the module whose stated purpose is that the answer "reads naturally". Both are seeded system locations every vault has; Unassigned is the *default* (`create.ts:92`, `catalog-import.ts:897`). The guide the banner declares obsolete (`README-HA.md:98`) gets `pliers` right |
| P15-S2 | Prompt | `bridge/src/spoken.ts:87-90` | `prepositionFor` matches only the leading word | **rejected** | — | Documented three times as leading-word matching (`:15-16`, `:23-25`, `:86`) with `"in"` as the declared catch-all. The word-list gap is a feature gap. Rests on taste. #586 already quotes `prepositionFor` and `ON_PREFIXES` by name. Lead spot-checked the three docstrings |
| P15-S3 | Functional | `bridge/src/spoken.ts:59-62`, `:74-79` | The several-match frame lists a zero-stock item like a stocked one | **rejected** | comment on #1363 | Demonstration reproduces and the mechanism is right, but **the frame reports stock for no item at all**, so it makes no false stock claim. Four independent statements say it is location-only (`spoken.ts:12`, `:57`, `bridge/README.md:234`, `Home-Assistant-Integration.md:26-27`). Distinctness from #1333 verified and correct — but distinctness is not a defect. The "how many" routing carried to #1363 as a comment |
| P15-S4 | Prompt | `bridge/src/spoken.ts:43-46` | Items with no location are spoken as being in "Unassigned"; `:45` is unreachable | limb (a) confirmed → merged; limb (b) **rejected** | #1459 | (a) merged into P15-S1 as its second site. (b) all four paths to a null name are closed — FK is on, `restoreSnapshot` refuses a dangling `location_id`, an archived location still resolves, one production caller. With FKs *forced off* it still does not fire, because `item_stock` keeps `placements` non-empty. `query.ts:41-42` read whole is the defensive reading, not a contradiction |
| P15-S5 | Prompt | `bridge/src/spoken.ts:93-97`, `:64` | `listToProse` is ambiguous when a name contains a comma or " and " | **rejected** | — | Reproduces, and a literal comma in a location name is *deliberately supported* (`location-path.ts:49-75`). Fails on four counts: the listener has three independent cues (two numbers, the declared count, a total that reconciles one way); no consumer parses the string; no fix exists that does not degrade every ordinary answer; and **not one location name in the fixture, `wiki-screenshots.mjs` or the wiki contains a comma or " and "**. #586 already quotes `listToProse` |
| P15-S6 | Prompt | `bridge/src/spoken.ts:50`, `:70`, `:76`, `:44` | A blank location name makes the sentence trail off | confirmed (**writer found**) | #1477 | `degraded`. The finder could not find a writer; the verifier did — **`bridge/src/push.ts:245-274`** merges a caller-supplied snapshot and publishes pushed bytes verbatim on a first push. Schema has a ceiling `CHECK` only (`length('')=0` passes); `validateTables` checks section shape and never a column value. Second-order in-app writer: `assembly.ts:131`'s `||` fallback is not re-trimmed. Fourth site added (`:44`, whitespace only — `''` is falsy and takes the honest arm, `'   '` is not) |
| P15-H1 | Prompt | `README-HA.md:158`, `SentencesStep.tsx:207` | `[the]` is listed before `my`, so "where is my X" binds "my X" | confirmed (**widened**) | #1457 | `wrong-data`. **The decisive step was settled from HA source**: `async_recognize_sentence_trigger` at tag 2025.2.0 (`hacs.json`'s minimum) `break`s on the second result with no `text_chunks_matched` comparison; the intent path *does* rank. Identical at 2025.8.0, 2026.1.0, `dev`. Driven under hassil 3.12.0 **and** 2.2.3. Widened: the wizard prescribes the failing phrasing in three places, incl. `FinishStep.tsx:127` **inside its own troubleshooting bullet**. Citations corrected (`:175` not `:174`, `:207` not `:206`, `:132` not `:141`) |
| P15-H2 | Prompt | `README-HA.md:157-171`, `SentencesStep.tsx:207-216` | `[the]` collapses every pattern, swallowing "where is &lt;person&gt;" | confirmed | #1458 | `wrong-data`. `async_process:347-374` reaches intent recognition only when no trigger matched. Differs from #1440 on four axes, decisively on the **version gate**: #1440 needs `home-assistant-intents` ≥ 2026.6.24; this needs none. #1440's own suggested direction flags this branch as what it could not settle. **This was the §11 Phase 14 carry-over** (register `:2846`), now driven; `README-HA.md` is a third site the row does not name. README half conditional on #578, per E1's precedent; the wizard half unconditional |
| P15-H3 | Prompt | `SentencesStep.tsx:120-124` | Same as P15-S1 | confirmed (**merged**) | #1459 | Found independently by a third unit |
| P15-H4 | Prompt | `custom_components/gubbins/strings.json:71`, `:97`, `:199` | Three stock services omit the permission `check_out` names | **rejected** | — | The permission is real and was **driven** (a narrow custom role gets false on all three, true once `stock:write` is added). But the premise fails three ways: "only the user-facing catalog drops it" is **false** — `openapi.yaml:2824`, `const.py:42`, `services.yaml:49` and `homeassistant/README.md:41` all do too, five surfaces identically; where it *is* named it is a **contrast** (`openapi.yaml:3110` "Needs `checkouts:write` (not `stock:write`)"), presupposing the baseline; and no shipped role reaches the state |
| P15-H5 | Prompt | `custom_components/gubbins/services.yaml:11-12` | Names a Developer Tools control HA renamed in 2024.8 | **rejected** | — | The rename is real and dated. But a YAML parse shows every service node carries `keys=['fields']` only, so the comment is invisible to users, and it is not an agent-prompt surface either. **The invited widening is not there**: `"Call service"` occurs exactly once repo-wide, and `homeassistant/README.md` uses `action:` fourteen times and "→ Actions" five |
| P15-C1 | Prompt | `AGENTS.md:28-31` | All three of the parity test's advertised guarantees are false | confirmed | #1460 | `degraded`. Mutation-proved against the **real unmodified test**: deleting the worktree section, inverting it, adding a fifth rule, and fabricating an index row all stay green (18 passed); the red control goes red. Verifier added two more showing a clean asymmetry — deleting a `verbatim` section is RED, deleting an `adapted` one is GREEN. Narrow reading rejected: the map is *named* `REPRODUCED`, holds all four, and mutation B makes the page state the **opposite** of `CLAUDE.md` on the rule the page calls first-acting. No lint, CI step or hook covers `AGENTS.md`. Different cause from #957 |
| P15-C2 | Prompt | `AGENTS.md:10-11`, `:15` | The "complete list" drops the sub-agent rule and links nowhere | confirmed (**narrowed**) | #1460 | `cosmetic`. Diff and both greps reproduce. Narrowed because `AGENTS.md:3-6` does say "read [CLAUDE.md] before making changes", so a compliant reader reaches it — only the narrower `:10-11` claim is false. The `###` rider **rejected**: `CLAUDE.md:424` sits inside a section the index *does* link. **Live instance of C1's limb two** |
| P15-C3 | Prompt | `CLAUDE.md:175-177` | The i18n "scope reality" line is 14× stale | confirmed (**parenthetical only**) | comment on #60 | `cosmetic`. Numbers reproduce. **Premise rejected**: `grep -rl useT` counts a file with one call; per-file conversion is partial nearly everywhere, and Dashboard (9/22) and About (2/4) — the two named as converted — are themselves partial, so the sentence was loose when written. **The softening limb rejected outright**: the hard rule already says it applies to every catalog string. Held at cosmetic on P2-39's precedent |
| P15-C4 | Prompt | `CLAUDE.md:360-362` | The memory-note id `auto-review-skill` resolves to nothing | **rejected** (consequence) | — | Facts hold and are **wider** — all four cited ids fail the documented lookup, three alias-recoverable. Rejected because the SessionStart hook emits all 135 note titles into every session, each title-shaped enough to map at a glance; `CLAUDE.md:357-363` states the operative fact itself; and the hygiene reading fails (every example in `:88-90` is something whose disclosure *is* the harm). §10 P2-36 already treats the vault as out of scope. → §11 |
| P15-K1 | Prompt | `.claude/skills/verify/SKILL.md:66` | The worktree recipe omits `--strictPort` | confirmed (**widened**) | #1472 | `wrong-data`. `grep -rn strictPort` over configs, skills and scripts: **no match repo-wide**. Driven: port held → "trying another one" → bound 5301; with the flag → exit 1. Wrong-target shown directly by curling both ports. Widened: the recipe says "a port of your own" then hard-codes 5199 for every reader, and duplicates the flagless command at `vite.worktree.config.ts:19`. The incident is on record **twice**, the second (a wrong *checkout*, not a stale server) sharper than the one the finder cited |
| P15-K2 | Prompt | `.claude/skills/auto-review/SKILL.md:28-30`, `:35-39` | The skill's founding premise is false | confirmed (**reshaped**) | #1473 | `degraded`. The verifier **rejected** "the premise is provably false as a general fact" and docked the finder: `:41-46` claims only that the *rubric* is adapted from upstream. The invocation limb is **unverifiable** here (the bundled definition is on disk nowhere readable; `/code-review` was not invoked, per instruction). What is confirmed is the harness-independent **contradiction**: `:35-39` defers to a maintainer gate `CLAUDE.md:359-368` abolished ("no approval gate to wait on") |
| P15-K3 | Prompt | `.claude/skills/auto-review/SKILL.md:48-50` | The re-sync protect-list omits the effort machinery | confirmed (**narrowly**) | #1473 | `degraded`, latent. Full enumeration done; two local sections *do* have upstream counterparts and are correctly unclaimed. "Two" is wrong — the effort machinery is a third, over three sites. **Broader version rejected**: "fold in any changes" is additive and frontmatter would survive. Confirmed narrowly: the risk is where local text *conflicts* with upstream, at `:88` and `:92`. Upstream fan-out is at `:30` (finder said `:34`; the file is 109 lines). Filed with K2 — one 28-line fix site |
| P15-K4 | Prompt | `CLAUDE.md:357` vs `:359-360` | Two diff scopes named in adjacent sentences | **rejected** | — | The divergence is real and was demonstrated in a scratch repo with `main` advanced. But **inert**: `:357-358` says run the skill *via the Skill tool*, so the agent builds no diff command; and step 5 commits before every run, so both phrasings select the same delta. The one residual misreading errs toward committing first, which the workflow already mandates |
| P15-K5 | Prompt | `.github/ISSUE_TEMPLATE/bug_report.yml:34-38` | No browser-extension option | **rejected** | — | Every premise checks out; the **consequence is falsified**. `gh issue list --search "\"Where did it happen\""` → `[]`; all 1000 issues are maintainer/agent-authored in the §5 format. #1449, #1450 and #1423 *are* extension bugs and none touches the dropdown. Field is `required: false`; blank issues are enabled. **P0-9 rejected the sibling line in this same file on this same reasoning.** The label limb restates the register's own guidance at `:273` |
| P15-K6 | Prompt | `.github/pull_request_template.md:9-14` | The checklist covers 2 of 6 CI gates | **rejected** (both limbs) | — | CI and hook facts confirmed, incl. that **no local hook covers `wiki:check` or the lockfile**. Limb 1 measures the checklist against a claim it never makes — `:7` is a bare "## Checklist" of project *conventions*. Limb 2 rejected on a documented decision: 29 PRs, 28 Dependabot, 1 by the maintainer **12 days before the template landed**, and `CONTRIBUTING.md:7-9`/`:78-90` deliberately retains it for the external-fork path. → §11 |
| P15-K7 | Prompt | `.github/pull_request_template.md:1-14` | No agent attribution trailer | **rejected** | — | Three grounds, and **the invited widening argues the opposite way**: the issue templates carry no trailer either, and that is *correct* — they target human reporters, so pre-populating "opened by an agent" would be a **false disclosure**. The surface agents actually use already carries it (#1449, #1450, #1423), appended from the rule with no template prompting it |
| P15-I1 | Prompt | `catalogs/en.json:1733`, `de.json:1733` | The webhook coverage note says locations cannot raise a webhook | **duplicate #1313** | comment | The audit already filed it as U10-2 → #1313. Re-derived independently and holds in full. One addition carried as a comment: `docs/wiki/Webhooks.md:90-110` documents the correct behaviour, so the app string and the wiki disagree |
| P15-I2 | Prompt | `catalogs/en.json:302`, `de.json:302` | The Contacts tooltip describes the Suppliers screen | confirmed (**widened**) | #1464 | `wrong-data`. Live render driven; keyed by route so it cannot appear elsewhere. Three attacks fail: the schema **deliberately** keeps contacts and suppliers apart (`v1-initial.ts:1972-1975`); `grep -rniE "supplier\|pricing\|price"` over `ContactsScreen.tsx` returns zero; and **it was never true** — `6a4173ee` predates `src/features/suppliers/` by 18 days. German carries it too. Adjacent, recorded: `dashboard.nav.tooltip.alerts` promises a "budget" lane `AlertKind` does not have |
| P15-I3 | Prompt | `catalogs/en.json:36` | The pagination hint names 3 of the 8 lists it drives | confirmed (limb a); limb b **rejected** | #1476 | `cosmetic`. (a) Eight consumers verified; exactly right when written (`bfb58952`, #20 names those three), drifted three times (#84, #384, #149). Five truncation messages tell the user to enable it for lists the hint excludes; the wiki is correct throughout. (b) **rejected** — a control named "Paginate list" *does* exist (`InventoryScreen.tsx:986`), and driving the real Settings dialog shows the search finds the row under both spellings (`text-terms.ts` matches substrings). The verifier's two tests were written to assert the finder's claim and **failed** |
| P15-I4 | Prompt | `catalogs/de.json:2058-2059` | The German Bookings permission subject diverges from the nav name | confirmed (**narrowed, widened**) | #1475 | `degraded`, not wrong-data. Uniqueness verified mechanically over all 24 subjects: 8 pairs, `users` diverges deliberately in *both* languages, bookings is the only German-only divergence. **Premise rejected**: *Reservierungen* is not another feature's name — the catalog already uses it for bookings elsewhere. What survives is narrower: the catalog *chose* **Buchungen** and the grid picks the other. Widened to a cluster — `:2073` misnames **two** screens in one sentence, `:2044` the same |
| P15-I5 | Prompt | `catalogs/de.json:1100-1101` | A supplier part count is labelled *Artikel* | confirmed (**narrowed**) | #1475 | `degraded`, not wrong-data. Driven on a live DB: one item, three order codes → "3 Artikel", byte-identical to the tag-usage string for a real item count. Four German terms for one concept, two reachable from the same screen. The schema **blesses** the multi-row shape (`v1-initial.ts:2031-2032`). `wrong-data` fails because in the common case the label is accidentally correct |
| P15-I6 | Prompt | `catalogs/de.json:410` vs `:414` | Two German nouns for "item" in one control | **rejected** | — | Three grounds: the co-occurrence is weaker than claimed (the hint is inside an `InfoHint` tooltip, and only one warning can show — `BarcodeField.tsx:82-91` is a ternary chain); the asymmetry premise is **false** (English itself uses asset/goods/item, sometimes in one sentence, and all 29 *Gegenstand* values translate asset/goods); and no consequence — the app has one entity these could denote |
| P15-I7 | Prompt | `catalogs/de.json:974` vs `:849` | Two German names for the Appearance tab on one screen | **rejected** | comment on #232 | Split is **wider** than reported (a third string at `:860`; 2–2 with no winner). But `SettingsDialog.tsx:264` hard-codes `label: 'Appearance'`, so a German user following *either* string finds neither word on screen. The wayfinding failure is #232's. Register precedent for this shape is rejection with a scope note (U8-9, U7-10a, U6-10) |
| P15-I8 | Prompt | `catalogs/en.json:233` | The singular live-region announcement disagrees with its verb | confirmed (**second site**) | #1476 | `cosmetic`. Resolved through the real `makeTranslator`: "1 item need attention"; German correct in both variants. All 89 `.one` keys enumerated — every other one with a verb agrees, and `alerts.announce.count.one` is the same shape done right. **Second site**: `ReportsScreen.tsx:243` hand-builds the identical sentence, and `lib/plural.ts`'s docstring names verb agreement as the hazard it leaves to call sites. Matches the register's own discriminator — U2-12/#1119 and U7-6/#1120 survived because the English was wrong |
| P15-I9 | Prompt | `catalogs/en.json:1179` | The only value using straight ASCII quotes | **rejected** | — | Uniqueness verified. Style preference on four grounds, the first decisive: **`PHASE_HANDOVER.md:802` records a waiver of this exact class** ("Waived NITs: P59 — curly-vs-straight apostrophe in the InfoHint copy"). No lint rule or house-style line exists; the twin is **test-pinned** (`event-catalogue.test.ts:54-58`); no rendering consequence |
| P15-O1 | Prompt | `bridge/src/openapi.ts:970-979` | `info.description` calls the API read-only and says every endpoint answers HEAD | confirmed | #1469 | `degraded`. Every documented path swept: 35 GET, 7 POST, **zero** GET mismatches, all 7 POST-only paths 404 on HEAD. Both claims fail on the same seven, one cause. **The default-posture defence fails decisively** — `openapiDocument` is a module-level const with no config import, served verbatim, so with writes *off* it still declares 7 POSTs and calls itself read-only. Contradicted three times in the same file (`:400`, and both tags). **False the day it was written** — 3 POSTs existed a month earlier |
| P15-O2 | Prompt | `bridge/src/openapi.ts:1836`, `:1846`, `:2365` | The snapshot example uses a `formatVersion` the endpoint refuses | confirmed (**widened**) | #1471 | `cosmetic` → `degraded`. Driven: the documented example → 422; `formatVersion: 1` → 200. The "forward-looking" defence fails on `push.ts:92`'s own docstring. **Widened**: the *response* example is unreachable **by construction** — `push.ts:64` sources it from the snapshot that survived the guard. Never true in any of 20 revisions of `types.ts`. Outside #1351: its ajv run reports 22 media-type examples clean, and `3` validates against `{type:'integer'}` regardless |
| P15-O3 | Prompt | `bridge/src/api/odata-metadata.ts:84` | `operationalMetadata` is declared `Edm.String`, emitted as an object | confirmed | #1470 | `degraded`. Sweep re-run independently: **one mismatch in 45**. Both counter-arguments tested and fail — the file documents every *other* non-obvious type choice beside it (`:47`, `:88-91` citing the spec by section, `:93-94`) and has no comment at `:84`; and while `Edm.Untyped` is indeed unavailable at the declared 4.0, **CSDL 4.0 supports `<ComplexType OpenType="true"/>`** and the file already declares six complex types. `odata-metadata.test.ts:56` matches `Type="[^"]+"` — a wildcard. Client limb honestly marked **reasoned, not measured** |
| P15-O4 | Prompt | `bridge/src/api/odata-metadata.ts:95`, `:257-259` | `tags` is advertised filterable; no OData-legal filter parses | confirmed | #1470 | `degraded`. **The finder's sweep was wrong and the verifier corrected it** — six of 26 failed, but five only on arbitrary *values* and all answer 200 with valid ones. The conclusion survives: for `tags` alone, no legal spelling parses (a syntax rejection), and the two that work are not legal OData against a collection. The comment at `odata-filter.ts:113-117` covers only the **parser's alias table**. Mechanism sharper than framed: a plural alias existing for the parser is promoted into a machine-readable capability claim by the derivation. Kept separate from #1353 (scalar vs collection, and a working spelling exists there) |
| P15-O5 | Prompt | `bridge/src/openapi.ts:245-246`, `:150-156`, `:168-172` | Three vocabularies are hand-typed in a file that says none are | confirmed (**widened**) | #1468 | `degraded`. **Third site found** — `locationIncludeParam` at `:168-172`. Demonstrated exactly as briefed: a ninth sort field and seventh include group added in a throwaway worktree leave `type-check` clean and **79 files / 1191 bridge tests green** while the served document goes stale. The contrast that proves it: the generated CSDL **corrected itself** on the same bridge, and the app's own sort drift test **fired**. Genuinely the 7th–9th — #250's guard states its scope as projectable and searchable fields; #1350 named the CSV list as the sixth |
| P15-O6 | Prompt | `bridge/src/openapi.ts:1861-1868`, `:1903`, `:1928` | Three list operations serve `$top`/`$skip` and declare neither | **rejected** | comment on #1351 | Facts confirmed and driven. The suggested defence is dead ( `/api/v1/items` *does* declare both), so it is an inconsistency — but no integrator suffers: `limit`/`offset` remain declared and are functionally identical aliases. The class is #1351's opening frame verbatim, and one of the three sites is a parameter array #1351 limb (a) already asks to change |
| P15-O7 | Prompt | `bridge/src/openapi.ts:2333`, `:2338` | The `ApiIndex.bridge` examples are stale | confirmed (**widened**) | #1465 | `degraded`. Live `0.32.0`/`8` against published `0.3.0`/`5`. The "illustrative" defence fails on the prose beside them (`:2331`, "a client that knows its own can spot a bridge left behind") and on `d121e461`'s own message ("now use a number the app can actually report"). **Widened to six sites**, all planted by that one commit; `version.ts:28` (finder said `:27`). Filed with P15-R1 — same commit, same literal |
| P15-R1 | Prompt | `README.md:23` | The status line says v0.3.0; the app is 0.32.0 | confirmed | #1465 | `cosmetic` alone, `degraded` merged. The milestone-label defence fails: `d121e461` changed this exact line from **v0.1.1**, in the commit that set `package.json` to 0.3.0, saying "package.json is the single source of truth". Finder corrected — **two** release bumps since, not 29 (that is the numeric distance) |
| P15-R2 | Prompt | `README.md:28` | "eleven types" of custom field; there are twelve | confirmed | #1466 | `cosmetic`. Counted independently. `COLOUR` **is** user-selectable — `CategoryManagerDialog.tsx:1775` feeds the whole array in unfiltered. `cc861339` added it *and* updated the wiki page; the README count was the one surface missed |
| P15-R3 | Prompt | `CONTRIBUTING.md:75-76` | Asserts a build-vs-test Node split that no longer exists | **rejected** | — | The drift is real; the consequence is not. **No `.npmrc` exists anywhere**, so `engines` is advisory — and `tests.yml:182-184` says so in the repository's own words, with a `bridge-min-node` job succeeding on Node 22.18.0 at the pinned SHA. So a contributor on 22 who believes the build works believes something **true**. The claim was also true against a README that was itself already wrong. Family is open as **#262** |
| P15-R4 | Prompt | `docs/modular-ui-plan.md:3` | A fully shipped plan reads "phases not yet started" | confirmed | #1467 | `degraded`. Shipping traced **phase by phase**; all nine landed in `a950f158` on the **same day** the plan was written, and the registry has grown past it (32 ids vs 22, 8 presets vs 5). Branch and worktree gone; `/code-review high` uncallable; the temp-config recipe superseded. `docs-todo-status.test.ts:44-54`'s `collect()` **never opens `docs/`**. Ruled against both #584 (banner *position*, inside `docs/todo`) and #957 limb 2 (recursion) — neither reaches a file the test never opens. **Cited as live design reference by seven production code comments** |
| P15-R5 | Prompt | `SECURITY.md:22` | The affected-area list omits the browser extension | **rejected** | — | Closest call in its unit. Three grounds: **there is nothing to misfile into** (a single free-text advisory destination, no picklist, no label, no queue); the line sits under "What to include" — content guidance, not scope, and already omits four other surfaces; and `:23` already asks for the runtime. Per `README.md:97` the extension is not published to a store. → §11 |
| P15-P1 | Prompt | `docs/todo/dashboard-card-customisation_2026-07-04.md:3`, `:171-173` | The banner and "start here" section both name completed tasks | confirmed (**narrowed**) | #1474 | `cosmetic`. E1 shipped **14 days before** the banner was written. **The finder's consequence was disproved** — the doc's own preamble means a session's first act is to read the task entry, and `:138` opens with "✅ Done". What survives is better evidenced: the **sibling plan** in the same family, same template, *was* kept current across two ships. Citations corrected (`:138` not `:150`; `card-fields-render.ts` **does not exist**) |
| P15-P2 | Prompt | `docs/todo/wiki_2026-07-11.md:351-354` | The only forward instruction names nine pages that exist | **duplicate #271** | comment | Facts hold and the structural question resolves in the finder's favour (§5 is a maintained tracker — Passes 1 and 2 carry `(DONE)` markers). But #271 limb (a) names the same document, sentence and argument; the line moved only because the doc grew |
| P15-P3 | Prompt | `docs/todo/wiki_2026-07-11.md:6` | The banner states 24 screenshots; there are 44 | **duplicate #271** | comment | `git log -S` dates "24" to `379ddb16`, true when written. #271 limb (b) already files it as "claims 24; holds 28". Drifting further is the same claim going more stale. Carried as a comment |
| P15-P4 | Prompt | `docs/todo/ecosystem-integrations_2026-07-03.md:273-276` | A reference doc's verdict declines a shipped feature | confirmed | #1456 | `degraded`. **Timing to the minute**: the banner landed 12:27, the MCP write tools 20:35, the **same day**, and `git log` on the doc returns exactly two commits. Ruled **live**, not a record: D2 is two imperatives plus a present-tense "the existing decision", with no as-of date, under a banner saying the file is "kept for its verdicts". The sibling REFERENCE doc does exactly what this one omits. **Consequence is the audit's own machinery** — §4.1 names these verdicts as decision sources and §6.7 forbids re-filing them. `:270`'s "stay read-only" is a security-shaped claim five `mutates:true` tools falsify |
| P15-P5 | Prompt | `docs/todo/weak-item-archetypes_2026-07-31.md:430-432` | W9 calls a shipped half "more urgently" | **rejected** | — | Both halves verified (expiry shipped, #684 closed; `stock_batches` genuinely lacks the columns). But **the pointer self-corrects**: W9 cites `(§5)`, and `:1141` — the first line there — says "✅ Fixed". A reader must resolve the reference to know what the phrase means. One-hop explicit cross-reference, so distance is irrelevant. "More urgently" is stale prose with no reachable consequence |
| P15-P6 | Prompt | `docs/todo/volumetric-locations_2026-07-24.md:323` | Links a file deleted the same day | confirmed (**narrowed, widened**) | #1474 | `cosmetic`. Deleted at 14:28 by the same issue whose plan was written at 09:30. The name survives repo-wide in exactly one place. **The finder's supporting reasoning rejected** — §5 item 7 is the printed label, and none of items 2–7 adds a location field with an `InfoHint`. **Widening the finder missed: 58 broken relative links across `docs/todo/**`, 56 in two archived docs**, caused by the archive move changing depth; `README.md:47-49` warns about *inbound* links only |
| P15-P7 | Prompt | `docs/todo/custom-field-types_2026-08-22.md:16` | "the 72 entries"; there are 84 | confirmed (**narrowed**) | #1474 | `degraded`. All three counts verified, and the `Grade` claim misses a **fifth** preset that was already wrong on the day. **Ruling: the record stands; the missing thing is the caveat** — and the same commit gave the *sibling* doc exactly that caveat while leaving this one. This is the only one of three siblings pinning **nothing** — no SHA, no date, no as-of. The warrant is load-bearing: C1's table is prefaced with an exhaustiveness claim, and four never-surveyed presets have that shape |
| P15-R6 | Prompt | `src/features/settings/theme-registry.ts:331` | The docstring says "the 14 preset `ACCENTS`"; the array holds 16 | confirmed | #1478 | `cosmetic`. Lead-verified personally. `:47` of the same file says 16, and `README.md:115` says sixteen — this one comment is the outlier. Reported by unit 9 as outside its own file scope; verified by the lead rather than dropped |
| P15-R7 | Prompt | `src/state/stores/usePreferencesStore.ts:503` | The docstring names the palette's shortcut as Cmd/Ctrl-K; it is `Ctrl+/` | confirmed | #1478 | `cosmetic`. Lead-verified personally. `hotkeys.ts:283` and `:772` both give `Ctrl+/`, `:24` calls it "the long-standing Cmd+/ shortcut", and `README.md:61` is right. `grep -rn "Ctrl+K\|Cmd+K\|Meta+K" src` excluding tests returns **nothing** — no such binding exists. Same provenance as P15-R6 |
| P15-D1 | Prompt | `docs/dev/google-drive-sync.md:12-13`, `:87` | Says the Drive option is hidden; it is disabled | confirmed (**widened**) | #1462 | `cosmetic`, `degraded` for the wiki limb. Component test written and run — the **first** such assertion in the repository. **Never true**: `git log -S 'driveConfigured &&'` returns **no commits ever**. **Fifth site the finder missed, and the sharpest: `docs/wiki/Cloud-Sync.md:23-25`** tells a user "if you don't see it, that build didn't include one" — a diagnostic that can never fire. #278 is where the wording originates, but decided the variable, not the wording |
| P15-D2 | Prompt | `docs/dev/google-drive-sync.md:116-117` | Claims a headless-smoke assertion that does not exist | confirmed (**narrowed**) | #1462 | `cosmetic`. **No test anywhere** asserts it — the whole test surface was searched, and the three `src/` hits are mocks forcing `false` that never assert on the button. So the "wrong harness" narrowing does not apply. Consequence: `disabled={!driveConfigured}` can be deleted and every check stays green |
| P15-D3 | Prompt | `docs/dev/deferred-features.md:1914-1934` | The Reports i18n deferral describes a state the code left behind | confirmed (**re-based**) | #1463 | `cosmetic`, load-bearing. Ruled **live** — the only section with no phase number and no "agreed &lt;date&gt;", closing with a forward target. **Differs from the rejected C-3 because here the census is the sole stated ground for the scheduling decision.** Strongest limb: `:1922-1926`'s rationale warns against exactly the mixed state that now exists, eighty lines apart in one file. Honest when written (2026-07-18); overtaken 2026-07-31 by #685 |
| P15-D4 | Prompt | `docs/dev/deferred-features.md` ×7 | Seven open entries for work recorded as done | **rejected** → folded | #1461 | Dated per-phase **records**, each opening "re-scheduled, not dropped". 146 open boxes carry only ~50 distinct titles; one line appears **21 times verbatim**. The live index at `:68` is correct. Symptom of D7 |
| P15-D5 | Prompt | `docs/dev/deferred-features.md:1130` vs `:1145` | Open and ticked 15 lines apart | **rejected** → folded | #1461 | Not a contradiction — two different phases' records (Phase 38's deferral and Phase 39's). Inconsistent retro-ticking inside a dated log is a maintenance-style issue in a record. Symptom of D7 |
| P15-D6 | Prompt | `docs/dev/deferred-features.md:1553-1554` | A Backlog item whose substance shipped | **rejected** → folded | #1461 | Record. **And the finder had the wrong half** — its evidence is die-cut roll sizes, not label stock; but the *Avery* half shipped too via #333, so the item is 100% obsolete rather than 50%. Symptom of D7 |
| P15-D7 | Prompt | `docs/dev/deferred-features.md:6-7` | The roadmap no longer indexes the document it governs | confirmed | #1461 | `degraded`. Ruled **live** — a present-tense banner above every dated section, and `README.md:23` points readers at it. Counts recomputed: 146 open, **zero** carrying `→ Phase N`, 114 `→ Backlog`; roadmap ends at 58, outcomes run to 83. **"→ Backlog" is not a substitute** — the row names six topics while at least 13 distinct ones appear nowhere in it. Absorbs D4, D5, D6: one document-level repair, not 15 line edits |
| P15-D8 | Prompt | `docs/dev/deferred-features.md:1424-1425`, `:1443` | "No god-object remains" on figures ~3× out | **rejected** | — | Record, and **demonstrably true when written**: `SettingsScreen.tsx` was deleted **four days after** the sweep recorded it at 497 lines. Exactly the shape `CLAUDE.md` protects. The `PHASE_HANDOVER.md` repetition sits in its own dated COMPLETE block. Drift measured for the record: `reconcile.ts` 730→2260, largest today `precip-engine.ts` at 3446 |
| P15-D9 | Prompt | `docs/dev/PHASE_HANDOVER.md:639-670` | §1's "binding" card carries three false claims | **rejected** | — | §1 is **not** an exception to U2-6. Four grounds: its ancestor heading is a dated COMPLETE record; its own cells scope themselves ("unchanged by Wave 1"); **`_specification.md:20` makes §1 a per-record artefact by construction** ("must be restated in every PHASE_HANDOVER.md"); and git dates each claim as post-hoc drift — the providers claim was written 12:42 and the third adapter landed 21:44 the **same day** |
| P15-D10 | Prompt | `docs/dev/PHASE_HANDOVER.md:673-677`, `:620-622` | §2 asserts a registry Phase 69 deleted | **rejected** | — | Record. `:615` is "orchestration notes **the Phase-62 agent** should know" and scopes itself in the same sentence; `:673`'s heading says "**snapshot**". Both inside the same COMPLETE block. The file carries its own correction *above* §2 in a newest-first document (`:444-461`, `:100-101`), and the live convention lives where it binds — `src/db/migrations/index.ts:1-13`. → §11: the file has no preamble saying it is a newest-first stack |
| P15-D11 | Prompt | three `docs/dev` files; `google-drive-sync.md:17` | The cited spec is unnamed and superseded | (a) **rejected**; (b) confirmed; (c) **new** | #1462, #1466 | (a) The spec **is** named, at `README.md:355`, and all targets resolve. (b) `cosmetic` — the clause is conditional ("before Phase 7") and is rendered unconditional; but the architecture **honours** it (no Google SDK ever; `google-drive-api.ts` is fetch-based) so only the citation is bad. (c) **New, stronger, handed to the lead and verified personally**: `README.md:355` calls a document banner-marked "SUPERSEDED … NOT a description of current behaviour" the "absolute source of truth" |

### Phase 16 — Wiki parity

Pinned SHA: `48554edeaf159eda390352e805fc7eaea74a2184`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| P16-L1 | Prompt | `docs/wiki/` ×14 pages | 33 `[[…]]` links render dead or as raw brackets, and `wiki:check` passes them all | confirmed | #1479 | `degraded`. 16 line-spanning links render as literal text; 17 anchor-only links render `class="internal absent"`. Verified against the **live published wiki**: literal `[[` counts in the rendered HTML match the source page-for-page. `wiki-check.mjs:38` matches per line; `:58` strips the fragment and `:85` then skips the empty target. `wiki-table-links.test.ts:27` repeats the per-line regex, and its own header records that "three pages shipped 31 broken links … before anyone read the rendered output" |
| P16-L2 | Functional | 6 images, 7 pages | Committed screenshots predate the screens; each omits a control the page names | confirmed | #1480 | `degraded`. `sync.png` (no Shared settings), `modules.png` (no Users/Suppliers/Webhooks rows), `webhooks.png` (older coverage note), `search-quick.png` (no bookmark button), `inventory-workspace.png` (Card view with no photos), `contacts.png` (no Export buttons). All 44 committed images regenerated from the committed script and compared pixel-wise; 3 byte-identical; every seeded-data difference set aside |
| P16-L3 | Functional | `Appearance-and-Theming.md:9`, `:20` | Both Appearance screenshots predate the settings search box | confirmed | comment on #269 | #269's own evidence had itself gone stale — the committed image **does** show the Keyboard shortcuts tab it says is missing. Corrected evidence left as a comment rather than a duplicate issue |
| P16-L4 | Prompt | `docs/wiki/` house style | 12 pages lack a "Where to find it" line | **rejected** | — | All twelve are concept or reference pages (Home, Introduction, FAQ, Glossary, Privacy, How-Your-Data-Is-Stored, Installing, Running-the-Bridge, Bridge-Overview, AI-Assistant-Query-MCP, Webhooks-MQTT-and-iCal, First-Run). §1 excludes style preference with no named consequence |
| P16-L5 | Prompt | `docs/wiki/*.md` | Some external URL is dead | **rejected** (verified negative) | — | Only 4 real external URLs exist and all return 200: `docs.docker.com/get-started/`, the repo, the issue tracker, `bootblock.github.io/Gubbins/` |
| P16-L6 | Prompt | `docs/todo/wiki_2026-07-11.md` §2 vs disk | The page map has drifted from the pages that exist | **rejected** (verified negative) | — | Exact match: 95 backticked names + Home = 96 content pages; only `_Sidebar`/`_Footer` are unnamed, correctly so. Every content page appears in `_Sidebar.md`; no orphans |
| P16-L7 | Prompt | `docs/wiki/*.md` | An image is missing alt text | **rejected** (verified negative) | — | Zero empty alts across 59 image references to 44 files; no image path outside `images/` |
| P16-L8 | Prompt | this plan, §9 Phase 16 | "All 99 pages under `docs/wiki/`" | **rejected** | — | There are 98 `.md` files (96 content + `_Sidebar` + `_Footer`). `ls docs/wiki` returns 99 entries because `images/` is a directory. A counting artefact in this plan, not a wiki defect; recorded in §13 |
| P16-W1 | Prompt | `Locations-and-Stock.md`, `Inventory-Views.md` | Selecting a location shows everything nested beneath it | confirmed | #1481 | `wrong-data`. The filter is direct-only, so a parent location reads as empty while its children hold stock |
| P16-W2 | Prompt | `Installing-Gubbins.md` | Other tabs keep working through an update; a storage prompt appears | confirmed | #1482 | `degraded`. Driven in the browser. A second mention of the unsaved-draft guarantee was left as a comment on #1073 rather than a duplicate |
| P16-W3 | Prompt | `Quick-Start.md` | Your region is guessed from your device | confirmed | #1483 | `degraded`. Only the currency is guessed, so dates stay British on every device |
| P16-W4 | Prompt | `Home.md`, `Introduction.md` | Any page can be hidden; the tour's area list is complete | confirmed | #1484 | `degraded`. Four pages are always on; the tour lists Search as an area and omits two real ones |
| P16-W5 | Prompt | `Self-Hosting.md` | The default setup listens on localhost only | confirmed | #1485 | `degraded`. The page's own `docker run`, 25 lines earlier, publishes on every interface |
| P16-W6 | Prompt | `Webhooks.md` | Six of seven location events, three of four payload presets, and a preview the default mode does not give | confirmed | #1486 | `degraded` |
| P16-W7 | Prompt | `Running-the-Bridge.md` | A raw `.sqlite` source is an equal option | confirmed | #1487 | `unusable`. Write-back and push, both documented on the page, answer a bare 404 against that source |
| P16-W8 | Prompt | 5 links across the wiki | Cloud sync explains the snapshot file the bridge reads | confirmed | #1488 | `degraded`. That page never mentions the snapshot file |
| P16-W9 | Prompt | `AI-Assistant-Query-MCP.md` | Assistant writes are logged as if you made them | confirmed | #1489 | `degraded`. The same page says sixteen lines later that they are logged against the system |
| P16-W10 | Prompt | `Tags-and-Attachments.md` | A menu path for card fields; a shortage draws on a substitute | confirmed | #1490 | `unusable`. The menu path does not exist and nothing reads the substitute |
| P16-W11 | Prompt | `Locations-and-Stock.md` | Icon and row buttons on the wrong surfaces; usable volume follows your display unit | confirmed | #1491 | `degraded` |
| P16-W12 | Prompt | `Items.md` | Removing an item is called Archiving; a name is the only required field | confirmed | #1492 | `degraded`. A consumable needs three |
| P16-W13 | Prompt | `Location-Photos-and-Regions.md` | Typed position and size fields, and an Erase photos option | confirmed | #1493 | `degraded`. None of the three exists |
| P16-W14 | Prompt | `Kits-and-Assemblies.md` + 2 siblings | Disassemble is the exact inverse of assemble | confirmed | #1494 | `degraded`. It differs three ways; two sibling pages each carry a further wrong claim |
| P16-W15 | Prompt | bridge and MQTT pages | The wiki names the bridge's settings | confirmed (coverage) | #1495 | `unusable`. Two of 35 `GUBBINS_*` settings are named; all nine `GUBBINS_BRIDGE_MQTT*` are absent, so the MQTT page describes what is published and never how to switch it on |
| P16-W16 | Prompt | `Modular-UI.md` | Start from a preset | confirmed (coverage) | #1496 | `degraded`. Seven of the eight preset names appear on zero pages |
| P16-W17 | Prompt | `Privacy.md` | The table of everything that moves data is complete | confirmed | #1497 | `degraded`. It omits the category lookup, which sends an item's name to Wikidata |
| P16-W18 | Prompt | `FAQ-and-Troubleshooting.md` | A quoted clock message; the correction threshold; the two restore paths | confirmed | #1498 | `degraded`. The message is never shown, the threshold is 150× too high, and each restore path is described as the other |
| P16-W19 | Prompt | `Keyboard-Shortcuts.md` | Shortcuts travel only in a backup | confirmed | #1499 | `degraded`. The group travels over cloud sync |
| P16-W20 | Prompt | `Diagnostics.md` | The report states your colour scheme | confirmed | #1500 | `wrong-data`. It reads the OS query alone, so a light-in-dark user's report says Dark |
| P16-W21 | Prompt | `Achievements.md` | Type a screen name into the palette; the calculator table | confirmed | #1501 | `cosmetic`. The `>` prefix is omitted and the table omits the divide sign |
| P16-W22 | Prompt | `Insurance-Schedule.md` | The schedule prints the serial number and values every asset | confirmed | #1502 | `wrong-data`. It prints an instance index, values unpriced assets at zero, and drops the notice that would say so |
| P16-W23 | Prompt | `Maintenance-and-Servicing.md` | Time-based schedules are set in months | confirmed | #1503 | `wrong-data`. The field is days, so an annual test becomes a twelve-day one |
| P16-W24 | Prompt | `Test-and-Calibration-Records.md` | Two result options; a Modular UI switch | confirmed | #1504 | `wrong-data`. Neither result option exists and there is no such switch |
| P16-W25 | Prompt | `Cycle-Counts-and-Stock-Takes.md` | A count sheet marks a loaned unit | confirmed | #1505 | `wrong-data`. Nothing marks it, and marking it Missing retires the item while the loan stays open |
| P16-W26 | Prompt | 3 lifecycle pages | Tab pointers, a capability gate, a defined term | confirmed | #1506 | `degraded`. Wrong tab, no gate note, and a page where the term never appears |
| P16-W27 | Prompt | `Condition-Grading.md` | The Condition help text suggests New, Used and Damaged | confirmed | #1507 | `cosmetic`. None is one of the four grades the field accepts |
| P16-W28 | Prompt | `Reorder-and-Shopping-List.md` | The reorder quantity is a target to buy back up to | confirmed | #1508 | `degraded`. It overrides the shortfall outright |
| P16-W29 | Prompt | `Budgets.md` | Spend is manual only | confirmed | #1509 | `degraded`. Received BOM parts are added automatically, and five shipped parts of the feature are absent |
| P16-W30 | Prompt | `Projects-and-BOM.md` | The costing toggle's axis | confirmed | #1510 | `degraded`. Described by an axis it does not have, and its two modes are never named |
| P16-W31 | Prompt | 6 pages + 9 app strings | The setting is called "Paginate list" | confirmed | #1511 | `degraded`. The Settings row reads "Paginate long lists" |
| P16-W32 | Prompt | `Danger-Zone.md` | The erase-target table is complete | confirmed | #1512 | `data-loss`. It omits Location photos, which destroys every location photo and the regions on them |
| P16-W33 | Prompt | `Export-and-Import.md` | A raw `.sqlite` export; the format list; recognised suppliers | confirmed | #1513 | `degraded`. The wizard has no `.sqlite` export, Catalogue CSV is omitted, and DigiKey is listed among recognised suppliers |
| P16-W34 | Prompt | `Backup-and-Restore.md` | Four zip contents are fixed; a raw `.sqlite` download cannot be restored | confirmed | #1514 | `degraded`. All four are optional, and the download can be restored. The page's "Try again" button was checked and is real — **rejected** as a separate candidate |
| P16-W35 | Prompt | `Storage-Triage.md` | Publish to cloud sync when storage is critically full | confirmed | #1515 | `degraded`. Sync aborts at 90%, a whole tier before the 95% pause the tip sits under; plus four naming and gating errors |
| P16-W36 | Prompt | `Sharing-Settings-Between-Devices.md`, `Migrating-from-Another-Tool.md` | The never-shared list is exhaustive; barcodes are mapped | confirmed | #1516 | `degraded`. The list omits the network-consent record; barcode columns land in the part number, so migrated items are not scannable |
| P16-W37 | Prompt | `Supplier-Parts-and-Price-History.md`, `Wishlist.md` | Four supplier-part controls; a spreadsheet import | confirmed | #1517 | `degraded`. Star, pin, pack size and MOQ are undocumented; the importer refuses a spreadsheet by design |
| P16-W38 | Functional | `projects.png`, `purchase-orders.png` | Four alt texts describe content the images do not show | confirmed | #1518 | `degraded`. The harness seeds no project or order data, so no re-run can produce either picture. A third capture, `reports.png`, trails off into a blank third because the script never scrolls — added as a comment on the same issue |
| P16-W39 | Prompt | 3 purchasing and sync pages | A replaced control, a pagination link, a deletion-conflict label | confirmed | #1519 | `cosmetic` |
| P16-W40 | Prompt | `Sales-and-Margin.md` | Revenue; a per-item margin; no time window | confirmed | #1520 | `degraded`. The headline is captioned Proceeds ("revenue" is in no catalog), the only breakdown is by category and carries no margin, and the 7d–365d window is never mentioned |
| P16-W41 | Prompt | `Reports-Overview.md` | Two module gates, the Low stock card, an uncapped CSV, `export:run` | confirmed | #1521 | `degraded`. The data-hygiene CSV inherits the same 100-row cap as the screen |
| P16-W42 | Prompt | `Valuation-and-Spend.md`, `Low-Stock-and-Gauges.md` | An unpriced-gauge notice on the Reports screen | confirmed | #1522 | `degraded`. `UnpricedGaugeNotice` has exactly one consumer, the insurance schedule. Plus a data-hygiene pointer that cannot help, and three unnamed spend sources |
| P16-W43 | Prompt | `ABC-Turnover-and-Aging.md` | Aging buckets stock by how long it has sat untouched | confirmed | #1523 | `degraded`. The page states the opposite 53 lines later, and the code agrees with the later one: age is the most recent **inbound** movement |
| P16-W44 | Prompt | `Parts-Catalogue.md` | Four Show scopes; two money columns; the totals rows | confirmed | #1524 | `degraded`. Two scopes are conditional, there are three money columns and sixteen toggleable columns, and Group by / Sort by are undocumented. Distinct from #611, which covers only the Header & branding block |
| P16-W45 | Prompt | `Sales-and-Disposals.md` | Selling is gated on the Sales module alone | confirmed | #1525 | `degraded`. Driven in the browser: a serialised asset and a consumable gauge both get no Sell row. The source location and lot are also undocumented |
| P16-W46 | Prompt | `Data-Hygiene.md` | An item is flagged only when nothing prices it | confirmed | #1526 | `degraded`. A supplier price in another currency resolves to NULL against a base currency, so a priced item is reported as unpriced |
| P16-W47 | Prompt | `Alerts.md` | Five categories, unconditionally, including "Low / out of stock" | confirmed | #1527 | `degraded`. Four lanes are module-gated; there is no out-of-stock lane, and an item at zero with no reorder point raises no alert — driven live. Three names wrong, and `alerts.png` shows four lanes on a page claiming five |
| P16-W48 | Prompt | `Upcoming-Agenda.md` | Six lanes; a look-back note under the list | confirmed | #1528 | `degraded`. Seven lanes ship (Reorder is missing from the page), the note renders only when such an entry survives filtering, and three of six truncatable lanes are unlisted |
| P16-W49 | Prompt | `Reminder-Notifications.md`, `FAQ-and-Troubleshooting.md:171` | Notifications need the app installed | confirmed | #1529 | `degraded`. Support is two capability checks with no standalone test, so reminders work in a desktop tab; iOS is the only real case. Four or more pending reminders also collapse into one and never fire individually |
| P16-W50 | Prompt | `Companion-Extension.md` | The extension is the one feature that reaches out to the web | confirmed | #1530 | `degraded`. The app's own CSP `connect-src` names four further origins it reaches directly. Plus a settings mislink carried on two pages, two of three app origins, and a two-line recipe that is three edits |
| P16-W51 | Prompt | `Loans-Check-Out-and-In.md` | Lending needs only the loan action | confirmed | #1531 | `degraded`. Gated on the Contacts module (a shipped test pins it), and refused for four kinds of item where the page documents one — and names the wrong control for that one |
| P16-W52 | Prompt | `Contacts.md` | The supplier box-out; deleting a contact | confirmed | #1532 | `degraded`. Both stated reasons are false (supplier parts and POs hold a Suppliers FK; Purchase orders depends on Contacts). Deleting a contact with no open loans skips confirmation entirely and cascades their whole loan history |
| P16-W53 | Prompt | `Bookings.md` | An "active" status group; a notice beneath the form; future bookings | confirmed | #1533 | `cosmetic`. The heading is **In use** (driven live), the notice is at the foot of the list, and the contact FK clears unconditionally |
| P16-W54 | Prompt | `Inventory-Views.md` | A favourites filter and a gold star on a card | confirmed | #1534 | `degraded`. No favourites chip exists (`fav:` is the only route) and the Card star computes `oklch(0.72 0.02 280)` at 16% opacity, not the gold token. Six further claims wrong, plus two code comments restating the same error |
| P16-W55 | Prompt | `Finding-a-Setting.md:33-34` | The query `hotkeys` keeps everything in that section | confirmed | #1535 | `degraded`. Driven live: `hotkeys` → "0 settings match"; `keyboard` → 27. `settings-search.tsx:14` carries the identical false example |
| P16-W56 | Prompt | `Dashboard-and-Widgets.md` | Attention cards jump to a filtered list | confirmed | #1536 | `degraded`. Three of four link to the bare inventory screen; only In transit carries a query |
| P16-W57 | Prompt | `Modular-UI.md:24-31` | Two kinds of module | confirmed | #1537 | `degraded`. Four groups render, including Integrations (Sync, Webhooks, Home Assistant); the screenshot's alt text repeats the omission |
| P16-W58 | Prompt | `Counting-by-Weight.md`, `Container-Weights.md` | Three confidence labels; any HA weight entity | confirmed | #1538 | `degraded`. The app ships full sentences, not those labels; the bridge accepts six units where Gubbins offers nine. Plus one preset with no note and a search that does not match material |
| P16-W59 | Prompt | `Custom-Fields-and-Capabilities.md` | Pick a glyph with the mouse; five hideable capabilities | confirmed | #1539 | `degraded`. A click only selects — double-click, Enter or **Use** commits. Eight capabilities are hideable, and the `Postcards` preset also hides `kits` |
| P16-W60 | Prompt | `Appearance-and-Theming.md`, `Achievements.md` | Effects appear at a named animation level | confirmed | #1540 | `degraded`. The five level names appear on no wiki page, so both references are unfollowable; foil and collector cards are gated on Total Gubbage with no note |
| P16-W61 | Prompt | `Low-Stock-and-Gauges.md:83-89` | Three ways to set a threshold | confirmed | #1541 | `degraded`. A per-item gauge percentage exists and is undiscoverable from the wiki. `Notifications-Settings.md:16-17` also overstates its one scrape-notification row as app-wide confirmations |
| P16-W62 | Prompt | `SettingsDialog.tsx` vs all pages | Every shipped setting is documented | confirmed (coverage) | #1542 | `cosmetic`. Item card click, Vibrate on scan, Default purge window and Default downgrade window appear on no page; Beep on scan only inside a search example |
| P16-W63 | Prompt | `Activity-Log.md:6-7` | The Activity screen is where to find it | confirmed | #1543 | `cosmetic`. Activity is a switchable page module; two sibling pages state their own gate and this one does not |
| P16-D1 | Prompt | `Loans-Check-Out-and-In.md:68`, `Roles-and-Permissions.md:114-115` | Overdue loans surface in Alerts | confirmed, **duplicate** | comment on #1293 | Two further sites #1293 does not name. Under its "retract the claim" branch both would survive uncorrected |
| P16-D2 | Prompt | `Bookings.md:68` | The agenda shows bookings and their return dates | confirmed, **duplicate** | comment on #1289 | A third site #1289 does not name |
| P16-D3 | Prompt | `Inventory-Views.md:5-6` | Five view densities | confirmed, **duplicate** | comment on #263 | Seven ship. Both of #263's own counts had gone stale, so the comment corrects them rather than opening a second issue |
| P16-D4 | Prompt | `Reports-Overview.md:29-30` | Sales & disposals has a window selector but no export | confirmed, **duplicate** | — (#585) | The same defect and the same two files #585(a) already names; not re-filed |
| P16-D5 | Prompt | `Bookings.md` | Which items can be booked is never stated | confirmed, **duplicate** | — (#585) | Already named in a comment on #585 with the same greps and code cites |
| P16-D6 | Prompt | `Loans-Check-Out-and-In.md` | Renewing a loan is never mentioned; the borrower union | confirmed, **duplicate** | — (#581) | #581's body opens with the renew claim and its comment covers the borrower union |
| P16-D7 | Prompt | `Activity-Log.md:22-24` | Who made a change is recorded | confirmed, **duplicate** | — (#774) | The first of the three sentences #774 already quotes. The page's Clear-log claim was checked separately and is **true** — the label is baked into the note text |
| P16-D8 | Prompt | `Storage-Triage.md:17` | Each cleanup keeps a copy of what it removes | confirmed, **duplicate** | — (#824) | Same claim, both halves; not re-filed |
| P16-D9 | Prompt | `Parts-Catalogue.md` | Header & branding controls | confirmed, **duplicate** | — (#611) | #611 covers that block; only the controls above it are new, and those went to #1524 |
| P16-D10 | Prompt | `Counting-by-Weight.md:78-80` | The bridge token is filled in on a Settings tab | **rejected** (already fixed) | comment on #264 | Commit `02adb5d2` corrected the page to the Sync screen; #264 appears closable |
| P16-R1 | Prompt | `Reports-Overview.md:29-30` | The list of reports with a window selector is not exhaustive | **rejected** | — | The enumeration is exhaustive and correct; valuation and stock aging take no window parameter at all, and dead stock runs off its configured threshold |
| P16-R2 | Prompt | `Upcoming-Agenda.md:45-49` | The bounded-lane bullet names only one kind | **rejected** | — | The single bullet names both warranty expiries and expiry dates, matching the predicate. A formatting oddity, not a parity defect |
| P16-R3 | Prompt | `Camera-Scanning.md` error box-out | The page does not distinguish error kinds | **rejected** | — | Each error kind produces its own message. The one real inaccuracy is sub-cosmetic: a login wall and a CAPTCHA both grade `BLOCKED` |
| P16-R4 | Prompt | `Companion-Extension.md` scrape merge | An in-dialog scrape persists a supplier part | **rejected as stated** | — | It only fills the form. The Amazon active-tab path does persist one, and that narrower case went into #1530 |
| P16-R5 | Prompt | `Activity-Log.md` change sample | The `→` arrow is a catalog string the sample misquotes | **rejected** | — | The arrow is a hard-coded decorative glyph marked `aria-hidden`, with an `sr-only` "changed to" beside it. The fenced sample is accurate |
| P16-R6 | Prompt | `Locations.md:83-85` | Location history omits an icon change | **rejected** | — | The action vocabulary is a closed six-value set with no action an icon change could take; the page's fuller enumeration is right |
| P16-R7 | Prompt | `Notifications-Settings.md:26` | The Product lookup cross-link is wrong | **rejected** | — | The pointer is correct — the section does sit under Scanning & labels — and `Camera-Scanning.md` mentions product lookup three times. (The *`Companion-Extension.md`* copy of a similar link **is** wrong and went to #1530) |
| P16-R8 | Prompt | `Roles-and-Permissions.md:17-24` | "The first four" misstates the screen order | **rejected** (nit) | — | The phrase introduces the table below it, not a position in the app |
| P16-R9 | Prompt | `Signing-In.md:22-24` | The confirmation modal has four points, the page lists three | **rejected** | — | The omitted point is documented more prominently at `:64-76`, with its own list and a heads-up box |
| P16-R10 | Prompt | `Signing-In.md` | "The only way to switch sign-in off" | **rejected** | — | The Danger Zone escape hatch sits behind the same sign-in gate, so the sentence's argument holds, and the page names it in the next clause |
| P16-R11 | Prompt | `Units-of-Measure.md` | The conversion table has drifted | **rejected** (verified negative) | — | All eight non-default rows re-derived through the real constants and formatter: byte-identical. Option lists match in content and order; the precision rule matches |
| P16-R12 | Prompt | `Branding.md`, `Kiosk-and-Tablet-Mode.md` | Surface opacity and the large-format query have drifted | **rejected** (verified negative) | — | Every figure matches, including the high-contrast guards. `:41-43`'s "follows the width of the window, not the kind of device" was attacked specifically and is correct |
| P16-R13 | Prompt | `Camera-Scanning.md`, supplier list | The seven supported suppliers have drifted | **rejected** (verified negative) | — | Parser labels are byte-identical to the wiki spellings, and the host permissions cover those seven and no others. The Amazon carve-out matches the exclusion's stated reason |
| P16-R14 | Prompt | `Activity-Log.md` export columns | The export column lists have drifted | **rejected** (verified negative) | — | Both column lists and the seven export formats match byte-exactly in both directions |
| P16-R15 | Functional | `users-screen.png`, `users-role-editor.png` | The two captures are the same image | **rejected** (verified negative) | — | Both pairs are byte-identical between the committed and fresh sets, and differ from each other |
| P16-W64 | Prompt | `Saved-Searches-and-Favourites.md` | Saving adds to a list; three routes can save; favourites float in any list; two recall outcomes | confirmed | #1544 | `data-loss`. Driven end to end: saving `mfr:acme` as "low" over an existing "Low" replaced it in place — same id, no dialog, no undo. Save is disabled for the graphical and plain-English routes; favourites do not lead the Visual-search read; a stored query that no longer parses is a silent third outcome. The 50-item cap, its eviction and the 60-character name truncation are also undocumented |
| P16-W65 | Prompt | `Text-Query-Syntax.md` | A typo'd field name is flagged; `field:` is "contains" | confirmed | #1545 | `degraded`. Driven: `mfrr:acme` raises no error and returns 0; `mfr:cme` returns 0 against "Acme Tools" because the text fields match by token prefix. Plus a multi-word custom-field name that mis-parses even when quoted, five undocumented forms, and enum spellings echoed in a different case |
| P16-W66 | Prompt | `QR-Codes-and-Label-Printing.md:104-105`, `:186`, `:243` | A "Columns per sheet" control; Settings → Labels & QR codes | confirmed | #1546 | `degraded`. `grep -rn "Columns per sheet" src/` returns nothing; the real control is **Sheet layout**, and a plain **Columns** field exists only inside `Custom…`. Driven: no settings tab matches "Labels"; the real path is Scanning & labels. Two sibling pages name that tab correctly |
| P16-W67 | Prompt | `Visual-Query-Builder.md` | Groups nest freely; a yes/no field gets a toggle | confirmed | #1547 | `degraded`. Driven: the innermost **Add group** is disabled at four levels deep, with no title, no `aria-describedby` and an unchanged tooltip. A boolean field renders a combobox. The text path reports the same limit; the wiki explains neither |
| P16-W68 | Prompt | `Natural-Language-Search.md:14`, `:17` | "low stock" is a recognised phrase | confirmed | #1548 | `degraded`. Driven: the chip reads "Low stock (under 5)". The 5 is a fallback used while the low-stock preference is off, which is the default; the page never says the phrase resolves to a number at all. The **Ask** button is also unnamed |
| P16-W69 | Prompt | `Continuous-Scan-and-Batch-Actions.md`, `Camera-Scanning.md` | Silence means the code was not read | confirmed | #1549 | `degraded`. The scan gate has three outcomes and the third is deliberately silent — a repeat of a code that first resolved to nothing. The module's own docstring names this confusion. Plus "for example" on a closed two-item set, a message that gives a name or a count but never both, and frame-skipping attributed to the native decoder |
| P16-W70 | Prompt | `Receipt-and-Label-OCR.md` | The engine is bundled; the engine is downloaded separately | confirmed | #1550 | `cosmetic`. Both are true — staged into the app's own files at build time, precache-excluded and fetched on first use — and nothing on the page reconciles them. The settings section is also platform-gated with no note, and the default tier is unstated |
| P16-D11 | Functional | `search-visual-builder.png`, 3 pages | The capture shows the NOT control | confirmed, **duplicate** | comment on #1480 | The committed image predates the **NOT** button and the `-not` hint, which two pages document against it. Same staleness as the six #1480 already lists |
| P16-D12 | Prompt | `Camera-Scanning.md:26-27` | A page links to itself | confirmed, **duplicate** | — (#1268) | Swept every page for a self-link: exactly one, matching #1268's claim. Not re-filed |
| P16-R16 | Prompt | `Search-Overview.md:29` | "the bookmark button" names the wrong control | **rejected** | — | The accessible name is "Saved searches", but the page describes the icon and the icon is a bookmark glyph. Nothing asserted is false |
| P16-R17 | Prompt | `Search-Overview.md:61-62` | "a short identifier in brackets" states the wrong length | **rejected** | — | The picker's identifier is 6 hex characters and a label short code is 8, but the page states no length. A clarifying half-sentence at most |
| P16-R18 | Prompt | `Text-Query-Syntax.md:78-84` | Locale date formats are rejected without documentation | **rejected** | — | `:78` documents `YYYY-MM-DD`, and the rejection message names the required form. Driven with three invalid formats |
| P16-R19 | Prompt | `Saved-Searches-and-Favourites.md:39-41` | "on your device, alongside the rest of your data" | **rejected** | — | They live in browser storage rather than the vault, but both halves are true and every user-visible consequence — device-local, carried in a backup, cleared by the Danger Zone — is stated correctly |
| P16-R20 | Functional | 5 search and scanning pages | A page with no screenshot is a defect | **rejected** | — | Nothing on those pages is wrong or contradicted. The house style asks for a screenshot "where a screenshot would help"; this is the #53 backlog's shape, not parity drift |
| P16-R21 | Prompt | `Camera-Scanning.md:13-15` | The thirteen symbologies have drifted | **rejected** (verified negative) | — | Exact in both directions against `ALL_NATIVE_FORMATS`; no extras either way |
| P16-W71 | Prompt | `Roles-and-Permissions.md:205-206`, `:208-213` | **Everything under &lt;area&gt;** covers actions added in a future update | confirmed | #1551 | `wrong-data`. `toggleSubject` (`role-grants.ts:144-155`) always writes `mode: 'explicit'` with the keys that exist at that instant, and never `'wildcard'` — so no route in the editor produces the grant the page describes. Driven: after ticking the row, saving and reopening, every box is ticked and the **All, including future** badge is absent, while the seeded Manager role's wildcard rows show it. The `:208-213` heads-up inherits the error, warning that an individual tick turns off a shortcut that was never on. The **Allow everything** bullet above it is accurate |
| P16-W72 | Prompt | `Loans-Check-Out-and-In.md:7`, `:19-20` | The **Check out** action | confirmed, folded in | comment on #1531 | Driven side by side: the menu row is **`Loan out…`**; `Check out` is only the dialog title and its submit button. Refines #1531 in one place — an **untracked** item does get a row, reading `Convert to Bulk to loan out…`, so the case with no row *and* no explanation is the **consumable gauge**, not the untracked item as that issue's suggested direction implied |

### Phase 17 — Cross-cutting static sweeps

Pinned SHA: `3864f22dc81ff9685610cce925189780e4b9b406`

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| P17-1 | Mechanical | `src/lib/{money,date-input,calendar-days,name-fold,save-file}.ts`, `features/import/file-source.ts` | None of the six data-correctness seams has a source-scanning guard, while four design-system rules of the same shape do | **rejected** | — | The finder's own caveat decided it: **no live, unfiled bypass exists** that such a scan would catch, so the finding has no consequence — only a hypothetical future one. Two supporting limbs also fail. (a) **No seam claims it is enforced**: all six headers read; every one is a normative instruction (`money.ts:15` "Nothing else … may hand-roll"; `date-input.ts:15` "NEVER re-derive") or a rationale (`calendar-days.ts:6-31` is a "## Why not" explanation, not even normative). The one shaped as a checkable universal, `file-source.ts:4-6`, the finder **verified true** — all 11 file inputs enumerated, exactly the four documented ones call `readImportFile`. The mirrors-X rule governs parity claims between two definitions; "use this seam" is not one. (b) **The precedent is a different class**: #1126, #847 and #958 each have a guard that EXISTS and is weaker than it claims, so the guard's green result is itself a false assertion. "No guard exists" asserts nothing and so cannot be false; the project has no precedent for filing absence. Adding the six guards is a reasonable enhancement, not an audit finding |
| P17-2 | Mechanical | `src/lib/clock.ts:54-55` | A dead `DAY_MS` export whose docstring invites the boundary arithmetic `calendar-days.ts` forbids | confirmed | #1570 | `cosmetic`, with a real hazard. Dead verified independently of the docstring: every `DAY_MS` hit outside the declaration is a locally-declared const in five test files, no barrel re-exports it, and all 25 `from '@/lib/clock'` imports pull seven other names. Deleting :54-55 → type-check exit 0, suite green. The hazard is not over-read: `calendar-days.ts:5-31` is a 27-line argument titled "Why not + N * 86_400_000", and `nextUtcDay()` exists for one stated reason — so callers "reach into this seam rather than writing + 86_400_000 at the call site, where the reader cannot tell a sanctioned UTC step from the DST-unsafe local one". The repo deliberately removed the affordance; this re-supplies it, from the module about time, with none of that context, and its docstring ("for callers converting a date choice into an offset") is verbatim the job of `offsetForDate` 80 lines below, whose own docstring spends 14 lines on why a naive multiple is wrong (#327, #326). Third declaration of the number after `constants.ts:352`. Distinct from #1268: that module is wholly dead and #377 decided to keep it; `clock.ts` is live with 25 importers |
| P17-3 | Mechanical | `src/features/lookups/components/LookupMatchDialog.tsx:74`; `package.json:42`; `eslint.config.js` | A dead `eslint-disable` directive, and a lint gate that cannot fail on it | **rejected** | — | Two limbs, both fail. **The gate limb is a documented decision and must not be filed** — confirmed verbatim at `.githooks/pre-commit:53`: "No `--max-warnings`: the accepted baseline keeps a handful of intentional warnings green." CI runs the same `npm run lint` (`tests.yml:93`), so the ceiling is unpinned by the same stated choice. **The dead-directive limb has no consequence**: `npx eslint . --report-unused-disable-directives` finds exactly one repo-wide, `npm run lint` exits 0, it is already recorded as a §12 baseline observation naming this exact file and line, and `npm run lint:fix` removes it. One stale comment, nobody misled. Considered and dropped as a "lint hygiene" sweep with P17-39: two zero-consequence nits do not clear §1's bar |
| P17-4 | Mechanical | `src/features/scraping/ScrapeBridgeContext.tsx:232-287`; union at `protocol.ts:49-64` | A `: void` handler over a 13-member union with no exhaustiveness guard | confirmed (narrowed) | #1559 | Latent — `degraded` when it bites, no live consequence at this SHA. Probe member added to `EXTENSION_MESSAGE_TYPES` + its `z.discriminatedUnion` arm → `npm run type-check` exit 0, nothing at :232. Contrast: the same probe on `BackgroundRequest` → `extension/src/background.ts(527,26) TS2345 … not assignable to 'never'`. **Narrowed — the finder's framing is partly wrong**: "handles 8 of 13, closes with a comment not a `default:`" reads as a present defect and is not one. `:285` states "*_REQUEST kinds and the PWA's own APP_READY are outbound-only — ignore our echo"; the five unhandled kinds are correctly ignored and the comment is a decision. The real finding is only the absent future-proofing guard. **Dropped**: the `extension/src/content-script.ts:246` limb — an if/else allow-list with its own §9.1 rationale and no discriminated switch to guard. Growth verified by `git log -L` over the union: five plain code changes (`7b74d83b` #664, `4cbb2209`, `480970a4`, `a058e4b7`, `206dfb39`), **no migration/column/CHECK** — the material difference from the C3-9 and C5-10 rejections. #562 is a genuine gap not a deliberate exclusion: read in full, it scoped itself by enumerating the eight callers of `src/lib/exhaustive.ts`, so a switch that never called it was structurally invisible. Adjacent, recorded not filed: `protocol.ts:91`'s "Bump this whenever a message kind is added" is also held up by prose alone — the probe compiled clean with `PROTOCOL_VERSION` left at 5 |
| P17-5 | Mechanical | `src/features/settings/settings.ts:574` | `navCountOption`'s `!` asserts `default ∈ options`, a relation the compiler does not check | **rejected** | — | The invariant is machine-checked and the guard is **total over routes**. Ran the finder's exact mutation (`'/bookings'.default → 'upcomming'`): `npx tsc -b --noEmit` exit 0 (the finder is right that the compiler misses it) but `settings.test.ts` → **4 failed / 70 passed**. The first failure is the guard the finder says does not exist, at `settings.test.ts:631-638`: it iterates `NAV_COUNT_ROUTES = Object.keys(NAV_COUNT_METRIC_CONFIG)`, so it is **total by construction** and covers a newly added tile route automatically — exactly the scenario the argument turns on. The finder's "settings.test.ts:658-683 covers only /inventory and /projects" describes the **tone** tests and mistakes them for the coverage; the drift guard is 20 lines above. The runtime consequence is real (`navCountTone` throws `TypeError`), so the `!` is genuinely unchecked *by the compiler* — but the defect cannot ship: it goes red on the first `npm run test:run`. Under the C3-9 / C5-10 reasoning (rejected because a gate stands in the way), a passing test is a **stronger** gate than a migration, not a weaker one. Lead re-read `settings.test.ts:628-642` and confirms |
| P17-6 | Mechanical | `reconcile.ts:579`; `snapshot.ts:1104`, `:584`; `tombstone.ts:220,228`; `v1-initial.ts:303`; `unique-keys.ts:147` | A snapshot tombstone naming a built-in `roles` row aborts every later merge, permanently | confirmed (reshaped) | **withheld — §11** | **`unusable`. TRUST-BOUNDARY FINDING, DELIBERATELY NOT FILED** (Phase 12/13/14 precedent). Demonstration, two `:memory:` DBs through `runMigrations`: first and second merge attempts both `DbError: A built-in role cannot be deleted.`; local roles count 8; **control** (an operator-created role id) merges cleanly, deleted count 1. `trg_roles_protect_builtin_delete` (`v1-initial.ts:302-309`) fires on the DELETE `applyPlan` builds from `reconcile.ts:579`, and the whole atomic apply rolls back. **Reshaped on four points, all of which make it worse.** (a) Not "can never sync again" but a **180-day failure cycle** punctuated by a TTL clone that succeeds and then re-poisons: `buildCloneStatements` never applies tombstones as DELETEs so the clone succeeds, but its final loop (`snapshot.ts:1268-1273`) copies the poison tombstone into the local `tombstones` table, and `WIPE_FILTER`'s `roles: 'is_builtin = 0'` spares the role row. (b) The recovered device becomes a **second source** — it republishes the poison (proved `true`). (c) A factory reset or a brand-new device is **equally blocked**: the test's local device *is* a fresh migrated DB with `last_sync_timestamp = 0`, and it still aborts, because the baseline re-seeds the built-in roles. (d) `restoreSnapshot` is **not** exposed — `conditionalTombstoneStatement` (`:606-615`) records a tombstone only when the row is absent — which narrows the entry points to the sync merge alone. **No escape hatch**: the only catch on the sync path wraps `provider.pushSnapshot`, not the merge; nothing drops an inapplicable tombstone, quarantines the payload or advances a watermark; `writeSyncMeta` is never reached, so every retry recomputes the identical plan. Danger Zone "Sync links & pending deletions" clears **local** tombstones and zeroes `last_sync_timestamp`, but the poison is in the remote and `needsFullResync(0, …)` returns false, so the device goes straight back to the delta path. Safe Mode touches only the local database. **Siblings are immune by construction, verified independently**: all four protected ids (`locations` Unassigned/In Transit, `users` System/Admin) return `error=undefined rowStillPresent=1`, because `TABLE_FILTER` (`snapshot.ts:82-89`) keeps them out of the local snapshot so `reconcile.ts:577`'s `if (l !== undefined)` never fires. `roles` is deliberately the exception — built-in roles are **editable**, so they carry no read filter and travel in every snapshot. **The precise gap**: the project already holds the protected-row-id fact twice (`ALWAYS_PRESENT_ROW_IDS` at `snapshot-integrity.ts:51`, `isProtectedRole` at `unique-keys.ts:147`) and neither is consulted on the tombstone-delete path. Lead spot-checked the trigger, `reconcile.ts:570-585`, `TABLE_FILTER`, `WIPE_FILTER` and both existing facts directly; `WIPE_FILTER`'s own comment states the hazard ("Any row protected by a `RAISE(ABORT)` delete trigger must be spared here or the entire clone transaction aborts") for the clone path and not for this one. **Precondition**: a crafted, corrupt or foreign snapshot — squarely inside the project's own stated threat model, since `tombstone.ts:214-219` calls `TOMBSTONE_TABLES` "a SECURITY BOUNDARY, not a convenience" and names the inputs "a backup file, a peer's snapshot in a shared sync folder, or a bridge push". No supported route produces it (`RoleRepository.delete:138-149` refuses on `isBuiltin`; `update:120-129` cannot set `is_builtin`; the unique-key resolver cannot retire a protected role after #708; the Danger Zone writes no roles tombstone; the bridge has no role delete and hydrates through `restoreSnapshot`). **The shipped providers are a shared folder and a shared Drive folder.** Not inside #708 (scoped to `resolveUniqueKeyCollisions`; the plain LWW delete at `reconcile.ts:579` was never in scope) nor #889 (parentage loops → FK failure; same failure shape, different mechanism). None of §10's 14 tombstone rows is about a protected row id |
| P17-7 | Functional | `reconcile.ts:263`, `:269`; `snapshot.ts:1302`; `merge.ts:356`; `restore-backup.ts:165` | The serialised-loan (#193) and booking-overlap (#194) repairs run only on the delta merge, not on the two wholesale apply paths | confirmed (reshaped) | #1552 | `wrong-data`. Loan legs and bookings seeded through the **real** repositories, matching the shipped `serialised-loan-sync.test.ts` fixture: restore leaves 2 open loans for one serialised item and 2 overlapping live bookings; the **control** — the same two snapshots through the delta merge — collapses each to 1, which is what isolates the missing pass rather than the invariant. `CheckoutRepository.listOpen` shows 2; checking one in succeeds and quantity settles at 1; `LIVE_BOOKING` (`AssetBookingRepository.ts:87`) filters only on `cancelled_at`/`converted_checkout_id`, so the calendar draws the overlap. **Reshaped**: the finder said the TTL clone reaches `restoreSnapshot`; it reaches `cloneWithSalvage` (`merge.ts:356`) → `buildCloneStatements`. Both paths are affected, each driven separately, but a fix targets **two different functions**. Two further `restoreSnapshot` callers are immune — `bridge/src/hydrate.ts:58` (fresh schema, empty DB) and `sync/backup.ts:283` (test-only, whole-tree grep confirms no production caller) — and Replace-mode restore is immune because the wipe removes the local row first. **Permanent on a backup-only install**: both resolvers have no other call site in the tree, opening the loans list does not repair, check-in does not repair, and `db-maintenance-actions.ts` contains zero occurrences of checkout or booking. **On a synced device the "heal" is itself a defect** — the next sync stamped `returned_at == checked_out_at`, a real loan recorded as held for 0 ms, precisely what P2-31 → #789 called out for `mergeItems`. Not inside #538: that fix generalised `resolveUniqueKeyCollisions` via the new `unique-key-repair.ts` and explicitly enumerated what it did not take on (spinning off #707, #708); a fixer actioning #538 today has nothing here to action. Strong in-repo corroboration, read by the lead directly: `unique-key-repair.ts:1-30` names **these exact two paths** for a different repair, and the `One of n flag sync invariant` note states the rule outright — the one-of-N flags are repaired on all four paths, these two on one. Four dedupe searches: #193/#194/#542 are two-devices-merging, #877 is discrete quantity on the merge path, #996 is cross-mode item merge, #826 is the local read-then-write race |
| P17-8 | Mechanical | `src/features/about/DiagnosticsCard.tsx:126`; registry at `diagnostics.ts:67`, `:91`, `:129` | A template-literal message key cast to `MessageKey` defeats a check the compiler would make, with no test standing in for it | confirmed | #1562 | `cosmetic`. **The cast is unnecessary and it suppresses a check that works**: deleting it leaves only the now-unused import (TS6133); adding a field with no catalog key then gives `TS2345: … '"about.diagnostics.field.engine"' is not assignable to 'MessageKey'` — TypeScript fully expands the template literal against the catalog union. With the cast in place the same mutation is invisible (tsc exit 0; 12448/12448; the rendered `<dt>` column ends `…"about.diagnostics.field.engine"`). `formatFieldValue`'s exhaustive switch forces a case and the total `Record` forces a payload label, so a new field is forced through **two** gates and past the third — which makes the omission look complete rather than absent. **The secondary claim is true and it matters**: `permission-labels.ts:11-14` and `permission-labels.test.ts:4-5` both assert these keys are "built by interpolation and cast, which the type system cannot follow into `en.json`"; stripping all five casts gives `tsc -b --noEmit` exit 0. That false claim sits in the very file this cast was plausibly copied from, stated twice. **The count is 12, not 13, and 10 of the 12 are guarded**: DiagnosticsCard 1, `useErrorMessage` 3, `permission-labels` 5, `builtin-role-labels` 2, `builtin-user-labels` 1; the 5+2 are guarded by `catalog-drift.test.ts:81-83`, the 3 error casts by `db-error-message.test.ts` whose incompleteness is #958, and `builtinUserDescriptionKey`'s gap is #1276 — DiagnosticsCard is the unfiled one. Second limb: `ENGLISH_DIAGNOSTIC_LABELS` and `about.diagnostics.field.*` are 21 labels each, byte-identical today (21 found, 0 mismatches), with nothing holding them together. Not a duplicate of #958, whose three items are different files |
| P17-9 | Functional | `index.html:6`, `:61-64`; `vite.config.ts:240-241` | The pre-boot shell's fixed near-black survives into the running app, so a Light-appearance user gets dark window chrome and a dark page canvas | confirmed | #1563 (limb A); limb B → §11 | `cosmetic`. Driven in the built app (`vite preview`, real Chromium), Appearance forced to Light, identical under both OS colour schemes: `htmlClass: ""`, `rootBg: rgb(11,11,15)`, `bodyBg: oklch(0.99 0.003 280)`, `themeColorMeta: #0b0b0f`. **`htmlClass: ""` is the decisive line** — it proves `applyAppearance` ran and removed the boot-time `class="dark"` from `index.html:2`, so the root element's dark presumption *is* undone at runtime and its background is the one piece that is not. Built-output evidence, not source: `html{-webkit-text-size-adjust:100%}`, `body{background-color:var(--background);…}`, `dist/index.html:8` and `:66` still `#0b0b0f`, `manifest.webmanifest` `"background_color":"#0b0b0f","theme_color":"#0b0b0f"`. No runtime updater exists — grep over `src public scripts` (tests excluded) returns zero. **Limb A (theme-color + manifest) is certain and platform-independent and needs no driving.** **Limb B (the root canvas) has a confirmed mechanism but unverifiable visibility here** — the computed-style pair settles the background-propagation argument exactly, but where it is *seen* is rubber-band overscroll on iOS/macOS Safari, which headless Chromium does not render. The `index.html:63` comment gives only a boot-time reason and is not a decision covering the running app; the `class="dark"` sibling on the next line **is** undone at runtime, which is the intended pattern this rule misses |
| P17-10 | Mechanical | `src/csp.ts:14-17`; `index.html`; `vite.config.ts:187`, `:214` | Nothing tests the zero-inline-script architecture the CSP depends on | confirmed | #1560 | Latent missing guard; `unusable` when it bites, production-only. `src/csp.test.ts` asserts nothing about `index.html` or `dist/index.html` — its 21 cases are `toCspOrigin` (4), `buildContentSecurityPolicy` (5), `withCspMeta` (3), `policyAllowsConnectOrigin` (6) and serialisation, with no `readFileSync`. The claim is **true at this SHA**: built, comments stripped (the `<head>` comment at `index.html:15` contains a literal `<script src>` that naive greps match), inline scripts with a non-empty body = 0. Mutation: an inline script added to `index.html` → `npm run type-check` exit 0, vitest green, `npm run build` exit 0 with 1 inline script in the emitted HTML; then served with the meta CSP in real Chromium → `window.__AUDIT_INLINE_PROBE__` undefined and a console CSP violation. Every local gate green, blocked only in the deployed build. Precedent #724 (open, from P0-7, untouched) is exactly this shape and does **not** cover it — its three sites are `tests.yml`, `e2e.yml`/`deploy.yml` and `scripts/lockfile.mjs`. The guard is trivially expressible and the pattern already exists in-repo: `src/lib/safe-area-token.test.ts:32` already does `readFileSync(repoPath(import.meta.dirname, 'index.html'))`. Cross-references #599 (no push/PR check builds the app at all), the adjacent gap in the other direction |
| P17-11 | Mechanical | `src/features/dashboard/useReorderFlip.ts:39-90`, `:47`, `:75`, `:83` | The layout effect acquires a frame and a `transitionend` listener per moved tile and returns no cleanup | confirmed (narrowed) | #1567 | `cosmetic`. Mechanism isolated in real Chromium: a running transform transition, then `transition:none` exactly as `:47` does → `{"fired":["transitioncancel"],"midway":"matrix(1,0,0,1,42.4114,0)"}`; `midway` proves it was genuinely mid-flight and only `transitioncancel` fired. Real app (dev server, Dashboard → Customise, 24 alternating moves 40 ms apart, listeners counted from an init script before any app code): immediately after the burst total 59, three widgets at net 16 each with an inline `transform 300ms var(--ease-emphasized)`, `{transitionrun:47, transitioncancel:43, transitionend:1}`; 2 s later total 12. **43 cancels against 1 end** is the mechanism in shipped code. **Narrowed on three points**: (1) accumulation is real but **bounded and self-healing** — about one per repeat during a burst, discharging to a permanent residue of one per reorderable tile (12 observed), not unbounded growth; (2) the second limb **did not reproduce** — after 24 rapid reorders every tile's inline transition was back to `""`; (3) **no user-visible symptom of any kind was observed**, so this is strictly weaker than its sibling #980, which does have one, and a maintainer could reasonably decline it on consequence alone. Context from the effect sweep: this is the only one of 90 `addEventListener` sites in `src/` whose registration has no removal path the effect controls — the other 24 apparent gaps are all accounted for |
| P17-12 | Performance | `src/features/inventory/InventoryScreen.tsx:1560`; `useLocationCycleCount.ts:127-140`; `CycleCountContext.tsx:147-149` | A fresh object literal as the `location` prop re-runs the count sheet's seeding effect on every parent render | confirmed (narrowed) | #1558 | `degraded` at large sheets, negligible on small ones. Real `CycleCountDialog` + `CycleCountProvider` + `useLocationCycleCount`, repository stubbed as `CycleCountDialog.test.tsx` already stubs it, `CycleCountLines` replaced by a render counter, five parent re-renders: LITERAL `{"baseline":2,"afterFive":12,"delta":10}` vs STABLE `{"baseline":2,"afterFive":5,"delta":5}` — **every parent render costs two sheet renders instead of one**. Mechanism verified from source: `CycleCountContext.tsx:147-149` runs `setLocation`/`setLoadedLines`/`setLoadedSerialised` **before** the `seededLocationRef` early return at `:151`, and `useLocationCycleCount.ts:131-137` rebuilds `data.discrete.map(...)` into a fresh array on every run, so `setLoadedLines` is always a real state change; that invalidates `usable` (`:239-247`, O(n) + two Set builds), `lines` (`:249-265`, O(n)), then `countedLines` and `coverage`. Sibling contrast confirmed: `AuditDayDialog.tsx:401-402` passes a stable store reference and has no extra render — only the Inventory entry has this. Unboundedness confirmed from the code's own comments (`useLocationCycleCount.ts:113-114` "uncapped … (issue #561)"; `CycleCountLines.tsx:12-14` "a bulk-storage shelf can put thousands of lines here"). **Honest limit**: the doubling is certain per `InventoryScreen` render, but how often that screen re-renders while the modal is open was **not measured**. Overlap with #1168 adjudicated: **file separately, cross-reference** — every site #1168 names is the draft-mirror path, its trigger is a keystroke, and both its remedies live in `count-draft.ts`/`useCountDraftStore.ts`; neither would stop the seeding effect re-running or the O(n) memo chain recomputing. They do compound, since the mirror effect's deps at `:233` include `location` |
| P17-13 | Mechanical | `src/features/hotkeys/hotkeys.ts:122-124` | The docstring says a drift test asserts the hotkey labels match the nav labels; no such test exists | confirmed (reshaped) | #1569 | `cosmetic`, folded. Negative established first: only `catalog-drift.test.ts` imports both registries, and it pins each to `EN_CATALOG[…messageKey]` **separately** with no cross-comparison; `grep -n "label" hotkeys.test.ts` → exit 1. Mutation both directions, full app suite each time: hotkey side given its own `messageKey` + label, and nav side given a new `messageKey` + label → **1 failed / 800 passed, and 2 failed / 12448 passed** both times (the one failure is another concurrent scratch file), type-check clean. **Reshaped — the finder's mechanism is wrong and its demo does not reproduce.** A label-only mutation (`nav.inventory` → `'ZZZ-DEAD-FIELD'`) goes **red** at `catalog-drift.test.ts`, and all eight hotkey test files pass including `ShortcutsOverlay.test.tsx:41`. **`HOTKEY_ACTIONS[].label` has zero production readers** — `HotkeySettings.tsx:336` and `ShortcutsOverlay.tsx:123` both render `t(action.messageKey)`, `useHotkeyHints.ts` reads only bindings, and the i18n fallback is `EN_CATALOG` (`useT.ts:23`), not the label. So the real seam is the **messageKey**, and there are **three** false clauses in one docstring: "the stable identifier used for the settings list" (never read there), "as the i18n fallback" (it is not), and "asserted by a drift test" (the label↔catalog identity is; the hotkey↔nav parity is not). The docstring is a copy of `nav-destinations.ts:89-92`, where every clause is true (#238 records the nav label's retention as deliberate), with "command-palette search text" swapped for a surface that does not use it. **The finder's own demo also fails**: it picks `nav.inventory`, and that action *is* pinned by `ShortcutsOverlay.test.tsx:41`. Nine of the ten go green; the one chosen goes red |
| P17-14 | Mechanical | `bridge/src/openapi.ts:736`, `:850`, `:2857` | Three more hand-typed vocabularies in `openapi.ts`, each with an importable `as const` source of truth | **duplicate #556** | — | Mechanism fully reproduced, finding not new. All three agree today; grep for the three constants in `openapi.ts` → exit 1. Mutation (added `'audit'` to `ACTIVITY_KINDS`): type-check clean across app/bridge/extension, bridge suite 79/1191 passed — everything green with the served and committed spec stale. **Derivation proven available rather than asserted**: the real fix was applied (`enum: [...TRACKING_MODES]` etc. + two imports) → type-check clean, `npm run smoke:bridge` all checks passed, `openapi.test.ts` 17 passed with the emitted YAML **byte-identical**, so it is a drop-in. **#556 (open) already names all three by line and by constant** — its table lists trackingMode → TRACKING_MODES, deadStockMode → DEAD_STOCK_MODES, activity kind → ACTIVITY_KINDS, plus borrowerType and checkout status, plus a sixth limb (condition published with no enum). Line numbers have drifted (686→736, 795→850, 2684→2857) but the constants and greps are named exactly, and #556 is entirely unactioned. **The finder's recommendation to widen #1468 is wrong and #1468 says so**: its closing paragraph reads "Distinct from #556, whose five are `enum:` arrays in schema definitions rather than prose in parameter descriptions" — it deliberately carved these out. `openapi.yaml` is not a second limb: `emit-openapi-yaml.mjs` regenerates it and `openapi.test.ts` asserts the committed file matches a fresh emit, so it is derived and cannot drift independently. Lead re-read #556 before withholding the filing |
| P17-15 | Mechanical | `bridge/src/api/item-view.ts:116-119`; `location-view.ts:53-54`; DTOs at `dto.ts:72`, `:110`, `:122`, `:127` | Four "kept in sync with the DTO" element-key whitelists held together by nothing | confirmed (reshaped) | #1554 | `degraded`. Field-for-field agreement today (3/3, 4/4, 5/5, 4/4); no test file names any of the four constants. Driven against a real bridge (`serve.mjs`, committed synthetic fixture, seeded token). **Direction 1** — delete `'weight'` from `CAPABILITY_KEYS`: type-check clean, bridge suite 79/1191 passed, then `GET …fields=capabilities.weight` → 400 `Unknown sub-field "weight"` while `bridge/openapi.yaml:5260` still publishes it as a **required** property (lead re-read the Capability schema and confirms `required: [key, valueNum, valueText, weight]`). **Direction 2 — the one the finder raised but did not test** — add `'unitPrefix'`, a key no DTO has: bridge suite passed, `GET …fields=capabilities.unitPrefix` → **200** with `"capabilities":[{},{}]`. The accepted-but-absent key is silently dropped from every element; `projectThrough` (`field-select.ts:172-176`) maps `pick(el, subKeys)` and `JSON.stringify` erases the `undefined`. That is the worse direction: a client that asks for a field and gets 200 has no signal anything was omitted. **Reshaped — the "unreachable by any test" claim is wrong and this changes the fix**: `ITEM_FIELD_REGISTRY` (`item-view.ts:200`) and `LOCATION_FIELD_REGISTRY` (`location-view.ts:69`) are exported and `FieldNode.elementKeys` is a public readonly field, so a test can already reach all four lists; the constants need no exporting. What is genuinely absent is a **comparison target** — the DTOs are TypeScript interfaces with no runtime representation — so the honest fix is at the type level (`elementKeys: readonly (keyof CapabilityDto)[]` plus an exhaustiveness helper). `elementKeys: readonly string[]` (`field-select.ts:44`) is why TypeScript is silent. Stands alone against #1468: different file family, different mechanism, no shared representation at all, different fix. Suggested limb attached: `respond.ts:95`'s "Identical to `sendJson` but for the media type", where `sendJson` takes an `extraHeaders` parameter `sendODataJson` lacks |
| P17-16 | Mechanical | `src/features/webhooks/bridge-client.ts:74` vs `bridge/src/events/webhook-log.ts:51`; `src/features/inventory/scale-reading.ts:28` vs `bridge/src/homeassistant/scale.ts:57` | Two app↔bridge wire shapes claim to mirror each other with no cross-side test | confirmed (reshaped) | #1555 | `degraded` (webhook limb), `cosmetic` (scale limb). Agreement today: 13/13 fields for the delivery record — names, types **and** order — and 3/3 for the scale entity. **The finder's demonstration is incomplete**: a bare `targetName` → `targetLabel` rename does **not** leave the bridge green — `server-webhooks.test.ts(46,5)` and `webhook-log.test.ts(20,5)` both raise TS2353 excess-property errors in the bridge's **own fixtures**, the kind a renaming developer sweeps in the same pass (an IDE rename does it automatically). Sweeping them: type-check clean across app/bridge/extension, bridge suite 79/1191 passed, `src/features/webhooks` 13 files/246 passed — everything green with the bridge serving `targetLabel` and the app still reading `targetName`. Driving the app's real parser: `{"seq":1,…,"targetId":"","targetName":"",…}` — `readDelivery` (`bridge-client.ts:317-343`) guards only seq/at/outcome/eventType, so the row survives and `WebhookDeliveryLog.tsx:127` renders an empty span. **Third limb the finder missed**: `openapi.ts:2763`/`:2787` hand-type `targetName` in the published spec too, and `openapi.test.ts` stayed green throughout. Scale limb: `unit` → `unitOfMeasurement` gives a stronger initial signal (3 bridge failures, all whole-object `toEqual` restatements of the DTO); swept, everything green, parsed entity `unit:""`, `ScaleReadPanel.tsx:308` loses the "kg" hint — but readings are unaffected (`/scale/state` returns a different DTO with its own unit), so the finder **over-rated** this limb. **Contrast case is the strongest part**: renaming `occurredAt` in the app's `WebhookEventView` produces five hard compile errors in the bridge. **The fix was applied, not proposed**: `export type WebhookDeliveryRecord = WebhookDelivery` and `export type ScaleEntityDto = ScaleEntity` → `tsc --noEmit -p bridge/tsconfig.json` clean, `npm run smoke:bridge` all checks passed (`import type` is erased, so the strip-only loader is fine), bridge suite 79/1191 passed. The U8-4 precedent does not transfer: its red came from independently-authored assertions on a converted value (a semantic pin); here the red is a mechanical restatement of the very rename being performed, in fixtures the renamer edits without thinking |
| P17-17 | Mechanical | `src/features/settings/theme.ts:118`; `theme-registry.ts:222`, `:243`; `src/styles/index.css:2964-2966`, `:2972-2973`, `:2979-2982` | The flourish threshold is encoded twice, and the CSS's copy of the ambient threshold is unguarded | confirmed | #1564 | `cosmetic`, latent. **Flourish limb** — a sixth level `lively` inserted after `headache`, the five length/rank assertions fixed: `suppressesFlourish('lively') === false` (so `useDecorationFlourishReduced` keeps the success burst and tilt transform running) while `applyAppearance` stamps `data-anim-level="lively"`, which the **presence** selector matches — so `.gubbins-spotlight-border::before` and `.gubbins-tilt::after` are `display:none`. Exactly the asymmetry `decoration-motion.ts` exists to prevent. The ambient assertion stayed green under this mutation — a clean control showing the test fails only for the stated reason. **Ambient limb** — a level `sparse` between `minimal` and `off`: the CSS **under**-suppresses, keeping the starfield and accent glow at a tier the registry says drops them. Nothing else catches it: with `lively` in place and only the length/rank assertions fixed, `src/features/settings` + `src/components/foundry` + `src/components/background` + `src/lib/env` = 83 files / 1190 tests passed. **Read from the built CSS, not reasoned from source** (`npx vite build`, `dist/assets/index-*.css`): the flourish gate really is attribute-presence only and the ambient gate really is two literal values. **A fourth site the finder missed**: `index.css:2473`/`:2481` use `:not([data-anim-level])` to scope the collector-card rarity frame to the headache tier — a third positional reading; `index.css:2275` documents the holographic foil relying on the same. Guard is writable: `device.test.ts:63-140` carries three stylesheet-reading parity guards (`readFileSync` over `index.css`) for #419, #251 and the `touch:` variant. **Residue, not restatement, against #961**: read in full, #961's item 3 covers three things all inside `decoration-motion.ts:7-8`'s docstring and names neither `theme.ts:118`, nor `index.css`, nor any CSS↔registry parity — it is a stale-comment sweep, this is an unguarded derivation |
| P17-18 | Mechanical | `src/db/repositories/item/status-filter.ts:37-46` and `:48-59`, plus five sibling sites | `ItemStatusFilter` and `ITEM_STATUS_FILTERS` are two definitions of one vocabulary, against the codebase's own idiom | confirmed (widened) | #1565 | `cosmetic`, latent. Union-only mutation names exactly two sites — `status-filter.ts(132,76)` TS2366 and `InventoryFilterBar.tsx(50,7)` TS2741 — and never the array; `STATUS_FILTER_FEATURE` is a `Partial<Record<…>>` so it is silent by design. Both filled: 799/800 files and 12447/12448 tests passed (the single failure was another verifier's concurrent file deletion), type-check clean across all three configs. **Consequence driven**: `buildStatusFilter(['unpriced'], ctx)` emits `CLAUSE=""` and `PARAMS=[]` — nothing at all — while the control emits its predicate; `status-filter.ts:183` iterates `ITEM_STATUS_FILTERS` and `continue`s on a status not in `selected`, so a selected status absent from the array is skipped **silently**, not thrown. The two circular tests: `ItemRepository.statusFilters.test.ts:331` is fully circular; `queries.test.tsx:117` is circular **but** `:113` and `:115` pin both halves against hand-written literals, so an addition *to the array* goes red there — only the union-only direction is unguarded. **P2-39 does not transfer** and the finder is right about that: that rejection was about a **prose comment** miscounting, read only by a human; here both definitions are machine-read and the divergence is demonstrable in emitted SQL. **Widened — the finder's premise is wrong.** "Everywhere else the repo derives the union from the array" holds only for the `as const` registries; all five siblings it lists are the same hand-written-union-plus-separate-array shape. All five widened at once and type-checked: `booking-status.ts(79,14)/(93,14)/(105,14)`, `CalendarScreen.tsx(99,7)/(107,7)` + `agenda.ts(422,14)`, `DashboardNav.tsx(46,7)/(60,7)/(74,7)/(87,7)`, `EventTypePicker.tsx(43,12)` TS1360 + `(89,16)` TS7053 — **identical outcome at every site: the compiler names the total-Record maps and never the order array**. `FIELD_DEF_PROMINENCE_MODES` produced no error at all, weaker still. Lead re-verified all six declaration sites. So it is a six-site pattern, not one exceptional site; `status-filter.ts` is simply the one whose failure mode the `Item model parallel lists` note already documents — and that note **warns** of the hazard without giving a reason for it, so it is not a sanctioning decision |
| P17-19 | Mechanical | `src/features/reports/report-csv.ts:37`, `:40`, `:46-52`, `:54-57` | The Reports CSV is a second unguarded serialiser, and its header row is not escaped at all | confirmed (merged with P17-27) | #1557 | `cosmetic`, latent. Raised independently by two finders and merged. Body-cell value matrix, real `cell` vs real `delimitedCell(v, ',')`: **all fifteen values the same**, `true`/`false` included. **Correction to both finders: booleans do not diverge** — `"true"`/`"false"` do not match `FORMULA_TRIGGER` (`^[=+\-@\t\r]`), so the `!isNumber` vs `typeof === 'string'` gap is empty across the seam's entire `TabularCell` domain; divergence needs a value that is neither string nor number whose `String()` starts with a trigger, which `TabularCell` forbids. All ten builders read (`:68-177`): every pushed value is a string, a number, `''` or `null`. **The header limb is the genuinely new half**: `report-csv.ts:56` is `header.join(',')` with no escaping, while `tabular-export.ts:74` runs every header through `delimitedCell` — `plain,value, with comma,has "quote"` parses as five fields against a three-field body. All ten headers are static lowercase ASCII today, so latent. **Substitution is decisive for CLAUDE.md rule 1**: rewriting `toCsv` as a wrapper over the seam's → 32 files / 528 tests passed; type-check then reported the payload (`TS6133 'cell' is declared but its value is never read`); deleting `FORMULA_TRIGGER` + `cell` + the unused `stripFloatNoise` import (**1,242 characters**) leaves type-check clean across all three configs and 12 files / 197 tests passed. So the copy is fully derivable with no behaviour change and rule 1 applies cleanly — rule 3 is not the answer, because nothing is left to hold together once the copy is gone. Closes the §11 Phase 12 carry-over |
| P17-20 | Mechanical | `bridge/src/low-stock-thresholds.test.ts:1-8` | The test's docstring claims a row-id and encoding drift guard that its own construction rules out | confirmed (narrowed) | #1555 (folded) | `cosmetic`. **Limb 1, the row id** — renaming `LOW_STOCK_QTY_FIELD`'s value: the bridge test stays **green** (3 passed, both sides moved through the same `settingRowId(…)` call) while `shared-low-stock.test.ts` goes **red** with `expected { qtyThreshold: +0, gaugePercent: 25 } to deeply equal { qtyThreshold: 7, … }` — note the silent revert to the default 0. **Limb 2, the encoding** — the finder asserted this without demonstrating it: the bridge test hand-writes `value: '6'` into `SettingsRepository.publish`, so `encodeSettingValue` never runs on that path; changing the write-side encoding only (`settings-sync.ts:143`) leaves the bridge test green and `shared-low-stock.test.ts` red. So of the three things the docstring claims to catch, **two are false** (row id, write-side encoding) and one is true (the table/migration, plus `decodeSettingValue` on the read side). **Not worth an issue on its own**: the module's substantive claim at `shared-low-stock.ts:25-29` is satisfied — the app-side test went red for both mutations, in exactly the way the docstring names — so no user, integrator or future maintainer is misled about whether **the system** is guarded. Only the second test's paragraph over-claims its own coverage, in a file the first test already covers: a two-line comment edit. Folded into P17-16's issue rather than P17-15's, because that one is also a settings/wire-contract parity claim about a docstring over-stating what a test pins |
| P17-21 | Prompt | `src/features/lifecycle/index.ts:1-16`; guard at `alerts-eager-graph.test.ts:104-113` | The app's only orphaned module, whose docstring calls it the feature's public surface | confirmed (reshaped) | #1569 | `cosmetic`. Orphaning verified independently of knip: no relative import, no `from '.'`, no dynamic `import()`, no `vi.mock`, no route-tree entry — the only two hits are the guard test's own docstring and describe title. `rm src/features/lifecycle/index.ts && npx tsc -b --noEmit` → exit 0. History exact: `git show d610c937 -- src` piped through `rg "^[-+].*@/features/lifecycle'"` → **six removals, zero additions**, and `git log -S` shows `d610c937` as the last commit that ever touched a barrel importer. **Sub-limb tested as asked**: adding a `./components` re-export and rebuilding produced **no new chunk and no chunk gaining a byte** of lifecycle-component code, yet the guard fires (`expected [ './components/AuditDayDialog' ] to deeply equal []`) — so that assertion's failure mode is disconnected from the regression it names; and deleting the barrel fails the same test on ENOENT rather than on behaviour. The second describe (the import-graph walk from `useAlerts`) is the live guard and passes throughout. **Ongoing cost demonstrable**: two commits have maintained the dead file since it died — `bac9d35b` reworded its docstring and `53b4e32b` (#684) added `effectiveExpiryDate` to a re-export list nothing reads. **Three corrections to the finder**: (1) `d610c937` did not rewrite the header to call it the public surface — line 2 is unchanged context in that diff and predates it; what `d610c937` added is the convention instruction, and the current wording is `bac9d35b`'s. (2) "#680's closing comment still describes the barrel as the hooks' import point" is **false** — that comment is accurate. (3) Severity is honestly `cosmetic`: following the docstring produces correct behaviour; the harm is a 44-line dead surface that reads as live, plus a false property in #377's record |
| P17-22 | Prompt | `bridge/src/events/model.ts:132-146` | An 11-name re-export block justified by bridge call sites that no longer exist | confirmed (narrowed) | #1569 | `cosmetic`, marginal. Deleting the nine: `tsc --noEmit -p bridge/tsconfig.json` exit 0; bridge suite 79 files / 1191 tests passed. **The finder's stated consequence is wrong on its own terms** — "two equally-plausible import paths whose drift is integrator-visible": a re-export and its source resolve to **one definition**, so drift is impossible by construction, and `openapi.ts` imports `KNOWN_EVENT_TYPES` from the app module anyway. That limb is rejected. **The sharper truth: the block's rationale never applied.** At the commit *before* the one that wrote it, exactly **two** of the eleven names had a bridge call site to preserve — precisely the two still live today — and `openapi.ts` was switched to the direct import in that very commit. So this is not "call sites that no longer exist"; it is a block that was over-broad from birth, under a sentence a reader takes as asserting bridge call sites that never existed. Lead re-traced every import: only `eventTypeForAction` (production, via `feed-model.ts:22`) and `EVENTS_TRUNCATED_TYPE` (two test files) are pulled from `./model`; the other nine occurrences in `bridge/src` are inside `model.ts`'s own internal import block. Above §1's bar only just — §1 excludes taste, not a false statement about the code, and the finder's own excluded tier is dead pass-through re-exports *with no false factual claim* |
| P17-23a | Prompt | `src/lib/env/feature-detection.ts:99-108`; claim at `src/features/scanner/nfc-reader.ts:47` | `hasNfc` is dead, while `nfc-reader.ts` says callers use it to gate the UI | confirmed (narrowed) | comment on #1268 | `cosmetic`. `rg` over the whole tree returns exactly two lines: the definition and the false claim. No caller does — `useNfcScan.ts:55` and `useNfcWrite.ts:49` both compute `supported = getNdefReaderCtor() !== null`, and `QrCodeDialog.tsx:89` / `ScannerOverlay.tsx:364` gate on that. It is the only predicate of the fourteen in `feature-detection.ts` with no call site. Deleting all four dead symbols together: `npm run type-check` exit 0 (app + bridge + extension), bridge suite 79/1191 passed. **The "strictly weaker gate" sub-claim is narrowed to unreachable**: the divergence exists in principle (`present-but-undefined` and `present-but-not-callable` give `hasNfc` true with a null ctor) but `NDEFReader` is a `[SecureContext, Exposed=Window]` interface — a real browser exposes it as an interface object or not at all, so reaching the divergent state needs a test double or a shim. **No shipped browser reaches it.** Report the false docstring, not the weaker gate. Filed as a **sixth item on #1268** rather than separately: #1268 is explicitly scanner-scoped, labelled documentation+scanner, cosmetic, and `nfc-reader.ts:47` is scanner text asserting behaviour the code does not have — same subsystem, class, shape and severity |
| P17-23b | Prompt | `bridge/src/homeassistant/scale.ts:42-46`; `src/db/db-storage.ts:27-28`; `bridge/src/config.ts:100-104` | Three more dead constants whose docstrings state that something consults them | **rejected** | — | All three are dead, but **none makes a false claim**, so the consequence does not follow. `WEIGHT_DEVICE_CLASS`: its docstring says the marker's "absence is not disqualifying (see `isScaleEntity`)" — an accurate description of `isScaleEntity`, and `scale.ts:136-137` records the unit-not-device_class decision deliberately; nothing claims anything consults it. `SAHPOOL_VFS`: the docstring is a fact about the library ("as registered by `installOpfsSAHPoolVfs`"), not a claim of consultation; `sqlite-bootstrap.ts:167` reporting `pool.vfsName` is arguably better than a local literal, and the string is only ever displayed (`widgets.tsx:704,782`), never compared, so no drift is possible. `LAN_HOST`: `config.ts:101`'s sentence is substantively true — `0.0.0.0` **is** the opt-in value, set through `GUBBINS_BRIDGE_HOST` — only the `{@link}` is unearned. They are cosmetic dead code across three unrelated subsystems, below the bar the finder itself applied to its own grouped sub-threshold tier, and #1268's own closing note places dead-module removal with the repository's existing dead-code housekeeping rather than with that issue |
| P17-24 | Functional | `src/features/about/diagnostics.ts:158-169` vs `src/lib/format.ts:341-347` | Two byte formatters render the same `storage.estimate()` figure differently on two translated screens | confirmed (reshaped) | comments on #956 and #228 | `cosmetic`. Real `Formatters.bytes` via `makeFormatters` vs the real `formatBytes` imported from `diagnostics.ts`: 1 500 → `1.5 kB` / `1,5 kB` / `1.5 kB`; 45 200 000 → `45 MB` / `45 MB` / `45.2 MB`; 123 456 789 → `123 MB` / `123 MB` / `123.5 MB`; 2 147 483 648 → `2.1 GB` / `2,1 GB` / `2.1 GB`; 999 999 → `1,000 kB` / `1.000 kB` / `1000.0 kB`. The #956 limb reproduces, and **#956's body scopes itself to `format.ts:341-346`/`:379-390` and lists its render sites as "the backup dialog, the storage-usage widget, and the import size-versus-limit message" — About is not among them**, so its fix leaves this second copy defective. **Reshaped on three points, one of which changes the fix**: (1) `StorageTriageDialog.tsx:194` is **not** a translated surface — that dialog is hard-coded English — so only the Dashboard widget supports the side-by-side claim; (2) **the naive fix would break a recorded decision** — `formatFieldValue` deliberately serves both the localized card and the English clipboard/GitHub payload (`diagnostics.ts:7-19` "deliberately English and stable so a bug report reads the same for the maintainer regardless of the reporter's UI language"; `:90-95` "Keeping the words out of `formatFieldValue` is what lets one formatter serve both"), so routing it through `fmt.bytes` would make the payload locale-dependent; the correct shape is to **inject** a byte formatter alongside the existing `DiagnosticVocab`, exactly as the words are, and the same gap exists for the six entity counts at `:219`; (3) severity is `cosmetic`, not `degraded` — no figure is wrong, only its rendering differs. Filed as a comment widening #956 plus a correcting comment on #228 (whose body asserts "Everything else formats correctly through `lib/format.ts` / `useFormatters`"), following the #962 item-4 precedent |
| P17-25 | Mechanical | `src/features/inventory/labels/label-sheet.ts:479-484`; `components/QrCodeDialog.tsx:309-314` | `escapeHtml` defined twice, byte-identically, in two modules that both print a user-typed item name | confirmed (narrowed) | #1566 | `cosmetic`, latent duplication. Bodies character-identical bar the `export` keyword. Substitution (delete the private copy, import the exported one): type-check clean across app/bridge/extension, 6 files / 161 tests passed. Drift mutation (harden the **shared** copy with a backtick entry, leave the copy alone): 5 files / 147 tests passed — **the hardening does not reach the QR dialog and nothing goes red**. **Narrowed**: all uses are text-content contexts (`label-sheet.ts:492`; `QrCodeDialog.tsx:135,143,146,147` — `<title>`, `<h1>`, two `<p>`), the inputs are an item name, a generated shortId and an app URL, both escape the complete set for text content and attribute values, and neither writes into a URL, JS or CSS context. **There is no character today that one escapes and the other does not.** What earns it a filing is the precedent: #257 (closed 2026-07-24) fixed `features/export/download.ts`; #646 (closed 2026-08-23) fixed the same two hazards hand-rolled in `QrCodeDialog.tsx`, and its body says so in as many words — "That change touched the shared seam only, so this hand-rolled copy still carries both hazards." Same pair of files, same shape, second time |
| P17-26 | Mechanical | `src/features/export/download.ts:17-31` vs `src/lib/download.ts:12-25` | Two object-URL downloaders, one documenting itself as "the single" one, with two different answers to the Firefox revoke race | confirmed (severely narrowed) | #1569 | `cosmetic`. **The technical contradiction is DISPROVED, from the cited source.** Bugzilla 1282407 fetched: "Deferring via `setTimeout(…, 0)` works… however, instantly-fulfilled promises do NOT provide sufficient delay. STATUS: RESOLVED FIXED (mozilla50)." So 0 ms is sufficient **by the bug's own workaround list**, and the bug has been fixed in Firefox since v50. 10 s is not "what the hazard needs" — it is merely more conservative. The finder's "both cannot be right" is false, and its corollary ("if 10 s is what the hazard needs, #257's fix to every export is under-specified") is disproved. Substitution is green: 6 files / 89 tests passed. History exactly as claimed, plus one commit the finder missed: `git show a6e4e818 -- src/lib/download.ts` adds **only** the docstring; the body is byte-unchanged since Phase 1, so the "never held together" claim holds. Both attach the anchor before `click()` and both defer the revoke past the synchronous tick, so **both satisfy both of #257's guarantees** — which #646's body already asserts in terms. The synchronous `anchor.remove()` is not the hazard either: #257's first guarantee is about clicking a never-attached anchor. The "pins a blob URL for ten seconds" cost is near-nil. **What survives is only the parity claim**: `features/export/download.ts:2` calls itself "The single browser-download side-effect shared by every export path… never a hand-rolled copy", the `Tabular export seam` note repeats it verbatim, and it is not single. Already settled elsewhere: U5-6 records that the save-before-destroying rule governs paths that destroy and that `download.ts` scopes itself the same way; #502 (closed) routed the destructive paths through `lib/save-file.ts`. The finder's fix direction is **backwards** — `src/lib` importing from `src/features` is a layering inversion; the canonical helper belongs in lib. New, but weak — folded rather than filed alone |
| P17-27 | Mechanical | `src/features/reports/report-csv.ts:37`, `:46-52`, `:54-57` vs `tabular-export.ts` | `report-csv.ts` is a second CSV serialiser that can derive from the seam with no behaviour change | confirmed (merged with P17-19) | #1557 | See P17-19 — the two candidates are the same finding, raised independently by two finders, and were merged before verification. Answers the §11 Phase 12 open question at the plan's line 3015 in the affirmative: substitution is behaviour-preserving, so CLAUDE.md rule 1 (delete one) applies and rule 3 is unnecessary |
| P17-28 | Functional | `src/lib/bridge-url.ts:18`, `:28`; consumers `webhooks/bridge-client.ts:146`, `inventory/scale-reading.ts:102`, `sync/bridge-build-check.ts:48-51` | One stored `bridgeUrl` is resolved by two rules, so a URL Push accepts makes Webhooks and the scale panel unreachable | confirmed (reshaped) | comment on #941 | `degraded`. Six consumers of the one `usePreferencesStore.bridgeUrl`, stored `http://h:8787/api/v1/snapshot`: Push resolves correctly; the build check, both webhook paths and both scale paths all double the prefix. Controls: `http://h:8787` and `http://h:8787/` both resolve correctly for all six. **Driven against the real bridge** (the pinned SHA extracted with `git archive` into a scratch tree, immune to concurrent mutations; real `serve.mjs`, seeded token, `ALLOW_PUSH=on`, `WEBHOOKS=on`): stored BASE → push 200 and webhooks 200; stored FULL → **push 200, webhooks 404, scale 404**, against a bridge with webhooks demonstrably on. **Reshaped — the finder's consequence is wrong and the truth is worse.** It said Webhooks reports `bridge-unreachable`; it does not. `bridge-client.ts:198-206` maps 404 → `not-enabled`, and the comment two lines above states the assumption outright: "a 404 on these paths means 'webhooks are switched off here' — NOT 'wrong URL'." So the user is told (`en.json:1861`) "Webhooks are switched off on your bridge… Turn them on in the bridge's configuration" — a specific, confident, **false** diagnosis sending them to change a setting that is already on. The app explicitly rules out the wrong-URL explanation, and this is precisely the case that breaks that reasoning. Scale limb confirmed at `ScaleReadPanel.tsx:139-155`: on a first listing `listed.current` is false, so a failure runs `setEntities([])` and the panel vanishes with no message. **Widened**: a fourth consumer the finder missed — `bridge-build-check.ts`'s `resolveIndexUrl` is idempotent only against `/api/v1`, so it also 404s and silently yields no opinion; **push is the only one of the four that works**. Mitigation acknowledged: `SyncScreen.tsx:821`'s hint reduces likelihood and is why this is not rated higher — but `push-to-bridge.ts:44-47` **declares the full-endpoint form supported** ("so pasting either the base or the full endpoint works") and `push-to-bridge.test.ts:29` pins it. **Added to #941 rather than filed**: same module, same four call sites, same misdiagnosis vocabulary — but #941's mechanism is a query/fragment under which all four break uniformly, and its own "Controls passed" section explicitly records that a **path**-carrying base joins correctly, so it scopes this case out. Decisive for the fix: #941's `new URL(path, base)` suggestion resolves both, while its "reject a query/fragment" suggestion would leave this entirely unfixed — which is what the comment says. Also noted on #941: its link to `src/features/scanner/scale-reading.ts:102` has moved to `src/features/inventory/scale-reading.ts:102`. Not settled: the scale run used `GUBBINS_BRIDGE_HA=off`, so its 404 in that run is the capability probe; the doubled-path 404 is proven for the scale endpoints at the unit level and by the webhooks row under identical conditions |
| P17-29 | Mechanical | `src/features/export/export-data.ts:891-894`, used at `:819`; seam at `tabular-export.ts:171-176` | Two Markdown-table cell escapers that already differ on CRLF | confirmed (widened) | #1556 | `wrong-data`. One-character difference confirmed: `/\r?\n/g` in the seam vs `/\n/g` in the copy. **The finder's stated weak limb is stronger than it thought**: there is a live, non-import, non-sync ingress — the **bridge write API and the MCP tool**. `bridge/src/api/v1.ts:745` reads the POST body's `note` through `readOptionalString` (`:832-840` — a **type check only, no normalisation**), `bridge/src/write.ts:226` passes it to `items.adjustQuantity`, and `src/db/repositories/item/stock.ts:365-368` hands it to `historyStatement`, which binds it into `INSERT INTO item_history` untouched; `bridge/src/mcp/tools.ts:547` is the same, stated as type-only by its own docstring. Nothing anywhere normalises line endings. Driven end to end against the real `:memory:` driver and the real `ItemRepository`: stored note `"used one\r\nused one more"` → vault activity `\| 2026-09-04 \| QUANTITY_CHANGE \| used one\r used one more \|`. And the corruption is real in a GFM renderer: `escapeCell` produces a **fabricated row** whose date column holds the second half of the note, where `markdownCell` produces one correct row. CommonMark §2.1: a bare CR is a line ending. Wrong-but-present data in a durable artefact the user keeps → `wrong-data`, above the finder's implied severity. **The `h.action` limb is rejected**: the finder's premise is wrong — `HistoryAction` is a **closed** `as const` union (`constants.ts:218-219`), not "a deliberately open union preserving unknown values". `item_history.action` is free text with no `CHECK`, so an unknown action from a newer peer would flow through unvalidated, but every member is a SCREAMING_SNAKE identifier and any future one will be, so no realistic value contains `\|`, `\\` or a newline. Rejected on consequence; at most one sentence in the issue as a robustness note. Correction to the finder's framing: #974 is **not** "the same mechanism" — it is the in-app renderer splitting on an escaped pipe, a different module and cause. Cite as a sibling |
| P17-30 | Prompt | 30 files / 38 lines naming `jsdom`; the five falsifiable ones at `hit-test.ts:7`, `item-drag.test.tsx:14`, `:54`, `useBoardPointerDrag.test.tsx:11`, `LocationSidebar.test.tsx:762`, `diagnostics.ts:273` | Comments name jsdom as the test environment; jsdom has never been a dependency, and five make false API claims | confirmed (narrowed) | #1568 | `cosmetic`. `grep -n 'jsdom' package.json` → no match; `git log -S 'jsdom' -- package.json` → **empty: no commit ever added or removed it**; `git log -S 'happy-dom' -- package.json` → `cf6dbbc8` (the first commit), which also set `environment: 'happy-dom'`; `ls node_modules/jsdom` → no such directory. jsdom appears in the lockfile only as an optional **peer** declaration of Vitest and is never resolved. Scratch test under the real config falsified all four API claims (`elementFromPoint` is a function returning `null`; `PointerEvent` is a real constructor carrying `pointerId`; `matchMedia` is defined), and the lead re-ran the same probe directly against happy-dom 20.11.2 with the same result. **The consequence is demonstrated, not hypothetical, and #781 is the proof**: `segment-layout.ts:85` says "Back to jsdom's own answer: no layout, every rect zero", and its `restoreSegmentLayout()` defines a **second permanent stub** instead of deleting the first — the false belief that the environment has no answer of its own is what caused an author to write one. **Narrowed**: #781's body already names `segment-layout.ts:2`, `:4`, `:85` as "three comments naming a testing environment that is not the one in use", so those 3 lines are filed; scope is therefore **38 lines / 30 files**. Also: the majority of the 41 make a claim that is behaviourally **true** (happy-dom really does lay nothing out) and wrong only in naming. Not a legacy leftover — the comments were introduced continuously from 2026-06-30 to 2026-08-29, one batch landing the day before this plan was written, so it is a live propagating convention; `eslint.config.js:189` names happy-dom and 47 files mention it, so the codebase asserts both. Count corrected from §11's "about 30 files" to 41 lines / 31 files. Resolves the §11 Phase 1 carry-over. Precedent #1222 |
| P17-31 | Prompt | `safe-mode-actions.ts:329`; `feature-detection.ts:12`; `StorageBanners.tsx:25`; `types/categories.ts:6`; `types/locations.ts:96-97`; `v1-initial.ts:2448` | Six comments in shipped source promise capabilities "in Phase N" for phases that had already shipped | confirmed | #1569 | `cosmetic`. All six blamed, all six forward-tense, **none in a dated record**: #1 `cf6dbbc8` 26-06-27 → shipped `3a42a461` 26-06-29; #2 → `f8a74875` **same day**; #3 → `206dfb39` **same day**; #4 written `6ab68420` 26-06-29 → the promised thing shipped `cd1844b7` 26-06-27, i.e. **already shipped when written**; #5 → `2696436d` **same day**; #6 same. **#6 is contradicted by the repository itself**: `LocationRepository.ts:501` sets `usable_volume`, `:505` `packing_factor`, plus the INSERT at `:330` — the columns do not "sit NULL". Locality claim holds exactly: `LocationAdvancedVolumeFields.tsx` was added by `2696436d` "(#457, phase 2)", its own head reads "The Advanced disclosure in the location dialogs (issue #457, Phase 2)", and it is imported and rendered by `CreateLocationDialog.tsx:25,276` and `EditLocationDialog.tsx:51,373` with catalog keys at `en.json`/`de.json` 1322,1323,1353. **CLAUDE.md's shield does not apply to any of the six**: the rule protects **dated records**, and all six are JSDoc or inline comments on live exported symbols carrying no date and no phase heading; `grep 'arrives in Phase 7' *.md` → nothing; none is a handover entry. The distinction was tested for all six and none dropped. Sharpest: #4 says "schemas/custom fields are Phase 3" while `FieldDefRow`, `CategoryField` and `CreateCategoryFieldInput` sit **204 lines below it in the same file**, backed by four real tables and four editor components. #1467 is the plan-doc sibling and is explicitly outside `docs/todo` — cross-reference, not a duplicate |
| P17-32 | Mechanical | `src/features/alerts/alerts.test.ts:386-403`; seam at `alerts.ts:452-465` | The test's only assertion sits inside an `if`, and its fixture makes the sort rule it names unreachable | confirmed (reshaped) | #1571 | `degraded`. `NOW = ms('2025-07-01')`; `buildMaintenanceDueAlerts` (`alerts.ts:313-338`) sets `overdue = dueAtMs != null && dueAtMs < now` and `severity: overdue ? 'critical' : 'warning'` at `:328`, so `t1` (2025-06-15) grades **critical** and `u1` (`dueAtMs: null`) grades **warning**; the comparator returns at `:454` on `SEVERITY_RANK` and the nulls-last branch at `:457-461` is never reached for this pair. **Mutation 1** — invert the nulls-last rule: this test green (66/66), alerts+calendar+dashboard green (34 files/480 tests), **the entire app suite 1 failed / 799 passed, 12447 of 12448 tests passed**, and the single failure is the known `CategoryLookupPanel.test.tsx` load flake, green in isolation on clean code. **So nothing anywhere in 12,448 tests pins the nulls-last tiebreak.** **Mutation 2a, the decisive one** — keep the assertion, drop the guard, and add the assertion the test's own name makes: `AssertionError: expected 'critical' to be 'warning'` at `:402`, with the two `toBeDefined()` assertions above it passing. **Mutation 2b** — the finder's exact second mutation, on unmutated production code: green, for the wrong reason. **Reshaped on two points**: (1) the vacuity limb is real but **currently non-biting** — with this fixture both `find`s always succeed, so the guard is not reachable-as-false today and is latent robustness debt, not the operative defect; **the fixture is**; (2) the finding is **wider** than one bad test — `buildLowStockAlerts` emits `severity:'warning', dueAt:null` unconditionally and a not-yet-overdue maintenance alert is warning-with-a-date, so nulls-last genuinely decides the Alerts list order for every user with both, and that whole rule has zero coverage repo-wide. Severity differentiator applied from the register's own precedents: `cosmetic` where the unguarded behaviour is covered elsewhere, `degraded` where it is not and is user-visible. Not a duplicate: #816 is the same shape in a different file and enumerates only three item-repository parity claims; #984 names `tooltip.test.tsx` and `currency-select.test.tsx` |
| P17-33 | Mechanical | `vitest.timeouts.ts`; `src/test/setup.ts`; `vite.config.ts:353` | Testing Library's `waitFor` keeps its 1 s default while the per-test budget was raised to 15 s for exactly the load reason the awaits also face | **rejected** | — | **Every factual limb is true and was re-verified; the consequence does not follow, and the proposed remedy is disproved by measurement.** The facts: `DEFAULT_TEST_TIMEOUT_MS = 15_000`, applied at `vite.config.ts:353`; `configure(` appears nowhere under `src/`, so `asyncUtilTimeout` keeps `@testing-library/dom@10.4.1`'s 1000 ms default (probe: `CEILING=1000 ELAPSED_MS=1012` under the shipped config, `CEILING=15000 ELAPSED_MS=15009` with it raised); 514 `waitFor(` + 190 `findBy*(` = 704 awaits across 76 files, of which exactly three raise the timeout locally, all in `tooltip.test.tsx` at 2000 ms and only because that component's own delay is 1000 ms; and `scripts/flake-retry.mjs:21` really does match one exact fingerprint (`COLD_START_FLAKE_SIGNATURE`), so a `waitFor` timeout is never retried. **The mechanism reproduces.** Under two full suites looping as competing load, the most exposed file (`CategoryLookupPanel.test.tsx`, two chained `waitFor`s in its `reachReview` helper) failed **4 times in 20 runs** at the shipped ceiling — at 1029, 1042, 1048 and 1072 ms, a **different test each time**, which is itself the tell for load rather than logic — and each failure surfaced as `TestingLibraryElementError: Unable to find an element by: [data-testid="lookup-review-apply"]`, i.e. as an assertion failure rather than as load, exactly as claimed. **Raising the ceiling changes nothing.** Re-run under the same load at two higher ceilings: **5 s → 2 failures in 14 runs**, at 5032 and 5050 ms; **15 s → 2 failures in 14 runs**, at 15009, 15009 and 15020 ms. Against 4 in 20 at 1 s, the rate is flat within noise, and **every failure consumes the entire ceiling** whatever it is set to. The reason is visible in the per-test timings: across both raised arms the **slowest passing test was 209 ms**. There is no population of awaits settling between 200 ms and 1000 ms for a bigger ceiling to rescue — an await either resolves almost immediately or never resolves at all — so the ceiling is not the binding constraint. **The proposed change would make things worse in two ways**: each failure costs 5 s or 15 s instead of 1 s, and at 15 s the raised ceiling equals `testTimeout`, so the `waitFor` can no longer report its own error — the test budget wins the race and the run reports a bare `Test timed out` instead of naming the element that never appeared. This is the outcome an earlier, interrupted verifier pass reported as "the failure rate is flat across 1s/5s/15s ceilings", now demonstrated rather than asserted. **Recorded so a later phase does not re-derive it**, and with one genuine question left standing that is *not* this candidate: something in that file's mocked-fetch chain occasionally never settles under load. That is a different defect from the one claimed here; it is not established (48 runs is a small sample, the file is green in isolation every time, and the memory note `App test preexisting flake` already records this file as a known load artefact); and it belongs with the flake itself. Dedupe: no occurrence of `asyncUtilTimeout` or of `waitFor` as a finding in §10; no match across all 1522 issues for `waitFor` / `asyncUtilTimeout` / "default timeout" / "passes in isolation". #742 is the worktree config's docstring and the lost cold-start retry — a different mechanism |
| P17-34 | Mechanical | `catalog-import.test.ts:1578`; `clone.test.ts:186-200`; `contacts-export.test.ts:16` | Three fixtures in the middle third claim a type they do not satisfy | **rejected** (merged into P17-36) | — | Merged with P17-36 before verification and rejected with it. All three re-checked and **all three are inert**, exactly as the finder itself said: `inferColumnMapping` reads only `def.name`/`def.id`; `clonedFieldValues` reads only `id`/`value`/`hasStoredValue`; and `contacts-export.ts` contains **zero** references to `borrowerType` (grep exit 1), so `'CONTACT'` versus `'contact'` never reaches a comparison. No assertion passes for the wrong reason today. The `clone.test.ts:186-200` factory emits **no diagnostic at all** — the `as ResolvedItemField` cast launders it — so it is outside the measured sweep entirely and is #783's shape rather than #1042's |
| P17-35 | Mechanical | `src/db/repositories/ItemRepository.phase4.test.ts:114-120`; type at `types/pagination.ts:13-24`; code at `item/aliases.ts:165` | The test compares `undefined` with `undefined`, because `Page<T>` has no `total` | confirmed | #1571 | `cosmetic`. `Page<T>` declares `rows`, `limit`, `offset`, `hasMore`, `startCursor?`, `endCursor?` — no `total`; `getHistory` (`item/core.ts:831`) returns exactly that. Mutation (delete `aliases.ts:165`, so `applyScrape` always appends a `SCRAPE_APPLIED` row): the file 10/10 passed and **the whole app suite 800 files / 12448 tests passed**. Corrected assertion `expect(after.rows.length).toBe(before.rows.length)` passes on clean code and, with the same mutation, goes red (`expected 2 to be 1`) — the two-sided proof. Independently corroborated by a second scratch run that wrote a genuine extra history row: the `rows.length` check saw it and the shipped `.total` assertion was still satisfied. **Bounded**: the test is not entirely dead — making the no-op path *throw* still turns it red; what it cannot catch is the one thing its name states. **Only guard confirmed**: every `applyScrape` reference checked; `name-fold-coverage.test.ts:87-88` calls it with a non-empty `aliasAdditions`, so the early return is never exercised there. **Why the slip is plausible, and it bounds the sweep**: `.total` **is** a real property on a sibling read shape in the same repository — `RelevanceSearch` (`item/search.ts:118-124`) — and a sweep of every `.total` access in `src/` and `bridge/` found this is the **only** one taken off a `Page<T>`. Line corrected from the finder's `:166` to `:165`. The contract at `aliases.ts:101` is unguarded in both halves: nothing asserts the returned item is unchanged either. #812 is the same class but enumerates `batched-item-reads.test.ts` ×4, `unlimited-integration.test.ts` and `item/gauge.ts`, and is scoped to guards that stay green when a property is **deleted**; this is a **phantom property**, so it goes in the new sweep rather than as a seventh item there |
| P17-36 | Mechanical | 170 test files repo-wide; gate at `tsconfig.app.json` | A sweep of fixture sites declaring a type they do not satisfy, in a tree no TypeScript project compiles | **rejected** | — | The gate claim is **true** and the measurement mostly reproduces, but the finding does not survive. **Gate proven**: `npx tsc -p tsconfig.app.json --noEmit --listFiles` → 1557 files, **0** test files and 0 `src/test` files, against 798 test files on disk; `tsconfig.node.json` covers four files and no tests; `bridge/tsconfig.json` **does** include its 79 bridge tests. **Measurement re-run** at this SHA with an equivalent probe: 861 diagnostics across 200 files (197 of them tests). The three clusters the finding rests on reproduce **to the digit** — 65 missing-required (TS2739/2740/2741), 16 excess-or-misnamed (TS2353/2561/2820), 9 real property reads (TS2339), 434 excluded possibly-undefined — but the aggregate does not (861 vs the finder's 1043; 197/155 files vs its 170), and that gap could not be accounted for. **Rejected on four grounds.** (1) **The mechanism is a thrice-rejected duplicate**: §10 U2-1 records it as `duplicate` of #601 and a "third re-finding", and U6-9 → #1180 adds "the type-check limb is a recorded duplicate and must not be re-filed"; this would be the fourth. (2) **Both advertised consequences are inert.** ScannerOverlay: the component was read end to end (1015 lines) and **never reads `item.barcode` or `item.serialNumber`** — barcode resolution is a repository query (`findByBarcode` at `:290`), and the one child receiving the item is mocked to `() => null`; the scanner resolves *codes* to items and never reads an item's stored barcode. The five sync snapshots: `forceLwwTies`, `shiftSnapshotTimestamps`, `buildCloneStatements` and `reconcile` all read the omitted sections as `?? []`, and `reconcileEdgeMembership` explicitly declares them `readonly E[] or undefined` — so the production code is **written to tolerate a snapshot missing these sections**, which makes `SyncSnapshot`'s non-optional declaration the looser end of the mismatch rather than the fixtures. (3) **Two named sites are already register-rejected** (U2-1a, U2-1b) and the ReportsScreen sites are owned by #1180's parked type limb; the `Item`-fixture cluster is covered by **#1042's own suggested direction**, which already generalises to "ten test files across `src` each hand-roll their own `Item` fixture… a single exported test factory would close all ten at once". (4) **75% of the largest excess-property cluster is a mis-diagnosis**: 12 of the 16 are in `src/db/rpc/worker-driver.test.ts` and the **fixtures are correct** — see P17-43. One point in the finder's favour, under-sold: `PurchaseOrdersScreen.test.tsx:48,241,587` carry `satisfies PurchaseOrderWithLines` clauses that fail (TS1360 ×3), so an author wrote a compile-time check the build never evaluates — but that *is* the gate gap, which is the duplicate. Consequence checked anyway: `PurchaseOrdersScreen.tsx` reads `po.currency`/`po.supplierId` behind `?? undefined`, a legal state. Inert |
| P17-37 | Mechanical | `package-lock.json` (`browserslist@4.28.5`, `fast-uri@3.1.5`); baseline row at the plan's §12.1 | The `npm audit` gate has regressed from 0 vulnerabilities to 2 high, both fixable by a lockfile refresh alone | confirmed | #1561 | `degraded`. Verified by the lead directly rather than taken from the finder. `npm audit` → 2 high: `browserslist <=4.28.6` (GHSA-c83g-rgw3-j3cx, GHSA-73wf-gq98-2v4g) and `fast-uri 3.0.0-3.1.5` (GHSA-5jgf-p345-68v8, GHSA-f65p-4m7j-42xc, GHSA-fph4-wmhf-6fwf, GHSA-jqff-g426-hqxp). `--package-lock-only` gives the identical `{high:2,total:2}` over `{prod:53,dev:596,optional:82,peer:8,total:649}`, so it is the lockfile's answer, not a stale install; `--omit=dev` gives 1 (fast-uri only). **Reachability traced, not assumed**: `browserslist` is dev-only (`@tanstack/router-plugin` → `@babel/core` → `@babel/helper-compilation-targets`, and `vite-plugin-pwa` → `workbox-build` → `@babel/preset-env` → `core-js-compat`). `fast-uri` is declared production via `@hookform/resolvers@5.9.1` → `ajv@8.20.0`, **but the app imports only the `/zod` subpath** (5 call sites) and `node_modules/@hookform/resolvers/zod/dist/zod.mjs` imports exactly `@hookform/resolvers` and `react-hook-form` — no `ajv`; grepping the built assets for `ajv` returns nothing. **So no shipped code is affected**; the exposure is the build and dev toolchain. **Both fixable inside the declared ranges** — `ajv` declares `fast-uri ^3.0.1` (3.1.6/3.1.7 exist), `@babel/helper-compilation-targets` `^4.24.0` and `core-js-compat` `^4.28.1` (4.28.7/8/9 exist) — so **no `package.json` change**, which places it outside #186/#401 and inside an `npm run lock` refresh. Second limb, filed with it: `grep -rn "npm audit" .github/workflows .githooks scripts` → **no hits**, so nothing would ever have surfaced this. Baseline updated in §12.1 with this SHA. Dedupe: `audit`/`vulnerab`/`advisory`/`CVE`/`security`/`browserslist`/`fast-uri`/`GHSA`/`high severity`/`dependenc` across all 1522 issues → no hit |
| P17-38 | Mechanical | `eslint.config.js:22-24`; live sites `GroupedItemList.tsx:355`, `:362`, `:417` | The `no-misused-promises` JSX exemption is justified for event handlers but silences every JSX attribute | **rejected** | — | Probe reproduced exactly: 25 hits, 22 event handlers, 3 non-handlers. Mutation (`fetchPreviousPage: () => Promise<unknown>` at `:513`), plain `npm run lint`: fires at `:545` (`no-floating-promises`), **silent at `:355`** — so the finder's mechanism is real. Rejected on three counts. (1) `readonly fetchPreviousPage: () => void` **is not a lie** — it is TypeScript's own idiom for "give me a function; I ignore its result", the same shape React's own handler types use; declaring it otherwise is a style preference. (2) `attributes: false` is **the documented typescript-eslint setting for React codebases** and is what the config comment is reaching for; the comment naming only "event handlers" overstates its scope, which is a comment nit, not a defect. (3) **The finder's sharpest supporting argument is wrong**: `SectionPager:448` writes `onClick={() => void fetchNextPage()}` where `fetchNextPage` is **already typed `() => void`**, so the `void` operator there is a no-op and is not evidence the author knew a promise was in play — "the two receivers disagree, one author-aware" collapses. No live rejection is possible either: TanStack's limit verified in `node_modules` (`queryObserver.js:177-187` does `promise = promise.catch(noop)` when `throwOnError` is unset), and `:545` calls it with no options, so the returned promise cannot reject. #601 (closed) confirmed via its full body and both comments to discuss only rule **scope**, never rule options — the string `attributes` appears nowhere in it |
| P17-39 | Prompt | `src/features/purchasing/PurchaseOrdersScreen.tsx:588-591` | A lint suppression's own justification argues for the fix it did not apply | **rejected** | — | Deleting the disable and adding `f` to the deps: ESLint exit 0 with no output, purchasing suite 15 files / 238 tests passed. The stability claim is **verified true** — `format.ts:409-422` is a process-wide `Map` cache keyed on locale, currency, weight, dimension and volume — so the comment is factually correct and merely **operationally inverted**. Rejected: zero user consequence, and the effect's behaviour is byte-identical either way (a preference change re-runs it, finds `currentReceived === prevReceivedRef.current`, and announces nothing). Considered as a "lint hygiene" sweep with P17-3 and dropped for the same reason: two zero-consequence nits do not clear §1's bar. Note that this is **not** a false-claim finding — the claim is true, which is why it does not join the docstring sweep in #1569 |
| P17-40 | Mechanical | `src/features/sync/providers/google-oauth.ts:143-145`, called from `:200`, called from `src/main.tsx:22` | A full `localStorage` on the Google sign-in return throws out of module top level, so the app never mounts | confirmed (reshaped) | #1553 | `unusable`. Found by the lead, from an adjacency Finder 2 declined to file. Scratch test: `expect(() => completeGoogleAuthRedirect(1_000_000)).toThrow(/quota/i)`; calls === 1; hash === `#/sync`. **Driven in the built app** (`vite preview`, real Chromium, OAuth fragment + matching `sessionStorage` state, `localStorage.setItem` refusing only the token key): control healthy → `reactMounted:true, fallbackGuidePresent:false, tokenStored:true`; quota → uncaught `QuotaExceededError`, `reactMounted:false`, **`fallbackGuidePresent:true`, `fallbackHeading:"Gubbins needs to be launched, not opened directly"`, `tokenStored:false`**. `storeGoogleToken` has exactly one caller, which has exactly one caller — a bare top-level statement in `main.tsx:22` above `createRoot` at `:66` — so there is no safe path through this write; grep for `onerror`/`unhandledrejection`/`addEventListener('error'` across `public/`, `index.html`, `main.tsx`, `App.tsx` → **zero**. **Reshaped: the lead's own "blank page" was wrong, and the truth is worse.** `#root` is not empty — `index.html:396-521` seeds it with the `.gb-fallback` "wrong way in" guide, which starts at opacity 0 and fades in after 800 ms **precisely because React normally wipes it first** (the file says so at `:47-54`). React never mounts, so the guide reveals itself and tells a user on the working hosted app, or an installed PWA, that they opened `index.html` as a `file://` page and should double-click `Run.bat`. Every instruction on that screen is wrong for the situation that produced it. The file's convention is confirmed **opposite** at all three sibling sites (`:159-163`, `:204-215`, `:219-223`). **Widened**: `clearGoogleToken` (`:147`) has the identical unguarded shape. Mitigation confirmed: `rewriteToSync()` runs before the throw, so a manual refresh recovers — but `tokenStored:false`, so the consent grant is silently lost. **Reachability, honestly**: needs the consent return leg **and** a refused write; the token is ~250-350 bytes against a ~5 MB quota, so storage must be essentially full — not far-fetched, since the app persists ~20 zustand stores to `localStorage` and #373 (closed) is precedent that one grew without bound. Access-**throwing** cases are already handled by `safeLocalStorage()` returning null, so quota and iOS storage pressure are the live throws. Not a duplicate of #914 (`device-id.ts:36-46`, a render-time `useMemo` throw landing in Safe Mode) or #880 (`backup-settings.ts:115`, a post-commit restore throw showing a bare browser error): this is a third, distinct site with a distinct consequence (pre-mount, no boundary, the misleading `file://` guide) and a distinct fix location. Worth noting on #880: its body's claim to be "the only unguarded storage write in the app" is false |
| P17-41 | Prompt | `src/features/settings/SettingsDialog.tsx:196-197` | The dialog's docstring claims a universal that is false at 10 of 69 `SettingRow` sites | confirmed (narrowed) | #1569 | `cosmetic`. Independent recount: 73 raw `<SettingRow` hits − 1 TypeScript generic (`SettingsRepository.ts:30`) − 3 test fixtures = **69 app-UI elements, 10 without a hint** (`ReminderSettings.tsx:120,134,150`; `DangerZone.tsx:42,63`; `HotkeySettings.tsx:139,220,339`; `DatabaseMaintenance.tsx:28`; `SettingsDialog.tsx:646`). Cross-check: `SettingsDialog.tsx` alone has 56 elements and 55 `hint=`, so 1 hint-less. **The "Erase data" limb does not survive and was dropped**: `SettingRow`'s `description` prop is **required**, and `hint` renders only an additional `InfoHint` badge (`SettingsSection.tsx:100-121`, no default, no fallback). "Erase data" carries a full description — "Selectively wipe inventory, photos, settings, sign-in or sync links from this device — or factory-reset everything" — plus `EraseDataDialog.tsx:317`'s "This can't be undone", per-section tooltips and a two-click `EraseConfirmRow` gate. It is not an unexplained control. The residue is **the false universal alone**. Docstring location confirmed at `:196-197`; §11's recorded `:1631` is **stale** — that line is the tail of a comment inside the `allTabs`/`tabs` rail-tab assembly — and is corrected in §11 by this phase. Phase 11's U8-3 rejected the *search* consequence (five of six plausible queries still find a hint-less row via label and description) and left the docstring claim unadjudicated; this settles it. #1222 is precedent that a false docstring universal is filable, but on its own it is a one-line edit, hence the fold |
| P17-42 | — | `dist/`, `extension/dist/` | Build output is tracked and should not be | **rejected** (verified negative) | — | Rejected by the lead on raising, and independently re-confirmed by the lint/dependency finder. `git ls-files dist extension/dist` returns **0 files for both**. `.gitignore:14` is a bare `dist` pattern, which git matches at any depth, so it covers both; `git check-ignore -v` confirms both are matched by that line, and `bridge/.gitignore:5` adds `dist/` belt-and-braces. No `.gitignore` negation re-includes anything under either (the only `!` lines are `.vscode/extensions.json`, `.vscode/settings.json`, `.env.example`, `bridge/webhooks.example.json`). `.prettierignore:2,6` and `.dockerignore:29-30` both ignore them. Nothing anywhere expects a committed `dist/`: `deploy.yml:112` runs `npm run build` then uploads `dist`, `Dockerfile:68` builds inside the container, `e2e.yml:67-69` builds first. Both exist on disk in the primary checkout only, as local build output. No defect |
| P17-43 | Mechanical | `src/db/rpc/worker-driver.test.ts:53`; type at `src/db/rpc/protocol.ts:124-127` | `Omit<RpcResponseEnvelope, 'id'>` over a union collapses to `{ ok: boolean }`, mis-typing all twelve `reply()` fixtures | confirmed (reshaped) | comment on #783 | `cosmetic`. **Found during verification, not by a finder** — it is 12 of the 16 excess-property diagnostics that P17-36's sweep attributed to bad fixtures. `RpcResponseEnvelope` is a **union**, and `Omit` over a union is non-distributive: it keys off the union's common members, so `Omit<RpcResponseEnvelope,'id'>` collapses to `{ ok: boolean }`, dropping `result` and `error`. Compiled under a config that includes the file, that is twelve `TS2353 … 'result' does not exist in type 'Omit<RpcResponseEnvelope, "id">'`. **The twelve call-site fixtures are correct; the annotation is wrong** — which is why P17-36's framing of them as fixture defects is a mis-diagnosis of 75% of that cluster. Inert, since the file is in no TypeScript program. Added as a comment on #783 rather than filed: #783 is exactly this shape (a test helper declaring a member the interface does not have, in the same uncompiled tree), is `cosmetic`, and already carries a maintainer comment adding a second such site |

### Phase 18 — Runtime performance at scale

Pinned SHA: _not started_

| ID | Class | Where | Claim | Verdict | Issue | Notes |
| --- | --- | --- | --- | --- | --- | --- |

## 11. Carry-over: unverifiable candidates and out-of-scope observations

Candidates no phase could demonstrate here, with what would settle each; and observations that are
not defects but that the maintainer should see (a decision that looks worth revisiting, a
programme in §4.2 that a phase found to be under-scoped). Nothing here is filed as an issue by the
audit; Phase 19 puts the list to the maintainer.

| From | Where | Observation | What would settle it |
| --- | --- | --- | --- |
| Phase 0 | `scripts/browser-smoke.mjs:4617-4632` | When `dist/index.html` is absent the PWA update-handshake block skips, pushes no result, and the run still exits 0 reporting every step passed. `e2e.yml` builds first, so CI is unaffected; the documented local `npm run test:e2e` is not. | Run `npm run test:e2e` with no `dist/` present and read the summary line and exit code. |
| Phase 0 | `.prettierignore:24-25` | `**/fixtures/**` appears to protect the wrong files in both directions: it excludes `bridge/src/fixtures/{test-identity,virtual-snapshot}.ts`, which are ordinary hand-written modules, and does not match `src/db/migrations/__fixtures__/`, which holds the golden `scripts/regen-schema-snapshot.mjs` calls byte-for-byte. | `prettier --file-info` on both paths, then check whether `npm run format` rewrites the golden. |
| Phase 0 | `.env.example:3-4` | The header calls itself "the single tracked template" when `bridge/.env.example` is also tracked and is the one documenting the roughly 35 `GUBBINS_BRIDGE_*` variables, including the credentials. | Read the two files side by side and decide whether the root file's scope sentence should be narrowed. |
| Phase 0 | `Run.ps1:75` | `$BasePath = '/Gubbins/'` is an untested duplicate of `DEFAULT_BASE_PATH` (`src/base-path.ts:14`). Rejected as a finding because the audiences are disjoint and it carries no parity comment, so the "mirrors X" rule does not fire — but it is a literal that would need changing in two places. | A decision on whether the launcher should ever honour `GUBBINS_BASE_PATH`. |
| Phase 0 | `package.json:89` | When `typescript-eslint` widens its peer range for TypeScript 7, `package.json`'s `^6.0.3` must move to `^7` in the same change or resolution silently stays on 6.0.3. That is #186's work; worth a line in its body. | Nothing — it is a note for whoever actions #186. |
| Phase 0 | repository settings | `secret_scanning_non_provider_patterns` is **disabled**. Enabling it is a settings toggle that closes a good part of #733's residue (connection strings, generic auth headers, private keys) with no code change. The settings side belongs to #454. | The maintainer's decision. |
| Phase 0 | repository settings | `main` has no branch protection and the active ruleset carries no required status checks, so `tests.yml` is advisory on pushes and `deploy.yml`'s `ci` job is the only enforced gate. This is context for #725, #726 and #727 rather than a defect in a file; the settings side belongs to #454. | The maintainer's decision. |
| Phase 0 | `.github/workflows/e2e.yml` | The nightly's 11 step failures do not reproduce locally (125/125 steps pass on this machine), so they are specific to the hosted runner. Not filed — no defect was demonstrated in the app or the suite. | Re-running the suite on `ubuntu-latest` with a raised `SMOKE_TIMEOUT_SCALE`, or profiling the runner. |
| Phase 0 | `scripts/browser-smoke.mjs:2262` | Whether `BarcodeDetector` is absent in Playwright's bundled Chromium on `ubuntu-latest`. It is absent on both local channels, headed and headless, which is enough to confirm #743; the Linux answer is inference. | One `workflow_dispatch` step evaluating `'BarcodeDetector' in window` on the runner. |
| Phase 0 | `hacs.json`, `custom_components/gubbins/` | Three HACS checks could not be settled by reading: the `description` / `topics` / `issues` / `archived` checks (they read repository settings), whether hassfest's `translations` plugin accepts the 13 KB `strings.json`, and the precise per-category matrix `hacs/action` applies. | Running `hacs/action` and `hassfest` once — which is what #749 asks for. |
| Phase 16 | `src/features/scanner/components/ScannerOverlay.tsx:640` | The NFC indicator renders `'Ready — tap an NFC tag'` and `'Starting NFC…'` as raw English literals inside a `role="status"` element, with no `useT()` in that branch, so a `de` user hears English in a live region. Not filed here: `Camera-Scanning.md:112-116` describes the string accurately, so this is a source-side i18n defect rather than wiki drift. | Phase 17's i18n sweep should pick it up; nothing further is needed to demonstrate it. |
| Phase 16 | `src/db/search/ast.ts` error text | The nesting-limit message shown to a user ends "(spec §5.1)" — an internal specification reference in world-readable UI copy. Out of Phase 16's scope (the wiki does not quote it), and noted rather than filed. | A decision on whether user-facing errors may cite internal specification sections. |
| Phase 16 | `docs/wiki/` screenshot coverage | Five search and scanning pages carry no image at all, and `scripts/wiki-screenshots.mjs` has only two search steps among its 32. Rejected as a parity defect — nothing on those pages is contradicted — but it is the shape of the #53 backlog. | The maintainer deciding whether #53 should cover them. |
| Phase 1 | `src/db/client.ts:73-88` | `getDatabaseDriver()` has no closed or quiescing state, so a concurrent call between `disposeDatabase()` and the OPFS write could open a second worker over the file being overwritten. The absence of the guard is proved; whether the second worker's handle acquisition throws, blocks or succeeds — and so whether the consequence is a benign lock error or corruption — is not. | In Chrome on the primary `opfs` layout: hold open `writePlainDatabaseFile`'s writable on `gubbins.sqlite3`, then call `getDatabaseDriver().init()` from the main thread and observe whether the new worker's `createSyncAccessHandle()` throws, blocks or succeeds. |
| Phase 1 | `src/db/sqlite-header.ts:96-103` | No route was established by which a user obtains a WAL-format `.sqlite` without deliberately changing a setting in an external tool. Safe Mode's own archive README points users at DB Browser for SQLite, and the header survives a clean close, so the artefact is stable once made — but the app, the bridge and every common tool's defaults all produce the openable form. | A survey of what real tools do to the journal-mode bytes on save, or one report of a user hitting it. |
| Phase 1 | `src/db/tab-lock.ts` | #763 was confirmed in Chromium only. Playwright's Firefox and WebKit builds are not installed on this machine, and back-forward-cache behaviour differs between engines. | Running the same two-tab scenario under Gecko and WebKit. |
| Phase 1 | `src/app/error/safe-mode-actions.ts:237-238` | Whether picking a multi-gigabyte file on a low-memory device yields a catchable error or loses the renderer was not measured — no such device was available, and building a multi-gigabyte fixture was not safe on this host. | Picking a >2 GiB and a ~4 GiB file on a real low-RAM Android renderer, recording whether the read rejects or the tab dies. |
| Phase 1 | `src/db/restore-candidate.ts` | #762's 5.2–5.7× peak was measured on a 64 GiB desktop. Whether that multiple tips a phone into losing the renderer is unknown. | The same probe on a real low-RAM Android, or under memory-pressure emulation. |
| Phase 1 | `src/features/sync/snapshot.ts:365` | The delta replay's ledger read plans as `SCAN stock_deltas` + `USE TEMP B-TREE FOR ORDER BY` — there is no `(created_at, id)` index, so every snapshot build sorts the whole ledger, and the keyset continuation cannot seek either. Adjacent to #544, which bounded the ledger's size rather than its read. Not filed: it is a distinct candidate that arose while disproving P1-42 and has had no verification pass of its own. | A measurement at a realistic ledger size, and a decision on whether the index is worth its write cost. |
| ~~Phase 1~~ **resolved by Phase 17** | repository-wide | About 30 files name `jsdom` in comments. It is not a dependency — the suite runs on happy-dom. Filing it against one file would be filing a thirtieth of a convention. **Phase 17 (P17-30) swept it**: the precise figure is 41 lines across 31 files, of which #781 already owns 3, leaving 38 across 30. Five of them are not merely mis-named but **false** — they assert `elementFromPoint`, `PointerEvent` and `matchMedia` are absent, and happy-dom provides all three. `git log -S 'jsdom' -- package.json` is empty: jsdom was never a dependency at any commit. | **Settled.** Filed as #1568. |
| Phase 1 | `src/features/sync/unique-key-coverage.test.ts` | The `EXEMPT` map records that `item_stock` and `stock_batches` need no natural-key collision rule because "id is derived from the key, so both devices mint the same id". True for rows the app mints, untrue for rows it applies — which is #771. The exemption is doing the opposite of its job. | Whether the exemption should be replaced by an assertion that a stored id matches its derived form. |

| Phase 2 | `src/db/repositories/item/aliases.ts:196-203` | The alias lookup scans the whole `item_aliases` table and sorts it on every call. Rejected today because there are exactly two writers, both per-item and dialog-gated, so the table is bounded by human interactions — but the growth is linear and reaches ~10,800x the indexable shape at 100k rows. The day a bulk alias path is added (a CSV alias column, a batch scrape, a write API), this is the first seam to break. | Nothing now. It is a note for whoever adds such a feature; `suppliers.name_key` is the working precedent for the fix. |
| Phase 2 | `src/db/repositories/item/search.ts:184` | The structured search's paged ordering is the only item read whose order is not total — no unique tiebreak. Four attacks failed to produce a page-boundary duplicate or drop: 5,000 and 20,000 rows tied on every term, eleven forced access paths, several page depths, and a sorter forced to spill. The engine's sorter was stable in every case. | Driving the same paged walk on **wa-sqlite** in a real browser over a large fully-tied dataset, or on a build whose sorter genuinely spills to multiple runs. The fix comes free with #797, so this only decides whether it is a defect or an unproven risk. |
| Phase 2 | `P:\Source\!Memories` (outside the repository) | The durable note *Inventory attention scaling ceiling* records "**Dead end — do not retry:** rewriting the thrice-repeated `NOT IN` as a correlated `NOT EXISTS` is slower (23.5 ms vs 17.2 ms)". Measured at 50k and 200k with no statistics, every **count** shape is 2.26–2.96x *faster* under `EXISTS`, reaching 3.97x at higher variant-parent density; #168 shipped that rewrite the same day on the strength of the count shape and it is still in the code. The note's own figures come from its `applicableStatuses` benchmark at 20k items, where the two forms tie within noise (1.09x measured). So the two measured different things — but the note's direction and its standing "do not retry" instruction would send a future session back to the slower shape. | The maintainer's decision. Re-scoping that paragraph to the shape it actually measured would settle it. Nothing in the repository is wrong; the code comment at `attention-sql.ts:35` was checked and is defensible. |
| Phase 2 | `npx vitest run --reporter=basic` | Fails under Vitest 4 with `Failed to load custom Reporter from basic` **and still exits 0**, so a CI step written that way would go green having run nothing. Noticed while running the item suite; Phase 0's territory, and no such step exists today. | Grepping `.github/workflows` and `package.json` for a `--reporter` argument, and pinning the reporter names Vitest 4 accepts. |
| Phase 2 | `items` columns `reorder_point`, `reorder_gauge_percent`, `expiry_date`, `acquired_at`, `warranty_expires_at` | Each has a TypeScript guard and **no** column CHECK, so a synced peer or a restored backup can write an unbounded or malformed value that no local write path would accept. The reverse of #769/P1-46, which set the policy for missing CHECKs. | Whether the policy #769 settles for `settings` extends to these five, and whether any consumer actually breaks on a bad value — none was named. |
| Phase 2 | `src/db/repositories/item/aliases.ts`, `items_fts` | Item aliases are searchable from nowhere: the search index covers `items` columns only, so a supplier part number mapped as an alias cannot be found from the search box, the palette or any picker. The wiki lists exactly the seven indexed columns and claims nothing more, so this is a feature gap rather than a contradiction. | Whether alias text should join the search index. |
| Phase 3 | `src/db/repositories/project/bom-lines.ts:117` | Re-pointing a BOM line at a different item keeps the previous item's cost snapshot, part number, manufacturer and description, so a point-in-time project costs the wrong part at the wrong price; and `updateLine` leaks a raw engine error where `addLine` throws a named one. Verified real, and verified unreachable: `useUpdateBomLine` has zero consumers across `src/`, `bridge/` and `extension/`, and no edit-BOM-line surface exists. | Whether an edit-line surface is ever built. Same shape as the Phase 2 `aliases.ts` row — a note for whoever adds the feature, not a defect today. |
| Phase 3 | `src/db/repositories/AssetBookingRepository.ts:343`, `MaintenanceRepository.ts:154-155`, `:183-185` | Three reads walked by `readAllPages` order on a non-total key, and unlike the `project/procurement.ts` case rejected this phase, all three **genuinely OFFSET-walk** — so #149's invariant is live here. No duplicate or drop could be produced; Phase 2 failed four attacks on the identical question at `search.ts:184`. What this row has that `search.ts:184` lacks: `AssetBookingRepository` carries **both** shapes, and `list:331`'s docstring argues the tiebreak is needed *because* the export offset-walks it, while `listUpcoming:343` — walked since #149/#606/#607 — omits it. | The same wa-sqlite browser walk §11's `search.ts:184` row already names. The tiebreak is free, so it is worth adding before anyone gives one of these feeds a deeper consumer. |
| Phase 3 | `locations.dead_stock_days` | The column's only guard is `> 0`, while the UI and the repository both clamp to the preference bounds — so a value from a tampered snapshot or a hand-edited database reaches `addCalendarDays`, overflows to an invalid date, and makes every comparison false, reporting **every** opted-in item as dead. This names the consumer the Phase 2 carry-over row said was missing for its five `items` columns. | Whether the CHECK should match the clamp (a one-line tightening), and whether the same applies to the five `items` columns the Phase 2 row lists. |
| Phase 3 | `src/db/repositories/LocationRepository.ts:624-644` | Collapsing the per-item history fan-out on a location delete into one `INSERT … SELECT` removes a stable **~38%** of the delete's cost at every size measured (1.67x / 1.64x / 1.62x at 2k / 8k / 32k items), and was verified to write all 32,000 rows correctly. Rejected as a finding because the ratio is flat, the structured clone is only 5% of the cost, and the gesture sits behind a confirmation dialog — but the improvement is real and cheap. | Nothing. A note for whoever is already editing that method. |
| Phase 3 | `src/db/repositories/PurchaseOrderRepository.ts:197` | While disproving the N+1 claim, the real cost at the export cap turned out to be the `LIMIT ? OFFSET ?` header walk — **435 ms of 599 ms at 10,000 orders, even with the lines batched**. That is keyset-pagination territory and a distinct candidate from the one rejected, with no verification pass of its own. | A measurement against a keyset shape, and a decision on whether the export path should seek rather than offset. |
| Phase 3 | `src/db/repositories/checkout-plan.ts:86-88`, `features/projects/assembly.ts:145-150` | Two domain unions are branched by if-chains and ternaries rather than switches, so the exhaustiveness protection the memory note relies on cannot fire. Adding a fourth borrower type and running `npm run type-check` across all three projects produces **zero** errors anywhere — the union has no coverage at any site, including a mapper that falls back silently and an unchecked cast in the checkout dialog. Both rejected because each union has one producer and a new member would need a migration, a column and a CHECK before it could compile. | Whether the project wants exhaustive switches on unions that are currently extended only by migration. Cheap hardening, no consequence today. |
| Phase 3 | `P:\Source\!Memories` (outside the repository) | The note `Query row shape guard` describes the guard as resolving "a template whose `${…}` spans are exported module-scope consts", omitting the **constant-folding** step that actually carries the location and category list reads. That omission is what produced candidate L-2, which was disproved. The code is correct; the note is incomplete. | Adding the folding step to the note. |
| Phase 3 | `src/db/repositories/ReportRepository.salesAnalytics` | The method has **no repository-level test at all** — the only coverage is of the pure report builder and of the hook wiring. Nothing exercises the SQL-to-seam mapping: the metadata parse, the quantity fallback, the rounding, the action filter or the category join. A hand-computed check confirmed the mapping is currently correct, so this is a coverage gap rather than a defect. | Whether the test-suite quality phase (17) picks it up, alongside the six-test sweep filed as #861. |
| Phase 4 | `src/features/sync/providers/google-drive-api.ts:107-136` | Whether Google refuses a large non-resumable Drive upload, with what status, and whether the app surfaces it usefully. The endpoints used, the absence of any resumable path, the absence of a size check, and the one extra full copy on the create path are all verified; the ceiling itself cannot be settled with a stubbed `fetch`, because a stub only replays what it was told. Filed as #907 on the memory limb alone. | A real Google account with Drive API access and a live token, pushing snapshots above the documented simple-upload guidance (say 8 MB, 64 MB and 256 MB), recording each HTTP status and body, then checking how `authedFetch` classifies it and what the Sync screen shows. |
| Phase 4 | `src/lib/save-file.ts:150-172` | Whether Chromium's `showSaveFilePicker` leaves a zero-byte file at the chosen path when `close()` rejects. If it purges the target instead, the missing `abort()` buys nothing for the stated harm and #895 reduces to its parity limb alone. The picker creates the target file at pick time, so an `abort()` may leave the same empty file regardless. | A real browser session with a genuine disk-full or quarantine failure at `close()` on a picker handle, inspecting the chosen path afterwards. A native save dialog cannot be driven by Playwright, which is why `browser-smoke.mjs` deletes the API outright before its triage steps. |
| Phase 4 | `src/features/sync/SyncScreen.tsx:342-353` | The comment justifying the missing-remote override states that `lastSyncedAt` is "cleared when a different remote is connected". After a Disconnect it is cleared even when the **same** account is reconnected, because `disconnect()` nulls `providerId` and the identity comparison can then never match. So the first sync after any disconnect-and-reconnect bypasses #196's guard, including when the shared snapshot genuinely has gone missing. U3-8 was rejected on the claim actually made; this is a different, real hole in the same decision's reasoning. | A decision on whether the override should key off something that survives a disconnect, and whether losing that warning for one pass matters given peers re-push their rows on the next delta merge. |
| Phase 4 | `src/features/sync/snapshot.ts:1206-1232` | For a **solo, never-synced** user who downgrades images and then restores a JSON Replace backup, the missing-file report's explanatory copy is wrong: it says the photos "may be photos added on another device that this one has not downloaded yet — a sync brings them back", and no sync will, because the bytes were deliberately deleted. U4-10 was rejected because the post-clone state is indistinguishable from the ordinary post-sync state for a synced user; this residue applies only to the user who has no peers. Self-healing after one triage run. | Whether the maintenance report's copy should distinguish "not downloaded yet" from "downgraded here", which needs the marker it currently cannot see after a clone. |
| Phase 4 | `src/features/danger-zone/EraseDataDialog.tsx` | The dialog is 756 lines and has **no test of any kind**, and there is no Danger Zone step in `scripts/browser-smoke.mjs`. Six of this phase's confirmed findings live in or behind that file (#879, #882, #891, #893, #894, #897) and none of them was visible to CI. Recorded as a coverage observation rather than filed, since a coverage gap on correct behaviour is not itself a defect. | Whether Phase 17's test-suite quality sweep picks it up, alongside the sweep already filed as #861. |
| Phase 4 | `src/features/sync/reconcile.ts:272-284` | #889's fix for the first two limbs (deriving the surviving sets after the cycle guard runs) does **not** address the third: `rejectParentCycles` leaves the surviving row's `parent_id` pointing at a row it has declined to write, which aborts the apply on its own with no child rows involved. Recorded here because a partial fix would look complete and leave the abort in place. | Nothing — it is a note for whoever actions #889. |
| Phase 5 | `src/components/foundry/useInstallPrompt.ts:42-57` | The hook's *timing* limb could not be settled here: whether a browser that has already fired `beforeinstallprompt` before the first hook instance mounts ever re-fires it for a later listener. The second-instance limb was demonstrated and is filed; this one needs a real install-eligible session, which Playwright cannot produce — its Chromium never fires the event, so every automated run is synthetic. | A human-driven Chrome and Edge session on the hosted deployment, opening Settings > App after the browser's own install affordance appears, and recording whether the in-app button becomes live. |
| Phase 5 | `src/features/command-palette/CommandPalette.tsx` | While disproving the eager-import claim, the eager graph turned out to carry `CreateItemDialog` at **83,557 B** through `ActiveTabScrapeListener`. That is a larger payload than the candidate that was rejected, and it had no verification pass of its own. | A measurement of what the entry chunk loses if the scrape listener defers the dialog, and whether the listener needs it before a scrape starts. |
| Phase 5 | Chrome DevTools Protocol CPU throttling | CDP's 4x throttle **over-applies to `Intl` constructor calls specifically**, measured at roughly 6x. Any throttled figure for `Intl.PluralRules` / `NumberFormat` / `DateTimeFormat` construction is therefore an instrument artefact, not a device measurement. Recorded because a later phase measuring formatter cost on a simulated low-end device would otherwise repeat the error. | Nothing. It is a note for whoever next throttles a formatter benchmark. |
| Phase 5 | `src/lib/utils.ts`, `src/styles/index.css` | Two by-products of a rejected candidate. `ring-bleed-x` is classified by tailwind-merge as a **ring colour** rather than a spacing utility, and it is deleted rather than merged. And `safe-area-token.test.ts`'s parity claim is one-directional: it checks that every declared group exists, never that every group in use is declared. | Whether either is worth a line of hardening. Neither has a live offender today. |
| Phase 5 | `src/db/errors.ts:175-194` | #751 / P1-5's premise was re-confirmed against the real engine, but its consequences are **not reachable**: no shipped VFS implements shared memory, so WAL is impossible; cross-tab contention returns a *primary* `SQLITE_BUSY`; and the reachable `IOERR` family has no case at all, so the collapse changes nothing. Recorded so whoever actions #751 knows the severity is lower than the body implies. | Nothing — it is a note for whoever actions #751. |
| Phase 5 | `docs/todo/` | `docs-todo-status.test.ts`'s walk is non-recursive, so this audit's own plan document is only guarded while it stays at the top level. Splitting it into `docs/todo/audit/` would take every phase record out of the guard's sight silently. Filed as part of #957; recorded here because it governs this document. | Nothing. A note for whoever reorganises `docs/todo/`. |
| Phase 6 | `src/components/foundry/toast.tsx:177-184` | The toast viewport is an `aria-live="polite"` container wrapping one `role="status"` element per toast, so each message sits inside two live regions with conflicting atomicity. No comment, commit or test justifies the nesting, and `live-region.tsx` pairs role and `aria-live` on a single element everywhere else. Whether real screen readers announce a toast twice, once, or not at all cannot be settled here. Not #219 (which names three other hand-rolled regions), #224 or #543. | Counting announcements with NVDA + Chrome and VoiceOver + Safari for one toast, and again for a second toast appended while the first stands. |
| Phase 6 | `src/components/foundry/number-input.tsx:152-174` | The change handler rewrites the element's value and moves the caret while a composition is in progress — proven to fire on an event carrying `isComposing` — but what a real input method does when the field it is composing into is emptied underneath it cannot be shown without one. The full-width digit half of the same finding **was** demonstrated and is filed as #968. | A manual session with a Japanese or Chinese IME on any number field, or Playwright driving CDP `Input.imeSetComposition`, recording whether the candidate window survives. |
| Phase 6 | `src/features/inventory/components/TypedFieldControl.tsx:330-337` | A BOOLEAN custom field with no value yet shows **No** as selected — visually and to assistive technology — while the field still saves as unset. The behaviour is deliberate and documented in place ("an untouched default still submits as unset"), so candidate U3-8 was rejected under §4.1 rather than filed. The residue is that the control asserts a choice the record does not hold, and a user who wants "No" may leave it untouched and store nothing. | The maintainer's decision on whether an unset boolean should look unset, or whether the first option should be committed on render. |
| Phase 6 | `src/components/foundry/usePwaUpdate.ts:97-109` | #981 was filed without asserting how likely a failed load is, because the evidence needed does not exist: §12 records no chunk map, and `dist/` is untracked. A `.catch` is correct regardless of chunking, but the exposure is unquantified. | One build, with the chunk map recorded in §12 — which that section still wants for Phase 18 in any case. |
| Phase 6 | `src/components/foundry/select-options.ts:24,41` and `use-anchored-popover.ts:35` | The popover's 240 px cap is stated three times — twice as a literal, once in prose. Candidate U3-6 was rejected because divergence has no consequence today (overscan absorbs the first-paint guess), but the fix if ever wanted is rule 1: export the cap and import it, rather than adding a drift test. | Nothing. A note for whoever touches the popover's sizing. |
| Phase 6 | `src/components/foundry/select.tsx:61-76` | `Select` exposes no `onBlur`, so an RHF `Controller` binding cannot report a touched field. Rejected as latent: no `useForm` in `src/` sets a validation mode, and `touchedFields`/`isTouched` appear nowhere. It becomes real the day a form wants `onBlur` or `onTouched` validation. | Whether any form should validate on blur; if so, the seam is missing before that can work. |
| Phase 6 | `src/components/foundry/number-input.tsx:233-236` | A caller's `className` lands on the number field's wrapper rather than its input. Rejected because the utilities live call sites actually pass are merged on the wrapper and relayed to the input — but a padding, background or border utility would silently do nothing, and nothing warns. | Nothing now. A note for whoever first styles a number field's interior. |
| Phase 6 | `src/components/foundry/reorder-list.tsx:112-124` | A caller that applies a move asynchronously loses both the announcement and the focus re-home, because an interleaved re-render consumes the pending record and the bail cannot tell "declined" from "not yet applied". The sole consumer applies synchronously today. | Nothing now. A note for whoever gives `ReorderList` a caller that persists before it re-renders. |

| Phase 7 | `src/features/inventory/ocr/OcrPrefillDialog.tsx:153-162` | The photo picker sets `capture="environment"`, and the code's own comment says that opens the camera directly on a phone — but the specification only says a user agent *should* invoke the capture control and is silent on whether a gallery route remains, and the reference documentation is if anything contrary. The copy/markup contradiction is filed (#1021); what could not be observed is what a real phone browser does, and therefore whether the common "photograph it in the shop, add it at home" path is actually unreachable. | Opening the dialog on an Android device running Chrome and an iOS device running Safari, pressing *Choose a photo*, and recording whether a gallery or Files route is offered alongside the camera. |
| Phase 7 | `src/features/inventory/labels/label-template.ts:154-157`, `label-sheet.ts:552` | The sheet mode has no US Letter page geometry — the page box is fixed to A4 and every sheet preset is European Avery stock. Candidate U6-14 was **rejected** because the printer notice renders only for a named A4 die-cut stock or a hand-entered layout, so the plain-paper Letter user never sees the unconditional "print at 100%" guidance, and nothing in the app or the wiki claims Letter support. The residue is the feature gap itself. | The maintainer's decision on whether US sheet stock (Avery 5160 and friends) is in scope. |
| Phase 7 | `src/features/inventory/labels/location-path.ts:36`, `location-tree.ts:309` | Two modules export `locationPath` with different meanings — ancestors only, and ancestors plus the node — and `LocationSidebar.tsx` imports from both modules in adjacent statements. Candidate U6-15 was **rejected**: no shipped call site is wrong, each function is documented at its definition, and the completeness claim the finder cited is about separate *walks*, which the second function is not (it wraps the first). But swapping the import type-checks, passes all 12,448 tests, and would print a location's own name twice on every printed label. | Nothing now. Cheap hardening (rename one) for whoever next touches either module. |
| Phase 7 | `src/db/repositories/item/kits.ts:403-423` | `readKitGraph` reads kit edges one node at a time (edge reads == node count, leaves included), and `AssemblyLedger.preload` adds a second pass. Candidate U5-8 was **rejected on measurement**: ≤3 ms on a desktop and ≤71 ms at 20× CPU throttle for a 100-node graph, against a bounded hand-authored kit — where #801, the open N+1 in this area, scales with an import file. The docstring's "level by level" was also checked and is accurate: it describes traversal order and attaches "batched" only to the item rows, which genuinely are one query. | Nothing — a one-line note for #112 if the maintainer wants the measurement on record. |
| Phase 7 | `src/features/inventory/dead-stock-options.ts` | Candidate U7-8 was **rejected**: removing a mode is type-checked, and adding one is caught by an existing parity test — it simply lives in a file named after the search surface rather than after this module, which is why the finder concluded no guard existed. The residue is that the addition-direction guard is **indirect**: it holds only because dead stock is one of three enum search fields, so dropping that search field would silently remove the coverage with nothing near the options file to say so. | Nothing now. A note for whoever changes the enum search fields. |
| Phase 7 | roughly nine capped reads across the app | Verifying U8-10 (the saved-container picker, filed) surfaced that about nine other reads are still bounded with no truncation notice — two in users, one in webhooks, one in tags, one in bookings and four in contacts. Only the tare-preset one was verified to this phase's standard. #149 fixed a named list of six and flagged one deliberate remaining gap; these were not among either. | Whether one combined "the remaining bounded reads" issue is better value than nine singletons — and a verification pass over the other eight before any of them is filed. |
| Phase 7 | `src/features/inventory/importers/migrations.ts` (Grocy) | U2-3 is filed (#1013), but one limb could not be settled here: whether a Grocy stock export actually carries a `best_before_date` column. The repository's own Grocy fixture has none, so the issue states the verifiable form — the Grocy spec has no `expiryDate` rule under any key — rather than asserting the export loses its dates. | A real Grocy instance, exporting Products / Stock overview to CSV and reading the header row. |
| Phase 7 | `bridge/src/homeassistant/scale.ts:23-25`, `bridge/src/openapi.ts:2191` | The other half of U8-11, which belongs to **Phase 13**, not this one. The bridge's unit-conversion table carries two unguarded prose claims — a parity comment against the app's own weight table, and an OpenAPI description that enumerates the same six units and is mirrored into the generated document. Adding a unit leaves type-check, the 1191-test bridge suite and the app suite all green while four prose restatements go wrong. The published API description is *live*, unlike the dead app-side copy that #1051 removes. The six shared factors were checked and are byte-identical today, and the three units the app has and the bridge omits are a documented, deliberate difference — so "make the two tables equal" would be the wrong fix. | Nothing here. Phase 13 should pick it up with this demonstration. |
| Phase 7 | `src/features/inventory/item-drag.tsx:430-469` | #1044's recovery-path limb is demonstrated and filed, but the frequency of the triggering condition is not established: it could not be shown that a shipped browser ever drops a `pointerup`. Synthesised input goes straight into the renderer, so releasing the button outside the window is not faithfully reproducible, and backgrounding the tab in headless Edge did not even flip `document.hidden`. | A human-driven session on Windows and macOS: press on an item card, drag outside the browser window, release there, return, then click a location row — and the Alt-Tab variant. Record whether the drop fires. |
| Phase 7 | `P:/Source/!Memories/Gubbins/Exhaustive switch guard seam.md` | The memory note claimed `noImplicitReturns` is **not** enabled; it is (`tsconfig.app.json:30`). Found while verifying U7-7. It does not change that verdict — both sites there return on every path — but it understates how much of #562's shape list is already protected. **Corrected in the vault during this phase** (commit `a9bfecd`), so this row is a record rather than an action. | Nothing. |
| Phase 8 | `src/styles/index.css`, `src/components/foundry/` | `aria-invalid` has **no visual treatment anywhere in the app** — a grep over `src/` and `src/styles/*.css` for a selector matching it returns nothing. Every "but the control marks it invalid" mitigation in this phase's findings is therefore inaudible *and* invisible: a sighted user typing `5.9` into a warranty box, `150` into a gauge percentage or `1 000` into a measurement sees the same box they always saw. It raised the assessed severity of about a dozen findings here. The styling and the primitives belong to Phases 5 and 6, both complete, so it is recorded rather than filed. | Nothing — it is demonstrated. It needs a decision on where the treatment belongs (a token-driven ring on the Foundry controls, or a rule in `index.css`) and an issue in that phase's scope. |
| Phase 8 | `ItemStockValue.tsx:23-31` and the same pattern elsewhere | U1-3 was rejected because Chromium *does* expose `aria-label` on a role-less `<span>` — the CDP AX tree reports `{"role":"generic","name":"Unlimited supply","ignored":false}`, with a control page proving the tool can report a missing name. The residue is that ARIA 1.2 **prohibits** a name on a `generic` role, so the app is relying on an unimplemented prohibition. | A real screen reader. NVDA, JAWS and VoiceOver each decide independently whether to announce a named `generic` node; none was available here. |
| Phase 8 | `ImportDataDialog.tsx:413-483` | U11-3 (the item catalogue re-indexed per keystroke) was rejected on measurement — 2.57 ms at the 10,000-item ceiling, linear, against ~125 ms at 2,000 locations for the limb already filed as #1001. About 75% of the keystroke cost is present with an **empty** catalogue and was not identified. It is inside the noise band this audit has twice rejected, so nothing was filed. | Profile the same keystroke with both the catalogue and the location list empty, to name the remaining three-quarters. Recorded as a measurement on #1001. |
| Phase 9 | `src/features/reports/reports.ts:585`, `src/features/reports/data-hygiene.ts:251` | The dead-stock and hygiene ages are durations where their inclusion cutoffs are calendar days, so in a DST zone whose span crosses a spring-forward a row flagged at 90 days reads 89 for the first hour of its life in the report (measured window: exactly 1 h, two ways). Rejected on materiality — but the DST memory note both **sanctions** the raw floor for `idleDays` by name and applies the normalised form to `daysUntilExpiry` for the same hazard, which is a tension the note itself does not resolve. | A decision on whether duration-to-count division should be normalised at these two sites, or the note amended to say why not. |
| Phase 9 | `src/features/projects/components/FinaliseAssemblyDialog.tsx:65` | The Finalise button is gated on `isPending` only, so the previous read's plan is on screen with the button live for the duration of a re-read (`listAssemblyParts` median 0.33 ms at 200 parts, plus two worker hops). Rejected — the write re-plans from live rows and only `takesAll` can differ — but gating on `isFetching` too would make the dialog's own "cannot promise something different from what lands" guarantee true at every instant rather than every settled instant. | Whether a spinner flash on every reopen is an acceptable trade for that. |
| Phase 9 | `src/features/reports/components/HygieneChecklist.tsx:55` | #546 grades this site as "supplementary rather than content". That is contestable: the **passing** branch renders its description unconditionally, so only the rows the user must act on lose their explanation. No code change either way — #546 already lists the site. | The maintainer's judgement on #546's severity note. |
| Phase 9 | `src/features/export/export-every-page.ts:104`, `src/lib/date-input.ts:20-23` | Both take `slice(0, 10)` of an ISO string, which is malformed for any year ≥ 10000 (`isoCalendarDay(Date.parse('10000-01-01'))` is `'+010000-01'`). That is inside `Date`'s range and passes every existing guard, and `BookingEditDialog.tsx:114-131` puts no `max` on either date input. | Whether Chromium's date widget admits a five-digit year typed by a user. Needs a browser drive; absent that it is an absurd-input case with no established route. |
| Phase 9 | Playwright `context.setOffline(true)` | It does **not** intercept a service-worker script fetch — the request still reached the probe's own server — so it cannot be used to emulate offline for `registration.update()`. A fully-stopped origin is the sound way. | Instrument artefact, recorded so a later phase does not repeat the mistake. |
| Phase 9 | `src/features/dashboard/widgets.tsx:971` | `useHealthyWidgetIds`' project predicate is deliberately kept in step with the widget's, under an in-file section rule ("Keep each branch in step with its matching widget above") — a parity claim across two definitions with no test. Correct today, and #1181 fixes both together. | Whether the register's "mirrors X" rule should reach an in-file section rule as well as a doc-comment. |
| Phase 9 | `src/features/projects/projects.ts:541` | `useSetReservation` invalidates the item-history key with a **BOM line** id rather than an item id. No user consequence today, because the sweep on the line above already covers the prefix — so it is wrong-keyed and only accidentally harmless. | A maintainer's eye; it will start mattering if the history key is ever moved out from under `items()`. |
| Phase 9 | app-wide | `aria-invalid` still has no visual treatment anywhere (`grep -rn "aria-invalid:" src/` is empty), which Phase 8 recorded and which recurred in three separate Phase 9 findings (#1155, #1160, #1162). In each, the number field correctly marks the entry invalid and the call site saves a different value anyway — so assistive tech is told the entry is invalid while a sighted user sees nothing at all. | Unchanged from Phase 8's entry; noted again because the frequency is now four phases' worth. |

| Phase 10 | `src/features/lookups/providers/wikidata-film.ts:130` | The film runtime is read from the truthy value, which carries no unit, so `MIN` selects the figure expressed in the largest unit. The unit-blindness and the false docstring are filed (#1252); what is not established is how often real entries carry mixed units. | An offline query over a Wikidata dump grouping `P2047` by `wikibase:quantityUnit` across the film subset, or the maintainer's own knowledge of the corpus. Not settleable by querying the live service, which this audit does not do. |
| Phase 10 | `src/features/scraping/parsers/amazon-parser.ts:37-51` | Whether Amazon renders `a-text-price` before `priceToPay` inside `#corePriceDisplay_desktop_feature_div`. The code comment's claim survives as written ("elsewhere on the page"), so the candidate was rejected — but if that ordering does occur, the buy-box parser takes the struck-out price. | One real Amazon product page in a "was £X" state, inspected for the document order of `.a-offscreen` nodes inside the core price block. A saved, scrubbed fixture in the repository would settle it permanently. |
| Phase 10 | `src/features/scanner/components/BarcodeScanDialog.tsx:283`, `BarcodeField.tsx:109` | `inputMode="numeric"` is a soft-keyboard hint, and letters typed or pasted are accepted verbatim — so the cost is an extra gesture unless a platform's numeric pane offers no letters key at all. Filed as #1229 on the contradiction; the on-device cost is unmeasured. | The dialog opened on a physical iPhone (iOS 17+ Safari) and a physical Android phone, photographing whether the pane the field summons carries an ABC key. |
| Phase 10 | `src/features/images/compression.ts:39,57`; `src/features/inventory/location-media.ts:64`; `src/features/reports/catalogue-branding.ts:26` | Four `createImageBitmap` sites rely on the platform's `imageOrientation: 'from-image'` default. Verified correct on Chromium and Firefox with a synthetic `Orientation = 6` JPEG and a positive control; WebKit is not installed on this machine. The canvas re-encode strips EXIF, so a wrong default would bake the rotation in permanently. | The same synthetic JPEG run through `processImageFile` on a real iOS or macOS Safari, decoding the resulting WebP to see whether the rotation was applied. |
| Phase 10 | `src/features/share/ImportLaunchScreen.tsx:34-36`; `vite.config.ts:300-311` | Two OS-integration limbs need an installed PWA that this session could not produce: whether a desktop OS actually offers "Open with Gubbins" for a declared `.html` handler (Chromium parses the entry, but applies any dangerous-extension policy at registration rather than parse time), and whether a multi-file "Open with" arrives as one launch carrying several handles or as several launches. | An installed PWA on Windows, macOS and Linux, driving a real file association and a real multi-selection. |
| Phase 10 | `src/features/command-palette/CommandPalette.tsx:139-143`; `ScannerOverlay.tsx:450-456` | Two screen-reader limbs could not be settled: whether the palette's dialog container is announced in the macrotask before focus reaches the input, and whether the failure toast is announced while the scanner's `aria-modal` overlay is up. Both findings were filed on their code-level halves (#1221, #1236). | NVDA, JAWS or VoiceOver driven against the running app with its speech log captured. A CDP accessibility tree records state, not utterances. |
| Phase 10 | `src/features/scraping/ScrapeBridgeContext.tsx:229-231` | The PWA's message listener checks `event.origin` and not `event.source`, where the extension's own content script checks both and records why. Rejected because a same-origin page already owns the OPFS database outright, and the shipped COOP and `frame-ancestors` close both delivery routes — so the check restores no boundary. It remains a cheap, symmetric hardening that would remove an asymmetry a future reader will trip over. | The maintainer's decision on whether defence-in-depth alone justifies the one-line change. |
| Phase 10 | `src/features/scraping/parsers/parsers.test.ts:168-179,202-212` | The `rs`, `adafruit` and `sparkfun` parsers have no page fixture: deleting all three `selectors` blocks leaves the whole scraping suite green. Not filed, because the best-effort selector posture is a recorded decision and the metadata fallback is the backstop — but a wrong-*field* selector (the shape #1266 documents for two of those three) would ship undetected. | A saved, scrubbed fixture page per parser, or a decision that host selectors are deliberately untested. |
| Phase 11 | `src/state/stores/useSessionStore.ts:57` | `signOut()` resets the derived authority to the store's **unrestricted** default until the async refresh lands. The widening is proved; no reachable consequence was found, because the app tree unmounts before `signOut()` on both paths and nothing guarded runs in the window. `DENIED_AUTHORITY` is already the value `authority-refresh.ts:50` reaches for in exactly this situation, and the permissive *initial* default is load-bearing for module-off boot, so a fix must reset only on the transition. | A background task, an in-flight mutation or a future call site that performs a guarded write between `signOut()` and the refresh. |
| Phase 11 | `src/features/calendar/CalendarScreen.tsx:298` | The Upcoming agenda renders one card per event with no virtualisation, where eight other long lists in the app use `@tanstack/react-virtual`. The **rebuild** half of the candidate was disproved by measurement (the pure build/filter/bucket chain is 1–2% of the render chain at every size, and `now` is fresh per render so a memo could not help). The lane predicates are O(catalogue), not O(due soon) — warranty and expiry ask 36,500 days ahead and field-due has no lower bound at all. | A browser profile of `/upcoming` against a vault seeded to ~500 / 2,000 / 5,000 agenda events, recording initial commit time and chip-toggle input-to-paint latency under mobile CPU throttling. |
| Phase 11 | `src/db/repositories/MaintenanceRepository.ts:154`, `:183` | Both reads are OFFSET-walked under an `ORDER BY` with no unique tiebreak, and every USAGE schedule collapses to the constant `0`. **Six attacks produced no reproduction** on `node:sqlite` — 250, 5,000 and 20,000 tied rows, a forced plan change via a new index plus `ANALYZE`, `cache_size = 1`, and inserts between pages all returned every row exactly once. This confirms rather than resolves the Phase 3 carry-over of the same three sites. | The same walk on the shipped engine — `wa-sqlite`/`sqlite-wasm` in a browser — which is what §11's `search.ts:184` row already names. The live half of the same `ORDER BY` is open as #1166, which says the tiebreak belongs in its change. |
| Phase 11 | `bridge/src/openapi.ts:2111` | The webhook **test** endpoint's OpenAPI outcome enum advertises `skipped`, which `createWebhookTestFirer` cannot produce — it builds a fresh deliverer with a closed failure circuit on every fire, deliberately. An integrator writing against the published spec handles a state the endpoint cannot return. (The delivery-log enum at `:2806` is correct; real deliveries do get `skipped`.) | Nothing — it is confirmed. It belongs to Phase 12/13, which owns `bridge/openapi.ts`. |
| Phase 12 | `bridge/src/mdns/discovery.ts`, `bridge/src/identity.ts:130-132`, `bridge/src/api/v1.ts:708-713`, `bridge/mcp.mjs:27`, `bridge/src/server.ts:385` | **Five security findings were confirmed and deliberately not filed**, at the maintainer's direction, and handed over privately instead. In descending seriousness: an unparseable request target throws outside the request handler's `try`, so ten unauthenticated malformed request lines trip the fault budget and exit the process; mDNS discovery selects the destination the Home Assistant token is sent to, with no address check, no correspondence check and no loopback gate; `GET /api/v1/events` demands `bridge:read` only while streaming a live superset of the `audit:view`-gated activity feed; a `stock:write` token reads item detail from a write response (no shipped role is affected); and the MCP server adopts a `.env` from whatever directory its client launched it in, which grants the write surface on a transport documented as carrying no credential. Every one is reproduced, and the mDNS and crash findings need no credential at all. | The maintainer's decision on disclosure and sequencing. The register rows carry the demonstrations; nothing is public. |
| Phase 12 | `bridge/src/inventory-scan.ts:102` | `forEachPage` discards `page.hasMore` at its cap, so `locationsTotal` publishes a truncated array's length as a whole-vault figure. Driven at 10,100 locations: `/metrics` reports 9,998, and the per-location gauge series truncates with it. Rejected on reachability — there is no bulk-creation path for locations anywhere in the app, and #1001 measures the location UI freezing from about 2,000 — so the cap is unreachable before the product becomes unusable for other reasons. | Whether a bulk-location feature ever lands. The one-line fix (return `hasMore`; have the caller flag or refuse) is noted against that day. |
| Phase 12 | `bridge/src/api/head.ts:31-70` | Deferring the response head leaves `res.headersSent === false` for a HEAD's whole lifetime, so `server.ts:667`'s "already committed?" guard always takes the wrong branch: a HEAD whose handler committed and then threw answers 500 where the GET answers 200, with a `Content-Length` describing nothing. Rejected because the trigger does not exist today — `itemsCsvRow` is total over real rows, and `items-csv.ts` is the only incremental writer a HEAD can reach, with later-page failures caught before they reach `server.ts`. | Nothing today. It is a latent trap for the first streaming route added after this one, and a docstring qualification plus a note at `server.ts:667` would defuse it. |
| ~~Phase 12~~ **resolved by Phase 17** | `src/features/reports/report-csv.ts:37-51` | A second, independent CSV implementation — its own formula-injection trigger, its own quoter, its own `toCsv` — whose docstring says it "mirrors the items-CSV exporter", with no drift test and against the `Tabular export seam` note's "don't hand-roll one". The two agree behaviourally today; the only divergence (`!isNumber` versus `typeof value === 'string'`) affects booleans, which never start with a trigger character. App-side, so outside this phase's scope. **Phase 17 (P17-19 / P17-27) settled it and filed it.** This row's behavioural analysis is confirmed exact — a fifteen-value matrix through both quoters agrees on every value, booleans included. What it did not record is a **second and materially different divergence**: `report-csv.ts:56` writes the header row as a bare `header.join(',')` with no escaping at all, where the seam runs every heading through `delimitedCell`. And the substitution is behaviour-preserving — rewriting `toCsv` as a wrapper over the seam's leaves 32 files / 528 tests green and lets 1,242 characters be deleted — so `CLAUDE.md` rule 1 applies rather than rule 3. | **Settled.** Filed as #1557. |
| Phase 12 | `bridge/src/ical/feed.ts:126-127` | `calendarModifiedAt = Math.max(snapshotMs, startOfUtcDay(now), startOfLocalDay(now))`. A snapshot whose `generatedAt` is behind the current day's start — reachable through clock skew, since `generatedAt` is an unvalidated `Date.now()` from whichever device wrote it — pins the calendar's entity-tag to the day boundary and collapses every change that day onto one tag. Wider than the second-flooring defect filed as #1368, and independent of it. | Taken up by Phase 13, confirmed by driving the real server (three different documents, one `ETag`, `304` on revalidation) and filed as **#1418**. |
| Phase 12 | `bridge/src/api/search` error bodies | Error responses echo the offending token back unbounded and unsanitised — `?fields=<script>alert(1)</script>` is reflected in full, as is a 500-character field name. No path, SQL or stack ever reaches the body, and the content type is correctly `application/json; charset=utf-8`, so no modern browser executes it. But the bridge sets **no** `X-Content-Type-Options: nosniff` and no `Content-Security-Policy` on any response. | Whether unbounded attacker-controlled reflection into a body served without `nosniff` is worth hardening, and whether the echo should be truncated regardless. |
| Phase 12 | `src/db/repositories/item/search.ts:183` | An offset-paged `$filter` page orders by `name COLLATE NOCASE, serial_no, created_at` with no `id` tiebreak, so it is not a total order — the "repeats a row on page 2" hazard the keyset-pagination note names. `:208-210`'s own comment acknowledges the trade ("an offset read's ordering is not the total order the cursor is defined against"), so it is deliberate. A bulk import creating many identically-named items inside one millisecond would make it bite. | Whether the tiebreak is worth adding, or whether the recorded trade still holds. |
| ~~Phase 11~~ **resolved by Phase 17** | `src/features/settings/SettingsDialog.tsx:196-197` and five other files | The dialog's docstring says "Every control also carries a rich-Markdown `SettingRow.hint`". Across every file rendering a `SettingRow` it is **10 of 69** without one. Candidate U8-3 was rejected because the *search* consequence it claimed does not hold — five of six plausible queries still find the hint-less row via its label and description. **Two corrections from Phase 17 (P17-41):** the location recorded here was `:1631`, which is **stale** — that line is the tail of a comment inside the `allTabs`/`tabs` rail-tab assembly, and the docstring is at `:196-197`. And the "including the Danger zone's *Erase data*" clause **does not survive**: `SettingRow`'s `description` prop is required and `hint` renders only an additional badge, so that control is fully described. | **Settled.** Filed as one limb of #1569. |
| Phase 11 | `src/features/users/components/RoleFormDialog.test.tsx:157` | The test named "keeps a grant this build does not recognise, and says so" never submits and never asserts the grant is kept. Its *other* assertion is genuinely mutation-sensitive, so U2-4 was rejected — but the coverage the name promises exists only one layer down, in `role-grants.test.ts:109`, and a regression in the dialog's **wiring** to that seam would be caught by nothing. | Nothing — a test-name fix, or the missing submit assertion. Too thin to file alone; sweep material. |
| Phase 11 | this document's own method | Two verifier sub-agents independently chose the scratch path `src/features/calendar/CalendarScreen.audit-scratch.test.tsx` and overwrote each other mid-run; one appended into the other's file before noticing. Both recovered, and the worktree finished clean — but a future phase running verifiers concurrently should require a per-candidate prefix in the scratch filename, not just the `.audit-scratch.` suffix. | Nothing — a convention change for §3.2. |
| Phase 10 | `src/features/inventory/components/ItemCard.test.tsx` | A full-suite run at the pinned commit showed six pre-existing failures in this file ("File path" assertions and a portaled dialog combobox). Unconnected to anything this phase touched, and noted only so it is not mistaken for fallout. | A run on a clean checkout to confirm it is environmental rather than a real regression on `main`. |
| Phase 13 | `bridge/src/events/webhook-ssrf.ts:95`, `bridge/src/identity.ts:152-154`, `bridge/src/mdns/records.ts:143-155`, `bridge/src/mdns/discovery.ts:113-126` | **Four security findings were confirmed and deliberately not filed**, following the precedent Phase 12 set, and handed over privately instead. In descending seriousness: an IPv4-mapped IPv6 literal in a webhook URL is classified publicly routable, so any user's subscription makes the bridge issue requests to loopback, the LAN and `169.254.169.254` with the private-destination guard on and no bridge credential — delivery to a loopback listener proved end to end; the mDNS responder decodes an attacker-sized datagram synchronously on the HTTP event loop, so ~18 packets a second from any LAN host stall request serving (`/health` 8 ms → 131.7 ms, measured, no credential); the iCal feed is gated on `bookings:read` alone while publishing loans, maintenance and warranties, so the narrow subscription role the README and wiki both prescribe reads borrower names, schedule notes and item details its role withholds; and the mDNS discovery map is uncapped and copied per datagram, so one on-LAN flood spikes the heap to ~305 MB inside the 2.5-second startup window. Every one is reproduced; the register rows carry the demonstrations. The first two need no credential at all. | The maintainer's decision on disclosure and sequencing. Nothing is public. Note that **#1389** (the unbounded MQTT inbound buffer, an equivalent trust-boundary denial of service) *was* filed before that decision was sought — it is public and labelled `security`, and the maintainer may want it closed or handled privately instead. |
| Phase 13 | `src/db/repositories/AssetBookingRepository.ts:343`, `src/db/repositories/MaintenanceRepository.ts:183-186` | Both `listUpcoming` reads are offset-walked — by the calendar feed and by the app's Upcoming agenda (`src/features/calendar/useAgenda.ts:124`, `:166`) — over orders with no tiebreak, which the project's own policy says must have one: `CheckoutRepository.listOpen` and `AssetBookingRepository.list` both carry an `id` tiebreak whose docstring names the exact hazard (#132). #132 fixed the two reads its export walked and did not look at these two. Rejected as a finding because it does not bite: 250 rows sharing one sort key, three insertion orders, and a raw `node:sqlite` probe to 20,000 rows with a 32 KiB cache to force a sorter merge all returned every row exactly once. It holds because the SQL text is identical across pages, the statement is cached, and the bridge hydrates with no `sqlite_stat1`. | Whether an unguaranteed order that currently holds is worth hardening. It becomes live under a future SQLite, a plan change, or an `ANALYZE`-stamped `.sqlite` data source. The fix belongs in the repositories, not in `bridge/src/ical/feed.ts`. |
| Phase 13 | `bridge/src/homeassistant/client.ts:188-196` | `await response.json()` buffers the whole Home Assistant response with no byte cap, where every inbound path in the bridge is bounded (`readJsonBody(req, MAX_BODY_BYTES)`, `config.maxPushBytes`). Under `--max-old-space-size=256` — a plausible Raspberry Pi setting — the process **dies** (`exit 134`) at roughly 110 MiB of upstream body; peak RSS is about 4× the body size. Rejected as a defect on reachability: a real 2,000-entity `/api/states` is 1.05 MiB and costs 7 MB, and an operator-configured hostile instance is a boundary already crossed. Its only meaningful attacker path is the held mDNS discovery finding, along which credential disclosure strictly dominates an OOM. | Nothing on its own. It is the consequence to cite if the held mDNS discovery finding is actioned — once the destination is attacker-chosen, the unbounded read also kills the process. |
| Phase 13 | `bridge/src/mdns/records.ts:21-22`, `custom_components/gubbins/manifest.json` | The DNS-SD service type exists in two code locations across two languages, related only by a descriptive comment, with no drift test. Rejected on the same ground Phase 0 used for `Run.ps1`'s duplicated base path: the comment makes no parity claim, so the "mirrors X" rule does not fire. Two further facts argue against acting: the two strings are **not** byte-identical (the manifest carries the DNS-SD root dot, so a naive text comparison fails on correct code), and a DNS-SD service type is a wire contract whose rename breaks every installed integration regardless. | Nothing. Recorded so a later phase does not re-find it. |
| Phase 13 | `bridge/src/mqtt/client.ts:339-343` | The offline-buffer replay's effect on Home Assistant's recorder could not be settled here — no live Home Assistant was available. What is measured is that 100% of the buffer's contents are superseded by the reconnect republish, and that no discovery config sets `force_update`, so an idle outage produces no update events at all. The residual claim in #1394 — that values which *did* move during an outage land in the history at the wrong instant — rests on Home Assistant's documented MQTT-sensor semantics rather than on a recorder table read. | Subscribing a real Home Assistant instance to the bridge's discovery topics, replaying a 240-packet retained burst carrying two distinct values, and counting rows in `SELECT COUNT(*) FROM states WHERE entity_id='sensor.gubbins'` before and after. |
| Phase 13 | `bridge/gubbins-bridge.service:57` | The `AF_NETLINK` finding (#1402) was settled from `systemd.exec(5)` and systemd's own `seccomp-util.c`, with the address-family filter reproduced byte-for-byte in libseccomp inside a Linux container rather than inferred. No systemd host was available, so the one thing not demonstrated end to end is that the reproduced filter matches a live unit. | One command on any Linux host with systemd: `systemd-run -p RestrictAddressFamilies='AF_INET AF_INET6' --pty --wait node -e 'require("os").networkInterfaces()'`. |

| Phase 14 | `custom_components/gubbins/__init__.py:188`, `:198` | Entry setup awaits `/health` then `/api/v1/status` serially before forwarding a platform, and the comment at `:194-197` says the status result is deliberately *not* a gate on setup. Confirmed as fact and measured (setup tracks the delay 1:1; backgrounding it collapses ~9 s to ~0.11 s with a byte-identical end state) but **not filed** — no warning is armed around `async_setup_entry` in HA 2025.1.4, entries are set up concurrently so HA's startup is unaffected, and on the real bridge the two endpoints are 2.00 vs 2.12 ms. | A decision on whether the tidy-up is worth making against the comment's own logic. |
| Phase 14 | `custom_components/gubbins/config_flow.py`, `bridge_id.py` | **C3's residual, after the fix.** The credential-free chain is closed, but an attacker who already knows a bridge's id can still relocate its entry with one advertisement and receive the token. That half is Home Assistant's own sanctioned pattern (`_abort_if_unique_id_configured` with `updates=`), and the id is public by design whenever `GUBBINS_BRIDGE_MDNS=on`. Closing it needs either a probe of the old address before relocating, or user confirmation — both of which change what #672's automatic heal promises. | A decision on whether discovery-based healing should be trust-on-first-use, and whether the wiki should say so. |
| Phase 14 | `custom_components/gubbins/` | **The repository ships no Python test harness at all** — no pytest, no `conftest.py`, no `test_*.py`. This phase installed Home Assistant 2025.1.4 and `pytest-homeassistant-custom-component` into a scratchpad to drive the integration, which is what turned most of this phase's HA candidates from unverifiable into demonstrated. Nothing of that is in the repo, so the C3 fix and every finding above have no automated guard. #749 asks for `hassfest` and `hacs/action`; neither drives the component. | Whether a Python test harness is worth adding, given #749 already covers the packaging half. |
| Phase 14 | `custom_components/gubbins/` (entity names) | Rejected as a feature gap under §1, but recorded because the mechanism is genuinely unwired rather than merely unused: no `translation_key` anywhere and no `entity:` block, so a complete `translations/de.json` was shipped in the harness and every entity name stayed English. HA's own quality scale has an `entity-translations` rule, and the remedy is small. | Whether to file it as an `enhancement`; nothing in the repo or the wiki currently claims the integration is translatable. |
| Phase 14 | `extension/src/background.ts:530-533`, `content-script.ts:270-288` | A7 was rejected because the trigger does not exist — no reachable parser can emit a payload the wire schema rejects. The **delivery path still validates nothing**, and the downstream half was demonstrated end to end (posted, acknowledged as delivered, cleared from the queue, dropped by the app's schema with no log). Adding one marketplace parser, widening the sender check, or letting the generic parser onto this path makes it silent data loss. | Whether to close it pre-emptively, or leave it until a change makes it reachable. |
| Phase 14 | `src/features/scraping/parsers/amazon-parser.ts` | B6's conclusion turns on one fact I could not establish: the real weight of a distributor product page. The cost curve was measured instead (~13 ms/MB at 1×, ~78 ms/MB at 4×), so at 200–500 KB #1426 is not worth acting on and at 1–3 MB it is. | Saving one real, scrubbed supplier page as a fixture would settle it permanently, and would serve the parser tests too. |
| Phase 14 | `homeassistant/custom_sentences/en/gubbins.yaml`, `src/features/home-assistant/steps/SentencesStep.tsx:204-215` | E2 covers the `custom_sentences` file. The in-app guide's other branch wires the same phrasings as **sentence triggers**, which Home Assistant checks *before* intent recognition, and its patterns carry an optional `[the]` that would also match a bare "where is Dad". That branch may shadow the built-in person answer more severely, and it was **not** driven — the harness hit a `hassil` version mismatch, not a product limit. | Re-running E2's A/B against the trigger branch with the `hassil` version Home Assistant pins, or a newer Home Assistant. |
| Phase 15 | `P:\Source\!Memories/Gubbins/The auto-review skill and autonomous issue landing.md` | All four memory-note ids `CLAUDE.md` cites (`i18n-typed-catalog-seam` `:161`, `feedback-worktree-junction-cleanup` `:312`/`:377`, `gubbins-concurrent-agents-worktrees` `:342`, `auto-review-skill` `:362`) fail the documented `cat "P:/Source/!Memories/<folder>/<title>.md"` lookup. Three are recoverable through each note's `aliases:` block; this one has none. Rejected as a finding (§10 P15-C4) because the SessionStart hook emits all 135 titles into every session, but the vault-side fix is one line. | Add `aliases: [auto-review-skill]` to that note's frontmatter. Vault-side, outside the repository, so the audit cannot do it. |
| Phase 15 | `SECURITY.md:22` | The affected-area list ("the web app, the optional bridge / integrations, or a dependency") omits the browser extension, the one component with an explicit privilege boundary (`.githooks/pre-push:11-17`, #557). Rejected (§10 P15-R5) because the advisory intake is a single free-text destination with no picklist, label or queue to misfile into, and the line is content guidance rather than a scope statement. | A maintainer's judgement on whether four extra words are wanted. Nothing to demonstrate. |
| Phase 15 | `.github/pull_request_template.md:9-14` | The checklist omits the two `CLAUDE.md` **(mandatory)** rules that bind an external contributor and have **no local hook** — the wiki rule and `npm run lock`. Rejected (§10 P15-K6) because the checklist claims no CI parity and the template is deliberately retained for the discouraged external-fork path (`CONTRIBUTING.md:78-90`), with 29 PRs to date of which 28 are Dependabot and one predates the template. | Whether the maintainer wants the two gates on a template no one has yet filled in. |
| Phase 15 | `docs/dev/PHASE_HANDOVER.md:1` | The file has **no preamble**. Line 1 is straight into the Phase-83 heading, with nothing saying it is a newest-first stack of dated, completed handovers whose `§0`–`§7` subsections belong to the block above them. Three candidates this phase (P15-D8, D9, D10) were rejected as records only after establishing that structure from ancestor headings 90+ lines up. | One sentence of framing at the top. Not filed because no sentence in the file is itself false. |
| ~~Phase 15~~ **resolved by Phase 17** | `scripts/check-bundle-size.mjs:19-20` | `PRECACHE_EXTENSIONS` claims to "mirror vite-plugin-pwa's precache globs" and omits `png`, which `vite.config.ts:218` includes — a `CLAUDE.md` "mirrors X" parity claim with no drift test. Noticed by the `docs/dev` finder, outside its unit. **Phase 17 re-checked it at this SHA and answered the open question: yes, the omission does change the reported total** — four icon PNGs totalling 109,777 B are precached and uncounted. But the whole of it is **already inside #719**, whose body names `:11`, `:20`, `vite.config.ts:218`, the `png` omission, the webmanifest, the counted-but-not-precached `sw.js`, and reproduces the exact 5-file / 108.32 KiB gap. | **Settled — no residue.** Wholly within #719 (recorded at P0-15). Nothing further to file. |
| Phase 15 | `bridge/src/mcp/tools.ts:385-386` | `gubbins_check_out` presents four borrower routes as equally available, while only `contactName` and `locationId` are reachable from within MCP, and there is no `projectName` twin to `contactName`. There is also no `/projects/:id` route in the app, so a *user* cannot read a project id off the URL bar to hand over. Recorded under §10 P15-M5's rejection as a feature request, not a text defect. | A maintainer's call on whether a contacts/projects read tool, or a `projectName` argument, is wanted. |
| **Phase 17 — ⚠️ TRUST BOUNDARY, DELIBERATELY NOT FILED** | `reconcile.ts:579`; `snapshot.ts:1104`, `:584`, `:1268-1273`; `tombstone.ts:220,228`; `v1-initial.ts:302-309`; `snapshot-integrity.ts:51`; `unique-keys.ts:147` | **A snapshot tombstone naming a built-in `roles` row strands every device on a shared sync, permanently, and a brand-new device cannot pair.** Confirmed and demonstrated (§10 P17-6), `unusable`. Built-in roles are **editable**, so — unlike the protected `locations` and `users` rows, which `TABLE_FILTER` keeps out of the local snapshot — they travel in every snapshot. A remote tombstone naming one therefore becomes `DELETE FROM roles WHERE id = ?`, `trg_roles_protect_builtin_delete` raises ABORT, and **the whole merge transaction rolls back**. Not self-healing: the same remote snapshot produces the same plan every time, and `writeSyncMeta` is never reached. The 180-day tombstone-TTL clone *succeeds* and then **copies the poison tombstone into the local table**, so the recovered device republishes it. A factory reset or a brand-new device is equally blocked, because the baseline re-seeds the built-in roles. The Danger Zone's "Sync links & pending deletions" clears only *local* tombstones, and Safe Mode touches only the local database — **there is no route out.** The precondition is a crafted, corrupt or foreign snapshot, which `tombstone.ts:214-219` names as inside the project's own threat model ("a backup file, a peer's snapshot in a shared sync folder, or a bridge push"), and **the shipped providers are a shared folder and a shared Drive folder**. The gap is precise: the project already holds the protected-row-id fact twice (`ALWAYS_PRESENT_ROW_IDS`, `isProtectedRole`) and consults neither on the tombstone-delete path. | **A maintainer decision, not a verification gap.** Nothing further is needed to establish it. What is needed is a decision on **disclosure**: whether this is fixed quietly first and filed afterwards, filed as a normal `security`-labelled issue, or handled under `SECURITY.md`'s private route. Filing it publicly would publish a working denial-of-service recipe against every shared-folder sync before a fix exists, which is why Phase 17 withheld it, following the precedent Phases 12, 13 and 14 set. |
| Phase 17 | `index.html:61-64` | The root element's fixed `#0b0b0f` background means CSS background propagation gives the **canvas** that colour in every appearance mode, so the area outside `body`'s box stays near-black in Light. The mechanism is confirmed — `getComputedStyle` gives `rootBg: rgb(11,11,15)` against `bodyBg: oklch(0.99 0.003 280)` with `htmlClass: ""` — but **where it is visible could not be driven here**: it shows as elastic / rubber-band overscroll on iOS and macOS Safari, which headless Chromium does not render. The `theme-color` and manifest limb of the same finding **was** certain and is filed as #1563. | An iOS or macOS Safari device, Appearance set to Light, rubber-banding a long Inventory list. |

## 12. Baselines

Filled by Phase 0 (gates) and Phase 18 (performance). A later phase that re-runs a gate and gets a
different result records both, with the SHA.

### 12.1 Gates (Phase 0)

All run in the primary checkout, which was clean at the pinned SHA `9924e6a7`.

| Gate | Command | Result at SHA | Notes |
| --- | --- | --- | --- |
| Type-check | `npm run type-check` | pass | all three tsconfigs (app, bridge, extension) |
| Lint | `npm run lint` | pass | exit 0 with **38 warnings**; `eslint .` carries no `--max-warnings`, so the accepted baseline is unpinned. 37 are `react-refresh/only-export-components`; the 38th is an unused `eslint-disable` at `LookupMatchDialog.tsx:74` |
| Format | `npm run format:check` | pass | "All matched files use Prettier code style" |
| Unit suite | `npm run test:run` | pass | 800 files / 12448 tests / 82.34 s. One intermittent failure under full-suite load (`CategoryLookupPanel.test.tsx:429`) passed in isolation in 2.24 s and on a clean re-run |
| Bridge suite | `npm run test:bridge` | pass | 79 files / 1191 tests / 7.73 s |
| Bridge smoke | `npm run smoke:bridge` | pass | both `mcp.mjs` and `serve.mjs` boot; 6 tools; `/health` reports `itemCount=4` |
| Build | `npm run build` | pass | precache **209 entries (6197.30 KiB)**, of which 205 unique URLs — 4 icons are listed twice and `src/sw.ts` de-duplicates them deliberately. Two chunks exceed Vite's 500 kB warning |
| Bundle check | `npm run check:bundle` | pass | reports `6108.38 KiB across 201 precache files`, which is **wrong** — see #719 |
| Browser smoke | `npm run test:e2e` | **fail (exit 1)** | 125/125 steps passed, 0 console errors, **4 page errors** (`AbortError: Transition was skipped`, one per browser context). Driven against a dev server on port 5231. See #735 |
| Wiki check | `npm run wiki:check` | pass | 98 pages, 44 images |
| Lockfile | `npm run lock:check` | pass | "Lockfile OK — `npm ci` accepts it". **Needs no Docker** — `--check` only runs `npm ci --dry-run`; the "needs Docker" note above was wrong and is corrected here |
| Extension build | `npm run build:extension` | pass | built to `extension/dist` |
| `npm audit` | `npm audit` | pass | 0 vulnerabilities |
| `npm outdated` | `npm outdated` | 20 behind | none blocking; `typescript` 6.0.3 vs 7.0.2 and `eslint` are the #186 / #401 boundaries |

### 12.1a Gates re-run (Phase 17)

Phase 17's scope includes `npm audit` and the dependency licences, so both were re-run at its pinned
SHA `3864f22d` from the primary checkout. §6's rule is that a re-run with a different result records
**both**, so the Phase 0 rows above stand as written and the change is recorded here.

| Gate | Command | Result at `9924e6a7` (Phase 0) | Result at `3864f22d` (Phase 17) | Notes |
| --- | --- | --- | --- | --- |
| `npm audit` | `npm audit` | pass — 0 vulnerabilities | **fail — 2 high** | **Regression.** `browserslist <=4.28.6` (GHSA-c83g-rgw3-j3cx, GHSA-73wf-gq98-2v4g) and `fast-uri 3.0.0-3.1.5` (GHSA-5jgf-p345-68v8, GHSA-f65p-4m7j-42xc, GHSA-fph4-wmhf-6fwf, GHSA-jqff-g426-hqxp). `--package-lock-only` returns the identical `{high: 2, total: 2}`, so it is the lockfile's answer rather than a stale local install; `--omit=dev` returns 1 (`fast-uri` only). **No shipped code is affected** — `browserslist` is dev-only, and `fast-uri` counts as production via `@hookform/resolvers` → `ajv` but the app imports only the `/zod` subpath, whose built module imports no `ajv`; a grep of the built assets for `ajv` returns nothing. Both are fixable **inside the already-declared ranges**, so this is an `npm run lock` refresh rather than a `package.json` change, and therefore outside #186 / #401. Nothing in `.github/workflows`, `.githooks` or `scripts` runs `npm audit`, so nothing would have surfaced it. Filed as #1561 (§10 P17-37) |
| `npm outdated` | `npm outdated` | 20 behind | 20 behind | Unchanged. `typescript` 6.0.3 → 7.0.2 and `eslint` remain the #186 / #401 boundaries. `tesseract.js-core` showing "Latest 6.1.2" below "Current 7.0.0" is a dist-tag artefact, not a downgrade |
| Lint | `npm run lint` | pass, 38 warnings | pass, 38 warnings | Re-confirmed **byte for byte**, including the one non-`react-refresh` warning at `LookupMatchDialog.tsx:74:15`. 2097 files linted, 0 errors |
| Dependency licences | `npm ls --json` + each package's `license` field | not run | **clean** | 566 distinct installed packages: 474 MIT, 32 Apache-2.0, 21 ISC, 13 BSD-2-Clause, 9 BlueOak-1.0.0, 7 BSD-3-Clause, 5 MPL-2.0, plus one each of `(Unlicense OR Apache-2.0)`, CC-BY-4.0, Unlicense, CC0-1.0 and `(MIT OR CC0-1.0)`. **Zero** packages with no licence field, zero `UNLICENSED`, zero "SEE LICENSE IN", zero GPL / LGPL / AGPL / EPL / CDDL / SSPL / BUSL. The only non-permissive entries are the five MPL-2.0 (`axe-core` via `eslint-plugin-jsx-a11y`; `lightningcss` and its win32 binaries via `@tailwindcss/vite` and `vite`), all **dev-only** — `npm ls axe-core lightningcss --omit=dev` is empty — and MPL-2.0's file-level copyleft imposes nothing on MIT code that merely uses the tool. Coverage is whole-graph: the lockfile holds 649 packages, 75 are absent from this Windows `node_modules`, and **none of the 75 is non-dev** — every one is a per-platform binary of a family already scanned |

### 12.2 Performance (Phase 18)

| Surface | Seed | Throttle | Measurement | Plan / trace summary |
| --- | --- | --- | --- | --- |

## 13. Change log

| Date | Phase | Change |
| --- | --- | --- |
| 2026-08-30 | — | Plan written. Twenty phases (0–19) partition every tracked path; no phase started. |
| 2026-08-30 | 0 | Phase 0 run and landed at pinned SHA `9924e6a7`. 54 candidates: 38 confirmed, 11 rejected, 3 unverifiable, 2 duplicates of existing issues. The 38 confirmed became 31 issues (#719–#749) — seven were folded into a sibling issue rather than filed alone, because they shared a root cause and a fix. §12.1 baselines recorded — every gate passes except `npm run test:e2e`, which is itself finding #735. Coverage check run: four sub-top-level paths assigned (see §8), two deliberate overlaps left. Corrected §12's claim that `lock:check` needs Docker. |
| 2026-08-30 | 1 | Phase 1 run and landed at pinned SHA `16ef2e07`. 77 candidates: 46 confirmed, 30 rejected, 1 unverifiable, 0 duplicates of existing issues. The 46 confirmed became 34 issues (#750–#783) — twelve were folded into a sibling issue sharing a root cause, and six unguarded parity claims were filed as one sweep (#775) following #254's precedent. Six finder units and twenty-six verifier passes ran. Notable: #764 is a regression against #164 and #172 (the index those added has never been chosen); #774 rose to `data-loss` when a second symptom showed the archive discards the attribution before pruning; #778 was reframed from a driver-fidelity claim into an unbounded-money-write defect. Eight carry-over entries added to §11, including one candidate (the ledger's missing `(created_at, id)` index) that arose while disproving another and has had no verification pass of its own. |
| 2026-08-30 | 2 | Phase 2 run and landed at pinned SHA `9072d220`. 56 candidates: 43 confirmed, 10 rejected, 2 duplicates of existing issues, 1 unverifiable. The 43 confirmed became 33 issues (#784–#816) — three were folded into existing issues as measured comments (#764 twice, #768) and seven into a sibling issue sharing a root cause and a fix. Seven finder units and eighteen verifier passes ran, on a seeded 50k-row database with no statistics. Notable: the verifiers overturned the finders on ten points, including a claimed doc-comment contradiction that proved defensible once the right query shape was measured (P2-36), a proposed batching fix that measured as a wash against the round trips it removed (P2-18), and three severity claims that the measurement did not support (P2-11, P2-12, P2-1). Two findings were widened by verification rather than narrowed: P2-21 grew from "picks arbitrarily among duplicates" to "the match key is not an identity key", and P2-41 from two parity cases to three. A second, detached worktree was used for verification so that the test unit's deliberate mutations could not corrupt a concurrent verifier's run. Six carry-over entries added to §11, one of them a correction to a durable note outside the repository. |
| 2026-08-31 | 3 | Phase 3 run and landed at pinned SHA `52fa966f`. 110 candidates: 74 confirmed, 33 rejected, 3 carry-over. The 74 confirmed became **52 issues (#817–#868)** plus 6 comments on existing issues — the rest were folded into a sibling issue sharing a root cause. Ten finder units and nineteen verifier passes ran. **The headline question of the phase — "find a repository write the ACL does not check" — was answered: there is none.** Two independent enumerations agree on 157 permission-gated mutating methods, of which exactly three assert nothing and all three are documented; no method touches the driver before its guard; all four internal repository-to-repository constructions pass the collaborator seam. The claim must be worded *no **unguarded** write*, though: the tag and supplier merges are guarded with the **wrong key** (#821), which a denied-authority sweep cannot see. Notable corrections by verifiers: two candidates were **disproved outright** on their premise (L-2's row-shape claim missed the guard's constant-folding step; C5-15's guard is on the column, not the repository), one finder's *fix* would have broken a dashboard badge (C3-3 needs a predicate, not a `LIMIT`), one finder's demonstration showed the **benign** ordering and the real one had to be found under a driver model (C5-7), one finder's measurement was wrong by a factor of five (C9-7, 25% undetected over 20 runs, not "solid"), and half of C3-7 was disproved over two million samples. Three candidates were **widened into more serious findings**: C4-1 from differing ids to a demonstrated double-landing *and* stock destroyed on the return path, U-4 from "no reachable divergence" to an account the app can neither delete nor disable, and C8-6 from a missing seam to a `data-loss` action that two documents describe as safe. **The audit's own tooling caused a real incident**: an unbounded recursive walk over a cyclic `locations` table (#819) exhausted the host and crashed it mid-phase, which is itself the demonstration for that issue; the phase was resumed from saved agent transcripts with no loss of work, and later interrupted twice more by session limits and resumed the same way. §5.3 was honoured for the one exploitable finding: the maintainer was asked before filing and chose a standard public issue. Eight carry-over entries added to §11. |
| 2026-08-31 | 4 | Phase 4 run and landed at pinned SHA `983986fd`. **64 candidates: 49 confirmed, 14 rejected, 1 duplicate of an existing issue (#750).** The 49 confirmed became **40 issues (#869–#908)** — nine were merged into a sibling sharing a root cause and a fix, including one five-item parity sweep (#901) following #254's precedent. Six finder units and twenty verifier passes ran. **Four candidates were reported independently by two finders** (U1-2≡U4-4, U1-5≡U2-7, U3-7≡U4-7, U5-5≡U6-9) and are recorded once. Seven findings are `data-loss` and four `unusable`. **The verifiers overturned or reshaped the finders on fourteen points.** Two candidates were rejected because their own mutation disproved them: U2-5 predicted a silent failure that produced 28 failures across 11 files, and U1-7's claim that two loops were untested was disproved by 13 and 46 tests entering them. Two security framings were deflated after investigation — U4-1 to robustness (no remote ingress; the project's own precedent in #641/#762 is not security-framed), and U6-4's privilege-escalation limb rejected outright, because read enforcement lives at the screen boundary by the decision recorded in #522, so the momentary unrestricted window has nothing to bypass. Three performance candidates were rejected **on measurement**: U5-13's poll costs 0.41 s of main-thread work per day, and U2-9's 88 rescans cost 2.4 ms with a best-case saving of 1.5 ms. Conversely U2-11 was rated LOW and unreachable by its finder, and the verifier confirmed it, found the route via #818 and widened it to three limbs; U5-12's finder proposed a mutation that proved the opposite of its claim, and inverting it exposed the real one-sided gap; and **U2-3b was found by a verifier while disproving U2-3**, which is now the settled answer to the register's carry-over P2-51 (`unverifiable` → `rejected`, on #542's own closing comment). Six carry-over entries added to §11, including two limbs that genuinely need hardware this session did not have. Every scratch test and every mutation was reverted before landing; the worktree carried no change outside this document. |
| 2026-08-31 | 5 | Phase 5 run and landed at pinned SHA `97a7e06c`. **112 candidates: 81 confirmed, 23 rejected, 8 duplicates of existing issues.** The 81 confirmed became **55 issues (#909–#963)** plus **8 comments** on existing issues (#243, #546, #209, #227, #228, #575, #232, #112) — the rest were folded into a sibling sharing a root cause and a fix, including three sweeps (#918 six persisted stores, #957 four guard holes, #961 four stale comments). Twelve finder units and roughly thirty verifier passes ran. **Four candidates were reported independently by two finders** (U9-C4≡U6-2, U11-C3≡U4-C3, U12-C5≡U4-C4, U12-C6≡U4-C6) and are recorded once. One finding is `data-loss` (#909), six are `unusable`. **The headline finding is #909**: the query client is left on TanStack's `'online'` default, so a write attempted on a flaky connection hangs on a spinner, is **discarded** if the tab closes, and reads return the *previous* result set rather than a blank — all while the pill promises "changes are saved locally". It went unnoticed because `onlineManager` never reads `navigator.onLine`, so a cold offline launch is unaffected. **The verifiers overturned or reshaped the finders on more than twenty points.** Six candidates were disproved outright on their premise (`useFullscreen`, `text-terms`, `derived-uuid`, `route-suggestions`, `apportionMoney`, `useConfirmSaved` re-entrancy). Four performance candidates were rejected **on measurement**, one of them (`Intl.PluralRules`) after establishing that CDP throttling over-applies ~6x to `Intl` construction — an instrument artefact now recorded in §11. Several headline limbs died while a sharper one survived: U7-GUARD-4's claimed #258 regression is false, but U7-GUARD-5 has a **live shipped call site** already in the vulnerable shape; U12-C2's five bypass shapes have no offender, but the guard's hard-coded class name lets `DbError` through. Conversely U11-C2 widened from a menu-focus case to "with the shipped bindings a keyboard user cannot rename a location", and U4-C1 from a config default to three demonstrated limbs. **Two verifier contradictions were settled by the lead** with its own component test (the 404 dialog does reopen on a second focus) and by reconciling two recovery-race runs whose asset cache headers differed. Findings were driven in three real browser engines, two static production builds, a Docker image at a non-root base path, and a deliberately corrupted database file. Six carry-over entries added to §11, including one limb that needs a human-driven install-eligible session. Every scratch test was removed before landing; the worktree carried no change outside this document. |
| 2026-09-01 | 6 | Phase 6 run and landed at pinned SHA `7ae84bd3`. **79 candidates: 58 confirmed, 13 rejected, 7 duplicates of existing issues, 1 unverifiable.** The 58 confirmed became **32 issues (#964–#995)** — the rest were folded into a sibling sharing a root cause and a fix, including four sweeps (#983 three latent Button traps, #984 two tests that cannot fail, #990 five wrong comments across six files, #982 two latent dialog faults). Eight finder units and thirteen verifier passes ran. One finding is `data-loss`: **#964**, where a single arrow key on a bounded number field replaces a decimal-comma entry — the form the field deliberately preserves so the user can correct it — with the bottom of its range. **Six verifiers were killed mid-run by a session limit.** Five had left scratch tests in the worktree; those were re-run and read rather than re-derived, salvaging demonstrations for eight candidates including two clean verifications (`colour-input` and the live-call-site half of the number-field `className` claim). One had left a config mutation and another an instrumentation patch in tracked files; both were found and reverted before landing, and a later verifier that clobbered the patch while reverting its own mutation restored it byte-for-byte and said so. **The verifiers overturned or reshaped the finders on more than twenty points.** Four candidates were disproved on their premise (`rail-modal`'s purity comment, the `interstitial` key collision, the F9 "Reduce effects" copy, the Markdown table swallowing prose — which is faithful GFM). Three were rejected because their live ingress turned out to be a **documented decision** or to have no ingress at all: the segmented group's blank-value fallback is spelled out at its own call site, and padded picker labels cannot arise because every name write trims. Two were rejected as belonging to later phases (dead icon exports → Phase 17) and two as hardening with no live offender (the popover's 240 px restatement, `Select`'s missing `onBlur`). Conversely several findings **grew** under verification: the scrim's hard-coded blur widened from two surfaces to eight; the region-canvas handle defect proved to begin below ~9.6% region size rather than ~5%, making it common rather than marginal; and the alerts-badge claim was **narrowed and then re-based** — the count does reach the menu row, but the live region that announces it exists only on the dashboard while the badge renders everywhere. Two performance candidates were measured in a real browser at 4× CPU throttling: both confirmed, and both filed with their own limits stated — the resize storm on visible state loss rather than its timings (which sit in the band this audit has twice rejected), and the periodic rescan explicitly as a CPU and battery tax rather than a jank source, since frames containing a rescan drop no more often than frames without one. Eight carry-over entries added to §11, including two limbs needing hardware this session did not have (a screen reader for the toast's nested live regions, a real IME for the composition rewrite). Every scratch test was removed and every mutation reverted before landing; the worktree carried no change outside this document. |
| 2026-09-02 | 7 | Phase 7 run and landed at pinned SHA `a28a061c`. **88 candidates: 79 confirmed, 5 rejected, 4 duplicates of existing issues (#601, #772, #801, #844).** The register carries 92 rows, because two candidates were split into limbs that took different verdicts. The 79 confirmed became **69 issues (#996–#1064)** plus **4 comments** on existing issues (#715, #783, #1009, #1018) — the rest were folded into a sibling sharing a root cause, including three sweeps (#1002 four missed query keys, #1018 six false comments, #1029 two faces of one tokeniser). Eight finder units and **41 verifier passes** ran, each in its own detached worktree so that a verifier's deliberate mutations could not corrupt a concurrent run — Phase 2's two-tree practice, needed here at scale. **Four findings are `data-loss`** (#996 cross-mode merge, #997 relation tombstone, #998 the import mapping drop, #1047 the wiped parameter field) and four `unusable`. **A session rate limit killed eleven verifiers mid-run, two holding uncommitted source mutations**; because every verification tree was detached at the pin and never merged, `main` was never at risk — all 41 were reset and the eleven re-run from clean, so no verdict rests on a partial transcript. **The verifiers overturned or reshaped the finders on more than thirty points.** Seven candidates were rejected outright, four of them because the thing the finder said was missing already existed: #601's closing comment records the test-tree exclusion (a **third** re-finding), a dead-stock parity test exists in a file named after the search surface rather than the module, an order-code regex is a decision recorded in four places, and a printer notice is never shown to the users the claim described. Two more were rejected on measurement or on reading — a kit-graph N+1 that costs ≤71 ms at 20× throttle, and a name collision whose "completeness claim" turned out to describe separate *walks*, which the second function is not. **Three findings' own evidence had to be rebuilt because the finder's did not work**: `aboveWindow`'s proposed mutation leaves its test green (no fixture pins the boundary), "add 'price' as a keyword" repairs three OCR cases and introduces a fourth, and the drop-target churn measured **0 registrations during a drag**, disproving its mechanism outright and reclassifying it from performance to documentation. Conversely several findings **grew**: #1026 from three examples (one of them wrong) to the shipped default layout plus eleven mis-reporting presets; #1013's wiki sentence from one false clause to three; #1057 from a speculated lost release to an observed discarded focus-loss event and a real wrong drop; and #1022 gained a third route needing only ordinary permissions. **An exact-rational referee settled #1036's direction** — over 18 800 representable cases the pure seam is wrong in 3.83 % and the SQL in 0 % — and the consequence proved to be confined to the bridge, where a file asserts the two "cannot part company" because of the very test that cannot see it. Measurements were taken in real browsers throughout: 4× CPU throttling for the device standard, a 3 070× mutation delta isolating a main-thread freeze, and printed-barcode geometry read from a real print document. **The register's own P2-43 is contradicted** by #1036, and a durable memory note outside the repository was corrected (`noImplicitReturns` is enabled; the note said it was not). Ten carry-over entries added to §11, including four prototype-key stragglers in other phases' scope that were found but deliberately **not** filed unverified. Every scratch test was removed and every mutation reverted before landing; the worktree carried no change outside this document. |
| 2026-09-02 | 8 | Phase 8 run and landed at pinned SHA `59ce7d19`. **123 candidates: 101 confirmed, 22 rejected, 0 unverifiable — and 8 of those rejections were rejected because an existing issue already covers them (#546, #692/#715, #715, #213/#230, #515, #957, #225/#234, #844).** The register carries **130 rows** (103 confirmed, 27 rejected), because seven candidates took different verdicts on different limbs. The confirmed findings became **72 issues (#1065–#1136)** plus **19 comments** on existing issues (#112, #211, #225, #230, #234, #515, #546, #553, #560, #715, #787, #815, #844, #957, #984, #1001, #1002, #1018, #1052) — the rest were folded into a sibling sharing a root cause, including six sweeps (#1073 six dialogs discarding a filled-in draft, #1074 three blur-only editors, #1075 seven item-dialog facet editors, #1079 eight surfaces rendering a failed read as the empty state, #1104 five invisible file-picker tab stops, #1107 four silent live regions). Eleven finder units and **60 verifier passes** ran, each verifier in its own detached worktree at the pin, so that a verifier's deliberate mutations could not corrupt a concurrent run. **Fifteen rows are `data-loss` and six `unusable`** — the largest severity profile of any phase so far. **A session rate limit killed 23 verifiers mid-run**, two of them holding uncommitted source mutations; because every verification tree was detached and never merged, `main` was never at risk, and all 60 trees were reset and the killed passes re-run from clean, so no verdict rests on a partial transcript. Two further incidents were caused by the audit's own tooling and are recorded rather than hidden: a killed verifier had written a Playwright probe into the **primary checkout** (removed), and one verifier tore down its dev server with a command-line filter rather than a port filter, killing 14 `node` processes and taking out some siblings' servers. **A third was found by a link check after filing**: a text substitution that expanded the permalink base had matched the letters `base` inside ordinary words, corrupting 24 places across 20 issue bodies (`database`, `baseline`, `base64`, `codebase`), and four permalinks named a path that does not exist at the pin — every one was repaired and all 97 distinct permalinks now resolve. **The verifiers overturned or reshaped the finders on more than thirty points.** Two candidates were disproved by *counting* what the finder had argued: `ChangeFlash`'s claimed instance reuse gave `onExistingNodes: 0`, and the grouped list's "two forced layouts per open section per frame" measured **59 layouts at eight open sections and 59 at one**, with the reads themselves scaling 4× in count and 1.05× in time. One finder's proposed mutation was **discarded as invalid** after it broke the virtualisers rather than isolating their cost. Two verdicts inverted a premise outright: a stale scroll margin could not arise because the offset is subtracted from a value that already contains it, and the section that was said not to re-render re-rendered **38 times** — so the depless `useLayoutEffect` one candidate called waste is what holds the other correct. **Two findings were raised by verifiers that no finder had named**: `V18-c`, that a component rendering its own `Modal` sits above the unsaved-changes provider — which makes the obvious fix for four other findings a no-op and leaves **two shipped guards as phantoms** (#1072) — and `U5-9b`, that the first "Show more" on a grouped section resets the whole scroller to the top (#1136), the inverse of #1063. **A cross-cutting fact raised the severity of about a dozen findings**: `aria-invalid` has no visual treatment anywhere in the app, so every "but the control marks it invalid" mitigation is inaudible *and* invisible; its site belongs to Phases 5 and 6, both complete, so it is recorded in §11 rather than filed. Measurements were taken in real browsers throughout — 4× CPU throttling as the device standard, a true 320 px phone context that showed the Table view's name, location and category cells painted at **0 px on every mainstream phone width**, and a seeded database worker that showed the Category manager spending **seven round trips per typed character**. Three carry-over entries added to §11. Every scratch test was removed and every mutation reverted before landing; the worktree carried no change outside this document. |
| 2026-09-02 | 9 | Phase 9 run and landed at pinned SHA `f1965c41`. **80 candidates: 67 confirmed, 16 rejected, 0 unverifiable — and 6 of those rejections were rejected because an existing issue already covers them (#546, #236, #241, #228/#962, #233, #924).** The register carries **83 rows**, because three candidates took different verdicts on different limbs (U3-2, U4-3, U7-10). The confirmed findings became **59 issues (#1137–#1195)** plus **4 comments** (#714, #233, #241, and a correcting comment on this phase's own #1144) — the rest were folded into a sibling sharing a root cause and a fix (U1-8 into #1143, U1-10 into #1149, U8-2 into #1188, U8-7b into #1191, U7-10b and U7-new into #1183, U3-8 into #1195). Eight finder units and **62 verifier passes** ran, each verifier in its own detached worktree at the pin. **Four rows were raised by verifiers that no finder had named** — the reorder tab's Enter-discards-a-sum (U1-10), the count sheet's draft mirror (U4-6b), the Recent-activity widget's English action titles (U7-new), and the entity decoder's prototype lookup (U8-7b) — and two of those were found *while disproving the candidate they were sent to check*. **Ten rows are `wrong-data` or worse and two are `unusable`**, the headline being #1145 (an item whose stock all sits away from its home location cannot be sold, written off **or loaned**, reachable from an ordinary delivery received elsewhere) and #1185 (unmounting the Dashboard mid-drag leaves a scroll loop that no click, release or navigation clears — the page cannot be scrolled until the tab is reloaded, proved in a real browser with a shipped default hotkey). **The verifiers overturned or reshaped the finders on more than twenty-five points.** Six candidates were disproved outright on their premise: the Finalise dialog *does* refetch on reopen (call count 2, not 1); the ABC tail rule is a recorded plan decision whose proposed guard **inverts the ranking** under mutation; the maintenance DST limb is sanctioned by the calendar-days seam and reproduces with **no DST at all**; the wake-lock race needs a window measured at **p99 0.3 ms**; the count sheet's per-keystroke derivations measured **28.46 ms against a 28.38 ms baseline** with every named cost removed; and the movement chart's `title` *is* exposed to assistive tech, so the claim had to be re-based onto the absent text equivalent and the sub-day label collisions. **Three findings were re-based onto a different mechanism than the finder gave, and each grew:** U5-5 from a name-fold bypass to a manufacturer-blind duplicate key; U2-6 from a quantity floor to a missing availability gate; and U3-7 from "thousands of round trips" to **500 unbatched commits** — measured in the running app at **14.25 s for a 500-line BOM**, against a batched mutation at 493 ms (28.9×), which also disproved this phase's own #1144 attribution and earned a correcting comment there. **Two measurements contradicted each other until the difference was found**: the purchase-list cost measured in-process attributed 94% to an alias scan, while the sibling path measured in the real app attributed it to disk syncs — the in-process figure has no OPFS, so it was a floor, exactly as its verifier had said. Findings were driven in real browsers throughout: print-media emulation with a generated PDF proving the insurance schedule's exclusion notices never reach paper (#1174, a #163 regression), a CDP accessibility-tree dump, a 4×-throttled production build for the count sheet, a live service worker returning 500/404/connection-refused for the update badge, and a Playwright drive of a held drag through a navigation hotkey. **Two export formats were verified by parse-back**: a C0 control character makes every XLSX unopenable to both expat and openpyxl (#1188), reachable end to end from an ordinary CSV through the shipped import path. Eight carry-over entries added to §11, including a Playwright instrument artefact (`setOffline` does not intercept a service-worker fetch) and a tension in the DST memory note that the note itself does not resolve. Every scratch test was removed and every mutation reverted before landing; the worktree carried no change outside this document. |
| 2026-09-02 | 10 | Phase 10 run and landed at pinned SHA `c36e822d`. **112 candidates: 87 confirmed, 21 rejected, 3 duplicates of existing issues (#808, #955, #226), 1 unverifiable.** The confirmed findings became **74 issues (#1196–#1269)** plus **4 comments** (#922, #955, #1094, #1122) — the rest were folded into a sibling sharing a root cause, including four sweeps (#1196 two quoting defects that must be fixed together, #1201 the gate and its false parity comment, #1263 three symptoms of one un-migrated parser, #1268 five stale scanner claims). Nine finder units and **34 verifier passes** ran, each verifier in its own detached worktree at the pin. **Seven rows are `wrong-data` or worse and three `unusable`** — the headline being #1224 (backgrounding the app kills the scanner for good and strands a continuous batch, which the project's own specification says must re-initialise), #1227 (a failed database read during a scan is entirely silent, and the double-scan gate then suppresses the retry) and #1261 (a price refresh overwrites the supplier part's currency, so a €5 part is summed into a £ total as £5 and lands in an append-only cost snapshot). **The verifiers overturned or reshaped the finders on more than forty points**, and the corrections were often the finding: #1211's cost is the query *shape*, not the 48 subqueries the finder counted (8.5× at two keywords); #1260's performance rationale measured the *opposite* way (the two-step rewrite is 1.23× slower) while its quality half proved worse than claimed; #1217's WCAG argument failed on measurement and the real defect — a bypassed Foundry primitive — is bigger; #1213 widened when `keepPreviousData` was found to *withdraw* real rows; and #1253 was re-based off the deep link onto four ungated Add-item entry points, one of them pinned open by a shipped test. **Seven candidates were disproved on their premise**: `CooldownMap` is called on every continuous scan, the `has:` limb is documented behaviour, the Alerts and Upcoming gates are a recorded #522 decision enforced in the hooks, the QR encoder can receive no non-ASCII byte across 28 adversarial combinations, an out-of-range entity produces the substitution the HTML standard mandates, a `!` assertion is required to compile, and a scanner parity test *does* fail on the drift it names. **One row was raised by a verifier that no finder named** (V38-1: an in-flight decode moves the inventory list to a location the user never chose, after the scanner is closed). **The lead's own dispatch had a coverage gap**: eleven candidates reached no verifier on the first pass and were caught while building the register, then verified before landing — they produced four of the phase's issues, including #1261. **The session exited with five verifiers mid-run**; every one of their trees carried an empty tracked diff, so `main` was never at risk, and all five were resumed from their transcripts rather than re-run. One verifier's cleanup filter matched `*vite.worktree*` across every worktree and killed two siblings' dev servers; both were told and neither had lost work. Findings were driven in real browsers throughout — a CDP accessibility-tree dump proving Chromium drops `aria-selected` from the palette's rows, 4× CPU throttling showing a builder keystroke blocking input for 1.5 s, two production builds measuring a 129 KB eager-graph saving, and a synthetic EXIF-rotated JPEG clearing an image-orientation claim on two engines. Nine carry-over entries added to §11. Every scratch test was removed and every mutation reverted before landing; the worktree carried no change outside this document. |
| 2026-09-02 | 11 | Phase 11 run and landed at pinned SHA `2b48ca70`. **120 candidates: 80 confirmed, 25 rejected, 14 duplicates of existing issues, 1 unverifiable.** Ten finder units produced 118; two more were found during verification — one spin-off while *disproving* a candidate (U4-5b), and one by driving all 56 settings controls in a browser (U11-1). The confirmed findings became **56 issues (#1270–#1325)** plus **13 comments** on existing issues (#219, #242, #496, #581, #585, #738, #830, #839, #842, #849, #1050, #1107, #1164) — the rest were folded into a sibling sharing a root cause and a fix. Every settings control was set, reloaded and read back in a real browser (50 of 51 preference controls clean); permission gating was driven as a low-privilege role across nine screens; and the time-based lanes were pinned to `America/New_York` and `Asia/Tokyo` in child processes. Six rejections were rejected *because* an existing issue already covers them, and three candidates had a limb disproved by measurement rather than argument — the agenda rebuild (1–2% of render cost), the maintenance OFFSET walk (six attacks, no reproduction) and the reminder-wake cache sweep (active-only refetch, ≤2 wakes a day). |
| 2026-09-03 | 12 | Phase 12 run and landed at pinned SHA `41f91908`. **77 candidates: 63 confirmed, 13 rejected, 1 duplicate of an existing issue (#362), 0 unverifiable.** The confirmed findings became **43 issues (#1327–#1369)** plus one correcting comment on #362 — the rest were folded into a sibling sharing a root cause, including six merges (#1333 the search DTO and the spoken sentence, #1334 and #1359 the two read-projection costs, #1336 three boolean parameters across two surfaces, #1339 the driver lifetime and its user-visible truncation, #1347 the two `Allow` locations, #1351 four OpenAPI operation defects, #1352 two undeclared headers, #1353 two CSDL contradictions, #1356 three permission call sites, #1330 the startup hang and the missing-directory throw, #1344 the README tree and its anchors). Eleven finder units and **twenty verifier passes** ran, each verifier in its own detached worktree at the pin. **Five security findings were confirmed and deliberately not filed**, at the maintainer's direction — recorded in §10 with their demonstrations and carried to §11 (see that row). The most serious was found *during verification* of an unrelated candidate and reproduced independently by the lead: `new URL` sits outside the request handler's `try`, so ten unauthenticated malformed request lines exit the process. **The verifiers overturned or reshaped the finders on nineteen points**, and four candidates were rejected because the finder's own demonstration did not reproduce (U7-4's depth budget, U9-2's live trigger, U11-4's claim that mDNS never logs, U11-8's `.tsx` trap, which `type-check:bridge` catches). Three rejections rest on measurement rather than reading: the `clientError` socket "leak" is reaped by Node's connection checker within 30 s; `server.close()` has closed idle keep-alive connections since Node 19; and the rate limiter's cap holds until 5,000 distinct keys/sec against a measured ceiling of 2,745 conn/sec on loopback. Two finder claims were **broadened** by verification — the zero-length `.sqlite` defect is really any `user_version: 0` file including another application's database, and `LocationDto`'s missing `archivedAt` also leaves a retained Home Assistant sensor for ever. Notable corrections recorded for later phases: #362 is now partly stale (`@odata.count` and `@odata.nextLink` **are** emitted); the bridge **cannot** adopt `ITEM_READ_COLUMNS_NO_THUMBNAIL` because the projection is hard-coded in `core.ts` and `search.ts`; and the cross-tree Vitest recipe in §6.2 was unreliable under concurrent load, so verifiers drove the real `serve.mjs` instead. Seven carry-over rows added to §11. |
| 2026-09-03 | 13 | Phase 13 run and landed at pinned SHA `ad513f52`. **95 candidates: 65 confirmed, 28 rejected, 5 duplicates of existing or held findings, 0 unverifiable.** The register carries **98 rows**, because three candidates took different verdicts on different limbs (U4-3, U5-9, U6-3). The confirmed findings became **50 issues (#1370–#1419)** plus one comment on #1335 — nine were folded into a sibling sharing a root cause and a fix (#1399 the two `safeWrite` surfaces, #1386 the example and README vocabularies, #1413 the two locations-tool defects, #1416 two schemas that misdescribe their own enforcement, #1403 the two systemd sandbox comments, #1415 the two systemd unit comments, #1412 a three-item parity sweep following #775 and #901, #1419 three stale calendar claims), and **four confirmed security findings were deliberately not filed**, following Phase 12's precedent — recorded in §10 with their demonstrations and carried to §11. Ten finder units and twenty-five verifier passes ran. **The most serious finding needs no credential at all**: an IPv4-mapped IPv6 literal in a webhook URL is classified publicly routable, because `new URL()` always serialises the hex form the guard's dotted-only fold never matches — proved end to end by a delivery arriving on a loopback listener under the shipped default policy, with the existing test green because it feeds `classifyPrivateAddress` a string no caller can produce. **One process failure to record: #1389 (the unbounded MQTT inbound buffer) is an equivalent trust-boundary denial of service and was filed publicly before the maintainer was consulted** — flagged in §11 for a decision. **The verifiers overturned or reshaped the finders on thirty-one points.** Nine candidates were rejected because the finder's own demonstration did not reproduce or its consequence collapsed under measurement: the webhook example's loopback caveat is documented in six places including the refusal itself; the offset walk over untiebroken orders returned every row exactly once at 20,000 rows with a forced sorter merge; `mqtts://` **does** reach a self-signed broker with `NODE_EXTRA_CA_CERTS` set, and the error names that remedy; the calendar builds 20,000 events in 753 ms with the string assembly costing 49 ms; the event pipeline's per-row reads cost 2 ms against 65 ms of hydration it already pays; a real 2,000-entity `/api/states` is 1.05 MiB, two orders of magnitude below the OOM threshold; the negative-scale reading **is** explained in a `role="alert"` with Apply disabled; the mDNS amplification is 1:1 on the wire because the reply always goes to the group; and 500,000 structured hostile mDNS packets produced zero throws. Four rejections rest on a recorded decision the finder missed — most sharply #416's commit, which added three units to the app's weight table and **touched the bridge's in the same change** to reword the rationale for not adding them. Notable corrections carried forward: the finder's warranty route into the iCal year-padding defect is wrong (the 10-year horizon excludes 9999) and loans are the real route; the MCP server is **no longer read-only by design**; `os.networkInterfaces()` **throws** rather than degrading under the shipped systemd unit; and Phase 12's `bridge/src/ical/feed.ts` carry-over was taken up, confirmed and filed as #1418. Six carry-over rows added to §11. |
| 2026-09-03 | 14 | Phase 14 run and landed at pinned SHA `32c3a672`. 52 candidates: 41 confirmed, 8 rejected, 3 duplicates of existing issues, 0 unverifiable. The 41 confirmed became **31 issues (#1420–#1450)** plus three comments on existing issues (#578, #1264, #1111) — nine were folded into a sibling sharing a root cause, one (C9) was confirmed as fact and left unfiled below the bar, and one (C3) was **fixed rather than filed at the maintainer's direction**. Five finder units and sixteen verifier passes ran. **The phase's decisive move was installing Home Assistant 2025.1.4 and `pytest-homeassistant-custom-component` into the scratchpad**, which turned the whole HA half from §3.4 unverifiable into demonstrated — the config flow, coordinator, entities, services and the real conversation agent were all driven against a real bridge, and the extension was driven in real Edge from the shipped `dist`. **C3 was exploitable and the audit stopped, as §5.3 requires.** The maintainer directed a fix (the software is not yet released publicly), so `bridge_id.py` was changed in commit `dd2399d6` — the audit's no-fixing rule set aside for that one finding, on the maintainer's instruction, and recorded here as the exception it is. The residual is in §11. **Verification overturned the finders on eleven points**, three of them reversals rather than corrections: A4's filter is not *ignored* but exclusionary, which makes the defect wider and inverts its mechanism; A5/B3's unhandled rejection does not exist in this browser at all; and D5's "every card points at a dead set" is false, because entity ids are reclaimed byte-identically — what is actually lost is the device's name, area, renames, icons and device id. Two candidates grew: C3 from "needs the bridge id" to "needs a port number", and D6 from a docstring nit into a functional defect where the expiry window never reaches the bridge at all. One finding (LEAD-1) was found during verification, reported independently by four verifiers, and confirmed by the lead against Home Assistant's own source. Seven carry-over entries added to §11, including the fix's residual and the absence of any Python test harness. |
| 2026-09-04 | 15 | Phase 15 run and landed at pinned SHA `4b2bd561`. 74 candidates: 43 confirmed, 27 rejected, 4 duplicates of existing issues, 0 unverifiable. The 43 confirmed became **28 issues (#1451–#1478)** plus 6 comments on existing issues — fifteen were merged into a sibling sharing a fix site, following the verifiers' own recommendations. Nine finder units and thirteen verifier passes ran. **The phase's rejections are its most valuable output, and they cost more work than the confirmations**: 27 of 74, nearly all disproved by evidence rather than judgement. The pattern that recurs is a finder reasoning correctly to a wrong verdict. `bug_report.yml`'s missing extension option looked certain until `gh issue list --search` showed **0 of 1000** issues have ever used the form; `CONTRIBUTING.md`'s Node claim collapsed when **no `.npmrc` exists**, so `engines` is advisory and CI proves it by passing on Node 22.18.0; the PR template's missing attribution trailer **inverted** on inspection, because the issue templates' matching absence is correct — pre-populating it for a human reporter would be a false disclosure. Six of the eleven `docs/dev` candidates fell to one distinction: `SettingsScreen.tsx` was deleted **four days after** the sweep that recorded its line count, so the record was true when written. **Verifiers corrected finders on substance repeatedly.** M-3 was confirmed only after striking out its supporting argument — the search grammar's lack of a `location:` term is a recorded decision with a drift test. S-1's finding moved off the bare singular verb, which is taste, onto the app wizard's false claim that the bridge handles plurals and the system locations; "is in **In Transit**" is the proof, and **three units reached that claim independently**. K-2 was reshaped away from "the premise is false", which is not provable from inside the repository, onto the harness-independent contradiction with `CLAUDE.md`. O-4's sweep was re-run and **found six failures rather than one**, five of them the verifier's own bad test values; the conclusion survived. P-1's stated consequence was **disproved** — a session reading the task entry meets "✅ Done" on the first line — and the finding re-based onto the sibling plan that *was* kept current. **The decisive win was settling one question from upstream source**: Home Assistant's `async_recognize_sentence_trigger` `break`s on the second match and does not rank, at four version tags, which turned the Phase 14 carry-over at `:2846` into two `wrong-data` issues (#1457, #1458) and showed the setup wizard prescribes the failing phrasing **inside its own troubleshooting step**. Two findings were discovered by verifiers rather than finders: M-9b (`adjust_gauge` promises the updated item and returns one with no gauge) surfaced while *disproving* M-9, and D-11(c) (`README.md:355` calls a ⛔ SUPERSEDED document "the absolute source of truth") was handed to the lead and verified personally. Three guards were shown to hold less than they claim, each mutation-proved: `agent-guide-parity.test.ts` catches none of its three advertised drifts for the two rules `AGENTS.md` calls most urgent (deleting an `adapted` section is green, deleting a `verbatim` one is red); `openapi.ts`'s "every list in this document is generated from its source" is false for three vocabularies, demonstrated by a ninth sort field passing type-check and 1191 bridge tests while the served document went stale; and `docs-todo-status.test.ts`'s `collect()` **never opens `docs/`**, which is why `docs/modular-ui-plan.md` still reads "phases not yet started" though all nine shipped the day it was written and seven production code comments cite it as live design reference. The lead verified M-1, D-11(c), R-6 and R-7 personally and spot-checked one rejection per unit. One brief this session issued was itself wrong: it told the `docs/dev` finder that `schemaVersion` must not be bumped for a schema change, and the finder checked, found `app-version.ts:16-27` says the opposite, and reported `releases.md` correct. |
| 2026-09-04 | 16 | Phase 16 run and landed at pinned SHA `48554ede`. **389 candidates: 282 confirmed, 76 rejected, 9 verified negative, 21 duplicates of existing issues, 0 unverifiable, 1 referred to Phase 17.** The confirmed findings became **72 issues (#1479–#1550)** plus 8 comments on existing issues (#263, #264, #269, #1073, #1289, #1293, #1480, #1518) — claims were grouped one issue per page, following the verifiers' own recommendations, so a page with six wrong claims is one issue listing six. Fourteen finder units and thirteen verifier passes ran, grouped by the wiki sidebar's sections; the coverage unit was verified by the lead. Every screenshot claim was checked by regenerating all 44 committed images from the committed `scripts/wiki-screenshots.mjs` against a dev server at the pinned commit and comparing pixel-wise, which separated genuine staleness from seeded-data noise. Notable: the broken-link finding (#1479) was proved against the **live published wiki** with `curl`, not just the source, because `npm run wiki:check` matches `[[…]]` per line and skips an anchor-only target — so 33 links pass a green check and render dead. Three findings reached beyond the wiki into code comments and one docstring that restate the same false claim (#1534, #1535, #1545). Two existing issues were found to have gone stale themselves and were corrected by comment rather than duplicated (#263's two counts, #269's evidence), and #264 appears already fixed by `02adb5d2`. Four carry-over entries added to §11. **Correction to this plan:** §9's Phase 16 prompt says "all 99 pages under `docs/wiki/`"; there are 98 `.md` files (96 content pages plus `_Sidebar` and `_Footer`). `ls docs/wiki` returns 99 entries because `images/` is a directory. Counts here are taken from the verifiers' verdict tables, one row per candidate. |
| 2026-09-04 | 16 | Phase 16 follow-up, at the same pinned SHA. A driver agent dispatched during the phase returned after it had landed, with two candidates no verifier had reached. Both were re-checked against the code and confirmed, taking the phase to **391 candidates, 284 confirmed and 73 issues (#1479–#1551)**. The new issue is #1551: the role editor's **Everything under &lt;area&gt;** shortcut is documented as covering actions added by a future release, and `toggleSubject` only ever writes today's keys as an explicit set — `wrong-data`, and silent until a later version adds an action the role then lacks. The second was folded into #1531 as a comment: it corrects that issue's suggested direction, since the app *does* explain the **untracked** case (`Convert to Bulk to loan out…`) and explains the **consumable gauge** case nowhere. |
| 2026-09-04 | 17 | Phase 17 run and landed at pinned SHA `3864f22d`. **43 candidates: 33 confirmed, 9 rejected, 1 duplicate of an existing issue (#556), 0 unverifiable.** The register carries **44 rows**, because one candidate (P17-23) took different verdicts on different limbs. Thirteen finder sweeps raised 42; the forty-third was found during verification, when a verifier disproved three quarters of another candidate's largest cluster and the mis-typing behind it turned out to be a finding of its own (P17-43). The 33 confirmed became **20 issues (#1552–#1571)** plus **5 comments** on existing issues (#941, #956, #228, #1268, #783) — the rest were folded into a sibling sharing a root cause, most of them into #1569, which collects six false statements about the codebase into one edit. **One confirmed finding was deliberately not filed** (P17-6, `unusable`): a snapshot tombstone naming a built-in role id strands every device on a shared sync permanently, the tombstone-TTL clone republishes it, and a brand-new device cannot pair. It is recorded in §10 with its demonstration and carried to §11 as a disclosure decision for the maintainer, following the precedent Phases 12, 13 and 14 set. One **limb** of a confirmed finding (P17-9's root canvas, visible only as Safari overscroll) also went to §11 as unverifiable here, while its certain limb was filed. The rejections carry the phase's other half: six of the nine turned on a consequence that does not follow rather than on a fact that is wrong, and the two largest — the "no seam has a source-scanning guard" sweep and the 170-file fixture sweep — were rejected after their supporting claims were driven and found inert, the latter being the fourth re-finding of a mechanism the register has already rejected three times. The `waitFor` ceiling candidate (P17-33) was settled by measurement rather than argument: the mechanism reproduces, and raising the ceiling changes the failure rate not at all. Also in this change: §7's price-rendering grep was corrected (it found 2 of 8 real hits), §11's location for the `SettingRow` docstring claim was corrected and one of its clauses dropped, three §11 carry-overs were resolved, §12 gained a Phase 17 re-run recording an `npm audit` regression from 0 to 2 high and a clean whole-graph licence sweep, and one §11 row that had been joined onto the row above it — and therefore did not render — was split, with no wording changed. |
