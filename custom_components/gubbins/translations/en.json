{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "title": "Gubbins inventory bridge",
        "description": "Connect to your local Gubbins bridge. The token is an API token minted in the Gubbins app (Users → an account → API tokens); it is stored securely by Home Assistant and never written to YAML.",
        "data": {
          "host": "Host",
          "port": "Port",
          "token": "Access token"
        }
      },
      "zeroconf_confirm": {
        "title": "Gubbins inventory bridge",
        "description": "Discovered a Gubbins bridge at {host}:{port}. Enter its access token (an API token minted in the Gubbins app (Users → an account → API tokens)) to finish. The token is never advertised over the network — it is stored securely by Home Assistant.",
        "data": {
          "token": "Access token"
        }
      },
      "reauth_confirm": {
        "title": "Gubbins inventory bridge",
        "description": "The bridge at {host}:{port} no longer accepts the stored token — it was probably revoked or rotated. Mint a new API token in the Gubbins app (Users → an account → API tokens) and enter it here to reconnect.",
        "data": {
          "token": "Access token"
        }
      },
      "reconfigure": {
        "title": "Gubbins inventory bridge",
        "description": "Update where the bridge lives. Re-enter the access token — Home Assistant never displays a stored token, so it cannot be pre-filled here.",
        "data": {
          "host": "Host",
          "port": "Port",
          "token": "Access token"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not reach the bridge. Check the host and port, and that the bridge is running.",
      "invalid_auth": "The bridge did not recognise the token. It may have been mistyped, or revoked in the Gubbins app."
    },
    "abort": {
      "already_configured": "This Gubbins bridge is already configured.",
      "already_in_progress": "This Gubbins bridge was found on the network and is already waiting to be set up. Finish that one instead: select Configure on its card under Settings → Devices & services.",
      "bridge_moved": "This Gubbins bridge is already set up — its existing entry now points at the address it answered on, so its entities, their history and any automations using them carry on unchanged.",
      "reauth_successful": "Reconnected to the Gubbins bridge with the new token.",
      "reconfigure_successful": "The Gubbins bridge details were updated."
    }
  },
  "services": {
    "search": {
      "name": "Search inventory",
      "description": "Search the Gubbins inventory (read-only) and return matching items, along with the resolved location ids for each match.",
      "fields": {
        "query": {
          "name": "Query",
          "description": "Free text, or the power-user syntax (e.g. cap:voltage>3.3)."
        },
        "limit": {
          "name": "Limit",
          "description": "Maximum number of items to return (1–25)."
        }
      }
    },
    "adjust_quantity": {
      "name": "Adjust quantity (stock in / out)",
      "description": "Change a discrete item's stock by a signed amount. This moves a number only — to record that someone has borrowed the item, use Check out instead. Requires the bridge to be started with GUBBINS_BRIDGE_ALLOW_WRITES=on; the change syncs back to the app conflict-free.",
      "fields": {
        "item_id": {
          "name": "Item id",
          "description": "The Gubbins record id of the item (find it via the search service or the API)."
        },
        "delta": {
          "name": "Delta",
          "description": "Signed change: negative for stock going out, positive for stock coming in."
        },
        "note": {
          "name": "Note",
          "description": "Optional note recorded in the item's activity log."
        },
        "idempotency_key": {
          "name": "Idempotency key",
          "description": "Optional. Names this attempt so repeating it is safe. Each of these changes is relative — a delta, a move, a loan — so calling again normally applies it a second time. Generate one value per intended change and reuse it if you repeat the call: the bridge then replies with the first attempt's result instead of changing anything again. Most useful on a large inventory, where a write can take longer than the call waits for it."
        }
      }
    },
    "adjust_gauge": {
      "name": "Adjust gauge (use / refill a consumable)",
      "description": "Change how much is in a measured consumable — grams of filament, millilitres of resin — by a signed amount. Requires the bridge to be started with GUBBINS_BRIDGE_ALLOW_WRITES=on; the change syncs back to the app conflict-free.",
      "fields": {
        "item_id": {
          "name": "Item id",
          "description": "The Gubbins record id of the consumable (find it via the search service or the API)."
        },
        "delta": {
          "name": "Delta",
          "description": "Signed change in the item's own unit, and may be fractional: negative for an amount used, positive for a refill."
        },
        "note": {
          "name": "Note",
          "description": "Optional note recorded in the item's activity log."
        },
        "idempotency_key": {
          "name": "Idempotency key",
          "description": "Optional. Names this attempt so repeating it is safe. Each of these changes is relative — a delta, a move, a loan — so calling again normally applies it a second time. Generate one value per intended change and reuse it if you repeat the call: the bridge then replies with the first attempt's result instead of changing anything again. Most useful on a large inventory, where a write can take longer than the call waits for it."
        }
      }
    },
    "check_out": {
      "name": "Check out (lend an item)",
      "description": "Lend an item to a person, a project or a place, optionally with a due date. Unlike adjusting a quantity this records who has it, so it shows as on loan and becomes overdue if the due date passes. Requires the bridge to be started with GUBBINS_BRIDGE_ALLOW_WRITES=on, and an account with permission to lend; the change syncs back to the app conflict-free.",
      "fields": {
        "item_id": {
          "name": "Item id",
          "description": "The Gubbins record id of the item to lend (find it via the search service or the API)."
        },
        "contact_name": {
          "name": "Borrower name",
          "description": "Lend to a person by name. If nobody of that name exists yet, they are added — exactly as lending in the app would. Supply only one borrower."
        },
        "contact_id": {
          "name": "Contact id",
          "description": "Lend to an existing contact by id, instead of by name. Supply only one borrower."
        },
        "project_id": {
          "name": "Project id",
          "description": "Lend to a project rather than a person. Supply only one borrower."
        },
        "location_id": {
          "name": "Borrowing location id",
          "description": "Lend to a place, such as a van. This is where the item has gone, not where it came from. Supply only one borrower."
        },
        "quantity": {
          "name": "Quantity",
          "description": "How many units to lend. Defaults to 1; a serialised item always goes out as one."
        },
        "due_date": {
          "name": "Due date",
          "description": "The day it should come back. It only counts as overdue once that day has ended. Leave empty for an open-ended loan."
        },
        "from_location_id": {
          "name": "Lend from location id",
          "description": "Take the units from this location; the return puts them back there. Defaults to the item's own location."
        },
        "note": {
          "name": "Note",
          "description": "Optional note recorded on the loan and in the item's activity log."
        },
        "idempotency_key": {
          "name": "Idempotency key",
          "description": "Optional. Names this attempt so repeating it is safe. Each of these changes is relative — a delta, a move, a loan — so calling again normally applies it a second time. Generate one value per intended change and reuse it if you repeat the call: the bridge then replies with the first attempt's result instead of changing anything again. Most useful on a large inventory, where a write can take longer than the call waits for it."
        }
      }
    },
    "check_in": {
      "name": "Check in (return a lent item)",
      "description": "Return a lent item, putting the stock back where it came from and closing the loan. Requires the bridge to be started with GUBBINS_BRIDGE_ALLOW_WRITES=on, and an account with permission to lend; the change syncs back to the app conflict-free.",
      "fields": {
        "item_id": {
          "name": "Item id",
          "description": "The Gubbins record id of the item coming back (find it via the search service or the API)."
        },
        "checkout_id": {
          "name": "Loan id",
          "description": "Which loan to close. Only needed when the item is out on more than one loan at once — otherwise the single open loan is used."
        },
        "note": {
          "name": "Note",
          "description": "Optional note recorded on the return, e.g. what state it came back in."
        },
        "idempotency_key": {
          "name": "Idempotency key",
          "description": "Optional. Names this attempt so repeating it is safe. Each of these changes is relative — a delta, a move, a loan — so calling again normally applies it a second time. Generate one value per intended change and reuse it if you repeat the call: the bridge then replies with the first attempt's result instead of changing anything again. Most useful on a large inventory, where a write can take longer than the call waits for it."
        }
      }
    },
    "transfer_stock": {
      "name": "Transfer stock (move between locations)",
      "description": "Move some of a counted item from one location to another. The item's total doesn't change — only where it is. Measured consumables can't be moved this way: a gauge tracks one body of material, so there is nothing to split. Requires the bridge to be started with GUBBINS_BRIDGE_ALLOW_WRITES=on; the change syncs back to the app conflict-free.",
      "fields": {
        "item_id": {
          "name": "Item id",
          "description": "The Gubbins record id of the item to move (find it via the search service or the API)."
        },
        "from_location_id": {
          "name": "From location id",
          "description": "The location the units are moving out of. There must be enough there — the whole amount moves or none of it does."
        },
        "to_location_id": {
          "name": "To location id",
          "description": "The location the units are moving into."
        },
        "quantity": {
          "name": "Quantity",
          "description": "How many units to move."
        },
        "idempotency_key": {
          "name": "Idempotency key",
          "description": "Optional. Names this attempt so repeating it is safe. Each of these changes is relative — a delta, a move, a loan — so calling again normally applies it a second time. Generate one value per intended change and reuse it if you repeat the call: the bridge then replies with the first attempt's result instead of changing anything again. Most useful on a large inventory, where a write can take longer than the call waits for it."
        }
      }
    }
  }
}
